You are the shared explorer for a review cluster. Read ONE file, in full,
with a single read: {{WT}}/internal/modelspec/parse.go

Then reply with:
1. One sentence on what the file does.
2. The exact line: NONCE={{NONCE}}

Rules: read-only, exactly one tool call, nothing else. When done, stop.
