Answer STRICTLY from memory of this conversation. Do NOT use any tool — no
file reads, no searches. If you don't remember something, say NOT-IN-CONTEXT
rather than guessing or re-reading.

1. What is the value of NONCE?
2. Quote the exact Go signature of the Parse function in parse.go.
3. List the fields (name and type) of the Spec struct in modelspec.go.
4. In one sentence: what does LoadRegistry in load.go return when the yaml
   file does not exist?
