You are the shared explorer for a review cluster. Your conversation will be
forked into several reviewer sessions that inherit everything you read here,
so be thorough: what you load into context now is all they get.

Read EVERY file in {{WT}}/internal/modelspec/ — production code, tests, and
template.yaml. Read the files in full, do not skim or use partial reads.

After reading, reply with:
1. A ~300-word summary of the module: purpose, public API, invariants,
   error-handling strategy, and how the pieces (parse/load/registry/template)
   fit together.
2. The exact line: NONCE={{NONCE}}
   (remember this nonce; a later session will be asked for it).

Rules: read-only — do not create, edit, or write any file. Do not explore
anything outside internal/modelspec/. When done, stop and wait.
