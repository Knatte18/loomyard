{"in": 1676, "cache_cr": 74694, "cache_rd": 54492, "out": 25204, "turns": 2, "compute": 101574, "tool_calls": 0, "models": ["claude-sonnet-5"], "found_marker": true}
Findings from re-reviewing the inherited `internal/modelspec/` context (no files re-read):

1. [registry.go, `Resolve`] `Resolve` branches only on `s.Alias == ""`; a zero-value or malformed `Spec` (e.g. `Spec{}`, or `Spec{Engine:"", Model:""}`) falls into the escape-form path and returns a "successful" `Resolved{Engine:"", Model:"", Params:{}}` with a nil error instead of failing loud — the one behavior this package's whole design philosophy (Parse fails loud on every malformed input) explicitly promises. — HIGH

2. [registry.go, `Resolve`] Alias-form resolution never re-validates the looked-up `Entry` against `knownEngines` or checks `Entry.Model != ""`; a `Registry` built by any means other than `LoadRegistry` (e.g. a literal in calling code or a test) can carry an unknown engine or empty model and `Resolve` will pass it through silently, unlike escape-form specs which are always engine-checked by `Parse`. — MED

3. [registry.go, `Resolve`] Shape detection ignores `Engine`/`Model` entirely once `Alias != ""`; a `Spec` with both `Alias` and `Engine`/`Model` set (violates the documented "exactly one shape" invariant but is legal to construct since `Spec`'s fields are all exported) silently drops the Engine/Model data rather than erroring. — MED

4. [load.go, `validateAlias`] `validateCharset` returns nil for an empty string (its range loop never executes), and `validateAlias` never explicitly checks `alias == ""` before calling it — an empty-string YAML key (`"": {engine: claude, model: x}`) is silently accepted into the registry instead of being rejected. — MED

5. [load.go, doc comment vs. code] The doc comment states "an alias key must match `[a-z0-9-]+`" (one-or-more), but per finding 4 the implementation actually accepts zero-length aliases — code contradicts its own documented contract. — MED

6. [parse.go, `Parse`/`parseBracket`] A spec with multiple bracket groups (e.g. `"sonnet[a=b][c=d]"`) is only rejected incidentally, via a misleading "invalid character '['" error from param-value charset validation, rather than a clear "only one bracket allowed" error. — LOW

7. [parse.go, `validateCharset`] The reported error position (`i` from `range s`) is a byte offset, not a rune index — for any future non-ASCII input this would misreport the visual character position (currently masked because the allowed charsets are ASCII-only, so failures usually occur at the first non-ASCII byte anyway). — LOW

8. [registry.go vs. template.yaml] `builtins()`'s engine/model values (`claude`/`sonnet` etc.) are duplicated by hand in `template.yaml` with no shared constant or cross-check; `template_test.go` only verifies the template loads with the *expected* hardcoded values, it doesn't assert those values match `builtins()`, so the two can drift silently if one is edited without the other. — MED

9. [leaf_enforcement_test.go, `isStdlib` heuristic] "No dot in the first path segment" is used to classify an import as stdlib; a hypothetical internal/vendored module path with no dot in its first segment would be misclassified as stdlib and slip past the allowlist undetected. — LOW

10. [parse_test.go] No test case covers the multiple-bracket-groups input (finding 6), so that rejection path's (misleading) error message is unverified. — LOW (test gap)

11. [parse.go / test suite] No Go fuzz test (`FuzzParse`) exists despite `Parse` being a hand-rolled string/grammar parser over arbitrary input; only curated table-driven cases are covered, leaving index-arithmetic edge cases (e.g. adjacent `:` and `[`, boundary slicing) unfuzzed. — MED (test gap)

12. [registry_test.go] No test exercises `Resolve` against a directly-constructed `Registry` containing an invalid `Entry` (unknown engine, empty model), so finding 2's silent-passthrough behavior is completely unverified either way. — LOW (test gap)

13. [registry_test.go / modelspec.go] No test exercises `Resolve` with a zero-value `Spec{}` or a `Spec` with both alias and engine/model set, so findings 1 and 3 have no regression coverage. — MED (test gap, given finding 1 is HIGH)

14. [load_test.go, "unknown entry field" case] The assertion checks only for the generic substring `"field"` in the error, which depends on yaml.v3's internal wording — a dependency bump that rephrases the error would silently pass a test that no longer verifies the right thing without anyone noticing the coupling. — LOW

15. [load.go, `LoadRegistry`] The non-`ErrNotExist` read-failure branch (wrapping arbitrary `os.ReadFile` errors, e.g. the config path being a directory, permission-denied) is implemented but has no test exercising it. — LOW (test gap)

16. [load.go, `LoadRegistry`] Hand-authored YAML with a duplicate top-level alias key (e.g. two `sonnet:` blocks — an easy copy/paste mistake in an operator-owned file) is not tested; yaml.v3's duplicate-key behavior (silent last-wins vs. error) is relied upon implicitly rather than verified. — LOW (test gap)

17. [registry.go, `builtins`] Built-in aliases carry zero param defaults — the documented "medium/high effort" defaults exist only in `template.yaml`. If `models.yaml` is ever absent, deleted, or hand-edited to remove `defaults:`, `Resolved.Params` silently becomes `{}` for e.g. `opus`/`fable` with no signal that the intended effort default was lost — the correctness of operator-visible defaults depends entirely on an out-of-band seed file staying in sync with intent, with no enforcement. — LOW/MED (design risk)
