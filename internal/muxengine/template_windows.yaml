psmux: ${env:LYX_MUX_PSMUX:-psmux}  # path to the psmux executable; set LYX_MUX_PSMUX to pin an explicit path
pwsh: ${env:LYX_MUX_PWSH:-pwsh}  # path to the powershell executable used inside panes; set LYX_MUX_PWSH to pin an explicit path (e.g. if the PATH pwsh is a broken WindowsApps alias stub)
width: 220  # psmux window width
height: 50  # psmux window height
collapsed_strip_rows: 3  # height a shrink:true ancestor collapses to once a descendant is present
top_band_rows: 1  # fixed height reserved for each anchor:top strand
min_full_rows: 3  # floor height the clamp rule tries to preserve for a full pane
strand_name: '<ROLE>:<ROUND>:<SHORT_GUID>'  # template for a strand's display name; unfilled tokens resolve to ""
