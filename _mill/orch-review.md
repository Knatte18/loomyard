# Orchestrator review — webster-told-geometry (T7)

Run in automode (every "[auto-pick]" answer in the Q&A log is the discussion agent's own call). Reviewed against `manifest/designs/producers-standalone.md`'s T7 brief (wave 3) and T8's brief (wave 4, since this discussion pulls several items forward from it), and against the worktree's overall mandate: deliver standalone-producers.

Spot-checked every load-bearing claim against current source — **all confirmed exact**, no drift found:

- Every cited line number in `internal/websterengine/state.go` (41/48/56/64), `render.go` (139/149/152/167/173/174/183/201/230/236/237), `runlevel.go` (109/470/486/530/721/724/728/832), `beginbatch.go` (78/198), `recordbatch.go` (51/102/126/129/133), `recoverbatch.go` (61/156/182), and `internal/webstercli/run.go` (62/71/85) — all check out to the character.
- `internal/hubgeom/doc.go` reads exactly as quoted: it already names `WebsterGeometry` as T7's addition and states standalone CLIs don't call `hubgeom` — the discussion's justification for a separate `internal/standalonegeom` package rests on this sentence and it's real.
- `internal/buildinfo/leaf_enforcement_test.go` and `internal/standalonestate/leaf_enforcement_test.go` both carry the claimed empty-allowlist `TestLeafInvariant_AllowlistOnly`.
- `internal/batcher/config.go:35`'s `configengine.Load(baseDir, moduleName, ...)` and its "not initialized" rewrap at lines 40-41 are exactly as described.
- `internal/webstercli/cli.go:181-182` already constructs `reedengine.New(reedCfg, hubgeom.ReedGeometry(layout))` in hub mode — confirming `standalonegeom`'s reed-geometry builder is a genuine, load-bearing need for this task's own standalone entry (Webster forks shuttle runs, shuttle drives reed), not a speculative addition.

## Scope verdict: correct, and the strongest catch yet of a design-doc scheduling error

The core lift (websterengine's `*lyxcwd.Location` surface → told strings, `websterengine.Geometry`, `hubgeom.WebsterGeometry`, the standalone CLI entry with `--stencils-dir`/`--target-dir`/`--plan-dir`) matches T7's brief precisely, including the parts of T7's own brief that explicitly anticipate reuse from T5 (`internal/preflight`/`buildinfo`/`standalonestate`).

**The standout: three items T7 pulls forward from T8's brief, all independently verified as necessary rather than presumed.**

1. **`internal/standalonegeom`.** T8's design brief (`producers-standalone.md:519-536`) already *pins* the exact standalone-mode table for reed's `Geometry` (SocketKey/SessionName/AnchorPath/LogsDir/RepoName/HubPath) that T8 will need a wave later. T7 needs the identical builder *now*, because Webster's own standalone entry forks shuttle runs, and shuttle drives reed (`internal/webstercli/cli.go:181-182` proves the hub-mode call chain exists today). Unlike T3's `hubgeom` — which invented a shared package the design doc never mentioned, ahead of any confirmed T6/T7 need — this is a package whose *exact contents* the design doc already specifies for T8, consumed early by a task that provably needs the same mechanism first. This is about as well-grounded as a "pull-forward" decision gets.
2. **The `CONSTRAINTS.md` rewords** (Stencil Ownership's read-location and seed-pass bullets, Durable-vs-Ephemeral clarification). The design doc assigns these to T8 (`producers-standalone.md:590-598`) on the stated assumption that T8 is "the first task to ship a told stencils directory and a `<state>` tree." That assumption is simply false given the wave order: T7's own brief (line 435) already commits to delivering "Webster's own standalone entry... not deferred to a follow-up," which ships an identical told stencils directory and `<state>` tree one wave *before* T8. The discussion catches this, states the contradiction plainly, and lands the rewords here instead — worded generally rather than naming burler/perch, so T8 confirms rather than rewrites. This is the same class of catch T5 made for `buildinfo.StencilMode()` (a real internal design-doc inconsistency, not a stale line number), and it's resolved the same way: transparently, with the deviation recorded in the commit rather than silently propagated.
3. **The `batcher` Config Strictness move.** Confirmed this is *not* new scope: `CONSTRAINTS.md:510-511` already carries this exact item as "a watch item for T7/T10," naming T7 by task ID and stating the exact trigger condition ("if a standalone Webster reaches `batcher.Active`, `batcher` moves to the degrading side"). T7 is exactly that trigger. The discussion is executing a pre-planned contingency, not inventing one.

**The `fabric-seams-become-engine-owned-interfaces` decision (`RefMatcher`, always-injected `FabricBisector`) is correctly scoped to what T7's own brief already asks for.** T7's brief states the goal as removing `internal/fabricengine` from websterengine's "reason to know about hubs" — the brief's own text only calls out the `StencilsDir` derivations explicitly, but the engine also holds two more `fabricengine` call sites (`NewRefScanner` ×2, `Open` ×1, confirmed at the cited lines) that the stated goal cannot be achieved without also converting. This is the same "enumeration obligation" pattern T4's review flagged approvingly — re-verifying against the tree instead of trusting an undercounting brief — not scope invented beyond the brief's intent. Checked whether this presumes buy-in the way `hubgeom` did: `burlerengine`/`perchengine` (T6) have zero live `fabricengine` call sites (grep confirms only stale comments), so this pattern isn't quietly deciding T6's or T8's architecture the way T3's `hubgeom` was — it's self-contained to websterengine.

**The `worktree-root-token-value-preserved` and `no-pre-run-git-gate-on-the-standalone-target` decisions are both careful, conservative calls that avoid silent behaviour drift.** Preserving `{{.worktree_root}}` = `anchorRoot` rather than converging it on `WorktreeRoot` avoids a prompt-text change nothing in this task would test for; the git-gate decision is directly pinned by T7's own required integration test (`RunCLIIn` from a temp dir outside any git repo must reach the run verb's flag validation, not a resolution error) — gating pre-run would fail that test by construction, so the "no gate" call isn't a preference, it's the only answer consistent with T7's own Verify line.

**Serves the worktree's actual mandate directly.** This is not an incremental refactor with standalone as an eventual side-effect — T7 ships a real, working `lyx webster run --stencils-dir s/ --plan-dir p/` from a directory with no `_lyx` and no git hub, with an explicit manual acceptance run pinned in Testing to prove it. That's the whole point of this worktree, landing a full wave ahead of T8.

**Correctly declines to touch what isn't T7's.** `cmd/lyx/stencilseed.go` and `cmd/lyx/main.go` are explicitly named Out, with the correct reason (T5 already gates them). No `burlerengine`/`perchengine`/`burlercli`/`perchcli` changes. No positional-argument shape. No three-tier `CONSTRAINTS.md` invariant (correctly left to T10). The roadmap is correctly left unmoved — the discussion's own note states wave 3 completes only when T6 also lands.

**The `constructoranchoring_test.go` conflict with T6 is self-flagged, not discovered by this review.** Matches the adjacency I flagged before you spawned T6/T7: both tasks rewrite interleaved rows in the same file. The discussion already commits to "whichever of T6/T7 merges second rebases" — correct, and exactly the mechanical (not logical) conflict expected.

## Minor notes (non-blocking)

- None found. Every cited line, function signature, and cross-reference to `CONSTRAINTS.md`/the design doc verified exactly against current source.

## Bottom line

No scope violations. This is the most consequential of the six discussions reviewed so far (T1–T5, now T7): it identifies a genuine wave-order contradiction in the design doc (T8 assumed to be first to falsify `CONSTRAINTS.md`'s stencil wording, when T7 actually lands that code one wave earlier) and resolves it transparently rather than deferring to a doc that would be visibly wrong for an entire wave. The `standalonegeom` pull-forward is the best-grounded of any "future task's territory" decision seen in this campaign — T8's own brief already pins the exact table being reused. Ready to proceed to planning as written.
