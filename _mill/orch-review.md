# Orchestrator review — standalone-docs-and-invariants (T10)

Run in automode (every "[auto-pick]" answer in the Q&A log is the discussion agent's own call). Reviewed against `manifest/designs/producers-standalone.md`'s Wave 5/T10 brief, `CONSTRAINTS.md`, `docs/overview.md`, `manifest/roadmap.md`, and live source across every converted package — this is the consolidation task, so its accuracy depends on the state of the *whole tree*, not one package.

Spot-checked every load-bearing structural claim — **all confirmed exact**, with two minor narrative inaccuracies noted below (non-blocking, don't affect the actual `CONSTRAINTS.md` content):

- `CONSTRAINTS.md` — 579 lines, 39 `##` sections (counted all), register statement at lines 3–6, `## Cwd Resolution Invariant` at line 8, `## Lyxdirs Single-Declarer Invariant` at line 47, `## Config Strictness Invariant` at line 499 — all exact.
- `docs/overview.md` — 435 lines. `## Cwd Resolution Invariant` at 63, `## Documentation lifecycle` at 86, `## Modules` at 266, `## Execution stack (orchestration layers)` at 320 — all exact.
- `manifest/roadmap.md` — `## Planned` at 8, its single item at 12–14; `## Done` at 105 (off-by-a-few from the discussion's citation because my own wave-4 close-out commit `b349fb1e` landed between when the discussion was drafted and now, shifting a couple of lines — the discussion's `~107/~110/~113/~116` citations are still within 1–2 lines of the real `107/110/113/116`). **Exactly five** links to `designs/producers-standalone.md` in the file today, confirmed by grep — matches the "five dangling links" claim precisely.
- `manifest/designs/producers-standalone.md` — 713 lines. Its own Wave 5/T10 brief (lines ~665–687 in the version this discussion read) matches what this discussion scopes, including the literal "record scout's remaining deviation if T9 was skipped" clause it correctly declines to act on (T9 shipped, commit `8aced4cb`).
- `internal/preflight/predicates.go` — `Wired` (line 47), `HubPresent` (line 73), `ResolveMode` (line 110) all present. `preflight.go`'s `Check` at line 31.
- **The mode-trigger claim is more precise than my own T8 review recorded, and it's right.** I verified directly: `burlercli/cli.go:71`, `perchcli/cli.go:107`, and `webstercli`'s pre-run all call `preflight.ResolveMode(cwd)` — not a bare `HubPresent` call as I summarized in the T8 review. `internal/preflight/doc.go` confirms `ResolveMode` is the CLI-facing entry point that *internally* uses `HubPresent`'s board-level stat (doc.go: "Keying on `HubPresent`'s board-level stat instead (as both `HubPresent` and `ResolveMode` do)"). The discussion's Told-Geometry Invariant content point 4 — "`preflight.ResolveMode` is what a standalone-capable CLI's pre-run consults — never `preflight.Wired`, and never a bare `HubPresent`" — is the accurate statement; my earlier characterization was the imprecise one. Good catch, correctly sourced from a fresh read of the shipped code rather than carried forward from an earlier review.
- **Config Strictness Invariant** (`CONSTRAINTS.md:499-523`) — read in full. Confirmed verbatim: it names T10 as the guard's home ("a set-equality grep guard named as a candidate and T10 named as its home"), and records the full spec this discussion says it will implement "verbatim" — pinned sets (`{shuttleengine, reedengine, perchengine, websterengine, batcher}` / `{fabricengine, boardengine, loomengine}`), the `gitrepoboundary_test.go` pinned-set style reference, the `configengine.Load(`/`LoadOrTemplate(` token pair, the `internal/configengine` exclusion, the `_test.go` skip, and the `allowedSpawners` allowlist requirement. Nothing paraphrased or invented.
- `internal/lyxcwd/docslink_test.go`'s `docsLinkSlug` — read in full. The em-dash/double-hyphen behavior the discussion cites (heading-slug deletion, not replacement, leaves two spaces around a stripped em-dash, which become `--`) is stated almost verbatim in the function's own doc comment.
- `internal/lyxcwd/enforcement_test.go`'s `TestEnforcement_FabricVocabulary` — confirmed it covers a "markdown-style body" subtest, backing the claim that the vocabulary walk covers `.md` prose.
- `cmd/lyx/tierpurity_test.go`'s `allowedSpawners` map — confirmed at lines 28–43 exactly as cited, and `cmd/lyx/gitrepoboundary_test.go` exists with the pinned-set style (`TestGitrepoBoundary_PinnedRunCallSites`, a `diffMethodSets` pinned-vs-actual comparison) the guard is said to mirror.
- **Cwd Resolution Invariant's current text** (`CONSTRAINTS.md:8-46`) — read in full and checked against the reword decision's four sub-points against the actual `resolveCore` implementation in `internal/lyxcwd/lyxcwd.go`: `hubPath := filepath.Dir(workTreeRoot)` is genuinely unconditional (line 115), and `RepoName: strings.TrimSuffix(filepath.Base(hubPath), hubSuffix)` (line 179) genuinely never checks the suffix was present — `TrimSuffix` is a silent no-op when it isn't. Both halves of the reword's central technical claim are exactly true of the shipped code, not an approximation.
- **Enforcement-basis test enumeration** — every one of the six "machine-enforced" test functions exists with the exact cited name: `tokenvocab`/`pattern`/`buildinfo`/`standalonestate`'s `TestLeafInvariant_AllowlistOnly`, `shedengine`'s `TestProducerSeamInvariant_AllowlistOnly`, `treadleengine`'s `TestRunnerSeamInvariant_AllowlistOnly`. And the two negative claims that motivate enumerating rather than gesturing both check out: `shuttleengine/seam_enforcement_test.go` really is `TestProviderSeamImportRule` (provider seam, no `lyxcwd` reference), and `scoutengine/seam_enforcement_test.go` really is `TestEngineSeamInvariant_BannedImports` (a banned list of `output`/cobra/`clihelp`/`*cli`, no `lyxcwd`). The discussion's point that the brief's literal phrasing ("an import-allowlist test per producer package where one exists") would have carried a false implication into `CONSTRAINTS.md` is correct and independently verifiable.

## Two minor, non-blocking narrative inaccuracies

Both are in the discussion's own "Technical context → Verified state of the tree" bullets — background exploration notes, not the `CONSTRAINTS.md` text this task actually ships. Neither propagates into a wrong Decision: the invariant's actual review-obligation package list (Decision, "Enforcement basis" section) correctly includes both `burlerengine` and `perchengine`, and the doc.go audit decision correctly omits neither from its scope.

1. **"`burlerengine`, `perchengine` ... do not mention it [`lyxcwd`] at all" is not accurate.** Both do, in a doc comment: `internal/burlerengine/geometry.go:10` and `internal/perchengine/geometry.go:9` each carry a `*lyxcwd.Location, but is deliberately not imported here` comment — the same category as `planparser`/`reedengine`/`websterengine`/`shuttleengine`/`scoutengine`, which the same bullet correctly buckets as "mention it in comments only." Confirmed by grep across all twelve listed packages; only `configengine`, `tokenvocab`, `pattern`, `buildinfo`, `standalonegeom` genuinely have zero mentions.
2. **The orchestrator/CLI-tier import list is incomplete.** It names `internal/preflight`, `internal/webstercli`, `internal/scoutcli`, `internal/hubgeom` as legitimately importing `lyxcwd`, but omits `internal/burlercli` and `internal/perchcli` — both of which import it directly in `wiring.go` and `cli.go`, exactly like `webstercli`, confirmed by grep. The underlying point the bullet is making ("the invariant is stating a fact about the current tree, not an aspiration") still holds — it's just under-enumerated the same way point 1 is.

Neither affects the Files list, the Verify commands, or any Decision's actual scope. Worth a one-line fix during planning since the discussion elsewhere holds itself to "all figures checked during exploration, not inherited from the design doc" — these two slipped through that check.

One more trivial count slip, same category: **"the twelve entries already there" in `allowedSpawners`** (Technical context, Config Strictness guard section) — the map has **fourteen** entries today (`cmd/lyx/tierpurity_test.go:28-43`, counted). Doesn't change the required action (add one more entry in the same style with a one-line reason).

## The load-bearing judgment calls all hold up

**The Told-Geometry Invariant's placement and four-point content are well-reasoned, not just asserted.** Placing it immediately after Cwd Resolution (generalization of "who may resolve" into "who must be told instead") mirrors the file's own existing adjacency convention (the gitrepo/gitexec pair). Going beyond the brief's literal "three tiers" to also pin the adapter direction (point 3) and the mode trigger (point 4) is justified concretely — point 3 is what keeps `hubgeom`/`standalonegeom` from inverting into a cycle, and point 4 is what stops a future CLI from routing mode selection through `Wired` or a bare `HubPresent` and silently relocating a live hub's state. Both risks are real, checkable properties of the current code, not hypothetical hardening.

**Enumerating the enforcement basis exactly, instead of using the brief's vaguer phrasing, is the correct call and is independently verified above** — the vaguer phrasing really would have been wrong for two of the eight seam/leaf test files in the tree.

**Building the Config Strictness guard here, despite the wiki brief not mentioning it, is justified and appropriately flagged.** `CONSTRAINTS.md` is the authoritative file, it explicitly assigns this task as the guard's home, and it already carries the guard's full specification "so T10 inherits a specification rather than re-deriving one" — deferring would still require editing the same invariant for strictly less value. The discussion states plainly that this is a scope addition beyond the wiki brief rather than quietly absorbing it — the right way to handle an authoritative-file/wiki-brief mismatch.

**Design-doc deletion and the five roadmap link rewrites are correctly framed as mandatory, not a style preference** — `internal/lyxcwd/docslink_test.go` fails the build on any dangling link, confirmed by reading the test, and the "doc survives this task because..." Done-entry clauses become false statements the moment the file is deleted regardless of whether the links are fixed.

**The `doc.go` audit is correctly additive.** Every package the discussion claims already carries told-geometry prose does (spot-checked `perchengine/doc.go`'s "Fabric-blindness and geometry-blindness" section), and the three-package "no `doc.go`, don't create one" exception (`configengine`, `webstercli`, `scoutcli`) is accurate — `burlerengine` does have a `doc.go` (218 lines, extensively documented) but currently lacks an explicit told-geometry/tier sentence, so it correctly falls into "add the sentence where absent" under the general rule rather than needing a special case.

**Scout's "no deviation to record" call is correct and precisely reasoned** — the brief's condition ("if T9 was skipped") is false since T9 landed in `8aced4cb`, and `scoutengine`'s two remaining `lyxcwd` mentions really are just comments (a `doc.go` rationale and a `toolchain.go` machine-global cache-path note), neither of which is anchor geometry.

## Bottom line

No scope violations against the design doc's Wave 5 brief, and the two "Out" items most likely to tempt scope creep — recording a scout deviation, and adding new import-allowlist tests to close the documented enforcement gaps — are both correctly declined with accurate reasoning. The one genuine scope *addition* (the Config Strictness guard) is authorized by `CONSTRAINTS.md` itself and flagged transparently rather than silently absorbed. The three minor inaccuracies above (two package-mention miscounts, one map-entry miscount) live entirely in background narrative prose, not in anything that ships into `CONSTRAINTS.md`, `docs/overview.md`, or the roadmap — worth a quick fix during planning but not a reason to send this back. Ready to proceed to planning as written.
