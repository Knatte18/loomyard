# Orchestrator review — orchestrator-preflight (T5)

Run in automode. Reviewed against `manifest/designs/producers-standalone.md`'s T5 brief, wave 2.

Spot-checked every load-bearing claim against current source — **all confirmed exact**, including precise line numbers: `loomengine/preflight.go`'s `Preflight`/`checkResolved` shape (lines 36, 58-142) matching the check-by-check description; `report.go`'s nine `CheckID` constants and unexported `addFailure`; all six cited `fabricengine` function locations (`PrimeName` at `worktreelist.go:86`, `Clean` at `warpclean.go:20`, `Ready` at `ready.go:17`, `Healthy` at `drift.go:52`, `StencilsDir` at `junctionnames.go:126`, `WeftWorktree` at `fabric.go:115`); `cmd/lyx/main.go:97`'s `EnableTraverseRunHooks`; `stencilseed.go`'s `buildChannel` var and `testing.Testing()` guard; `tools/deploy/main.go:62`'s exact ldflags string; `stencilstore.Mode`'s `ModeProduction`/`ModeDev` iota; `internal/lyxcwd/anchor.go:112-129`'s `normalizePath`/`samePath`; `export_test.go`'s `CheckResolvedForTest`; and `preflight_integration_test.go`'s exactly 13 test functions plus its `hubforge`-imports-`fabriccli` cycle comment. `internal/preflight`, `internal/buildinfo`, `internal/standalonestate` correctly don't exist yet; `internal/hubgeom/doc.go` correctly does (T3 landed).

## Scope verdict: correct, and one genuinely valuable catch

The core lift (`internal/preflight` owning tiers 1-2, `loomengine` composing it with check 4 via type aliases) matches the design's T5 brief precisely, including the non-obvious rationale for why T5 exists this early (T7 sits a whole wave before T8 and needs the same shared leaves).

**The standout: the discussion caught a real self-contradiction in the design doc and resolved it transparently.** T5's own brief (confirmed at `producers-standalone.md:363`) asks for `internal/buildinfo` to be simultaneously "a stdlib-only leaf" *and* to expose a `StencilMode()` accessor returning `stencilstore.Mode`. Those can't both hold: `internal/stencilstore` imports `internal/stencil` (confirmed non-stdlib), so a `buildinfo.StencilMode()` returning that type would make `buildinfo` a non-leaf — defeating the exact property (importable from any `*cli` package with zero cycle risk) the brief itself calls load-bearing in the same paragraph. The discussion resolves it by keeping `buildinfo` to `Channel`/`IsDev()` only and adding `stencilstore.ModeFor(dev bool) Mode` as the single mapping site — correctly identifies this as "a deliberate, documented deviation from T5's `StencilMode()` wording," and commits to recording it in `buildinfo/doc.go` so a future reader doesn't "fix" it back to the broken version. This is exactly the kind of design-doc staleness earlier reviews (T1, T3, T4) have already found in line numbers and omitted files — here it's a substantive logical contradiction, caught and fixed rather than propagated.

**The two new leaf invariants (`Buildinfo Leaf Invariant`, `Standalonestate Leaf Invariant`) are correctly *not* the same situation as T2's flagged invariant addition.** T2 introduced a novel cross-cutting classification rule (strict-vs-degrading config loaders) that nothing in the design or repo precedent specifically called for. Here, the discussion is doing exactly what every other stdlib-only leaf in this repo already does — `tokenvocab`, `pattern`, and `modelspec` each have their own `leaf_enforcement_test.go` — applied to two *brand-new* leaf packages this very task introduces. Confirmed `tokenvocab/leaf_enforcement_test.go`'s `go/parser`/`ImportsOnly`/allowlist shape is real and is what's being copied. This isn't new architecture invented mid-task (unlike T3's `hubgeom`); it's the standard, expected treatment for a package this task is already committed to creating. The three-tier invariant and the Cwd Resolution Invariant reword are correctly left to T10, matching that task's own explicit brief.

**The `seedStencils` gate scope (`preflight.Wired` = tier 1 + `fabricengine.Ready` only, not the full four-function tier-2 report) is a sharp, correctly-grounded reading of an ambiguous design phrase.** T5's brief says "the identical tier-1-AND-tier-2 check," which could be read as the full `Check`/`OK` report. The discussion instead anchors on T8's own brief, which explicitly qualifies this same check as "`fabricengine.Ready`-class" — a phrase I independently recall from my own earlier read of T8's brief, and it matches. The discussion also gives a concrete cost argument for why the narrower reading is right regardless: `Clean` spawns `git status` on every single `lyx` invocation (via `EnableTraverseRunHooks` firing before every command), and a dirty-hub `Clean` failure would silently stop stencil-seeding — a behavior change nobody asked for. The discussion flags this explicitly as "Note for the reviewer," anticipating exactly this kind of scope check — worth acknowledging as good practice, and it holds up against independent verification.

**Correctly declines to wire up any consumer of `standalonestate`/`buildinfo`.** Nothing in `cmd/` imports either package at the end of this task — explicitly stated as expected, not an oversight, deferring the actual CLI wiring to T7/T8 as the design specifies. Unlike T3's `hubgeom` (invented mid-task, not named anywhere in the design), these two packages are explicitly commissioned by T5's own brief for exactly this reuse pattern, so there's no analogous "future-task architecture decided early" concern here.

**File-ownership boundary with T4 holds.** T5's brief states it owns `cmd/lyx/stencilseed.go` and `tools/deploy/main.go` for this wave, and T4's own discussion (already reviewed) touches neither file — confirmed no overlap. The one T4/T5 adjacency (`internal/loomengine`, different files: T4's `plan.go`/`plan_test.go` vs. T5's `preflight.go`/`report.go`) was already self-flagged in T4's own discussion as a plausible-but-mechanical rebase point.

## Minor notes (non-blocking)

- None found. Every cited line number, function signature, and test count verified exactly against current source.

## Bottom line

No scope violations. This discussion matches T4's bar for rigor and clears it in one respect: it found and transparently fixed an actual internal contradiction in the design document itself, rather than just catching stale line numbers. The two new leaf invariants and the narrowed `Wired` predicate are both well-grounded, precedented decisions, not unrequested scope. Ready to proceed to planning as written.
