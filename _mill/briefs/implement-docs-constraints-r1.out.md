Confirmed HEAD is `c81cdabf857211019abaf18888219ce8f021a1cd`.

Summary of work (all 5 of 5 cards in batch `10-docs-constraints` committed and pushed to `webster-rewrite`):

- Card 43 (`8979bb96`): rewrote `/home/knatte/Code/loomyard/wts/webster-rewrite/internal/websterengine/doc.go` to fold in the durable design rationale from the now-deleted `manifest/designs/webster-rewrite.md`, removing all builderengine-dependency prose and describing the shipped batcher-derived-batch / planparser+batcher / fork-return-contract / declared-order-scheduler / integration-bisect / crash-resume design.
- Card 44 (`46ba9dbb`): revised the `## Webster: the fork-based sibling` section of `/home/knatte/Code/loomyard/wts/webster-rewrite/docs/reference/builder-contract.md` from the v2 A/B-compatible framing to the shipped flat-format reality, and noted `builder` is now obsolete as a plan-format consumer.
- Card 45 (`1e37e035`): updated `/home/knatte/Code/loomyard/wts/webster-rewrite/docs/overview.md`'s `webster`/`builder` module bullets and added new bullets for `internal/planparser` and `internal/batcher`.
- Card 46 (`0226258a` + `609e68e7`): moved the `webster: rewrite for flat card list` roadmap item from Planned to Done in `/home/knatte/Code/loomyard/wts/webster-rewrite/manifest/roadmap.md` (merged into the existing `webster` Done entry, with a pointer to the `internal/websterengine` package doc), updated the `builder`/`plan-format v3` Done entries' stale cross-references, and deleted `manifest/designs/webster-rewrite.md`. Note: this landed as two commits because a `git add` with one already-deleted pathspec silently failed to stage the `roadmap.md` edit alongside it - both are now pushed and `git status` is clean.
- Card 47 (`c81cdabf`): added the **Planparser Sole-Parser Invariant** and **Batcher Registry+Config Invariant** to `/home/knatte/Code/loomyard/wts/webster-rewrite/CONSTRAINTS.md`.

Verify (`go build ./...`) passes. One out-of-scope observation for awareness, not acted on: deleting `manifest/designs/webster-rewrite.md` leaves stale `[webster-rewrite.md](...)` links in several unrelated Someday-tier design docs (`fabric.md`, `raddle.md`, `codeintel-redesign.md`, `loom-finalize.md`, `loom.md`, `webster-parallel-execution.md`) and in the durable `docs/reference/plan-format-v3.md`; none of those files are in this batch's or the plan's declared file-touch list, so I left them untouched per the scope-isolation rule.
