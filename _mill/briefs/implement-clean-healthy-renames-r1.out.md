Both card commit subjects match exactly. 2 of 2 cards committed — all complete.

Both cards (16 and 17) of batch `04-clean-healthy-renames` are fully implemented, committed, and verified:

- **Card 16** (`refactor(fabric): rename HostClean to Clean, check warp and weft`, commit `f5d0ed5b`): rewrote `/home/knatte/Code/loomyard/wts/fabric-collapse-external-surface/internal/fabricengine/hostclean.go` to export `Clean` checking both `l.WorktreeRoot` and `l.WeftWorktree()` (skipping the weft side when it doesn't exist), updated the caller in `internal/loomengine/preflight.go`, and extended `internal/loomengine/preflight_integration_test.go` with `DirtyWeftOnly`/`BothDirty` regression cases. Had to also update `setupPreflightFixture` (and add a `commitWeftStatus` helper used by `TestPreflight_SeedMissing`/`SeedUnknownField`/`SeedHalfFinished`) to commit the seeded `status.json` into the weft worktree, since the new weft-side dirtiness check would otherwise flag every fixture as dirty due to the untracked seed file/lock — this is a legitimate fixture fix within the card's declared file scope, not a test-weakening shortcut.
- **Card 17** (`refactor(fabric): rename PairInSync to Healthy`, commit `7edfac80`): renamed `PairInSync` to `Healthy` in `internal/fabricengine/drift.go` (also trimmed its doc comments), updated the caller in `preflight.go`, all four in-package test call sites, and every stale doc-comment/test-name mention across `internal/fabricengine/{commit,doc,junctionnames,reconcile,status}.go` and the four test files, plus `internal/hubgeometry/hubgeometry.go` and `internal/loomengine/preflight_integration_test.go`.

`go build ./...` and `go test -tags integration ./internal/fabricengine/ ./internal/loomengine/` both pass. No tracked uncommitted changes remain. One out-of-scope stale doc mention (`docs/shared-libs/hubgeometry.md:127`, mentioning `PairInSync`) was left untouched since it's outside this batch's declared file scope and outside the "fabricengine/hubgeometry/loomengine packages" grep instruction.

{"status":"success","commit_sha":"7edfac802ab00cbb3b96b29dfa4b70c3a773af3e","session_id":"0eff3227-7c44-4c4d-bf19-3cd852bd7b18","cards_done":[16,17]}
