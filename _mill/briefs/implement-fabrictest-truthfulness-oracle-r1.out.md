Summary of key files touched (all absolute paths under `/home/hanf/Code/loomyard/wts/fabric-mutation-record-envelope`):

- `internal/fabricengine/fabrictest/refusal.go`, `refusal_test.go`, `verbs.go`, `verbs_test.go`, `doc.go`, `matrix_test.go` — repointed to `fabricengine.Check`, changed `VerbCase.Run`'s signature to return the mutation record, wired the oracle into every cell.
- `internal/fabricengine/fabrictest/mutationoracle.go` (new) — the `AssertRecordMatchesDiff` truthfulness oracle (omission/commission, net-effect folding, worktree-admin and ancestor coverage widenings, the `"."` target asymmetry).
- `internal/fabricengine/fabrictest/mutationoracle_test.go` (new) — 25 hand-built unit test cases for the oracle.
- `internal/fabricengine/prune.go` — fixed a genuine pre-existing recording gap (`removeStalePair`'s `git worktree prune` admin-registration cleanup was unrecorded); this required a plan-file amendment (`_mill/plan/07-fabrictest-truthfulness-oracle.md`) since `prune.go` wasn't originally in this batch's Edits list.

One notable deviation from the literal card-32 instruction worth flagging to the reviewer: rather than claiming (inaccurately) that each of doc.go's nine historical sabotage rows now "fails on both the survival assertion and the honesty assertion," I verified this is not generally true — destroy.go's recording chokepoint records a primitive after it succeeds regardless of whether the gate that let it through should have refused, so neutering an authorization check produces an honest record of an unauthorized act (survival catches it; honesty has nothing to disagree with). I documented this precisely in doc.go instead, using the real, verified defect this batch found and fixed (prune.go's unrecorded admin cleanup) as the concrete example of what the honesty assertion actually catches that survival does not.
