Both cards' commit messages match exactly. 2 of 2 cards committed — all cards complete.

{"status":"success","commit_sha":"4e6de6d83501fdc87faf9dce622afb8b8761add1","session_id":"3112e535-af1e-4c23-83a0-0fed94c391b6","cards_done":[14,15]}

Summary: 2 of 2 cards committed (Card 14: `6c9bbc86` removed the dead `git add -f`/`hasPathspecMagic` branch from `internal/gitrepo/gitrepo.go` and recorded the Never Force-Add Invariant in `CONSTRAINTS.md`; Card 15: `4e6de6d8` added `internal/gitrepo/noforceadd_test.go`, an untagged grep guard). No changes were needed to `internal/fabricengine/weftgit_pathspec_integration_test.go` — I ran the full `-tags integration` suite for both `internal/gitrepo` and `internal/fabricengine` and every existing case (including the `:(exclude)`-magic passthrough test at line 167) still passes without the removed `-f` branch, since the exclude pattern `**/_lyx/*/**/*.lock` requires a subdirectory under `_lyx` and never actually matched the flat `_lyx/run.lock` path those tests exercise, so `git add` never hit the ignored-path refusal `-f` used to suppress. Batch verify (`go test -tags integration ./internal/gitrepo/ ./internal/fabricengine/` plus `go build ./...`) passes, and the working tree has no uncommitted tracked changes.
