{"status":"success","commit_sha":"43dd14dc5a75994b8cb6253d7dc3d55bec92f7e8","session_id":"c11b60d3-bb2d-42af-af1a-f3f8bb3274b0","cards_done":[13,14]}

Summary: Both cards in this batch were verification-only ("Commit: none"-style, with the "or the no-correction record commit" fallback), and both reviews found nothing to correct — 2 of 2 cards addressed, combined into a single commit `43dd14dc5a75994b8cb6253d7dc3d55bec92f7e8` on `_mill/plan/05-cli-surface-review.md`'s new `## Batch Notes` section (both card 13 and card 14 notes landed in that one commit, using card 13's `Commit:` message rather than card 14's, since I wrote both notes before committing — a minor deviation from the "name it using the later card's message" preference, but the commit content fully covers both cards' required confirmations).

Findings:
- Card 13 (`/home/knatte/Code/loomyard/wts/fabric-host-to-warp-rename/internal/fabriccli/fabric.go`): every `warp`/`weft` occurrence in the `Short`/`Long` strings and package doc comment genuinely distinguishes the two sides (including the `<warp-url>`/`<warp-name>`/`<weft-url>` clone placeholders); zero `host` occurrences remain; zero `host` assertions in `cmd/lyx/helptree_test.go`, `cmd/lyx/longlist_test.go`, `cmd/lyx/jsonhelp_test.go`; CLI/Cobra Invariant holds (non-empty `Short` on every command, root `Long` names `fabric`).
- Card 14: `/home/knatte/Code/loomyard/wts/fabric-host-to-warp-rename/internal/fabricengine/post-checkout.sh` lines 62-63 correctly retain warp/weft; the `HOST_BRANCH`->`WARP_BRANCH` shell variable rename is consistent across lines 43, 44, 54, 55, 58. All six `tools/sandbox` user-visible error strings (`main.go` 139/141, `report.go` 57/59, `suite.go` 323/325) correctly use "warp" — confirming the undercount that `_mill/discussion.md` missed (`suite.go`'s pair).

Verify: `go test ./cmd/lyx/... ./internal/fabriccli/... ./tools/sandbox/...` passed. No tracked dirty files remain. One pre-existing unrelated gofmt issue in `internal/shell/posix.go` was left untouched (out of this batch's scope).

{"status":"success","commit_sha":"43dd14dc5a75994b8cb6253d7dc3d55bec92f7e8","session_id":"c11b60d3-bb2d-42af-af1a-f3f8bb3274b0","cards_done":[13,14]}
