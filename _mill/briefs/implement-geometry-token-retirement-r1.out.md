All 3 cards for batch 06-geometry-token-retirement are committed. Verify (`go test ./... && go test -tags integration ./...`) passes in full. `git status --porcelain --untracked-files=no` is clean.

Card-count self-check: 3 of 3 cards committed (28: `refactor(pattern): delete the DirName const and Dir accessor`, 29: `test(lyxcwd): retire the _pattern and _raddle geometry tokens from the enforcement map` — split across two commits, one for the enforcement_test.go switch/map edit and one folded-in fix for a genuine-miss stale `_pattern` comment discovered in `internal/fabricengine/junctionnames.go` during card 30's sweep, 30: verification-only, `Commit: none`, requirements performed this turn).

Two plan-scope extensions were made (each committed and pushed first, per protocol) because card 28/29 as originally scoped missed two files: `internal/pattern/pattern_test.go` (a stray `_pattern` directory-name fixture in `TestDirective_InactiveWithoutFile`, retargeted to a neutral `stray_dir` name) and `internal/fabricengine/junctionnames.go` (a stale `_pattern` illustrative example in `dedupUnion`'s doc comment, reworded to `_extra`). Both are recorded in `_mill/plan/06-geometry-token-retirement.md`.

Card 30's grep sweep (`grep -rn '_pattern\|_raddle' internal/ cmd/ docs/ manifest/ tools/ README.md CLAUDE.md CONSTRAINTS.md`) confirmed: the two declared deliberate survivors (`internal/lyxcwd/raddle_guard_test.go`, `internal/fabricengine/structuraldirs_test.go`) are intact and unmodified; all other `internal/`/`cmd/` Go hits are legitimate (deliberate negative assertions, filename references preserved under the no-rename decision, already-migrated tests asserting the new un-reserved behavior, or the batch-4-documented unrelated-purpose `tierPuritySkipDirs` entry in `cmd/lyx/tierpurity_test.go`); the one genuine miss found (`junctionnames.go`) was fixed in this batch rather than deferred; every remaining hit is prose in `docs/`, `manifest/`, `tools/`, `README.md`, `CLAUDE.md`, `CONSTRAINTS.md`, or the explicitly-named Go comment sites — all batch 7's worklist.

Relevant files: `internal/pattern/pattern.go`, `internal/pattern/patternpath_test.go`, `internal/pattern/pattern_test.go`, `internal/lyxcwd/enforcement_test.go`, `internal/fabricengine/junctionnames.go`, `_mill/plan/06-geometry-token-retirement.md`.

{"status":"success","commit_sha":"b1af052bba8989414e3d8de9bcda001e4b6cf692","session_id":"d0852cf1-6d42-4b74-a99c-4238194c509a","cards_done":[28,29,30]}
