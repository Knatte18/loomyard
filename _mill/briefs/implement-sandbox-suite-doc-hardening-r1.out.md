No tracked modifications remain. `verify: null` per the batch frontmatter, so no verify command to run. The single card is committed and pushed.

Summary of work:
- Edited `C:\Code\loomyard\wts\lyx-sandbox-fixes\tools\sandbox\SANDBOX-SUITE.md`: extended scenario S2's `**Watch:**` line to state the absence of a lyx-owned host-commit command is an intentional design choice (not to be filed as an enhancement), and extended scenario S6's `**Watch:**` line to state the JSON error envelope is the deliberate machine-parseable contract while preserving the raw-subprocess-leak counter-case as a legitimate finding.
- Committed as `73fa9dc` (`docs(sandbox): harden S2/S6 against re-filing declined findings`) and pushed to `lyx-sandbox-fixes`.

{"status":"success","commit_sha":"73fa9dc","session_id":"fa2841da-31f3-483d-baf4-b1c119b4b7cd"}

{"status":"success","commit_sha":"73fa9dc","session_id":"fa2841da-31f3-483d-baf4-b1c119b4b7cd"}
