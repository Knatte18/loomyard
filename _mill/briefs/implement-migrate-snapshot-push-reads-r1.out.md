All 6 cards of this batch are complete: cards 14, 15, 16, and 17 combined into commit `fe19517e` (matching card 17's later `Commit:` message per the combined-commit allowance, since all four touch only `internal/gitrepo/snapshot.go`), card 18 in commit `1c586537`, and card 19 in commit `80485d3c`. The batch's `verify:` command (`go test -tags integration -race -count=1 ./internal/gitrepo/...`) passes cleanly, and `git status --porcelain --untracked-files=no` shows no uncommitted tracked changes.

Summary of what changed:

- `/home/knatte/Code/loomyard/wts/loomyard.worktrees/native-clients/internal/gitrepo/snapshot.go` — migrated `remoteName` (card 14), `SnapshotSHA`'s ref read (card 15), `isStrictDescendant` (card 16), and `SetSnapshotSHA`'s `^{commit}` canonicalization plus its adopted-ref read, with an explicit `r.goGit()` probe added before the adopt-on-conflict loop (card 17), all from the git CLI onto go-git, following the batch's call-granular boundary and RWMutex/fingerprint-gate rules.
- `/home/knatte/Code/loomyard/wts/loomyard.worktrees/native-clients/internal/gitrepo/push.go` — migrated `hasUnpushed` (card 18) to a go-git reachability walk with an equal-hash shortcut, upstream resolved manually from branch config (`@{u}` is unresolvable via go-git's `ResolveRevision`), object lookups routed through `lookupObjectRetrying`, and the CLI's "any failure means true" contract preserved exactly (diverging from `gitnativepoc`'s poc, which returned `(false, err)`).
- `/home/knatte/Code/loomyard/wts/loomyard.worktrees/native-clients/internal/gitrepo/parity_test.go` and `/home/knatte/Code/loomyard/wts/loomyard.worktrees/native-clients/internal/gitrepo/gogit_test.go` — added mixed-backend interop parity cases (card 19): pre-warmed-handle cases for every enumerated call site, hard-variant repack-between-write-and-read cases (proven, by watching them fail without the reindex mechanism during design, to actually require the fingerprint gate) for `SHAExists`, `SetSnapshotSHA`'s canonicalization, `isStrictDescendant`, and `hasUnpushed`, and a cross-instance case (`TestSHAExists_MixedBackend_CrossInstanceReindexSeesWriteFromOtherRepo`) pinning that the gate is shared on-disk state rather than a per-`Repo` counter.

No pre-existing tests were touched, weakened, or removed. All lint findings surfaced by `golangci-lint run --build-tags integration` (two `errcheck` findings in `snapshot.go`/`push.go`, three in `push_test.go`, and three `unused`-oracle findings in `oracle_test.go`) were confirmed pre-existing via `git stash` comparison against the pre-batch state — none are new.

{"status":"success","commit_sha":"80485d3c","session_id":"bb6aaef2-87e2-4660-b0a1-6b1a03742ac2","cards_done":[14,15,16,17,18,19]}
