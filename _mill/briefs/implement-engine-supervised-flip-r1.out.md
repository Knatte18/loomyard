All 3 cards (10, 11, 12) of this batch are committed, matching their declared `Commit:` messages exactly. Batch verify (`go test ./internal/codeintelengine/...`) passes. Working tree is clean (no uncommitted tracked changes).

Summary of work — 3 of 3 cards committed, all complete:

- **Card 10** (`8dab43cb`): Renamed `nativeDaemonIdleTimeout` → `daemonIdleTimeout`, added `supervisedArgv` helper wired into `ensureSupervised`'s spawn step, updated related doc comments. Files: `/home/knatte/Code/loomyard/wts/codeintel-daemon-persistence/internal/codeintelengine/ensureserver.go`, `ensureserver_test.go`, `doc.go`.
- **Card 11** (`994983b0`): Added the wedged-daemon re-dial-under-lock-then-one-restart escalation inside `ensureSupervised`, factored into a pure `reconnectUnderLock` helper, using `proc.KillPID` best-effort before respawn, with a deadline guard before any kill. Added process-free unit tests (`TestReconnectUnderLock_ReuseOrRestart`) and lock-release coverage in `supervised_test.go` (extended the existing timeout test, added `TestEnsureSupervised_WedgedEscalationReuseReleasesLock` using an in-process fake unix-socket LSP server). Files: `ensureserver.go`, `ensureserver_test.go`, `supervised_test.go`.
- **Card 12** (`ddd532f6`): Flipped `ensureServer` to resolve the toolchain then dispatch to `ensureSupervised`, falling back to `ensureNative` on any supervised error (never on a toolchain-resolution failure). Updated all the now-false doc comments (`ensureserver.go`, `doc.go`, `refs.go`'s `Options.WorktreeRoot`, `docs/overview.md` module table) and `refs_test.go`'s call-chain doc/assertion text. Added `TestEnsureServer_SupervisedFailsForNonToolchainReasonFallsBackToNative` covering the previously-untested native-fallback safety net. Files: `ensureserver.go`, `ensureserver_test.go`, `refs.go`, `refs_test.go`, `doc.go`, `docs/overview.md`.

Verified: `go test ./internal/codeintelengine/...`, `go build ./...`, `go vet ./internal/codeintelengine/...`, `go test ./internal/codeintelcli/... ./cmd/lyx/...` (including `TestTierPurity_UntaggedTestsSpawnNothing`) all pass. `golangci-lint` shows only pre-existing `fileLock.Release` errcheck findings that already existed on `codeintel-v1` in the same style, unrelated to this batch's changes.

{"status":"success","commit_sha":"ddd532f6","session_id":"bc840343-623b-4d3b-b159-19cba5c37305","cards_done":[10,11,12]}
