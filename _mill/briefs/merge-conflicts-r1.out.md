Perfect! The conflict in `docs/overview.md` has been resolved and staged. The resolution combined both branches' module additions (ghissues and selfreport) while using the `internal/hubgeometry` package name from this branch's renaming effort, which preserves this branch's intent to rename the paths module.

{"status":"success"}
