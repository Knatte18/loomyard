Excellent! Both resolutions are correct:

1. **tierpurity_test.go**: Both `checkedcall_test.go` and `cwdmutation_test.go` map entries are preserved (non-overlapping keys from each branch).
2. **fabric.go**: Both `"context"` and `"errors"` imports are now present in sorted alphabetical order.

Both files have been staged and the merge is ready to continue with `git merge --continue`.

{"status":"success"}
