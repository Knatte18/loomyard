Excellent. The conflict has been successfully resolved. The CONSTRAINTS.md file had one conflict in the "Fabric Git Invariant" section between the current branch (which documents the closed gap with the new interfaces and machine-checked enforcement) and the main branch (which had the pre-implementation state). I kept the HEAD side (webster-bisect-fabric-migrate) because it reflects what this task accomplished - closing the documented gap and adding the new Fabric methods with their machine-checked enforcement.

{"status":"success"}
