1 of 1 card committed — Card 4 (the batch's only card) is complete, matching its `Commit:` message exactly. Full verify (`go test ./...` and `go test -tags integration ./...`) both pass with no FAILs.

{"status":"success","commit_sha":"3cee9079f9fe5021dd791164ab0aca860a41b212","session_id":"09c6f338-c238-4fa0-a077-6e103fd7c9ae","cards_done":[4]}

Summary: 1 of 1 card committed (Card 4: re-measure and append the after-fix benchmark block). Edited `/home/knatte/Code/loomyard/wts/test-suite-overhaul/docs/benchmarks/test-suite-timing.md`, appending a new `### 2026-08-01 — timeout/window seams shrunk` block under `## Linux baseline` (above the existing `### 2026-08-01 — githubclient + webstercli now the floor` block), measured on the same machine (AMD Ryzen AI 7 445, Ubuntu 26.04, Go 1.26.0, 12 logical CPUs) via 3 warm `go run ./cmd/testtiming[ -full]` runs after `go build ./...`: Tier 1 ~3.86s (was ~6.23s, ~38% faster), Tier 2 ~6.48s (was ~33.40s, ~81% faster). The Cause section attributes the drop to the `ghAuthTokenTimeout` var-seam and the `--wait 1ns` flag, and explains the remaining gap above the 2026-07-13 pre-regression floor (~1.03s/~4.97s) via `internal/scoutengine`'s own real-time supervision tests — a module that didn't exist in 2026-07-13 and is out of this task's scope. No earlier block was edited (append-only preserved). Committed as `3cee9079` and pushed. Both verify commands (`go test ./...` and `go test -tags integration ./...`) pass with `RESULT: all packages passed` equivalents (no FAIL lines).

{"status":"success","commit_sha":"3cee9079f9fe5021dd791164ab0aca860a41b212","session_id":"09c6f338-c238-4fa0-a077-6e103fd7c9ae","cards_done":[4]}
