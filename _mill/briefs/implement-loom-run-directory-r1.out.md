All 4 cards for this batch (12, 13, 14, 15) are committed, and the batch's `verify:` command passes: `go build ./...` succeeds and `go test ./internal/loomengine/... ./internal/loomcli/...` reports `ok` for both packages.

Summary of work:

- **Card 12** -- `internal/loomengine/config.go` (deleted `LoomStatusRel`/`LoomStatusFile`/`LoomStatusLock`/`LoomRunLock`, kept `loomDirName` for the seven surviving accessors), `internal/loomcli/wiring.go`, `sharedbootstrap.go`, `landingdeps.go` retargeted onto `shedrun.StatusFile`/`StatusLock`/`RunLock`/`StatusRel`. Also fixed `internal/battencli/wire.go` and `paths.go`, which still called the deleted accessors (a compile-breaking gap outside the card's original file list -- flagged and added to the plan before editing, per protocol). Updated `internal/loomengine/config_test.go` and `loomstatus_test.go` accordingly.
- **Card 13** -- `internal/loomcli/sharedbootstrap.go`'s `seedAndCommitBootstrap` now writes `shedrun.Seed{Recipe: loom, Driver: go, Params: {parent}}` before the status-file seed, and includes `shedrun.SeedRel(shedrun.SelfRunID)` in the step-3 commit pathspec. Factored the seed shape and commit pathspec into two pure helpers (`loomSeedFor`, `bootstrapCommitPaths`) so `sharedbootstrap_test.go` can pin them at Tier 1 without a real fabric.
- **Card 14** -- `internal/loomcli/arm.go` gained `resolveRunID`, extracted from `arm`, which resolves the addressed run-id, records it on `c.runID`, and refuses the four generic verbs (`run`/`step`/`status`/`pause`) when no seed exists at that run-id, via `shedrun.ReadSeed`/`List`/`MissingSeedMessage`, carrying no `kind` field. `cli.go` gained the `runID` struct field; `wiring.go` now reads `c.runID` instead of a hardcoded `shedrun.SelfRunID`. New `arm_seed_test.go` covers the refusal per verb, the existing-run-id listing, the status-file-without-seed case, and the write-nothing-when-refused asymmetry. Fixed `wiring_test.go`/`wiring_commitstatus_test.go` fixtures that call `wire`/`wireLightweight` directly to set `runID: shedrun.SelfRunID`.
- **Card 15** -- `internal/loomcli/cli.go`: `run`/`step`/`status`/`pause` gained `cobra.MaximumNArgs(1)`, updated `Use` strings and `Long` text/examples to mention the optional run-id positional; `start`'s arity is untouched. `parity_test.go` gained a test asserting all four verbs accept 0/1 positionals and refuse 2.

Three plan-edit commits extend `_mill/plan/04-loom-run-directory.md`'s file lists for cross-file fallout discovered while implementing (battencli's loom-status callers, the `runID` struct field's home file, and the `wiring_test.go`/`wiring_commitstatus_test.go` fixture fallout) -- each committed and pushed before the corresponding code change, per the brief's scope-extension protocol.

{"status":"success","commit_sha":"32cb9ee6641cf71f28481c7c8982a0c4519d3d6d","session_id":"060abfe6-f21a-4ddd-a258-b2458c105f00","cards_done":[12,13,14,15]}
