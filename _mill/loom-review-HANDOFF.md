# `loom` crucible campaign — glyph-hardening — HANDOFF

> Refreshed after every round's verification, per `crucible/orchestrator-prompt.md`'s hygiene section. If this session's context resets, a fresh orchestrator should read this file first, then `_mill/loom-crucible-orchestrator-kickoff.md` for the campaign charter.

## Campaign identity

- Worktree: branch `crucible-loom-glyph-hardening`, parent `main`. **Do not hardcode a specific host path here** — this campaign has already run from at least two different host/user environments (`/home/knatte/...` for rounds 1–4, `/home/hanf/...` for this orchestrator session); a fresh session should resolve its own worktree path (`git rev-parse --show-toplevel`) rather than trust any path string in this file or in a prior round's own report.
- Mission: harden `loom` against the newly-landed quarry-glyph-plan-alphabet surface (PR #230) — never before exercised through loom's real phase machine, only through the landing task's own unit/integration suite.
- Model/effort rotation, pre-approved by the operator for up to four rounds unless it converges sooner: **Opus/high (r1) → Sonnet/xhigh (r2) → Fable/high (r3) → Opus/high (r4)**. Round 4 was NOT a clean pass (found+fixed 2 more BLOCKING bugs), so the operator authorized **round 5 (Opus/medium)** beyond the original budget. Round 5 was ALSO not a clean pass — found+fixed 2 MORE BLOCKING bugs, on the module's single hottest live path. The operator then authorized **round 6 (Opus/medium again)** — a deliberate choice, per the operator's own read that method/angle diversity (a genuinely fresh fixture, a newly-opened seam) has been what's finding real material, not model diversity as such. **Round 6 was ALSO not a clean pass** — 28 findings, 2 more BLOCKING, one of them in the exact code round 5 shipped. See "Convergence status" below.

## Current state

**Rounds 1 through 7 are all CLOSED-AND-VERIFIED.** Round 7 (fable-high-r7) — the campaign's second Fable deployment, and its first-ever review of the `unify-webster-burler-wiring` merge (`7e54cc280`, `internal/cliwire`) that squash-merged into this branch after round 6 closed — found 6 findings (1 BLOCKING, 1 MEDIUM, 2 LOW including the two seeded coverage gaps, 1 NIT), fixed all 6, and this orchestrator independently verified the round's work in full via a dedicated verification fork: cold gates all green from scratch (`go build`, `go vet`, `go test -count=5 ./...` — 82 packages, zero flakes, `go test -tags integration` over 9 packages), and 6/6 sabotage-proofs PASS, including a real constructed-fixture proof for F3 (planted a throwaway `standalonestate.Derive` caller under `tools/`, confirmed the pre-fix enforcement test's scan genuinely missed it and the post-fix one catches it). Working tree is clean. 12 commits ahead of `origin/crucible-loom-glyph-hardening`. Nothing pushed.

**The cliwire merge is now itself CLOSED-AND-VERIFIED material, not just "landed and unreviewed."** Round 7 confirmed all six of round 6's wiring-duplication guarantees (R6-7/8/9/15/16/17) survived the consolidation into `internal/cliwire` for both `webstercli` and `burlercli`, live-drove the standalone refusal scenarios (S1–S9) against a fresh HEAD binary, and found one genuine gap the consolidation itself introduced room for (F2: `--stencils-dir` was the one told-directory flag never stat'd at the wiring boundary) plus two enforcement-test blind spots (F3/F4) — all fixed.

**Separately, a structural observation from this session led to a new mill-wiki task, not a crucible finding:** `webstercli`/`burlercli`'s CLI wiring logic (target-dir/plan-dir resolution, standalone-vs-hub mode detection, nested-geometry guards, refusal messages) is hand-duplicated between the two packages and has repeatedly drifted — R6-7/8/9/15/16/17 are six instances of exactly that shape. Logged as `unify-webster-burler-wiring` in the project wiki (not this campaign's job to fix; a follow-on consolidation task, explicitly pointed at `internal/shedrecipe`'s registry-of-constructors shape as an in-repo precedent, since that pattern was checked and confirmed genuinely immune to the same drift).

**`unify-webster-burler-wiring` has since been spawned** (its own worktree, `wts/unify-webster-burler-wiring`, own branch, own `_mill/status.md`) — this crucible campaign's own worktree took no part in that spawn beyond running `/mill-spawn`, per worktree isolation. Its design phase asked the operator to settle the new `internal/cliwire` package's shape across ~20 questions; answered in this session (not committed here — that task owns its own design record in its own worktree). Headline decisions, for context if a future crucible round touches this seam again: new package `internal/cliwire` (not folded into `standalonegeom`/`clihelp`); a `Module` descriptor value per CLI (`webstercli`/`burlercli` each declare their own, `cliwire` stays caller-agnostic — the actual shedrecipe analogy, data outside the shared code); one `ResolveStandalone` entry point performing the whole ordered prologue including the durable-sink redirect (locks the ordering obligation into the type instead of two copies of a comment); plan-dir logic moves into the shared module even though burler doesn't use it today (nil `*PlanRules` for burler) — deliberately closing the R6-8/R6-9 shape rather than leaving it possible again; a `CONSTRAINTS.md` invariant WITH mechanical enforcement (a caller-set pin on `standalonestate.Derive` restricted to production files, plus a banned-declaration check on the old duplicated function names).

**Note on host continuity:** this orchestrator session runs on a different machine (`/home/hanf/...`) than round 4's own live driving (`/home/knatte/Code/lyx-test-HUB/...`, which does not exist here). Verification below substitutes real GitHub artifacts (cloning/inspecting the sandbox repo `github.com/Knatte18/lyx-test`) for on-disk process/tmux checks where the host gap made those unreachable — see "What this orchestrator could and could not re-verify" below.

### Round 4 (opus-high-r4) — CLOSED-AND-VERIFIED

**Findings:** 38 total (2 BLOCKING, 11 MEDIUM, 15 LOW, 10 NIT). R4-22 withdrawn after implementation (confirmed correct: reverted, `shed.md` and `internal/shedcheck`'s `done-cycle` check independently confirm the withdrawal was right). R4-38 not fixed (unreproduced WARN, outside loom/glyph scope, recorded honestly). The remaining 36 are ALL fixed and committed — including 7 (R4-07, R4-11, R4-23, R4-25, R4-26, R4-33, R4-34) whose commits had already landed under the round's own session but were missing from the fixer report table; independently confirmed each is real (correct session attribution, production diff + regression test) before completing the table.

**Independent verification (this orchestrator):**
- **Cold gates, full run, all green:** `go build ./...`, `go vet` (17 in-scope packages), `go test -count=5` (18 packages, 0 FAIL), `go test -tags integration` (17 packages, 0 FAIL), whole-repo `go test ./...` (81 packages ok, 0 FAIL).
- **9/9 sabotage-proofs PASS** — both BLOCKING findings (R4-01, R4-02) plus all 7 findings missing from the fixer-report table (R4-07, R4-11, R4-23, R4-25, R4-26, R4-33, R4-34), since those 7 carried no explicit test-verification claim in the round's own report. Each: production hunk reverted to the exact pre-fix shape, regression test fails at the intended assertion, restored, working tree confirmed clean after every single one.
- **Live claims verified against real GitHub artifacts, not narrative:** cloned/inspected `github.com/Knatte18/lyx-test`'s `r4-crash-hub` and `r4-drift-hub` branches. Commits match the round's report EXACTLY — `r4-crash-hub`: `cc8a389`/`d961780`/`9c7fe62` ("1: default-name-helper", "2: compose-farewell", "3: farewell-test-and-main-wiring", matching Live-3). `r4-drift-hub`: `6f9024b`/`c893872`/`ee5e13b` ("1: rename-default-helper", "2: farewell-doc", "3: readme-note", matching Live-4/Live-5). Pulled the actual diff of `6f9024b`: `defaultName` → `orDefault`, rewritten at BOTH the declaration and the `ComposeFarewell` reference — byte-for-byte the exact-tier auto-repair Live-4 claims, genuinely proven, not asserted.
- **One unexplained extra branch found:** `r4-verify-hub` (not mentioned anywhere in round 4's report) carries the same history as `r4-drift-hub` plus two more real commits (`15c8eed`/`431c5e4`, "1: shout-helper"/"2: shout-wiring", timestamped ~19:58–19:59, i.e. during the review/Job-A window, before the fix phase started at 21:12). Real webster-shaped work, provenance unknown — not contradicting anything, just unaccounted for. Worth asking the round or operator about if it matters later; not a blocker.
- **What this orchestrator could and could not re-verify:** sabotage-proofs and cold gates are fully reproduced firsthand (strongest evidence tier). The GitHub-artifact check independently confirms the live runs actually happened and actually did what's claimed at the code/commit level (also strong — this is what round 2/3's own verification leaned on too). What could NOT be re-checked from this host: the process/tmux-level mechanics of Live-1, Live-2, and Live-5's kill-9 tests (real PIDs, pane state, orphan detection) — those are ephemeral and were only ever inspectable live, on the host round 4 actually ran on. This is a genuine limitation of this verification pass, not a finding against round 4.

**Full detail:** `_mill/loom-review-opus5-high-r4.md`, `_mill/loom-review-opus5-high-r4-fixer-report.md` (fixer report corrected in `1a1214784`).

### Round 5 (opus-medium-r5) — CLOSED-AND-VERIFIED

**Clean-room review:** findings formed WITHOUT reading any prior `_mill/loom-review-*` material; prior-round material was consulted only after round 5's own findings list was complete and committed.

**Findings:** 7 total (2 BLOCKING, 1 MEDIUM, 2 LOW, 2 NIT). All CONFIRMED, all 7 fixed and committed, one commit per fix.

- **R5-2 (BLOCKING, `69263d3e4`)** — `claudeengine.TrustDismissSequence` sent a bare `Enter` at claude's trust-this-folder gate. Claude 2.1.263's gate caret defaults to the REFUSING option (`No, exit`), so lyx's own "dismissal" confirmed the refusal and claude quit. Every agent lyx spawns in a directory claude has not previously been accepted in — i.e. every freshly-created fabric worktree pair, the module's own primary entry path — died at startup, surfaced only as an opaque `master pane died`. Fixed by reading the caret out of the capture and walking it onto the accepting option; presses nothing when it cannot locate one (fails safe, not a blind keypress).
- **R5-7 (BLOCKING, `1640a59bb`)** — reachable only past R5-2: claude's Bypass-Permissions modal (raised on every `--dangerously-skip-permissions` launch, i.e. every lyx launch, in a fresh environment) was not a gate `Startup` recognized, and its own selection caret is the same `❯` glyph `Startup` reads as its READY marker — so the pane classified READY, the startup deadline stopped applying, and the run parked on the dialog for the whole `master_timeout_min` (480 min as configured) before reporting a timeout rather than dying fast. Fixed by giving the gate its own needle (`yes,iaccept`, the accepting option's own label — never appears in a running claude TUI) checked before the ready check.
- **R5-1 (MEDIUM, `ba43f12d5`)** — `webstercli`'s standalone integration test booted a real tmux server via the `reedUp` seam and never tore it down: one orphan server leaked per suite run, forever, with a dangling cwd. Fixed with a `t.Cleanup` teardown helper.
- **R5-6 (LOW, `a86e6fd9b`)** — `planglyph.DoneChecks` silently PASSED a blocking done-check whose target the batched resolve answer did not cover, contradicting its own doc comment ("passing it would be a false success") and diverging from `CanonicalizeHandles`' guard on the same class of boundary. Fixed: an uncovered target now returns a wrapped `ErrQuarryUnavailable` instead of being skipped.
- **R5-3 (LOW, `5f21b5aa2`)** — `websterengine.Run` dropped a plan-fingerprint re-baseline persist failure when a validation error was also in flight, instead of reporting both (as both CLI bracket verbs already do for the same coincidence). Fixed to report both.
- **R5-4, R5-5 (NIT, `7025ffb1c`/`187161faa`)** — doc-only: a self-contradicting doc comment (`repositoryRootOf` said "topmost", the code returns the nearest) and an undocumented always-nil error return, both duplicated across `webstercli`/`burlercli`'s wiring.

**Live crash-kill scenario (item 2), delivered in full on a genuinely fresh fixture** (not reusing `r4-crash-hub`/`r4-drift-hub`): two full hub-mode `lyx webster run` completions to `outcome: done` on previously-untrusted hubs (both startup gates cleared autonomously, no human at the keyboard), then a real `kill -9` mid-Webster-batch on a second fresh fixture — begun-batch confirmed from `state.json`, both target processes confirmed alive by `ps`, killed, confirmed dead, `outcome.yaml` confirmed absent, the orphan strand confirmed still live — followed by a clean `lyx webster run` resume that reclaimed the orphan and finished `outcome: done`.

**Why four prior rounds missed R5-2/R5-7:** both are invisible on any fixture claude has ever been driven in by hand — every prior round reused or inherited such a fixture. Only a genuinely fresh, never-hand-driven fixture (this round's own item-2 mandate) exposes them. **Durable method lesson for any future round:** do not reuse `r5sandbox`/`r5sandbox2`, or any hub whose repo path has ever hosted an interactive claude session — build fresh from a path claude has never seen.

**Independent verification (this orchestrator, same host as the round — full firsthand reproduction, no artifact-substitution gap this time):**
- **5/7 findings sabotage-proofed firsthand**, prioritizing both BLOCKING findings: R5-2 (reverted `TrustDismissSequence` to a bare `Enter`; `TestTrustDismissSequence` and `TestTrustDismissSequence_NeverConfirmsWithoutSelectingAccept` fail exactly as expected), R5-7 (removed `"yes,iaccept"` from `startupGateNeedles`; `TestStartup_Classification/bypass_permissions_gate_is_a_gate_not_ready` fails, misclassifying the modal as Ready, while the adjacent false-positive guard test still correctly passes), R5-6 (reverted the `continue`-vs-error branch in `doneCheckVerdicts`; `TestDoneCheckVerdicts_UncoveredTargetIsInfrastructureNotAPass` fails), R5-3 (reverted `runlevel.go`'s error-composition back to silently dropping `rebaseErr`; `TestRun_ValidationErrorAndRebaselineSaveFailure_ReportsBoth` fails at the intended assertion), and R5-1 (removed the `tearDownStandaloneReed` call from the integration test; re-running it left a real, live orphan `tmux -L lyx-383523e1` server behind, confirmed via `pgrep`, then killed and the test file restored). R5-4/R5-5 are doc-only (no behavior to sabotage); both commits inspected directly and match the report. Every sabotage was restored via `git checkout --` and the working tree confirmed clean (`git diff --quiet`) after each.
- **All cold gates re-run clean from scratch on this host:** `go build ./...`, `go vet ./...`, `go test -count=5 ./...` (81 packages, all `ok`, 0 FAIL), `go test -tags integration` over the round's 7 named packages (all `ok`). Also independently confirmed the R5-1 fix behaviorally: re-ran the integration suite fresh and swept `pgrep -af "tmux -L lyx-"` afterward — zero leaked servers.
- **Live crash-kill/resume claim independently confirmed from real, still-on-disk artifacts (same host, no substitution needed):** `/home/hanf/Code/r5sandbox2/lyx-test-HUB/r5-kill2`'s `_lyx/webster/outcome.yaml` reads `outcome: done, batches_done: 2` exactly as reported; `state.json` and `summary.md` match the round's report verbatim, including both digest SHAs; `git log` in that fixture shows the exact two commits (`ecd3b2d`, `b8e0a46`) with matching messages; the actual `internal/greet/greet.go` in that worktree contains both `Hello` and `Farewell` with `Hello`'s doc comment pointing at `Farewell`, exactly as narrated; both fork transcripts (`agent-aaa20bbbfdc139964.jsonl`, `agent-ac40a19ac982c0765.jsonl`, ~44KB each) exist as real files under `~/.claude/projects/`.
- **Substrate teardown independently confirmed:** zero live `tmux`/`lyx` processes; the only two live `claude` processes on the host started at 11:03 and 11:09 (before round 5 began), matching the round's own teardown note exactly.
- **Nothing failed.** This is the first round whose live claims this orchestrator could verify entirely firsthand on the same host, with zero artifact-substitution gap.

**Full detail:** `_mill/loom-review-opus-medium-r5.md`, `_mill/loom-review-opus-medium-r5-fixer-report.md`.

### Round 6 (opus-medium-r6) — CLOSED-AND-VERIFIED

**Findings:** 28 total (2 BLOCKING, 10 MEDIUM, 10 LOW, 6 NIT). All 28 fixed, one commit each, on top of `12f865288`.

- **R6-1 (BLOCKING, `3c0fcb2a9`)** — round 5's own R5-2/R5-7 fix, read from the other side. `Startup` matched its gate needles against the WHOLE pane capture with no requirement that a gate actually be rendered, so a healthy, working pane whose own transcript happened to contain ordinary phrasing like "trust this folder" was misclassified as a gate — `wait.go` then played `Up`/`Down`/`Enter` into a live agent mid-turn, or (the other branch) a healthy run was classified `OutcomeDied` at the startup deadline. Fixed by requiring POSITIVE evidence a dialog is rendered — a locatable accepting-option line, or claude's own `Enter to confirm` gate footer — shared between `Startup` and `TrustDismissSequence` via one `locateGateLines` helper so the two can never disagree.
- **R6-3 (BLOCKING, `6d1d97e7a`)** — on the glyph surface: `containment-file-overlap` built both sides of its overlap test from an index that included read-only `Uses:` refs (not just `Targets`), so two cards that merely READ overlapping things emitted a `SeverityBlocking` finding, refusing `lyx webster run` outright and every dispatch after it. Fixed by giving `resolveContainment` a targets-only index (`writingTargetCards`), leaving the Targets+Uses+Pairs index (`targetCards`) untouched for `statusFindings`/`DetectDrift`, where it is correct.
- **R6-2 (MEDIUM, `54940630c`)** — `gateAcceptNeedles` was missing `yes,proceed`, the older trust-gate wording `Startup`'s own fixture already recognized as a GATE but could never actually dismiss — every agent hitting that wording died at the startup deadline.
- **R6-4 (MEDIUM, `674da4a9f`)** — `persistPlanFingerprintRebaseline` saved the caller's whole in-memory `*State`, so on the `record-batch` path a re-baseline that ran after `SeenForkTranscripts` was appended, but before a later step failed, persisted the transcript as consumed on a call that itself failed — wedging resume in the three-verb refusal circle. Fixed to persist only `PlanFingerprint`, under the still-held lease.
- **R6-6 (MEDIUM, `1d56d9014`)** — an early standalone refusal (anything before `wireStandalone`'s log-sink redirect) fell through to `logger`'s cwd-anchored fallback, which writes `<repo>/.lyx/logs/trace-*.log` into ANY plain git repo standing at its root — including the operator's own checkout, on a mistyped subcommand. Fixed: the fallback now arms only when the resolved location is a worktree lyx owns (`isLyxWorktree`).
- **R6-7 (MEDIUM, `5a25c9fe5`)** — `--target-dir` was never stat'd before `resolveStandaloneTarget` climbed to a `.git` root, so a typo'd flag silently retargeted the enclosing repository the operator was standing in, with nothing in the output distinguishing that from the no-flag invocation. Fixed to stat and refuse an absent path or non-directory, in both `webstercli` and `burlercli`.
- **R6-8 (MEDIUM, `07da97a31`)** — standalone's `run` refused a moved `--plan-dir` (round 3's F-A3 hardening); `wireHub` never made the same check, so hub mode reproduced the identical failure. Fixed to detect the override in hub mode too.
- **R6-9 (MEDIUM, `13f20e3dd`)** — the standalone missing-plan refusal told the operator to pass `--plan-dir`; `run` itself then refused that exact flag with a contradictory message. Fixed to name the default plan location first.
- **R6-26 (MEDIUM, `aac237c14`)** — `shedbuild.Parse` decoded only the first YAML document in a recipe file, silently truncating the producer graph at a stray `---`. Fixed to refuse a second document.
- **R6-27 (MEDIUM, `88cf01e8e`)** — `hasBlockingFinding`/`planFindingsHaveBlocking` tested `== SeverityBlocking`, so an unrecognized or zero `planglyph.Severity` failed OPEN — the exact disposition the same file's own comment says was deliberately rejected for the error path. Fixed to test `!= SeverityInformational` in both halves of the parity pair, failing closed.
- **R6-28 (MEDIUM, `345218ca5`)** — `discussionparser.missingSections` used an unchecked `bufio.Scanner`, so one line over 64 KB silently stopped the scan and reported every heading below it as missing — a document that was valid all along. Fixed to check `scanner.Err()`.
- **R6-5, R6-10 through R6-25 (10 LOW), R6-19 through R6-24 (6 NIT)** — narrower correctness/doc fixes across `planparser`, `planglyph`, `shuttleengine`, `standalonestate`, `websterengine`, `CONSTRAINTS.md` and `loom-plan-spec.md`; see the review report for the full list.

**Live substrate was entirely BLOCKED for the round itself** — every `tmux`, `lyx reed up`, and `go run ./tools/deploy` invocation was refused by the round's own permission classifier. All 28 fixes were proven hermetically and by reading; the round explicitly flagged R6-1/R6-2 as provider-UI-facing and deserving one live confirmation before the campaign closes.

**Independent verification (this orchestrator):**
- **All 28 findings verified.** 12 sabotage-proofed firsthand by a dedicated verification fork (both BLOCKING plus all 10 MEDIUM: R6-1, R6-2, R6-3, R6-4, R6-5, R6-6, R6-7, R6-8, R6-9, R6-26, R6-27, R6-28) — production hunk reverted, named test fails at the intended assertion, restored, tree confirmed clean after each. R6-15 (LOW, the symlinked-state-home nested-geometry guard — security-relevant, so given the same treatment as a MEDIUM) sabotage-proofed directly by this orchestrator after the verification fork finished with the shared file. The remaining 9 LOW (R6-10 through R6-14, R6-16, R6-17, R6-21, R6-22) confirmed by running each named regression test fresh and watching it pass; the 6 pure-doc/no-behavior-change items (R6-18, R6-19, R6-20, R6-23, R6-24, R6-25) confirmed by reading each commit's diff against its claim.
- **Two real (minor) coverage gaps surfaced during sabotage-proofing, not correctness bugs** — worth carrying forward, not silently patched by this orchestrator (crucible discipline: verify and record, do not self-fix): R6-6's cited test (`TestIsLyxWorktree_GatesTheCwdAnchoredFallback`) exercises the `isLyxWorktree` helper directly, never the actual `armDurableSinkLocked` call site the fix lives in — the fix itself is correct on direct code reading, but its regression test doesn't cover the real wiring point. R6-27's `loomcli` twin (`planFindingsHaveBlocking`) has no dedicated regression test at all — verified only by reading, confirming it is the identical fix applied identically to `loomshed`'s.
- **A genuine live confirmation of R6-1/R6-2, obtained where the round itself could not.** This orchestrator confirmed `tmux`/`claude` both work in this session (unlike the round's) and drove a real scenario on a brand-new fixture (outside this worktree, never `r4-*`/`r5sandbox*`): let claude's real one-time trust-folder gate render, fed the real capture through the actual `Startup`/`TrustDismissSequence` functions, played the exact returned key sequence into the live tmux pane, and confirmed the gate was genuinely dismissed end to end. Then, with the agent healthy and mid-turn, prompted it to state the trust-gate's own wording — it answered `"Do you trust the files in this folder?"`, a real, unscripted transcript containing the exact `filesinthisfolder` gate needle, i.e. the R6-1 hazard occurring naturally. Fed through the same functions: `StartupReady`, empty dismiss sequence — confirmed live, under real conditions, that the fixed code no longer presses keys into a working agent. (Under the pre-fix needle-anywhere rule this exact real capture would have been misclassified and dismissed with real keystrokes — the mechanism R6-1 closes, now disproven live, not just hermetically.) Throwaway test file deleted, tmux server killed, fixture removed, zero stray processes confirmed afterward.
- **All cold gates re-run clean from scratch:** `go build ./...`, `go vet ./...`, `go test -count=5 ./...` (80 packages, zero flakes across 5 runs), `go test -tags integration` over the round's 7 named packages. Tree confirmed clean (`git status --short` empty, `git diff --quiet`) both mid-verification and at the end; HEAD unchanged at `d2e03acb9`.
- **One process note, not a defect:** two independent verification forks worked this branch concurrently at one point and both touched `internal/webstercli/wiring.go` in overlapping windows (this orchestrator's own R6-15 sabotage-proof collided with the dedicated fork's R6-7/8/9 work on the same file). Caught immediately via `git status`/`git diff`, no edits were lost or silently merged, and the orchestrator serialized the remaining work after that — but it is a real coordination hazard for any future round that parallelizes verification across files a single fix touches from multiple angles.

**Full detail:** `_mill/loom-review-opus-medium-r6.md`, `_mill/loom-review-opus-medium-r6-fixer-report.md`.

### Round 7 (fable-high-r7) — CLOSED-AND-VERIFIED

**Effort/model note:** Fable/high, the operator's own deliberate choice — after four straight Opus deployments (r1/high, r4/high, r5/medium, r6/medium), the operator judged the round needed a stronger tier than Opus/medium and picked Fable/high for model diversity (the fresh-model-angle argument raised after round 5, not taken up until now). This is the campaign's second Fable deployment; round 3 (fable-high-r3) was the first and found 2 real BLOCKING defects in territory two prior rounds had already "closed" — the same shape repeated here.

**Mission:** a genuine adversarial pass over the whole surface, PLUS the first-ever crucible review of `unify-webster-burler-wiring` (`7e54cc280`) — new package `internal/cliwire`, absorbing six round-6 wiring-duplication findings (R6-7/8/9/15/16/17) into one shared module for `webstercli`/`burlercli`.

**Findings:** 6 total (1 BLOCKING, 1 MEDIUM, 2 LOW — including the two seeded coverage gaps D1/D2 — 1 NIT). All 6 fixed, one commit each, on top of `4df538ef6`.

- **F1 (BLOCKING, `963ba557c`)** — round 6's own R6-1 fix, narrowed but not closed. R6-1 required "positive evidence a dialog is rendered" (a gate needle plus a locatable accepting-option line or claude's gate footer) but never required those evidence lines to sit near each other or near the caret. A single line of ordinary agent prose beginning with an accept phrase (`- Yes, I accept the risk of X`) or containing "press Enter to confirm" still supplied both the needle and the "evidence" — so a healthy pane got `Up,Up,Up,Enter` pressed into its own input box (arrow-key history recall + submit) from prose alone, or was killed `OutcomeDied` at the startup deadline with Send/Interrupt refused throughout. Fourth consecutive round to find real material in this exact seam (r4 fingerprint wedges aside — r5, r6, r7 all in-or-around provider-startup). Fixed with an adjacency requirement (accept line ≤1 line from the last caret, footer ≤4 lines below it — calibrated against the live-transcribed gate fixtures from rounds 5/6) shared between `Startup` and `TrustDismissSequence` so they can never disagree, same principle R6-1 itself established.
- **F2 (MEDIUM, `9e20c46f3`)** — `--stencils-dir` was the one told-directory flag `cliwire`'s consolidation never stat'd at the wiring boundary (unlike `--target-dir`/R6-7 and `--plan-dir`), in both modes, both CLIs — a typo was accepted silently and surfaced only at the first stencil read, after the run lock, quarry resolve, and (standalone) reed's tmux boot. Fixed via a shared `cliwire.Module` method called from both hub wirings and `ResolveStandalone`'s override branch.
- **F3 (LOW, `a9c706a93`)** — `callerset_enforcement_test.go`'s whole-repo `standalonestate.Derive` caller-pin only walked `internal/`+`cmd/`, missing `tools/` or any future top-level Go dir, and matched selector expressions only (a dot-import would hide every call). Verified genuinely real by constructing a throwaway `Derive` caller under `tools/` — pre-fix scan missed it, post-fix scan catches it. Fixed: whole-repo walk + dot-import ban.
- **F4 (NIT, `b9c77ef27`)** — `bannedecl_enforcement_test.go`'s nine-name ban list covered the old webstercli/burlercli spellings but not cliwire's own current helper names, so re-declaring a helper under cliwire's own naming would slip past. Fixed: current names added, residual documented.
- **D1 (LOW, `64b7b452d`)** — closed the coverage gap this orchestrator's own round-6 verification flagged: R6-6's regression test drove `isLyxWorktree` directly but never the real `armDurableSinkLocked`/`ensureDurableSink` call site. New integration test drives it end-to-end (plain git checkout → no `.lyx` created; lyx-owned worktree → trace file lands correctly).
- **D2 (LOW, `6c218891a`)** — closed the other seeded gap: `loomcli.planFindingsHaveBlocking` (R6-27's twin) now has its own dedicated fail-closed test, not verified only by "the two bodies look identical."

**Independent verification (this orchestrator, via a dedicated verification fork):**
- **Cold gates, full run, from scratch, all green:** `go build ./...`, `go vet ./...`, `go test -count=5 ./...` (82 packages, zero FAIL, zero flakes), `go test -tags integration` over the 9 named packages.
- **6/6 sabotage-proofs PASS.** F1 is the strongest possible confirmation available short of a live pane: reverting `startup.go` reproduces the EXACT hazard — `TrustDismissSequence` returns the real `Up,Up,Up,Enter` sequence against the prose-only fixture, i.e. the verification watched the actual keypress sequence that would have been typed into a live agent's input box, not just a boolean misclassification; both `Startup` classification tests fail at the intended assertion. F2: all 4 named tests fail without the fix (fall through to an unrelated refusal or `nil`). F3: genuinely reproduced by construction — a planted `tools/`-dir `Derive` caller passes the pre-fix scan and fails the post-fix one (fixture removed after). F4 confirmed correct by reading (a straightforward map addition). D1/D2 confirmed to genuinely exercise their real call sites (not vacuous), green at HEAD.
- **Teardown confirmed clean:** zero `lyx-*` tmux servers; the round's own report records finding and killing 18 PRE-EXISTING leaked tmux servers on this host from the `unify-webster-burler-wiring` task's own Sep-7 development runs (OBS-1, not this branch's code — HEAD verified non-leaking) — this orchestrator's fork independently confirmed zero remain.
- **Spot-check:** `writingTargetCards` (R6-3) re-confirmed still Targets-only.
- **What was NOT independently re-driven live:** F1's fix was not re-confirmed against a real fresh-fixture claude pane (round 7 itself didn't drive one either — its fix is calibrated against rounds 5/6's own live-transcribed captures and proven via the exact-keypress-sequence sabotage-proof above, which this orchestrator judges strong but not identical to a live pane). No sandbox hub was built this round or in verification — the hub-mode halves of R6-7/8 and F2 are hermetic-only, same limit as before.

**Full detail:** `_mill/loom-review-fable-high-r7.md`, `_mill/loom-review-fable-high-r7-fixer-report.md`.

## Convergence status — NOT yet declared; operator decision needed

Round 4 was the last round in the pre-approved four-round budget. It was NOT a clean safety pass — it found and fixed 2 more genuine BLOCKING bugs (R4-01, R4-02, both fingerprint-restamp wedges) that three prior rounds, across three different models (Opus r1, Sonnet r2, Fable r3), all missed. The operator authorized round 5 (Opus/medium) beyond the original budget to close the remaining verification gap and attempt a genuine clean pass.

**Round 5 was ALSO not a clean safety pass.** It found and fixed 2 MORE genuine BLOCKING bugs (R5-2, R5-7) — this time on the single hottest live path in the whole module (every agent lyx spawns goes through claude's startup-gate sequence), missed by all four prior rounds for a specific, structural reason: the defects are invisible on any fixture claude has ever been driven in by hand, and every prior round reused or inherited such a fixture. Round 5's own R5-7 was reachable ONLY after R5-2 was fixed — a defect hidden behind another defect on the same path. This orchestrator's independent verification (see above) found round 5's fixes themselves fully solid — 5/7 sabotage-proofs firsthand (including both BLOCKING), all cold gates green, and for the first time in this campaign the live crash-kill/resume claim verified entirely firsthand from real on-disk artifacts, with zero substitution gap.

Per the fabric campaign's own precedent, true convergence looks like a round finding nothing severe (fabric's R6: "no BLOCKING and no MEDIUM — first round to meet the bar"), not a round finding-and-fixing severe things, however well the fix is verified afterward. **This campaign is now 7 rounds deep, across six different model/effort combinations, and every single round has found real material — five of the seven found BLOCKING defects, and rounds 4, 5, 6, and 7 form an unbroken run of FOUR.** Every one of those four is in or around the provider-startup seam's evolving fix lineage (round 4's fingerprint wedges aside): round 6 found its BLOCKING material (R6-1) inside the exact code round 5 shipped to fix R5-2/R5-7, and round 7 found ITS BLOCKING material (F1) inside the exact code round 6 shipped to fix R6-1 — two consecutive instances of a fix's own successor round finding a hole in it. Round 7's own read on this, worth weighing directly: each iteration is a strict narrowing (R6-1's whole-capture matching → R7-F1's missing-adjacency), and — for the first time in this run of four — everything OUTSIDE that one seam, including the brand-new `cliwire` merge this round reviewed for the first time, came back clean under genuine adversarial pressure. Round 7's own verdict: "NOT MET — but the trajectory has changed shape."

**Decision needed from the operator: whether to commission round 8, and if so, with what model/effort/scope.** This orchestrator is not making that call unilaterally — same discipline as every prior round's decision. Observations to weigh, not a recommendation:
- Round 7 itself suggests a NARROWER option the campaign hasn't used before: a targeted, seam-only re-check of `internal/shuttleengine/claudeengine/startup.go` (one file) rather than a full round-8 sweep of the whole surface — on the theory that the rest of the glyph/cliwire surface now looks converged by two consecutive rounds' evidence (round 6's general sweep found nothing outside the seam+cliwire-adjacent items; round 7's found nothing outside the seam either, and cliwire itself is now verified sound). This would be a genuine departure from "one concern per round" being satisfied by a full crucible round — worth an explicit operator call, not an assumption.
- If a full round 8 is preferred instead: model diversity has now been tried in both directions this rotation (opus-medium ×2 as r5/r6, fable-high ×2 as r3/r7) without breaking the startup-seam pattern — an untried combination (Sonnet, or Opus/xhigh-or-max, per your earlier "kraftigere" instinct) is the remaining unexplored axis if the operator wants one more full pass before considering a seam-only approach.
- Two coverage gaps from round 6 (R6-6, R6-27-twin) are now closed (round 7's D1/D2); no new coverage gaps were seeded this round to carry forward, beyond F3/F4's own newly-fixed enforcement blind spots (already closed in the same round).
- The `unify-webster-burler-wiring` side-task material is no longer "landed and unreviewed" — round 7 gave it a first, thorough pass. It does not need a dedicated future round; ordinary re-reads as part of any future full sweep are sufficient.

**Honest limits, unchanged or newly added this round:**
- Windows path behavior has never been reachable from any host this campaign has used (Linux throughout) — R6-15's case-fold half and `cliwire`'s mirrors of it are mechanical, never driven.
- `burlercli`'s standalone reed bring-up remains live-unverified, as in every prior round.
- `DetectDrift`'s exact-tier auto-repair path live through a real Webster fork has still never been triggered by any round's live driving (open since round 3).
- **F1's fix was not re-confirmed against a real fresh-fixture claude pane** — neither round 7 nor this orchestrator's verification drove one; the sabotage-proof reproduced the exact keypress sequence that WOULD have been typed into a live pane, which is strong evidence but not a substitute for an actual live confirmation the way this orchestrator obtained for R6-1 in round 6. This is the single most valuable thing a live-capable session could still add before the startup seam is called done.
- No sandbox hub was built in round 7 or in this orchestrator's verification of it — the hub-mode halves of R6-7/8 and F2 remain hermetic-only, unchanged from prior rounds' limits.
- The ~45-item residue from round 6's sweep over loom's pre-glyph pipeline machinery (mostly doc drift, some non-blocking correctness items) remains recorded but deliberately out of this campaign's declared scope — a separate mill-wiki task, not folded into this campaign.

## `#004` mill-task gate — CLEARED (corrected understanding)

**`#004` `standalonegeom-webster-run-and-log-hygiene` completed the full mill flow (plan → go → holistic-approve → done) and squash-merged to its PARENT — which is THIS crucible branch (`crucible-loom-glyph-hardening`), not `main`.** This orchestrator initially misread a closed-but-unmerged GitHub PR (`#231` on `Knatte18/loomyard`) as the task having stalled; the operator corrected this: the standard loomyard mill procedure for this task was close the PR + `mill-merge` (squash to parent), and this crucible session was deliberately set as `#004`'s parent when it was spawned (since `#004` is itself a product of this crucible campaign's own findings, F16/F22 — it belongs to this campaign's tree, not to `main` directly). The fix landed here as commit `d7c52df6c` ("webster standalone mode: run refuses to start Master; logs write untracked into target repo") — 18 files, 879 insertions, including a new `CONSTRAINTS.md` invariant line: a `shuttleengine` runner whose anchor is deliberately outside its worktree root is now constructed only through the new `shuttleengine.NewDetachedRunner`, exclusively from standalone CLI wiring — `NewRunner`'s containment assertion (the shared safety invariant four other callers rely on, per round 1's investigation) is never relaxed. This is exactly the "separate constructor" resolution this orchestrator's own earlier investigation anticipated as one legitimate design option.

**Consequence: round 3's mission is now EXPANDED, per the operator's explicit instruction.** Since this fix landed inside THIS branch/session's own tree, it is now in-scope material this campaign must also harden before the branch merges to `main` — it has never itself been through a crucible round. F16/F22 are no longer "permanently out of loom's scope" as rounds 1/2 stated; that framing is now stale (see "Explicitly OUT of scope" correction needed in the next seed).

## Round 1 (opus-high-r1) — CLOSED-AND-VERIFIED

**Timing:** 78.4 min wall-clock, 364 tool calls, ~564k tokens. Job 1 (review): ~31 min. Job 2 (fix): ~34 min. Post-fix re-verification + reports: ~12 min.

**Findings:** 22 total (13 BLOCKING, 5 MEDIUM, 3 LOW, 1 NIT). 20 fixed across 14 commits (`c2b64e001`..`9726310b6`). 2 recorded, deliberately NOT fixed: **F16** (standalone `lyx webster run` can't start Master — `shuttleengine.NewRunner`'s anchor/worktree containment check structurally refuses `standalonegeom`'s deliberately-divergent geometry) and **F22** (standalone mode writes untracked `.lyx/logs/` into the target repo) — both out of loom's own scope (loom never constructs `standalonegeom`, confirmed by code, not just asserted).

**Headline:** the glyph mechanics themselves tested fine in isolation, but the integration surface was broken hard — no multi-batch plan with a `Create`/`Delete`/`Rename` card could ever complete through Webster (root cause: whole-plan re-resolution against the post-change tree on every batch dispatch, F5/F6/F7/F21). Post-fix, a three-batch hand-authored plan with a handle-`Create` and a declared `Rename` ran end-to-end for the first time ever, via webster's zero-LLM-cost standalone probe harness (round 1 could not reach a real hub-mode `lyx loom run` — three environment gaps, two standalone-only).

**Independent verification (this orchestrator):** 9/9 sabotage-proofs passed (every BLOCKING finding's commit, plus one MEDIUM/LOW spot-check). All gates green cold. The one design-shaped change (`c6ee9eb16`, two new `internal/planglyph` exported functions `ValidateDispatch`/`PendingPlan`) confirmed correctly scoped — called only from the two sites it fixes, Gate Self-Check Parity Invariant's named functions provably unchanged. F16/F22 confirmed genuinely unreachable from loom by grep (`internal/loomcli`/`loomengine`/`loomshed`/`loomrecipe` never construct `standalonegeom`). All three doc fixes (F13/F15/F19) confirmed landed correctly. Teardown clean. **Nothing failed.**

**Mill-wiki task opened for F16/F22** (Hard Rule 5): `#004` `standalonegeom-webster-run-and-log-hygiene`, via the sanctioned `.millhouse/millpy-add` wrapper. Investigated on the operator's direct question ("are these really large enough for their own task?") — confirmed yes for F16 specifically: `shuttleengine.NewRunner`'s containment check is a shared safety invariant with FOUR callers outside webster (`burlercli`, `webstercli`, `shuttlecli`, `loomcli`), so relaxing it for standalone geometry is a genuine cross-module design decision, not an inline patch. Spawned as its own worktree; currently in progress (see "Blocking gate" above).

**Full detail:** `_mill/loom-review-opus5-high-r1.md`, `_mill/loom-review-opus5-high-r1-fixer-report.md`.

## Round 2 (sonnet-xhigh-r2) — CLOSED-AND-VERIFIED

**Mission:** round 1 proved every glyph mechanism through hand-authored plans and direct bracket-verb calls; round 2's job was to prove the same mechanics survive contact with a REAL orchestrator — a real `lyx loom run`, hub mode, real LLM sessions, a plan authored by a real `Plan-Write` session, not hand-edited.

**Result — this succeeded spectacularly.** The round set up a real disposable sandbox hub (`/home/knatte/Code/lyx-test-HUB`, warp repo `github.com/Knatte18/lyx-test`), fixed the stale-PATH-`lyx` hazard round 1 flagged (reinstalled from current HEAD to both `go/bin` and `.local/bin`), and drove a genuine `lyx loom run` all the way from `Preflight` through `Webster-Review` (APPROVED) with **zero glyph-surface defects blocking it anywhere** — the first time loom's real phase machine has ever carried a glyph-bearing plan this far — then reached `Publish`, which opened a real GitHub PR and correctly halted at that human-review gate (loom's own designed boundary, not a wedge).

**Findings:** 7 total, **0 BLOCKING**, 2 MEDIUM, 2 LOW, 3 NIT — all 7 fixed (`06977e1ca`..`0fcbfee98`, plus fixer-report commit `fb29b6b22`). All residual doc/stencil drift (stale mechanical-check counts, a terminology slip, a missing canonical check-ID list that nearly caused a live review round to mis-fire) plus one real Master-prompt gap (no scripted response to a round-1-introduced refusal shape) and one narrow format-checker widening. Two findings are code-shaped (F-parse1, F-webster1) with new tests; five are pure doc/stencil text.

**Independent verification (this orchestrator) — the real-PR/real-hub claim was the priority, per "prove the scenario reached the code":**
- `gh pr view 1 --repo Knatte18/lyx-test` confirmed a REAL PR: exact title/branch match, body is real Master-written content, `commits` field lists exactly the two SHAs the round claimed (`f235810`, `ed1263a`).
- The sandbox hub and its `glyph-demo-greet` worktree still exist on disk; both commits are real, on top of the real pre-existing HEAD, and their content matches the plan cards exactly (`FormatGreeting` function + table-driven test in one commit, `main()` wiring in the next).
- **One discrepancy found, now resolved:** `gh pr view` showed the PR as **CLOSED (unmerged)**, contradicting both reports' "remains open" claim. **Operator confirmed they closed it themselves** (deliberately not merged — the sandbox hub is disposable test infrastructure). Just a stale fact in both reports (closed mid-round, after the review-notes commit but before the final fixer-report commit, which never re-checked) — not a code defect, not a process failure, nothing further to investigate.
- 2/2 sabotage-proofs passed (F-parse1, F-webster1). All gates green cold (`go build`/`vet`/`test -count=5` on the 11 in-scope sets, `-tags integration` on 5 sets, whole-repo `go test` — 81 packages, 0 FAIL, `TestEnforcement_MarkdownLinks` green). All doc-fix text confirmed landed correctly, quoted. Teardown clean.

**Full detail:** `_mill/loom-review-sonnet5-xhigh-r2.md`, `_mill/loom-review-sonnet5-xhigh-r2-fixer-report.md`.

## What remained open after round 2 — CLOSED by round 3

- ~~A declared `Rename` card executing through a real Webster batch, live~~ — **CLOSED.** Round 3's real `Plan-Write` session authored two declared Rename cards against a pre-existing symbol; a real Webster fork executed both; the pipeline ran to `Finalize → done`. Independently confirmed via the real commit (`0d26ff0`) in the sandbox hub.
- ~~`Finalize` — never reached~~ — **CLOSED.** First live `Finalize → done` completion, same run as above.
- `DetectDrift`'s exact-tier auto-repair path specifically, live through a real Webster fork — still not triggered by any round's live driving (no out-of-band rename occurred in any live run yet); remains verified only at round 1's standalone-rig level. Candidate for round 4 if a genuinely adversarial pass wants new live territory.
- PR #1's actual disposition — resolved (operator confirmed they closed it deliberately, unmerged; sandbox test infrastructure).

## What round 3 leaves open (deliberately deferred, not fixed)

- Full `--plan-dir` override propagation into Master's in-pane verb invocations (standalone mode) — a cross-cutting design decision touching reed's spawn environment; F-A3's loud refusal covers the operator today.
- `lyx reed` standalone support (attach/status against a derived-geometry session) — operator can reach it via raw `tmux -L lyx-<hash8> attach` today; its own module task if wanted.
- `burlercli`'s standalone reed bring-up was wired but not live-verified this round (no standalone burler scenario was in scope) — same seam as `webstercli`'s, which WAS live-verified.

## Round 3 (fable-high-r3) — CLOSED-AND-VERIFIED

**Two missions, both delivered.** Mission A: `#004`'s own fix (`d7c52df6c`) only partially solved F16 — three more BLOCKING layers were live-found and fixed (F-A1: no reed session existed for standalone Master to spawn into; F-A3: a `--plan-dir` override spawned a Master that could never see its own plan; F-A4: every rendered prompt hardcoded hub-relative paths). Result: the first-ever completed standalone `lyx webster run`. Mission B: two real BLOCKING defects two prior rounds missed (F1: file-rename plans failed validation on a false `glyph-not-found`; **F2: `websterengine.Run` validated the WHOLE plan at entry, unscoped by completed cards, permanently wedging any resume after a landed Create/Delete/Rename card — hits hub mode too, since loom's own Webster row re-invokes `Run` on shed resume**), plus the Rename-through-Webster gap CLOSED live all the way to `Finalize → done` (first-ever, closing round 2's residual too).

**Counts:** 12 findings — 5 BLOCKING (F1, F2, F-A1, F-A4, F-B8), 3 MEDIUM (F3, F-A3, F-B7), 3 LOW (F1b, F4, F6), 1 NIT (F5) — all fixed across ~15 commits (`c2bcaa4e1`..`7f16dde7e`, docs `4acbdea4d`, reports `85a941cef`/`43278a098`).

**Independent verification (this orchestrator) — the most thorough of the three rounds so far:**
- **10/10 sabotage-proofs PASS** (every finding: F1+F1b, F2, F3, F4, F6, F-A1, F-A3, F-A4, F-B7, F-B8) — production hunk reverted, regression test fails at the intended assertion (or, for two multi-file signature changes, fails to compile without the fix — an even stronger proof), restored, empty diff confirmed each time.
- **The decisive check, going beyond what the round itself proved**: the operator specifically raised whether crash-resilience was proven by a genuine process death or just a clean re-invocation. Round 3's own F2 proof was the latter (re-running `lyx webster run` over an already-completed run). This orchestrator's verification instead built a real 2-card Create plan, ran a real standalone webster session, and **`kill -9`'d the driver AND the tmux server hosting the live Master `claude` process** the moment batch 1's commit landed (confirmed via `pgrep` catching the live process before the kill, confirmed gone after; `state.json` showed batch 1 done, batch 2 never started, **no `outcome.yaml` written** — proof of an unclean death, not a graceful stop). Resuming with a plain `lyx webster run` did NOT refuse — it continued and completed batch 2 cleanly. **This is genuine, reproduced crash resilience, independently confirmed with a real kill, not narrative.**
- **Both headline live claims confirmed genuine via real artifacts still on disk**: the standalone E2E's commits (`00489fe`/`cefc499` in `/home/knatte/Code/lyx-r3-standalone`, `func Farewell` present, target `go test` green) and the hub Finalize run's commit (`0d26ff0` in `/home/knatte/Code/lyx-test-HUB/glyph-rename-format`, real rename diff, zero `FormatGreeting` occurrences left, genuinely pushed to `origin/glyph-rename-format`).
- All cold gates green (`go build`, `go vet` on the 16-package expanded set, `go test -count=5`, `-tags integration` on 7 packages, whole-repo `go test ./...` — 81 ok). `docs/overview.md:303` confirmed carrying the new webster-standalone behavior text. All 7 regression spot-checks against rounds 1+2's fixes (r1-F1, r1-F4, r1-F9, r1-F17, r1-F18, r2-F-parse1, r2-F-webster1) confirmed present and unregressed at current HEAD. Teardown clean.

**Nothing failed. Round 3's 12 fixes are genuinely closed — this is the strongest verification pass of the campaign so far, precisely because it went one step past what the round itself proved on the crash-resilience question.**

**Full detail:** `_mill/loom-review-fable5-high-r3.md`, `_mill/loom-review-fable5-high-r3-fixer-report.md`.

## Round 3's seed, as spawned (historical record — round 3 is now closed, see above)

**A) Review the newly-merged `#004` fix (`d7c52df6c`) for the first time — it has never been through a crucible round.** Read the full diff (`git show d7c52df6c`). Files: `internal/shuttleengine/run.go` (new `NewDetachedRunner`, containment assertion untouched for `NewRunner`), `internal/shuttleengine/wait.go`, `internal/standalonegeom/logsdir.go` (new — the `.lyx/logs/` fix for F22), `internal/standalonegeom/doc.go`, `internal/webstercli/wiring.go`, `internal/burlercli/wiring.go`, `internal/logger/sink.go`, `CONSTRAINTS.md` (new invariant line, quoted above). Be adversarial: this is unreviewed code from outside the original glyph-hardening scope, landed via a different task's own mill-go pipeline, not via this campaign's own discipline. Live-verify F16 and F22 are ACTUALLY fixed now: a real standalone `lyx webster run --target-dir <repo> --plan-dir <dir>` should now actually start Master (previously refused outright); standalone mode's `.lyx/logs/` should no longer appear as untracked in the target repo's `git status`. **This is a genuinely separate LLM-driving live scenario from loom's own hub-mode work** — budget for it the same way (one real session at a time, foreground, cost declaration applies) since starting Master standalone was exactly what was broken before.

**B) Continue hardening loom's own glyph integration surface**, informed by round 2's "What remains open" above: rounds 1+2 closed 27 findings (0 BLOCKING remaining), and the primary hub-mode-run mission already succeeded — so round 3 is shaped as a genuine safety pass over loom's own glyph surface, PLUS an attempt to close the one interesting remaining live gap if time allows: a real declared `Rename` card executing through a real Webster batch (seed a second live run from a worktree with a pre-existing symbol to rename, rather than a fresh `main`-derived one, so `Plan-Write` has a rename-shaped task to work with).

1. Rewrite `_mill/loom-review-prompt.md` for round 3 covering BOTH missions above. Correct the stale "F16/F22 permanently out of loom's scope" framing from rounds 1/2's seeds — that fix is now IN scope, having landed in this same branch.
2. List rounds 1 and 2's 27 fixes as CLOSED-AND-VERIFIED (this file has the evidence) so round 3 does not re-litigate them.
3. Spawn `crucible-reviewer-high`, `model: fable`, tag `fable-high-r3`.
4. Verify independently exactly as rounds 1 and 2 were verified (sabotage-proofs, cold gate re-runs, live-claim verification via real artifacts, not narrative) — do not relax the bar for either mission.
5. If round 3 converges clean on both missions, the pre-approved rotation calls for one more round (Opus/high, final safety pass, r4) — but per the README's own guidance, convergence (a safety pass + this orchestrator's gates + an operator-assisted check all agreeing) can close the campaign before using all four rounds. Surface merge-readiness and let the operator decide whether r4 is still wanted.

## Next action — seed round 4 (Opus/high), with an operator-raised recalibration

**Round 4 is the pre-approved final safety pass in the rotation (Opus/high, capping the four-round budget).** Before seeding it, the operator raised a real methodological point worth encoding into the seed, not just remembering: **round 2 ran on Sonnet, and Sonnet's own "0 BLOCKING" result is weaker evidence of convergence than the same result from a more capable model** — precisely why crucible rotates models and puts the strongest one last. Round 3 (Fable) already found 2 real BLOCKING defects (F1, F2) in territory round 2 never actually drove (file-rename plans, mid-plan resume) — which doesn't yet prove Sonnet missed anything WITHIN its own tested territory (the real hub-mode run, handle canonicalization, Plan-Review, Webster-Review), but it does mean that question is still genuinely open.

**Consequence for round 4's seed: it must NOT be a formality "confirm merge-readiness, no known residual" pass.** It should be a genuinely adversarial full pass over EVERYTHING — including round 2's own territory that no prior round has re-examined skeptically — not just the residuals rounds 2/3 happened to leave named. Concretely, round 4's high-yield focus should include:
1. A genuinely skeptical re-look at round 2's own hub-mode run and its handle-canonicalization/Plan-Review claims — not re-running the same scenario, but reading the code paths round 2 exercised with fresh, adversarial eyes, the way round 3 did for Mission A's supposedly-already-reviewed `#004` fix.
2. `DetectDrift`'s exact-tier auto-repair path live through a real Webster fork — still never triggered by any round's live driving (see "What round 3 leaves open" above).
3. Whatever a third-model, fourth-round pass turns up that three prior passes (Opus, Sonnet, Fable) across two different effort tiers didn't.
4. A genuine crash-resilience sweep beyond F2's specific fix — this campaign's crash-recovery evidence is now strong for ONE scenario (kill mid-Webster-batch, standalone) but round 4 could extend it: kill mid-batch in HUB mode specifically (not yet done with a real kill — round 3's kill test was standalone), and/or kill during a review segment (Bouncer/Burler round) on a glyph-bearing plan.

List rounds 1, 2, and 3's now-CLOSED-AND-VERIFIED findings (this file has the full evidence for all three) so round 4 does not re-litigate what's already proven — but "already proven" should mean "independently verified," not "self-reported," and round 2's territory has only ever been independently verified for the SPECIFIC fixes it made, never adversarially re-reviewed the way rounds 1's and 3's own material has now effectively been (each round implicitly re-examines the prior rounds' surface as part of driving live scenarios through it).

1. Rewrite `_mill/loom-review-prompt.md` for round 4 incorporating the above.
2. Spawn `crucible-reviewer-high`, `model: opus`, tag `opus-high-r4`.
3. Verify independently exactly as rounds 1–3 were verified — sabotage-proofs, cold gate re-runs, real-artifact checks for any new live claims. This is the FINAL round in the pre-approved budget — hold the bar at least as high as round 3's, which was the most rigorous so far (the real kill-9 crash test set a new standard; match or exceed it for whatever round 4's own headline claims turn out to be).
4. If round 4 comes back clean (a genuine adversarial pass finding nothing, or finding and fixing only minor residue) and this orchestrator's own verification agrees: the campaign has converged. Per the README's own convergence bar, surface merge-readiness to the operator explicitly, including this campaign's own stated limits (Windows path behavior never reachable from this Linux host; `DetectDrift`'s exact-tier live-fork path, if round 4 also doesn't reach it) — do not claim more than what was actually proven.
