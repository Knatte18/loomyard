# reed + shuttle crucible campaign — orchestrator handoff

Campaign: manual crucible review+fix loop on `internal/reedengine` (+ `internal/reedcli` + new `internal/hubgeom`) and `internal/shuttleengine` (+ `internal/shuttlecli`), bundled per the mill wiki task because shuttle drives its LLM agent runs through reed's tmux substrate. Motivated by wave-1 — three commits, `2b21ee57` (T1), `5b096ebd` (T2), `b98ee2ba` (T3, "shuttleengine + reedengine + tokenvocab told-geometry") — the last of which changed `reedengine.New`'s signature from `(cfg, *lyxcwd.Location)` to `(cfg, Geometry)`, moved `HubLogsDir` from `reedengine` to `fabricengine`, and introduced `internal/hubgeom` as the one-way told-geometry adapter. Must land before wave 3 (T6/T7). Orchestrator-driven, not mill-go — see `crucible/README.md` / `crucible/orchestrator-prompt.md`.

## Campaign structure (operator's call, 2026-08-18)
- **Reed first, standalone campaign** (running now). Shuttle is a **separate, later campaign** once reed converges — not interleaved.
- Round rotation for reed's campaign (operator-specified): **R1 Opus/Medium → R2 Opus/High → R3 Fable/High → R4 Opus/Medium**, used only as far as needed to converge (a safety pass may end the campaign earlier than R4; do not force all four if convergence is reached sooner).
- Shuttle's own campaign (not yet started) gets its own round-prompt file and its own rotation when reed converges — ask the operator for shuttle's rotation at that point.
- **Operator-mandated cost rule for shuttle specifically: any real `claude` process a round agent spawns while live-driving shuttle (every `lyx shuttle run` invocation) MUST use `--model haiku` (or the cheapest available), unless a specific finding genuinely requires testing model-specific behavior.** Already applied to reed's one claude-spawning test in round 1 (see F3 below).

## Wave-1 attribution (established during round 1 verification, 2026-08-18)
Wave-1 is three commits, not one: `2b21ee57` (T1), `5b096ebd` (T2), `b98ee2ba` (T3). Per-finding attribution, via `git log -S` on the exact code each finding touched:
- **T1 (`2b21ee57`)** touches no reed-related file at all (loomengine/planparser/webster only). Zero findings trace to it.
- **T2 (`5b096ebd`)** touched `internal/reedengine/config.go` (`LoadConfig` → `configengine.LoadOrTemplate`). This is the source of ONE of F5's three stale rows (the `reedengine.LoadConfig` "blocks standalone: yes" row in `producers-standalone.md`).
- **T3 (`b98ee2ba`)** is the source of F4, F6, F7, F8, F9, F10 (all LOW/NIT doc-and-dead-code fallout from the `Layout`→`Geometry` rename) and two of F5's three rows.
- **F1 (BLOCKING) and F3 (MEDIUM) are NOT wave-1-caused at all** — both trace via `git log -S` to `93ad5b01` ("mux -> reed rename"), reed's earliest history. They are old, previously-uncaught bugs the campaign happened to surface, not regressions wave-1 introduced.
- **F2 (MEDIUM) predates wave-1 too**, but wave-1's `geometry.go` doc comment turned an implicit, accidentally-correct derivation into an explicit contract ("AnchorPath is... the cwd every pane is spawned with") the code did not honor — wave-1 is what promoted a latent bug into a documented contract violation, even though it didn't write the buggy line itself.
- **Net: only 7 of round 1's 10 findings are wave-1-caused, and all 7 are LOW/NIT.** The one BLOCKING finding and one of the two MEDIUM findings are unrelated to the commit that motivated this campaign. Useful context for calibrating round 2+, not a reason to narrow scope — a safety pass should still look everywhere, per the prompt's own instruction.

## State right now
- **Round 1 (`opus-medium-r1`) — CLOSED AND INDEPENDENTLY VERIFIED.** 10 findings (1 BLOCKING, 2 MEDIUM, 2 LOW, 5 NIT), all fixed, none deferred. Prompt version preserved in git history at commit `c0569063`; reports `_mill/reed-review-r1.md` / `_mill/reed-review-r1-fixer-report.md` (both committed).
- **Round 2 (`opus-high-r2`) — CLOSED AND INDEPENDENTLY VERIFIED (2026-08-18).** 6 findings (1 BLOCKING — R2-F1, 2 MEDIUM — R2-F2/R2-F6, 1 LOW — R2-F3, 2 NIT — R2-F4/R2-F5), all fixed, none deferred. R2-F6 was disclosed as surfacing during Job 2's own concurrent sweep rather than Job 1, and the doc updates for R2-F1/R2-F3 landed one commit behind their fixes — both process deviations stated plainly in the fixer report, not hidden.
  - Headline: **R2-F1** — a worktree directory name containing `.`/`:` made `reed up` hang 20s then fail opaquely, stranding an untearable session on the shared per-hub tmux server (tmux silently rewrites `.`/`:` → `_` and exits 0); `down` on it falsely reported `{"ok":true}`. Fixed by `validateToldTmuxIdentity` at the `withOpLock` chokepoint — refuses in ~0.01s with an actionable message, before any tmux round trip. Commit `ae96397f`.
  - R2-F2 (MEDIUM) — `Down`'s reap-root snapshot included dead panes, risking a SIGKILL of an unrelated process tree after pid reuse. Commit `530a1602`.
  - R2-F6 (MEDIUM) — the capability probe bypassed the module's socket-scoped `TmuxCmd` and hit the operator's global default tmux socket, causing 2 failures in 9 concurrent-suite runs. Commit `28c3aa0f`.
  - R2-F3 (LOW) — a hub resolving to filesystem root yields a socket key with `/`, which tmux can't open and doesn't report. Commit `e9ad525f`.
  - R2-F4/R2-F5 (NIT) — stale doc claims / retired `layout` vocabulary. Commits `3a40c8cb` / `eff5eda6`.
  - **Orchestrator's independent verification (cold state, same protocol as round 1):** `go build`/`vet` clean; `go test -count=5` on all reed packages green; whole-repo `go test ./...` green; integration-tagged gate green; full tmux-only smoke suite 18 PASS / 1 expected SKIP (psmux-specific), zero stray tmux processes. **All four of round 2's new regression tests sabotage-proved** — `TestSmokeUpRefusesAWorktreeNameTmuxWouldRewrite`/`TestWithOpLock_RefusesARewrittenSessionNameBeforeTouchingTmux` (reverted the `validateToldTmuxIdentity` call in `withOpLock`), `TestSessionReapRoots` (reverted `sessionReapRoots` to `p.PID > 0`), `TestProbeCapabilityLocked_GoesThroughTheSocketScopedTmuxCmd` (restored the direct `exec.Command` bypass) — each failed at exactly the assertion the fixer report claimed, then each fix was restored and the diff came back empty. **Independently live-drove R2-F1's fix on a fresh fixture** (dot-named worktree `svc.v2` under a deep three-segment anchor, built from scratch, not reusing round 2's fixture): `reed up` refused in 0.009s with the documented message naming the offending directory; `status`/`down` afterward both echoed the same refusal reason; zero tmux processes ever started. Doc fixes R2-F4/R2-F5 spot-checked against current file content — both accurate.
- **Round 3 (`fable-high-r3`) — CLOSED AND INDEPENDENTLY VERIFIED (2026-08-18).** 2 findings (1 MEDIUM — R3-F1, 1 NIT — R3-F2), both fixed, none deferred. A near-clean pass: no BLOCKING, no new class of bug.
  - **R3-F1 (MEDIUM)** — `validateToldTmuxIdentity` (round 2's fix) only refused tmux's `.`/`:` session-name rewrite; tmux ALSO silently vis-encodes every ASCII control character, DEL, and invalid-UTF-8 byte into a multi-character escape (TAB → literal `\t`, exit 0) — the exact same failure shape as R2-F1 (20s hang, opaque error, stranded unaddressable session, `down` falsely reporting `ok:true`), just a wider character class. Reachability lower than R2-F1 (needs a hand-renamed worktree; `fabric add` slugs are git-ref-safe and cannot carry control bytes). Fixed by extending the same `withOpLock` pre-flight to refuse the whole vis-encode class, refuse-never-sanitize, same posture as R2-F1. Commit `12ca3ea5`.
  - **R3-F2 (NIT)** — `reed.json`'s persisted `Socket`/`Session` diagnostic was stamped once at init and never refreshed, going stale (misleadingly) after a worktree rename. Now re-stamped from told geometry on every load. Commit `7af91088`.
  - Also: two sandbox-suite scenarios round 2's fixer report had named but not added (rewrite-refusal, default-socket purity) were added this round, commit `cd4d655a`.
  - **Orchestrator's independent verification (cold state, same protocol as rounds 1-2):** `go build`/`vet` clean; `go test -count=5` on all reed packages green; whole-repo `go test ./...` green (only non-test packages skip, as expected); integration-tagged gate green; full tmux-only smoke suite 18 PASS / 1 expected SKIP. **Both new regression tests sabotage-proved**: `TestValidateToldTmuxIdentity_SessionName`'s six new vis-encode rows (reverted the vis-encode refusal branch in `validateToldTmuxIdentity`) failed at exactly the six intended assertions; `TestLoadOrInitStateLocked_ExistingFileLoadsStrandsAndRestampsIdentity` (reverted the always-re-stamp behavior to init-once) failed at exactly the intended stale-value assertions — both restored, empty diff confirmed. **Independently live-drove R3-F1's fix on a brand-new fixture** built from scratch with a DIFFERENT control byte (0x01) than round 3's own test used (TAB) — `reed up` refused in 0.010s with the documented message naming the exact worktree directory; zero tmux processes ever started (confirmed via `pgrep -x tmux` / `ps -eo comm`, not the argv-substring-prone `ps aux | grep`).
- **Convergence assessment after R3: near-clean** — no BLOCKING, one MEDIUM that is the same shape/mechanism as R2-F1 (not a new bug class) with materially lower reachability, one NIT. Operator chose (2026-08-18) to spend the rotation's last slot anyway rather than stop here.
- **Round 4 (`opus-medium-r4`) — CLOSED AND INDEPENDENTLY VERIFIED (2026-08-18).** 5 findings (2 MEDIUM — R4-F1/R4-F4, 2 LOW — R4-F3/R4-F5, 1 NIT — R4-F2), all fixed, none deferred. This was the rotation's originally-planned FINAL slot, but it did NOT come out clean — it found more than round 3, not less, and two findings are a genuinely new defect class the rotation had not probed.
  - **R4-F1 (MEDIUM)** — the identity pre-flight (`validateToldTmuxIdentity`) missed tmux's THIRD silent session-name rewrite class: `vis(3)` doubles the backslash (`\` → `\\`), same as it substitutes `.`/`:` (R2-F1) and vis-encodes control chars/DEL/invalid-UTF-8 (R3-F1). Same failure shape: 20s hang, opaque error, session stranded unaddressable on the shared per-hub server, `down` falsely reports `ok:true`. An exhaustive printable-ASCII sweep (0x20-0x7E) now proves `.`, `:`, `\`, plus the control/DEL/invalid-UTF-8 class is the COMPLETE rewrite surface of tmux 3.6's `session_check_name` — this closes the family exhaustively rather than incrementally. Commits `621165ce` + `b3c74d54` (message fix: prints `%s` not `%q` so an operator sees the actual directory name, not a re-escaped one).
  - **R4-F4 (MEDIUM) — new defect class: state-loss recovery.** The header pane is 1 row by default; `ensureHeaderPaneLocked` splits against the topmost pane to rebuild it, and tmux cannot split a 1-row pane. Normally masked because `st.HeaderPaneID` short-circuits. If `.lyx/reed.json` is lost while the tmux session stays up — e.g. an ordinary `git clean -xdf` in the worktree, sanctioned by the Durable-vs-Ephemeral State Invariant — the top pane becomes an untracked 1-row band and every subsequent `up`/`resume` fails PERMANENTLY (`no space for new pane`), while `status` reports the session healthy throughout. Only escape was `down`+`up`, undocumented. Fixed by retrying the split once behind a `select-layout even-vertical` re-tile before giving up. Commit `9ec33880`.
  - **R4-F5 (LOW), surfaced verifying R4-F4's fix (provenance disclosed, same as R2-F6's precedent)** — pane adoption guessed which of several untracked panes was idle, sometimes typing a strand's command into a pane already busy running `header --blocking`, then reporting a nonexistent process `live:true`. This is the one symptom class an orchestrating caller (shuttle, a later campaign) cannot defend against from outside. Fixed by narrowing adoption to the sole-alive-non-header-pane case. Commit `be746b85`.
  - **R4-F3 (LOW)** — the told-geometry backstop (`validateToldTmuxIdentity`) covered 2 of 7 `Geometry` fields; `AnchorPath` non-absolute/empty silently resolved state and pane cwd against the wrong tree. New `validateToldAnchorPath`, called from `withOpLock` before `os.MkdirAll`. Commit `cb935402`. Currently unreachable in production — confirmed independently (not just cited) by reading `lyxcwd.go`: `git rev-parse --show-toplevel` always returns an absolute path, so `hubgeom.ReedGeometry`'s `AnchorPath` is structurally always absolute today. Still a correct backstop for the standalone teller it's written for.
  - **R4-F2 (NIT)** — `ServerName` bounded nothing; a hub basename ≥80 chars overflowed `sun_path` (measured live ceiling on this host: 92 chars total key length). Now truncates its readable half at 48 bytes on a rune boundary via `truncateAtRuneBoundary` — collision-free since the hash half is untouched. Commit `b3f58a2b`.
  - **Orchestrator's independent verification (forked sub-agent, full cold-state protocol, same rigor as rounds 1–3):** all hermetic gates green (`build`/`vet` clean, `-count=5` on all reed packages `ok`, whole-repo `go test ./...` `ok`, `-tags integration` `ok`); `./deploy-dev` redeployed clean; full tmux-only smoke suite **23 PASS / 1 SKIP**, matching the fixer report exactly. **ALL FIVE of round 4's fixes sabotage-proved** — each reverted in turn, each failed at exactly its intended assertion (R4-F1's backslash rows + smoke test; R4-F4's recovery test + scrubbed-state smoke test; R4-F3's wiring test; R4-F5's `TestPlanPaneTarget` "several untracked" row; R4-F2's bound test), each restored with an empty diff after. **Independently live-drove R4-F1 and R4-F4 on brand-new fixtures**, deliberately different names from round 4's own (`zetahub-HUB/core\prod`, `omegahub-HUB/svc-recover`): both held exactly as claimed — backslash refusal in 0.009-0.010s before any tmux round trip, scrubbed-state recovery returned `ok:true` with the header correctly re-landing at `pane_top=0` and a follow-up `add` restoring the normal layout. Independently confirmed R4-F3's "unreachable in production" claim by reading `lyxcwd.go` directly. Doc/help spot-checks (`geometry.go`, `doc.go`, `reedcli/cli.go` Long help, `SANDBOX-REED-SUITE.md` M20/M22) all accurate. Teardown clean (`ps -eo comm | grep -cx 'tmux: server'` = 0). **The orchestrator's independent verification agent formed its own convergence judgment (not just repeating R4's): agrees with continuing rather than declaring done, on the same grounds — the sabotage-proofed coverage for R4-F4/R4-F5 is real but narrow (covers exactly one loss mode), and three concrete neighboring loss modes remain genuinely untested by any of the four rounds' suites.**
  - **Teardown correction discovered this round, now standing guidance:** `pgrep -x tmux` reports **zero even while a tmux server IS running** — a tmux server's process `comm` is literally `tmux: server`, not `tmux`. Use `ps -eo comm | grep -cx 'tmux: server'` or `tmux -L <socket> ls` instead. Carried into R5's prompt.
- **Round 4's own recommendation (independently corroborated by the orchestrator's verification): do not declare convergence on rotation-exhaustion alone.** R4-F4/R4-F5 are a new defect class (state-loss/corruption recovery) that no round has systematically probed, unlike R4-F1 which is the third and now-exhaustively-closed instance of the told-identity rewrite family. Operator decision (2026-08-18): spend a round beyond the planned four-round rotation, scoped narrowly to state-loss/corruption recovery — model/effort **Opus / High**.
- **Round 5 launching now: Opus / High**, tag `opus-high-r5` — beyond the originally planned rotation, narrowly SCOPED (not a general safety pass). Four concrete untested loss/corruption modes named in its prompt: (1) `reed.json` truncated/corrupted mid-write, (2) a stale/older `reed.json` restored while panes have moved, (3) `.lyx` (incl. lock file) deleted wholesale while an op is genuinely in flight, (4) `reed.json` written by one worktree read after that worktree is renamed (re-verifying R3-F2 was diagnostic-only staleness, not a targeting bug). Also asks R5 to confirm R4-F4/R4-F5's fixes generalize beyond the exact one-file-deleted-while-idle case round 4 drove.
- Seed prompt: `_mill/reed-review-prompt.md` (rewritten fresh for round 5, committed). Round 4's version preserved in git history at commit `282abf12`.
- RESIDUAL currently seeded: the four state-loss/corruption modes above are R5's whole mandate — see the prompt's "Scope" section, which is deliberately narrow rather than a floor to expand from.
- DEFERRED list: empty (no round 1–4 deferred anything).
- Shuttle campaign: not started. Waiting on reed's convergence (R5's outcome + the orchestrator's independent verification) before starting it.

- **Round 5 (`opus-high-r5`) — CLOSED, orchestrator's independent verification IN PROGRESS (2026-08-18).** The scoped state-loss/corruption pass did NOT come out clean — 8 findings (**1 BLOCKING — R5-F3**, 4 MEDIUM — R5-F1/F2/F4/F5, 2 LOW — R5-F6/F7, 1 NIT — R5-F8), all fixed, none deferred. This is the campaign's SECOND BLOCKING finding (the first was R1's F1). Root cause shared by four of the eight: **reed trusted a persisted `PaneID` absolutely** — nothing recorded which tmux session incarnation minted a binding, nothing checked it belonged to this worktree's session, nothing checked the table was internally consistent. Three findings produced a FALSE-HEALTHY report (`ok:true`/`live:true` while wrong); two silently destroyed a SIBLING worktree's running work while reporting success.
  - **R5-F3 (BLOCKING)** — a strand `PaneID` colliding with `HeaderPaneID` (or two strands sharing one `PaneID` — both ordinary once tmux ids restart at `%0` on server rebirth) made `select-layout` emit a duplicate pane number; tmux accepts this with exit 0 and silently DESTROYS the panes it has no cell for. Live-reproduced: a single `reed up` cut a 2-pane session to 1, reported `ok:true`, then reported the strand `live:true` against the header pane running `lyx reed header --blocking`. Fixed via two independent layers: `removeDuplicatePaneCells` (render path structurally cannot emit a pane number twice) + `clearConflictingPaneBindings` (sanitizes the loaded table before any op reads it). Commit `24c475a0`.
  - **R5-F2 (MEDIUM)** — pane bindings from a previous tmux server incarnation were trusted as live (reed's only defense, `clearAllPaneBindings`, fires only when THIS invocation spawned the server). A restored/stale `reed.json` made `status` report `live:true` for a process running nowhere, and `resume` — the recovery verb — refused to rebuild (`resumed:0`) because it saw the false binding as live. Fixed via new `internal/reedengine/generation.go`: a `PaneGeneration` stamp (`session_id`/`pid`/`session_created` via one `display-message` probe) recorded alongside bindings; adopt/clear/refuse decided against the live generation on every load. Commit `80909e4c`. This is the mechanism R5-F5 and (in the common case) R5-F4 build on.
  - **R5-F4 (MEDIUM)** — a persisted `PaneID` was spent as a tmux target (`kill-pane`, `send-keys`) with no check it belonged to THIS worktree's session. Since the tmux socket is per-HUB (shared across worktrees) and pane ids are server-global, a stale/copied `reed.json` in one worktree could kill or type into a SIBLING worktree's live pane. Live-reproduced: `remove` in `svc-alpha` killed `svc-beta`'s pane and its running process, reporting `{"ok":true}` on both ends. Fixed via `resolvePaneInThisSessionLocked` + `paneIDsInSession` filtering `RemoveStrand`'s kill list. Commit `ab36dcb6`.
  - **R5-F5 (MEDIUM)** — after a worktree rename, the old tmux session keeps running (session name derives from worktree basename) while `resume` in the renamed directory booted a NEW session and silently double-launched every strand — an LLM agent strand would run twice, burning tokens invisibly, with the orphan permanently unaddressable by any reed verb. Fixed: built on R5-F2's generation stamp, `resume`/`up` now REFUSE (not auto-kill) when the recorded session is still alive under a different name, naming the orphan and the exact remedy. Commit `2c7e88ac`.
  - **R5-F1 (MEDIUM)** — a corrupt/truncated `reed.json` gave an opaque `unmarshal state: unexpected end of JSON input` on every verb except `down` (which destroys the session to "fix" it); a bare `null` document silently decoded to a valid empty state, losing the strand table with `ok:true`. Fixed: `ReedState.UnmarshalJSON` rejects `null`; new `unreadableStateError` names the file and both remedies. Commit `c4ff17cb`.
  - **R5-F8 (NIT)** — `requireSessionLocked` swallowed a state-read failure and reported "no reed session (0 strands persisted)" — indistinguishable from genuinely empty state, actively misleading paired with R5-F1's error. Fixed via `noSessionMessage`'s readable/unreadable split. Commit `788dd562`.
  - **R5-F6 (LOW)** — `rm -rf .lyx` (sanctioned by the Durable-vs-Ephemeral State Invariant) while an op holds `reed.lock` unlinks the lock's inode; a second op's `MkdirAll`+`O_CREATE` mints a new inode and is granted the lock immediately — mutual exclusion voided silently (measured: 11027ms correctly-blocked control vs 107ms race). Fixed: `withOpLock` now detects (cannot prevent — the old inode is unreachable by name) via `os.SameFile` and reports when the lock file it holds is no longer the file at that path. Commit `c95de3db`.
  - **R5-F7 (LOW)** — R4-F3's own regression guard (`TestWithOpLock_RefusesAnUnusableAnchorPathBeforeCreatingState`) asserted against a CWD-relative `.lyx` with no cleanup; a single run of any pre-R4-F3 binary left permanent litter that disarmed the guard for good, reading as a live regression. Surfaced during R5's OWN baseline gate (Job 1, before any file touched — provenance disclosed in the review). Fixed: guard now clears its litter before asserting and on cleanup. Commit `5e145a71`.
  - Also fixed in the same pass: `internal/reedengine/render/policy.go`+`rules.go` structural duplicate-cell guard, `reconcile.go`'s `clearConflictingPaneBindings`, `strand.go`'s `paneIDsInSession`, `io.go`'s `resolvePaneInThisSessionLocked`, `lock.go`'s post-op identity check — all listed above under their finding.
  - **Gates per R5's own report (NOT YET independently confirmed):** `build`/`vet` clean; `-count=5` on all reed packages green; whole-repo `go test ./...` and `-tags integration ./...` green; smoke suite 22 top-level PASS + 4 subtests / 1 expected SKIP / 0 FAIL (this round added 3 top-level tests over round 4's baseline). Teardown clean across 5 sockets. No `claude` process spawned (scope didn't need one).
  - **R5's own convergence recommendation: do NOT converge yet.** Root cause was real and coherent, not noise — proposes a SIXTH round, narrowly scoped to what R5 itself just changed (the generation-probe mechanism, the pre-boot refusal, the shared load chokepoint 5 of 8 fixes now touch), explicitly NOT another general safety pass.
- **Orchestrator's independent verification of R5: COMPLETE (2026-08-18) — everything R5 claims holds.** All gates green, exact match to R5's own numbers (26 PASS lines, 1 SKIP, 0 FAIL). All 8 sabotage-proofs confirmed (each fix reverted → failed at intended assertion → restored → empty diff). Live re-drove R5-F3/R5-F4/R5-F5 on brand-new fixtures never used by round 5 (`iotahub-HUB/{wt-north,wt-south}`, `kappahub2-HUB/wt-before→wt-after`) — all three held, including a live-constructed case proving R5-F4 and R5-F5 are genuinely independent defense layers (a valid, non-stale generation stamp whose `PaneID` still points at a sibling worktree's pane bypasses R5-F5's refusal but is caught by R5-F4's membership filter). Re-confirmed R3-F2 live (rename targeting derives fresh from the current path).
  - **New finding from the verification itself, NOT flagged by R5:** two of round 5's eight fixes have **no regression test that catches their wiring (call site) being silently dropped**, even though both the full hermetic suite and full smoke suite stay green — only the underlying function's own unit test would fail, and nothing exercises the call site itself.
    - `clearConflictingPaneBindings`'s call in `spawn.go` (part of R5-F3's BLOCKING fix) — partially mitigated by an independent second layer (`removeDuplicatePaneCells` in the render path, which IS well-covered), but this layer itself has zero wiring-level coverage.
    - `noSessionMessage`'s split, wired into `requireSessionLocked` (R5-F8, NIT) — no mitigation at all, though NIT severity limits the blast radius.
  - Docs spot-checked accurate. Teardown clean after cleaning up one genuine orphan tmux server left by the R5-F5 sabotage-proof itself (that test's premise is reproducing a double-launch, which doesn't self-clean by design).
  - **Orchestrator's own independent convergence judgment: agrees with R5 — do not converge yet — but widens R5's own proposed R6 scope** to explicitly include closing the two wiring-test gaps above, not just R5's own named residual-risk items (generation-probe intermittent failure, cross-worktree races on the new pre-boot refusal, operator-wedge potential when tmux isn't directly reachable).
- **Operator decision (2026-08-18): run R6.** Model/effort: **Opus / High** (same as R2 and R5 — the two rounds that found this campaign's BLOCKING findings).
- **Round 6 launching now: Opus / High**, tag `opus-high-r6` — sixth round, well beyond the planned four-round rotation. Narrowly scoped, two parts: **Part A** — probe the round-5-built `PaneGeneration` mechanism's own failure modes (intermittent probe failure and whether "fail open" is right at every call site; a real race between two worktrees against the new pre-boot refusal; whether the refusal's manual-only remedy is reachable by an operator without direct tmux access). **Part B** — add wiring-level regression tests for the two gaps found above, mirroring how `TestRemoveStrand_NeverKillsAPaneOutsideThisSession` already pins a call site rather than only its helper.
- Seed prompt: `_mill/reed-review-prompt.md` (rewritten fresh for round 6, committed). Round 5's version preserved in git history at commit `a3d2dec7`.
- RESIDUAL currently seeded: Parts A and B above are R6's whole mandate — deliberately narrow, not a floor to expand from. A broad wiring-audit across all of rounds 1–5 is explicitly out of scope; only the two named gaps plus anything noticed incidentally.
- DEFERRED list: empty (no round 1–5 deferred anything).
- Shuttle campaign: not started. Waiting on reed's convergence (R6's outcome + the orchestrator's independent verification) before starting it.

- **Round 6 (`opus-high-r6`) — CLOSED AND INDEPENDENTLY VERIFIED (2026-08-18). REED CAMPAIGN CONVERGED.** 0 BLOCKING, 2 MEDIUM (R6-F2, R6-F3), 2 LOW (R6-F1, R6-F4), plus the 2 named Part-B wiring-test gaps (R6-B1, R6-B2) — 6 total, all fixed, none deferred. First round in the campaign where BOTH the round itself AND the orchestrator's independent verification recommend converging.
  - **Central substrate discovery (independently reconfirmed against real tmux 3.6):** `display-message -p -t '=<name>:' '<fmt>'` against an ABSENT session exits **0**, not 1, and expands session-scoped fields to empty while server-global fields (`#{pid}`) still fill — unlike `has-session`/`list-panes`, which reliably exit 1 for absent/no-server/unparseable/empty alike. Round 5's `refuseLiveForeignSessionLocked` had been built on the opposite assumption (that a probe error meant "session gone"), so it fail-opened on the one check whose entire job is to refuse.
  - **R6-F2 (MEDIUM)** — because the fail-open silently reclassified any transport failure as "gone," a single transient probe failure at the pre-boot call let `up` boot and THEN refuse post-boot — depositing exactly the bare squatting session the pre-boot check exists to prevent. Fixed: existence now decided via `list-sessions` (only exits 1 for an unreachable socket) through new `classifyRecordedSessionLocked`. Commit `d67ad559`. Live-reproduced pre-fix (residue session deposited) and post-fix (refuses before booting, zero residue) on two independent fixtures (R6's own + the orchestrator's separate re-drive).
  - **R6-F1 (LOW)** — every non-booting diagnostic verb (`status`, `attach`, `add`, etc.) routed an operator with a renamed/orphaned worktree straight into "run resume," which then refused — the one verb that actually explains the situation was never the one suggested. Fixed: `requireSessionLocked` now runs the same foreign-session check first. Commit `210ccb70`.
  - **R6-F3 (MEDIUM)** — `down` reported unqualified `ok:true` while abandoning a live orphan session (and its running strand process) on the shared socket forever, deleting the only record naming it — the false-healthy pattern rounds 4–5 established as highest-weight, and `down` is also the ONLY lyx-only escape from R6-F1/R5-F5's refusal for an operator without direct tmux access. Fixed: `DownResult.AbandonedSession` reports (never kills — reed can't distinguish a rename-orphan from a hand-copied sibling's live session, R5-F4's exact ambiguity) before the state file is deleted. Commit `fc2d3391`.
  - **R6-F4 (LOW)** — two doc comments (`paneGenerationLocked`, `serverPIDLocked`) asserted the false substrate behavior the whole finding rests on refuting. Fixed + pinned by `TestDisplayMessageDoesNotErrorForAnAbsentSession` (integration-tagged, real tmux). Commit `106eacc3`.
  - **R6-B1** — closed the `clearConflictingPaneBindings` wiring gap the R5 verification found: `TestStatus_NeverReportsAStrandLiveOnAPaneAnotherOwnerClaims` drives the whole `Status` op, isolating the reconcile-side layer from its independent render-side sibling. Commit `f1da29cc`.
  - **R6-B2** — closed the `noSessionMessage` wiring gap: a smoke-tagged test (the branch is unreachable via `execHook`, needs a real `*exec.ExitError`). Commit `65ca1520`.
  - **Orchestrator's independent verification (forked sub-agent):** independently re-measured the central substrate fact directly against live tmux 3.6 (exact match). All gates green matching R6's claims exactly (25 top-level PASS + 4 subtests / 1 SKIP / 0 FAIL). **All 6 fixes sabotage-proved** — each reverted, each failed at its intended assertion (including confirming R6-B1's new test is the ONLY thing that catches its gap — no other test in the suite does), each restored to an empty diff. **Independently live-drove R6-F2 and R6-F3 on brand-new fixtures** (`thetazeta-HUB/...`, names never used by any prior round) — both held exactly as claimed. **Independently re-derived** (not just cited) the "genuine two-worktree boot race" non-finding's reasoning: empirically confirmed tmux session IDs monotonically increment and never reuse within one running server, so `SameIncarnation`'s three-field comparison structurally cannot collide across a race-window recreation. Docs spot-checked accurate. Teardown clean, `git status --porcelain` empty throughout.
  - **R6's own convergence recommendation (first "yes" in the campaign), independently corroborated:** findings have shifted decisively from "reed does the wrong thing" (R1-F1, R2-F1, R5-F3) to "reed does the right thing but says the wrong thing about it" — nothing in R6 destroys data, crosses a worktree boundary, or produces a false-healthy strand report. Both R6 and the orchestrator's verification name the same residual, low-stakes gap: a systematic wiring-level regression-test audit across all six rounds' fixes remains un-swept (the two Part-B gaps were found by sabotaging call sites, not by reading code) — explicitly NOT a reason to keep going now, but worth naming if reed is ever revisited; would not need an expensive model.

## CAMPAIGN STATUS: REED CONVERGED (2026-08-18)

Six rounds, 37 findings total (10+6+2+5+8+6), every one fixed and independently verified by the orchestrator from a cold state — sabotage-proofed regression tests, live re-drives on fixtures never reused across rounds, doc spot-checks, clean teardowns. Two BLOCKING findings across the campaign (R1-F1: inert pane-child reaping; R5-F3: duplicate-pane-cell destruction), both closed and re-verified multiple times since. Full round-by-round detail above; round reports at `_mill/reed-review-r{1..6}.md` / `-r{1..6}-fixer-report.md`, all committed.

**Operator confirmed reed done (2026-08-18): "Er vi ikke ferdige nå?" in response to the convergence report — REED CAMPAIGN OFFICIALLY CLOSED.**

## SHUTTLE CAMPAIGN — STARTED (2026-08-18)

Second, separate campaign on `internal/shuttleengine` + `internal/shuttleengine/claudeengine` + `internal/shuttlecli`.
**Round rotation (operator's call, 2026-08-18): same pattern as reed — Opus/Medium → Opus/High → Fable/High → Opus/Medium, used only as far as needed to converge.** Operator confirmed via "Ok. finalize" after the orchestrator recommended reusing reed's rotation.
Standing mandatory rule (unconditional on the round, stated at the top of this file): every real `claude` process a round agent spawns while live-driving shuttle MUST use `--model haiku` (or cheapest available), unless a specific finding genuinely requires testing model-specific behavior.

**Round 1 seeded and launching: Opus / Medium**, tag `opus-medium-r1`. First round of a fresh campaign — no prior residual, full clean-room review + fix per `crucible/review-prompt-template.md`.
Seed prompt: `_mill/shuttle-review-prompt.md` (written fresh, committed).

Key framing baked into the prompt:
- Wave-1 changed `Runner`'s construction from a single `*lyxcwd.Location` to two independently-passed, untyped `string` params (`anchorPath`, `worktreeRoot`) — NO structural type safety distinguishing them, unlike reed's `Geometry` struct which at least has named fields and a validating pre-flight. A swapped argument pair at the one production call site (`shuttlecli/cli.go`'s `hubgeom.ReedGeometry(layout)` → `NewRunner(..., reedGeom.AnchorPath, reedGeom.WorktreeRoot, ...)`) would compile cleanly — this is round 1's single highest-yield focus item.
- Shuttle drives reed directly through `ReedOps`, and reed's own six-round campaign changed several behavioral contracts underneath it (the foreign-session refusal on `up`/`resume`, `Down`'s new `AbandonedSession` field, `LoadState`'s new actionable-vs-opaque corrupt-state errors) — round 1 is explicitly asked whether `sweepOrphansOpportunistic` and the `Interrupt`/`Send`/`Inject` paths still assume reed's OLDER, simpler behavior.
- This module has documented history (from a torn-down prior worktree, lessons folded into `crucible/review-prompt-template.md` itself): a round killed mid-fix with an uncommitted diff, and a round that interleaved review and fix. The prompt calls this out explicitly at the top so R1 does not repeat it.
- `LLM-DRIVING: YES` — 4 smoke tests, each spawns exactly ONE real `claude` process (no fan/cluster shape exists in this module — shuttle runs one agent per `Start` call by design). No execution ban needed; the mandatory `--model haiku` rule and the "name the exact test, never bare `-run Smoke`" discipline both apply. No N×-concurrent sweep for this module (real `claude` subprocess cost).
- Out of scope: reed's own code (campaign closed — read reed's CURRENT contract as context, but do not re-review or fix reed itself; a genuine reed defect found while reviewing shuttle gets named as an out-of-campaign finding, not fixed here), burler/perch/loom (built on top of shuttle), Windows live-driving (host is Linux; `posix.go`'s Windows path named as a gap).

- **Round 1 (`opus-medium-r1`) — CLOSED AND INDEPENDENTLY VERIFIED (2026-08-18).** 10 findings (3 MEDIUM — R1-F1/F2/F3, 5 LOW — R1-F4/F5/F6/F7/F10, 2 NIT — R1-F8/F9), all fixed, none deferred. Confirms the prediction that shuttle's post-wave-1 surface is much shallower than reed's — one round found real, worth-fixing issues but nothing near reed's BLOCKING-grade defects, consistent with shuttle's wave-1 diff being a largely mechanical told-string substitution rather than a new stateful construction seam.
  - **R1-F1 (MEDIUM)** — all 4 smoke tests previously spawned real `claude` on the account default model (an operator-instruction violation, same pattern reed's own `smokeClaudeModel` constant already existed to prevent). Now all pin `haiku`. Commit `f16eaba7`. **No automated regression test exists for this — enforced only by a shared constant + all 4 call sites referencing it (confirmed via grep, not a failing test).** Real gap, same shape as reed's R5→R6 wiring-test gaps; named for R2 to pick up if it has capacity, not blocking.
  - **R1-F2 (MEDIUM)** — `Wait`'s three mechanism-failure exits (as opposed to classification exits) discarded the run's identity (guid/sessionId/runDir), leaving a caller with nothing to act on exactly when a reed op fails mid-run (e.g. `lyx reed down` in a second process while a run is in flight). Now returns `(*Run).identity()` alongside the error at all three sites; the CLI envelope carries the fields. Commit `342482ed`. Deliberately NOT reclassified as `OutcomeDied` — reed exposes no sentinel distinguishing "gone" from "refuses" (a named, accepted out-of-campaign residual, since growing one is a reed change).
  - **R1-F3 (MEDIUM)** — `NewRunner`'s told `anchorPath`/`worktreeRoot` pair (post-wave-1, two adjacent untyped strings replacing one self-consistent `*lyxcwd.Location`) was validated nowhere; a swapped argument pair compiles cleanly and silently relocates 3 of its 4 consumers. New `validateToldPaths`: both non-empty, both absolute, `anchorPath` equal-to-or-inside `worktreeRoot` (the swap detector — real hub geometry always satisfies this). Commit `b68cfd21`. Mirrors reed's own `validateToldAnchorPath` from its campaign, same reasoning ("does not fail — it succeeds against the WRONG tree").
  - R1-F4 (LOW) — one test fixture defeated the swap-detection guarantee its own helper promises. Commit `e0f5107e`. Confirmed via sabotage: this was purely a coverage gap, not a live bug (the target test still passed with the old fixture).
  - R1-F5 (LOW) — an unreadable/truncated `run.json` was reported as "not a shuttle strand", indistinguishable from the strand never existing, while the agent was still live in its pane. Commit `fc1dc45f`.
  - R1-F6 (LOW) — untracked-strand message asserted one cause among several. Commit `e30f2786`.
  - R1-F7 (LOW, PLAUSIBLE) — `buildResumeCmd` dropped `--model`/`--effort`/`--dangerously-skip-permissions`; a `lyx reed resume` of an autonomous shuttle run would come back permission-gated on the default model and stall silently. Commit `7951c86a`. Not live-driven (would need a real resume-triggering scenario); flag agged as plausible-not-confirmed honestly rather than overclaiming.
  - R1-F10 (LOW) — teardown wasn't logged through `internal/logger` at all (CONSTRAINTS.md's Live-Substrate Spawn Observability invariant), so the trace showed every run starting and none ending. Commit `6e1cccf3`.
  - R1-F8/F9 (NIT) — stale package doc, incorrect fork-hook comments. Commits `7d3cd491`/`7fa47ef8`.
  - **No large findings this round** — checked explicitly against the new size-vs-severity rule (crucible Hard Rule 5, added this session): all 10 are scoped hardening fixes, nothing rose to "needs its own mill-wiki task."
  - **Orchestrator's independent verification (forked sub-agent):** all gates green matching R1's claims. **9 of 10 fixes sabotage-proved** (each reverted, each failed at its intended assertion, each restored to empty diff) — R1-F1 is the one exception, confirmed to have no automated test (only grep-confirmable wiring), consistent with what R1 itself could have but did not add. **Independently live-drove R1-F2 and R1-F3 on a fresh scenario** (different prompt/timing from R1's own reproduction) — both held. Docs spot-checked accurate. Teardown clean (zero tmux, zero stray `claude`, `git status` empty).
  - **Orchestrator's independent convergence judgment: solid, proceed to round 2.** Nothing here changes the "shuttle converges faster than reed" prediction.

- **Round 2 (`opus-high-r2`) — CLOSED AND INDEPENDENTLY VERIFIED (2026-08-18).** 11 findings (2 MEDIUM — R2-F3/F9, 7 LOW — R2-F1/F2/F5/F6/F8/F10 + one more, 2 NIT — R2-F4/F7), no BLOCKING. **10 fixed inline; 1 (R2-F11) named NOT-FIXED-THIS-ROUND — the first real application of the new size-vs-severity rule (Hard Rule 5).**
  - **R2-F9 (MEDIUM)** — the startup deadline (`startup_timeout_s`, default 90s) was consulted on only 1 of 4 exit paths through `checkLivenessTick`'s not-yet-started window. A trust prompt that won't dismiss, or a pane that fails every capture, silently burned the full `run_timeout_min` (30 min default — a real paid `claude` pane) instead of dying at 90s, AND was misclassified `timeout` ("agent was working") instead of `died` ("never started") — the wrong signal for a caller deciding whether to escalate or retry. Fixed: deadline check lifted into `classifyStartupWindow`, called from every not-ready exit. Commit `e6ea9d05`.
  - **R2-F3 (MEDIUM)** — closed round 1's own named gap: R1-F1's `--model haiku` pin had zero regression coverage. New untagged source-scan guard asserts all 4 real-claude spawn sites thread the pin, with a site-count check so a recognizer silently matching fewer sites fails loudly rather than passing vacuously. Commit `934abdb9`.
  - R2-F1 (LOW) — `lyx shuttle run` emitted TWO JSON envelopes when geometry resolution aborted AND a flag was also bad (breaks any caller unmarshalling as one object, including this package's own smoke tests). Fixed: `ShouldAbort` moved to the top of `RunE`. Commit `bbe5636b`.
  - R2-F2 (LOW) — a fork-audit failure discarded an already-reached `OutcomeDone` classification, returning an empty `Outcome`. Fixed: returns the populated `Result`. Commit `41a70c3d`.
  - R2-F5 (LOW) — `AuditForks` slurped whole transcripts into memory (measured 178MB peak RSS on an 83MB real transcript, 2.15×) — a real availability risk inside webster's Master, a long-lived process. Fixed: streaming via `bufio.Reader.ReadString('\n')` (deliberately NOT `bufio.Scanner`, whose 64KB line cap would regress long tool-result lines into errors). Commit `354946c1`.
  - R2-F6 (LOW, PLAUSIBLE) — `sendVerified`'s delivery check could spuriously replay a whole choreography (typing the agent's instruction twice) when an earlier occurrence of the needle scrolled off-viewport between baseline and poll. Fixed the reachable half: re-baseline when the count drops below baseline. Commit `cdb22bf8`. **Does NOT close the case where one occurrence scrolls off exactly as the new one arrives (count unchanged) — that residual is R2-F11, below.**
  - R2-F8 (LOW) — 5 live-substrate log sites (2 in `run.go`, 2 in `wait.go`, the orphan-sweep and strand-teardown-failure paths) were on the bare `log` package, invisible to the durable trace sink — a real CONSTRAINTS.md Live-Substrate Spawn Observability violation, the sibling gap to R1-F10. Fixed: all 5 moved to `logger.Warn`. Commit `ec205fee`.
  - R2-F4/F7/F10 (NIT/NIT/LOW) — three doc-drift fixes, all found by DRIVING not reading: `template.yaml`'s run_dir comment misnamed the anchor as the worktree root (the exact confusion `validateToldPaths` guards against); `docs/overview.md` overclaimed the Agent-deny as unconditional; `OutcomeAsking` documented in three places as "the agent asked a question" when it actually means "ended turn without satisfying the file contract" (confirmed live: a progress-report message classified `asking`, not a question). Commit `22461927`.
  - **R2-F11 (LOW severity, LARGE size) — NOT FIXED, first application of the size-vs-severity rule.** The residual `sendVerified` case R2-F6 doesn't close: one occurrence scrolling off exactly as the new one arrives leaves the needle count unchanged — indistinguishable from non-delivery using a count alone. Closing it means REPLACING the delivery signal (not adjusting it), and the signal being replaced is a live-proven guarantee whose failure direction is silent — getting a replacement wrong regresses in the dangerous direction. Recommended: a position-aware check (needle in the tail region relative to baseline) validated live against a real Claude TUI under genuine churn, as its own scoped task.
  - **No large finding besides R2-F11.**
  - **Orchestrator's independent verification (forked sub-agent):** all gates green. **7 of 10 fixes sabotage-proved directly** (each reverted, failed at intended assertion, restored to empty diff): R2-F9, R2-F1, R2-F2, R2-F6, R2-F3, R2-F8 all confirmed. R2-F5 verified structurally (read `forEachTranscriptLine` directly — single-line buffer reused, no accumulating slice, structurally cannot retain the whole file) rather than by re-measuring RSS, time budget permitting. R2-F4/F7/F10 spot-checked accurate. **Independently live-drove R2-F1 on a fresh scenario** (different from R2's own reproduction) — held, including the third "still reports its own envelope" leg. R2-F9's live drive was not attempted (same limitation R2 itself named — cannot force a stuck trust-prompt against an already-trusted real `claude` without a fake); relied on the hermetic sabotage-proof. **Independently assessed R2-F11's SIZE judgment and agrees it is correctly LARGE** — confirmed a position-aware check is technically buildable today with no reed API change (so "needs a design step" isn't a blocked-dependency excuse), but the real reason it's not a small inline fix is RISK: replacing a live-proven detector with something unvalidated, in the direction that fails silently, genuinely warrants its own task with live-TUI validation rather than a rushed inline swap.
  - **Orchestrator's independent convergence judgment: agrees with R2's own read — solid, converging but not converged, proceed to round 3.** Everything found was unhappy-path polish, the tail of round 1's fixes, or doc drift found by driving — no new defect class, no BLOCKING.

## Course correction from the operator (2026-08-18), applies from round 3 onward
**Operator feedback: the original mill-wiki task instructed reed and shuttle to be tested AGAINST EACH OTHER — cross-module joint adversarial testing — not each module reviewed in isolation with the other treated as an already-trusted dependency.** Operator accepted reed-first sequencing as reasonable (reed is the more "mechanical" layer; shuttle inherently is not, since its whole job is orchestrating reed + a live LLM). But shuttle's remaining rounds must weight reed×shuttle joint/composition testing much more heavily than rounds 1-2 did.
- Round 1 had exactly one composition scenario (L12: reed's foreign-session refusal propagating correctly through shuttle's wrapping).
- Round 2's high-yield list included composition as one item among many, not the primary focus.
- **Round 3 onward: treat the reed↔shuttle boundary as the primary test surface** — shuttle driving real agent runs WHILE reed is deliberately pushed through its own hardened failure modes (foreign-session refusal, corrupt/lost state, worktree rename, tmux server rebirth) DURING an in-flight shuttle run, not shuttle's happy path against an assumed-healthy reed. This is a real course correction, not additive — bake it into round 3's prompt as a first-class focus area, not an afterthought.

## R2-F11 — pending operator decision: open as its own mill-wiki task?
Per the new crucible rule, a LARGE finding gets spun into its own mill-wiki task via the normal mill flow (mill-start/plan/go — never raw wiki file edits, per `CLAUDE.md`'s "Mill wiki — never touched directly"). This has NOT been done yet — awaiting operator confirmation to proceed (which mill skill/flow to invoke, and when — now or after the shuttle campaign converges).

- **Round 3 launching now: Opus / Medium** (operator's explicit pick — deviates from the originally planned Fable/High rotation slot; operator prioritized the joint-testing mandate over continued strict model-diversity rotation), tag `opus-medium-r3`. **First round whose primary mandate is reed×shuttle joint/composition adversarial testing**, not shuttle-alone correctness — 7 named scenarios, all sharing the shape "start a real live shuttle run, THEN trigger a reed-side hardened-failure-mode event mid-flight, observe what shuttle does": worktree rename during a live run, `reed.json` corruption during a live run (not before), tmux server killed during a live run, cross-worktree contamination under REAL concurrent 2-agent load (budgeted exception to the 1-process-at-a-time rule, capped at 2), `interrupt`/`send` (not `Wait`) against a mid-refusal reed, a `down`+`up` session-rebuild cycle under a live run, plus anything newly practical from rounds 1-2's unverifiable lists.
- Seed prompt: `_mill/shuttle-review-prompt.md` (rewritten fresh for round 3, committed). Round 1's version preserved at `33a14555`, round 2's at `fa93d9f5`.
- R2-F11 explicitly carried forward as OUT of round 3's scope (named-not-fixed by round 2, not to be re-litigated or re-fixed this round) — the mill-wiki-task decision remains pending separately.

## Next action
1. **Awaiting operator decision on R2-F11**: open its own mill-wiki task now, or hold until the shuttle campaign converges.
2. Wait for round 3 (`opus-medium-r3`) to complete, then the orchestrator independently verifies from a cold state — same protocol, with extra attention to whether the joint-testing scenarios actually reached the reed-side mechanism they claim to test (the "prove the scenario reached the code" lesson from the fabric campaign, cited in `crucible/README.md`, applies especially hard here — a scenario that races a reed event against a shuttle poll and stays green must be checked for whether it genuinely raced, not just ran twice in sequence).
3. After round 3 closes: decide convergence on BOTH axes separately if round 3's own report distinguishes them — shuttle-alone correctness (rounds 1-2's axis) vs. reed×shuttle joint composition (round 3's new axis) — do not conflate a clean round 3 into "shuttle is done" without confirming both. Do not assume all 4 rotation slots are needed.
