# Discussion fix — round 3

Source review: `_mill/reviews/20260614-095338-discussion-review-r3.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- [NOTE] "Enforcement test must distinguish cwd-based filepath.Dir" — Decisions → paths-enforcement + Constraints: dropped the `filepath.Dir` clause entirely; the guard now bans only `os.Getwd` and `git rev-parse --show-toplevel` (the sole sources of a cwd/root), avoiding the fuzzy heuristic and false positives on the ~30 legitimate `filepath.Dir` uses.
- [NOTE] "muxpoc state migration must also move board.AtomicWrite anchor" — Decisions → cwd-not-worktree-root (muxpoc): named the two exact anchor points — `SaveState`'s `board.AtomicWrite(cwd, …)` base arg (`state.go:108`) and `socketArg`'s `os.Getwd()` (`cmd.go:289-294`) — and noted that `socketArg`'s currently-swallowed error is surfaced once `paths.Resolve` replaces it.

## Pushed Back

(none)
