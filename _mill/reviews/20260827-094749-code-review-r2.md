MILL_REVIEW_BEGIN
# Review: Add a local-only file category to weft — holistic

```yaml
duration_s: 176.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-27
```

## Findings

None. Cross-checked every card in all seven batches against the delivered source and found the
implementation matches the plan precisely, including the Shared Decisions and every batch-local
decision.

Specific points verified:

- `internal/fabricengine/merge.go` (cards 1-2): `MergeIn`/`Merge` merge the warp side only, weft
  `MergeStart`/conflict collection/pre-merge sync deleted, `WeftOutcome` stamped
  `mergeOutcomeAlreadyUpToDate` at both checkpoints, `unifyConflictPaths` called with a literal `nil`
  weft-conflicts argument.
- `internal/fabricengine/mergeguards.go` (cards 8-10): `pairDirtyReason`, `detachedHeadReason`,
  `syncedToUpstreamReason` narrowed to warp; `resolveMergeSources` dropped its `l` parameter and its
  refusal arm while keeping the weft SHA read; both `merge.go` call sites and the dead
  `lyxcwd.ResolveWorktree` call in `Merge` updated to match.
- `internal/fabricengine/destroy.go` (card 11): `resetMergeSides` narrowed to
  `(rec *Mutations, warpSHA string) error`; `ownedWeftCheckout`/`pathOwnershipWeftCheckout` fully
  removed (the prior round's non-blocking item is resolved — no dead enum case remains); all four
  call sites and `export_test.go`'s `ResetMergeSidesForTest` updated to the narrowed signature.
- `internal/fabricengine/pull.go`/doc.go (cards 14-16): weft arm is non-fatal end to end,
  `PartialPullError.WeftPulled` faithfully reports, `Error()` branches correctly, and every
  `PartialPullError` construction in `Pull` uses `result.WeftPulled` rather than a hardcoded `true`.
- `internal/fabricengine/cleanup.go` (card 18): `raddleFoldedBack` and the `Protected` gate it fed are
  gone; `force` documented as reserved; flag registration untouched.
- `CLAUDE.md`/`internal/fabriccli/fabric.go` (card 19): raddle clause and cleanup flag help corrected
  exactly as specified, without over-flattening to "never merged".
- `internal/fabricengine/pushanchored.go`/`mergestateactive.go` (cards 21-22): both new entry points
  match their required shape, `PushRebaseFree`/unwrapped `ErrPushRejected`, weft-only probe scope.
- `internal/shedengine/shed.go`/`run.go` (cards 25-26): `CommitStatus` field and `persist` restructure
  match exactly, including firing outside the write lock and on every persist call.
- `internal/loomrecipe/loomrecipe.go`/`internal/loomcli/wiring.go`/`cli.go` (cards 27, 29-30): "five
  told values" is now correct in both `loomrecipe.go:16` and `wiring.go`'s doc comment — the prior
  round's other non-blocking item is resolved. `newCommitStatusSeam`'s disposition order, both
  `ShedPaths` fill sites, and `commitStatusMessage` all match the spec precisely.
- Doc/design updates (`doc.go`, `CONSTRAINTS.md`, `manifest/designs/loom.md`, `manifest/designs/shed.md`)
  all correctly rewritten for the warp-only merge, the non-fatal pull, and the per-transition commit
  seam, with headings left intact per the Markdown Link Integrity invariant.
- Test coverage: `mergeweftlocal_integration_test.go` (5 scenarios), `weftguards_integration_test.go`
  (4), `cleanup_raddlegate_integration_test.go` (3), `pushanchored_integration_test.go` (4),
  `mergestateactive_integration_test.go` (4), `run_commitstatus_test.go` (5 properties, including the
  load-bearing lock-boundary property 5), and `wiring_commitstatus_test.go` (7 properties) all present
  and each individually verified against its card's required scenarios. Card 12's renamed/split
  inversions (`TestMergeIn_NoWeftCounterpart_NothingMutated`,
  `TestMergeIn_DirtyWarpRefuses`/`TestMergeIn_DirtyWeftDoesNotRefuse`,
  `TestMergeCrucible_WeftDetachedDoesNotRefuse`) and card 17's CLI renames
  (`TestRunCLI_MergeNonexistentBranchReportsSourceNotFound`,
  `TestRunCLI_MergeStageResolvesAWarpSideConflict`,
  `TestRunCLI_MergeContinuePartialStagingListsTheRemainingPaths`) are all present.

Neither prior non-blocking item is escalated; both are now fixed in source, not merely still
non-blocking.

## Verdict

APPROVE
Implementation matches the plan across all seven batches with no deviations found; both prior non-blocking items are now resolved.
MILL_REVIEW_END
