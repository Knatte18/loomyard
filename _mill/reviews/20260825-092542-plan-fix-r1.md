# Fixer report: plan-fix round 1

## Fixed

- `20260825-092054-plan-review-r1.md` — "[NIT:decision] `Plan-Revalidate` row (discussion's round-5 BLOCKING decision) is dropped".
  Verified accurate: `_mill/discussion.md` was rewritten by mill-start's round-5 gap-fix pass (commit `b63e6a9b`) after this planner had already read it, so the plan was written against a stale copy that predated the `Plan-Revalidate` decision.
  Re-read the current file and its round-4-to-round-5 diff in full before fixing, so the whole round-5 delta is covered rather than the one finding alone.
  Applied across the plan:
  - `00-overview.md` — the count Shared Decision rewritten to sixteen recipe rows / fifteen table entries / fourteen registry engines;
    a new Shared Decision recording the `Plan-Revalidate` row, its `on_stuck: Plan-Write` choice, and the live-lock the rejected `Plan-Bouncer` target would cause;
    a third new Shared Decision recording the stale-verdict replay hazard as confirmed-present and filed;
    `internal/loomrecipe/revalidate_test.go` added to All Files Touched.
  - `03-plan-review-segment-rows.md` — card 9 now adds `NamePlanRevalidate`, the third recipe row, `Plan-Bouncer.on_done: Plan-Revalidate`, the `loomRowEngines` entry mapping it to the already-registered `PlanValidate` engine, the `shape_test.go` row, sixteen-row counts in `recipe_test.go`, and a seventeen-entry `wantSequenceOrder`;
    a new card 11 adds the scenario test proving a fixer-introduced format regression is caught before `Batchifier` and bounces to `Plan-Write`.
  - `04-docs-and-stale-text-sweep.md` — every count updated, the `Plan-Revalidate` producer-table row added to card 12's instructions, and the cards renumbered 12–17 to keep card numbering globally unique after batch 3 grew a card.

- Same finding, hazard half — `_mill/discussion.md`'s "Known hazard … that the plan writer must confirm rather than assume".
  Confirmed at plan time rather than assumed: `loomshed.NewPlanWrite`'s rotate-and-commit decorator rotates the plan artifact directory only, and `shedadapters.archiveStaleOutputs` only ever runs over the Bouncer's own next spawn's outputs — so nothing clears round *n*'s verdict and ledger when `Plan-Revalidate` bounces to `Plan-Write`, and `judged(n)` re-settles `Done` over a plan the judge never saw.
  Filed rather than fixed, per the discussion's own instruction, as a third bullet on the follow-up roadmap item card 15 adds, and recorded in the overview's Shared Decisions.

## Pushed Back

_(none — the round's single finding was accurate and carried no harm under the decision tree.)_
