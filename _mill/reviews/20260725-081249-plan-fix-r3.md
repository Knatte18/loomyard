# Plan-fix report — round 3

Source review: `_mill/reviews/20260725-081130-plan-review-r3.md` (verdict REQUEST_CHANGES,
1 BLOCKING + 1 NIT).

## Fixed

- [BLOCKING] "Card 12 misses launchAgent stub update in main_test.go" — extended Card 12's
  Requirements to update every `launchAgent` stub in `main_test.go` from the 3-arg to the new
  4-arg `(hostRepoDir, claudePath, instruction, binDir string)` signature (and to stub
  `devBinPath` to a non-existent path in suite-routing tests so they resolve `sourceProd`);
  added `tools/sandbox/suite.go` and `tools/sandbox/resolve.go` to Card 12's Context; retitled
  the card and its commit to cover launchAgent. Without this the batch-3 verify would fail to
  compile.
- [NIT] "prependPath PATH-name casing on Windows" — Card 5's `prependPath` Requirements now
  specify case-insensitive variable-name matching (split on first `=`, uppercase key, compare
  to `PATH`) so a Windows `Path=...` entry is edited in place, not duplicated. Card 6's test
  Requirements now assert the lowercase `Path=` in-place edit.

## Pushed Back

_None._
