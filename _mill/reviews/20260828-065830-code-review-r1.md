MILL_REVIEW_BEGIN
# Review: Producer-agnostic final-summary artifact + wire Finalize — holistic

```yaml
duration_s: 122.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-28
```

## Findings

No findings.

Verified end-to-end across all four batches:

- `internal/summaryparser` (batch 1) is a genuine stdlib-only leaf: `FileName`, `Path`, `Summary`,
  `Parse`, `CommitMessage` match the plan's signatures exactly, error prefixes are `summaryparser: `
  throughout, `CommitMessage`'s trim behaviour matches the commitmessage-body-trim decision
  case-for-case, and `leaf_enforcement_test.go` enforces the empty allowlist.
- Batch 2's rename sweep is complete and consistent: `landingshed.Deps.WebsterDir` is gone,
  `FinalSummaryPath` is in place with the required doc comment, `internal/loomcli/landingdeps.go`
  fills it via `summaryparser.Path(geom.WebsterDir)`, and `seam_enforcement_test.go`'s allowlist drops
  `websterengine` and adds `summaryparser`. The two stale field-count comments
  (`shedrecipe/recipe.go`, `loomcli/landingdeps_test.go`) are both corrected to fifteen/sixteen. All
  four deleted `websterengine` names (`SummaryFileName`, `SummaryPath`, `Summary`, `ParseSummary`) have
  zero remaining production or test references anywhere in the tree; every retargeted call site
  (`websterengine/summary.go`, `runlevel.go`, `webstercli/recordbatch.go`, `shedadapters/webster.go`
  + `doc.go`) now goes through `summaryparser`. The three files the plan named to keep bare
  `"summary.md"` literals (`recordbatch_test.go`, `runlevel_test.go`, `smoke_test.go`) were left
  untouched, matching the production-only-invariant-scope decision.
- Batch 3's `Finalize` change is correct and precisely placed: the artifact parse runs before
  `CommitStatus`, before merge-in, and before the parent-side merge (verified against
  `finalize_test.go`'s ordering assertions and `commitstatus_test.go`'s explicit event-order checks);
  `MergeOptions.Message` is set unconditionally regardless of `Squash`; the step-5 retry reuses the
  same `mergeOpts` value (asserted by
  `TestFinalize_MergeInRequired_RetryCarriesSameComposedMessage`); both `NewFinalize` and `NewPublish`
  reject an empty `FinalSummaryPath` with distinct, correctly named errors; and the integration tests
  write real artifacts to disk as required for the tagged build.
- Batch 4's docs are internally consistent and already in their final state: `final-summary-spec.md`
  covers every required point (format/validation, sole-parser, told-path, both consumers, the
  Finalize/Publish disposition split, producer-appended sections) and avoids warp/weft/host vocabulary;
  `webster-spec.md` correctly shrinks to a pointer, fixes both the Finalize/Publish PR-text mislabel
  and the "dumps verbatim" mischaracterization, and keeps its own writer-side content;
  `docs/overview.md` gets the `summaryparser` module row, the `final-summary-spec.md`/`loom-plan-spec.md`
  documentation-lifecycle list entries (repairing the pre-existing `loom-plan-spec.md` omission), and a
  corrected `Other docs` entry set; `manifest/roadmap.md` moves the item to `## Done` with the literal
  `1.` marker and a link to the new spec.
- No out-of-plan files, no duplicated helpers across batches, and `fabricengine.MergeOptions` already
  carries the `Message` field the plan assumed needed no engine-side change.

## Verdict

APPROVE
All four batches are complete, internally consistent, and match the plan and constraints precisely.
MILL_REVIEW_END
