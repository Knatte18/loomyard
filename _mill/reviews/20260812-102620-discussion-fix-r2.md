# Discussion fixer report — round 2

Source review: `_mill/reviews/20260812-102526-discussion-review-r2.md` (APPROVE, 0 blocking, 3 NITs).

## Fixed

- `20260812-102526-discussion-review-r2.md` — "Files-to-change omits the four inbound-reference docs": the four `manifest/designs/` files now carry an explicit bullet naming the six references they hold, with the reference table declared authoritative for the docs half.
- `20260812-102526-discussion-review-r2.md` — "`UpdateJSON`'s behaviour on an undecodable existing file is unstated": the signature decision now states it errors with no write (inheriting `ReadJSON`'s precedent at `state.go:73-75`), with the rationale that zero-value-and-overwrite would silently discard a corrupt index's entries; a matching scenario was added to the `internal/state` test list and a Q&A entry records the disposition.
- `20260812-102526-discussion-review-r2.md` — "`internal/state`'s \"package header\" is not a godoc package comment": verified that `state.go:1-5` is separated from `package state` at `:7` by a blank line at `:6` and that the package has no `doc.go`, so the block never reaches `go doc`. The rule now lands in a new `internal/state/doc.go` package comment — the in-repo analog is `internal/fabricengine/doc.go` — and `Files to change`, the `no-new-constraints-invariant` decision, and the Q&A log were all updated to match.

## Pushed Back

None.
All three findings verified accurate against the tree, and none of the fixes breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope.
