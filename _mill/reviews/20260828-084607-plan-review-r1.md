MILL_REVIEW_BEGIN
# Review: reed: header pane's boot sometimes leaves shell/log noise in its scrollback — holistic

```yaml
duration_s: 396.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-28
```

## Findings

### [BLOCKING:scope] Requirements name identifiers whose defining file is absent from Context
**Location:** batch 1 card 5; batch 2 card 6
**Issue:** Card 5's Requirements direct documenting `ReedState.HeaderPaneID`, defined in `internal/reedengine/state.go`, but `state.go` is in neither Card 5's `Context:` (`lifecycle.go`, `probe.go`, `io.go`) nor `Edits:` (`doc.go`). Card 6's Requirements direct documenting that the ldflags stamp "is what makes `stencilstore.ModeFor(buildinfo.IsDev())` return `ModeDev`", but neither `internal/stencilstore/stencilstore.go` nor any `internal/buildinfo` file is listed in Card 6's `Context:` (`smoke_lifecycle_test.go`) or `Edits:` (`smoke_test.go`).
**Fix:** Add `internal/reedengine/state.go` to Card 5's `Context:`, and add `internal/stencilstore/stencilstore.go` (and/or `internal/buildinfo`) to Card 6's `Context:`. Mitigating: `doc.go`'s existing paragraph already spells out `HeaderPaneID` in full and Card 6's own requirement text already states the fact to transcribe verbatim, so neither gap forces genuine cold-start exploration — it is a mechanical Context-list omission, not a design problem.

## Verdict

REQUEST_CHANGES
One mechanical Context-completeness gap spanning two cards; otherwise a thorough, source-grounded, internally consistent plan.
MILL_REVIEW_END
