MILL_REVIEW_BEGIN
# Review: reed: header pane's boot sometimes leaves shell/log noise in its scrollback — holistic

```yaml
duration_s: 140.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-28
```

## Findings

None. The implementation matches all three batches' cards, the overview's Shared Decisions, and CONSTRAINTS.md precisely.

Verified in detail:
- **Batch 1**: `Engine.suppressHeaderLaunch` (lock.go) initialised from `testing.Testing()`, documented per card 1; `lifecycle.go` dropped the `testing` import and threads `e.suppressHeaderLaunch` through `headerLaunchLine`. `splitPaneAboveLocked`/`splitHeaderPaneAtTopLocked` both carry the new `launchCmd` parameter, including the even-vertical retry path (`internal/reedengine/lifecycle.go:573-637`). `send-keys` typing into the header pane is gone; `launchStrandLocked`/`sendKeysLiteralArg` (spawn.go) are untouched, matching the "strand panes untouched" batch-local decision. All four new/edited tests in `lifecycle_test.go` (P1 pin, suppressed-default pin, retried-split pin) match card 2/4 requirements exactly, including the substring-not-exact-match rationale for posix/pwsh quoting. `doc.go`'s header-pane paragraph was updated per card 5, with no touch to the stencil-seed or tmux-subcommand-set material reserved for batch 2/existing text.
- **Batch 2**: `clihelp.SkipStencilSeedAnnotation`/`AnnotationEnabled` (annotations.go) match card 8 literally, pinned by annotations_test.go. `cmd/lyx/stencilseed.go`'s `skipStencilSeed` extraction, signature change to `seedStencils(cmd *cobra.Command)`, and early-return ordering (testing.Testing() → skipStencilSeed → stencilSeedTarget) match card 9 exactly; CONSTRAINTS.md's Stencil Ownership Invariant bullet was amended verbatim-plus-one-sentence, correctly line-broken. `internal/reedcli/header.go` carries the annotation on `headerCmd` (card 10) with Use/Short/Long/RunE/flag registration otherwise untouched. `smoke_headerseed_test.go`'s `TestSmokeHeaderDeclinesStencilSeedPass` arranges both Warn emitters exactly as card 7 specifies (dev-refusal via a same-stamp/drifted-body board file, port-back drift via a differently-drifted `contracts/stencils` copy) and asserts empty stderr only. `cmd/lyx/stencilseedgate_test.go` covers exactly the five predicate rows and the annotation-registration check card 11 calls for, with no forbidden ordering/spawn assertion.
- **Batch 3**: `headerBlockingPayload` (header.go) returns the exact `ED2+ED3+home` + trimmed-text bytes, replacing the blocking branch's inline `fmt.Fprint` call while `blockForever()` stays untouched. `header_test.go`'s `TestHeaderBlockingPayload` covers CRLF-trim, no-trailing-newline, and interior-newline-preserved cases without ever driving `--blocking` or the enveloped path through `RunCLI`, matching card 13's Test Tier Purity concern. `smoke_headerscrollback_test.go` holds both `TestSmokeHeaderPayloadClearsPaneScrollback` (the direct `ED 3` proof, 50 junk lines + payload into a private harness pane) and `TestSmokeHeaderPaneScrollbackIsClean` (the composite B: boot/resume/heal, each asserted via `capturePaneScrollback` for the header line and no other non-empty line), matching cards 14/15 including the shared-file batch-local decision.
- **Cross-batch contracts**: `capturePaneScrollback` and `buildLyxBinaryWithLDFlags` (both landed in batch 2's `smoke_test.go` per its batch-local decision) are consumed correctly by batch 3's new smoke tests; signatures match at every call site. `clihelp.SkipStencilSeedAnnotation`/`AnnotationEnabled` flow correctly from clihelp → stencilseed.go → header.go with no duplicated literal anywhere. `stencilstore`/`fabricengine` helper signatures (`Path`, `ApplyStamp`, `BodyHash`, `RelPath`, `ModeFor`, `Reconcile`, `StencilsDir`, `CommitSeededStencils`) all match their call sites in `stencilseed.go` and both smoke-header test files.
- **No out-of-plan files**: every file in the manifest is accounted for by some batch's Context/Edits/Creates list.
- **No global utility duplication**: `capturePane` vs `capturePaneScrollback` are deliberately separate per card 6's own documented rationale (viewport vs full scrollback), not a duplication.

## Verdict

APPROVE
Implementation is faithful to the plan, Shared Decisions, and CONSTRAINTS.md across all three batches with no findings.
MILL_REVIEW_END
