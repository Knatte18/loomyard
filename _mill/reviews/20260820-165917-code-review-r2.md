MILL_REVIEW_BEGIN
# Review: Extract scout into its own standalone repo — holistic

```yaml
duration_s: 475.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-20
```

## Findings

### [BLOCKING:scope] card 38's fix edited a Context-only file and reversed an explicit instruction
**Location:** `/home/knatte/Code/quarry/wts/quarry/internal/cli/paths_test.go:81-95, 143-156`
**Issue:** Card 38 (batch 5) lists `paths_test.go` under `Context:` (read-only) and `cli_test.go` under `Edits:` only, and explicitly says "Do not remove `t.Parallel()` from either test — the fix is isolating this test's seam, not de-parallelizing the suite." The shipped code removed `t.Parallel()` from both `TestResolveConfigPath_UserConfigDirError` and `TestResolveStateDir_UserCacheDirError` in `paths_test.go` (both now carry "Deliberately not t.Parallel()" comments), and also removed it from `TestRunCLIIn_TargetDirResolvesAgainstInjectedSeamCwd` in `cli_test.go`. This edits a file outside its declared scope and contradicts the card's own explicit constraint, with no record of the deviation in the plan or discussion.
**Fix:** Update card 38 to declare `paths_test.go` under `Edits:`, and either restate the de-parallelization as the intended fix or revert to the literal instruction (noting the literal instruction alone would still leave both tests writing the same package-level var concurrently under `t.Parallel()`, so de-parallelizing is likely the more correct choice — but the plan must say so).

### [BLOCKING:consistency] duplicate card number 38 across batch 5 and batch 6
**Location:** `_mill/plan/05-quarry-live-and-equivalence.md:122` (Card 38: fix the test-isolation race) and `_mill/plan/06-lyx-removal.md:30` (Card 38: delete the two scout packages)
**Issue:** `contracts/specs/loom-plan-spec.md:161` states cards are numbered flat 1..N across the whole plan with "no batch-scoped restart." This plan follows that convention everywhere else (batch 4 ends at 33, batch 5 starts at 34), but batch 6 also starts at 38 instead of 39 — round 2's insertion of the race-fix card into batch 5 was not reflected in batch 6's numbering. Two cards now share the identifier "Card 38" with unrelated content, breaking any card-number cross-reference (commit messages, the port log, this review).
**Fix:** Renumber batch 6's cards 39-46 (shifting by one) so numbering stays flat and unique across the plan.

### [BLOCKING:design] quarry's slog Warn-level handler governs only 2 of 7 ported log call sites
**Location:** `/home/knatte/Code/quarry/wts/quarry/quarry/ensureserver.go:26-32` vs. `/home/knatte/Code/quarry/wts/quarry/quarry/lspclient.go:575-609`
**Issue:** Card 12 requires a package-level `slog` handler "so quarry's own logging goes to stderr at slog.LevelWarn unless the process has configured otherwise," declared once in `ensureserver.go`. The shipped code declares `defaultLogHandler` there and calls it explicitly at `ensureserver.go`'s 2 sites (`defaultLogHandler.Warn`/`.Info`), but `lspclient.go`'s 5 `slog.Warn` sites call the bare package-level `slog.Warn`, which resolves to Go's stdlib `slog.Default()` — a separate logger never wired via `slog.SetDefault(defaultLogHandler)` (confirmed absent repo-wide). The doc comment's claim of one Warn-level handler governing "quarry's own logging" is false for 5 of 7 sites; a caller who reconfigures `slog.SetDefault` to control quarry's verbosity affects `lspclient.go` but not `ensureserver.go`, and vice versa for the local `defaultLogHandler`.
**Fix:** Either call `slog.SetDefault(defaultLogHandler)` once (e.g. in an `init()`) so the bare `slog.Warn` calls route through it, or change all 7 call sites to call `defaultLogHandler.Warn`/`.Info` explicitly.

### [NIT:consistency] port-equivalence.md's comparison count doesn't match its own table
**Location:** `/home/knatte/Code/quarry/wts/quarry/docs/port-equivalence.md:71, 123`
**Issue:** The prose states "All 26 comparisons matched byte for byte" and "All 26 query/verb comparisons," but the verdict table (lines 76-102) has 27 data rows.
**Fix:** Recount the table rows and correct both prose statements to 27 (or state which row is not actually a distinct comparison, if one was double-counted intentionally).

## Verdict

REQUEST_CHANGES
Two plan-discipline breaks (out-of-scope test edit reversing an explicit instruction; duplicate card numbering) plus a real logging-config inconsistency in the ported engine.
MILL_REVIEW_END
