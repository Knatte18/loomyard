# Plan fix — round 6

Source review: `20260813-063922-plan-review-r6.md` (holistic, REQUEST_CHANGES, 3 BLOCKING, 1 NIT — one finding demoted from BLOCKING by the stage ceiling).

## Fixed

- `20260813-063922-plan-review-r6.md` — "[BLOCKING:scope] Card 69's zero-hit Copy* grep fails on files no card touches" and "[BLOCKING:scope] The lyxtest sweep renames the qualifier but not the dangling helper name": verified accurate, and fixed together, because they are one problem seen from two sides — the per-call-site cards replace call expressions and nothing in the plan ever touched prose naming the same helpers.
  Confirmed the scale: a bare-token grep for the four retired names returns 244 hits against 141 call expressions, and `comm` against the `lyxtest`-token file list confirms exactly two files (`internal/configcli/configcli_test.go`, `internal/loomengine/config_test.go`) that batch 1 card 2's sweep can never select because they carry no `lyxtest` token at all.
  Fixed by adding **card 69, a closing prose sweep** to batch 11, placed after the deletions and before the gate, with those two orphan files in its `Edits:`.
  The card deliberately forbids a `perl -pi -e` substitution and says why: the four retired names have no one-to-one successor (three collapse onto `hubforge.NewHub`, `CopyWarpHub` splits between `hubforge.NewHub` and `gitkit.CopyRepo`), and sentences like `loomengine/config_test.go`'s "no `CopyWeft`, no `SeedConfig`" are making a *tiering* claim that only stays true if the replacement names the right successor.
  Cards 69–76 renumbered to 70–77 across batches 11 and 12, with every internal cross-reference updated;
  batch 11's frontmatter card count is now 5.
  I did not take the finding's literal suggested fix (add the two files to batch 4 card 22 and batch 6 card 36) because that would have fixed the two files it named while leaving the ~85-hit prose gap the second finding identified — the sweep card closes both.
- `20260813-063922-plan-review-r6.md` — "[BLOCKING:scope] fabriccli's hand-rolled _board scaffolding is stand-in-hub scaffolding, unnamed by card 39": verified accurate and fixed.
  Confirmed at `internal/fabriccli/cli_test.go`'s `TestRunCLI_EnvMapToOption`, whose own comment states the premise that no longer holds — "`CopyPaired` never materializes a `_board` dir, so seed it directly here".
  Card 39 now names the test, requires deleting the `os.MkdirAll` + `os.WriteFile` pair and the comment explaining it, points at batch 3 card 17's `TestNewHub_IsARealHub` as the assertion that licenses the deletion, and adds the branch for the case where the test needs a non-default fabric config (`hubforge.SeedFabricConfig`, not a hand-written file).
  The card also records *why* it is named explicitly rather than left to the mapping table: a mapping table retargets fields, it never tells you to delete a block.
- `20260813-063922-plan-review-r6.md` — "[NIT:consistency] fabric_test.go's header comment goes stale; card 51 doesn't touch it" (demoted from BLOCKING): verified accurate and fixed in card 51.
  The header claims `open_integration_test.go` pins the same contract "against a real paired `lyxtest` fixture (`CopyPaired`; …)", and card 48 migrates that very file onto `hubforge.NewHub` in the same batch — so it becomes a false claim about a sibling file, not just an unrenamed word.
  Card 51 now requires rewriting it, keeping the two test names, and cites batch 2 card 12's identical warning as the reason.

## Pushed Back

_None._
