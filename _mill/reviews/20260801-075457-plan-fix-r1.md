# Plan fix report — round 1

Source review: `_mill/reviews/20260801-074831-plan-review-r1.md` (verdict REQUEST_CHANGES, 3 BLOCKING + 1 NIT). All four findings VERIFIED accurate against the code and FIXED (no harm; the user directed auto-accept of the recommended resolution for every round).

## Fixed

- [BLOCKING] "fabriccli/fabric.go's per-worktree LoadConfig never migrated to repo-wide BoardDir" — CONFIRMED: `grep` shows eight `fabricengine.LoadConfig(cwd)`+`NewTopology(cfg)` sites in `fabric.go` (:280,317,378,411,443,472,501,531) plus a ninth in `weft_verbs.go:124` (`LoadConfig(weftBaseDir)`), and `setupCLIRepo` (cli_test.go:38) masks the regression by hand-writing a per-worktree `fabric.yaml`. Fixed by broadening Batch 4 card 17 (retitled "fabriccli reads repo-wide fabric config + clone --subpath") to migrate all nine sites to `LoadConfig(hubgeometry.BoardDir(l.Hub))`, adding `internal/fabriccli/weft_verbs.go` to its Edits and `topology.go`/`config.go` to Context; and updating card 19 to fix `setupCLIRepo` to seed config at `BoardDir`. Added `weft_verbs.go` to All Files Touched.
- [BLOCKING] "hostLayoutFor's Resolve(worktreeRoot) fallback breaks under the new cwd gate; card 2 vs card 4 self-contradict" — CONFIRMED: `hostlayout.go:25` calls the gated `hubgeometry.Resolve(worktreeRoot)`, which under card 2's gate would hard-error `ErrCwdOutsideAnchor` for a subpath-anchored hub, and card 4's assertion contradicted card 2's spec. Fixed per the discussion's own gate-scope caveat: Batch 1 card 2 now adds a gate-free `hubgeometry.ResolveWorktree` (factored `resolveCore(cwd, applyGate)`); Batch 2 card 7 rewires `hostLayoutFor`'s non-sibling fallback to `ResolveWorktree` (added `hostlayout.go` to its Edits + All Files Touched); card 4's test now asserts `ResolveWorktree` does NOT error (gate-free) while the gated `Resolve(worktreeRoot)` DOES error — removing the contradiction.
- [BLOCKING] "Card 12 offers a template-resolution option that risks a configsync↔fabricengine import cycle" — CONFIRMED: batch 4 card 16 establishes `fabricengine -> configsync`, so the parenthetical `fabricengine.ConfigTemplate` alternative in card 12 would close a cycle. Fixed by mandating `configreg.Template("fabric")` only and removing the `fabricengine.ConfigTemplate` alternative, with the cycle rationale stated inline.
- [NIT] "Clone anchor mismatch check cannot distinguish 'unset --subpath' from 'explicit root'" — CONFIRMED accurate. Fixed by documenting it as an accepted limitation in Batch 4's Batch Scope (the mismatch guard targets a wrong non-default value; `anchor-value-always-explicit` makes the record authoritative on adopt; a distinct sentinel default is deliberately not added for a benign edge).

## Pushed Back

(none — all four findings were factually accurate, consistent with the discussion's decisions, and fixing them caused no harm.)
