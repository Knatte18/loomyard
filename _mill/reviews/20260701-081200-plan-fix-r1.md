# Plan fix — round 1

## Fixed

- Ref: `_mill/reviews/20260701-080816-plan-review-r1.md` — "Card 11 test lands in integration-tagged file, unreachable by verify": moved the invalid-URL `cloneRepo` test target from `clone_integration_test.go` (`//go:build integration`, unreachable by the batch's plain `go test`) to `clone_test.go` in batch 02, card 11.
- Ref: `_mill/reviews/20260701-080816-plan-review-r1.md` — "SetValues silently drops a valid key missing from a pre-existing file": redesigned `SetValues`'s working-tree construction in batch 01, card 1 so the working tree is always the full template shape with `existing`'s overrides merged in (mirroring `Reconcile`'s own merge step), guaranteeing every template-known key has a real settable node regardless of what `existing` contains. Added a new test case (g) to card 2 covering a template-known key absent from a partial `existing` file.

## Pushed Back

(none)
