# Plan fix — round 1

Source review: `_mill/reviews/20260819-094820-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 4 NIT).

## Fixed

- r1 holistic — "loom-status-spec.md's header blockquote dangles after Card 2's rewrite": batch 1 card 2 now also deletes the header line pointing at the stale-content note it already deleted.
- r1 holistic — "`validOutcomes` left with no stated disposition in the coherence.go rewrite": batch 1 card 2 now names all three stale package-level maps for deletion, with the reason Go's unused-var rule does not catch a surviving package-level one.
- r1 holistic — "New guard test's file/name pairing breaks the repo's established convention": the guard file is renamed to `internal/loomshed/seam_enforcement_test.go` throughout batch 2 card 3, the overview's Shared Decisions, and All Files Touched; the card states why the seam filename is the correct half of the two in-repo pairings for a six-dependency package.
- r1 holistic — "Fixture Context omits most of the goodplan directory it is told to mirror": batch 3 card 11's requirement is reworded to state that the fixture plan is self-authored and that the overview plus one representative card file is the whole reference, so the three unlisted card files are deliberately not read rather than accidentally omitted.

## Pushed Back

_(none — all four findings were factually accurate, and no fix broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task.)_
