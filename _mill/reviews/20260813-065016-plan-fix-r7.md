# Plan fix — round 7

Source review: `20260813-064926-plan-review-r7.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 2 NITs).

## Fixed

- `20260813-064926-plan-review-r7.md` — "[BLOCKING:scope] Batch 6 card 36 lacks the Context needed to resolve its own \"provably a no-op\" test": verified accurate and fixed.
  Confirmed at `internal/fabricengine/clone.go` (weft-primary step, ~lines 449-466): `CloneHub` computes `suffixedBranch := WeftBranchName(warpBranch)` and runs `checkout -b <suffixedBranch>` on the weft during the clone.
  `setupPreflightFixture`'s own `git checkout -b fabricengine.WeftBranchName("main")` therefore does not become redundant on a real hub — it **errors** with "branch already exists" and takes every test calling that helper with it.
  Card 36's instruction is upgraded from a conditional check ("drop it only if it is provably a no-op") to a hard deletion requirement with the reason stated, and `internal/fabricengine/clone.go` is added to its `Context:` so the claim is verifiable at the point of use rather than taken on faith.
  Also noted in the card that the two `git checkout -b "warp-only"` calls elsewhere in the same file are unrelated and stay, since a bare reading of "delete the checkout line" could otherwise catch them.
- `20260813-064926-plan-review-r7.md` — "[NIT:scope] Batch 4 card 27's nested-anchor resolution cites lyxcwd/fabricengine identifiers outside Context": fixed by adding `internal/lyxcwd/anchor.go` (home of `ValidateAnchorRel`, the "no existence requirement" claim) and `internal/fabricengine/junctionnames.go` (home of `BoardDir`) to card 27's `Context:`.
- `20260813-064926-plan-review-r7.md` — "[NIT:consistency] Verified plan claims otherwise hold up well under source cross-checking": no action, and none requested — the finding records a clean cross-check of the string literals, the 9/12/21 `CopyWarpHub` split, the 37/38/7 `Copy*` counts, the nine `NewPairedForTest` call expressions, card numbering 1-77 with no gaps, the DAG, and the two owner-map targets.

## Pushed Back

_None._
