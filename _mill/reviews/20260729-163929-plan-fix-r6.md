# Plan-fix report — round 6

Source review: `_mill/reviews/20260729-163929-plan-review-r6.md` (reviewer opus · xhigh; verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT). Final authorized verification round. Both findings verified accurate against source; a genuinely new BLOCKING that five prior rounds missed.

## Fixed

- [BLOCKING] "Batch 2 breaks nested-junction integration test" — verified `remove_junctions_integration_test.go:33` (`TestRemove_TearsDownNestedJunction`) wires with `nestedLayout` (`RelPath == "sub"`, line 61) using a `newFabricFixture` that seeds `fabric.yaml` only at the weft ROOT. After card 6, `WireJunctions` loads config from `filepath.Join(l.WeftWorktreePath(slug), "sub")` (`<slug>-weft/sub`) — the correct real-nested-layout config location per the `weft_verbs.go:112` weft-base convention — where no config exists, so the test fails under batch-2 `verify:` (and `Remove`→`removeHostJunction` at line 74 hits the same). This is a test-fixture gap, not a code-design bug (the baseDir is right). Card 6 now also edits `remove_junctions_integration_test.go` to seed `fabric.yaml` at the nested weft base (`ConfigDir`/`ConfigFile(Join(WeftWorktreePath(slug), "sub"), "fabric")` + `ConfigTemplate()`) before wiring; added the file to card 6 `Edits:` and All Files Touched, and corrected batch 2's Batch Tests "existing tests stay green" claim to name this nested exception.
- [NIT] "Card 15 marks design slice 1 done against an overclaiming description" — verified `fabric-unified-view.md:104`'s slice-1 text claims migration of `_lyx`/`_raddle`/`_board` hardcoded wiring, but the delivered scope migrates only `_lyx`/`_pattern` (weft-backed) while `_board`/`_portals`/`_launchers`/`_raddle` stay hardcoded via `HubReservedNames()`. Card 15 now requires the "done" annotation to correct that overclaim by noting the scope split (hub-structural names stay hubgeometry-owned; only the weft-backed junction name-set comes from `pathspec`).

## Pushed Back

(none — both findings were factually accurate; applied under the fix-everything default.)
