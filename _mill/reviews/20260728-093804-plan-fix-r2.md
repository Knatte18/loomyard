# Plan fix report — round 2

Source review: `_mill/reviews/20260728-093706-plan-review-r2.md` (verdict REQUEST_CHANGES, 1 blocking, 2 NIT).

## Fixed

- `20260728-093706-plan-review-r2.md` — "[BLOCKING] Token cache path has no test-isolation seam": verified and accurate, and the asymmetry the finding points at is the real defect — card 25 gave the `gh` shell-out an explicit injectable seam *for exactly this reason* while card 26 named a cache path with no override mechanism at all, leaving card 30's "fully hermetic" requirement unsatisfiable by an implementer who followed card 26 literally. Card 26 now requires the directory to be resolved by reading `%LOCALAPPDATA%` / `$XDG_CONFIG_HOME` / `$HOME` **and nothing else** — no hardcoded absolute path, no `os.UserConfigDir` indirection — so a test redirects the whole cache with `t.Setenv`, and states that a resolution finding no usable variable is a cache miss rather than an error. Card 30 now requires every case to redirect to a `t.TempDir()` before touching anything, plus one case that asserts the redirection actually took effect (an empty temp dir must report a miss, not silently fall back to a real user path) — a redirection nobody checks is the same failure wearing a different hat.
- `20260728-093706-plan-review-r2.md` — "[NIT] hasUnpushed 'three new states' miscounts against the existing spike": verified against `internal/gitnativepoc/read_test.go`, which carries `AheadOfUpstream`, `UpToDate`, `Behind` and `NoUpstreamConfigured` subtests — so of the five states card 8 enumerates, three are carried over and only "configured but never fetched" and the failure-swallowing path are genuinely new. Corrected the count to two and added the sharper point the miscount was obscuring: none of the five has ever been compared against a live CLI oracle, which is what actually changes here. Repaired a sentence fragment the edit left behind.
- `20260728-093706-plan-review-r2.md` — "[NIT] ACL requirement lacks API specificity and in-repo precedent": accurate. "Use `golang.org/x/sys` for the ACL work" named a module and no API, on the one piece of this batch that is both fiddly and security-relevant. Card 26 now names the target surface — `SetNamedSecurityInfo` / `SetKernelObjectSecurity` with `PROTECTED_DACL_SECURITY_INFORMATION`, so inheritance is **broken** rather than merely supplemented — and adds `internal/fslink/fslink_windows.go` to Context as the repo's only existing direct `golang.org/x/sys/windows` syscall example, to be followed for error handling and build-tag shape.

## Pushed Back

None. All three findings were factually accurate, and no fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope.
