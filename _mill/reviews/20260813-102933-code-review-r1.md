MILL_REVIEW_BEGIN
# Review: lyxtest: build real fabric hubs, invert the lyxtest/fabric dependency — holistic

```yaml
duration_s: 308.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-13
```

## Findings

### [BLOCKING:scope] copyDirRecursive's symlink-refusal test coverage was dropped
**Location:** `internal/gitkit/gitkit_test.go` (whole file); helper at `internal/gitkit/gitkit.go:259-262`
**Issue:** Batch 11 card 68 explicitly requires keeping "every test covering a survivor: `MustRun`, `SeedConfig`, `CopyRepo`, `rewriteOriginURLInConfig`, `copyDirRecursive`'s symlink refusal, and the template builder's quiet-config properties," but the final `gitkit_test.go` has no test exercising `copyDirRecursive`'s symlink-refusal error branch (`os.ModeSymlink` check) at all — a `grep` for `Symlink`/`symlink` across `internal/gitkit` matches only the production comments/code, never a test.
**Fix:** Add a test that plants a symlink in a source tree passed to `copyDirRecursive` (directly, or via a `CopyRepo` template mutation) and asserts the documented error is returned, per card 68's own stated retention requirement.

### [NIT:consistency] Stale weftname-import-owner list in enforcement doc comment
**Location:** `internal/lyxcwd/enforcement_test.go:751`
**Issue:** `TestEnforcement_FabricVocabulary`'s doc comment still says "it also fails any file outside {fabricengine, fabriccli, gitkit} that imports internal/weftname," but the actual `weftnameImportOwners` map (lines 613-623) and CONSTRAINTS.md's Fabric Vocabulary Invariant both correctly include `internal/hubforge` in that set — the map's own inline comment (lines 617-621) explains the addition, but the higher-level doc comment above the test function was never updated to match.
**Fix:** Reword line 751 to name all four owners (`fabricengine`, `fabriccli`, `gitkit`, `hubforge`).

## Verdict

REQUEST_CHANGES
Migration is otherwise thorough and plan-consistent; one explicitly-promised test-coverage regression must be restored.
MILL_REVIEW_END
