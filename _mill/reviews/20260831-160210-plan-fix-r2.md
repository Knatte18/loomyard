# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260831-155343-plan-review-r2.md`

## Fixed

- `20260831-155343-plan-review-r2.md` — "[BLOCKING:design] Card 8's \"capture, never invent\" premise is infeasible for 401/403 and unhandled for symlink/submodule": card 8's capture list is now split into three explicit tiers. Three captures are unconditional (missing-path 404, real file, real directory) because a target for each is guaranteed reachable. Three are conditional with a stated fallback — symlink probe, submodule probe, and the above-1MB probe — each recorded as not observed and derived from the pinned 404 or file shape when no target exists, matching the disposition the >1MB case already had. The raw-symlink `curl` capture is best-effort with an explicit "record the question as unresolved" outcome, which is what batch 3's conditional sentence reads. The 401 and 403 bodies, plus the no-status and multi-line bodies, are now stated outright as derived from the observed 404 envelope rather than live-captured, with the reason given: a 401 cannot be produced without revoking the authentication every other capture depends on, and a 403 must not be deliberately triggered. The two probe fixtures stay unconditional `Creates:` because card 14 asserts a rejection scenario for each, and the card now says so explicitly.
- `20260831-155343-plan-review-r2.md` — "[BLOCKING:scope] Batch 1 never assigns the two new manual-check notes to a card": card 4's Requirements now requires appending the two live checks — one children-mode run and one guard trip against a large public repository — to the harness header's existing "NOT covered here" note, alongside the absolute-fixture and generator notes it already extends. This matches what card 13 already does for the sibling harness.
- `20260831-155343-plan-review-r2.md` — "[NIT:consistency] Card 6's boundary scenario doesn't name a fixture": the boundary scenario now pins `small-root-rec.json` on both sides, run with ceilings of 3 and 2, so the at-ceiling and one-over runs are provably one entry apart.
- `20260831-155343-plan-review-r2.md` — "[NIT:consistency] Card 11's fallback-cost sentence is ambiguous": reworded to state that both fallback calls are paid only when the raw attempt has already failed, and separately that the probe's own response carries the discarded base64 payload.

## Pushed Back

_(none — every finding verified accurate with no harm from fixing.)_
