# Discussion fix — round 5

Source review: `_mill/reviews/20260913-103221-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, none demoted).

## Fixed

- `20260913-103221-discussion-review-r5.md` — "[NIT:scope] Grammar block disposition less precise than :64/:66/:112": the `contracts/specs/llm-model-spec.md` Scope bullet now gives the Grammar section the same per-line treatment — `:8` and `:24` (the two productions) must admit the bare form, `:13` gains the bare-token rule, and the `:15-19` yaml example block gains a shorthand line.
- `20260913-103221-discussion-review-r5.md` — "[NIT:consistency] Inventory heading mislabels registry_test.go lines": Technical context's inventory is split into two labelled groups — literal `version=` bracket spellings (which the rename touches, with a note that only `:64`/`:66` and `docs/overview.md:302` actually re-spell) and canonical `"version"` map keys that a `version=` grep never matches (`registry_test.go`, `modelspec.go:46,116`, and the seven consumer sites).

## Pushed Back

None.
