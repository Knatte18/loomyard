MILL_REVIEW_BEGIN
# Review: fabric: config-driven junction list — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude Opus 4.8 (claude-opus-4-8)
reviewed_file: plan/
date: 2026-07-29
```

## Findings

### [BLOCKING] Batch 2 breaks nested-junction integration test
**Location:** Batch 2 (cards 6 & 7 + Batch Tests claim); affects `internal/fabricengine/remove_junctions_integration_test.go` `TestRemove_TearsDownNestedJunction`
**Issue:** That test resolves `nestedLayout` with `RelPath="sub"` and calls `WireJunctions(nestedLayout, slug)` (line 61) then `Remove` (line 74). Per card 6, `WireJunctions` now loads `junctionNames(filepath.Join(l.WeftWorktreePath(slug), l.RelPath))` = `<slug>-weft/sub`; `configengine.Load` errors when `<slug>-weft/sub/_lyx/config/fabric.yaml` is absent (config.go:57-60, no template fallback). The fixture only seeds/commits `fabric.yaml` at the weft **root** (`newFabricFixture`/`SeedConfig`), never under a `sub/` subpath, so `junctionNames` returns an error and `WireJunctions` fails at line 62 — batch 2's `verify:` (`go test -tags integration ./internal/fabricengine/`) fails. Card 7's own removeHostJunction path hits the same missing-config scenario it documents as residual risk (lines 78-83 would then also fail). The plan neither edits this test nor lists it in Edits/All Files Touched, and the Batch Tests section wrongly asserts every existing test stays green.
**Fix:** Add `remove_junctions_integration_test.go` to a batch-2 card (and All Files Touched) and seed a `fabric.yaml` at the nested weft base `<slug>-weft/sub/_lyx/config/` before `WireJunctions(nestedLayout, slug)`; correct the Batch Tests no-behavior-change claim to name the nested exception.

### [NIT] Card 15 marks design slice 1 done against an overclaiming description
**Location:** Batch 3 / card 15 (`manifest/designs/fabric-unified-view.md:104`)
**Issue:** Slice-1 text says this slice migrates `_lyx/_raddle/_board`'s "hardcoded junction wiring" into config, but the plan keeps `_board`/`_portals`/`_launchers`/`_raddle` hardcoded in `HubReservedNames()` (only `_lyx`/`_pattern` become pathspec-driven); marking it done leaves that inaccuracy.
**Fix:** When annotating item 1 done, note the scope split — hub-structural names stay hubgeometry-owned via `HubReservedNames()`; only the weft-backed junction name-set comes from `pathspec`.

## Verdict

REQUEST_CHANGES
Nested-junction test breaks batch 2's verify; plan must seed the subpath weft config.
MILL_REVIEW_END
