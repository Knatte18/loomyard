# Plan fixer report — round 1

Source review: `_mill/reviews/20260630-184751-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 2 NITs).
All three findings passed the `mill-receiving-review` decision tree (factually accurate, no harm) → fixed.

## Fixed

- `20260630-184751-plan-review-r1.md` "[BLOCKING] main_test.go breaks; not in any card's Edits" — Verified against `tools/sandbox/main_test.go:340`: `TestRun_SuiteRoutesSuiteToLaunch` calls `run([]string{"-parent", tmpDir, "suite"})` with no `-loomyard`, which the required-flag guard (Card 2) would fail; and once `runSuite` fetches a report on clean exit, the test's report-less stub would also hit the missing-report error. Added `tools/sandbox/main_test.go` to Card 7 Edits and to the overview's All Files Touched; Card 7 now updates `TestRun_SuiteRoutesSuiteToLaunch` to pass `-loomyard <t.TempDir()>` and have its stub write a valid report. Also generalized Card 7 so every success-path `runSuite` stub (`TestRunSuite_LaunchInvocation`, `TestRunSuite_Overrides`) writes a valid report — they would have failed for the same fetch reason.
- `20260630-184751-plan-review-r1.md` "[NIT] No coverage for the missing-`-loomyard` error path" — Card 7 now adds `TestRun_SuiteRequiresLoomyard` asserting a non-zero return and that `launchAgent` is not called when `-loomyard` is absent.
- `20260630-184751-plan-review-r1.md` "[NIT] SANDBOX-SUITE.md intro prose still says \"bug to file\"" — Verified `SANDBOX-SUITE.md:8` ("…as a LoomYard bug to file"); Card 4 now also rewords the "## What this is" intro to "a finding to record in the report".

## Pushed Back

_None._
