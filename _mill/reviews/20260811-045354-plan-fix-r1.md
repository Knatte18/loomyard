# Plan fix — round 1

Source review: `_mill/reviews/20260811-045354-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NITs).

## Fixed

- r1 holistic, `[BLOCKING:scope]` "Enumeration method misses `internal/websterengine/template.go`'s stale doc" — verified against `internal/websterengine/template.go`, whose `ConfigTemplate` doc enumerates the template's contents as "role model-specs, batchifier selection, and Master session configuration" and is falsified the moment card 5 deletes the `batcher: ""` line.
  Added `internal/websterengine/template.go` to card 5's `Edits:` with an explicit requirement to drop the "batchifier selection" clause, rather than to batch 3 as the finding offered: the clause is falsified by card 5's own edit, so deferring it would leave the tree self-contradicting for the whole of batch 2, and it is the exact sibling of the `Config` type-doc amendment card 5 already carries.
  Updated the overview's `All Files Touched` accordingly.
- r1 holistic, `[NIT:consistency]` (demoted from BLOCKING) "Card 5 names a webster config test that doesn't exist" — verified by listing `internal/websterengine/config_test.go`'s test functions;
  the literal carrying `Batcher: ""` lives in `TestConfigTemplate_RoundTripsThroughLoadConfig` and no `TestLoadConfig_TemplateDefaults` exists.
  Corrected both occurrences (card 5's `Requirements:` and batch 2's `## Batch Tests`).
- r1 holistic, `[NIT:consistency]` "\"Eleven sites\" count in doc-site-ownership doesn't reconcile with batch 3's card scope" — verified;
  the number came from the discussion's own site list and does not survive contact with either a file count or a clause count.
  Dropped the number from the `doc-site-ownership` decision and from batch 3's `## Batch Scope`, replacing it with the enumeration *method* plus an explicit statement of why a count is not asserted, and recorded the two sites the plain grep cannot reach.

## Pushed Back

_(none — all three findings verified accurate and none of the fixes broke existing functionality, conflicted with a documented decision, or destabilized work outside this task.)_
