# Discussion fix report — round 5

Source review: `_mill/reviews/20260825-120203-discussion-review-r5.md` (APPROVE, 0 blocking, 3 NIT).

## Fixed

- r5 `[NIT:scope]` "Digit-spelled row counts escape the stated sweep" — widened the stated sweep from `git grep -n sixteen` to the digit spellings as well (`16-row`, `16 rows`, `sixteen-row`), named both `internal/loomshed/stub.go` comment sites (lines 2 and 12) instead of one, added the two `internal/loomrecipe` test-file sites the widened sweep surfaces, and restated the `stub-stays` decision so it covers both comment sites and notes the claim itself changes, not just the count.
- r5 `[NIT:scope]` "Fixture stencil-seeding helper's own comment goes stale" — named `fixture_test.go`'s `seedBouncerStencils` helper explicitly, its `seeds`-map entry, and its own doc comment at lines 71–78 ("the four stencils a live Discussion-Review or Plan-Review segment reads"), which becomes five stencils across three segments.
- r5 `[NIT:decision]` "No stated disposition for an in-flight run parked on `Webster-Review`" — added an Open risks entry recording it as accepted-with-precedent, citing the recipe header's own durable-row-name warning and both shipped perches, which retired a row name the same way with no migration.

## Pushed Back

(none — `--orch` mode has no PUSH BACK path; all three findings were verified accurate against source before fixing.)
