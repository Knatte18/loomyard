# Discussion fix — round 1

Source review: `_mill/reviews/20260826-111526-discussion-review-r1.md` (reviewer `orchestrator`, verdict APPROVE after the `design`-only blocking-class ceiling demoted its one `consistency` finding from BLOCKING to NIT).

## Fixed

- `20260826-111526-discussion-review-r1.md` — "Config Strictness Invariant citation misdescribes actual enforcement": verified the finding against source before acting (`internal/configengine/config.go:101-113` hard-fails only on template-named keys the file lacks; `internal/reedengine/config.go:50` unmarshals with no `KnownFields`, so an extra on-disk key is silently dropped; CONSTRAINTS.md's Config Strictness Invariant covers `Load` vs `LoadOrTemplate` on absence, not unknown keys). Rewrote the rationale in the `width-height-keys-keep-their-name-and-change-their-meaning` Decision onto the real mechanism — a rename makes the new key **missing** from every already-materialized `reed.yaml`, which `yamlengine.MissingKeys` hard-fails on with `run "lyx config reconcile"`, while the old key goes unused without error. Corrected the Constraints entry to state reedengine's actual pinned position (degrading, `LoadOrTemplate`) and to separate the missing-key check from that invariant, and corrected the matching Q&A log line.

## Pushed Back

_None._
