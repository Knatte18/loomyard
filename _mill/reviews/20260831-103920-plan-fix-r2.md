# Plan fix — round 2

## Fixed

- r2 holistic — "[BLOCKING:scope] Card 1 Requirements name files/funcs absent from Context": verified all six references (`apply_test.go`, `lock_test.go`, `render/rules_test.go`, `render/pins_test.go`, `render/height_test.go`, and `clampToFit` in `render/height.go`) appear in Card 1's Requirements prohibitions while none appears in `Context:` or `Edits:`. Took the finding's second remedy rather than its first: adding six do-not-touch files to `Context:` — an allowlist of files the implementer READS — would point the implementer at exactly the fixtures it must leave alone. Reworded step (5)'s prohibition to "any test file other than the one listed under `Edits:`" and deferred the enumeration to the `no-render-or-configsync-change` Shared Decision that already carries it, and reworded the trailing prohibitions to name the render clamp rule and the configsync module without a file path.

## Pushed Back

_None._
