# Discussion Fix — round 4

Source review: `20260609-123653-discussion-review-r4.md`

## Fixed

- **[NOTE] spawn constants ownership after hideProcWindow moves** — Added explicit statement that `createNoWindow`/`createNewProcessGroup` stay in `spawn_windows.go` (spawnSync still needs them) and `internal/git`'s `git_windows.go` declares its own constant independently.
- **[NOTE] Line-number references drifted by one** — Removed exact line numbers from the "Key files to read" list; added a note directing the plan writer to grep for call sites rather than trust cited lines.
- **[NOTE] Template trailing comments differ from current source** — Changed "preserving per-key descriptions" to "providing updated per-key descriptions" to accurately reflect that descriptions are reworded, not preserved verbatim.

## Pushed Back

_(none)_
