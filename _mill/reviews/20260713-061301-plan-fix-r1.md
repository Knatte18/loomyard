# Plan fixer report — round 1

Source review: `_mill/reviews/20260713-060932-plan-review-r1.md` (APPROVE, 4 NITs).

## Fixed

- [NIT] "allowedNonHermetic guard-file entry semantics ambiguous" — card 5 now
  states the guard-file entry is a per-file scan exclusion (tierpurity
  semantics), never a package-level exemption; `cmd/lyx` satisfies the
  requirement via its real `TestMain`.
- [NIT] "Full-hermetic breakage has no verify gate or fallback in-plan" —
  card 12 now carries the contingency: on a hermeticity-traceable red, switch
  the helper to the additive `GIT_CONFIG_COUNT` form (same keys), update
  godoc + CONSTRAINTS wording, re-run timing.
- [NIT] "Card 10 forward-references a doc created in batch 4" — card 10 now
  notes the `fixture-copy.md` target lands in batch 4 and the between-batch
  dangling link is accepted.
- [NIT] "Card 4 Context omits helper source files" — added
  `internal/lyxtest/lyxtest.go` and `internal/lyxtest/hermetic.go` to card 4
  Context.

## Pushed Back

(none)
