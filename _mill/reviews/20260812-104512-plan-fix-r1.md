# Plan fix — round 1

Source review: `_mill/reviews/20260812-103859-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `20260812-103859-plan-review-r1.md` — "[NIT:decision] state/doc.go card omits the one-direction-race statement the Shared Decision names it for".
  Verified accurate: the `## Shared Decisions` entry "this task closes one direction of the race, not both" names `internal/state/doc.go` as one of four locations the guarantee must be stated precisely, and batch 1 card 3's `Requirements:` covered only the generic read-modify-write rule, the non-composability of `ReadJSON` + `WriteJSON`, and single-consumer adoption.
  Took the reviewer's first branch (add the statement) rather than its second (narrow the decision's location list), because the limit is real for **any** `UpdateJSON` consumer, not just the fabricengine one: the primitive serialises its own caller against every other write, but cannot serialise a different two-phase writer that computes its payload outside the lock.
  Card 3's `Requirements:` now instructs that limit in consumer-agnostic terms, explicitly forbidding `internal/fabricengine` internals in the wording, and cross-references the Shared Decision by name.

## Pushed Back

_(none)_
