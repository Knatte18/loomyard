MILL_REVIEW_BEGIN
# Review: llm-driven child can park forever on Claude Code's own workspace-trust dialog — holistic

```yaml
duration_s: 188.1
verdict: APPROVE
reviewer_model: opus
reviewed_file: plan/
date: 2026-09-23
```

## Findings

### [NIT:consistency] Run.attached and RunState.Started docs go stale
**Demoted-from:** BLOCKING
**Location:** batch 1 / card 1.
**Issue:** `AwaitStarted` reads `run.attached`, and through `checkLivenessTick` it also flips and persists `run.state.Started`.
That falsifies the `attached` field doc in `internal/shuttleengine/run.go` ("read only by Wait's started seed").
It also falsifies `RunState.Started`'s doc in `internal/shuttleengine/rundir.go` ("flipped true … the moment Wait's own startup probe observes").
Card 1 says "Make no other change to `internal/shuttleengine/run.go`", and `rundir.go` is Context-only, so the implementer is barred from fixing either comment.
**Fix:** Have card 1 reword the `attached` doc so it names `AwaitStarted` as the second reader, move `rundir.go` to Edits, and reword `Started`'s doc so it names `AwaitStarted`'s probe as a writer.

### [NIT:consistency] awaitStartedTickCap rationale says the probe "spawns nothing"
**Location:** batch 1 / card 1, `awaitStartedTickCap` doc comment requirement.
**Issue:** Each tick calls `reed.Status()` and `reed.CapturePane()`, and both run real tmux processes: `Engine.Status` calls `e.tmux.listPanes` in `internal/reedengine/lifecycle.go`, and `Engine.CapturePane` calls `e.tmux.output("capture-pane", …)` in `internal/reedengine/io.go`.
So the Live-Substrate retry clause applies directly, and the cap is not "belt-and-braces" over a loop that spawns nothing.
**Fix:** Reword the required doc sentence: each tick spawns tmux through reed, so the count cap is the clause's required bound, and the `maxStatusRetries` slack rationale stays as written.

## Verdict

APPROVE
One doc-accuracy gap is barred from being fixed by card 1's own "no other change" rule.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
