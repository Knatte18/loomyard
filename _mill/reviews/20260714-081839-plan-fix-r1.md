# Plan fix — round 1

Source review: `_mill/reviews/20260714-081731-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260714-081731-plan-review-r1.md` — "[BLOCKING] Status half of spawn-count guard is vacuous": card 4 now builds the fixture with **full host+weft pairs via `w.Add(f.Layout, slug, AddOptions{SkipPush: true})`** instead of bare `git worktree add`. Verified against `status.go`: the loop's `os.Stat(weftPath)` weft-exists gate (~lines 110–116) `continue`s before reaching `hostLayoutFor` (~line 129), so an unpaired host worktree never resolves and the `Status` non-growth assertion would pass even with the regression present. Pairing makes every added worktree pass the gate. Added `internal/warpengine/add.go` to card 4's Context for the `Add`/`AddOptions` API. Rationale for the fix is spelled out inline in the card so the implementer cannot regress it.
- `20260714-081731-plan-review-r1.md` — "[NIT] Card 4 guard comment overstates Status scaling": the prescribed guard comment now reads "once per enumerated worktree **with a present weft sibling**, so `--show-toplevel` scaled with paired-worktree count", which is accurate for both `Status` (weft-gated) and `Reconcile` (resolves before its weft check).

## Pushed Back

(none)
