# Plan fix — round 3

Source review: `_mill/reviews/20260809-093542-plan-review-r3.md`

## Fixed

- `[BLOCKING:scope]` "Bare-word exclusion list misses a ninth ordinary-English site" — verified: `internal/lyxtest/lyxtest.go` carries "Template builders: cached, built once per test binary via sync.Once", a live bare-word hit in a file no card edits and no enumerated token covers.
  Added "Template builders" to card 18's enumerated exclusions and the file to the card's `Context:`.
  Re-running the scan for this class surfaced a second, related gap the finding did not name: `internal/gitrepo/parity_test.go`'s header comment wraps the already-enumerated phrase "fixture builders" across two comment lines, so a line-based grep sees only the bare continuation word "builders" and would report an enumerated site as novel.
  That line is now enumerated too, with the line-wrap stated as the reason, and the file added to `Context:`.
  The exclusion list's rationale paragraph now names all four plan-time additions and why each was added rather than discovered mid-run.

## Pushed Back

_None._
