# Discussion fix — round 3

Source review: `_mill/reviews/20260913-110349-discussion-review-r3.md` (verdict APPROVE, reviewer `opus`, effort medium, 0 BLOCKING / 3 NIT, none demoted).

## Fixed

- "Stale grep counts and final-sweep expectation" (`[NIT:consistency]`).
  Verified: `grep -rl -- '-HUB'` now returns 62 files with 11 under `_mill/`, against the 54/3 the doc quoted — the pinned totals go stale every review round by construction, since each round adds a review file and a brief that both discuss the literal.
  Rewrote the File-inventory preamble to state the exclusion as "**every hit under `_mill/`**" with no pinned total, and said explicitly that the count is omitted on purpose because it grows per round.
  The 51 product-source figure and the 7/31/13 group split are unchanged — the reviewer independently confirmed those still verify.
  Applied the same correction to Testing → Sentinel integrity, which had named `_mill/status.md` as the single permitted mill hit; it now discounts the whole `_mill/` tree.

- "Class (b) label contradicts the per-HUB rewrite" (`[NIT:consistency]`).
  Accurate and a genuine self-contradiction: class (b) was defined as "coincidental substring → leave untouched" and the inventory heading said "do NOT change the literal", yet `internal/reedcli/smoke_teardown_test.go:270` was filed there with an instruction to rewrite `per-HUB` → `per-hub`.
  Introduced a fourth class — **(d) Conceptual prose → reword, do not substitute** — and moved that entry into it.
  Class (d) is the case where the text genuinely means the hub (so it is not class (b)) but names the *concept* rather than the suffix literal (so it is not class (a), and `-LYXHUB` would be wrong there too).
  Tightened class (b)'s definition to "does not denote the hub at all" so the two cannot be confused again, and updated the inventory heading, the group-split sentence, and the final-sweep expectation to match.

- "Measured-snapshot doc treated unlike class (c)" (`[NIT:decision]`).
  Accurate: `manifest/designs/reed-fabric-standalone-api.md:320` is framed as a *measured* public-surface census (it sits among `wc -l` counts and import censuses) and was scheduled for substitution, while `docs/research/session-fork-spike.md` was left alone as a historical record — with no stated reason for the difference.
  Added a **Distinguishing a live census from a dated log** bullet to Decision *Test-literal classification* stating the rule: a document asserting something about the *current code* is updated with the code (leaving `HubSuffix ("-HUB")` in the census would make it false after the swap), while a document asserting *what happened on a given date* is never retro-edited.

Also corrected, while editing the same sentence: the group split described the 2 non-class-(a) test files as "class-(b)/(d)". They are `startup_test.go` (class (c)) and `smoke_teardown_test.go` (class (d)); both class-(b) sentinels sit inside files that already carry class-(a) work. Now stated correctly and named explicitly.

## Pushed Back

None.
