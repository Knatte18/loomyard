# Plan fix — round 2

Source review: `_mill/reviews/20260818-094356-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING, 1 NIT).

## Fixed

- `20260818-094356-plan-review-r2.md` — "[BLOCKING:scope] Card 28's AppendIntegrationFailure call site not in Context".
  Verified: `AppendIntegrationFailure` is declared at `internal/websterengine/summary.go:104`, and card 28's Requirements direct a new call to it.
  Added `internal/websterengine/summary.go` to card 28's `Context:`.

- `20260818-094356-plan-review-r2.md` — "[BLOCKING:scope] Card 33's FabricBisector type not in Context".
  Verified: `FabricBisector` is declared at `internal/websterengine/integration.go:31`, and card 33's Requirements name it as the closure's return type.
  Added `internal/websterengine/integration.go` to card 33's `Context:`.

- `20260818-094356-plan-review-r2.md` — "[NIT:consistency] Card 27's \"Both FindRun calls\" sentence is ambiguous".
  Verified: `internal/websterengine/runlevel.go` contains exactly one real `shuttleengine.FindRun` call, at line 530 — the file's other two matches are prose inside a comment block.
  The second call site is in `internal/websterengine/recoverbatch.go` and is card 26's work.
  Reworded card 27 to name the single call in its own file and to point at card 26 for the other, and added `internal/websterengine/recoverbatch.go` to card 27's `Context:` so the cross-reference it now names is readable.

## Pushed Back

_(none — all three findings were factually accurate and none of the fixes carries harm.)_
