# Plan fix report — round 1

Source review: `_mill/reviews/20260626-194048-plan-review-r1.md` (verdict APPROVE, 3 NITs)

## Fixed

- `20260626-194048-plan-review-r1.md` — "[NIT] Banned-token scan also reads comments in main.go": Card 1 now warns that the enforcement scan is a whole-file substring match (comments included), so the literal `os.Getwd` / `--show-toplevel` strings must not appear in `main.go` even in comments.
- `20260626-194048-plan-review-r1.md` — "[NIT] `lyx`-not-on-PATH exec failure handling left implicit": Card 1 now requires distinguishing an `*exec.ExitError` (subprocess streamed its own stderr) from a non-`ExitError` startup failure, writing a clear "lyx not found on PATH" cause to stderr before exiting non-zero.
- `20260626-194048-plan-review-r1.md` — "[NIT] No test asserts clone-error propagation": Card 1's test list gains case (e) — the clone runner returning an error must be surfaced by the decision function.

## Pushed Back

_None._
