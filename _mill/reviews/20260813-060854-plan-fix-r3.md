# Plan fix — round 3

Source review: `20260813-060747-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 2 NITs).

## Fixed

- `20260813-060747-plan-review-r3.md` — "[BLOCKING:design] Card 36 deletes a load-bearing fabric.yaml override, not a template duplicate": verified accurate and fixed.
  Confirmed against the source: `internal/loomengine/preflight_integration_test.go`'s `seedRepoWideFabricConfig` writes `pathspec: _extra`, `internal/fabricengine/template.yaml` ships `pathspec: ""`, and `setupPreflightFixture` wires `_extra` through `fabricengine.WireJunctions(…, []string{"_lyx", lyxdirs.DotLyxDirName, "_extra"})`, so `RepoWiredNames` must agree with it.
  This was my error, not a reviewer misreading: I generalised from batch 4's identically-named helper without checking the content, and the two differ exactly where it matters.
  Card 36 now retargets the helper onto `hubforge.SeedFabricConfig(t, h, "branch_prefix: \"\"\npathspec: _extra\n")` and deletes it, instead of dropping the call, and states why — placement is scaffolding, content is a genuine override, triage outcome 3.
  The card also names the concrete failure the drop would have caused: `TestPreflight_MissingOptionalJunctionIsAJunctionFault` and the two `_extra` junction-corruption sub-tests would stop classifying `_extra` as a real optional junction, which reads as a passing suite with two fewer meaningful assertions.
  Batch 6's `## Batch Scope` and `## Batch Tests` are corrected to match, and `## Batch Tests` now names the `_extra` junction-health tests as the ones to watch on the verify run.
- `20260813-060747-plan-review-r3.md` — "[NIT:scope] Card 27's seedRepoWideFabricConfig call-site count is off by one": corrected "five call sites" to "four" and "replace all five calls" to "all four" in `04-small-consumers.md` card 27.
  Confirmed by `grep -o 'seedRepoWideFabricConfig(t,'` — four calls (lines 69, 128, 195, 278);
  my five included the helper's own declaration line.
- `20260813-060747-plan-review-r3.md` — "[NIT:scope] checkResolved call count in card 35 / Batch Tests conflates text hits with call sites": corrected in both places in `06-stuck-packages.md`.
  Confirmed: 12 call expressions, 26 bare-word mentions, the other fourteen being `t.Fatalf("checkResolved: %v", err)` literals.
  Card 35 now states both figures and why they differ, so the next reader does not re-derive the wrong one;
  `## Batch Tests` now says "twelve call sites".

## Pushed Back

_None._
