# Plan fix report — round 3

Source review: `_mill/reviews/20260801-081627-plan-review-r3.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 3 NIT). All four findings VERIFIED accurate and FIXED. All concern the ripple of Batch 4's CLI `LoadConfig(cwd)`→`BoardDir` migration and `CloneHub` signature change into pre-existing tests.

## Fixed

- [BLOCKING] "Batch 4's BoardDir config migration breaks TestRunCLI_EnvMapToOption" — CONFIRMED: `TestRunCLI_EnvMapToOption` (cli_test.go:179-212) drives `push` against a `lyxtest.CopyPaired` fixture seeding fabric config at `fixture.WeftPrime` (not `BoardDir`), and `CopyPaired` never materializes a `_board` dir — so card 17's `weft_verbs.go:124` → `LoadConfig(BoardDir(l.Hub))` migration would fail this pre-existing test at Batch 4's own verify. Fixed card 19 to update BOTH pre-existing fixtures (`setupCLIRepo` and `TestRunCLI_EnvMapToOption`) to seed fabric config at `hubgeometry.BoardDir(fixture.Hub)`.
- [NIT] "Card 14 omits the third no-cleanup CloneHub return path" — CONFIRMED: `os.MkdirAll(hubPath,…)` failure at clone.go:74-76 (`return "", err`) also needs `"", "", err` under the 3-value signature. Fixed card 14 to enumerate all three early no-cleanup return paths (60-63, 69-71, 74-76).
- [NIT] "Card 9's stale-removal Detail string can silently overwrite the add-missing outcome's Detail" — CONFIRMED: stale-removal writes `pr.Detail` after `junctionRepointedDetail` may have set it. Fixed card 9 to require append (join with `"; "`), not overwrite, so a re-pointed-and-stale-removed pair reports both.
- [NIT] "Card 18 doesn't call out updating clone_adopt_test.go's four pre-existing CloneHub call sites" — CONFIRMED: the signature change forces threading `subpath`/`anchor` through the four existing calls (adopt/fresh/strict-abort/orphan) to keep the file compiling. Fixed card 18 to note this compile-forced update explicitly.

## Pushed Back

(none — all four findings were factually accurate and fixing them caused no harm.)
