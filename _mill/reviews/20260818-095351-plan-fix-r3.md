# Plan fix — round 3

Source review: `_mill/reviews/20260818-095321-plan-review-r3.md` (holistic, APPROVE, 0 BLOCKING, 2 NITs).

## Fixed

- `20260818-095321-plan-review-r3.md` — "[NIT:consistency] Batch 7's Batch Tests paragraph miscounts the tagged fixture files".
  Verified: cards 31 and 32 touch six `internal/websterengine` fixture files, of which `beginbatch_test.go`, `recordbatch_test.go`, `recoverbatch_test.go` and `runlevel_test.go` carry `//go:build integration` while `audit_test.go` and `template_test.go` do not;
  `internal/webstercli/verbs_test.go` is card 33's file in a different package and is separately tagged.
  Rewrote batch 7's Batch Tests paragraph to state four of six, name the untagged pair, and split out the `webstercli` file as its own sentence.

- `20260818-095321-plan-review-r3.md` — "[NIT:consistency] Card 13 leaves one accessor's doc comment citing a non-existent invariant".
  Verified: `internal/websterengine/state.go`'s `ReportsDir` comment reads "Per the Hub Geometry Invariant", and `CONSTRAINTS.md` defines no invariant by that name — its nearest neighbour is the Hub Containment Invariant, which is about something else — while the other three accessors correctly cite the Cwd Resolution Invariant.
  Added a one-line requirement to card 13 correcting the reference while that comment is already being edited.

## Pushed Back

_(none — both findings were factually accurate and neither fix carries harm.)_
