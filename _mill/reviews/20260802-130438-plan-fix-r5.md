# Plan fixer report — round 5

Source review: `_mill/reviews/20260802-130258-plan-review-r5.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 3 NIT). All four findings verified accurate against source → all FIXED, none pushed back.

## Fixed

- **[BLOCKING] Card 22's target fabric verb-count ("15") is arithmetically wrong** (`20260802-130258-plan-review-r5.md`) — Confirmed the tree already has 15 registered verbs (10 topology in `fabric.go` + 5 content-sync in `weft_verbs.go`, matching `helptree_test.go`'s 15-entry `wantSubs`); the `fabric.go:10` "14-verb" claim is pre-existing stale by one, so adding `diff` makes 16, not 15. Corrected card 22's target to **16** with an explicit note about the pre-existing 14→15 drift so the implementer doesn't ship a still-wrong count.
- **[NIT] F0 scenario also enumerates/counts fabric verbs, uncovered by any card** (`20260802-130258-plan-review-r5.md`) — Confirmed `SANDBOX-FABRIC-SUITE.md:85` (F0) says "list all 15 verbs (`clone`, …, `unwire`)". Extended card 30's requirements (and its title) to also update F0's count to 16 and add `diff` to its explicit verb enumeration.
- **[NIT] revert.go's own file header and resolveRevertTarget's doc comment still name the deleted RevertWithWeft** (`20260802-130258-plan-review-r5.md`) — Confirmed `revert.go:1` (file header) and `revert.go:63` (`resolveRevertTarget`'s doc comment) both name `RevertWithWeft`. Card 18 already edits `revert.go`; added an explicit requirement to reword both to describe the surviving resolver/classifier and its use by `Fabric.Diff`'s `weftAnchorForWarpSHA`, and to grep `revert.go` (including comments) for any residual reference.
- **[NIT] Card 17's PairInSync grep scope excludes loomengine, leaving stale test comments** (`20260802-130258-plan-review-r5.md`) — Confirmed `internal/loomengine/preflight_integration_test.go` carries six `PairInSync` doc-comment mentions. Added that file to card 17's Edits (already in All Files Touched, and already edited by card 16 in the same batch) and widened card 17's grep scope to include the `loomengine` package.

## Pushed Back

(none)
