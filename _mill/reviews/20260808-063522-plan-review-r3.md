MILL_REVIEW_BEGIN
# Review: Scoutengine: rewrite CONSTRAINTS.md as a seam rule, convert leaf test to banned-list, add LSP guard — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-08
```

## Findings

### [NIT] Card 3's temporary lspclient.go rename is underspecified
**Location:** batch 1 / card 3 **Issue:** "Temporarily renaming `internal/scoutengine/lspclient.go`" doesn't say the new name must keep a `.go` suffix; renaming to a non-`.go` name (e.g. `.bak`) drops the file from the build entirely, breaking compilation for every symbol other files reference (`lspClient`, `lspLocation`, etc.) instead of producing the intended isolated `t.Fatal` in the guard test. **Fix:** Add a one-clause note that the temporary name must still end in `.go` so the package keeps compiling and only the guard's hardcoded `lspclient.go` target path goes missing.

## Verdict

APPROVE
Plan is internally consistent, decision-aligned, and every source-grounded claim checked out against the actual files.
MILL_REVIEW_END
