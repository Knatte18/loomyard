# Discussion fix — round 2

Source review: `_mill/reviews/20260809-122312-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r2 NIT `[scope]` "raddle.md residue list is a bound, not an inventory" — verified: `raddle.md:7` reads "living in `weft`: an always-run step after Webster", which is retired framing and already contradicts that file's own `## When it runs: deferred to merge-time, not mid-task` section. Replaced the closed three-line enumeration with an end-to-end re-read rule applied to all three owned files, listed `:7` as a fourth confirmed site, and recorded why the enumeration *method* was the defect: `:7` carries the retired framing without using the word "slot", so no slot-focused line list could have caught it.
- r2 NIT `[consistency]` "Honesty paragraph may read as un-guarding the CONSTRAINTS.md anchor" — verified: `constraints-entry` point 4 listed "everything outside `manifest/` and `docs/`" as not reached, one sentence from the citation decision that depends on `../../CONSTRAINTS.md#fabric-git-invariant-warp--weft` being resolved. Rewrote point 4 to state that the root restriction is **source-side only** (which files are scanned), and that every target — in-root or not — has its file and `#anchor` resolved. Named the two concrete target sets the ambiguous reading would have un-guarded: the round-1 invariant citation and the ten `../../internal/*/doc.go` targets.
- r2 NIT `[design]` "Fixture strategy for the mandated scenario list is undecided" — verified against `internal/lyxcwd/enforcement_test.go:128`, which skips any directory whose name *contains* `"testdata"`, so a `testdata/` fixture tree walks to zero files and every subtest built on it passes vacuously. Decided the seam in `## Testing`: pure data-in/data-out helpers (slug, heading-set with duplicate `-1` suffixes, fence-aware link extractor) table-tested with no tree at all; `t.TempDir()` as the `repoRoot` argument to `walkEnforcementRoots` for the cases that need a real tree; and the live `manifest/` + `docs/` walk kept as its own subtest via `repoRootForEnforcement(t)`. Added an explicit "do not put the fixture tree under `testdata/`" warning with the vacuous-pass rationale.

## Pushed Back

None. All three findings verified accurate against the cited code and docs, and no fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task's scope.
