# Plan fix report — round 1

Source review: `_mill/reviews/20260906-182215-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING, 0 NIT).

## Fixed

- r1 holistic, "Cards 5/6 assert unobservable `internal/logger` private state" — verified: `internal/logger` exports only `LogsDir`, `Arm`, `SetDurableSinkDir`, `NotifyExit`, `Sweep`, `StartSpan`, `TraceID`, `MintOrAdoptAndExport`, `Debug`, `Info`, `Warn`, `SetVerbosity`, `SetOutput`;
  `sinkDirOverride` is unexported and has no accessor, and there is no `export_test.go` in the package.
  Fixed by rewriting both cards' assertions to the force-a-write-and-check-the-filesystem technique rather than by adding a new accessor, since `ensureDurableSink`'s `testing.Testing()` suppression is reached only on the empty-override branch — so a non-empty override makes one `logger.Info` call arm a real sink with no `LYX_TRACE` redirect, and the `wireHub` case is observed against a pre-set sentinel directory the same way.
  Both cards gained `internal/logger/logger.go` in `Context:` for the `logger.Info` call.
- r1 holistic, "Card 8 requirement rests on a false claim about the test's own code" — verified: `TestRunCLIIn_StandalonePreRun_TargetDirectoryUnchanged` redirects `XDG_STATE_HOME`/`LOCALAPPDATA` but never calls `standalonestate.Derive`;
  only the sibling `TestRunCLIIn_StandalonePreRun_ReachesRunsOwnValidationGate` does.
  Fixed by instructing the extension to add its own `Derive` call, mirroring the sibling, before anything asserts against `stateDir`.
- r1 holistic, "Card 8's stated residual mechanism (`NotifyExit`) is unreachable from the test it governs" — verified by repo-wide grep: `logger.NotifyExit` is called only at `cmd/lyx/main.go:45` and `:57`, and `RunCLIIn` delegates to `clihelp.Execute`/`ExecuteIn`, neither of which reaches it.
  Fixed by re-attributing the residual to the shipped `lyx` binary explicitly and stating in the card that the test's own path never executes `NotifyExit`, so the doc comment records why the assertion is scoped rather than claiming a mechanism this test exercises.

## Pushed Back

_(none — all three findings were factually accurate and none of the fixes broke existing functionality, conflicted with a documented decision, or destabilised work outside this task.)_

## Also fixed (self-found while verifying)

- Card 7 named `logger.Error` in the source guard's call set;
  `internal/logger` exports no `Error` function.
  Narrowed the guard to `logger.Info` and `logger.Warn`, and recorded that `Debug` sits below the Info-or-above sink-arming threshold and must not fail the guard.
