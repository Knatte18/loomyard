# Plan fixer report — round 1

Source review: `_mill/reviews/20260628-193324-plan-review-r1.md` (verdict APPROVE, 0 blocking, 2 NITs).

## Fixed

- (none)

## Pushed Back

- [r1] "Card 1 Context omits the moved engine files" — Pushed back. The four engine files
  are Card 1's own `Deletes:` (old path) → `Creates:` (new path) targets under the git-mv
  model documented in `00-overview.md` › Shared Decisions. The `Context:` allowlist governs
  files read but **not** changed by the card; a card's own Creates/Deletes targets are
  inherently read during `git mv` and cannot also be listed as read-only Context without
  contradiction (and the field format forbids an explanatory note). The reviewer flagged it
  as "optional ... Not blocking." Legitimate pushback per mill-receiving-review rule 5
  (unsatisfiable / conflicts with documented design).
- [r1] "Integration-tagged file is compiled but never run" — Pushed back. Adding
  `-tags integration` to the `go test` portion of `verify` would execute the full e2e
  integration suite (real git/weft fixtures, slow) on every implementer and fixer round,
  for a change that is comment-only in `configcli_integration_test.go` with no behaviour to
  exercise. The compile surface is already confirmed by `go vet -tags integration ./...` in
  the existing `verify`. Running the suite adds environment-dependent flakiness risk with
  zero behavioural-coverage gain. The reviewer concurs ("No action needed"). Legitimate
  pushback per mill-receiving-review rule 4 (destabilizes other work).
