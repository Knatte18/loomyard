# Discussion fix — round 3

Source review: `_mill/reviews/20260906-100129-discussion-review-r3.md` (reviewer `opus`, effort medium, verdict REQUEST_CHANGES — 1 BLOCKING, 3 NIT after the blocking-class ceiling demoted three).

Every finding was verified against the tree before acting. All four were accurate; none were pushed back.

## Fixed

- r3 — "begin-batch/record-batch are not producer rows" (`[NIT:consistency]`, demoted from BLOCKING) — verified: `internal/shedrecipe/registry.go` holds fourteen entries and none is `BeginBatch`/`RecordBatch`, and `internal/webstercli/beginbatch.go`'s `Short` reads "Master's bracket call immediately before forking one batch's implementer".
  Rewrote `drift-boundaries` with a **Seam correction** paragraph naming `Plan-Revalidate` as the only `ShedProducer` row and the two webster boundaries as bracket CLI verbs over `websterengine` functions, rooted at `websterengine.Geometry.WorktreeRoot`.
  Added an explicit paragraph on what the Gate Self-Check Parity Invariant actually binds here (the `Plan-Revalidate` ↔ `validate-plan --require-approved` pair only — the bracket verbs have verbs and no rows, so there is no pair for parity to hold).
  Corrected `planglyph-root-resolution`'s first bullet to match, splitting the one bullet into the producer-row case and the bracket-verb case.

- r3 — "amendments.md is a third planparser write path" (`[NIT:consistency]`, demoted from BLOCKING) — accurate and self-evident from the document: the Constraints section said "exactly one new write path, `RewriteRefs`; no third" while `amendment-log` had `planparser` appending to a file neither `SetApproved` nor `RewriteRefs` touches.
  Constraints now says **two** new write paths and names both; `amendment-log` names the append as the second path explicitly; `rewrite-write-path` states why the append is not folded into `RewriteRefs` (it appends to a file `RewriteRefs` never touches and substitutes nothing).
  Added the CONSTRAINTS.md Planparser-bullet edit to the same-commit doc list, which now names three separate CONSTRAINTS.md edits rather than one.

- r3 — "New `lyx quarry` module: package name and sandbox coverage unstated" (`[NIT:scope]`, demoted from BLOCKING) — verified `cmd/lyx/sandbox_coverage_test.go` exists and requires a `**Covers:**` scenario or an allowlist entry, and that the CLI/Cobra Invariant's package-naming line records exactly one deviation today (`stencilcli` → `internal/stencilstore`).
  Added a **Module identity** bullet to `cli-verb-surface` naming `internal/quarrycli` and recording the `quarrycli` → `internal/planglyph` naming deviation, plus a **Sandbox disposition** bullet choosing a real scenario over an allowlist entry (the four verbs are read-only queries and trivially exercisable, so exclusion would be unjustified).
  Added matching Constraints bullets and a Testing bullet.

- r3 — "quarry's git spawn is invisible to two mechanical guards" (`[BLOCKING:design]`) — verified: `bannedTokens` is `{gitexec.Run, exec.Command, gitkit.Copy, hubforge.NewHub}` with no quarry token, and `cmd/lyx/spawnobservability_test.go` AST-matches `exec.Command` call expressions in loomyard production files only, so a `DeltaGit` call — whose `exec.Command` lives in quarry's `internal/gitsrc` — trips neither.
  Added a new decision `quarry-spawn-guard-visibility` extending both guards: a narrow `DeltaGit` token in `bannedTokens` (deliberately not `quarry.Open`, since `Resolve`/`TOC`/`Glyphs`/`Expand` read files and `Name` does no I/O — only `DeltaGit` spawns), and a `DeltaGit`-as-spawn case in the observability guard so the single call site must import `internal/logger` and log the spawn as `Info`.
  Rejected the allowlist route in-text: the invariant permits an exemption only for a site structurally barred from importing `internal/logger`, which `planglyph` is not.
  Added matching Constraints and Testing bullets.

## Pushed Back

None.

## Gaps auto-picked

One BLOCKING finding, resolved by auto-picking the recommended option per the operator's standing instruction. Logged in `discussion.md`'s `## Q&A log` with the `[auto-pick]` marker and its rationale.
