# Plan fix — round 1

## Fixed

- `20260826-181305-plan-review-r1.md` — "[BLOCKING:scope] Card 17 omits `internal/state` from Context despite naming its functions": verified both `state.UpdateJSON` (line 110) and `state.ReadJSONStrict` (line 138) live in `internal/state/state.go`, and that no file already in card 17's `Context:` shows `ReadJSONStrict`'s `(T, bool, error)` return shape. Added `internal/state/state.go` to card 17's `Context:`.
- `20260826-181305-plan-review-r1.md` — "[NIT:consistency] `constructoranchoring_test.go`'s header comment goes stale after Card 1's move" (demoted from BLOCKING): verified the header's "As of this batch there are two groups, not three" paragraph enumerates the `.lyx` group at lines 22-24 without `LoomStatusFile`. Card 1 now requires adding `loomengine.LoomStatusFile` to that enumeration in the same edit that moves its assertion into the group.
- `20260826-181305-plan-review-r1.md` — "[NIT:consistency] Card 13's resume-across-machines paragraph rewrite may leave a now-false middle sentence" (demoted from BLOCKING): verified `manifest/designs/loom.md` lines 274-275 carry the "commits the seed once ... the only other commit of that file is the checkpoint the landing rows make (see below)" claim and its "every persist in between" continuation, neither of which card 13 previously named. Card 13 now names both sentences for deletion explicitly, and records that the "(see below)" cross-reference points at a section the same card deletes.

## Pushed Back

_None. All three findings were factually accurate, and no fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task's scope._
