# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260826-064828-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260826-064828-plan-review-r1.md` — "[BLOCKING:scope] Card 1's Context omits a file its Requirements name".
  Verified accurate: card 1's Requirements direct the new `BurlerGeometry` doc comment to name `standalonegeom.BurlerGeometry` as the mode where the two fields still diverge, which is a factual claim about another package, and that file appeared only in card 2's `Context:`.
  Added `internal/standalonegeom/burlergeom.go` to card 1's `Context:` list.
- `20260826-064828-plan-review-r1.md` — "[NIT:consistency] Card 14 leaves `## Planned` with a stale lead-in after emptying it".
  Verified accurate: the item card 14 moves is the only item in the only `## Planned` subsection, so the section ends up empty while its own lead-in still says "grouped into sub-categories below for readability".
  Extended card 14's Requirements to rewrite that lead-in so it references no sub-categories and reads correctly over an empty section, and to keep the `## Planned` heading itself — the file's own Maintenance section names the three sections and states that numbering restarts in each, so deleting one would falsify those rules and leave the next Planned item with nowhere to land.

## Pushed Back

_None._
Both findings verified accurate, and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task.
