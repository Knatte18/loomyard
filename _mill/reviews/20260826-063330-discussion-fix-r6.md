# Discussion fix report — round 6

Source review: `_mill/reviews/20260826-063252-discussion-review-r6.md` (APPROVE, 0 blocking, 2 nits).

## Fixed

- `[NIT:decision]` "loom.md 'The gate' is In with no disposition" — the Docs bullet in Scope → In is now a per-site disposition list. `manifest/designs/loom.md` ("The gate") is marked **add, not correct**, with the reason stated: nothing there is falsified (the section delegates to the package docs and names neither a root nor the four-mode branch), but segment re-entry is a design-level property of the gate and belongs in its design home. The other three doc sites (`shedadapters/doc.go`, `shedadapters/burler.go` + `bouncer.go`, `manifest/roadmap.md`) gained explicit dispositions in the same list.
- `[NIT:consistency]` "roadmap item text carries the corrected route claim" — the roadmap disposition now states that the Done entry must not carry `manifest/roadmap.md:16`'s "confirmed present in the shipped `Discussion-Validate` → `Discussion-Write` → `Discussion-Bouncer` path" claim forward, since the Problem section refutes it (`Discussion-Bouncer`'s `on_done` is `Plan-Write`; nothing routes back). The Done entry states what shipped instead of a reachability claim.

Both fixes also recorded as Q&A log entries.

## Pushed Back

(none)
