# Discussion fix — round 6

Source review: `_mill/reviews/20260913-103558-discussion-review-r6.md` (verdict APPROVE, 0 BLOCKING, 5 NIT, none demoted).

## Fixed

- `20260913-103558-discussion-review-r6.md` — "[NIT:consistency] parse.go header does not say key=value": the `parse.go` Scope bullet is narrowed from three doc comments to two (`Parse`'s at `:37`, `parseBracket`'s at `:122`) and states outright that the file header `:1-4` needs no edit, since it names the four shapes without spelling the bracket's interior.
- `20260913-103558-discussion-review-r6.md` — "[NIT:consistency] knownParams doc comment becomes false, not just incomplete": the `modelspec.go` Scope bullet now says `:109-113` is **corrected**, re-scoping "a bracket or a registry Defaults map" to `Entry.Defaults`/canonical `Params`, in addition to gaining the `bracketKeys` pointer; the "gates KEYS ONLY" sentence is called out as staying.
- `20260913-103558-discussion-review-r6.md` — "[NIT:decision] Package-doc grammar line disposition is self-contradictory": the `modelspec.go` Scope bullet is split into three per-line dispositions — `:11-12` rewritten to the pinned `item` production, `:46` explicitly unchanged (canonical consumer example), `:109-113` corrected.
- `20260913-103558-discussion-review-r6.md` — "[NIT:decision] Three spellings deferred to mill-plan": the `contracts/specs/llm-model-spec.md:8` production is now pinned verbatim (`<alias>[item,item,...]` where `item` is `key=value` | `<effort>`), with the rejected alternation form and the reason (it is restated in `modelspec.go:11-12`, so an open spelling invites drift); `:24` references the same `item` definition. The two remaining delegations (duplicate-message punctuation, second-substring mechanism) are left to mill-plan as the review itself allows — both have stated observable contracts.
- `20260913-103558-discussion-review-r6.md` — "[NIT:scope] CONSTRAINTS.md disposition not stated for the new sync obligation": the vocabulary Decision now states outright that the `bracketKeys` ⊆ `knownParams` obligation is package-internal and test-enforced, does not belong in `CONSTRAINTS.md` (which records cross-package invariants), and leaves the existing Modelspec Leaf Invariant unchanged since the task adds no import.

## Pushed Back

None.
