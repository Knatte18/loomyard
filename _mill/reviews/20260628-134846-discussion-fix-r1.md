# Discussion fix report — round 1

Source review: `_mill/reviews/20260628-134759-discussion-review-r1.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- 20260628-134759-discussion-review-r1.md — "[NOTE] runGH return contract left unspecified": added the `runGH` signature `(stdout, stderr string, exitCode int, err error)` mirroring `gitexec.RunGit`, with LookPath-not-found surfaced as `err` + `exitCode == -1` so the handler branches into gh-not-found / gh-non-zero-exit / success.
- 20260628-134759-discussion-review-r1.md — "[NOTE] Label \"replace default\" depends on pflag semantics": specified pflag `StringArray` (not `StringSlice`) with default `["bug"]` and documented the first-Set-replaces / subsequent-Set-appends behaviour; warned against hand-rolled seed-then-append.
- 20260628-134759-discussion-review-r1.md — "[NOTE] Existing help tests won't enforce registration": made the plan add `"ghissues"` to the `requiredModules` lists in both `cmd/lyx/helptree_test.go` and `cmd/lyx/jsonhelp_test.go`, noting the superset-check rationale.

## Pushed Back

(none)
