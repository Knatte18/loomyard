# Fixer report: plan-fix round 1 — self-report-tier2

Source review: `20260912-111239-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 0 NIT).

## Fixed

- `20260912-111239-plan-review-r1.md` — "[BLOCKING:design] Batch 6 cards 20/21 split breaks per-card compilation".
  Verified accurate against source: card 20's own text said "card 21 adds the parameters" while editing the four call sites, so the tree would not build on card 20's commit.
  Fixed by moving the boundary rather than merging the cards (merging would have forced renumbering cards 21-28): card 20 now adds **only** the three `FrictionDir` struct fields, which compile unread, and card 21 now owns the `render.go` signature widening, the directive injection, **all four call sites**, and the test updates in one commit.
  Card 20's requirements state explicitly why the signature change and its call sites can never be split, citing batch 5's card 17 as the same pattern;
  card 21 restates it at the top of its own requirements and notes that the existing tests call the four exported composers directly, so they must be updated in the same commit too.

- `20260912-111239-plan-review-r1.md` — "[BLOCKING:scope] Card 22 never targets the file the `loomCLI` struct is declared in".
  Verified accurate against `internal/loomcli/cli.go:31-61`: `loomCLI` is declared there alone, and card 22's `Edits:` named only `internal/loomcli/wiring.go`.
  Fixed by adding `internal/loomcli/cli.go` to card 22's `Edits:` and replacing the one-line "store it on the receiver" requirement with an explicit requirement naming the new `frictionDir string` field, its placement beside the existing `registry`/`runner` fields, its doc comment, the `c.frictionDir = frictionDir` assignment in `wire`, and the deliberate zero value in `wireLightweight`.

`00-overview.md`'s All Files Touched was regenerated from the cards' own `Edits:`/`Creates:` union after both fixes, picking up `internal/loomcli/cli.go`.

## Pushed Back

_None._
Both findings were factually accurate, and neither fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task.
