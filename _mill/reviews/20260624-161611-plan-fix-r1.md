# Plan fix report — round 1

```yaml
slug: weft-orphan-branches
round: 1
source_review: _mill/reviews/20260624-161241-plan-review-r1.md
```

## Fixed

- `20260624-161241-plan-review-r1.md` — [BLOCKING] "Verify never runs the integration-tagged
  tests": confirmed `add_test.go` and `weft_test.go` are `//go:build integration` and plain
  `go test` compiles none of them. Changed `verify` in `00-overview.md` and `01-…md` to
  `go test -tags integration -run 'TestWeft|TestAdd|TestSeeder' ./internal/worktree/`. Added a
  `-run` scope (the full `-tags integration` run has a pre-existing unrelated failure
  `TestRunCLI`; the weft/add/seeder tests are green at baseline) and a Card 3 test-naming
  requirement so every new test matches the regex. Documented the rationale in `## Batch Tests`.
- `20260624-161241-plan-review-r1.md` — [NIT] "Card 3 subtask test must seed weft branch Y":
  added Card 3 sub-case 5 (missing-parent-weft-branch ⇒ clear error + full rollback, per
  discussion Testing).
- `20260624-161241-plan-review-r1.md` — [NIT] "Card 4 docs anchor": specified the new
  `### Branch model` subsection is inserted after `### Junction model` in `docs/overview.md`.

## Pushed Back

- `20260624-161241-plan-review-r1.md` — [BLOCKING] "verify command violates canonical
  PYTHONPATH= shape": **factually wrong.** `_plan_validate.py:837-845` gates the
  `verify-not-isolated` check on `is_python_project` and only requires the `PYTHONPATH=` prefix
  for Python projects; loomyard is a Go project, so the check does not apply — which is why the
  prepare-stage validator returned zero errors for this `go test` command. The mill-plan skill's
  "Verify command shape" section also explicitly states non-Python projects (Go, C#) use the
  native runner *without* the prefix. Adding `PYTHONPATH=` would contradict that documented
  convention and seed a wrong pattern for future Go tasks (HARM CHECK: conflicts with documented
  design).
