MILL_REVIEW_BEGIN
# Review: .lyx hygiene -- relocate transients out of _lyx, fix .lyx junction geometry (slice 9) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (Anthropic), tool-use mode
reviewed_file: plan/
date: 2026-08-07
```

## Findings

### [NIT] scoutcli card 29 misdescribes assert-no-callers' existing worktreeRoot resolution
**Location:** batch 4 / card 29 **Issue:** the requirement says "each verb also needs `anchorRoot := resolveAnchorRoot(cwd, dir)` alongside its existing `worktreeRoot := resolveWorktreeRoot(cwd, dir)` line," but `assertNoCallersCommand` in `internal/scoutcli/cli.go` has no such line — it resolves `worktreeRoot` inline (`var worktreeRoot string; if layout, resolveErr := lyxcwd.Resolve(cwd); resolveErr == nil { ...; worktreeRoot = layout.WorktreePath() }`), never calling the shared `resolveWorktreeRoot` helper the other three verbs use, and it does not default to `filepath.Abs(dir)` outside a hub the way the helper does. **Fix:** rephrase the card to describe `assertNoCallersCommand`'s actual inline pattern and explicitly instruct adding `anchorRoot := resolveAnchorRoot(cwd, dir)` there too, threading it into `baseOpts.AnchorRoot`, so this verb doesn't silently keep resolving a different (pre-slice-9) daemon identity than refs/definition/symbol for a subpath-anchored repo.

## Verdict

REQUEST_CHANGES
One NIT: card 29's scoutcli description misstates assert-no-callers' existing code shape.
MILL_REVIEW_END
