# Plan fix — round 7

Source review: `_mill/reviews/20260811-164749-plan-review-r7.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 3 NIT).

This is the last round the configured `roles.plan-review.holistic.rounds: 7` cap allows.
Every finding below is fixed, but the round returned BLOCKINGs, so the max-rounds escape applies and the plan is left unapproved for operator review rather than self-approved.

## Fixed

- `20260811-164749-plan-review-r7.md` — "[BLOCKING:scope] `mutateGitExclude` widening misses a 4th caller file".
  Verified: `internal/fabricengine/gitexclude_integration_test.go` calls it at four sites, two binding the current second return as `changed, err :=`.
  Added to card 17's `Edits:` and to `## All Files Touched`, with the three-caller list relabelled production-only and the `_, changed, err :=` repoint spelled out.
- `20260811-164749-plan-review-r7.md` — "[BLOCKING:scope] Deleting fabrictest's `Check` breaks `matrix_test.go`, not listed".
  Verified: `matrix_test.go:108` carries `[]Check{CheckContainment, CheckOwnership, CheckDirtiness}`.
  Moved from card 28's `Context:` into its `Edits:` so that card's own commit compiles, with a grep instruction and the compiler named as the authority.
- `20260811-164749-plan-review-r7.md` — "[BLOCKING:design] Admin-entry coverage derivation is wrong for weft targets".
  Verified: `primeWeftAdminPermittedRoot` applies `weftname.SiblingPath("", slug)` to a **warp** slug, so a weft target already named `<slug>-weft` yields a `<slug>-weft-weft` key that does not exist.
  Card 29 now requires testing the target's basename for `weftname.Suffix` and stripping it before the weft helper, warp targets going to the warp helper unchanged;
  card 32's admin-entry case gains a weft-side row, since that is the row the naive derivation gets wrong.
- `20260811-164749-plan-review-r7.md` — "[NIT:consistency] `runReconcile` has four `output.Err` sites, not three".
  Verified.
  Reworded to "the three post-`l` sites", with the `resolveWarpLocation()` failure explicitly left bare under the pre-mutation carve-out.
- `20260811-164749-plan-review-r7.md` — "[NIT:consistency] `createWeftWorktree`'s branch-created condition has no source".
  Verified: it always runs `worktree add -b <branch>`, and the adopt-vs-create decision lives in the caller.
  The entry is now unconditional on `err == nil && exitCode == 0`, with the reason stated.
- `20260811-164749-plan-review-r7.md` — "[NIT:consistency] `CommitWeftAt`/`PushWeftAt` claim overstated".
  Verified: the unexported `commitWeftAt`/`pushWeftAt` still exist and are what `Bolt.Commit`/`Bolt.Push` delegate to.
  Card 36's correction is reworded to "no exported pair remains; `boardengine` reaches the unexported pair through `Bolt`", with an explicit instruction not to write "neither function exists any more".

## Pushed Back

_None._
Every finding was factually accurate and confirmed against the source before fixing.
