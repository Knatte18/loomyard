# Discussion fix — round 3

Source review: `_mill/reviews/20260826-112229-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r3 `[NIT:scope]` "Fixture-breakage sweep found only SeedConfig" — added the `empty-stage-class-enumeration` Decision stating the mechanical enumeration rule (`git add` + bare `git commit` at `h.PrimeWeft()`, minus sites that write a file first), listing all three members and naming `internal/fabriccli/pushbypass_integration_test.go` as the one excluded site with its reason; added both preflight sites to Scope In.
- r3 `[NIT:decision]` "configengine shared-lib doc has no disposition" — `docs-in-the-same-commit` now names `docs/shared-libs/configengine.md` and its `## Exported functions` section explicitly, with the rationale for inclusion.
- r3 `[NIT:consistency]` "Backfill remedy overstates its reach" — `no-backfill-for-existing-hubs` now qualifies the remedy: `Topology.Add` forks from the invoking worktree's own HEAD branch, so a pair added from an already-broken pre-fix pair still needs `lyx config reconcile --apply`.

## Pushed Back

(none)
