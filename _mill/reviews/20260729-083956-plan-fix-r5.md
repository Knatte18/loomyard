# Plan fix report — round 5

Source review: `_mill/reviews/20260729-083458-plan-review-r5.md` (verdict: REQUEST_CHANGES, 3 BLOCKING, 2 NIT).

## Fixed

- `_mill/reviews/20260729-083458-plan-review-r5.md` — "[BLOCKING] Card 25 breaks the Test Tier Purity guard": confirmed `ghguard_test.go`/`gitrepoboundary_test.go` are both individually allowlisted in `tierpurity_test.go`'s `allowedSpawners` map, and the new `boardguard_test.go` (untagged, calls `exec.Command`) would trip the same guard without a matching entry. Fixed by adding `cmd/lyx/tierpurity_test.go` to Card 25's Edits with the exact new `allowedSpawners` entry required.
- `_mill/reviews/20260729-083458-plan-review-r5.md` — "[BLOCKING] Batch 2 leaves internal/fabricengine non-compiling between Card 4 and Card 6": confirmed Card 4's `suffixWeftPrimaryBranch` signature change left `CloneHub`'s own call site (same file) on the old signature until Card 6. Fixed by having Card 4 itself capture the new `hostBranch` return value at the call site (keeping `CloneHub`'s own return shape unchanged — that shrink is still Card 6's job), and rewording Card 6 to describe finishing that same call site rather than duplicating the capture.
- `_mill/reviews/20260729-083458-plan-review-r5.md` — "[BLOCKING] Batch 3 leaves internal/boardengine non-compiling for most of the batch": confirmed at scale (dozens of struct literals across `render_test.go`/`board_test.go`/`config_test.go`/`template_test.go`, unresolved until Cards 17/18). Verified this is architecturally different from the other two BLOCKING findings this round: mill's own execution model dispatches one implementer session per BATCH and runs `verify:` once at the batch's end, with no mechanism checking intermediate per-card compilability — unlike the two findings above, where fixing the SAME file's own direct call site was cheap and immediate. Consolidated what WAS cheaply fixable (`boardtest/bench_test.go`'s `seedWiki` raw-YAML fixture, previously split across Cards 13 and 17, now landing entirely in Card 13 alongside that file's other fixes) and added an explicit, reasoned acknowledgment to Batch Scope stating the remaining gap is a deliberate, documented tradeoff — not silently accepted, not glossed over — so a future reader understands why it exists rather than mistaking it for an oversight.
- `_mill/reviews/20260729-083458-plan-review-r5.md` — "[NIT] Card 1 Context omits internal/gitrepo/gitrepo.go": confirmed `StageAllAndCommit` is defined there. Fixed by adding it to Card 1's Context.
- `_mill/reviews/20260729-083458-plan-review-r5.md` — "[NIT] Card 17 miscounts configengine/config_test.go fixtures": confirmed via direct grep — exactly two `home:`-containing occurrences (lines 35, 77), not three (three OTHER lines use `path: _board\n` alone, no `home:`). Fixed the count and cited the exact line numbers.

## Pushed Back

- `_mill/reviews/20260729-083458-plan-review-r5.md` — "[BLOCKING] Batch 3 leaves internal/boardengine non-compiling for most of the batch" (partial pushback on the suggested FIX, not the finding itself, which is factually accurate and acknowledged above): declined to fully reorder/merge Cards 11-19 so every intermediate commit compiles. Reason: mill's own execution model (one implementer session per batch, `verify:` invoked once at the batch's end — see `mill-go/SKILL.md`'s "1. Implement"/"3. Code Review loop" structure, both batch-scoped) means no mechanism ever checks intermediate per-card compilability within a batch; the two OTHER findings this round (Card 4/6, Card 25) were fixed immediately because they were cheap, single-file, single-concern fixes — full resequencing here would either bloat Cards 11/12 into much larger, harder-to-review diffs (folding in every downstream test file's fallout) or require renumbering most of the batch's 10 cards under real risk of introducing a NEW numbering/cross-reference defect at round 5-of-6, for a property (per-commit git-bisectability within one batch) nothing in this task's own build/verify/review pipeline actually checks.
