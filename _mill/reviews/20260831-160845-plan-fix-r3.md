# Fixer report: plan-fix round 3

Source review: `_mill/reviews/20260831-160131-plan-review-r3.md`

## Fixed

- `20260831-160131-plan-review-r3.md` — "[BLOCKING:design] \"curl absent from PATH\" scenario has no described isolation mechanism": card 13's harness scaffolding now requires a helper that builds a `curl`-free stub directory at runtime under the harness scratch, holding a copy of the stub `gh` and nothing else, and requires the scenario runner to take the stub directory as a parameter defaulting to the normal one. The card records why the tree harness's own trick cannot be reused: emptying PATH hides `gh` too, and this scenario needs `gh` to resolve while `curl` does not. The sixth raw-path scenario now names that directory explicitly, so the absence is constructed rather than assumed, and every other scenario is unaffected.
- `20260831-160131-plan-review-r3.md` — "[NIT:consistency] Duplicate Context entry in batch 1 card 6": duplicate removed. The duplicate was introduced by round 1's own fix — the entry was already present in card 6, so round 1's NIT reporting it as missing was itself inaccurate and the edit landed on top of an existing line.

## Pushed Back

_(none — both findings verified accurate with no harm from fixing.)_
