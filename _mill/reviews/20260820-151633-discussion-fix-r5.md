# Discussion fix — round 5

Source review: `_mill/reviews/20260820-151546-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- `20260820-151546-discussion-review-r5.md` — "Roadmap's Bouncer item still states the superseded discriminator": Scope → Out now carries an explicit exception stating that `manifest/roadmap.md:23`'s single-artifact seed-vs-judge sentence is amended to the pair predicate in this task's commit, and that the pair predicate is additionally recorded durably in `internal/shedadapters/doc.go` and `manifest/designs/shed.md`'s Engine-adapters section so it survives the roadmap entry's eventual deletion.
- `20260820-151546-discussion-review-r5.md` — "Focus-file token: read side defined, writer side unstated": the focus-file decision now states the token's meaning explicitly — `round-<N>-focus.json` carries directives *for* round `N`, a `Bouncer` rejecting round `N` writes `round-<N+1>-focus.json`, and the seed call writes `round-1-focus.json` — together with why the ambiguity had to be closed here (a mis-named file degrades silently to "no directive" under the file's own fail-safe rule).
- `20260820-151546-discussion-review-r5.md` — "Two source citations imprecise": the `runDir`-creation Q&A entry no longer claims `PerchProducer.resolveRunID`'s `MkdirAll` covers a run dir (it covers the scratch dir, `perch.go:120-126`), and the Technical-context line now says `ResolveFan` *rejects* a fan exceeding `maxClusterN` (`config.go:102-104`) rather than capping it. Both underlying decisions are unchanged.

## Pushed Back

None.
