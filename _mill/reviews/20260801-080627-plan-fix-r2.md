# Plan fix report — round 2

Source review: `_mill/reviews/20260801-080400-plan-review-r2.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 3 NIT). All four findings VERIFIED accurate against the code/exploration and FIXED.

## Fixed

- [BLOCKING] "Unwire's WeftContent port drops a branch, contradicting its own 'verbatim' claim and Card 30's test" — CONFIRMED: `undo.go:122-138` has a three-way branch (weft-absent → `not_present`; weft-present-but-`_lyx`-absent → `not_present`; `_lyx`-present → `cleared`), and a raw-adopted dormant pair (`createDormantWeftForRawHost`) would wrongly report `cleared` under the two-way paraphrase, contradicting Card 30's `not_present` assertion. Fixed Batch 6 card 23 to spell out the exact three-way branch with the nested `os.Stat(l.WeftLyxDirFor(slug))` check before `RemoveAll`.
- [NIT] "Card 33's docs/overview.md:170 edit target contains no 'lyx init' text" — CONFIRMED: `lyx init` occurs only at overview.md:168/181/188; line 170 (`config` bullet) has no such substring. Fixed card 33 to drop the erroneous line-170 sub-instruction and keep only the line-181 (`loom`) edit, noting 168/188 are the removed `init` entries.
- [NIT] "Stale `internal/initengine` reference in configsync.go survives both batches" — CONFIRMED: `configsync.go:99`'s doc comment names the initengine package (seed-only heuristic), which card 11's conditional didn't cover and card 28 didn't list. Fixed by adding `internal/configsync/configsync.go` to card 28's Edits with an explicit line-99 reword instruction, and softening card 11's mention to defer to card 28.
- [NIT] "Card 9's stale-removal placement across Reconcile's outcome branches is underspecified" — CONFIRMED: `Reconcile` has five per-pair outcomes; the card said only "after add-missing for a healthy-or-repaired pair". Fixed card 9 to pin stale-removal to the weft-worktree-exists branch (same branch as `checkJunctionHealth`/`WireJunctions`), for both already-healthy and just-repaired outcomes, and explicitly NOT in the weft-missing/raw-adopted branches (no established junctions to converge yet).

## Pushed Back

(none — all four findings were factually accurate and fixing them caused no harm.)
