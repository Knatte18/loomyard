# Discussion fix report — round 6

Source review: `_mill/reviews/20260831-181212-discussion-review-r6.md` (verdict APPROVE, 0 BLOCKING, 1 NIT, no demoted findings).

## Fixed

- r6 review, "[NIT:consistency] Merge-record path paraphrase adds a stray `.git`" — Technical context's "two merge predicates" paragraph now quotes `mergestate.go:215-217` verbatim (`<weft gitdir>/fabric-merge.json`, the weft repo's own `.git` for the prime pair and `.git/worktrees/<name>/` for each linked pair) and states outright that the gitdir already is `.git`, so a plan writer copying the path cannot double-count it. The stale `mergestate.go` line reference in the same sentence was corrected at the same time: `mergeSourceInFlight` is declared at line 226, not 209.

## Pushed Back

None. The finding was factually accurate and the fix broke nothing, conflicted with no documented design decision, and destabilized nothing outside this task's scope.
