# Discussion fix — round 2

Source review: `_mill/reviews/20260714-080323-discussion-review-r2.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- `20260714-080323-discussion-review-r2.md` — "[NOTE] SiblingLayout does not clean its input; Resolve does": `SiblingLayout` now `filepath.Clean`s its input (matching `Resolve`'s `Cwd`/`WorktreeRoot` cleaning), and the equivalence test asserts all four `Layout` fields explicitly (incl. `WorktreeRoot`). Updated in Decisions/sibling-layout-method and Testing.
- `20260714-080323-discussion-review-r2.md` — "[NOTE] Guard logic duplicated at two call sites": decided the guard is lifted into one unexported warpengine helper (`hostLayoutFor(l, root) (*Layout, error)`), not inlined at both sites; keeps `SiblingLayout` a pure no-spawn primitive and preserves per-site `Resolve`-error handling. Documented in Decisions/sibling-layout-method.
- `20260714-080323-discussion-review-r2.md` — "[NOTE] Spawn-count guard assertion form left open": guard test now runs at two worktree counts and asserts the `--show-toplevel` count does not grow (it is 0 in both post-fix), replacing the brittle single-N `== 0` / imprecise "bounded by one-time List" framing. Updated in Decisions/spawn-count-regression-guard and Testing.

## Pushed Back

(none)
