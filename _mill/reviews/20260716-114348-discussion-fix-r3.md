# Discussion-fix report — round 3

Source review: `_mill/reviews/20260716-114240-discussion-review-r3.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- 20260716-114240-discussion-review-r3.md — "[NOTE] repo \"always non-empty\" ignores empty-Prime
  path": verified `hubgeometry.go:126-131` leaves `Prime == ""` when no `Main` entry exists, and
  `filepath.Base("")` is `"."`. Updated the repo-derivation decision to require the `Repo` setter to
  fall back to `filepath.Base(WorktreeRoot)` on empty `Prime`, and added a test bullet for the
  fallback.
- 20260716-114240-discussion-review-r3.md — "[NOTE] tokenvocab leaf/acyclic direction is review-only,
  unlike peers": verified peers ship enforcement (`internal/modelspec/leaf_enforcement_test.go`
  `TestLeafInvariant_AllowlistOnly`, `internal/lyxtest/leaf_enforcement_test.go`). Added a
  machine-enforced import test for `tokenvocab` (allowlist stdlib + `hubgeometry` + `stencil`) and a
  CONSTRAINTS.md leaf-invariant entry to the Dependency-graph decision, Constraints, and Testing.

## Pushed Back

(none — both NOTEs were factually accurate and low-risk; fixed under the default rule.)
