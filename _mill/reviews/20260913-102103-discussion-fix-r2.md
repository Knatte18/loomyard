# Discussion fix — round 2

Source review: `_mill/reviews/20260913-101922-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 5 NIT — one demoted from BLOCKING by the stage's `blocking_classes` ceiling).

## Fixed

- `20260913-101922-discussion-review-r2.md` — "[NIT:scope] Two accept-table cases break, discussion says none do" (demoted from BLOCKING): Testing gains an explicit `TestParse_Accepts` subsection re-spelling `"alias multiple params"` (`parse_test.go:29-33`) and `"dotted version value"` (`:44-48`) onto `v=` with unchanged expected `Params`; the blanket stay-green claim is now scoped to "every case other than the two rewrites and the one deletion"; the Scope bullet for `parse_test.go` names both edits.
- `20260913-101922-discussion-review-r2.md` — "[NIT:consistency] Conflicting line cite for the deleted reject case": the Decisions section's `parse_test.go:186` corrected to `175-179`, matching Testing.
- `20260913-101922-discussion-review-r2.md` — "[NIT:design] Migration-hint scope unstated for other unknown keys": the `version=`-removal Decision now states the hint clause is conditional on the offending key being exactly `version`, and names `sonnet[speed=fast]` (`parse_test.go:165-169`) as keeping the unadorned message.
- `20260913-101922-discussion-review-r2.md` — "[NIT:consistency] Duplicate error names a key absent from the input": the duplicate-key Decision now specifies an error that names the canonical key **and** quotes both offending segments as written, with the rationale for why neither canonical-only nor as-written-only is complete, and adds the canonical-only form to its Rejected list.
- `20260913-101922-discussion-review-r2.md` — "[NIT:scope] `internal/loomengine/template.yaml` omitted from the `effort=` inventory": added to both the Problem paragraph (four roles) and the Scope "Out" untouched-sites bullet, which now also states no `.yaml` outside `internal/modelspec` is touched.

## Pushed Back

None.
