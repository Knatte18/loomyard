# Plan fix report — round 1

Source review: `_mill/reviews/20260627-151332-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 2 NITs).

## Fixed

- [r1 BLOCKING] "Bad-flag path emits 'unknown flag', not 'unknown command'" — split the two
  cobra messages everywhere: the `help-and-unknown-surfaces` decision (00-overview) now states
  unknown subcommand → `unknown command "x"` (+ suggestions) vs bad flag → `unknown flag: --x`;
  Card 8 (batch 2) and Card 17 (batch 5) now instruct asserting `unknown command` only for
  unknown subcommands and `unknown flag` (or just exit 1) for bad flags. Card 17 specifically
  calls out muxpoc's existing `TestRunCLIUnknownFlagFails` as a bad-flag → `unknown flag` case.
- [r1 NIT] "board card says 12 subcommands but there are 11" — corrected all four count
  references in 04-board.md (scope, Card 12 title + commit, Batch Tests) to 11.
- [r1 NIT] "SilenceUsage only set on root, not module Command()s" — `clihelp.Execute` (Card 2)
  now sets `cmd.SilenceUsage = true` on the executed command for seam/root parity; the
  `exit-and-error-contract` decision (00-overview) documents that SilenceUsage is set on both
  the production root and the module seam.

## Pushed Back

(none)
