# Plan fix report — round 3

Source review: `20260623-113238-plan-review-r3.md` (verdict REQUEST_CHANGES, 1 BLOCKING, 1 NIT).

## Fixed

- `20260623-113238-plan-review-r3.md` — "[BLOCKING] Card 10 tests unexported applySkipEnv
  from external pkg": verified (all `internal/board/*_test.go` are `package board_test`; no
  white-box file exists; `applySkipEnv` is unexported). Rewrote **card 10** to *create* a new
  white-box test file `internal/board/skipenv_internal_test.go` (`package board`) that calls
  `applySkipEnv` directly, instead of editing the external `cli_test.go`. Updated card 10
  Edits→Creates; updated All Files Touched (removed `cli_test.go`, added the new file in sorted
  position). `applySkipEnv` stays unexported.
- `20260623-113238-plan-review-r3.md` — "[NIT] Two surviving BOARD_SKIP_* env-setters not
  acknowledged": verified (`cmd/lyx/main_test.go:42/76` and `internal/ide/menu_test.go`). Added
  a note to **card 9** stating both are intentionally untouched and why they do not regress:
  `main_test.go` reaches the board via `RunCLI` (env folded by `applySkipEnv`); `menu_test.go`
  only calls read paths (`HealthCheck`/`GetTask`), never `writeOp`/`spawnSync`.

## Pushed Back

(none)
