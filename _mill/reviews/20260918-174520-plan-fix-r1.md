# Plan fix — round 1

Source review: `_mill/reviews/20260918-174128-plan-review-r1.md`

## Fixed

- `20260918-174128-plan-review-r1.md` — "[BLOCKING:scope] Card 30's Context omits the two files its own Requirements text cites": added `internal/reedengine/lock.go` and `internal/reedengine/watchloop.go` to card 30's `Context:` list in `_mill/plan/05-watchdog-daemon.md`. Verified accurate — the card's Requirements has `enterSession` call `reedengine.New` (declared in `lock.go`) and both describes `watchLoop`'s disabled-watchdog parking behaviour and calls `go eng.Watch(ctx)` (both in `watchloop.go`), while card 31 already listed `watchloop.go` for the same reason.
- `20260918-174128-plan-review-r1.md` — "[NIT:consistency] Unused \"Rename mechanic\" section in selvage-pane batch": deleted the `## Rename mechanic` section from `_mill/plan/03-selvage-pane.md`. Verified accurate — every card in that batch declares `Moves: none`, and the `move-mechanic-missing` validator check requires the section only for a batch carrying a non-empty `Moves:` entry.

## Pushed Back

_None._
