# Plan fix — round 2

Source review: `_mill/reviews/20260819-092058-plan-review-r2.md` (holistic, opus/high, REQUEST_CHANGES, 3 BLOCKING + 6 NIT).

## Fixed

- r2 "Conflict-envelope contract test unreachable under batch 6 verify" — verified the three existing functions in `envelopecontract_integration_test.go` (`TestRunCLI_ReconcileReportsAFailedPairAsAFailure`, `TestRunCLI_PruneEmitsAnEmptyArrayNotNull`, `TestRunCLI_ReconcileDoesNotFailOnAPairThatVanishedMidWalk`) are all missed by `-run Merge`.
  Card 17 now requires a new `Merge`-named test function in that file and names why extending the existing ones would never execute in-batch.
- r2 "`logger.Warn` named with no logger file in Context" — confirmed none of cards 7's or 11's listed files calls `logger`.
  Added `internal/fabricengine/checkout.go` to both cards' `Context:`.
- r2 "finalize.md's discrepancy-document claim left standing" — verified lines 16–19 promise a second "discrepancy document" shape this plan does not ship.
  Card 18 now reworks that list to the shipped shape plus the `*ErrUnmergeableState` refusal, and the removed shape is carried into `## Someday`.
- r2 "[NIT] Cleanup carve-out is an unrecorded third supersession" — verified `discussion.md:231` and `:645` both group `Remove`/`Cleanup`.
  Promoted card 13's reasoning into the Shared Decision "Cleanup needs no merge guard", citing the `combined-lock-around-mutating-steps-only` clause it supersedes;
  Decision 1's supersession count corrected (now four, including the `MergeStart` probe below).
- r2 "[NIT] Audit findings have no disposition anywhere in the plan" — accurate; `_mill/` is task-local.
  Card 18 now pins one `## Someday` entry each for the monorepo verb surface, the two-sided reset-to-SHA verb, merge-in-progress in `lyx fabric status`, and the discrepancy-document shape, and states explicitly which remaining findings are deliberately not roadmap items and where the two consumer-facing ones (no post-conclude undo, squash leaves no ancestry link) land instead — the `fabricengine/doc.go` merge section.
- r2 "[NIT] Wrong file count prescribed for the boundary-guard comment" — verified `internal/gitrepo` has 9 non-test files today and the comment omits `blobread.go`.
  Card 1 now says to add both `blobread.go` and `merge.go` and state 10.
- r2 "[NIT] Roadmap move left conditional" — verified `manifest/roadmap.md` has a `## Done` section at line 132.
  Card 18 pins the move to that section in its existing style, dropping the conditional.
- r2 "[NIT] MergeStart's conflict probe silently diverges from the pinned surface" — added the Shared Decision "MergeStart's post-error conflict probe reuses ConflictedFiles" naming the superseded `public-surface-shapes` godoc spelling, and cross-referenced it from card 1.
- r2 "[NIT] Vocabulary walk unrun until batch 6" — appended `./internal/lyxcwd/` to batches 1–5's untagged `verify:` in both the batch files and the overview's Batch Index, and stated the rationale in batch 1's `## Batch Tests`.

## Pushed Back

(none)

## Validation

- `_plan_dag.validate`: 6 batches, DAG OK.
- `_plan_validate.run`: 0 findings.
