# Plan fix — round 1

Source review: `20260812-183004-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 6 NITs).

## Fixed

- `20260812-183004-plan-review-r1.md` — "[NIT:consistency] Batch 8 frontmatter card count wrong": `08-fabricengine-external.md` frontmatter corrected from `cards: 13` to `cards: 11`; the file holds cards 42–52.
- `20260812-183004-plan-review-r1.md` — "[NIT:consistency] Batch 9 frontmatter card count wrong": `09-fabricengine-inpackage-weft.md` frontmatter corrected from `cards: 11` to `cards: 10`; the file holds cards 53–62.
- `20260812-183004-plan-review-r1.md` — "[NIT:consistency] Batch 4 scope prose overstates its site counts": corrected to "seventeen `Copy*` sites and fourteen `SeedConfig` sites" in `04-small-consumers.md`, and the matching "nineteen sites" in `00-overview.md`'s DAG-chain paragraph to "seventeen".
  Re-derived per package: webstercli 1, configcli 1, idecli 2, boardtest 1, burlerengine 2, shuttlecli 4, perchcli 6 = 17 `Copy*`;
  1+1+0+0+2+4+6 = 14 `SeedConfig`.
  The migrated-site total is unaffected — 17+20+2+11+37+38+7 = 132, matching batch 10's arithmetic and the overview.
- `20260812-183004-plan-review-r1.md` — "[NIT:consistency] NewPairedForTest counts conflate raw text hits with real call sites": partially fixed, see Pushed Back for the one sub-claim that did not verify.
  `08-fabricengine-external.md`'s `## Batch Scope` now states the call-expression counting method explicitly (the same `grep -o` method the `Copy*` table uses) and gives the real figures: eleven call expressions total, ten migrating, one staying.
  Card 51 corrected from "ten calls in `warpforward_integration_test.go`" to five, and from "two in `checkout_index_refresh_test.go`" to one.
  Card 51's `fabric_test.go` clause now distinguishes its one call expression from its three message-string mentions.
- `20260812-183004-plan-review-r1.md` — "[NIT:consistency] Verify-shape decision doesn't state the scout-tag case it already uses": `00-overview.md`'s verify-shape decision now covers `-tags scout` alongside `-tags smoke`, names `internal/scoutengine`'s three `//go:build scout` files as batch 1's trigger, and records why each tag gets its own `go vet` invocation rather than a comma-joined `-tags` list (Go reads that list as a conjunction).
- `20260812-183004-plan-review-r1.md` — "[NIT:consistency] "two seed-commit messages" misdescribes hub.go": `02-fabrictest-dissolution.md` card 7 reworded to "the single seed-commit message … and the two `README` bodies naming `fabrictest`".

## Pushed Back

- `20260812-183004-plan-review-r1.md` — "[NIT:consistency] NewPairedForTest counts conflate raw text hits with real call sites", sub-claim that `weftgit_exclude_test.go` has 2 call expressions rather than the 4 card 50 states: factually wrong.
  `grep -o 'fabricengine\.NewPairedForTest(' internal/fabricengine/weftgit_exclude_test.go | wc -l` returns 4, and a bare `grep -c` on the same file also returns 4 — this is the one file where the two counts coincide, because it carries no string-literal mention of the identifier.
  Card 50's "four" is left as written.
  The finding's main claim was accurate for the other two files and is fixed above.
