# Discussion fix — round 1

Source review: `_mill/reviews/20260919-172030-discussion-review-r1.md` (verdict APPROVE, 3 NITs, 2 demoted from BLOCKING).

## Fixed

- r1 `[NIT:consistency]` "Invariant's mechanical proxy contradicts the task's own scope" — the invariant now governs the *recorded* `Seed.Driver` read rather than the driver constants, names the seeding sites as permitted consumers of the vocabulary alone, and restates the proxy against that split.
- r1 `[NIT:decision]` "Autonomous step cap has a derivation but no number" — the cap is now the literal `120`, with the derivation stated and the requirement that SKILL.md and the Go-composed launch prompt pin the same value.
- r1 `[NIT:design]` "Timestamped report path can collide on a fast relaunch" — the report path gains a 4-hex per-attempt suffix; the bare-timestamp-plus-accepted-residual alternative is recorded as rejected, and the spec-composition test now pins uniqueness under a frozen clock.

## Pushed Back

None.
