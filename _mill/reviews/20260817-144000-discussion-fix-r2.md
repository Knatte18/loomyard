# Discussion fix — round 2

Source review: `_mill/reviews/20260817-143843-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r2 NIT `[consistency]` "Q&A log still answers Debug for the log level" — rewrote the stale Q&A entry to answer `logger.Info` and point at the dedicated level entry and the `info-level-observability` decision, so the artefact no longer carries two opposite answers. Also tightened the adjacent env-marker Q&A entry to match round 1's corrected two-of-four claim.
- r2 NIT `[design]` "Import-widening analysis covers cycles only" — split the rationale into two checks, adding the allowlisted-importer half: `internal/modelspec` and `internal/gitkit` both import `internal/configengine` under leaf-invariant caps, and both enforcing tests are direct-import allowlists (`parser.ParseFile(..., parser.ImportsOnly)` over their own package's files), so neither is tripped. Verified against `internal/modelspec/leaf_enforcement_test.go:52-70` and `internal/gitkit/leaf_enforcement_test.go:47-77` before asserting it.
- r2 NIT `[scope]` "Doc-surface enumeration misses configengine.md's key-properties bullet" — replaced the prose enumeration with a five-row table naming every stale claim including line 47's "Errors are strict" bullet, and made that table the single authority for the line ranges so the Q&A entry can no longer disagree with it (it previously said 124-131 against the prose's 127-131).

## Pushed Back

None. All three findings verified accurate against source and none met a Decision Tree harm category.
