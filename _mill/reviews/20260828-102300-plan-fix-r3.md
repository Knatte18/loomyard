# Plan fix — round 3

Source review: `_mill/reviews/20260828-101834-plan-review-r3.md`

## Fixed

- r3 "[BLOCKING:scope] Card 20's Context omits the files its literal fixture needs" — verified: card 20's Requirements names `reedengine.New`/`Engine` (`internal/reedengine/lock.go`), `reedengine.Config` (`internal/reedengine/config.go`), `reedengine.Geometry` (`internal/reedengine/geometry.go`), and reasons about `HeaderText`'s embedded-template fallback and `tokenvocab.Ctx`'s field consumption (`internal/reedengine/header.go`), none of which were in its `Context:`.
  All four added, matching card 5's already-corrected treatment of the identical `New`/`Geometry` fixture pattern.

## Pushed Back

_None. The finding was factually accurate and the fix breaks nothing._
