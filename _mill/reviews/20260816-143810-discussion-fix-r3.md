# Discussion fix — round 3

Source review: `_mill/reviews/20260816-143726-discussion-review-r3.md` (APPROVE, 0 blocking, 2 NIT).

## Fixed

- r3 — "Repo-root `.gitattributes` not in the file inventory": added three new lines to §Scope In, and rewrote the CRLF paragraph in §Stencil mechanics to name both `.gitattributes` files and state which one covers the `//go:embed` source bytes. Verified the repo-root file lists all fifteen stencils individually (lines 7-21) with no `stencils/**` glob, and that no test guards the omission.
- r3 — "Marker-free stencils are a new category, disposition unstated": recorded that these are the first registered stencils never passing through `Fill`, that `stencilstore.Validate` parses them anyway via `stencil.TopLevelMarkers` (`validate.go:52,56`), and that they must stay marker-free. Also folded into the banner decision, so the requirement is stated in the files themselves.

## Pushed Back

None.
