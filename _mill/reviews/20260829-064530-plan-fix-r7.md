# Fixer report: plan-fix round 7 — reed-pane-reap-consistency

Source review: `_mill/reviews/20260829-063054-plan-review-r7.md` (holistic, **APPROVE**, 0 BLOCKING + 2 NIT, one ceiling-demoted from BLOCKING).
Both findings were verified against the real source files before acting;
both were accurate, and both were fixed.

Operator override in effect: `max_review_rounds` raised from 6 to 7 for this loop only (session-local; `mill-config.yaml` unchanged), with Handoff after round 7 regardless of verdict and every finding fixed first.
Round 7's own verdict is `APPROVE` with zero blockers, so the approval is earned on its own terms and the override's "regardless" clause never had to be exercised.

## Fixed

- r7 holistic, `[NIT:consistency]` (demoted) "Card 3 contradicts itself on the reap log's id values" — confirmed, and introduced by my own round-6 fix. The card's original sentence named `plan.deadPanesToKill` / `plan.untrackedPanesToKill` as the logged values, while the partial-kill requirement added in round 6 introduced accumulated actually-killed locals. The two agree on the success path and diverge on exactly the path round 6 existed to cover: a failing `kill-pane` returns from inside the loop, so the scheduled lists would name panes that were never destroyed. Card 3 now states unambiguously that the two keys carry the accumulated locals, never the scheduled lists, with the reason — this is a destruction record, not a record of intent, and a log claiming to have destroyed a still-alive pane is worse than no log.
  The reviewer's observation that the card's own third test case could not discriminate between the two readings was also acted on: that case must now assert both halves — the destroyed pane's id present in the output **and** the failed pane's id absent from it. The absence half is what actually pins the distinction, since both candidate sources contain the first pane.
- r7 holistic, `[NIT:scope]` "reconcile.go's file-header comment is unassigned" — confirmed at `internal/reedengine/reconcile.go:1-5`: it says `planReconcile` "decides which strand pane bindings to clear and which dead panes to kill", which card 1 falsifies by promoting untracked kills to a first-class struct field and card 2 makes routine, yet no card owned it and neither of card 12's grep terms reaches it. Assigned to **card 1** rather than the reviewer's suggested card 10: card 1 is the card that makes the sentence wrong, so fixing it there keeps the file header honest at every commit boundary, whereas deferring it to the batch 3 doc sweep would park a false header at the top of the file across three batches. The card records that reasoning so the placement is not later mistaken for a stray edit.

## Pushed Back

_None._
Both findings were factually accurate, and neither fix broke existing functionality, conflicted with a documented decision in `_mill/discussion.md` or `CONSTRAINTS.md`, or destabilised work outside this task's scope.

## Post-fix validation

`_plan_dag.validate` passes.
`_plan_validate.run` returned zero findings with an empty `skip_checks`.

## Review-loop summary

Seven holistic rounds, 37 findings total (13 BLOCKING, 24 NIT including 5 ceiling-demoted).
36 fixed;
1 resolved via the finding's own stated alternative with the underlying reader-expectation problem addressed directly (r4's batch-numbering NIT, documented in that round's report).
Round 3 additionally consumed one transient agent-API failure, re-dispatched per the Agent-mode recovery rule without consuming a round.
