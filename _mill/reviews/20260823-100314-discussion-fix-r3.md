# Discussion fix — round 3

Source review: `_mill/reviews/20260823-100214-discussion-review-r3.md` (APPROVE, 0 BLOCKING, 4 NIT, none demoted).

## Fixed

- `20260823-100214-discussion-review-r3.md` — "[NIT:design] Parity test's verdict mapping is unstated": `parity-tests-per-gate` now specifies a three-way comparison and its exact mapping (`Done` ↔ `ok: true`; `Stuck` ↔ `ok: false` with a `findings` key; returned error ↔ `ok: false` without one), states why a binary comparison would be blind to the case the pinned short-circuit order exists to protect, and pins the findings-vs-fault distinction to the structural presence of the `findings` key rather than message wording. The `## Testing` parity paragraph carries the same mapping and now requires each fixture set to exercise all three outcomes.
- `20260823-100214-discussion-review-r3.md` — "[NIT:consistency] Cross-gate parity rule filed under a package-named invariant": the parity rule moves out of the Discussionparser section into its own short **Gate Self-Check Parity Invariant**, since it binds both gates (including the `planparser`-backed one) and every mechanical gate added later. `new-constraints-invariant` now specifies two new `CONSTRAINTS.md` sections; the Scope bullet and the `docs-in-the-same-commit` decision were updated to match.
- `20260823-100214-discussion-review-r3.md` — "[NIT:consistency] Scope bullet contradicts the later test decision": the Scope bullet now names the tightening (subset check → exact-set assertion over `parent.Commands()`, with a rename) instead of "extend to six verbs", and points at the Registration-tests paragraph.
- `20260823-100214-discussion-review-r3.md` — "[NIT:consistency] Quote attributed to the wrong file": the `Plan-Sweep` section-parsing quote is re-attributed to `manifest/designs/loom.md:119`, with a note that `manifest/roadmap.md`'s own entry only points at that design section.

## Pushed Back

_None._
