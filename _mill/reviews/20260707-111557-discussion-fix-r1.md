# Discussion fix — round 1

Source review: `_mill/reviews/20260707-111351-discussion-review-r1.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- `20260707-111351-discussion-review-r1.md` — **"[NOTE] 'Collect all offenders' only holds for top-level markers"** — reworded the guard's error-shape bullet to "all *top-level* offenders collected & sorted," added an explicit "Scope of the collect-all guarantee" bullet (top-level-only; branch-internal caught incrementally via `missingkey=error` halting at first miss), and reworded the Testing case to "Multiple *top-level* offenders collected & sorted" plus a new "Branch-internal miss caught incrementally" scenario.
- `20260707-111351-discussion-review-r1.md` — **"[NOTE] Dedup/precedence between the walk and missingkey for a top-level absent key"** — pinned the sequencing in both the guard Decision and Technical-context mechanics: parse → top-level walk (single `TrimSpace(values[X])==""` check covers absent *and* empty) → if any offender, return one sorted error **without executing** (so `missingkey=error` never double-reports a top-level key) → execute only when top level is clean, where `missingkey=error` then catches branch-internal misses. Renumbered the two guard mechanisms (1 = top-level absent-or-empty via walk, 2 = reached-but-absent non-top-level via missingkey) and fixed the rationale references accordingly.
- `20260707-111351-discussion-review-r1.md` — **"[NOTE] No malformed-template test scenario"** — added a "Malformed template → wrapped error" scenario (unclosed `{{if}}` → `fmt.Errorf("parse template: %w", …)`, not a panic) to the Testing table, matching the pinned parse/exec error wraps in Technical context.

## Pushed Back

_None. All three NOTEs were factually accurate and low-cost; fixed under the default rule._
