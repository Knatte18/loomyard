# Discussion fix — round 5

Source review: `_mill/reviews/20260818-055800-discussion-review-r5.md` (APPROVE, 0 BLOCKING, 2 NIT, none demoted).
Auto mode: both NITs fixed under the `mill-receiving-review` fix-everything default.
Convergence gate satisfied on this round: `round (5) >= min_rounds (1)` and no finding carried a `demoted` flag.

## Fixed

- r5 · "Tautology comment cannot just widen its count" — verified that `cmd/lyx/constructoranchoring_test.go:81-87` (and `137-143`) hold six `loomengine`/`websterengine`/`perchengine` rows that still pass `l` rather than `l.AnchorPath()` and are therefore not tautological, sitting between the `planparser` rows (79–80 / 135–136) and the `pattern` row (88 / 144). The decision now requires the amended comment to **name** its covered rows — the two `planparser` rows plus the `pattern.File` row — and explicitly forbids restating a contiguous count, which would misdescribe the six intervening rows.
- r5 · "Stencils-helper hedge is already resolvable" — verified `internal/loomengine/prompt_test.go:36-48` writes all three `pattern-directive-*` stencils from the embedded defaults, and `internal/burlerengine/prompt_test.go:43-56` does the same through a map. Replaced the "confirm, and extend if not" branch with the established fact for both packages, and noted in the burler Testing bullet that its `stencilsDir` side needs nothing new either.

## Pushed Back

None.
Both findings verified against current source before fixing.
