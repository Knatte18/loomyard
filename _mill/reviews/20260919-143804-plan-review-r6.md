MILL_REVIEW_BEGIN
# Review: Shed-generic watchdog for ly-drive and loom's CLI verbs — holistic

```yaml
duration_s: 744.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: claude-opus-5 (harness-reported model id; the brief dictates sonnetxhigh)
reviewed_file: plan/
date: 2026-09-19
```

## Findings

### [BLOCKING:scope] No help-tree test ever sees the shed subtree
**Location:** batch 5 / card 32 (and batch 5 `## Batch Tests`)
**Issue:** Card 32 says `helptree_test.go` "asserts the root help names every subtree" and the batch prose says the four guards "pick the new subtree up automatically once registered" — both false against source: `cmd/lyx/helptree_test.go`'s `TestHelpTree_RootNamesAllModules` compares against a hardcoded `requiredModules` slice and `TestHelpTree_VerbModuleSubcommands` against a hardcoded `wantSubs` table, and `jsonhelp_test.go` exercises only root/board/selfreport, so all three pass vacuously for `shed`; `cmd/lyx/helptree_test.go` appears in no card's `Edits:` and not in All Files Touched, so `_mill/discussion.md`'s Scope requirement of "a seam-enforcement scan **and a help-tree test** enforcing it" never lands.
**Fix:** Give card 32 an `Edits:` entry for `cmd/lyx/helptree_test.go` (add `shed` to both tables, plus All Files Touched), or state explicitly that `internal/shedcli/cli_test.go`'s bare-listing case is the help-tree proof and correct card 32's claim about which guards are live-tree-derived (only `drift_test.go`, `longlist_test.go` and `registration_test.go` are).

### [NIT:consistency] Card 31 contradicts itself on cli_test.go's cwd
**Demoted-from:** BLOCKING
**Location:** batch 5 / card 31
**Issue:** The card requires the `lyx shed step --recipe lifecycle <slug>` refusal case to "pass in a directory that is not a git worktree at all", then states "`table_test.go` and `cli_test.go` stay untagged tier 1: neither drives `RunCLIIn`" — but `RunCLI` delegates to `RunCLIIn("", …)` → `clihelp.Execute`, which reads the process cwd (the package source dir, inside this repo's worktree), so an injected non-git cwd is reachable only through `RunCLIIn`.
**Fix:** Pick one arm — either let `cli_test.go` call `RunCLIIn(t.TempDir(), …)` and delete the "neither drives `RunCLIIn`" clause (still tier 1, since the verb-set refusal precedes `Arm` and therefore `lyxcwd.Resolve`), or drop the non-git-directory requirement from the assertion.

### [NIT:decision] ly-drive against a non-steppable recipe has no disposition
**Demoted-from:** BLOCKING
**Location:** batch 6 / card 36 (with batch 5 / card 29)
**Issue:** Card 36 rewrites `ly-drive` to take any recipe name and loop `lyx shed step --recipe <name>`, but card 29 makes `lifecycle`'s `Verbs` set `{run, status, pause}`, so the only other shipped recipe refuses `step` — and card 30's verb-set refusal is a bare `output.Err` with no `kind` field, which the skill's own `## Error envelopes` contract ("carries a `kind` field with exactly one of five values") has no arm for. The plan never states which recipes are steppable today nor what the skill does with that refusal.
**Fix:** State the disposition in card 36 — either add a "refusal carrying no `kind`" hand-back arm to `## Error envelopes`, or record in the skill that `loom` is the only steppable recipe shipped and the argument exists for the next consumer.

### [BLOCKING:scope] Card 21 names shedverbs.KindBusy with step.go absent from Context
**Location:** batch 4 / card 21
**Issue:** The `Spec` fill prescribes `StepBusyKind: shedverbs.KindBusy`, but `KindBusy` is declared in `internal/shedverbs/step.go` (batch 3, card 12) and that path is in neither card 21's `Context:` nor its `Edits:`; no exemption applies, since step.go is an *earlier* card's `Creates:` target, not a forward reference. Card 22, by contrast, does list it.
**Fix:** Add `internal/shedverbs/step.go` to card 21's `Context:`.

### [NIT:consistency] Deletes: none on cards that determinately empty files
**Location:** batch 4 / cards 23 and 26
**Issue:** `internal/loomcli/status.go` declares exactly `renderStatusLine`, `statusUnavailableLine`, `printStatusLinesOnChange`, `statusCmd` and `pause.go` exactly `pauseCmd` — all five listed for deletion — so both files are certainly emptied, not conditionally; the same holds for `internal/lifecyclecli/run.go` (only `runCmd`) and `status.go` (only `statusCmd`). Both cards carry `Deletes: none`, and card 26 additionally orders "preserve `internal/lifecyclecli/run.go`'s doc comment" about the absent mutations array in a file it also orders deleted.
**Fix:** Make the deletions unconditional in `Deletes:` and name `internal/lifecyclecli/arm.go`'s `PostRun` field doc as the new home for the mutations-array rationale.

### [NIT:scope] Three inventory counts misstate the source
**Location:** batch 1 / card 4, batch 2 / card 7 + `## Batch Tests`, batch 6 / `## Batch Tests`
**Issue:** (a) card 7 and batch 2's test prose say `internal/lifecyclecli/wire_test.go` has "three `c.env.LoomRun.*` field reads" / "three seam-presence assertions" — it has two LoomRun reads (lines 58–59) among four seam assertions; (b) card 4 lists `internal/loomcli/wiring_test.go` and `internal/lifecyclecli/wire_test.go` among the qualified `…recipe.ShedPaths` sites, but `wiring_test.go` names the type only in doc comments (lines 163, 168) and `wire_test.go` never names it at all; (c) batch 6 says the sandbox guard "live[s] under `tools/sandbox`" — it is `cmd/lyx/sandbox_coverage_test.go`. None changes the work, since both cards mandate a repo-wide grep as the floor.
**Fix:** Correct the three counts/locations so the grep floor is not read as a verified ceiling.

## Verdict

REQUEST_CHANGES
Four blocking gaps: missing help-tree coverage, a self-contradictory test card, an unstated skill disposition, one Context omission.
_Note: 2 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 2._
MILL_REVIEW_END
