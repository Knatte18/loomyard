# Plan fixer report — round 3

Source review: `_mill/reviews/20260802-122953-plan-review-r3.md` (verdict REQUEST_CHANGES, 3 BLOCKING + 1 NIT). All four findings verified accurate against source → all FIXED, none pushed back.

## Fixed

- **[BLOCKING] Card 18 misses a surviving test's live SyncWeft call** (`20260802-122953-plan-review-r3.md`) — Confirmed `syncweft_integration_test.go:117` `TestRebuildIndex_EqualsIncrementallyBuiltIndex` calls `f.SyncWeft(...)` at `:125` and tests the surviving `RebuildIndex`; also `TestWeftSHAForWarpSHA_DetachedPathSelfCorrection`/`_StalenessSurvivesRebuild` survive and use `CommitWeft`. Rewrote card 18's deletion instruction to enumerate exactly the `TestSyncWeft_*`/`TestRevertWithWeft_*` functions to delete, added an explicit requirement to migrate `TestRebuildIndex_...`'s `SyncWeft` setup onto `Fabric.Commit`, and stated the file is KEPT (not deleted) because of the three surviving tests.
- **[BLOCKING] Four fabricengine test files calling renamed/deleted symbols are absent from the plan** (`20260802-122953-plan-review-r3.md`) — Confirmed: `snapshot_integration_test.go` (14+ `SnapshotWarpSHA` + `CommitWeft` at `:38,:486`), `weftgit_pathspec_integration_test.go` (`CommitWeft` incl. `:(exclude)` cases `:167,:212`), `pull_integration_test.go` (`CommitWeft` `:50`) are internal-package; `weftgit_exclude_test.go` is EXTERNAL `package fabricengine_test` (`CommitWeft` at `:96,:184`). Fixes: (a) added `snapshot_integration_test.go` to card 19's Edits and broadened its `SnapshotWarpSHA` update to a package-wide grep covering both files; (b) added new card 30 (batch 6) migrating the external `weftgit_exclude_test.go` onto `Fabric.Commit` (grep-and-rename cannot fix an external-package unexported-method reference), and enumerating the in-package files card 26's grep sweep covers so none are "absent from the plan"; (c) added `weftgit_pathspec_integration_test.go` to card 14's Edits (see next finding overlap); (d) added the newly-declared test files to All Files Touched.
- **[BLOCKING] New `lyx fabric diff` verb indexes args[0] with no length guard** (`20260802-122953-plan-review-r3.md`) — Confirmed sibling fabric verbs guard `len(args)` and `cmd/lyx` has no `recover()`. Added `Args: cobra.ExactArgs(1)` to card 22's requirements so zero-arg invocation returns the JSON error envelope instead of panicking.
- **[NIT] weftPathspecFilter's `:(exclude)`-magic passthrough left stale** (`20260802-122953-plan-review-r3.md`) — Added a requirement to card 26 to correct `weftPathspecFilter`'s doc comment (stale producer list naming buildercli/webstercli/perchcli) and note the passthrough is now unreachable by any production caller, while leaving gitrepo's general `:(exclude)` handling in place as a deliberately-kept general-layer capability.

### Additional coupling fixed (surfaced while verifying BLOCKING 2)

The `:(exclude)`-magic cases in `weftgit_pathspec_integration_test.go` (`:167,:212`) exercise `CommitWeft` passing `:(exclude)` through to `git add` and depend on the `-f` that batch 3 removes — so they would break at batch 3's runtime, not just batch 6's compile. Added a card-14 requirement to reconcile those cases (delete/rewrite the force-add-dependent `:(exclude)` passthrough cases, keep the positive-pathspec cases) and expanded batch 3's `verify:` to include `./internal/fabricengine/` so any such break surfaces in the batch that causes it rather than as a spurious failure in batch 4.

## Pushed Back

(none)
