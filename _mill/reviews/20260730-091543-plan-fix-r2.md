# Plan-fix report — round 2

Source review: `_mill/reviews/20260730-091422-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NIT). All findings verified accurate; all fixed; none pushed back.

## Fixed

- **[BLOCKING] "Card 3 uses goquery before go.mod declares it"** — Card 3 (`htmltext.go`) is actually the first card to import an external dep (goquery), not Card 6. Card 3 now runs `go get github.com/PuerkitoBio/goquery` and carries `go.mod`/`go.sum` in its `Edits:`. Card 6's `go get` line reduced to `go-readability` only (goquery is already present from Card 3, and go-readability pulls it transitively). Every intermediate commit is now buildable.
- **[NIT] "fetch_test.go coverage gap on double-fallback-failure"** — Card 6's `fetch_test.go` scenario list now includes the two browser-also-returns-empty cases: `<100`-char readability + empty browser → returns the short readability text (weblens lines 247–252); no-extraction + empty browser → returns the final `Could not extract readable content` message.
- **[NIT] "Lock-age staleness path has no test or documented manual check"** — Card 9's `selftest.sh` now adds an automated staleness case: create a `bin/.build.lock` with no binary, backdate its mtime past the ~120s threshold (`touch -d '200 seconds ago'`, with a logged skip if the platform's `touch` lacks `-d`), then assert `run.sh` reclaims the orphaned lock and rebuilds promptly instead of waiting out the full deadline.

## Pushed Back

None.
