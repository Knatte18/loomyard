MILL_REVIEW_BEGIN
# Review: burlerengine + perchengine told-geometry — holistic

```yaml
duration_s: 377.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-18
```

## Findings

None. This is a round-3 fresh review (no prior review files consulted), and the plan is grounded correctly throughout.

Verification performed against live source, not just plan prose:

- `hubgeom.go`/`hubgeom_test.go`/`doc.go`: `ReedGeometry`'s exact shape, doc-comment wording, and the three-distinct-directories fixture all match what cards 3–5 describe adding/rewriting.
- `burlerengine/engine.go`: the exact `e.layout.WorktreePath()`/`e.layout.AnchorPath()` (×2) call sites card 6 names are the only ones present; `lyxcwd` has no other use in the file, confirming the import removal is safe.
- `engine_test.go`: exactly 8 `New(...)` call sites and exactly one stray `e.layout.AnchorPath()` outside them (line 538) — both counts in card 8 check out.
- `perchengine/engine.go`/`identity.go`: `New`'s param position, the single `GateDir: e.layout.WorktreePath()` use, and `RunsDir`/`ScratchDir`'s exact current signatures all match cards 12–13.
- `run_test.go`: exactly 31 `newTestLayout(t)` sites and exactly 39 `New(...)` sites, matching card 15's counts precisely.
- `cmd/lyx/constructoranchoring_test.go` and `notransients_test.go`: repo-wide grep confirms these are the only `perchengine.RunsDir`/`ScratchDir` call sites outside the packages already covered by earlier cards, and the line/expression counts card 20/21 give (4 rows, 7 expressions across 6 lines) match exactly.
- `00-overview.md`'s `## All Files Touched` is the exact union of every card's `Edits:`/`Creates:` across all three batches (22 files) — verified by independent enumeration.
- Batch DAG (1→2→3, linear, no cycle), all three `file:` paths exist, global step numbering 1–21 has no gaps, every card's `Moves: none` is correctly bare (no rename mechanic needed), and integration-tagged test edits (card 19) are covered by batch 3's `-tags integration` verify clause.
- Repo-wide grep for `burlerengine.New(`/`perchengine.New(`/`perchengine.RunsDir(`/`ScratchDir(` turns up no caller outside the files this plan already edits — the "no other caller" claims in several cards' rationale hold.

No constraint violations, no vague `Requirements:`, no missing `Context:` entries for anything genuinely load-bearing, no over-engineering.

## Verdict

APPROVE
Plan is accurate against source on every checked claim; no blocking or nit findings.
MILL_REVIEW_END
