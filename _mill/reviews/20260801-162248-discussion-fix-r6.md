# Discussion fix — round 6

Source review: `_mill/reviews/20260801-162202-discussion-review-r6.md` (verdict: APPROVE, 2 NOTEs).

## Fixed

- **[NOTE] Empty correspondence index not addressed** — added an explicit empty-index case to `rebase-detection-scope`: zero recorded `Warp-SHA` entries means nothing to check for drift, so `Fabric.Pull` treats it as no-drift and performs the plain fast-forward/advance, distinct from the no-surviving-anchor abort case (which requires an actually-recorded-then-orphaned history).
- **[NOTE] Reachability primitive vs. divergence classifier naming** — named the general `IsAncestor(sha, ref string) (bool, error)` primitive explicitly in `warp-refresh-primitives`, serving both the divergence classifier and the anchor-walk's per-entry reachability checks as one method; added it to the `gitrepo Client Boundary Invariant` Constraints bullet's pinned-list obligation.

## Pushed Back

None.
