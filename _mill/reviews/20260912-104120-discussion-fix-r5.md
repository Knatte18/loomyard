# Discussion fix — round 5

Source review: `_mill/reviews/20260912-103846-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 4 NIT).

## Fixed

- `[NIT:consistency]` "Webster carve-out contradicts 'skill names no producer'" — took the reviewer's second option rather than the first: the discrimination moves into the envelope as a Go-supplied `next_interrupt_policy` (`"reinvoke"` / `"handback"`), sourced from a new exported table in `internal/loomshed` keyed by the existing `Name*` constants and pinned by a meta-test against `internal/loomrecipe`'s `loomRowEngines` mapping. The skill branches on the policy word and never learns a row name, so Scope § Out's rule now holds without a carve-out.
- `[NIT:scope]` "drive.go's shed-construction extraction is outside the scope inventory" — added the `drive` shed-construction extraction to Scope § In with an explicit behaviour-preserving-for-`drive` guarantee, named the covering tests (`drive`'s existing suite plus one direct unit test on the helper), and widened Scope § Out's behaviour-preservation bullet to name `drive` alongside `run`.
- `[NIT:design]` "Step's pre-lock preamble is unstated" — pinned that `Step` runs the same `validate()` plus both `MkdirAll` calls before acquiring, with the reason (`internal/lock` opens `O_CREATE` without creating parents, and `Step` is exported for every shed so it cannot assume a caller made the directory). Added the two matching assertions to the shedengine test list.
- `[NIT:design]` "Ordering of the `busy` refusal against bootstrap is undecided" — decided: an early non-blocking run-lock probe at the top of `step`, before the bootstrap, reusing the probe `run` already performs at its own step 5; refuses with `kind: "busy"`. Recorded that `shedengine.Step`'s own acquisition remains the authority and that a driver starting in the probe-to-acquire window is benign, so the double check reads as deliberate. The motivating cost is named: strand churn against a live session is a real side effect, not merely a wasted invocation.

## Pushed Back

None.
