MILL_REVIEW_BEGIN
# Review: Replace reed's header pane with a status-line and Selvage — holistic

```yaml
duration_s: 428.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-09-18
```

## Findings

### [BLOCKING:scope] batch 6 misses an existing reedUp call site that will fail to compile
**Location:** batch 06-standalone-watcher, cards 41-43
**Issue:** `internal/webstercli/cli_test.go:620`, in `TestRecoverBatchCmd_BootsStandaloneReedSessionFirst`, does `c.reedUp = func() error { bringUps++; return errors.New(...) }`. Card 41 widens the `reedUp` field to `func(ctx context.Context, watch bool) error`, but no card in batch 6 (card 43 only touches `wiring_test.go` in each package) edits `internal/webstercli/cli_test.go`, so this assignment stops compiling. `internal/burlercli` has no equivalent stray assignment (verified via grep — only `wiring.go`/`wiring_test.go` reference `c.reedUp` there), so this is specific to webstercli.
**Fix:** Add `internal/webstercli/cli_test.go` to card 42 or 43's `Edits:` and retarget this test's closure to the new signature (and pass `ctx`/`watch` through its assertion of exactly one bring-up call).

### [NIT:consistency] geometry.go's leading comment keeps a stale file reference after the rename
**Location:** batch 02-vocabulary-and-config, card 8 (interacts with card 12)
**Issue:** `internal/reedengine/geometry.go`'s leading comment reads "New and every method stay in their existing files (lock.go, lifecycle.go, strand.go, header.go)". Card 8 only corrects the "eight-field"/`RepoName`/`HubPath` phrasing in this file; card 12 (later in the same batch) renames `header.go` to `statusline.go`. No card updates the file-list sentence, so it keeps naming a file that no longer exists after batch 2.
**Fix:** Add one clause to card 8 (or a follow-up line in card 12) updating "header.go" to "statusline.go" in geometry.go's leading comment.

### [NIT:consistency] batch 04's own scope text undercounts the set-option calls it specifies
**Location:** batch 04-status-line-pins, Batch Scope paragraph vs. cards 24/26
**Issue:** The batch's opening paragraph says "all six set-option calls live in one function", but card 24's own list (status on, status-position, status-left, status-right, status-left-length, window-status-format, window-status-current-format) is seven calls, and card 26's requirement text explicitly says "asserts the seven options are issued" — the executable cards agree with each other on seven but contradict the batch's own summary of "six".
**Fix:** Change "six" to "seven" in the Batch Scope paragraph (or state explicitly that the window-status segment is counted as one conceptual pin implemented as two calls).

## Verdict

REQUEST_CHANGES
One concrete compile-breaking scope gap in batch 6; two minor self-consistency nits elsewhere in the plan text.
MILL_REVIEW_END
