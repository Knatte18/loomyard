MILL_REVIEW_BEGIN
# Review: burler: split the round prompt into an orchestrator + three instruction files — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-29
```

## Findings

### [NIT] docs/shared-libs/stencil.md's burler consumer entry is now stale
**Location:** `docs/shared-libs/stencil.md:50`
**Issue:** States burler's prompt is "the pre-assembled bulk blob... passed as a value, not read via tools" — true pre-refactor, but now only the orchestrator is passed as a value; the three instruction files are written to disk and read by the agent via tools.
**Fix:** Update the sentence to note the orchestrator is passed as a value while the three instruction files are read via tools per the new materialization step.

### [NIT] Per-round instruction dirs under `.lyx/burler` are never cleaned up
**Location:** `internal/burlerengine/engine.go:120`
**Issue:** `os.MkdirTemp(burlerDir, "round-")` mints a fresh directory every `Run` call with no corresponding removal, so `.lyx/burler/round-*` accumulates indefinitely across rounds in a long-lived worktree.
**Fix:** Consider having the loop owner (or a future Run cleanup step) prune stale round directories, or note the accumulation as accepted machine-local litter in doc.go.

## Verdict

APPROVE
Implementation matches the plan's nine cards, shared decisions, and CONSTRAINTS updates precisely; only trivial doc drift found.
MILL_REVIEW_END
