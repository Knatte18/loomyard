# Plan fix — round 1

Source review: `_mill/reviews/20260707-112724-plan-review-r1.md` (verdict APPROVE, 2 NITs).

## Fixed

- `20260707-112724-plan-review-r1.md` — **"[NIT] Empty-value guard only covers top-level markers"** — added a godoc requirement to Card 1 pinning the caller contract that follows from top-level-only empty-checking: required markers (`fasit`/`target`) must live at the template top level, never inside a conditional branch, since a branch-internal present-but-empty value would render a silent blank. This makes the deliberately-pinned scope's boundary explicit rather than implicit; the design already guarantees fasit/target are top-level.
- `20260707-112724-plan-review-r1.md` — **"[NIT] Nil-Tree guard could also cover Root"** — updated Card 1 step 3 to defensively nil-check both `tmpl.Tree` and `tmpl.Tree.Root` before ranging `.Nodes`, skipping the walk cleanly for an empty/comment-only parse.

## Pushed Back

_None. Both NITs were accurate and low-cost; fixed under the default rule._
