MILL_REVIEW_BEGIN
# Review: fabric: audit and migrate all remaining direct git mutations onto Fabric — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-4.5 (Sonnet 5, per system context)
reviewed_file: plan/
date: 2026-08-01
```

## Findings

### [BLOCKING] Card 5's Context omits runlevel.go, where RunDeps is defined
**Location:** batch 02-webster-bisect-migrate, Card 5
**Issue:** Requirements direct the implementer to add `Bisector: gitrepo.New(worktree)` to the `websterengine.RunDeps{...}` literal in `runlevel_test.go`, but `RunDeps` (and its new `Bisector` field, added by Card 4) is defined in `internal/websterengine/runlevel.go`, which is listed in neither `Context:` nor `Edits:` for Card 5. Batch 3's structurally identical Card 9 gets this right — it lists both `chain.go` (WarpResetter) and `spawn.go` (SpawnDeps) in Context — making the Card 5 omission an inconsistency, not a stylistic choice.
**Fix:** Add `internal/websterengine/runlevel.go` to Card 5's `Context:` list.

### [NIT] Cards reference Shared-Decision names not declared in 00-overview.md
**Location:** batch 01-fabric-warp-methods Card 2 ("per the `new-fabric-method-tests` decision"); batch 04-regression-guard-and-constraints Card 10 ("per the `regression-guard` decision")
**Issue:** `00-overview.md`'s `## Shared Decisions` section only declares four decisions (verb-only Fabric method names, consumer-side interface seam, nil-defaulted seam field, Go test tiering and verify commands) — neither `new-fabric-method-tests` nor `regression-guard` exists among them, so these are dangling references to undocumented decisions.
**Fix:** Either add the two missing `### Decision:` entries to `00-overview.md`, or drop the parenthetical decision-name references from the two cards (the requirement text is otherwise self-contained).

## Verdict

REQUEST_CHANGES
Card 5's Context gap (RunDeps struct source) breaks Context Completeness relative to Card 9's own precedent in the same plan.
MILL_REVIEW_END
