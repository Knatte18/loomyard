# Discussion fix — round 1

Source review: `_mill/reviews/20260906-121226-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 4 NIT, 2 demoted from BLOCKING).
Auto mode: every finding is FIX by rule;
PUSH BACK is unavailable with no operator present.

## Fixed

- `20260906-121226-discussion-review-r1.md` — "[NIT:consistency] Session slice contradicts measured loomcli usage" (demoted from BLOCKING).
  Verified accurate: `grep -ohE 'c\.reed\.[A-Za-z]+\(' $(ls internal/loomcli/*.go | grep -v _test) | sort -u` returns exactly `AddStrand`, `AttachArgv`, `RemoveStrand`, `Status`, `TmuxPath`, `Up` — six methods, and none of `Down`, `Resume`, `SessionName`, `Socket`.
  The `reed-publishes-named-slices-consumers-may-still-narrow` decision now states the session slice as those six, names the producing command, states that the two slices deliberately overlap on `AddStrand`/`RemoveStrand`/`Status` and that overlap is permitted because a slice describes one consumer's dependency rather than a partition of the type, states that both slices are declared in the provider package, and explicitly forbids the wider four-method claim.
  The Q&A-log echo of the same decision was corrected to match.

- `20260906-121226-discussion-review-r1.md` — "[NIT:consistency] CleanClaudeEnv is not an external identifier" (demoted from BLOCKING).
  Verified accurate: the only non-test mention outside `reedengine` is `internal/burlerengine/doc.go:205`, a doc comment.
  Two further identifiers in the original list have the same defect — `AddStrand` (`internal/reedcli/add.go:1,4`) and `requireSessionLocked` (`internal/reedcli/attach.go:52`, and not exported at all) — so the count is 13, not 14.
  The Technical-context bullet now states 13, defines the metric as "referenced in code by production packages outside the package", names all three comment-only false positives with their file:line, and records comment-prose contamination as a shared caveat on every grep-derived number in the doc — the same trap that produced the false `lyxcwd`→`fabricengine` edge already recorded.
  A follow-on bullet keeps the `CleanClaudeEnv` rename recommendation and notes that having no external caller strengthens rather than weakens it.
  The headline in the `reed-verdict-defer-and-gate-on-a-second-consumer` decision was updated from 15 to 13.

- `20260906-121226-discussion-review-r1.md` — "[NIT:consistency] Engine method count is 17, not 18".
  Verified accurate: `go doc ./internal/reedengine Engine | grep -c '^func (e \*Engine)'` returns 17.
  Corrected in both places it appeared, with all 17 method names enumerated and the producing command named per the doc's own reproducibility rule.

- `20260906-121226-discussion-review-r1.md` — "[NIT:consistency] Link test does not validate GitHub-issue links".
  Verified accurate: `internal/lyxcwd/docslink_test.go:363` skips every `http://`, `https://` and `mailto:` target, with a dedicated subtest asserting the skip.
  The Testing section's link-integrity bullet now scopes `TestEnforcement_MarkdownLinks` to repo-relative links only and states that external URLs are a manual-review item verified by reading.

## Pushed Back

None.
