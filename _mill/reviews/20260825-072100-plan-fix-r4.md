# Fixer report: plan-fix round 4

Source review: `_mill/reviews/20260825-071758-plan-review-r4.md`

## Fixed

- `20260825-071758-plan-review-r4.md` — "[BLOCKING:design] Interstitial fixture has no marker-discovery fallback" — verified against `_mill/discussion.md`'s Gotchas: the interstitial's challenge text appears only after JavaScript runs, so none of the eight seeded markers can be assumed present in the captured static bytes. Card 2 now requires the interstitial marker to be discovered from the fixture, the same way the login-wall marker already was, names concrete candidates (the auto-submit form's `action` value or a hidden field name), requires both discovered markers to be absent from all three negative cases, and requires a halt-and-report rather than a weakened marker if nothing distinctive exists.
- `20260825-071758-plan-review-r4.md` — "[NIT:consistency] redditListing/redditChild field casing diverges from redditThing's" — verified; read literally, unexported fields decode to zero values silently. Card 5 now states all three types' fields as exported Go fields with explicit lowercase `json:"..."` tags, citing `hackerNewsItem` as the in-module pattern, and spells out `redditThing`'s tags too.
- `20260825-071758-plan-review-r4.md` — "[NIT:consistency] fetcher.go's struct doc comment not updated for doNoRedirect" — verified against `plugins/prowler/fetcher.go:12-14`, which says "the two side-effecting operations" and "Both fields must be set" while three fields already exist. Card 7 now requires updating that doc comment in the same pass, and the card correctly says "fourth field" rather than "third".
- `20260825-071758-plan-review-r4.md` — "[NIT:consistency] Card 7's Context includes browser.go with no evident use" — verified; nothing in card 7 touches `browser.go`. Replaced it with `plugins/prowler/fetch.go`, which the card does need (the new field parallels `f.do`'s existing call sites there), and stated that use in Requirements.

## Pushed Back

_(none)_
