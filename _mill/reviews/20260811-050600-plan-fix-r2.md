# Plan fix — round 2

Source review: `_mill/reviews/20260811-050025-plan-review-r2.md` (APPROVE, 0 BLOCKING, 4 NITs — one carrying `**Demoted-from:** BLOCKING`).

## Fixed

- r2 — "[NIT:consistency] Card 10 inserts into shed.md beyond the batch's own \"token-only\" scope claim" (demoted from BLOCKING) — verified accurate: batch 2's ownership-crossing decision claimed "only the literal token is replaced" while card 10 also inserts `Discussion-Validate` at `shed.md:13` and `:41`.
  Took the reviewer's first option (narrow the wording) rather than its second (drop the insertion): dropping it would contradict `_mill/discussion.md`'s Scope "In", which assigns both insertions to this task explicitly, and would ship the self-contradicting interim state `shed-followups.md:423–424` exists to prevent.
  The ownership-crossing decision now excepts the two insertions by name, and a new batch-local decision states them with their rationale.
- r2 — "[NIT:scope] Card 9 edits loom.md:75, outside task C's stated \"rows 2-7\" grant" — verified accurate: the crossing was real and undocumented in the batch file, unlike the `Batchifier`-cell and `## The gate` exceptions.
  Added a batch-local decision recording it as deliberate and self-consistency-driven (card 8's own insertion falsifies the line), citing task A's override precedent at `shed-followups.md:449–453` and noting that `:75`'s second open question stays task E's.
- r2 — "[NIT:scope] Card 2's discussion-format.md \"Plan producer\" site list omits line 56" — verified against the live file: `:56` reads "The Plan producer explores the codebase itself...".
  Added `:56` to card 2's enumerated site list.
- r2 — "[NIT:consistency] Card 12 misattributes the \"Override recorded\" convention to shed-followups.md:289" — verified: `:289` reads `**Instruction repaired 2026-08-09 (task A, as landed).**`, not `**Override recorded ...**`;
  the other five citations are correct.
  Dropped `:289` from the precedent list and added a one-line note that it is the distinct "Instruction repaired" convention (also at `:92`), flagging that `_mill/discussion.md`'s Decision `discussion-review-gate-name-disposition` lists it in error.

## Pushed Back

_None._
All four findings verified accurate against the live tree.
The one case where the reviewer offered two remedies was resolved toward the option that does not contradict a documented Decision, rather than by declining the finding.
