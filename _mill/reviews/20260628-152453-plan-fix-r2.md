# Plan-fix report — round 2

Source review: `_mill/reviews/20260628-152343-plan-review-r2.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 4 NITs).

## Fixed

- r2 "[BLOCKING] Benchmark still uses removed `id_or_slug`, never converted" — added
  `internal/board/boardtest/bench_test.go` to Card 2 Edits (and overview All Files
  Touched) with the conversion of `BenchmarkGet`'s payload to `{"slug":"task-0"}`. Also
  broadened batch 1 verify to `go test ./internal/board/... ./cmd/lyx/` so the `boardtest`
  subpackage at least compiles under the batch gate.
- r2 "[NIT] `main_test.go` keeps `id_or_slug` payload" — added `cmd/lyx/main_test.go` to
  Card 2 Edits (and overview) with conversion of the `remove '{"id_or_slug":"nope"}'` case
  to `{"slug":"nope"}` so it exercises the genuine not-found path.
- r2 "[NIT] Card 8 commit leaves an existing render test red" — added an atomicity note to
  batch 3 Batch Scope stating intermediate per-card red WITHIN the batch is accepted (the
  batch `verify:` runs after all cards, not per-commit).
- r2 "[NIT] roadmap/overview confirm dropped silently" — added an explicit docs-check note
  to Card 1: grep of `docs/` finds no `set-phase`/per-command references and the rename
  does not change the roadmap module table or overview execution stack, so no doc edit is
  required; the `--help` `Long` blocks (batch 2) are the doc surface.
- r2 "[NIT] Card 1 cites `TestMergeTasks` as a `SetPhase` call site" — corrected the Card 1
  example to name only the real `s.SetPhase` call sites (`TestSetPhase`/`TestSetPhaseNil`/
  `TestSetPhaseMissing`), dropping `TestMergeTasks`.

## Pushed Back

- None.
