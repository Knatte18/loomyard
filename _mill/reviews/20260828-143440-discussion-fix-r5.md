# Discussion fix — round 5

Source review: `_mill/reviews/20260828-143233-discussion-review-r5.md`

## Fixed

- `[BLOCKING:design]` "Adoption-prose inventory has no sweep method" — correct, and the demonstration is decisive: the hand-list already missed `reconcile.go:127` and `doc.go:279`.
  Running the sweep confirmed it missed more than the finding named — `doc.go:39` (adoption listed as one of the header's three exclusion seams), `lifecycle.go:230` (*"no pane to adopt or split"*), and the `state.go:45` hit.
  Replaced the fixed list with a stated sweep (`grep -rn "adopt" internal/reedengine/*.go internal/reedcli/*.go`) plus a two-branch disposition rule: rewrite anything describing the `planPaneTarget` adoption seam, leave anything where "adopt" means the server-rebirth generation probe (`adoptPaneGenerationLocked` and its prose in `generation.go`/`state.go`/`server.go`) or `reedcli/up.go`'s config-key wording.
  The known hits are kept only as an explicit cross-check on the sweep, not as its substitute, and the reason the half-updated state is the actual hazard is now stated: a comment left saying `planPaneTarget` "never adopts the header" is worse than none, because it implies adoption still exists and merely excludes the header.
- `[NIT:consistency]` "Smoke pid assertions contradict kill-pane-only" — correct, and a self-contradiction I introduced: the round-1 Testing section asserted a pid is dead immediately after the verb returns, while the `untracked-reap-stays-kill-pane-only` Decision deliberately declines the `descendantClosurePIDs`/`reapPaneChildren` wait, and reed's own comments state pane subtrees die asynchronously.
  Both regressions now specify `#{pane_pid}` only, under a bounded poll rather than a single sample, with descendant liveness explicitly not asserted and `RemoveStrand`/`Down` named as where subtree death is guaranteed instead.
  Also recorded that M22's launched command is a *child* of the pane process, so the leak that test pins is the pane and its own process, not the subtree — and that the `#{pane_pid}` check still does the job M16 needs it for, since under adoption the pane pid provably survives.
- `[NIT:scope]` "RemoveStrand's tail behaviour changes while listed Out" — correct: one rule for every call site means Remove's tail reconcile newly reaps untracked alive panes once the last strand's binding is gone.
  Rewrote the Out entry to call `RemoveStrand` a deliberate exception to "Out means unaffected", stating the inherited behaviour change, that it is the intended consequence rather than an oversight, and that mill-plan must not read Out as licence to special-case Remove back to the old gate.

## Pushed Back

_None._
