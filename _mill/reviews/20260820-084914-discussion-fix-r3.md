# Discussion fix — round 3

Source review: `_mill/reviews/20260820-084807-discussion-review-r3.md` (APPROVE, 0 BLOCKING, 5 NIT).

## Fixed

- `20260820-084807-discussion-review-r3.md` — "wiring_test.go's pinned type string is undisposed": replaced the open "check whether it names the row-1 constructor" note with the actual disposition — `wiring_test.go:118`'s `%T` literal `"*loomshed.preflightProducer"` becomes `"*preflightshed.preflightProducer"`, plus the two comments at `:99` and `:113-117`.
- `20260820-084807-discussion-review-r3.md` — "Closed grep covers only one of the two moved symbols": extended the closed-grep statement to `NewPreflightProducer` as well, and added its one production-comment site, `internal/loomcli/cli.go:32-33`, to the change list.
- `20260820-084807-discussion-review-r3.md` — "Verify command contradicts itself": updated the Q&A answer to the three-invocation form, with the reason the smoke-tag check exists.
- `20260820-084807-discussion-review-r3.md` — "CheckSeedMissing's stale doc clause is not disposed of": named the removal of `report.go:48-49`'s "and fabric is otherwise ready and healthy" clause alongside the `CheckSeedUnreadable` narrowing, under the same Documentation-Lifecycle argument.
- `20260820-084807-discussion-review-r3.md` — "drop-check3blockseed rationale is not true on resume": rewrote the rationale as two explicit halves — the within-one-`Run` non-advance (`run.go:187-200`) and the across-a-resume case, where row 1 is never re-run (`run.go:101-111`) and step 1's read gate (`run.go:77-83`) is what actually covers it. Reworded the matching regression scenario to require both paths be tested, since a fresh-run-only test would pass while the resume path regressed.

## Pushed Back

None.
Every finding verified accurate against the cited code — including the resume-path one, which corrected a rationale that reached a true conclusion by an argument that does not hold across `Run` calls.
None of the four pushback grounds applied.
