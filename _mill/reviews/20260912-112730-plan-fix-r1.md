# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260912-112300-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 3 NITs).

## Fixed

- r1 BLOCKING:design — "Card 4's config test rests on a false premise about strict Load".
  Verified against `internal/configengine/config.go`'s shared `load` body: the strict path calls `yamlengine.MissingKeys(template, fileBytes)` and returns `missing keys: …; run "lyx config reconcile"` whenever anything is missing, never falling through to the template's value;
  only `LoadOrTemplate`'s absent-file path reaches `fallbackToTemplate`, and loom does not use it.
  Card 4's rationale now states that precisely, records the real migration consequence (every already-reconciled `loom.yaml` on disk misses the new key and errors until `lyx config reconcile --apply` writes it in), and the mandated test cases are rewritten: a full-`ConfigTemplate()` seed exercises the true default, a full-template seed with `selfreport: false` substituted exercises the override, and the plan now explicitly forbids the omitted-key-loads-true assertion.
  Added `internal/yamlengine/reconcile.go` to the card's `Context:` for the `MissingKeys` reference.

- r1 NIT:scope — "Card 3 undercounts rows needed in the notransients guard".
  Confirmed `transientSet` in `cmd/lyx/notransients_test.go` is a flat table the guard walks element by element, one row per accessor.
  Card 3 now says both accessors get their own row, two rows not one, and states why an unlisted accessor is simply never checked.

- r1 NIT:scope — "Card 5's Context omits the file declaring the outcome constants it references".
  Confirmed `type Outcome` and `Done Outcome = "done"` are declared in `internal/shedengine/producer.go`, not in `status.go` or `run.go`.
  Added that file to Card 5's `Context:` and named `shedengine.Done` explicitly in the requirement, with a note that `run.go` uses it unqualified in-package.

- r1 NIT:consistency — "Card 9's roadmap move doesn't address the Done section's tense convention".
  Confirmed every Done entry is phrased in shipped tense.
  Card 9 now directs rewriting the moved entry's prose into shipped tense, and additionally drops its two now-spent parallel-build independence clauses.

## Pushed Back

_(none — all four findings verified accurate against the cited code and were fixed)_
