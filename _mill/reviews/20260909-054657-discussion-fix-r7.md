# Discussion fix — round 7

Source review: `20260909-054625-discussion-review-r7.md` (opusmedium, APPROVE, 3 NITs, 1 demoted from BLOCKING).

## Fixed

- `[NIT:consistency] Enum-sync test stated two ways: count vs set` — Testing's meta-test line and the r3 Q&A entry both restated as set equality (with an explicit supersession note on the Q&A entry), so the weaker cardinality form can no longer be lifted into the plan.
- `[NIT:design] Enum↔slice comparison bridge left unstated` — the decision now names the bridge in one clause: const-block identifiers are valued by `iota` position, then compared via `refKindName` renderings; no third hand-maintained name table.
- `[NIT:consistency] isHandleRef described as having callers to migrate` — parenthetical dropped; `isHandleRef` is deleted as dead (test-only callers), and `IsHandleRef` is documented as new API for planglyph, not a rename.

## Pushed Back

(none)
