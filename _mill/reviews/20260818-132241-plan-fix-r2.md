# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260818-132241-plan-review-r2.md`

## Fixed

- `20260818-132241-plan-review-r2.md` — "[NIT:design] Card 11's no-build-tag rationale is factually wrong".
  Verified accurate: `cmd/lyx/hermeticenv_test.go` reads every `*_test.go` file's raw content and matches substrings against it, with no build-tag filtering anywhere, so a `//go:build integration`-tagged `testmain_test.go` would satisfy its presence check identically.
  The card's instruction (leave the file untagged) was already correct; only its stated mechanism was false.
  Replaced the rationale with two true ones — the `internal/perchcli/testmain_test.go` precedent, which is itself untagged despite covering an integration-tagged fixture, and the fact that `TestMain` is the single entry point of the package's whole test binary, so an untagged one covers the existing untagged `cli_test.go` too rather than the integration binary alone — and added an explicit sentence recording that the guard is tag-insensitive, so the corrected fact is written down rather than merely omitted.

## Pushed Back

_(none — the finding was accurate and the fix carries no harm.)_
