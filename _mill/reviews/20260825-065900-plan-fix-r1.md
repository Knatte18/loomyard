# Fixer report: plan-fix round 1

Source review: `20260825-065443-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 2 NIT).

## Fixed

- `[BLOCKING:scope]` "shape_test.go's TestNew_ProducerTable breaks on the new Segment/MaxBounces" — verified against `internal/loomrecipe/shape_test.go:141-146`: `TestNew_ProducerTable` does carry its own unconditional `Segment == ""` / `MaxBounces == 0` assertions, each with an error message asserting no row gains a non-empty or non-zero value.
  Card 18 now instructs converting both to per-row comparisons against `want.segment`/`want.maxBounces` and rewriting the two obsolete error messages, replacing the previous "leave `TestNew_ProducerTable` alone" instruction, which was correct only about its length check.
- `[BLOCKING:design]` "Card 11 cites a template/struct agreement mechanism that doesn't exist" — verified: `internal/loomengine/config_test.go` carries only per-field literal round-trips (`TestLoadConfig_WellFormed` and the two malformed-spec tests), with no key-set agreement check.
  `internal/websterengine/config_test.go`'s `TestConfigTemplate_ContainsEveryConfigYAMLTag` is the repo's actual precedent.
  Card 11 now says outright that this is a new test rather than an existing mechanism to reuse, names that precedent to copy, adds its file to the card's `Context:`, and states why the check earns its place (the Config Strictness Invariant makes a struct field with no template key a silent hole rather than a load error).
- `[NIT:consistency]` "stub.go's stale row-count prose left untouched" — verified: `internal/loomshed/stub.go` says "13-row producer list" in both its file header and `stubProducer`'s doc comment, and "backs three rows" in the latter.
  Card 14 previously named only the stubbed-row list.
  It now enumerates all three stale claims and their corrected values: both "13-row" occurrences to "14-row", "three rows" to "two rows", and `Discussion-Review` dropped from the list.
- `[NIT:consistency]` "Card 24's rationale mischaracterizes an already-stale figure" — verified against `internal/loomrecipe/coverage_guard_test.go`'s `loomRowEngines`: exactly three rows map to `Stub` today, so "five" was already wrong before this task.
  Card 24 now says only "thirteen" is a regression this task introduces, records that "five" was pre-existing, states that both are fixed regardless because the corrected end state is identical either way, and adds `internal/loomrecipe/coverage_guard_test.go` to its `Context:` so the implementer can confirm the count rather than take the card's word for it.

## Pushed Back

_(none — all four findings were factually accurate, and no fix broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task's scope.)_
