# Plan fix — round 3

Source review: `_mill/reviews/20260919-135433-plan-review-r3.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 4 NIT, two of the NITs demoted from BLOCKING by the stage ceiling).

Every finding was verified against the real source before acting;
all seven were factually accurate and none triggered a HARM CHECK exception.

## Fixed

- r3 holistic, "Generic verb bodies never told to call clihelp.SetExit" — verified in `internal/clihelp/exec.go`: `RunRootCtx` returns `es.code`, and `es.code` is written only by `SetExit` and `Abort`, while `output.Err`/`ErrFields`/`Ok` merely return 1 or 0. A literal implementation of the plan as written would have exited 0 on every refusal, silently breaking every caller that branches on the exit code, `ly-drive` included. Added it as a batch-3 scope decision with the mechanism spelled out, restated in cards 11, 12, 13 and 15, extended to `internal/shedcli`'s own pre-run in card 30, and made card 20 grep for an unwrapped `output.*` call — a gap no envelope-content test would catch.
- r3 holistic, "shedverbs helpers are unexported, yet loomcli tests are retargeted onto them" (NIT, demoted from BLOCKING) — a contradiction introduced by round 2's own fix. Resolved by exporting the four helpers the arming modules' tables reach: `StepEnvelope`, `RenderStatusLine`, `UnavailableLine` and `PrintStatusLinesOnChange`, declared as such in cards 12 and 14 with a stated reason (loom's ten-key envelope and its exact rendered watch line are loom's own shipped contract). Cards 22 and 23 now retarget onto those names with the delete-the-duplicate fallback removed, which also resolves the contradiction with card 28's requirement that `status_test.go` keep pinning the rendered line for the `loom` label.
- r3 holistic, "Card 22 edits internal/loomcli/cli.go without listing it" — added `internal/loomcli/cli.go` to card 22's `Edits:`; the `parentFlag` field and the entry-observation carrier both live on the struct in that file.
- r3 holistic, "Byte-for-byte help text has neither a live source nor the claimed guard" (NIT, demoted from BLOCKING) — verified `longlist_test.go` only asserts `root.Long` names each registered top-level module and `jsonhelp_test.go` exercises root, `board`, `config`, `ide`, `reed` and `selfreport`, so neither can fail on a reflowed `loom` or `lifecycle` `Long`. Moved the `loomVerbTexts` declaration into card 21 and `lifecycleVerbTexts` into card 25, both of which edit their module's `cli.go` and run before the constructors are deleted, so the copy is lifted from live source rather than from git history. Card 24's false guard claim replaced with an accurate statement of what is and is not machine-checked (`drift_test.go`'s blank-`Short` check, and nothing else), and card 32's matching claim corrected.
- r3 holistic, "Card 31's run/step parity cases have no stated fixture arm or substrate bound" — verified `run` reaches `c.reed.Up()` and `step`'s bootstrap reaches `ensureStatusStrand`, which calls it too, and that every existing loomcli test touching that substrate is `//go:build smoke`. Card 31 now pins the arm per verb: `run` against a hub with no status file (refuses at the first `PreRun` statement, above `reed.Up()`), `step` against a held run lock (refuses at the early probe, above the bootstrap), `status` and `pause` against a seeded file (read-only, lightweight, never reach the substrate), and `lyx lifecycle run` against a `StateDone` slug (refuses in `PreRun` before `BuildShed`). Added the explicit bound — no parity case may reach reed, tmux, an LLM producer, or the engine's own producer call — with the reason it still proves what it needs to: the divergence risk between the two paths lives entirely in arming and pre-run resolution, which every one of those arms exercises in full. Card 34 now confirms the bound held.
- r3 holistic, "Card 6's 'no string in this package names loom' exceeds its own Edits" (NIT) — verified `internal/lifecycleshed/doc.go` and `teardown.go` also name the loom session. Narrowed the clause to the moved file and `InnerRunDeps`, with both out-of-scope files named and the reason stated: the discussion scopes this neutralization to the inner-run producer, and `teardown.go` describes what the shipped lifecycle recipe actually tears down.
- r3 holistic, "Card 39's grep is narrower than the rule it claims to prove" (NIT) — the confirmation now greps for the `lyx loom ` prefix rather than the single `lyx loom step` literal, since rule (a) covers every `lyx loom <verb>` and the skill also carries `lyx loom status`, `lyx loom start` and `lyx loom run`. Added the one legitimate exception: a `lyx loom start` surviving inside a recipe-gated loom-only section, which must be confirmed to sit inside such a gate.

## Pushed Back

None.
