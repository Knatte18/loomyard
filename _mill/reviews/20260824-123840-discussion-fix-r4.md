# Discussion fix — round 4

Source review: `_mill/reviews/20260824-123703-discussion-review-r4.md` (APPROVE, 0 BLOCKING, 5 NIT).
Auto mode: every NIT treated as FIX per `mill-receiving-review`'s default, with PUSH BACK unavailable.

## Fixed

- `20260824-123703-discussion-review-r4.md` — "**"14 checks is a miscount" rests on a false premise**".
  Verified against `contracts/specs/loom-plan-spec.md:200-217`: the list is 14 rows and row 1 bundles `format-unrecognized` / `plan-unapproved`, so "14" is a row count, not a miscount.
  `validator-checks` reframed as a metric mismatch, the figure defined as a count of distinct `Check:` IDs, and the rewritten spec now required to carry one row per ID.
- `20260824-123703-discussion-review-r4.md` — "**`rename-mechanic-section` contradicts the disposition table**".
  Reworded to "three `move-*` checks drop, two survive renamed", matching the table and the 15 − 4 arithmetic.
- `20260824-123703-discussion-review-r4.md` — "**"`Custom` gets no mechanical check at all" is self-contradicted**".
  Narrowed to "no *type-specific* check" in both `validator-checks` and `path-missing-rework`, with the card-generic checks that still bind `Custom` enumerated explicitly so no implementer writes a blanket skip.
- `20260824-123703-discussion-review-r4.md` — "**Stale-"14" inventory is a hand list and undercounts**".
  Replaced the hand list with a re-runnable Sweep 3 grep, added the missed `internal/planparser/doc.go:58` site, noted `validate_test.go`'s three occurrences, and dropped the fixed "six sites" count.
- `20260824-123703-discussion-review-r4.md` — "**Design-doc banner disposition is narrower than the banner**".
  The `plan-card-format.md` scope bullet now dispositions the banner clause by clause, including repointing the "reconcile or delete them when this lands" instruction at the two owning roadmap items.

## Pushed Back

None.
Auto mode makes PUSH BACK unavailable, and all five findings verified as factually accurate against the cited files in any case.
