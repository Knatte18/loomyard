MILL_REVIEW_BEGIN
# Review: loom: redesign the Discussion format — holistic

```yaml
duration_s: 1349.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 5 (self-reported model id: claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-24
```

## Findings

### [NIT:consistency] Card 1(f) undercounts discussion-format.md's inbound pointers as "exactly two"
**Demoted-from:** BLOCKING
**Location:** Batch 01, Card 1, requirement (f).
**Issue:** Card 1 instructs the new doc's lifecycle section to name "the exactly two inbound pointers that go stale" at deletion — `loom.md`'s producer-table pointer and `roadmap.md`'s Done-entry link — but the plan's own later cards add three more links to `discussion-format.md`: Card 2's second `loom.md` link (the new "what to also flag" subsection's own pointer), Card 3's `plan-card-format.md` blockquote link, and Card 4's first-edit `roadmap.md` group-intro link. All five break identically ("missing file") once the doc is deleted, regardless of whether they were added "for" Fix 1 or Fix 2, so the doc's own "Fix 2 is unaffected by this deletion" framing is also misleading — the Fix 2 *content* survives in `loom.md`, but the *links* pointing at `discussion-format.md` from Fix-2-related spots do not.
**Fix:** Reword requirement (f) to have the doc name all five inbound pointers (or state the count generically, e.g. "every link elsewhere in `manifest/` pointing at this file"), rather than asserting a specific wrong count of two.

## Verdict
APPROVE
MILL_REVIEW_END
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
