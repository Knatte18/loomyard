# Plan fix — round 4

Source review: `_mill/reviews/20260731-095014-plan-review-r4.md`

## Fixed

- `20260731-095014-plan-review-r4.md` — "[BLOCKING] Cards 11 and 20 cite gitrepo functions absent from their Context". Verified: Card 11's Requirements names `f.Warp.SHAExists` and `f.Warp.ChangedFilesSince` and makes a specific claim about `ChangedFilesSince`'s own godoc that the implementer cannot confirm without reading it, and Card 20 cites `f.Warp.SHAExists` in its dangling-baseline case; neither card listed the file both live in. Added `internal/gitrepo/gitrepo.go` to both cards' `Context:` lists. The reviewer's supporting point is correct and worth recording: cards 16, 19, 21, and 22 already list that file whenever they name a gitrepo method, so these two were an oversight rather than a deliberate exclusion. Worth noting for future rounds that the automated validator cannot catch this class — it matches backticked path tokens, and a bare method reference like `f.Warp.SHAExists` never looks like a path.

## Pushed Back

_None._
