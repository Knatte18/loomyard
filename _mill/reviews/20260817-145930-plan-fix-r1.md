# Plan fix — round 1

Source review: `_mill/reviews/20260817-145807-plan-review-r1.md`

## Fixed

- `20260817-145807-plan-review-r1.md` — "[BLOCKING:scope] Context omits the package two cards' Requirements name".
  VERIFY: accurate — card 4's Requirements name `modelspec.LoadRegistry`, card 8's name `batcher.ConfigTemplate`, and neither package appeared in the owning card's `Context:` or `Edits:`.
  HARM CHECK: none — adding two read-only Context entries breaks nothing, contradicts no documented decision, and destabilizes nothing.
  Added `internal/modelspec/load.go` (the file declaring `LoadRegistry`) to batch 2 card 4's `Context:`, and `internal/batcher/template.go` (the file declaring `ConfigTemplate`) to batch 2 card 8's `Context:`.
  The declaring files are named rather than the package directories, since `Context:` entries are file paths.

## Pushed Back

_None._
