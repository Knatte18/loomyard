# Fix report: `_mill/discussion.md` — fabric: merge-conflict primitive

```yaml
review: 20260819-070550-discussion-review-fable-r0.md
fixer: mill-implementer (Fable 5)
verdict: all 19 findings verified accurate; 19 fixed, 0 pushed back
```

Every finding was re-verified against the worktree, git's own documentation (`git merge --help`: "fast-forward updates … no way to stop those merges with --no-commit"), and the millhouse skills before acting.
The root-cause fix reverses the earlier rejection of a persistent merge-state record;
B1, B3, B4 and B11 fall out of that one reversal, and B2/B5 build on it.

## Fixed

- **B1** — new `a-recorded-merge-not-a-derived-one` and revised `no-new-commit-until-both-sides-are-clean` decisions: pre-merge SHAs for both sides are captured in a fabric-owned record before any merge command runs;
  a fast-forward is allowed (git doc quoted in Technical context), and abort is a uniform record-driven gated reset of both sides, which also handles the ff and squash cases.
  Test row pinning ff-then-abort added.
- **B2** — new `already-up-to-date-is-a-result-not-a-fabrication` decision: both-sides no-op is a lock-free `AlreadyUpToDate` result (Commit's degenerate-no-op precedent, millhouse fast-path contract);
  one-side no-op concludes without fabricating an empty commit, per-side outcomes live only in the internal record;
  `RecordCorrespondence` pairing a new SHA with an unchanged one declared legal (states, not deltas).
  Three test rows added.
- **B3** — `MergeInProgress` is now record-derived;
  git merge state without a record is refused by all four verbs with `*ErrForeignMergeState`, citing the human-plain-git carve-out.
  Foreign-merge test row added.
- **B4** — the record survives a crash between a conflicted `--squash` and self-abort;
  public `MergeAbort` completes recovery from the recorded SHAs.
  Crash-recovery test rows added.
- **B5** — new `commit-phase-concludes-per-side-and-never-rolls-back` decision: fixed internal conclude order, per-side committed SHAs in the record, `*ErrMergeIncomplete` with fixed side-free text (cause logged, never printed), idempotent `MergeContinue` retry;
  rollback of a landed merge commit explicitly rejected with rationale.
  Partial-failure test row added.
- **B6** — mapping rule written: identity for weft paths under `<AnchorRel>/<wired-name>/` (junction geometry makes weft-relative == worktree-root-relative);
  weft conflicts outside the wired set abort both sides with `*ErrUnmergeableState` (fabric's own write path cannot produce them, so they prove out-of-band writes);
  namespace root fixed to worktree root with the subpath-anchor rationale;
  no-collision argument grounded in warp-side junction excludes + Never Force-Add;
  list is sorted.
- **B7** — `merge-in <branch>` argument now required;
  the optional form and both its possible defaults are in Rejected with reasons;
  consistent with the Q&A's "the caller supplies it".
- **B8** — new `weft-source-is-derived-and-must-exist` decision: `WeftBranchName(source)` normative;
  missing `<source>-weft` (probed via `weftBranchExists`) is a guard failure with a fixed not-fabric-managed reason;
  warp-only merge and fork-on-demand rejected with reasons.
- **B9** — `safety-guards-are-aggregated-and-side-free` decision: best-effort fetch-then-`--ff-only` for `Merge` (millhouse precedent), vacuous pass on no-upstream sides (`Fabric.Pull` precedent, invisibility argued);
  `MergeIn` gains dirty-pair, foreign-state and source-resolution guards with millhouse's origin-preference freshness rule;
  remote-only source defined.
- **B10** — handle decision extended: caller resolves the target worktree via pair enumeration + `lyxcwd.ResolveWorktree` + `Open`;
  Go API is path-anchored (no cwd gate), CLI keeps the standard cwd gate;
  the "parent always checked out" assertion removed;
  no-worktree-on-target-branch flagged `**OWNER DECISION NEEDED**`.
- **B11** — lifecycle decision rewritten: quartet is record-driven and genuinely uniform;
  `Merge`'s cross-call quartet role named as crash recovery;
  public `MergeAbort`'s SHA source is the record.
- **B12** — new `public-surface-shapes` decision with verbatim Go: `gitrepo` primitives (`MergeStart`/`MergeOutcome`/`MergeConclude`/`ConflictedFiles`/`MergeHeadPresent`), `MergeOptions`, single `MergeResult` (embedding `MutationRecord`), all five fabric signatures, six named error types, the internal `mergeState` record, conflicts-as-result semantics, CLI exit codes, `-m`/empty-message defaults (git's prepared `MERGE_MSG`/`SQUASH_MSG`).
  Constraints section now carries the Mutation Record, Destruction Chokepoint (with the new weft-side gated reset in `destroy.go`, force-true dirtiness justified by record-gating), gitrepo Client Boundary and gitexec Checked-Call invariants, plus Durable-vs-Ephemeral for the record's git-dir placement (corrIndex precedent).
- **B13** — new `verify-before-conclude-not-post-commit-rollback` decision: verify runs in the uncommitted window where `MergeAbort` is the checkpoint;
  the post-commit gap and the missing two-sided reset-to-SHA verb are recorded in Scope Out and Audit findings.
- **N1** — lock rationale corrected to per-pair (`<weftPath>/.weft/weft.write.lock`);
  the Commit-during-merge raw-error consequence handled by a new typed refusal guard on `Fabric.Commit` (Scope In + test row).
- **N2** — two-verbs rationale rewritten on policy grounds (locality, isolation, unbounded window), with the accurate statement of what git actually forbids;
  Q&A left verbatim.
- **N3** — citations corrected: `cleanup.go:93-95`, vocabulary walk includes `contracts/stencils/**/*.md` and embedded templates, `add.go:116-172`.
- **N4** — guard aggregation decision (all guards evaluated, sorted deduped closed-set reasons, no ordering leak);
  vocabulary test spec extended to error strings and the guard-reason set;
  byte-identical dirty-warp/dirty-weft guard test row added.
- **N5** — Testing section states the `integration` tag requirement and both packages' existing `HermeticGitEnv` `TestMain` compliance.
- **N6** — CLI decision states that `lyx fabric merge --continue`/`--abort` conclude a conflicted `merge-in`, naming the asymmetry.

## Pushed back

None.
All 19 findings verified accurate: B1 against `git merge --help`;
B6 against `junctionnames.go`, the warp-binding record (`internal/fabriccli/fabric.go:78-82`) and `cleanup.go`'s legacy-content comment;
N1 against `weftgit.go:26-27`/`commit.go:178-186`;
the rest against the cited constraints, code, and millhouse skills.

## Owner decisions needed

- `Merge` when no worktree is checked out on the target branch: hard error (recommended, caller materializes a pair via `lyx fabric add`/`checkout`) vs fabric-materialized temporary pair vs millhouse-style in-place merge mode.
