# Plan fixer report — round 1

Source review: `20260717-112650-plan-review-r1.md` (REQUEST_CHANGES,
0 BLOCKING, findings: 1 MAJOR, 1 MINOR, 2 NIT — 0-BLOCKING round, 4c path).

## Fixed

- r1 "BeginBatch forward-references RestartChain (later card)" — reordered
  batch 5: `chain.go`/`RestartChain` is now card 19 (lands first), BeginBatch
  card 20, RecordBatch card 21; cross-reference updated.
- r1 "Card 38 authors suite content specified in card 40 (cross-batch)" —
  card 38 now writes only the minimal `**Covers:** webster` stub the coverage
  guard needs; card 40 owns the full W1/W2 scenario prose.
- r1 "Exported builderengine helpers referenced without their source in
  Context" — added `internal/builderengine/runlevel.go` to card 25's Context
  and `internal/builderengine/spawn.go` to card 29's Context.
- r1 "Run-exit cross-check reads pointer Result.ForkAudit without nil note" —
  card 30 now states: nil `ForkAudit` on a done fork-authorized run is itself
  a hard error (fail-loud, never skipped).

## Pushed Back

(none)
