# Fixer report: plan-fix r1

Source review: `20260919-052758-plan-review-r1.md`

## Fixed

- `[BLOCKING:design]` "Card 8's literal `seedReedConfig` recipe collides with `_lyx` already existing" — verified against `internal/cliwire/standalone.go`'s two `stencilstore.Reconcile` calls and `internal/stencilstore/reconcile.go`'s `seedGitattributes`, which `MkdirAll`s the stencils and specs base directories and so creates the state directory's `_lyx` before the card's seeding step runs. Card 8 now calls for a recursive, already-exists-tolerant directory create and names why, instead of an unqualified transplant of the single-level form, and `internal/cliwire/standalone.go` is added to the card's `Context:`.
- `[NIT:consistency]` "Card 10 asks to carry a Covers: tag that M1 does not have" — verified: `tools/sandbox/SANDBOX-REED-SUITE.md`'s M1 carries no coverage tag, and the three that do sit outside the split. The carry-the-tag instruction is dropped from card 10, replaced by a statement that neither half needs one, and batch 4's `## Batch Tests` paragraph is reworded so it no longer claims a tag must survive the split.

## Pushed Back

_(none)_
