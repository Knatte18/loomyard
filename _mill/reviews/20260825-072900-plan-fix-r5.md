# Fixer report: plan-fix round 5

Source review: `_mill/reviews/20260825-072705-plan-review-r5.md`

## Fixed

- `20260825-072705-plan-review-r5.md` — "[NIT:consistency] maxTopComments doc comment left stale by card 5/9" — verified against `plugins/prowler/reddit.go:19-21` ("Used by the Hacker News adapter"), which card 5 falsifies by making `formatRedditThread` a second consumer. Moved `plugins/prowler/reddit.go` from card 5's `Context:` to its `Edits:` and required the doc-comment update there, explicitly scoped as the only change card 5 makes to that file. Batch 3 already depends on batch 2, so the cross-batch overlap on `reddit.go` carries a dependency edge.
- `20260825-072705-plan-review-r5.md` — "[NIT:design] Token-cache concurrency guard is advisory-only, never in a machine-run verify" — verified; `-race` appeared only as an implementer aside in two `## Batch Tests` sections and in no scheduled command. Card 11's Requirements now schedule `go -C plugins/prowler test -race ./...` as a gated step alongside the offline suite and `selftest.sh`, with a race report stated as a hard failure of the card. The two advisory mentions now point at that gated run instead of standing alone.

## Pushed Back

_(none)_
