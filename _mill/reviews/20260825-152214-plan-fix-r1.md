# Plan fix — round 1

Source review: `_mill/reviews/20260825-152214-plan-review-r1.md` (holistic, REQUEST_CHANGES, 0 BLOCKING, 3 NIT).

## Fixed

- r1 holistic — "[NIT:scope] Card 4's rationale cites claudeengine identifiers absent from its Context": added `internal/shuttleengine/claudeengine/startup.go` to card 4's `Context:` in `plan/02-shuttle-attach.md`. Accurate — `claudeengine.Startup`, `StartupPending`, and the trust-dialog needles all live in that file and card 4's Requirements names all three.
- r1 holistic — "[NIT:design] finalize's Outcome-write placement leaves a ForkSubagents-audit failure stuck at \"running\"": card 2 in `plan/01-shuttle-await-operator-and-run-outcome.md` now requires the `Outcome` write **before** the fork-audit block rather than after it, with the reason recorded inline. Accurate — `finalize` returns early on an `AuditForks` failure, so the prior placement would persist `"running"` for a run that genuinely reached `OutcomeDone`. Fixed by placement rather than by disclosure, since writing first costs nothing and still precedes the cleanup block.
- r1 holistic — "[NIT:consistency] Card 15's Done-entry rationale contradicts the link it prescribes": reworded card 15's connecting sentence in `plan/05-loomrecipe-regression-and-docs.md` to drop the "rather than at a design doc" clause, keeping the concrete instruction (link `manifest/designs/loom.md`'s crash-recovery section) that already matches both existing `loom:` Done entries.

## Pushed Back

_(none)_
