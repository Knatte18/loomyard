# Discussion fix — round 6 (final round, cap reached)

Source review: `_mill/reviews/20260912-104258-discussion-review-r6.md` (verdict REQUEST_CHANGES, 3 BLOCKING, 2 NIT).

Round 6 is the configured cap (`roles.discussion-review.holistic.rounds: 6`).
Per the operator's explicit instruction at the cap — approve and hand off regardless — every finding below is fixed and the phase proceeds to Handoff under the Convergence gate's implicit-approval-at-round-cap branch.

## Fixed

- `[BLOCKING:design]` "Early run-lock probe precedes the MkdirAll it needs" — pinned that `step` performs `os.MkdirAll(filepath.Dir(loomengine.LoomRunLock(location)))` **before** the early probe, and recorded that this is therefore not a verbatim reuse of `run`'s step-5 probe but that probe hoisted together with the `MkdirAll` `run` already performs at its step 4. Explicitly rejected treating a missing-parent error as "lock free".
- `[BLOCKING:design]` "Interrupt-policy meta-test pins against a test-only symbol" — moved the meta-test to `internal/loomrecipe`'s own test package, the only package that can see both the unexported `loomRowEngines` var and the exported `loomshed` table, and made the primary assertion run against the production authority (the rows `loomrecipe.New` assembles) with `loomRowEngines` as the adapter-identity cross-check.
- `[BLOCKING:design]` "Policy unavailable when the interrupted step is the first one" — added the additive `interrupt_policy` key to `lyx loom status`'s envelope, sourced from the same `loomshed` table, so the interrupted-step branch reads the policy from the `lyx loom status` call it already makes rather than from a prior step envelope that may not exist. Noted that `status` keeps its lightweight wiring because the lookup is a map read keyed by a name it already holds. Rejected both "hand back when no policy is in hand" (wrong-defaults the most common interruption) and a producer-name fallback (reintroduces the phase knowledge the policy key removes).
- `[NIT:consistency]` "`next_interrupt_policy` missing from the pinned envelope tuple" — added it to the pinned tuple, now ten keys, with the six `StepResult`-derived and four CLI-computed keys named as such so the "full envelope key set" test has one unambiguous target.
- `[NIT:design]` "Interrupted-step 'moved' comparison has no stated baseline" — stated the baseline as the last successful step envelope's `next`/`history_length` pair, and pinned a pre-loop `lyx loom status` read as the baseline when there is none (first step of a session, or the first step after an operator re-invokes past the iteration cap).

## Pushed Back

None.
