MILL_REVIEW_BEGIN
# Review: Shed: outer phase-FSM skeleton — holistic

```yaml
duration_s: 407.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-15
```

## Findings

### [NIT:consistency] Card 7/Card 8 disagree on whether card 7 calls `persist`
**Demoted-from:** BLOCKING
**Location:** batch 02-run-loop.md, cards 7 and 8.
**Issue:** Card 7's step-3 pause exit is written "against [persist's] signature," which a literal reading has card 7 calling `s.persist(...)` before card 8 (the very next card) ever defines that method — `go build ./internal/shedengine/` would fail at the end of card 7's commit. Card 8 then says "rewrite card 7's pause exit to call this method," which only makes sense if card 7's pause exit does *not* yet call `persist`. The two cards' wording contradicts on card 7's actual deliverable.
**Fix:** Reword card 7's step-3 requirement to say explicitly that the pause exit is implemented inline (e.g. a direct `state.UpdateJSON` call) for now, and that card 8 extracts/refactors it into the shared `persist` method — removing the "write this branch against its signature" phrasing that implies calling a not-yet-existing method.

### [NIT:consistency] Batch 3 doesn't pin the external-writer mechanism batch 4 pins explicitly
**Location:** batch 03-pause-and-resume-scenarios.md (card 14) vs. batch 04-persistence-and-hard-error-scenarios.md's batch-local decision (card 18/19).
**Issue:** Batch 4's local decision requires every simulated external writer to go through `state.UpdateJSON` against a lenient map using the same `StatusLockPath`, matching the design's `external-writer-lock-contract` decision. Batch 3's card 14 (producer sets `pause_requested` mid-`Call`) and card 15/16 give no equivalent instruction, so an implementer could write those mutations via a bare struct write instead of the lock-cooperating path the design specifies as the only supported external-writer shape.
**Fix:** Add the same batch-local note to batch 3 (or point at batch 4's), pinning that any mid-`Call` external mutation in this batch's tests also goes through `state.UpdateJSON`/`WriteJSON` with the shared `StatusLockPath`.

## Verdict

APPROVE
Card 7/8's persist hand-off is self-contradictory and risks a non-compiling intermediate commit; one NIT alongside it.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
