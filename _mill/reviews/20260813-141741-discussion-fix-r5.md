# Discussion fix report — round 5

Source review: `_mill/reviews/20260813-141617-discussion-review-r5.md` (APPROVE, 0 BLOCKING, 4 NIT).
Two findings carry `Demoted-from: BLOCKING`, so the convergence gate's demoted predicate did not pass;
round 5 is the configured cap, so this round closes the phase as an implicit approval and the commit message records the shortfall.

## Fixed

- `20260813-141617-discussion-review-r5.md` — "prune.go claimed by two conflicting carve-outs": verified against `internal/fabricengine/prune.go` that its three git-derived `pe.Error` assignments are the shape-(D) exec-path bail and the two fallback-path messages the prior-call rule keeps the exit code for — none is a plain string-sink merge. `merge-rule-at-non-error-string-sinks` narrowed to `cleanup.go`'s two `entry.Error` assignments only, with an explicit statement that no `prune.go` sink takes the rule and why, plus a pointer back to the default rule's carve-out checklist so a `string` sink is not read as a licence to merge branches other decisions keep apart.
- `20260813-141617-discussion-review-r5.md` — "Prose-correction inventory has no enumeration method": added a regeneration grep for the prose sweep and named the three further falsified statements it finds — `docs/shared-libs/README.md`'s "windowless `RunGit` primitive", `destroy.go`'s three executor godocs (return shape, the `err == nil AND exitCode is zero` recording predicate, and `createGitWorktree`'s named-return rationale), and `gitrepoboundary_test.go`'s "exactly one `gitexec.RunGit` call site" header. Stated that the hand-list is what the query found today, not a closed set, and that prose is the one inventory in this task with no compiler behind it.
- `20260813-141617-discussion-review-r5.md` — "New guard's gitexec-side raw token is never stated": the new guard's two raw tokens now stated explicitly as `gitexec.RunGit` (unshortened) and `r.run(` (parenthesised), each with the reason it must not be harmonised with the other, and the shorter-prefix rationale scoped to the three token guards where it is correct.
- `20260813-141617-discussion-review-r5.md` — "`pull --rebase` exit branch is control flow, not a message": stated that the site takes the same `errors.As` split as shape (D) — recover a `*GitError` to attempt the abort, return immediately on anything else — so a mechanical merge cannot fire `rebase --abort` after a failure in which git never ran.

## Pushed Back

(none — all four findings verified accurate against the code, and no fix was in conflict with existing functionality, a documented design decision, or work outside this task's scope)
