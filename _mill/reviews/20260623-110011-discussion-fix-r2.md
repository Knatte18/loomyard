# Discussion fix report — round 2

Source review: `_mill/reviews/20260623-105937-discussion-review-r2.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- `20260623-105937-discussion-review-r2.md` — "[NOTE] sync exit code does not reflect detached push": added an explicit clause to the `edit-validate-and-sync-behavior` decision stating that `weft sync`'s `spawnPush` is detached, so the config-CLI confirmation means "committed and push launched," not "pushed"; the plan must not promise push-completion reporting.
- `20260623-105937-discussion-review-r2.md` — "[NOTE] seedGitExclude generalization uses junction `name`, not link path": clarified in the `junction-list-owned-by-paths` decision that the `{name, link-path, weft-target}` record carries three fields because junction creation uses link-path+weft-target while exclude seeding iterates the `name` field.

## Pushed Back

- (none)
