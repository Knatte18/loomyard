# Plan-fix report — round 1

Source review: `_mill/reviews/20260730-085306-plan-review-r1.md` (holistic, REQUEST_CHANGES, 5 BLOCKING + 2 NIT). All findings verified accurate; all fixed; none pushed back.

## Fixed

- **[BLOCKING] "Card 6 forward-references Card 7's fetchWithBrowser"** — moved `newFetcher()` from Card 6 to Card 7 (it references `fetchWithBrowser`, defined in Card 7's `browser.go`). To avoid a symmetric forward-reference from Card 5, the `fetcher` seam **type** is now defined in Card 1 (`fetcher.go`), so Cards 5/6/7 all reference an already-defined type. Card 6 now defines only `fetchPage`; its test constructs its own `fetcher`.
- **[BLOCKING] "fetchReddit has no injectable HTTP seam but Card 6's tests assume one"** — redesigned the seam: `fetcher.do func(*http.Request) (*http.Response, error)` (a raw transport, callers apply their own headers) replaces the old header-applying `httpGet`. `fetchReddit(ctx, f fetcher, url)` now issues its request (with the `prowler-reader/1.0` UA) through `f.do`, so a stubbed `do` exercises the Reddit branch with no network. Card 5, Card 6, and Card 7 updated consistently; `newFetcher` wires `do: httpClient.Do`.
- **[BLOCKING] "run.sh always rebuilds under PROWLER_BUILD_ONLY, breaking selftest.sh's reuse assertion"** — Card 8 rewritten so the BIN-exists reuse short-circuit is independent of `PROWLER_BUILD_ONLY`; the env var now only selects the final action (`exit 0` vs `exec`) via a `run_or_exit` helper. A second `PROWLER_BUILD_ONLY=1` run reuses the binary and leaves its mtime untouched, satisfying Card 9 test 2.
- **[BLOCKING] "No test exercises main()'s multi-URL join / per-URL-error behavior"** — Card 7 now extracts `runAll(ctx, f fetcher, urls []string) string` out of `main()` (join with `\n\n---\n\n`, per-URL errors captured inline) and adds `main_test.go` asserting the join order and that a failing URL yields its inline `# Error fetching …` segment without aborting siblings. Added `main_test.go` to Card 7 Creates, the Batch-1 unit-test set, and the overview's All Files Touched.
- **[BLOCKING] "New shell scripts have no `.gitattributes` LF pin"** — Card 14 now edits `.gitattributes`, adding `plugins/prowler/1.0.0/scripts/*.sh text eol=lf` (mirroring the existing `internal/fabricengine/post-checkout.sh text eol=lf` pin) so a Windows/git-bash checkout cannot CRLF-corrupt `run.sh`/`selftest.sh`. Added `.gitattributes` to Card 14 Edits and the overview's All Files Touched.
- **[NIT] "go.sum placeholder strategy contradicts intermediate compile"** — Card 1's `go.mod` now carries no `require` lines; each importing card runs `go get <dep>` (Card 6: go-readability + goquery; Card 7: chromedp + final `go mod tidy`), so stdlib-only Cards 1–5 build with an empty `go.sum` and every intermediate commit is buildable. Batch Scope reworded to describe the incremental-deps flow and batch-boundary verification.
- **[NIT] ".gitignore append doesn't account for the mill-managed fence"** — Card 14 now specifies the `plugins/prowler/1.0.0/bin/` entry is appended **after** the `# === end mill-managed ===` line, outside the fence, so a future `mill-setup` regeneration does not drop it.

## Pushed Back

None.
