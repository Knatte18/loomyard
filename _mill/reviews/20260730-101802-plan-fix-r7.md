# Plan-fix report — round 7 (operator-granted extension)

Source review: `_mill/reviews/20260730-101623-plan-review-r7.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 0 NIT). Finding verified accurate and new (not a repeat of prior rounds); fixed; none pushed back.

## Fixed

- **[BLOCKING] "settings.json permission set unverified against real precedent"** — VERIFIED against the real `plugins/weblens/settings.json` (read this session): it grants **three** entries — `Skill(weblens:*)`, `Bash(bash *)`, `Bash(node *)` — explicitly gating the `node` child its `run.sh` spawns via `exec node`, the exact relationship prowler's `go build` child has to its wrapper. The discussion's "exactly two entries" was based on a mischaracterization of weblens' shape. Card 11 now grants three entries — `Skill(prowler:*)`, `Bash(bash *)`, `Bash(go *)` — with the `Bash(go *)` child-spawn grant mirroring weblens' `Bash(node *)`, and documents the weblens-precedent verification inline. Added an overview Shared Decision recording the override.

## Pushed Back

None.
