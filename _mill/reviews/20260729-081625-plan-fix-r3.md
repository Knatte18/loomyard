# Plan-fix round 3 — codeintel-v1

Source review: `_mill/reviews/20260729-081309-plan-review-r3.md` (verdict: REQUEST_CHANGES, 2 BLOCKING, 3 NIT).

## Fixed

- `_mill/reviews/20260729-081309-plan-review-r3.md` — "Card 21 uses builtins() without registry.go in Context": added `internal/codeintelengine/registry.go` to batch 05 card 21's Context.
- `_mill/reviews/20260729-081309-plan-review-r3.md` — "Card 43 misses two more stale doc.go claims of its own kind": added item (7) to card 43's Requirements, correcting the "exactly six methods" claim (now seven, `textDocument/definition`) and the "uniformly... no error needs a distinct exit code" claim (now one exception, `ErrAmbiguousSymbol` at exit 2).
- `_mill/reviews/20260729-081309-plan-review-r3.md` — "[NIT] Batch 4's depends-on:[1] is unexplained by its own cards": on verification this was accurate and deeper than stated — neither `daemonstate.go` nor `probe.go` (batch 4) ever calls `hubgeometry`/`internal/lock`, and the overview's own prose additionally mis-attributed the worktree-scoped spawn-race lock to batch 4 (it lives in batch 6, per batch 04's own Scope Boundary note). Changed batch 4's `depends-on` from `[1, 2]` to `[2]` (overview DAG + batch 04's frontmatter), corrected the overview's rationale paragraph to state the real reason (shared `CONSTRAINTS.md`/`leaf_enforcement_test.go` edit ordering with batch 2, not a code dependency), and noted that batch 6 — the actual consumer of batch 1's accessor and batch 2's `internal/lock` — still reaches batch 1 transitively via batch 4→batch 2→batch 1.
- `_mill/reviews/20260729-081309-plan-review-r3.md` — "[NIT] Card 33 leaves a stale comment in refsCommand": added removal of the stale inline comment to card 33's Requirements.
- `_mill/reviews/20260729-081309-plan-review-r3.md` — "[NIT] ensureSupervised's loser retry path has no backoff on one branch": added the same 100ms sleep to step (3)'s "healthy, release, loop back" path in batch 06 card 24, with the race window explained (winner releases the lock and writes state before its own dial-retry loop confirms the bind actually completed).

## Pushed Back

None.
