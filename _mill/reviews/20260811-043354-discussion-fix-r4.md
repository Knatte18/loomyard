# Discussion fix — round 4

Source review: `_mill/reviews/20260811-043259-discussion-review-r4.md` (verdict APPROVE, 0 blocking, 2 NITs).
Both verified against source before applying;
neither pushed back.

## Fixed

- `20260811-043259-discussion-review-r4.md` — "Acceptance command misses the Tier-2 evidence": Testing now states the tier split explicitly (candidates 1–3 Tier 1; candidate 4 and the nil-guard scenario Tier 2), names both `//go:build integration` files carrying the decisive evidence, and Acceptance now requires `go test -tags integration ./...` alongside `go build ./...` and `go test ./...`. Verified both tags directly: `internal/webstercli/verbs_test.go:1` and `internal/websterengine/runlevel_test.go:1`.
- `20260811-043259-discussion-review-r4.md` — "Nil-guard placement wording contradicts itself": corrected a self-contradiction introduced in round 2's fix. The guard takes the deleted `Select` block's position at `:327–330`, ahead of the `Batch` call at `:331`; it is explicitly *not* beside the zero-batch refusal at `:336`, which sits after the call that would panic. The refusal is now cited as the right analogy and the wrong location. Scope → In updated to match.

## Pushed Back

None.
