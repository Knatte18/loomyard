# Plan fix — round 1

Source review: `_mill/reviews/20260701-090132-plan-review-r1.md` (verdict
REQUEST_CHANGES, blocking_count 2, nit_count 2).

## Fixed

- **[BLOCKING] NeverInitialized test contradicts fixture's always-present weft
  placeholder** (`04-initcli-undo.md`, Card 10, `TestRunInit_Undo_NeverInitialized`) —
  verified `lyxtest.buildWeftPrime` (`internal/lyxtest/lyxtest.go:190-204`) always
  seeds `_lyx/config/placeholder` regardless of host-side `init`, and confirmed no
  production `warpengine` spawn code ever creates that file (grepped for
  `placeholder`/`_lyx/config` in non-test `warpengine` sources — zero hits), so it's
  pure fixture scaffolding, not a real "never initialized" state. Updated the test spec
  to remove the placeholder (and the resulting empty `_lyx/config`/`_lyx` dirs) before
  calling `--undo`, so the fixture genuinely represents a never-initialized directory.
- **[BLOCKING] PartialRecovery(b) requires real push but mandates a fixture that
  forbids it** (`04-initcli-undo.md`, Card 10, `TestRunInit_Undo_PartialRecovery`) —
  verified `lyxtest.CopyPairedLocal`'s doc comment (`internal/lyxtest/lyxtest.go:511-517`)
  explicitly states `WeftBare` is empty and push is unsupported. Split the test spec:
  part (a) keeps `CopyPairedLocal`, part (b) now uses `lyxtest.CopyPaired(t)` and
  asserts local `HEAD` matches `WeftBare`'s `HEAD` after the recovering `--undo` call.
- **[NIT] unseedLyxJunction hardcodes single junction while its sibling generalizes**
  (`03-warpengine-unwire-junctions.md`, Card 6) — added a batch-scope note explaining
  this is an accepted, deliberate scope-narrowing (mirrors `HostJunctions` currently
  returning exactly one entry), not an oversight, and stating that a future second
  junction type would require revisiting `unseedLyxJunction`/`UnwireResult` together
  rather than generalizing preemptively now.
- **[NIT] "Mirrors seedLyxJunction" claim reorders the validation checks**
  (`03-warpengine-unwire-junctions.md`, Card 6) — reordered `unseedLyxJunction`'s
  Requirements to resolve the canonical target via `EvalSymlinks` *before* the
  `fslink.IsLink` check, matching `seedLyxJunction`'s literal check order exactly
  (previously specified in the reverse order).

## Pushed Back

None — all four findings were factually accurate and low-harm to fix.
