# Plan fix — round 1

Source review: `_mill/reviews/20260818-061722-plan-review-r1.md`

## Fixed

- `20260818-061722-plan-review-r1.md` — "[BLOCKING:scope] Card 10's Context omits internal/lyxcwd/anchor.go, the file its own Requirements depend on": verified — `ErrCwdOutsideAnchor` (`internal/lyxcwd/anchor.go:100`) and `ErrStaleAnchorMarker` (`anchor.go:56`) are both declared in `anchor.go`, not the listed `lyxcwd.go`. Added `internal/lyxcwd/anchor.go` to card 10's `Context:`.
- `20260818-061722-plan-review-r1.md` — "[BLOCKING:design] The mandated \"reachable only through\" comment is itself inaccurate — a third path exists": verified — `readRecordedAnchor` returns an `ErrInvalidAnchor`-wrapping error at `internal/lyxcwd/anchor.go:177` when a recorded anchor exists but fails `ValidateAnchorRel`, a third route into the residual branch. Card 10 now requires the comment to name `lyxcwd`'s anchor-resolution path generically, cites all three known routes as examples rather than a closed set, and explicitly forbids an exhaustive two-sentinel list; card 12's echo of the same instruction was updated to match.
- `20260818-061722-plan-review-r1.md` — "[NIT:consistency] Garbled sentence in Card 12's doc.go requirement": verified — reworded to "does the hub-level directory this write targets exist".

## Pushed Back

_None._
