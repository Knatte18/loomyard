MILL_REVIEW_BEGIN
# Review: fabric: Fabric.Commit classify+dispatch + unified diff/status — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-30
```

## Findings

### [NIT] worktree.go's exclude-file handling contradicts the plan's stated go-git behavior
**Location:** `internal/gitrepo/worktree.go:34-56`
**Issue:** Card 11 (`04-unified-diff-status.md`) asserts, as a "confirmed" fact, that `wt.Status()` reads `.git/info/exclude` on its own so "no separate exclude-file filtering is needed"; the shipped code adds `readGitDirExcludePatterns` plus a `wt.Excludes` assignment specifically because go-git's worktree filesystem is chrooted and silently fails to read that file. The fix is correct and well-tested (`diff_integration_test.go`'s exclude-artifact assertions exercise it genuinely), but the plan text and the shipped doc comment now assert opposite things about the same library behavior.
**Fix:** Note in the plan/discussion doc (or a follow-up comment) that Card 11's "confirmed" go-git behavior claim was superseded by this file's own findings, so a future reader isn't misled by the stale plan text.

### [NIT] push subcommand's internal doc comment omits --warp-path
**Location:** `internal/fabriccli/weft_verbs.go:196`
**Issue:** The comment directly above `pushCmd` still reads "push subcommand: commits then pushes, or in bypass mode pushes directly via --weft-path," but the bypass branch below it now also handles `--warp-path` (card 5). The file header comment was correctly updated to mention both flags; this inline one-liner was missed.
**Fix:** Update the comment to mention both `--weft-path` and `--warp-path`.

## Verdict

APPROVE
All five batches match their plan cards and Shared Decisions; no out-of-plan files, no constraint violations, only two cosmetic doc NITs.
MILL_REVIEW_END
