# Plan fix — round 2

Source review: `_mill/reviews/20260811-035818-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- r2 `[NIT:consistency]` "predicate-sites rejects not routed to any section" — verified accurate: the discussion's `predicate-sites-are-real-and-must-stay-expressible` decision carries two `Rejected:` bullets that no card requirement routed anywhere, so the verdict could have transcribed the mixed-tri-state disposition without the reasoning for why those sites do not stay raw.
  Both rejections are now required content inside Card 1's item 7 mixed-tri-state sub-part, stated in full: sweeping them later is rejected because they are not debt and sweeping would be a regression, and folding them into "raw, permanently correct" is rejected because an earlier draft did exactly that and hid a bare-exit-code failure path inside a site labelled as needing no diagnostic.
  Placed in item 7 rather than item 5's rejected-alternatives list, per the finding's own reading — these are dispositions of a migration class, not alternatives to the shape decision.

## Pushed Back

_None._
