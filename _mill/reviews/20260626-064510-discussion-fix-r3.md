# Discussion fix report — round 3

## Fixed

- `20260626-064454-discussion-review-r3.md` — "[NOTE] Subtest names in tables don't match decision labels": corrected group E and the keep-list to the literal `t.Run` names `TestWeftPrechecksHardRequireWeftRepo` / `TestWeftPrechecksRejectExistingWeftWorktree` (no `/` separator), and added a "Subtest names" note in the consolidation mechanics directing the plan to target literal names.
- `20260626-064454-discussion-review-r3.md` — "[NOTE] Group A fold needs a per-case assertion hook": added a "Group A mechanism" note stating `TestAdd` is table-driven with no per-case assertion field, so the plan must add a per-case `extraAssert` hook (or a HappyPath-guarded block) so the folded weft-spawn/dormant assertions run only for the HappyPath row.

## Pushed Back

(none)
