MILL_REVIEW_BEGIN
# Review: invariants and docs for the told-geometry rule — holistic

```yaml
duration_s: 237.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-19
```

## Findings

None. Verified all five batches end-to-end against the plan and against source.

Batch 1 (`CONSTRAINTS.md`): the new `## Told-Geometry Invariant` heading lands verbatim between Cwd Resolution and Lyxdirs Single-Declarer, anchors as `#told-geometry-invariant`, and its content is cross-checked against source: the five-way `ResolveMode` outcome table matches `internal/preflight/predicates.go` exactly (degrade on hub-absent resolve, degrade on `ErrNotAGitRepo`, degrade on `ErrCwdOutsideAnchor` with no re-probed hub geometry, refuse verbatim on `ErrCwdOutsideAnchor` inside a wired worktree, refuse on any other error). The six machine-enforced packages' allowlists genuinely omit `lyxcwd` (spot-checked `treadleengine`, `shedengine`, and confirmed `tokenvocab`/`pattern`/`buildinfo`/`standalonestate` leaf tests and `shuttleengine`/`scoutengine` seam tests never mention `lyxcwd`). The Cwd Resolution Invariant's new four-sub-point bullet matches `internal/lyxcwd/lyxcwd.go` and `anchor.go` precisely (repo-only check via `ErrNotAGitRepo`, absent-vs-stale-anchor handling, exact-equality cwd gate, unconditional `HubPath`/trimmed `RepoName`).

Batch 2 (`cmd/lyx/configstrictness_test.go` + `tierpurity_test.go` + `CONSTRAINTS.md`): the pinned degrading/strict sets match the tree exactly (`configengine.LoadOrTemplate(` in `shuttleengine`/`reedengine`/`perchengine`/`websterengine`/`batcher`; `configengine.Load(` in `fabricengine`/`loomengine`/`boardengine`, `internal/configengine` itself excluded, only `config_test.go` also matches and is a `_test.go` file). The `tierpurity_test.go` allowlist entry and the `CONSTRAINTS.md` `Enforced by` flip both landed correctly, and the old two-line specification prose was deleted rather than compressed, per the batch's own instruction.

Batch 3 (five `doc.go` audits): all five sentences are additive, correctly placed, and factually accurate against source (`burlerengine.Geometry`'s fields are indeed `WorktreeRoot`/`AnchorPath`; `buildinfo`'s allowlist is genuinely empty). No `warp`/`weft`/fabric-sense `host` token introduced. The `buildinfo` design-doc reword drops the file-path reference while keeping the `StencilMode()` rationale, and `producers-standalone` now appears in `CONSTRAINTS.md`/`buildinfo/doc.go` only as prose about "waves"/"earlier design," never as a link to the deleted file.

Batch 4 (`docs/overview.md`): both self-expiring allowlist entries (`../CONSTRAINTS.md#package-naming` in this file, `#hub-geometry-invariants` never created) are left untouched. The Modules-section sentence correctly keeps `preflight`/`hubgeom`/`standalonegeom` out of the shared-infrastructure parenthetical (verified the import direction: `hubgeom`/`standalonegeom` import the engines, not the reverse). The Execution-stack paragraph and repo-tree diagram line for `internal/standalonegeom` are both present, and the diagram's column alignment (`/` + padding to column 30) is correct.

Batch 5 (`manifest/roadmap.md` + design-doc deletion): the Planned item moved to the head of Done with rewritten past-tense wording, `## Planned` is left empty with its lede intact, all four existing Done entries lost their "the doc survives this task because…" clause and repoint at `#told-geometry-invariant`, referencing `preflight`/`hubgeom`/`standalonegeom` package docs exactly where the plan specifies. `manifest/designs/producers-standalone.md` is deleted, and a repo-wide grep found no remaining reference to it outside expected prose mentions ("producers-standalone waves"/"earlier producers-standalone design") and this task's own `_mill/` artifacts.

No out-of-plan files, no cross-batch contract breaks, no duplicated helpers (the plan's mandated `configStrictnessDiffSets` split from `diffMethodSets` is intentional, not duplication), no constraint violations found.

## Verdict

APPROVE
All five batches match the plan and verify against source with no discrepancies found.
MILL_REVIEW_END
