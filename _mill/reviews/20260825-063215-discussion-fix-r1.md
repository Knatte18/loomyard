# Discussion fix report — round 1

Source review: `_mill/reviews/20260825-063143-discussion-review-r1.md` (orchestrator, APPROVE, 0 BLOCKING / 1 NIT).

## Fixed

- `[NIT:consistency]` "producer table rows" update left unspecified in shape, unlike every other decision — added a `design-doc-table-keeps-one-row` decision to `_mill/discussion.md` pinning the table at one row (Producer cell relabeled `Discussion-Review` *(segment: `Discussion-Bouncer` + `Discussion-Burler`)*, `Kind`/`Type`/Input/Output unchanged), on the `Webster` row's own black-box precedent, and re-pointed the Scope bullet at that decision.

## Pushed Back

_None._
