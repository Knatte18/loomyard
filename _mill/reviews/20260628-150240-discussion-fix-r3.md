# Discussion-fix report — round 3

Source review: `_mill/reviews/20260628-150156-discussion-review-r3.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- r3 "[NOTE] upsert-batch top-level `{tasks}` wrapper not in allowlist" — added the
  `upsert-batch` wrapper to the reject-unknown-keys enumeration (allowed `{tasks}`;
  typo'd wrapper or absent/empty `tasks` errors instead of silently succeeding with
  `count:0`), plus a Testing case and Q&A entry.
- r3 "[NOTE] `get` not-found semantics (null vs error) unspecified" — added a bullet to
  the error-on-missing-target decision confirming `get` stays a query (valid-but-absent
  slug/id returns `task:null`; only malformed payloads error), plus a Testing case and
  Q&A entry.

## Pushed Back

- None.
