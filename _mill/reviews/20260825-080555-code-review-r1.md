MILL_REVIEW_BEGIN
# Review: Fix prowler: Reddit adapter blocked — holistic

```yaml
duration_s: 211.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-25
```

## Findings

### [BLOCKING:consistency] reddit-block-page.html fixture violates Card 1's own trim requirement
**Location:** `plugins/prowler/testdata/reddit-block-page.html:1-3`
**Issue:** Card 1 (`01-block-detection.md`) requires the trimmed fixture to keep "the document's opening `<html>`/`<head>`/`<title>` region plus the contiguous region containing the distinguishing marker text." The shipped file contains no `<html>`, `<head>`, or `<title>` tag at all (verified by grep — zero matches) — it opens directly on `<body class=theme-beta><div><style>...`. Line 1's inline `<style>` block is also truncated mid custom-property declaration with no closing `</style>`, and the elision comment on line 2 lands in the middle of what reads as a single base64 data-URI attribute value spanning lines 1/3 (line 3 opens with a bare base64 fragment `er363Xbjy1XG9m...`, not a tag), which is not "the contiguous region containing the distinguishing marker text" cut at a clean boundary — it looks like an arbitrary byte-offset truncation rather than the structural trim the card specifies.
**Fix:** Re-derive the fixture per Card 1's literal requirement — retain the opening `<html>/<head>/<title>` region, elide only at clean structural boundaries (attribute/tag boundaries, never mid-attribute-value), and keep the `<!-- trimmed for fixture use -->` markers at those points; verify with a plain grep for `<html`/`<head`/`<title` before re-committing.

## Verdict

REQUEST_CHANGES
Fix the malformed reddit-block-page.html fixture to satisfy Card 1's explicit trim requirement before merge.
MILL_REVIEW_END
