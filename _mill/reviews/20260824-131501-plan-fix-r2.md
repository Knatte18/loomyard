# Plan fix — round 2

Source review: `_mill/reviews/20260824-131501-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING, 0 NIT).

## Fixed

- `20260824-131501-plan-review-r2.md` — "Card 12 inverts the actual behavior of the test it edits": verified accurate against `internal/websterengine/template_test.go`. `TestRenderForkPrompt_OmitsRenameMechanic` calls `requireNotContains(t, text, "## Rename mechanic")` and proves the section's absence, per the fork-context-hygiene Shared Decision recorded in the file's own `requireNotContains` doc comment. Card 12's prose claimed the opposite. Rewrote the card to name the test explicitly, state what it actually proves, and forbid changing `requireNotContains` to `requireContains` or adding a presence assertion, so the false premise can no longer lead an implementer into breaking a passing test.
- `20260824-131501-plan-review-r2.md` — "Card 10 leaves a stray format-3 fixture in `sections_test.go` that trips the plan's own Sweep 1": verified accurate. `TestParsePlan_PlanLevelSections_AbsentAreEmpty` builds an inline `format: 3` overview and a six-label format-3 card body, and only calls `ParsePlan`, so it qualifies for neither Sweep 1 exemption card 26 allows. Extended card 10's requirements to move that fixture to format 4 while keeping its three section-default assertions unchanged, closing the gap inside the card whose scope already owns the file rather than leaving it for a verification-only card that changes nothing.

## Pushed Back

_None. Both findings were factually accurate and source-grounded, and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task._
