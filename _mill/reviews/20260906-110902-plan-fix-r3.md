# Plan fix — round 3

Source review: `_mill/reviews/20260906-110454-plan-review-r3.md`

## Fixed

- r3 "[BLOCKING:scope] webster's own Run gate never moves to planglyph" — confirmed at `internal/websterengine/runlevel.go:334`. Card 24 now moves that gate too, with the same severity rule and `ErrQuarryUnavailable` disposition; `runlevel.go`/`runlevel_test.go` added to its `Edits:` and to All Files Touched, `./internal/websterengine/` added to batch 5's verify, and the batch-local decision reworded from three surfaces to four.
- r3 "[BLOCKING:scope] RecordDeps carries no Plan for the three record-batch consumers" — confirmed: `RecordDeps` has no `Plan` field while `BeginDeps` does. Card 33 now states that `RecordDeps` gains `Plan *planparser.Plan`, populated at the one call site from the `ParsePlan` call `internal/webstercli/recordbatch.go` already performs, so no second parse is introduced.
- r3 "[BLOCKING:scope] quarrycli escapes the seam-signature guard" — confirmed: the guard is two hand-maintained blank-identifier slices. Card 27 now edits `cmd/lyx/seamsignature_test.go`, adding both `quarrycli` entries and updating its doc-comment counts; the file is added to All Files Touched.
- r3 "[NIT:decision] directory-target can never fire after card 5" — confirmed and load-bearing: canonicalizing every path ref would rewrite `internal/foo` into the valid unit self glyph `internal/foo#`, satisfying the `package-spelling` hard finding by erasing it. Card 5 now gates canonicalization on the ref carrying a file extension, leaving extensionless path refs for card 4's `directory-target` to classify, with a test case for it.
- r3 "[NIT:consistency] the RunCLIIn recount instruction produces a wrong number" — confirmed: `seamsignature_test.go` pins eleven `RunCLI` and ten `RunCLIIn`, so CONSTRAINTS' "eleven of twelve" is already wrong and counting `root.AddCommand` arity would be wrong the other way. Card 29 now derives both figures from the two pinned slices (twelve and eleven post-task) and states the sentence is corrected rather than incremented.
- r3 "[NIT:scope] the new chokepoint guard may trip the tier-purity guard" — confirmed: every `go env GOMOD` guard carries an `allowedSpawners` entry. Card 15 now pins `runtime.Caller(0)` root resolution explicitly, matching `registration_test.go`/`sandbox_coverage_test.go`, so the guard spawns nothing and needs no allowlist entry.
- r3 "[NIT:scope] no batch verify runs the markdown link guard" — confirmed: the guard is `internal/lyxcwd/docslink_test.go`. `./internal/lyxcwd/` added to the verify of batches 4, 6 and 7 — the three that rewrite `manifest/` or `docs/` content — with the reason stated in each Batch Tests section.
- r3 "[NIT:completeness] card 4's two new checks have no named function or dispatch slot" — card 4 now names `checkBareSymbolTarget` and `checkDirectoryTarget` and pins their dispatch position immediately after `checkCardPathMalformed`, matching cards 3, 10 and 14.

Also corrected while applying the above: batch 5's verify gained a chained `go test -tags integration ./internal/websterengine/`, because `runlevel_test.go` is `integration`-tagged and card 24's new cases would otherwise never execute. Caught by the `verify-excludes-edited-tagged-test` validator check on re-run.

## Pushed Back

_None. All eight findings verified accurate against the cited source and none met a harm-check escape._
