# Plan fix — round 1

Source review: `_mill/reviews/20260814-170912-plan-review-r1.md` (APPROVE, 0 BLOCKING, 3 NITs).

## Fixed

- r1 / "Card 25 misattributes a sandbox sentence to F13 instead of F9" — verified: the "paired weft worktree or the hub's `_board`" sentence is at `tools/sandbox/SANDBOX-FABRIC-SUITE.md:254`, inside F9, and F13 (lines 321-335) carries no `_board` reference. Card 25 now names F9 explicitly and states that F13 needs no edit.
- r1 / "Card 14 cites add.go's step as \"(10)\" but the source labels it \"(10b)\"" — verified: `internal/fabricengine/add.go:185` reads `// (10b) Wire the new worktree's warp junctions eagerly`. Card 14 now cites `(10b)`.
- r1 / "Card 20 leaves a stale \"three junctions\" reference in remove_junctions_integration_test.go" — verified: `TestRemove_SweepsAnchoredLinksOnSubpathHub`'s doc comment at line 107 says "three junctions existed one directory down", which goes false once the board link leaves the anchored directory. Card 20 now instructs updating that comment too and requires both counts in the file to agree.

## Pushed Back

_(none — all three findings were factually accurate, and none of the fixes breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task.)_
