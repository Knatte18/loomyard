# Fixer report: plan-fix round 4

Source review: `20260908-150054-plan-review-r4.md`

## Fixed

- `20260908-150054-plan-review-r4.md` — "[BLOCKING:scope] wireStandalone doc comments left stale by the rewrite".
  Verified against `internal/webstercli/wiring.go` and `internal/burlercli/wiring.go`: both `wireStandalone` doc comments open by naming `resolveStandaloneTarget` and claim to be the only `standalonestate.Derive` call site, and webster's closes by justifying its already-absolute parameters against a default-vs-override comparison "below" — three claims the cards' own deletions falsify, and none reachable by either batch-3 check, which match on the AST.
  Cards 6 and 7 now each carry an explicit `wireStandalone` doc-comment retarget: describe what the function now does (call `wireModule.ResolveStandalone` and compose that CLI's own engines onto the result) and point at `cliwire.ResolveStandalone` for the ordering and asymmetry rules rather than restating them.
- `20260908-150054-plan-review-r4.md` — "[NIT:scope] R6-17's --profile call site is rewired with no assertion".
  Verified: `internal/burlercli/cli_test.go` covers only the missing-`--profile` refusal and `decodeProfile`, so nothing pins that `--profile` resolves against `c.cwd` rather than the process working directory.
  Chose the reviewer's first option over review-verified-only.
  Card 7 now appends `TestRunVerb_RelativeProfileResolvesAgainstSeamCwd` to `cli_test.go`, driving the `run` verb the way `TestRunVerb_AbortedPreRunEmitsOneEnvelopeNotTwo` already does, with `c.cwd` at a `t.TempDir()` holding the profile and a relative `--profile` value, asserting the emitted error does not contain `read --profile`.
  `internal/burlercli/cli_test.go` moves from card 7's `Context:` to its `Edits:`, and the card's blanket "do not edit `cli_test.go`" is narrowed to forbid changing any existing case while allowing this one append;
  the same blanket rule on `cli_integration_test.go` is unchanged.
- `20260908-150054-plan-review-r4.md` — "[NIT:scope] Card 3 names planparser.PlanDir without it in Context".
  Verified: card 3's `Requirements:` names `planparser.PlanDir` and the `hubgeom.WebsterGeometry(loc).PlanDir` equality while `internal/planparser/parse.go` was in neither `Context:` nor `Edits:`.
  Added it to card 3's `Context:`, matching cards 5 and 6.

## Pushed Back

_None._
