# Plan fix — round 3

Source review: `_mill/reviews/20260731-094248-plan-review-r3.md`

## Fixed

- `20260731-094248-plan-review-r3.md` — "[BLOCKING] Card 5's crucible sweep misses live (non-history) mentions in gitrepo-review-prompt.md". Verified against the file. Card 5 corrected only the "What to read" list, while three "High-yield focus" bullets instruct a future reviewer to actively drive-test the deleted mechanism (argument injection naming `SetSnapshotSHA` and its `update-ref <ref> <sha>` example, `SetSnapshotSHA`'s adopt-on-conflict race with real concurrent writers, and `remoteName()`'s "origin" fallback) and the "Fixing" section names `snapshot.go` in a style-convention enumeration. Card 5 now covers all four, with the surgical-versus-delete distinction spelled out per bullet — the injection bullet keeps its two surviving methods, the other two bullets go entirely. Found and added one the review did not name: the round-framing paragraph after those bullets singles out "the snapshot-ref concurrency/adopt-on-conflict machinery" as an area deserving particular skepticism, which is also a live instruction. Restated the frozen-versus-live rule as instruction-versus-record rather than keyword presence, which is the distinction that actually separates the two, and added a second closing grep pass over `crucible/` — the directory the mandated internal sweep does not reach, which is precisely why this class was nearly missed.

## Pushed Back

_None._
