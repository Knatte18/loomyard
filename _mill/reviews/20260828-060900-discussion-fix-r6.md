# Discussion fix — round 6 (extension round)

Source review: `_mill/reviews/20260828-060729-discussion-review-r6.md` (APPROVE, 0 BLOCKING, 3 NIT, 0 demoted).

All three NITs verified against source before fixing, per `mill-receiving-review`.
PUSH BACK is unavailable under `--orch`; none was warranted.

Round 6 was the one-round non-progress extension granted after round 5, `extension_used` now spent.
Convergence was not reached — `prev_verdict_was_approve` was False, since round 5 was REQUEST_CHANGES — so this round closes under the implicit-approve-at-round-cap rule, recorded in the commit message.

## Fixed

- r6 `[NIT:consistency]` "websterengine already has the proposed test shape" — verified `internal/websterengine/gitwrap_test.go` is `//go:build integration` + `package websterengine`, the exact tagged-internal combination the r5 fix claimed did not exist in-package. Named it as the model to copy, noted that it reuses the package's hermetic `TestMain`, and dropped the "both obvious choices are wrong" framing from both the Testing section and the r5 Q&A entry.
- r6 `[NIT:consistency]` "`singlellm-parity` field set contradicts itself in one bullet" — verified `singlellm.go:171` emits `lastAssistantMessage` alongside the other five keys, so "the same fields the `OutcomeAsking` branch already emits" disagreed with the enumerated list beside it. Made the enumerated six-key list authoritative and stated the deliberate omission of `lastAssistantMessage`, with the reason: on `Asking` it is the question itself, on `Died`/`Timeout`/`default` it is stale or absent and would invite being read as a cause.
- r6 `[NIT:consistency]` "`withCapturedOutput` is unreachable regardless of export" — verified the helper is `internal/logger/logger_test.go:17` in `package logger`, so no other package can reach it under any export. Replaced the conditional "check whether it is exported or needs a local equivalent" with the settled fact: `shedadapters` and `mergeresolve` each need their own `logger.SetOutput` capture helper.

## Pushed Back

None.
