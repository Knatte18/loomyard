# Plan fix report — round 5

Source review: `_mill/reviews/20260731-200312-plan-review-r5.md` (verdict REQUEST_CHANGES, blocking_count 2, nit_count 1)

## Fixed

- "ensureDurableSink's testing-gate order defeats the SetDurableSinkDir seam" — reordered batch 4's Card 10 so a non-empty `SetDurableSinkDir` override is checked and taken as the opt-in signal BEFORE the `testing.Testing()`/`LYX_TRACE` gate, instead of after it. Under the old order every unit test calling `SetDurableSinkDir(t.TempDir())` (batch 4 Cards 15/16, batch 5 Cards 21/22, batch 6 Card 26) would have had `sinkOK` forced false regardless, since `testing.Testing()` is always true under `go test` and none of those tests set `LYX_TRACE=1` — silently defeating the seam every later test card depends on. Also clarified batch 5's Card 21 case 4 (the one case that must stay genuinely unarmed) to call `SetDurableSinkDir("")` explicitly, since it is now the odd one out among that card's cases.
- "Card 44's spawn-observability citation points at the wrong line, risking a nil-pointer crash" — corrected batch 13's Card 44: `cmd.Start()` is actually at `ensureserver.go:545-546`, not line 520 (which only builds `cmd`, before `cmd.Process` exists). The `logger.Info` call must land after line 555's `logFile.Close()` on the success path, where existing code already safely reads `cmd.Process.Pid` at line 562 — not near line 520, where it would nil-pointer-panic.
- "Card 35's cited line for the header-pane split error is the validate-guard, not the raw split call" — batch 9's Card 35 now also cites `lifecycle.go:565-567` (the raw `e.tmux.output("split-window", ...)` error, previously uncited) alongside `:576-578` (`validateSplitCreatedNewPane`'s separate failure branch).

## Pushed Back

(none)
</content>
