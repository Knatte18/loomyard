# Plan fix — round 5

Source review: `_mill/reviews/20260809-095150-plan-review-r5.md`

## Fixed

- `[BLOCKING:scope]` "Card 12 misses two duplicate builder mentions in status-schema.md" — verified: the first JSON example's inline `//` comments repeat both phrases (`start_sha`'s "repo HEAD stamped when Builder begins" and `pause_requested`'s "diverges from builder, which uses a separate flag file").
  Card 12 now names both, each in the right clause: the `start_sha` comment with the phase-rename set, the `pause_requested` comment with the module-prose set (which is a restatement, not a rename), and states explicitly that the divergence note appears twice.
- `[BLOCKING:scope]` "Card 13 misses the Precedence section's opening `(e.g. builder.yaml)`" — verified: that line sits outside the worked-example block the card rebuilds, and `builder.yaml` is one of card 18's own banned module-word tokens.
  Card 13 now treats the whole Precedence section, naming the opening hierarchy line and its `(e.g. webster.yaml)` replacement before the worked example, and the previously vague "the precedence line's example" clause was removed as redundant.
- `[NIT:consistency]` "tools/sandbox/main.go's 'ten subcommands' count goes stale" — verified: the header comment leads with a count that becomes wrong once the `builder-suite` case is deleted, and no acceptance-grep pattern reaches it.
  Card 1's `tools/sandbox/main.go` instruction now includes the ten→nine change and says why it cannot self-surface — matching the treatment `suite.go`'s seven→six counts already had.

## Pushed Back

_None._
