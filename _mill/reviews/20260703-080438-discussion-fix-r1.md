# Discussion fix — round 1

Source review: `_mill/reviews/20260703-080319-discussion-review-r1.md` (APPROVE, 3 NOTEs).

## Fixed

- "File-name references to reword undercounted" — Scope bullet and Technical context now name every single-file reference in `sandbox_coverage_test.go` (three assertion strings, the `t.Fatalf` in the read path, and the package/function doc comments), not just the three error-message strings.
- "Vacuous-single-file guard left optional" — the ≥2-files glob assertion is now a committed decision in both the glob Decision's rationale and the Testing section ("decided, not optional").
- "attach 'no interactive TTY' rationale is imprecise" — the attach Decision's rationale is restated as terminal-takeover collision (`launchAgent` inherits the operator's stdio) plus the need for human visual confirmation, grounding the second-terminal scenario wording.

## Pushed Back

(none)
