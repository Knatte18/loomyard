# Discussion fix — round 4

Source review: `_mill/reviews/20260823-100502-discussion-review-r4.md` (APPROVE, 0 blocking, 5 NITs).

## Fixed

- r4 "PushBranch closure's handle source contradicts the seam" (demoted from BLOCKING) — resolved by passing `pushBranch func() error` into `landingDeps` prebuilt over `drive.go`'s already-open handle, rather than constructing it from `l` inside the seam.
  The `two-opens-in-drive-rather-than-a-shared-handle` decision now states outright that exactly two `Open` sites exist and that the push closure must not add a third.
- r4 "Config load and Open errors in `drive` have no disposition" (demoted from BLOCKING) — both now refuse on the envelope, argued in `scalar-read-errors-refuse-or-defer-by-consumer` rather than left to analogy.
  The new hard-failure mode for `lyx loom drive` on an unreconciled hub is named explicitly as an accepted consequence, citing `landingshed`'s own package doc on why strict loading is required rather than chosen.
- r4 "\"performs no I/O\" vs the env read inside `landingDeps`" — resolved by adding a `pushSkipped bool` parameter, moving `EnvSyncOptions()` out to `drive.go`.
  The no-I/O claim is now literally true (no filesystem, git, or process-environment read), so no rewording was needed.
  Knock-on: the tier-1 drift guard can pass `pushSkipped: true`, which removes the earlier "every field except `PushSkipped`" carve-out entirely — that carve-out was an artefact of the older seam shape, and the Testing section now says so.
- r4 "No test named for the two new `Fabric` methods" — added an `internal/fabricengine` testing subsection stating both: `OriginURL()` folds one assertion into the `OpenParent` integration test;
  `PushBranch(opts)` gets no new integration test (a real push needs a remote the fixture lacks), with the `SkipPush`/`SkipGit` short-circuit as the cheap assertion and `TestEnforcement_FabricVocabulary` named as the load-bearing guard.
- r4 "`deps.go` comment count is off by one" — corrected to name the single `OpenFabric`/`OpenParentFabric` field-doc deferral plus its laziness wording, noting the second deferral lives in `wiring.go`, already listed separately.

## Pushed Back

None.
All five findings verified accurate against source;
none met the harm-check criteria for pushback.
