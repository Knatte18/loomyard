MILL_REVIEW_BEGIN
# Review: Reconsider the collapsed strand strip default size — holistic

```yaml
duration_s: 256.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-31
```

## Findings

### [BLOCKING:scope] Card 1 Requirements name apply.go:200 but omit apply.go from Context
**Location:** Batch 01-collapsed-strip-default, Card 1, Requirements step (4)
**Issue:** The instruction "do not touch ... the parallel comment at `apply.go:200`" names a specific location and content ("the parallel comment") in `internal/reedengine/apply.go`, but that file is listed in neither `Context:` nor `Edits:` for this card — the implementer has no told path to confirm what that comment says or why it's excluded.
**Fix:** Add `internal/reedengine/apply.go` to Card 1's `Context:` list, or drop the specific file:line reference and keep the boundary generic ("do not reword any other occurrence of this measurement").

All other criteria check out against source. Both templates' current line 5 is byte-identical (verified); `config_test.go` lines 61/62 and 138/139 match the quoted assertion/error-string text exactly; `doc.go`'s "Silent layout rescale" bullet spans lines 368–373 with the quoted phrase at 371–372 and the untouched `CollapsedStripRows` mention at 373, using the `//     ` continuation prefix as claimed. `apply.go:200`'s own comment names only field identifiers, no numeric literal, so leaving it untouched is correct — the finding above is about the missing Context entry, not a wrong instruction. `config.go`'s `LoadConfig`/`Config` struct and `configsync.ReconcileAll`'s key-based `Added`/`Removed` (never rewriting an existing key's value) both match the render/no-migration Shared Decision's rationale. The render package's `CollapsedStripRows: 2`/`3` unit inputs (rules_test.go, pins_test.go, height_test.go) and `lock_test.go`'s two `CollapsedStripRows: 2` fixtures are confirmed deliberately-chosen, unrelated to the template default. `mill-config.yaml`'s `pipeline.done_gate` is exactly `go test ./... && go test -tags integration ./...`, confirming the integration-tier-is-a-landing-gate decision needs no plan-side wiring, and `attachgeometry_integration_test.go` is confirmed value-agnostic (`e.cfg.CollapsedStripRows`, not a literal) at both assertion sites (lines 248-249, 367-368) and both `//go:build integration && linux` cases (100x30, then resize to 100x90). The Batch Index DAG (single batch, no cycle, file present), All Files Touched (alphabetical, matches the four Edits), Moves (`none`, no Rename mechanic needed), and the single-commit-for-value-plus-assertions-plus-comments-plus-doc.go Shared Decision are all well-formed and consistent with the source files.

## Verdict

REQUEST_CHANGES
One Context-completeness gap (apply.go referenced but not listed); otherwise plan is source-grounded and internally consistent.
MILL_REVIEW_END
