# Discussion fix — round 1

Source review: `_mill/reviews/20260809-122332-discussion-review-r1.md` (verdict APPROVE after class-ceiling demotion; 0 BLOCKING, 6 NIT).
Every finding verified accurate against source before fixing. No pushback.

## Fixed

- r1 — "Bare-`v3` residue contradicts the manifest's rejected alternative" (`[NIT:consistency]`, demoted from BLOCKING). Verified: `shed-followups.md:169` does reject keeping in-text `v3` as a historical label, and the four sites (`internal/planparser/validate.go:11`, `validate_test.go:240`, `parse_test.go:36`, `parse_test.go:53`) do carry it. Added Decision `bare-v3-label-in-scope` bringing all four into scope, moved the Scope bullet from **Out** to **In**, added acceptance gates 4 and 5, and recorded that the renamed doc needs no separate bare-`v3` pass because every occurrence in its body is already reached by `six-pattern-set`, `plan-format-5-in-scope`, or `v2-is-erased-repo-wide`.
- r1 — "Override-note list omits two recorded overrides" (`[NIT:decision]`, demoted from BLOCKING). Verified: note 1 covered four of B's departures, not `:188`, `:197` or `:199`. Rewrote note 1 as an eight-item list covering every instruction this task departs from — `:169`, `:188`, `:197`, `:199`, `:209–210`, `:216`, `:227`, `:228`.
- r1 — "Deleting `:5` leaves an orphan blockquote continuation line" (`[NIT:consistency]`). Verified against `docs/reference/plan-format-v3.md`: `:4` is a bare `>` between the two blockquote paragraphs. `plan-format-5-in-scope` now deletes `:4` and `:5` as a pair; the v2 erasure inventory and the hand-edit site list updated to say `:4–5`.
- r1 — "Gate 2's shed-followups filter is a content match, not a path match" (`[NIT:scope]`). Verified: `grep -v 'shed-followups.md'` filters on line content, so a real hit elsewhere citing that filename would be silently exempted. Gate 2 now uses `--exclude=shed-followups.md` and anchors the roadmap filter as `^\./manifest/roadmap\.md:18:`, with a sentence stating the exclusions must anchor on the path field.
- r1 — "No stated disposition for shed-followups' now-dangling links" (`[NIT:decision]`). Verified: `:53`, `:74`, `:192`, `:214` cite `plan-format-v3.md`. Added an "Accepted consequence" paragraph to `exclusion-set` accepting the stale citations, giving the reason (the file records what each task was told at scoping time), naming the override notes as where a reader learns the file moved, and exempting the file from acceptance gate 8.
- r1 — "Manifest's 'ten Go files' vs this task's 32" (`[NIT:consistency]`). Verified: `grep -rl 'gopkg.in/yaml.v3' --include='*.go' . | wc -l` returns 32; `shed-followups.md:197` says ten. Noted the correction in `yaml-v3-exclusion-is-structural` and added it to override note 1.

## Pushed Back

None. All six findings verified accurate, and no fix broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task's scope.

## Operator flag

Finding r1's fix reverses the operator's own earlier answer that bare-`v3` residue is out of scope. The reversal rests on two grounds the original answer did not have in view: `shed-followups.md:169` rejects that class explicitly, and the answer's stated premise ("this task changes paths and names, not prose") was discarded by the later repo-wide v2-erasure decision. Recorded in the Q&A log as superseded and flagged at handoff for the operator to reverse if they disagree.
