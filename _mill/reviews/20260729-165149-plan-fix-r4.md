# Plan fix report — round 4

Source review: `_mill/reviews/20260729-164747-plan-review-r4.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 1 NIT). Every finding was verified accurate against source and fixed per `mill-receiving-review`'s fix-everything default; all edits confined to `_mill/plan/` files. Zero pushbacks.

## Fixed

- [BLOCKING] "Card 11's new escalation path leaves lock-release unspecified and untested" — verified against `ensureserver.go`: steps 2/3/4–7 each release the spawn lock before returning/continuing, but the escalation's two new terminal paths (reuse-success `healthy == true`, and the deadline-guard `ErrServerSpawnTimeout`) had no release specified, so a leaked `gofrs/flock` handle would wedge every subsequent same-process `ensureSupervised` call — exactly the batch-mode case (`buildOptions` gives each positional arg the same `WorktreeRoot`/lock). Added an explicit **Lock release (required)** clause: every escalation return path releases the lock (simplest via `defer fileLock.Release()` right after acquiring it, avoiding double-release with steps 4–7). Added lock-release test coverage to `supervised_test.go`: (1) the existing uncontended-lock/deadline-timeout test now also asserts `lock.TryAcquireWriteLock(lockPath)` succeeds after the call; (2) a new **reuse-success** escalation test using a `spawnAndHoldSubprocess` PID (so a mis-taken wedged branch would kill the fixture, never the test process) and an in-process fake LSP unix-socket server (per `lspclient_test.go`'s `TestLSPClient_DialTransport_InitializeOverUnixSocket`) that fails the first probe and answers the second — driving step-1 finalize-failure → under-lock re-dial success, then asserting reuse (no `KillPID`, fixture PID alive) and a free lock afterward, deterministically and with no real gopls. Added `internal/codeintelengine/lspclient_test.go` to card 11's `Context:` for that fake-server pattern.
- [BLOCKING] "Card 12 references `TestEnsureNative_Integration` from a file absent from its Context/Edits" — verified `TestEnsureNative_Integration` lives in `ensureserver_integration_test.go`, which card 12 names (to justify not threading `binPath`) but doesn't list. Added `internal/codeintelengine/ensureserver_integration_test.go` to card 12's `Context:`.
- [NIT] "Shared Decision misattributes the `supervised_integration_test.go` doc fix to batch 4" — verified: the `supervised_integration_test.go` and `ensureserver_integration_test.go` header-comment fixes are batch 5's card 13 (no batch-4 card touches those files). Corrected the overview's "docs land in the same commit" Decision to attribute those two comments to batch 5 and extended its "Applies to" line to `batches 2, 3, 4, 5`.

## Pushed Back

- None.
