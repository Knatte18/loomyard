MILL_REVIEW_BEGIN
# Review: Launch ly-supervise and orchestrator via lyx reed add — holistic

```yaml
duration_s: 152.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-09-18
```

## Findings

### [NIT:consistency] Roadmap cross-reference uses bold-asterisk, not the file's backtick convention
**Location:** `manifest/roadmap.md:90,92`
**Issue:** The two updated Someday entries reference the newly-Done item as `now-Done **ly-supervise + orchestrator**` (double-asterisk bold), while every other cross-reference to a Done item in this same file (lines 20, 39, 86 — `now-Done \`Adopt quarry's glyph alphabet as the plan alphabet\``, `now-Done \`Real-Linux validation\``, `now-Done \`reed: watchdog daemon\``) uses backtick-code style; line 92 even mixes both styles in one sentence (bold for the ly-supervise item, backtick for `born-as-strand`).
**Fix:** Rewrite both references as `` `ly-supervise + orchestrator: launch via `lyx reed add`, not ad hoc` `` (or the file's established short-form backtick style) to match the convention the rest of the file already uses.

## Verdict

APPROVE
All three batches, cross-batch contracts, tests, and constraints check out against source; only a cosmetic markdown-style nit found.
MILL_REVIEW_END
