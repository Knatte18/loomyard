# Plan-fix round 6 — codeintel-v1

Source review: `_mill/reviews/20260729-091507-plan-review-r6.md` (verdict: REQUEST_CHANGES, 2 BLOCKING, 1 NIT).

## Fixed

- `_mill/reviews/20260729-091507-plan-review-r6.md` — "Batch mode ships with no CLI help-text update": added `Long`-text requirements to batch 10 cards 39 and 40, documenting the batch-mode JSON shape, exit-code ranking, and one example per verb (`refs`/`definition`'s four-way status set, `symbol`'s three-way set with no `ambiguous`/exit-2).
- `_mill/reviews/20260729-091507-plan-review-r6.md` — "Context completeness gaps recur across several cards": added `refs.go` to batch 07 card 27; `registry.go` + `detect.go` to batch 08 card 30; `definition.go` to batch 08 card 31; `refs.go` to batch 09 card 33; `refs.go` + `symbol.go` to batch 10 card 38.
- `_mill/reviews/20260729-091507-plan-review-r6.md` — "[NIT] ensureSupervised's retry loop doesn't specify true-error handling": added an explicit sentence to batch 06 card 24 that a genuine (non-nil) error from `readDaemonState`/`lock.TryAcquireWriteLock` aborts the loop immediately, distinct from the "not acquired"/"absent" retry branches.

## Verification beyond this round's named findings

Re-ran a refined version of round 5's scripted Context-completeness sweep (tighter identifier patterns, checked `refs.go`/`symbol.go`/`definition.go`/`detect.go` in addition to the `errors.go`/`lspclient.go`/`registry.go` set round 5 covered) across all 47 cards. Four residual candidates surfaced; all four were hand-verified as false positives and left unchanged: card 6 (script artifact, no actual match on manual re-grep), card 17 and card 27 (both purely descriptive/contrastive prose about a path *not* taken, not a construction — the same class of false positive identified and ruled out in round 5), and card 30 (the script does not check the `Creates:` field — card 30 *creates* `symbol.go` itself, defining `Symbol`/`SymbolMatch` rather than needing to read them from elsewhere). This is the second independent sweep (round 5's and this one, with refined patterns) to converge on zero remaining genuine gaps in this finding class.

## Pushed Back

None.
