# Plan fixer report — round 2

Source review: `_mill/reviews/20260717-174231-plan-review-r2.md` (verdict APPROVE; 2 NITs).

## Fixed

- `20260717-174231-plan-review-r2.md` → **[NIT] refs subcommand lacks positional-arg validation**: Card 13's Requirements now pin `Args: cobra.ExactArgs(1)` on the `refs` command, so bare `lyx codeintel refs` or a 2-arg call fails cleanly through the JSON envelope (matching Card 14's assertions).
- `20260717-174231-plan-review-r2.md` → **[NIT] "hub base" for LoadRegistry overlay is ambiguous**: Card 13 now names the exact field — `codeintelengine.LoadRegistry(layout.Cwd)`, passing the resolved `*hubgeometry.Layout`'s `Cwd` field, matching the established `internal/buildercli/cli.go:187` call `modelspec.LoadRegistry(layout.Cwd)` — and states explicitly that `ConfigFile(baseDir, "servers")` resolves `<baseDir>/_lyx/config/servers.yaml`, so `l.Hub` would silently miss every overlay. `internal/buildercli/cli.go` added to Card 13's `Context:` as the precedent.

## Pushed Back

(none — both NITs were accurate. Note: the reviewer suggested `l.WorktreeRoot`; the fix instead names `layout.Cwd`, the field the repo's only existing `modelspec.LoadRegistry` call site uses — the same worktree-rooted intent, matched to the actual in-repo convention rather than a plausible-but-unused field.)
