# Plan fix — round 2

Source review: `_mill/reviews/20260829-112102-plan-review-r2.md`

## Fixed

- `20260829-112102-plan-review-r2.md` — "[NIT:scope] Usage-error \">2 args\" branch untested".
  Added test 21 to batch 1, card 4: invoke with three arguments and assert exit status 2 specifically, byte-empty stdout, an empty call log, and `usage:` on stderr.
  Asserting the exact code rather than merely non-zero is deliberate — 2 is the code card 5 reserves for the usage error alone, and distinguishing it from the general failure code is the only thing that makes reserving it worth anything.
- `20260829-112102-plan-review-r2.md` — "[NIT:scope] Stub's \"unsupported invocation\" rejection path never directly exercised".
  Added test 22 to batch 1, card 4: invoke the stub directly with the shape `auth status`, bypassing `github-tree.sh`, and assert exit 98, `unsupported invocation` on stderr, and that the call log still gained a line.
  The log assertion is the part worth having beyond the reviewer's ask — the stub logs before it validates, and several other tests assert "the call log is empty" to prove a rejection happened before any `gh` call, so that ordering is load-bearing and until now was assumed rather than pinned.
  Updated the assertion count from twenty to twenty-two in batch 1's Batch Scope, in card 4's Requirements, and in batch 2's Batch Tests.

## Pushed Back

_None — both findings were accurate and neither fix carried a harm case._
