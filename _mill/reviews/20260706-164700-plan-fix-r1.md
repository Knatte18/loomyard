# Plan fix — round 1

Source review: `_mill/reviews/20260706-164612-plan-review-r1.md` (verdict APPROVE, 2 NITs).

## Fixed

- `[NIT] Batch 1 scope omits card 2→3 intermediate non-compile` — added a note to
  `01-effort.md`'s Batch Scope that card 2 widens `buildLaunchCmd`'s arity but
  leaves its `Prepare` call site to card 3, so `claudeengine` compiles only after
  card 3 and the batch verifies at the end (mirroring batch 2's wording).
- `[NIT] Card 9 "branch on Kind" is a no-op for classification` — reworded card 9's
  Requirements in `02-ask-signal.md` to keep the existing two-way
  `allOutputFilesExist → done else asking` branch (NOT a `Kind` switch, which would
  be dead since both non-done kinds classify identically); `Kind` stays a
  parse-time discriminator that selects the message source in `events.go`.

## Pushed Back

_None. Both NITs were accurate and harmless to apply._
