MILL_REVIEW_BEGIN
# Review: fabric: cutover -- rewire consumers onto fabric, delete warp/weft — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-26
```

## Findings

### [BLOCKING] SANDBOX-FABRIC-SUITE.md still names the deleted `warp clone` CLI verb
**Location:** `tools/sandbox/SANDBOX-FABRIC-SUITE.md:163`
**Issue:** F1's Watch text reads "...fabric's uniform branch-suffix scheme applies from the very first pair, unlike `warp clone`'s mirrored (identical) branch names." `warp clone` is a deleted CLI verb (not the legitimate `Warp`/weft-repo-role API terms the Shared Decisions preserve), and this file is explicitly in card 24's Edits list for exactly this class of stale-reference sweep. Card 27's own Tier-2 rule for non-`.go` docs states a remaining hit is "only acceptable if it is a legitimate weft-repo/role description, not a deleted-module name" — naming the removed `lyx warp clone` command fails that test.
**Fix:** Reword to describe the branch-naming difference without naming the deleted CLI verb, e.g. "...unlike the pre-fabric mirrored (identical) branch-naming convention."

## Verdict

REQUEST_CHANGES
One stray reference to the deleted `warp clone` CLI verb survives in a card-24-edited sandbox doc.
MILL_REVIEW_END
