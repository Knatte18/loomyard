# Plan fix report — round 4

Source review: `_mill/reviews/20260801-082616-plan-review-r4.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT). Both VERIFIED accurate against the code and FIXED.

## Fixed

- [BLOCKING] "Four per-pair `fabric.yaml`-base call sites never migrated to the repo-wide base" — CONFIRMED via `grep`: six `junctionNames(filepath.Join(<pair weft base>, RelPath))` sites exist; round-1/2/3's card 7 migrated only two (reconcile.go:161, :341). The other four — `junctionRepointedDetail` (reconcile.go:390), `PairInSync` (drift.go:92, loom preflight), `Topology.Checkout` (checkout.go:156), `Topology.Remove` (remove.go:104) — would each read a per-worktree `fabric.yaml` that batches 3-4 stop materializing, breaking `checkout`, loom preflight, `remove`, and reconcile Detail on every worktree. Fixed card 7 to migrate all six sites to `RepoWiredNames(<layout>)`, adding `drift.go`/`checkout.go`/`remove.go` to its Edits (and All Files Touched); extended card 10 with regression coverage exercising `PairInSync`/`Checkout`/`Remove` against a repo-wide-only (`BoardDir`) config fixture, not a per-pair `SeedConfig`.
- [NIT] "Card 15 self-qualifies calls with the enclosing package's own name" — CONFIRMED: `clone.go` is `package fabricengine`, so `fabricengine.CommitWeftAt`/`PushWeftAt`/`SyncOptions{}` won't compile. Fixed card 15 to use the unqualified forms (matching card 23's `Unwire`); also fixed the same defect in card 16 (`fabricengine.WiredNames`/`WireJunctions` → unqualified), which the review did not flag but shares the defect.

## Pushed Back

(none — both findings were factually accurate and fixing them caused no harm.)
