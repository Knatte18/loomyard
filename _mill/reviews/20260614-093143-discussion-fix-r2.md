# Discussion fix — round 2

Source review: `_mill/reviews/20260614-093045-discussion-review-r2.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- [NOTE] "remove early-return vs. portal teardown ordering" — Scope → `worktree remove`: stated that portal/launcher teardown is sequenced before/independently of the `remove.go:40-42` target-exists check, so it still runs when the worktree dir is gone.
- [NOTE] "rollback step self-failure unspecified" — Decisions → transactional-add-rollback: added that rollback is best-effort, continues through all steps on partial failure, and surfaces the original add error (rollback-step errors logged, never masking the root cause).
- [NOTE] "HealthCheck 'readable/parseable' is partly contradictory" — Decisions → board-is-sole-tasks-reader + Testing → board HealthCheck: pinned the contract to "stat the board dir + open/read `tasks.json`, no JSON unmarshal" and aligned the test wording (a corrupt-but-readable file passes health-check).

## Pushed Back

(none)
