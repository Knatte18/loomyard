# Plan-fix report — round 1

Source review: `20260801-165253-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 4 NIT).

## Fixed

- [BLOCKING] "Card 7 missing index.go from Context" — verified accurate (`ErrStaleSHA`/`ErrNoCorrespondence` are defined in `internal/fabricengine/index.go`, not `revert.go`). Added `internal/fabricengine/index.go` to Card 7's `Context:` (02-fabric-pull.md).
- [BLOCKING] "Card 12 leaves a stale, directly-contradicted SHAExists claim unfixed" — verified accurate (the "## Warp-rebase and remote-reconcile" section's "Detection is already honest and shipped … `SHAExists` catches this" paragraph flatly contradicts the slice's reachability-not-existence decision, in the same file Card 12 already edits). Extended Card 12 part (2) to correct BOTH the section paragraph and the slice-6 build-order line (04-docs-sandbox.md).
- [NIT] "Card 9's git-log record-separator placement likely misaligns SHA/paths" — verified accurate (`--name-only` appends paths after the `--format` output, so a trailing record separator misassigns paths). Added an explicit separator-placement caveat to Card 9 requiring the record separator to LEAD each commit block, cross-checked by card 10's `TestPull_IdentifiesPatternResidue` (02-fabric-pull.md).
- [NIT] "PATTERN residue pathspec is not RelPath-scoped" — verified accurate (the real on-disk location is `RelPath/_pattern`). Documented the `RelPath`-blind limitation in Card 9 as an explicit godoc note, consistent with the accepted `relpath-is-dot-for-slice-2` precedent the slice already relies on (02-fabric-pull.md).
- [NIT] "Card 3's gogit.go comment fix perpetuates a pre-existing inaccuracy" — verified accurate (`gogit.go`'s go-git-lookup list wrongly includes the CLI-bound `hasUnpushed`). Changed Card 3 to a CONTENT-level fix: remove `hasUnpushed`/`HasUnpushed` from that parenthetical list entirely rather than case-renaming a still-wrong entry (01-gitrepo-primitives.md).
- [NIT] "Fetch has no explicit error-path test" — verified accurate (`Pull` has an error-path test; Card 1 required only Fetch's positive path). Added a `TestFetch_NoRemoteConfigured_*` requirement to Card 1 mirroring `Pull`'s no-stderr-leak error-path assertion (01-gitrepo-primitives.md).

## Pushed Back

_None — every finding was accurate, fixable within the plan files, and harm-free._
