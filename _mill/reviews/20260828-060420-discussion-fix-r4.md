# Discussion fix — round 4

Source review: `_mill/reviews/20260828-060256-discussion-review-r4.md` (verdict APPROVE, 1 NIT — demoted from BLOCKING by the blocking-class ceiling).
The finding was verified against source before being applied; it was not pushed back.

## Fixed

- `20260828-060256-discussion-review-r4.md` — "[NIT:scope] Broken-fixture inventory misses landingshed's own tests": confirmed `internal/landingshed/finalize_integration_test.go:167-193` builds a `landingshed.Deps` literal, calls `landingshed.NewFinalize(deps)` and then a real `fz.Call(ctx)`, so it breaks on both new gates and needs an artifact on disk; confirmed `internal/landingshed/commitstatus_test.go:52,74,99` builds `&Finalize{...}` from the shared `newFinalizeDeps(t)` helper (`finalize_test.go:52`) and calls `Call`, beyond the nine literals in `finalize_test.go`. Rewrote the broken-fixture inventory: the helper is now named as the single site the in-package fix belongs in (covering all twelve `Call`-invoking literals), and `finalize_integration_test.go` is called out separately as the one site that bypasses the helper, with its own remediation.

## Pushed Back

_None._
