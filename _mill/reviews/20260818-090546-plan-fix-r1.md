# Plan fix — round 1

Source review: `_mill/reviews/20260818-090150-plan-review-r1.md`

## Fixed

- `20260818-090150-plan-review-r1.md` — "[BLOCKING:scope] Card 8 misses a live `e.layout` field access in engine_test.go".
  Verified accurate: `internal/burlerengine/engine_test.go:538` reads `e.layout.AnchorPath()` off the constructed `*Engine` inside `TestEngine_Run_PatternDirectiveReachesInstruction1`, and it is the only `.layout` reference in the package outside the sites cards 6 and 8 already covered.
  Card 8's Requirements now names that site explicitly and specifies the rewrite as `e.geom.AnchorPath` — field access, no call parens.
- `20260818-090150-plan-review-r1.md` — "[NIT:consistency] Card 15's stated call-site count is off (33 claimed, 31 actual)".
  Verified accurate: `grep -c "newTestLayout(t)" internal/perchengine/run_test.go` returns 31.
  Card 15 now says thirty-one.
- `20260818-090150-plan-review-r1.md` — "[NIT:consistency] Card 21 / batch-3 grep note undercounts and mis-locates hits".
  Verified accurate on both halves: `cmd/lyx/notransients_test.go` carries six lines with `perchengine.RunsDir`/`ScratchDir` calls (seven call expressions, since the `mirroredPairs` row carries both), and `internal/perchengine/doc.go` contains zero occurrences of the literal string `lyxcwd`.
  Card 21 now states six lines / seven call expressions and keeps its already-correct enumeration; batch 3's `## Batch Tests` grep note now names cards 12 and 13 as the only two places a hit could survive, and explains that card 14's `doc.go` is edited for accuracy rather than for the grep.

## Pushed Back

_(none — every finding verified accurate and none of the fixes caused harm under the receiving-review decision tree.)_
