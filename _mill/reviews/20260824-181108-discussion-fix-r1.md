# Discussion fix — round 1

Source review: `_mill/reviews/20260824-181040-discussion-review-r1.md`

## Fixed

- `20260824-181040-discussion-review-r1.md` — "Archive-name helper's exact signature left unspecified": named the helper explicitly as `planparser.ArchiveDirName(stamp, suffix string) string`, gave it a Go signature block with its doc comment in the `planparser-declares-the-archive-name-loomshed-moves-the-files` Decision, and stated the division of labour concretely (the helper formats nothing and stats nothing; `loomshed`'s `planWrite` formats the stamp, walks the collision suffixes, and does the `os.MkdirAll`/`os.Rename` calls). Propagated the name into the Scope bullet and the Testing section.

## Pushed Back

None.
