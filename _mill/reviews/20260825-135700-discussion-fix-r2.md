# Discussion fix — round 2

Source review: `_mill/reviews/20260825-135412-discussion-review-r2.md` (holistic, APPROVE, 0 BLOCKING / 3 NIT).

## Fixed

- r2 holistic — "[NIT:scope] Guard's seam half accepts any seam value": the guard decision now requires the partner `Bouncer`'s `commit_seam` to *equal* the overlay subdirectory the Burler's target paths live under (`_lyx/discussion/…` → `discussion`, `_lyx/plan` → `plan`), not merely to be non-empty, and names three shapes that fail loudly rather than being skipped — no partner `Bouncer` row for the segment, a partner carrying no `commit_seam`, and target paths straddling two overlay subdirectories.
  Testing section gained the matching wrong-value and loud-failure scenarios, plus the note that negative cases feed synthetic recipe YAML through `shedbuild.Parse` rather than mutating the shipped bytes.
- r2 holistic — "[NIT:consistency] loom.md already asserts a contradicting claim": verified against `manifest/designs/loom.md`'s black-box paragraph in "The gate", which does state "only the review *profile* (rubric + fasit) differs per phase".
  That sentence is now named as a **rewrite** site in both the docs Decision and the Scope "In" list, so the plan writer corrects it rather than appending the new record beside a false claim.
- r2 holistic — "[NIT:consistency] \"Tests that encode the current value\" mis-scopes the sweep": the inaccurate "every other occurrence is a `burlerengine` or `burlercli` fixture" sentence is replaced with the accurate enumeration — `internal/burlerengine`'s own tests, `internal/burlercli` (`run.go` usage example, `cli_test.go`), `internal/shedrecipe/entries_burler_test.go`'s synthetic config maps, and `manifest/designs/hardener.md`'s future Tenter round (warp source, correct, stays).

## Pushed Back

(none)

## Note on one cited location

The finding also named `internal/shedadapters/burler_test.go` as carrying a `fix-scope: source` / `FixScopeSource` occurrence; a grep of that file returns no match, so it is omitted from the rewritten enumeration.
The finding's substance is unaffected and was fixed in full — the original sentence was inaccurate, and the two other homes it named (`internal/shedrecipe/entries_burler_test.go`, `manifest/designs/hardener.md`) both check out.
