# Fixer report: plan-fix round 1 — reed-pane-reap-consistency

Source review: `_mill/reviews/20260828-145700-plan-review-r1.md` (holistic, REQUEST_CHANGES, 4 BLOCKING + 3 NIT).
Every finding was verified against the real source files before acting;
all seven were accurate, and all seven were fixed.

## Fixed

- r1 holistic, `[BLOCKING:design]` "Card 12 judges files batch 4 owns, without depending on it" — confirmed: card 12's sweep spans `internal/reedcli/*.go`, which the `*.go` glob matches for the three `*_test.go` smoke files batch 4 rewrites. Batch 3 now carries `depends-on: [2, 4]` in both the overview Batch Index and its own frontmatter, and both batches' Scope paragraphs were restated (batch 3 runs last; batch 4 no longer claims parallel-safety with it).
- r1 holistic, `[BLOCKING:design]` "Card 9 forbids fixing a comment that does assert the deleted seam" — confirmed at `internal/reedengine/state.go:41-48`: `ReedState.HeaderPaneID`'s comment names the header as excluded from "every strand accounting, adoption, reconcile, and layout path", which is pane adoption, the twin of `doc.go`'s header-invariant sentence card 8 rewrites. The blanket prohibition was replaced by a per-field instruction: rewrite the `HeaderPaneID` clause, leave the `PaneGeneration` clause alone. Card 12's survivor rule was narrowed in the same pass so it no longer waves the `HeaderPaneID` hit through as generation-probe prose.
- r1 holistic, `[BLOCKING:scope]` "Card 3's Context carries no file showing `e.tmux.execHook`" — confirmed: the field is declared in `internal/reedengine/overlay.go`, and `lock_test.go` (card 3's only test file) never touches it. Added `internal/reedengine/overlay.go` and `internal/reedengine/lifecycle_test.go` to card 3's `Context:`, and named both in the Requirements prose so the implementer knows which one carries the declaration and which the usage shape.
- r1 holistic, `[BLOCKING:scope]` "Card 13 under-enumerates the stale text in the foreign-pane test" — confirmed: beyond the two comments the card named, `TestSmokeUpWithOnlyForeignPanesKeepsSessionUsable` carries two "not-yet-adopted initial pane" comments whose pane counts (2 and 3) become 1 and 2 post-fix, and its doc comment quotes the old `"session has no panes to adopt or split"` string that card 4 explicitly deferred to a later batch with no card claiming it. Card 13 now enumerates all four sites plus the error-string quote.
- r1 holistic, `[NIT:decision]` "`lifecycle_test.go`'s initial-pane comment has no disposition" — confirmed: the discussion assigned it "read and confirm, likely out" and neither sweep term appears in the comment, so card 12 could not reach it. Card 12 now closes it explicitly, with the expected "confirmed out" disposition and the condition under which it would instead be a missed rewrite. `internal/reedengine/lifecycle_test.go` added to card 12's `Context:`.
- r1 holistic, `[NIT:consistency]` "Card 8 misattributes doc.go's \"each adopt the other's panes\" passage" — confirmed at `internal/reedengine/doc.go:223`: the clause belongs to the session-name-rewrite bullet, about two sibling worktrees colliding on one session name, and `doc.go` carries no `adoptPaneGenerationLocked` prose at all. Card 8's "do not touch" pointer now identifies the bullet correctly and warns against hunting for a generation-probe paragraph that does not exist.
- r1 holistic, `[NIT:scope]` "Batch 4's `-run` filter omits a neighbouring test whose behaviour changes" — confirmed: `TestSmokeUpSurvivesAScrubbedStateFileWhileTheSessionIsUp` shares a file with cards 13-16 and its pane set goes from three to one under the new reap. Added to batch 4's `verify:` alternation. Its load-bearing assertion (rebuilt header at `pane_top` 0) still holds for a sole full-height header, so card 16 requires it read and confirmed rather than rewritten, with comment corrections scoped to any pane set or layout precondition the reap falsifies.

## Pushed Back

_None._
All seven findings were factually accurate, and no fix broke existing functionality, conflicted with a documented decision in `_mill/discussion.md` or `CONSTRAINTS.md`, or destabilised work outside this task's scope.

## Post-fix validation

`_plan_dag.validate` passes.
`_plan_validate.run` returned one `context-completeness` finding after the first fix pass (card 12's new `lifecycle_test.go` reference), fixed by adding the path to that card's `Context:`;
the gate then returned zero findings with an empty `skip_checks`.
