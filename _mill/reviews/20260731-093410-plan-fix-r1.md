# Plan fix — round 1

Source review: `_mill/reviews/20260731-092703-plan-review-r1.md`

## Fixed

- `20260731-092703-plan-review-r1.md` — "[BLOCKING] Card 12's per-branch-scoping case needs a fixture not in its Context". Verified and found sharper than reported: `newFabricFixture` lives in `reconcile_stale_registration_test.go` in `package fabricengine_test`, while `index_integration_test.go` — whose helpers Card 12 directs the implementer to reuse — is `package fabricengine`, so `Topology.Checkout`'s fixture is not merely heavier, it is unreachable from Card 12's own test package. Took the reviewer's second option: the case now switches the weft worktree's branch with a plain `git checkout -b` through `lyxtest.MustRun` inside the existing lightweight fixture, with both reasons stated in the card (the package boundary, and that `SnapshotWarpSHA` scans only the weft worktree's current branch, so a weft-side switch *is* the mechanism under test while the coordinated checkout is only how that state arises in production). Dropped `internal/fabricengine/checkout.go` from Card 12's `Context:`, which is no longer read.
- `20260731-092703-plan-review-r1.md` — "[NIT] Card 19's unborn-weft fixture has no named construction pattern". Verified: every other fixture instruction in batch 4 names a helper or file to mirror, and this one did not. Card 19 now names the construction explicitly — a `newUnbornWeftRepo` helper mirroring `newUnbornWarpRepo`'s `t.TempDir()` + `git init -q -b main` + the two git configs — and states why `lyxtest.CopyWeft` cannot serve, since its fixture already has commits and so cannot reach the unborn state.

## Pushed Back

_None._
