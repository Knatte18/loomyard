# Plan fix — round 5

Source review: `20260813-062834-plan-review-r5.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 1 NIT).

## Fixed

- `20260813-062834-plan-review-r5.md` — "[BLOCKING:scope] hubforge never added to the Fabric Vocabulary owner set": verified accurate and fixed in `02-fabrictest-dissolution.md` card 12.
  Confirmed against `internal/lyxcwd/enforcement_test.go`: `fabricVocabularyOwners` is an exact-directory-string map with no prefix matching, scanning non-`_test.go` files under `internal`/`cmd`, and `internal/hubforge/hub.go` lands as untagged production code carrying `WarpBare`, `WeftBare`, `PrimeWeft`, `PairWarpWorktree` and `PairWeftSibling`.
  Card 12 previously said outright that `internal/hubforge` "does **not** replace" the retired `fabrictest` entries — that was my error, and it would have failed `TestEnforcement_FabricVocabulary` at batch 2's own `verify:`, which runs `go test -tags integration ./internal/lyxcwd/...`.
  Card 12 now requires `"internal/hubforge": true` in `fabricVocabularyOwners`, states why it is load-bearing rather than housekeeping, and specifies the explanatory-comment shape to carry over from the retired entry.
- `20260813-062834-plan-review-r5.md` — "[NIT:consistency] weftname-import subset left conditional against CONSTRAINTS.md's unconditional claim": fixed by taking the finding's first option — card 12 now adds `"internal/hubforge": true` to `weftnameImportOwners` unconditionally rather than "only if the compiler says it is needed".
  The reasoning is recorded in the card: the map is an allowlist of what *may* import `weftname`, not a record of what does, so an entry with no current importer matches `CONSTRAINTS.md`'s stated subset rather than being stale.
  I did not take the second option (declaring `CONSTRAINTS.md` aspirational), because `CONSTRAINTS.md` was deliberately written ahead of the code for this task and making the code match it is this task's definition of done.

## Also fixed (same finding's other end, not separately raised)

- `11-helper-deletion.md` card 67 previously instructed the implementer to remove `internal/gitkit`'s `weftnameImportOwners` entry if deleting `buildWeftPrime`/`buildWeftOnly` left `weftname` unimported.
  That instruction contradicted the same landed invariant from the other direction, so it is reversed: the Go import may go, the allowlist entry stays, and the card now says why.

## Pushed Back

_None._
