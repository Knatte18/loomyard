# Plan fixer report — round 3

Source review: `_mill/reviews/20260802-140903-plan-review-r3.md` (holistic).

## Fixed

- `[BLOCKING] verify: never re-runs the Hub Geometry literal guard after render.go changes` — verified accurate: `TestEnforcement_GeometryLiterals` only executes when `./internal/hubgeometry/...` is a test target, and batch 2 + the overview verify omitted it, so Card 3's `renderCardPointers` "no literal `_lyx`" requirement was unchecked after batch 1. Fix: added `./internal/hubgeometry/...` to batch 2's frontmatter verify, the overview's batch-2 DAG verify, and the overview's module-wide `verify:`; updated batch 2's `## Batch Tests` prose to explain the guard re-scan.
- `[NIT] Unused plan parameter on both renderers` — verified: neither renderer reads any field of `plan` after the split, and no near-term use is anticipated; both call sites are edited in this same card. Fix: dropped `plan *planparser.Plan` from both `RenderForkPrompt` and `RenderRecoveryPrompt` (new signature `(batch, prevDigest, reportPath, l, selfFixCap)`); updated the `beginbatch.go:218` and `recoverbatch.go:216` call sites to drop `deps.Plan`; added a `template_test.go` bullet to update the test call sites and remove the now-dead `testPlan` helper.
- `[NIT] Card 2's prose orders implementer-body.md's sections differently from the cited source range` — verified: the enumeration listed `## Your cards` before the FRESH-READ rule, contradicting source order. Fix: reworded the implementer-body requirement to state the source's lines-43–82 section order is preserved verbatim, and enumerated the five sections in that exact order (FRESH-READ → Prior-batch context → Your cards/per-card loop → bounded self-fix → batch-report).

## Pushed Back

- (none)
