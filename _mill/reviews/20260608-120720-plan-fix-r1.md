# Plan-fix report — round 1

Source review: `_mill/reviews/20260608-120610-plan-review-r1.md` (holistic)

## Fixed

- [BLOCKING] "Config struct missing yaml struct tags for ProposalPrefix" — card
  5 (batch 2) now pins explicit yaml struct tags on `Config`
  (`path`/`home`/`sidebar`/`proposal_prefix`) with a note that yaml.v3 would
  otherwise marshal `ProposalPrefix` as `proposalprefix`, matching the
  discussion `config-schema` keys consumed by cards 18/20.
- [NIT] "Card 2 ambiguously references cli.go for a non-existent struct-field
  rename" — card 2 (batch 1) reworded: the `wikiPath` struct field lives only in
  `board.go`; `cli.go`'s like-named local variable is explicitly left unchanged.
- [NIT] "`go.sum` listed in both Context and Edits for card 5" — removed `go.sum`
  from card 5's `Context:` (kept in `Edits:`).

## Pushed Back

(none)
