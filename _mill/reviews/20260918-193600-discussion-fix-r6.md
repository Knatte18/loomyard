# Discussion fix — round 6

Source review: `_mill/reviews/20260918-193404-discussion-review-r6.md` (APPROVE, 2 NITs, 1 demoted from BLOCKING).

## Fixed

- `20260918-193404-discussion-review-r6.md` — "`_mill/` swept in by the whole-tree rule, no disposition": confirmed `_mill/` is tracked (`.gitignore:22` excludes only `**/_mill/*.active`) and that `crucible/` and `docs/research/` both exist. Added an explicit carve-out to the Scope rule excluding those three trees wholesale, before classification, with the reason stated — these files record what a task decided at the time, so rewriting them falsifies the record rather than updating it. Drew the distinction against `historical-prose-rewritten-not-glossed`, which governs documents asserting what the tree currently is. Also clarified that the "~233 hits across ~57 files" figure is the post-carve-out in-scope count, not the raw grep count.
- `20260918-193404-discussion-review-r6.md` — "`/proc` argv probe loses its unique discriminator": verified `findDriverPIDs` (`internal/loomcli/smoke_test.go:249-279`) filters on `cwd == worktree` plus a lone argv element `"drive"`, and that `run` is a verb on `shuttle`, `burler`, `webster` and loom. Rewrote disposition 2 to require the adjacent `"loom"` + `"run"` argv pair rather than a lone `"run"` element, and extended the rationale paragraph to name the over-match hazard alongside the under-match one it already covered.

## Pushed Back

(none)
