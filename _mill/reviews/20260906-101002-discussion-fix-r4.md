# Discussion fix — round 4

Source review: `_mill/reviews/20260906-100759-discussion-review-r4.md` (reviewer `opus`, effort medium, verdict REQUEST_CHANGES — 2 BLOCKING, 4 NIT after the blocking-class ceiling demoted three).

Every finding was verified against the tree before acting. All six were accurate; none were pushed back.

## Fixed

- r4 — "`root:` shorthand × glyph canonicalization unordered" (`[BLOCKING:design]`) — verified `normalizeRefIfPath` (`internal/planparser/normalize.go:88`) is classifier-gated and carries its own comment naming the regression the gate prevents.
  Added a new decision `root-and-canonicalization-order`: `root:`/`//` resolve first while the ref is still path-shaped, canonicalization runs after, and a surface glyph is always repository-root-relative and never `root:`-joined.
  Recorded the consequence explicitly — a bare-filename surface glyph under a non-`.` root names a repo-root file and fails loudly at `path-missing`/`Resolve` rather than resolving to the wrong file silently.

- r4 — "`RewriteRefs` keyspace vs. on-disk surface spelling" (`[BLOCKING:design]`) — accurate and sharp: the model holds canonicalized root-resolved glyphs while `_lyx/plan/` bytes hold whichever of the three spellings `file-spelling` accepts.
  Extended `rewrite-write-path` with a **Keyspace** bullet: keys are canonical model strings, and the parser retains each ref's surface lexeme beside its canonical form so the writer can locate the exact bytes.
  Named the failure mode this prevents — a drift repair reporting success while silently skipping refs it could not byte-match.

- r4 — "`prosa-symbol-target` disposition unstated" (`[NIT:decision]`, demoted from BLOCKING) — verified `checkProsaSymbolTarget` (`internal/planparser/validate.go:491`) flags any `Prosa` ref where `isPathRef` is false, which after canonicalization is every ref.
  Added a new decision `prosa-target-rule`: redefined in glyph terms rather than retired — file self glyphs and unit self glyphs pass, member glyphs are the finding, and `language: none` keeps today's behaviour unchanged.
  Noted that the unit self glyph must be on the allowed side or the rule would contradict `package-spelling`.

- r4 — "Scope claims new producer rows; `drift-boundaries` denies them" (`[NIT:consistency]`, demoted from BLOCKING) — a contradiction introduced by round 3's own fix: `drift-boundaries` was corrected but the Scope and Testing bullets still promised new rows and new parity pairs.
  Rewrote both to match: the `Plan-Revalidate` row and its verb are extended together, the two webster boundaries are in-`websterengine` changes behind existing bracket verbs, and no new row, verb, or parity case appears.

- r4 — "Contract-doc inventory omits two affected stencils" (`[NIT:scope]`, demoted from BLOCKING) — verified both citations.
  Added `contracts/stencils/webster/webster-body-implementer.md` (its deviation-union paragraph instructs the implementer to resolve a package-qualified symbol to its file itself, invalidated by glyph refs and superseded by the mechanical scope guard) and `contracts/stencils/loom/loom-rubric-plan-review.md` (names `prosa-symbol-target` and per-symbol granularity guidance) to the same-commit docs inventory.

- r4 — "`DeltaGit` token catches only direct textual call sites" (`[NIT:consistency]`) — accurate; the token narrows the gap but cannot close it, since a test calling a `planglyph` wrapper contains no such substring.
  Added a **Stated limit, not a claimed cure** bullet to `quarry-spawn-guard-visibility` recording the limit and instructing that a later change must not restate it as full coverage.

## Pushed Back

None.

## Gaps auto-picked

Two BLOCKING findings, both resolved by auto-picking the recommended option per the operator's standing instruction. Both are logged in `discussion.md`'s `## Q&A log` with the `[auto-pick]` marker and rationale.
