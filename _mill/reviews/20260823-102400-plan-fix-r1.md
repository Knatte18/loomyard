# Plan fix — round 1

Source review: `_mill/reviews/20260823-102022-plan-review-r1.md`

## Fixed

- r1 `[BLOCKING:scope]` "Card 6's Context omits planparser/validate.go" — added `internal/planparser/validate.go` to batch 3 / card 6's `Context:`, so the implementer can read `checkCommitSubjectMismatch`'s required prefix form rather than inferring it from the testdata fixture alone.
- r1 `[NIT:consistency]` "`zero-argument-verbs` decision has no locatable source in the plan" — reworded batch 3 / card 5 to "per the discussion's `zero-argument-verbs` decision", matching card 8's phrasing for `parity-tests-per-gate`.
- r1 `[NIT:consistency]` "go-conventions-and-verify-scope's stated rule doesn't cover its own CONSTRAINTS.md trigger" — extended the `go-conventions-and-verify-scope` Shared Decision to name `CONSTRAINTS.md` as a trigger for the `./internal/lyxcwd/...` scope addition, with the source-side-only rationale spelled out.

## Pushed Back

_None._
