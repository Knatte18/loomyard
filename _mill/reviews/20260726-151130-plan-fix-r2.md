# Plan fixer report — round 2

Source review: `_mill/reviews/20260726-150527-plan-review-r2.md`

## Fixed

- r2 review, "[BLOCKING] Card 3 moves state_test.go/roundfiles_test.go
  whole; neither compiles" — verified: `TestProfileHash`/`TestDeriveRunID`/
  `TestValidRunID` live in `state_test.go` and
  `TestBuildRoundProfile_FieldMapping` in `roundfiles_test.go`, all with
  perch-side subjects. Card 3 now extracts those tests verbatim into new
  `internal/perchengine/identity_test.go` and
  `internal/perchengine/adapter_test.go` at move time
  (rename-plus-extraction on the test side), names exactly which tests
  remain in each moved file, and the overview's All Files Touched gained
  both new files.
- r2 review, "[BLOCKING] Card 8 requires 'EXACTLY TWO' files while also
  requiring the old 'EXACTLY ONE' pin stay intact" — verified the three
  `EXACTLY ONE` pins in `template_test.go`. Card 8 now explicitly replaces
  the circling/milestone `EXACTLY ONE` pins with the two-file pin (triage's
  pin untouched) and scopes the keep-intact instruction to every OTHER
  pinned statement.
- r2 review, "[NIT] Card 10's pre-round targeting walk doesn't compose with
  judgeReadSet's signature" — card 7 now names the shared helper
  explicitly (`latestValidHandoff(rounds)` taking only completed round
  records, with `judgeReadSet` layered on top) and card 10 reuses it by
  name pre-round.
- r2 review, "[BLOCKING] Card 12's central mechanism claim is false:
  configengine.Load ignores unknown keys" — verified:
  `configengine.Load` checks only `yamlengine.MissingKeys` and
  `LoadConfig` uses plain `yaml.Unmarshal`. Card 12 now states the correct
  mechanism — a strict `yaml.Decoder.KnownFields(true)` decode in
  `LoadConfig` (mirroring `decodeProfile`) is what rejects the leftover
  `judge_effort` key — and adds `internal/configengine/config.go` +
  `internal/yamlengine/reconcile.go` to Context.

## Pushed Back

- (none)
