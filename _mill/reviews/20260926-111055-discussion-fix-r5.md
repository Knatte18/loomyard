# Discussion fix — round 5

Source: `20260926-111027-discussion-review-r5.md`.

## Fixed

- "batten llm-child teardown race left undispositioned" — Scope Out now states the `child_driver: llm` exposure explicitly: pre-existing, not widened, deferred to `shed-llm-driver` (#28); this task covers the shipped default `child_driver: go`.
- "Blocking friction-lock wait is not cancellable" — the wait-on-lock decision now accepts the non-cancellable wait in writing, with its bound and why a context-aware acquire alone buys nothing (Reflect itself is non-cancellable).
- "ly-drive \"near a hundred steps\" worst case also moves" — the row-count decision records that the worst-case figure and the 120 cap were checked and need no edit.

## Pushed Back

(none)
