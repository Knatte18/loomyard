# Plan fix — round 4

Source review: `_mill/reviews/20260814-173551-plan-review-r4.md` (REQUEST_CHANGES, 2 BLOCKING, 0 NITs).

## Fixed

- r4 / "[BLOCKING:scope] Card 9's Context omits junctionnames.go, needed for fabricengine.HubScratchDir" — verified: card 9's Requirements assert against `fabricengine.HubScratchDir(res.HubPath)`, which is declared in `internal/fabricengine/junctionnames.go` (card 1's own `Edits:` target), and that file was absent from card 9's Context while sibling card 8 lists it. Added `internal/fabricengine/junctionnames.go` to card 9's Context.
- r4 / "[BLOCKING:scope] Card 19's import-pruning instruction omits `os`, leaving an unused import" — verified against source: the surviving case `TestBoardJunction_ExcludedFromPathspecRoutes` (`boardjunction_integration_test.go:307-332`) references only `slices`, `filepath`, `testing` and `fabricengine`; the five deleted cases are the file's only `os` users. The card's fixed drop-list would have left an unused `"os"` import, failing to compile and breaking batch 2's `-tags integration` verify. Card 19 now instructs pruning against actual usage rather than a fixed list, and names `os` alongside `fslink`, `gitexec` and `lyxcwd` as what goes at planning time.

## Pushed Back

_(none — both findings were factually accurate against source, and neither fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task.)_
