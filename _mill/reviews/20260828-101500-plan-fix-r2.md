# Plan fix — round 2

Source review: `_mill/reviews/20260828-100823-plan-review-r2.md`

## Fixed

- r2 "[BLOCKING:scope] Card 5's Context omits the Engine/Geometry definitions it requires" — verified: `Engine`/`New` live in `internal/reedengine/lock.go`, `Geometry` in `internal/reedengine/geometry.go`, and `newTestEngine` in `internal/reedengine/lock_test.go`. All three added to card 5's `Context:`, and the `resizeSignalPath` requirement now names the fixture shape explicitly (`New(Config{}, Geometry{AnchorPath: ...})`, or reuse `newTestEngine` and override `e.geom.AnchorPath`).
- r2 "[NIT:consistency] header_test.go's new tests never restore logger.SetOutput" — verified: card 19's tail rebinds the process-global stderr sink and card 20 drives that path via `cmd.Execute()`. Card 20 now requires `t.Cleanup(func() { logger.SetOutput(os.Stderr) })` on every test that drives the blocking path, and `internal/logger/logger.go` was added to its `Context:`.
- r2 "[NIT:consistency] template.go's \"exactly four keys\" comment goes stale" — verified at `internal/reedengine/template.go:12`. `internal/reedengine/template.go` added to card 3's `Edits:` (and to the overview's All Files Touched), with a requirement to change the count and add `watchdog` to the enumeration while leaving the rest of that godoc untouched.
- r2 "[NIT:consistency] ensureServerAndSessionLocked's docstring omits watchdog" — verified: the function comment reads "validates capability, debug_log, mouse, and header template before any tmux round trip". Card 11 now requires that enumeration be updated too, alongside the local preceding comment it already asked for.
- r2 "[NIT:design] liveBoxLocked becomes production-dead code within this same batch" — verified: `apply.go:149` is its only production caller and card 7 rewrites that line. Resolved by the reviewer's second option rather than a note: card 6 now **widens `liveBoxLocked` in place** to `(render.Box, bool)` instead of adding a `liveBoxLockedOK` sibling behind a delegating wrapper, and updates the two call sites (`apply.go`'s placeholder `box, _ :=`, and the existing `windowsize_test.go` assertion) so its own commit compiles. Cards 7, 12, and 21 were updated to the single-method name. This ends with no dead code and keeps every existing doc comment in `apply.go`, `attach.go`, and `doc.go` that names `liveBoxLocked` accurate without a rename. The `discussion.md` decision that specified a sibling-plus-wrapper justified it as "so no existing caller changes" — a premise that does not hold once card 7, in the same batch, rewrites the sole caller, so the rationale is preserved while the mechanism is not.

## Pushed Back

_None. Every finding was factually accurate and no fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task._
