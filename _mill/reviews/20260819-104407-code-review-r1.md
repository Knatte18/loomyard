MILL_REVIEW_BEGIN
# Review: loom: phase-machine scaffolding — holistic

```yaml
duration_s: 234.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-19
```

## Findings

No findings. Checked against the plan's three batches and every listed source/doc file:

- Batch 1: `LoomRunLock` accessor added correctly, byte-identical wording/anchoring to `LoomStatusLock`; both `cmd/lyx/constructoranchoring_test.go` and `cmd/lyx/notransients_test.go` updated with matching rows. `loomengine.Status` rewritten to the thin three-field product shape; `checkCoherence` rewritten to the new `(shedengine.Status, Status)` signature with the exact rule set (mandatory slug/parent, `current_producer == "Preflight"`, five-state tolerance excluding `done`, history outcome/RFC3339-UTC checks, the "only-Preflight-history" fresh-start narrowing, StartSha/PauseRequested half-finished checks) and the retry-deadlock regression test present. `runCheck4` correctly threads `shedengine.Status`/unmarshalled `Status` into `checkCoherence`, distinguishing infra failure from determined `CheckSeedIncoherent`. Docs (`loom-status-spec.md`, `shedengine/doc.go`'s divergence-paragraph removal, `shed.md`, `loom.md`, `docs/overview.md`) all consistently rewritten off the old `phase`/`stage`/`narration` vocabulary onto `current_producer`/`state`/`activity`.
- Batch 2: `internal/loomshed` created with the told-geometry seam guard (`TestToldGeometryInvariant_AllowlistOnly`), its own `entryErr`/`cancelErr` duplicated per the stated decision, and every real producer (`Discussion-Validate`, `Plan-Validate`, `Batchifier`, the lazy `Webster` wrapper, `Preflight`) calling `entryErr` first and consulting `cancelErr` on every non-`Done` exit. `batcher.Active`'s absent-config/absent-`_lyx` fallback-to-template behaviour is correctly mapped to `Done`, not `Stuck`, and both batchifier and webster tests assert this explicitly. `Deps`/`New` in `loomshed.go` build the exact 12-row table with the exact `OnStuck` bounce targets from the shared decision, refuse a nil `deps.Preflight`, and the test drives `shedengine`'s own unexported `validate()` indirectly via `Run`, as required. `Seed` uses `state.UpdateJSON`'s `found` argument (no TOCTOU stat-then-write) and creates the lock parent directory before acquiring, matching `internal/state`'s documented gaps. `manifest/designs/loom.md`'s row 9/10 and the `Plan-never-reads-support-log` paragraph are both correctly corrected.
- Batch 3: `fixture_test.go`'s shared builder seeds through production `Seed` (never hand-written JSON), makes rows 3/7/9 genuinely pass, and injects only `Preflight`/`WebsterRun` per the two-rows-only decision. `sequence_test.go` asserts the full 12-row literal name order and terminal `RunDone`/`StateDone`/`current_producer` persistence. `resume_test.go` covers resume-not-restart (via cross-run `Preflight`-count, since `Result.History` is cumulative), unconditional crash-recovery re-call, pause-boundary-and-flag-clear, both bounce-routing shapes, budget exhaustion, and cancellation across all five real producers — the cancellation case for the `Preflight` wrapper correctly never reaches `loomengine.Preflight` (and thus never spawns git) because `entryErr` short-circuits first, keeping the file untagged-safe. The integration-tagged files correctly live in `package loomshed_test` (avoiding the `hubforge`→`fabriccli`→`loomengine` cycle), seed via `loomshed.Seed`, and stay scoped to outcome-mapping only. `manifest/roadmap.md` moves the item into `## Done` with the required short-entry rewrite and correct cross-links.

No BLOCKING or NIT items found: no out-of-plan files, no duplicated helpers beyond the deliberately-documented `entryErr`/`cancelErr` divergence, no missing test coverage relative to what each batch's own `## Batch Tests` section commits to, and no constraint violations (Told-Geometry, Test Tier Purity, Hermetic Git Test Environment, Documentation Lifecycle, Planparser Sole-Parser, Shed Producer-Seam) observed across the reviewed surface.

## Verdict

APPROVE
Full cross-batch implementation matches the plan, shared decisions, and CONSTRAINTS.md with no deviations found.
MILL_REVIEW_END
