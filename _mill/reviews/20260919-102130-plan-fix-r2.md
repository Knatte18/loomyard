# Plan fix — round 2

Source review: `20260919-101245-plan-review-r2.md` (holistic, sonnetxhigh).

## Fixed

- `20260919-101245-plan-review-r2.md` — "[NIT, demoted from BLOCKING] Card 15 keeps/rewrites a design doc the Documentation Lifecycle says to delete": accurate, and verified against both cited sources. `docs/overview.md`'s Documentation lifecycle section says a `manifest/designs/` module-design doc is deleted when its module lands and that the package header comment becomes the source of truth; `manifest/roadmap.md`'s maintenance rules add that a Done entry points at the module's own package documentation. The card now deletes `manifest/designs/reed-born-as-strand.md` instead of rewriting it, points the Done entry at `internal/loomcli`/`internal/reedengine`, and repairs the four inbound links the deletion would otherwise dangle (`reed-header-selvage.md`, `shed-generic-watchdog.md`, and `reed-mailbox.md` twice) per the Markdown Link Integrity invariant. No separate rationale-relocation step was added: cards 8, 10, 11 and 12 each already require their reasoning in the doc comment beside the code. Recorded as a new Shared Decision, `a-landed-items-design-doc-is-deleted-not-rewritten`.
- `20260919-101245-plan-review-r2.md` — "docs/overview.md's second four-step restatement is left stale": accurate. `docs/overview.md` restates the bootstrap's steps at the `loom` module bullet and again at the separate `the bootstrap` bullet under the execution-stack section. Cards 10 and 12 now name both explicitly, and the `docs-land-with-the-behaviour-card-that-makes-them-true` Shared Decision was updated to record that the file carries two independent restatements rather than one.
- `20260919-101245-plan-review-r2.md` — "The watchdog-fires-under-no-attach property is never actually proven": accurate and load-bearing. `ensureStatusStrand` calls `c.reed.Up()`/`c.reed.Status()` on a concrete `*reedengine.Engine` and must return nil before the watchdog call is reached, so no offline test in this package can drive the real `RunE` to that call site. Card 13 now states that split explicitly — it asserts the stub's arguments offline and says in its own file doc comment that the gate position is covered by the smoke tier — and card 15 gains the smoke case that actually proves it: `loom start --no-attach` still leaves a live watchdog daemon for the fixture hub. Card 11's field doc comment wording was trimmed to stop claiming an assertion the Tier 1 test no longer makes.
- `20260919-101245-plan-review-r2.md` — "Card 14's Selvage-pane-id assertion needs identifiers outside its Context": accurate. `LoadState` and `ReedState.SelvagePaneID` are declared in `internal/reedengine/state.go`, which was in no card's `Context:`. That file is now in the card's `Context:` and the requirement names the exact accessor rather than describing the check abstractly.
- `20260919-101245-plan-review-r2.md` — "[NIT, demoted from BLOCKING] \"Permanent full share\" misattributed to the status strand's Decision": accurate. The status strand carries `ShrinkWhenWaitingOnChild: true` and `manifest/designs/loom.md`'s own step-2 text says it collapses once a forked child exists, so its full height is conditional, while the operator strand's is unconditional. Card 10's doc requirement now describes the operator pane's row budget as its own stronger property and says the cited `childless-full-height-is-acceptable` Decision supplies the reason it is acceptable, not the property itself.

Card ordering changed as a consequence of the third finding: the smoke-tier watchdog reap moved ahead of the new smoke tests and is now card 14, the combined operator-strand plus watchdog-gate smoke coverage is card 15, and the doc retirement is a new card 16.
The reap moved because the daemon leak begins the moment card 12 lands — every existing test in this package's smoke tier already drives `lyx loom start` — so placing the reap after the new tests would have left it trailing a leak it did not cause.

## Pushed Back

_None. Every finding was accurate and every fix was applied, the two ceiling-demoted NITs included._
