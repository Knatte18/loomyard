# Discussion fix report — round 3

Source review: `_mill/reviews/20260824-132914-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r3 "Five link retargets left to the plan writer" — replaced the deferral sentence in Technical context with a per-link disposition table covering all five sites; the two that would ship a false sentence (`manifest/roadmap.md:14`, `manifest/designs/plan-card-format.md:3`) are delete-the-clause rather than retarget.
- r3 "Stale-doc inventory misses the registry's own comment" — added `internal/shedrecipe/registry.go`'s twelve-keys count comment and `manifest/designs/shed-recipe.md`'s motivation paragraph to the same-commit correction list in Scope, and expanded Technical context's stale-doc section from two claims to four, stating explicitly that `shed-recipe.md` is corrected rather than kept as historical rationale.
- r3 "Commit failure discards a completed agent run" — added a bullet to `commit-produced-artifacts` recording the resume consequence (`StateFailed` halt → resume re-calls `Discussion-Write` → archive-and-respawn) and why the wasted run is accepted over mapping the fault to `Stuck` or building retry-in-place machinery.

## Pushed Back

None.
