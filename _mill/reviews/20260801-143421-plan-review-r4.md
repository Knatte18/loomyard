MILL_REVIEW_BEGIN
# Review: Formalize the Tier 1/2 substrate rule and re-tier mis-tagged tests — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-01
```

## Findings

### [NIT] Redundant isTierTagged re-check in Card 4's sleep-guard wiring
**Location:** batch 1, Card 4 (`cmd/lyx/tierpurity_test.go`)
**Issue:** The walk callback already `return nil`s earlier for `isTierTagged(data)==true`, before reaching the restructured banned-token block; the new Sleep check's `!isTierTagged(data)` condition is therefore always true at that point and dead weight (harmless, but confusing to a future reader who might assume it does real work).
**Fix:** Note in Card 4's Requirements that the condition is deliberately defensive/explicit, or drop it and rely solely on the earlier early-return plus `!pathAllowlisted(relPath, allowedLongSleepers)`.

### [NIT] Scout-tagged tests are never actually executed by any verify: in this plan
**Location:** batch 2 (scoutengine-scout-tag), overview Decision "`-tags scout` test execution is a manual ... step"
**Issue:** Genuinely matches the "Integration test reachability" criterion's letter (tests moved/retagged into a new opt-in category, never run by any batch's machine-enforced `verify:`) — the four retagged files plus the two split subtests are unverified as passing under `-tags scout` by anything in this plan's own automation.
**Fix:** Already well-justified (gopls absent from this environment, mirrors the pre-existing `smoke` tag's identical manual-only precedent) and explicitly documented as a Decision — accepted as-is; consider a one-line follow-up note (e.g. in the PR description or a TODO) that a `gopls`-equipped machine should run `go test -tags scout ./internal/scoutengine/... -count=1` once, post-merge, to confirm the retag/split didn't break anything runtime-wise.

### [NIT] Card 15's "fourth ## Commands example line" undercounts existing lines
**Location:** batch 3, Card 15 (`docs/benchmarks/running-tests.md`)
**Issue:** The current `## Commands` block already has four example command lines (Tier 1, Tier 2, per-test JSON timing, one-package verbose); the new `-tags scout` line would be the fifth, not "a fourth."
**Fix:** Cosmetic only — the implementer will add the line regardless; reword to "a new" or "a fifth" example line for precision.

## Verdict

APPROVE
Plan is thorough, internally consistent, and every cross-file/line-number claim I could check against source verified exactly.
MILL_REVIEW_END
