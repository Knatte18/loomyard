MILL_REVIEW_BEGIN
# Review: self-report Tier 1: Go-detected structural anomalies — holistic

```yaml
duration_s: 505.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-09-12
```

## Findings

### [BLOCKING:scope] `configtemplate.go` never listed in any card's Context/Edits
**Location:** batch 1 card 4; batch 3 card 9
**Issue:** `ConfigTemplate()` is declared in `internal/loomengine/configtemplate.go`, a file distinct from `config.go`; card 4's Requirements reference it three times (`writeLoomConfigWithKey` "builds off `ConfigTemplate()`"; the well-formed-config test seeds "the full `ConfigTemplate()`"), and card 9's Requirements reference `loomengine.ConfigTemplate()` inside `fastDeadlineLoomConfig`. Verified via `Grep` across `_mill/plan/`: `configtemplate.go` appears in no card's `Context:` or `Edits:` list anywhere in the plan, only `config.go` and `config_test.go`/`smoke_test.go` (which call it) are listed.
**Fix:** Add `internal/loomengine/configtemplate.go` to card 4's `Context:` (and to card 9's `Context:` if its own reasoning about `fastDeadlineLoomConfig`'s behavior needs it).

### [NIT:consistency] Moving Tier 1 to Done leaves stale sibling cross-references in `roadmap.md`
**Demoted-from:** BLOCKING
**Location:** batch 3 card 9 (the `manifest/roadmap.md` edit)
**Issue:** Card 9 correctly drops Tier 1's own "Independent of Tier 2 and of the `lyx loom step` item above" sentence when moving it to Done, but does not touch the two sibling Planned entries that cross-reference it: the `lyx loom step` entry's "Independent of the two self-report items below" becomes false (only Tier 2 remains below), and the Tier 2 entry's "Independent of Tier 1 and of the `lyx loom step` item above" now dangles a reference to an item no longer in the Planned section. Verified against the current `manifest/roadmap.md` Planned section text. This is exactly the "carries a superseded statement" consistency failure mode, and the repo has precedent for keeping such cross-references current (the Someday `webster: worktree-per-card parallel execution` entry already says "The now-Done `Adopt quarry's glyph alphabet…` item…").
**Fix:** Add a requirement to card 9 to update the `lyx loom step` entry's "the two self-report items below" to "the self-report item below" (or similar), and optionally soften Tier 2's "Independent of Tier 1" phrasing since Tier 1 is no longer Planned.

## Verdict

REQUEST_CHANGES
Two fixable gaps: a missing Context entry (`configtemplate.go`) and a stale roadmap cross-reference left by the Done move.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
