# Plan fix — round 5

Source review: `_mill/reviews/20260731-095651-plan-review-r5.md`

## Fixed

- `20260731-095651-plan-review-r5.md` — "[BLOCKING] Card 20 Context omits snapshot.go, whose function it asserts against". Verified: Card 20's dangling-baseline case asserts on `SnapshotWarpSHA`, defined in `internal/fabricengine/snapshot.go` by batch 3 Card 11, and every sibling card touching that function already lists the file. Added it to Card 20's `Context:`.
- `20260731-095651-plan-review-r5.md` — "[NIT] Batches 3-4 verify scope omits cmd/lyx despite editing fabricengine test files". Verified, and the reviewer's parity argument holds exactly: batch 1's stated reason for including `./cmd/lyx/...` is that the module-wide tier-purity and hermetic-env guards live there and fire on a test-file edit made anywhere, which applies identically to batches 3 and 4. It is concrete rather than theoretical for batch 3, whose card 10 creates an untagged `index_test.go` against a guard that matches its banned tokens as raw substrings even inside comments. Added `./cmd/lyx/...` to both batches' `verify:` commands, in the batch frontmatter and the overview's Batch Index, and recorded the reason in each `## Batch Tests` section.
- `20260731-095651-plan-review-r5.md` — "[NIT] Card 12's negative guidance cites a file outside Context and outside the plan manifest". Verified. Rephrased the guidance to name the external test package `fabricengine_test` and the fixture's `*hubgeometry.Layout` requirement without naming the unreachable file or helper. That keeps the reason checkable — the package split is the whole point — while removing a path the implementer has no business opening; adding it to `Context:` would have invited exactly the read the guidance exists to prevent.

## Pushed Back

_None._
