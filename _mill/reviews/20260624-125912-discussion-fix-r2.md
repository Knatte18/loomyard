# Discussion fix report — round 2

```yaml
slug: weft-orphan-branches
round: 2
source_review: _mill/reviews/20260624-125848-discussion-review-r2.md
```

## Fixed

- `20260624-125848-discussion-review-r2.md` — [NOTE] "Unborn-branch detection differs from
  detached": updated `Decisions → detached-head-guard` and the Technical-context
  parent-resolution bullet to specify both abort signals explicitly — a detached HEAD prints
  literal `"HEAD"` (exit 0), an unborn branch exits non-zero; the guard fires on either, so
  an unborn branch is never mistaken for a valid start-point.

## Pushed Back

(none)
