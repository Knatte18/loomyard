# Fixer report — plan-fix round 3

Source review: `_mill/reviews/20260820-094306-plan-review-r3.md` (holistic, round 3, verdict APPROVE).

## Fixed

- `[NIT:consistency]` (demoted from BLOCKING) "Card 7's `linearChain` signature contradicts its own next sentence" — verified accurate: the card stated a two-parameter signature and then required a `*testing.T` first parameter the signature omitted, which is two contradictory instructions for one helper.
  Every existing helper in `internal/shedengine/testsupport_test.go` takes `t` first, so the signature was restated as `linearChain(t *testing.T, names []string, producers []ShedProducer) []ProducerDef` and the following sentence reworded to stop re-introducing the parameter it now already has.
- `[NIT:consistency]` "Card 19's 'following item' cross-reference claim is inaccurate" — verified accurate at `manifest/roadmap.md:24`, which refers back as "(previous item)", while the bold-name backreference sits at `:46` in a different subsection.
  The card's reasoning now describes both backreferences correctly and states that neither is falsified by the move, so neither changes.
- `[NIT:consistency]` "`parallel-work.md` still lists the shipping task under 'Can start now' after card 19's own edit" — verified accurate.
  The finding exposed a real incoherence in the card, not only a stale line: correcting a scope claim on a bullet that should no longer exist would have left a shipped task advertised as startable.
  Card 19 now removes the bullet outright, updates the running-now paragraph to name this task as landed, and updates the light-caveat paragraph whose stated precondition — this task landing first — is now satisfied.
  The in-place scope-claim correction is explicitly dropped, with the reason recorded in the card.
  The card's title was updated to match.

## Pushed Back

_None._
