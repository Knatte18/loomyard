# Discussion fix — round 2

Source review: `_mill/reviews/20260816-142855-discussion-review-r2.md` (APPROVE, 0 blocking, 7 NIT).

## Fixed

- r2 — "New-test count stated three different ways": §Scope, §Testing, and the §Q&A log now all name the same four tests, banner-strip included.
- r2 — "Q&A restates test 4 in the exact terms §Testing forbids": the Q&A answer now states stripped-body equality via `stencil.StripLeadingComment`, not whole-file byte equality.
- r2 — "Stale stencil count in the sandbox suite not in the doc inventory": `tools/sandbox/SANDBOX-CORE-SUITE.md:232` added as the fifth required doc update; verified the file does read "all fifteen registered stencils".
- r2 — "Design doc's step 4 falsehood has no disposition": the design-doc update scope now covers step 3, step 4, and the wholly-absent banner-strip step; verified `render.go:181`/`:240` derive `fabricengine.StencilsDir(l.HubPath)` internally, contradicting step 4's "already take it as a parameter".
- r2 — "Superseded 'one allowlist entry' left standing in the Q&A log": entry 1 now marks itself superseded in place and names two as the live figure.
- r2 — "Burler fixture seeding left as an open choice": defaulted to seeding, with the "not a fix for a failure" caveat kept in both §Technical context and §Testing.
- r2 — "Line-number drift in §Technical context": corrected 180/239 → 181/240 and `template_test.go:128` → `:122`; both verified against the source.

## Pushed Back

None.
