# Plan fix report — round 2

Source review: `_mill/reviews/20260627-152150-plan-review-r2.md` (verdict APPROVE, 3 NITs).

## Fixed

- [r2 NIT] "'verbatim' body copy ignores arg-index shift" — added a general **arg-index-shift**
  rule to the `cobra-style-c-seam` shared decision (00-overview): under `WrapRun(fn(out,
  args))`, `args` is the post-subcommand list, so payload/slug rebinds from `rest[1]`/
  `fs.Arg(0)` to `args[0]`. Added explicit pointers in Card 12 (board: payload `args[0]`,
  "json payload required" = `len(args)==0`), Card 16 (warp: slug/branch `args[0]`), and
  Card 9 (ide: spawn slug `args[0]`).
- [r2 NIT] "--json leaf test may pick a flagless leaf" — Card 22 now pins the leaf to
  `lyx warp remove --help --json` (owns `--force`) so the populated-`flags` assertion is not
  vacuous; explicitly forbids a flagless leaf like `board upsert`.
- [r2 NIT] "muxpoc table-test shares now-divergent assertions" — Card 17 now instructs
  dismantling any shared `exit 1 + out.Len()==0` guard (e.g. `TestRunCLIErrors`) into per-case
  expectations, since no-arg / unknown-subcommand / bad-flag now diverge in both exit code and
  output.

## Pushed Back

(none)
