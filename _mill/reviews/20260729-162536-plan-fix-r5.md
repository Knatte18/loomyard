# Plan-fix report — round 5

Source review: `_mill/reviews/20260729-162536-plan-review-r5.md` (reviewer opus · xhigh; verdict REQUEST_CHANGES, 1 BLOCKING + 0 NIT). Verification round; finding verified accurate.

## Fixed

- [BLOCKING] "Card 5 Context omits hubgeometry.go" — verified card 5's `filterHubReserved` Requirements call `hubgeometry.HubReservedNames()` while its `Context:` listed only `config.go`, `topology.go`, `weft_verbs.go`. Added `internal/hubgeometry/hubgeometry.go` to card 5's `Context:`.

## Preemptive (same class, self-audited to avoid a repeat finding)

- Card 12 `Context:` — added `internal/lyxtest/lyxtest.go` (defines `CopyPairedLocal`/`SeedConfig`/`MustRun`, which the new integration tests call).
- Card 14 `Context:` — added `internal/fabricengine/config.go` (defines the `Config`/`Config.Pathspec` the new reserved-slug test constructs).

## Pushed Back

(none — the finding was accurate; applied under the fix-everything default.)
