# Plan fix — round 1

Source review: `_mill/reviews/20260818-061440-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 4 NIT).

## Fixed

- r1 holistic, "[NIT:consistency] Stale lyxcwd vocabulary survives in renamed test" — verified against `internal/pattern/pattern_test.go:310-311`, `:319` and `:331`; card 1's reword instruction now also covers the inline comment and the two error-message strings, with the assertion *logic* still pinned unchanged, and the header-comment length corrected to six lines.
- r1 holistic, "[NIT:consistency] Card 2 comment forward-references card 3's test" — accurate: card 2's commit would have shipped a comment naming `TestPlanSpec_PatternDirectiveAnchoredUnderAnchorPath` before card 3 creates it. Took the review's first suggested resolution: the pointer-list amendment moved into card 3's Requirements, `cmd/lyx/constructoranchoring_test.go` added to card 3's `Edits:`, and card 2 now explicitly leaves each comment's existing pointer list alone.
- r1 holistic, "[NIT:consistency] Four verified-detail slips in Requirements prose" — all four confirmed by re-reading the cited lines. Card 1's "five-line header comment" corrected to six (`pattern_test.go:299-304`); card 2's "four-line tautology comment" corrected to six (`constructoranchoring_test.go:73-78`, `:129-134`) and "the six constructor rows sitting between them" corrected to seven (`:81-87`, `:137-143`); card 3's attribution dropped — `plan_test.go:281-283` records only why the test uses a non-`"."` `AnchorRel`, so the card now states the real reason its root cannot host a fixture (relative and never created, so writes would land in the source tree) and explicitly warns against restating that comment as if it recorded the filesystem-free property.
- r1 holistic, "[NIT:scope] Card 4 leaves engine_test.go's enumerating header stale" — accurate, and inconsistent with cards 1 and 5, which do maintain their files' headers. Card 4 now requires the `internal/burlerengine/engine_test.go` file-header enumeration to name the PATTERN-directive detector.

## Pushed Back

_None._
All four findings verified accurate against the cited source lines, and no fix broke existing functionality, conflicted with a documented decision in `_mill/discussion.md`, or destabilised work outside this task.

Note on the card-3 attribution fix: the dropped claim originated in `_mill/discussion.md`'s own *anchoring-proof-relocates-to-the-call-site* decision, which cites `plan_test.go:281-283` for a property that comment does not state.
The decision's conclusion — that the proof must be a new sibling test on a real `t.TempDir()` — is unaffected and still correct on the verifiable reason, so only the attribution was corrected, not the decision.
