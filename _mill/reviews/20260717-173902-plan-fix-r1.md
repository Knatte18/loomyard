# Plan fixer report — round 1

Source review: `_mill/reviews/20260717-173514-plan-review-r1.md` (verdict REQUEST_CHANGES; 1 BLOCKING + 4 MINOR/NIT).

## Fixed

- `20260717-173514-plan-review-r1.md` → **[BLOCKING] Card 13 edits the engine but declares no engine file**: defined an exported `BuiltinRegistry() Registry` accessor in batch 1 Card 2's `registry.go` and added it to batch 1's external interface; batch 3 Card 13 now calls `codeintelengine.BuiltinRegistry()` and the conditional "add it in the engine" clause is removed — no cross-batch undeclared edit remains.
- `20260717-173514-plan-review-r1.md` → **[MINOR] Card 10 needs a production seam Card 9 does not create**: added the injectable-transport constructor `newLSPClientFromRW(rwc io.ReadWriteCloser)` explicitly to batch 2 Card 9's Requirements (production `newLSPClient` spawns; the seam builds a subprocess-free client; `kill()` guards nil `*exec.Cmd`); Card 10 now only writes the test against that seam.
- `20260717-173514-plan-review-r1.md` → **[MINOR] Cross-links a non-existent doc**: batch 3 Card 16 and batch 4 Card 17 now cross-link the in-tree `docs/research/codeintel-spike.md` (+ `docs/modules/codeintel.md`) and refer to `docs/modules/websterv2_extension.md` (which lives on `main`, not this worktree) by name in prose rather than as a relative link that would dangle at HEAD.
- `20260717-173514-plan-review-r1.md` → **[MINOR] Integration test unreachable by any verify gate**: batch 4's `verify:` changed from `null` to `go test -tags integration ./internal/codeintelengine/...` (in both the overview Batch Index and the batch file), so Card 12's live-gopls test runs once Card 17 installs `gopls` (it `t.Skip`s if absent); batch Scope + Batch Tests prose updated to match.
- `20260717-173514-plan-review-r1.md` → **[NIT] helptree pinned sets not updated**: `cmd/lyx/helptree_test.go` moved to Card 15's `Edits:` (and out of `Context:`), with Requirements to add `"codeintel"` to `requiredModules` and a `{codeintel, [refs]}` case to `TestHelpTree_VerbModuleSubcommands`; the overview's All Files Touched now lists `cmd/lyx/helptree_test.go`. Verified the claim: `helptree_test.go:27` has a pinned `requiredModules` slice and per-module `wantSubs` cases.

## Pushed Back

(none — all five findings were accurate and harmless to fix.)
