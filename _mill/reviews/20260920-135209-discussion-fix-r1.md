# Discussion fix — round 1

Source review: `_mill/reviews/20260920-135051-discussion-review-r1.md` (verdict APPROVE, 0 blocking, 2 nits).

## Fixed

- `20260920-135051-discussion-review-r1.md` — "[NIT:consistency] GateOutcome.FindingsPath dangles after finalize": verified accurate against `internal/shuttleengine/wait.go:588` (`os.RemoveAll(run.runDir)` on the Done cleanup), which deletes the run directory the findings file lives in before `Wait` returns. `GateOutcome.FindingsPath` is now documented in the Contract-shape decision as diagnostic text only, never dereferenced after `Wait` returns, with the gate closure's own `logger.Warn` named as the durable record of the findings text, and the field's doc comment required to say so.
- `20260920-135051-discussion-review-r1.md` — "[NIT:design] Budget's route into the Burler sites is unstated": verified accurate — `Gate` carries no budget and the four-sites decision named only a `Gate` field on `RunOpts`. Introduced `GateSpec{Gate, Attempts}` as the single carrier at every hop, packed where both values are first known (the `shedrecipe` entry constructor, which reads the row's `config:` block and `Env` together). The Contract-shape, Attempt-budget, and four-sites decisions, the Scope bullets, and the Testing section now all name `GateSpec`, so the gate and its budget cannot be separated at any seam.

## Pushed Back

None.
