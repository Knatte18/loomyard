# Discussion fix — round 6

Source review: `_mill/reviews/20260919-094604-discussion-review-r6.md` (verdict APPROVE, 3 NITs, reviewer `opusmedium`).

This is the round cap (`roles.discussion-review.holistic.rounds: 6`).
The convergence gate's `min_rounds`/demoted predicate is not satisfied — round 6 carries one finding demoted from BLOCKING, and `--orch`'s two-consecutive-APPROVE clause would need a seventh round — so this round is taken as an implicit approval under the gate's round-cap path, on the operator's explicit standing instruction to approve and hand off at the cap.

## Fixed

- "Allowlist mechanism misattributed to cliwire" — verified against source: `internal/cliwire/bannedecl_enforcement_test.go` has no allowlist at all (`policedCliDirs` at `:23` is a policed-directory list, `bannedWiringDeclarations` a banned-name map), and the nearest allow-shaped precedent is `internal/gitkit/callerset_enforcement_test.go`'s `allowedCopyRepoCallerDir` const (`:21`, compared at `:75`) — a directory, not a file set. Re-grounded the decision on gitkit, stated plainly that nothing in the repo allowlists at file granularity yet so this test introduces that shape, and split the Technical-context "pattern to copy" entry into two precedents: cliwire for the AST walk / `runtime.Caller(0)` root resolution / residual-note style it actually supplies, gitkit for the allow shape.
- "Naming constraint omits the policy value's field names" — extended the stated naming constraint from parameters, locals and the type name to any *member* of a seam value a host file touches: the three answers are reached by method call (exempt under (i)), and no field or method name a host reads may contain `selvage`, with `keep.selvageAlive` named as the concrete failing shape.
- "Cited lines for the `selvagePaneID` spelling point at call sites" — repointed the citation at the actual parameter declarations, `reconcile.go:32` and `spawn.go:39`, kept `apply.go:85` as the genuine local, and noted explicitly that `reconcile.go:213` / `spawn.go:149` are `st.SelvagePaneID` selectors a case-sensitive rule already catches and therefore not the evidence for case-insensitivity.

## Pushed Back

None.
