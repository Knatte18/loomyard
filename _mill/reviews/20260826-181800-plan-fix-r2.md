# Plan fix — round 2

## Fixed

- `20260826-181555-plan-review-r2.md` — "[NIT:consistency] Rename-group zero-bullet Refs nil guarantee is unenforced".
  Batch 1, card 2's nil-versus-empty sentence is split: the blanket claim now covers the six non-`Rename` labels via `parseRefField`'s own return, and the `renameLabel` branch gains an explicit instruction to initialize the group's `Refs` to a non-nil empty slice before appending pair endpoints, so all seven labels share one stated invariant.

## Pushed Back

_(none)_
