# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260826-065718-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 1 NIT).

## Fixed

- `20260826-065718-plan-review-r2.md` — "[BLOCKING:scope] Batch 2's Call-entry clear breaks shedrecipe's CommitSeam tests, uncaught by any verify".
  Verified accurate against `internal/shedrecipe/entries_bouncer_test.go`: `layoutSettledBouncerRound1` writes round 1's report, APPROVED verdict, and ledger, and `PlanResolvesToCommitPlan` and `DiscussionResolvesToCommitDiscussion` each make one `Call` and assert their injected closure was invoked exactly once — assertions card 10 breaks, from one package away, with no batch `verify:` covering them.
  Added card 13 to batch 2, repairing all three `TestBouncerEntry_CommitSeam` subtests onto the harvest vehicle and rewriting `layoutSettledBouncerRound1`'s doc comment, which cites the removed replay behaviour as precedent.
  Extended batch 2's `verify:` (and the overview's Batch Index entry for it) with `./internal/shedrecipe/...` so the break is caught inside the batch that causes it rather than at the task-wide done gate.
  Corrected the batch's own Batch Tests claim that `internal/shedadapters` has no production importers whose behaviour changes — `internal/shedrecipe` is exactly that importer, and the corrected text now says so.
- `20260826-065718-plan-review-r2.md` — "[BLOCKING:scope] Card 10's Context omits the file declaring `fixedClock`".
  Verified accurate: `fixedClock` is declared at `internal/shedadapters/archive_test.go:10`, and that file was neither in card 10's `Context:` nor one of its `Edits:`/`Creates:` targets.
  Added `internal/shedadapters/archive_test.go` to card 10's `Context:` list.
- `20260826-065718-plan-review-r2.md` — "[NIT:consistency] A stale defect-2 narrative survives in a file card 12 already reads".
  Verified accurate: `internal/loomrecipe/revalidate_test.go`'s doc comment on `TestSequence_PlanRevalidateCatchesPostSegmentRegression` narrates the stale-verdict replay as a live defect "filed on the follow-up roadmap item this task adds rather than fixed here", which this task falsifies, and neither inventory's enumeration method reaches a test file in another package.
  Extended card 12 to edit that comment in the same card that runs the file's tests, keeping the surviving reason the test still asserts nothing about post-bounce behaviour.

## Pushed Back

_None._
All three findings verified accurate against the code, and no fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task.

## Note

Adding card 13 to batch 2 renumbered batch 3's two cards from 13/14 to 14/15, keeping card numbering globally unique and ascending across batches.
Every in-plan cross-reference to those two cards was updated with them.
