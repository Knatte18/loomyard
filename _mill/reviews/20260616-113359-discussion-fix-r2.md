# Discussion fix — round 2

Source review: `_mill/reviews/20260616-113304-discussion-review-r2.md` (verdict APPROVE, 2 × `[NOTE]`).

## Fixed

- `[NOTE] state_test.go has no full expected-output literal` — clarified in Technical
  context: `TestSocketName` only asserts a `"muxpoc-"` prefix + char-class (no
  hardcoded `muxpoc-mhgo-mux-design` expected literal), so renaming the input cwds is
  brand-consistency only; the one full paired example is the `state.go:176` doc
  comment, renamed on both sides.
- `[NOTE] worktree package _mhgo literals not enumerated` — added
  `internal/worktree` (and `cmd/mhgo/main_test.go`) to the managed-state-dir source-
  site list in Scope → In, naming its `_mhgo` literals in `portals.go`, `add.go`,
  `config.go`, `cli.go`, and the package's `*_test.go` files.

## Pushed Back

(none)
