# Discussion fix — round 2

Source review: `_mill/reviews/20260811-042536-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 5 NIT).
All five fixed under `mill-receiving-review`'s fix-everything default; none met a pushback criterion.

## Fixed

- r2 — "Body's pre-producer phrases have no disposition" — resolved the self-contradiction in Decision `producer-model-rewrite-shape`: the generic phrases **are** swept to named producers, and the decision now enumerates all 13 sites verified against the live files (`discussion-format.md:3,7,10,12,14,15,31,47,54,71,72,83`; `plan-format.md:24`) with the target name for each. What stays unrewritten is the schema, stated as such. Added Testing item 6, a zero-hit grep over both files.
- r2 — "`:71–74` restatement is decided in Scope but absent from the change table" — folded `:71–74` into the enumerated sweep as an explicit non-exception (its two subjects are renamed to `Discussion-Write`/`Discussion-Review`; the open question of which one writes the ledger stays open), and added the sites to the Technical-context change table.
- r2 — "Q&A log still states the old blast-radius count of 7" — corrected to 8 occurrences on 8 lines, pointing at the decision rather than restating a second count.
- r2 — "`:304`/`:306` exception in the archive disposition is malformed" — rewritten: all nine `Discussion-Review-Gate` sites keep their wording including `:304`; `:304` carries both tokens and only its `Plan-Review-Gate` half is swept; `:306` carries only `Plan-Review-Gate` and is removed from the exception list as a plain rename site.
- r2 — "Markdown Link Integrity invariant not named in Constraints" — added the invariant to Constraints with its scan scope, its coverage gaps, its enforcing test, and an explicit "do not allowlist around a break this task creates"; tied Testing item 4 to `go test ./internal/lyxcwd/ -run TestEnforcement_MarkdownLinks` instead of leaving it a manual pass.

## Pushed Back

None.
