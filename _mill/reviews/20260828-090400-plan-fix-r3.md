# Plan fix report — round 3

Source review: `_mill/reviews/20260828-090106-plan-review-r3.md`

## Fixed

- `[BLOCKING:scope]` "Card 10 names a test function from a file outside its Context" — added `internal/reedcli/smoke_headerseed_test.go` to batch 2 card 10's `Context:`.

Swept the same finding class across every remaining card, since three consecutive rounds each surfaced one instance of it:

- Batch 1 card 3 — added `internal/reedengine/lifecycle_test.go` to `Context:`; its Requirements directs re-running card 2's test, which lives there.
- Batch 2 card 9 — added `internal/fabricengine/stencilcommit.go` to `Context:`; its Requirements names `fabricengine.CommitSeededStencils`, defined there.
- Batch 3 card 13 — added `internal/reedcli/cli.go` and `internal/lyxcwd/lyxcwd.go` to `Context:`; its Requirements names reed's `PersistentPreRunE` and `lyxcwd.Resolve` in the prohibition explaining why the JSON-envelope path is not asserted untagged.

## Pushed Back

_None._
