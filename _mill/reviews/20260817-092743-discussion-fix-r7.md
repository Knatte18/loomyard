# Discussion fix report — round 7

Source review: `_mill/reviews/20260817-092700-discussion-review-r7.md` (APPROVE, 0 blocking, 1 NIT, reviewer `sonnet` at xhigh effort).
No demoted findings, so the convergence gate is satisfied at this round.

## Fixed

- r7 `[NIT:consistency]` "Constraints list omits Durable-vs-Ephemeral Invariant" — accurate: T6's brief explicitly reworks that invariant's text, yet it appeared in neither `_mill/discussion.md`'s Constraints enumeration nor the design doc's Related list. Added to both. While closing the completeness claim, two further invariants this work now engages were added alongside it rather than leaving the same gap open one finding wider: Test Tier Purity (T6's untagged-vs-integration test tiering turns on it) and Dev/Prod Binary Separation (T6 moves the ldflags-stamped build channel out of `package main`). The design doc's Related entry now also distinguishes the four invariants tasks here *reword* from those that merely bind the work.

## Pushed Back

None.
