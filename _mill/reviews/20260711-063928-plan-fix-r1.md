# Plan fixer report — round 1

Source review: `_mill/reviews/20260711-063823-plan-review-r1.md` (REQUEST_CHANGES,
1 BLOCKING + 1 NIT).

## Fixed

- r1 review: "[BLOCKING] Seed-only materialize can't be byte-identical" — verified
  against `internal/yamlengine/reconcile.go` (merged output is marshalled from the node
  tree; doc comment says "equivalent to the template", not byte-identical). Card 9
  rewritten: `SeedOnly && absent` writes `m.Template()` verbatim via
  `fsx.AtomicWriteBytes`, `Added` computed via `yamlengine.MissingKeys(template, nil)`,
  `Applied=true` (dry run: same `Added`, no write, `Applied=false`); test (a)'s
  byte-identity assertion is now valid; initengine "created" heuristic unaffected.
- r1 review: "[NIT] modelspec.ConfigTemplate referenced but not in Context" — added
  `internal/modelspec/template.go` to Context of cards 8 and 9. Validator-safe: the
  `non-existent-path` check exempts paths that are `Creates:` targets elsewhere in the
  plan (card 5).

## Pushed Back

(none)
