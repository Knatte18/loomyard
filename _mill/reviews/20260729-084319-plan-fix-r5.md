# Plan-fix round 5 — codeintel-v1

Source review: `_mill/reviews/20260729-083505-plan-review-r5.md` (verdict: REQUEST_CHANGES, 4 BLOCKING, 2 NIT).

## Fixed

- `_mill/reviews/20260729-083505-plan-review-r5.md` — "Card 8's Edits: field omits toolchain.go": moved `toolchain.go` from Context to Edits in batch 02 card 8.
- `_mill/reviews/20260729-083505-plan-review-r5.md` — "Card 24 constructs daemonState/uses its accessors with daemonstate.go missing from Context": added `daemonstate.go` to batch 06 card 24's Context.
- `_mill/reviews/20260729-083505-plan-review-r5.md` — "Card 25's test directly drives internal/lock with lock.go missing from Context": added `internal/lock/lock.go` to batch 06 card 25's Context.
- `_mill/reviews/20260729-083505-plan-review-r5.md` — "Several cards construct errors.go/symbol.go types without listing those files in Context": added `errors.go` to batch 08 card 30 and batch 09 card 37; added `symbol.go` to batch 10 card 42.
- `_mill/reviews/20260729-083505-plan-review-r5.md` — "[NIT] Card 1 mentions ConfigTemplate() from an uncontexted file": dropped the aside sentence (the reviewer's own simpler alternative) from batch 01 card 1.
- `_mill/reviews/20260729-083505-plan-review-r5.md` — "[NIT] Card 21's shared-daemon regression assertion lacks a concrete pass condition": gave batch 05 card 21 a deterministic query (`workspace/symbol` for `"Resolve"`) and an exact pass condition (identical first-candidate location across both connections), with the discriminating rationale spelled out.

## Proactive sweep (beyond this round's named findings)

Four Context/Edits-completeness rounds in a row (2 through 5) each found *more* instances of this exact gap class than the previous round fixed — a pattern strongly suggesting isolated per-finding fixes were not converging. Before re-running the review, I did a full pass across all 47 cards checking every reference to an `Err*`/`lspClient` method/`Registry{}`/`Entry{}`/`builtins()` identifier against that card's own Context/Edits list (grep-assisted, each candidate hand-verified against the actual card text to rule out false positives from purely descriptive/contrastive prose). This found and fixed additional genuine gaps the review had not yet reached:

- Batch 05 card 18 (`finalizeConnection`): missing `errors.go` (references `ErrServerTimeoutSentinel`).
- Batch 04 card 16 (state/staleness/probe tests): missing `errors.go` (references `ErrServerTimeoutSentinel`).
- Batch 07 card 26 (`References`/`lookup` split): missing `lspclient.go` (calls `client.initialize`/`kill`/`close`, `newLSPClient`).
- Batch 08 card 31 (`lspClient.definition`/`Definition` tests): missing `lspclient.go` (calls `client.definition` directly; only `lspclient_test.go`, the sibling test file, was listed).
- Batch 08 card 32 (`Symbol` tests): missing `lspclient.go` (hand-builds a client via `newLSPClientFromRW`, defined there, not in `lspclient_test.go`).
- Batch 06 card 24 (`ensureSupervised`): missing `internal/proc/proc_linux.go` (calls `proc.DetachBreakaway`, defined on both platforms; only the Windows file was listed).

Several other candidates the same sweep surfaced (batch 05 card 17, batch 07 card 27, batch 08 card 29, batch 09 card 35, batch 10 card 41) were hand-verified as purely descriptive/contrastive prose mentions with no actual construction or method call in that card's own new code, and left unchanged.

## Pushed Back

None.
