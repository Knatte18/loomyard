# Discussion fix report — round 4

Source review: `_mill/reviews/20260831-180631-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r4 review, "[NIT:consistency] \"Every peer carries integration tag\" is false" — Testing → "Build tier" now says every *hub-driving* peer is integration-tagged, names all five, and names `argsarity_test.go`, `envelope_test.go` and `testmain_test.go` as the untagged files that spawn nothing. Verified by reading line 1 of each file in `internal/fabriccli`.
- r4 review, "[NIT:consistency] Doc inventory table is not the literal grep output" — the enumeration method is now described as two passes (the grep, plus a read of the merge-relevant sandbox scenarios F18/F19/F20), the F18 row is marked as found by the second pass, and `SANDBOX-FABRIC-SUITE.md:142` (F3's `Goal` line) is added as an out-of-scope row.
- r4 review, "[NIT:consistency] Stale line cite for envelope.go's read-only-verb sentence" — `envelope.go:16-18` corrected to the file-header comment's lines 14-15 (line 16 is `package fabriccli`), and the key-set contract quoted under `key-always-present` is now cited at lines 8-12. Both ranges verified against the file.

## Pushed Back

None. All three findings were factually accurate and none of the fixes broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task's scope.
