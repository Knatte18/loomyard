# Plan fix — round 2

Source review: `_mill/reviews/20260819-132626-plan-review-r2.md` (holistic, `sonnetxhigh`).

## Fixed

- `[BLOCKING:scope]` "Card 4's Context omits mergepaths.go, which its Requirements directly name" — accurate; both `unifyConflictPaths` and the total-function `weftPathVisible` that card 4's index-membership rationale leans on are declared in that file, and none of the six files the card listed carried them.
  Added `internal/fabricengine/mergepaths.go` to card 4's `Context:`.

- `[BLOCKING:design]` "Finalize's dirty-worktree reason detection has no stable, cross-package identifier" — accurate; the reason constant is unexported and sits in a closed, verbatim-pinned set, while the aggregated reason list is the only exported surface, so the plan as written left the consumer no option but a hardcoded literal with nothing guarding drift.
  Card 24 now has two halves: it first adds an exported predicate method on the already-exported guard-error type in the owner package, reporting whether the aggregated reasons contain that package's own dirty-worktree constant, and explicitly forbids exporting, renaming, or adding to the closed reason set — the task needs a predicate, not a new member.
  The producer's step 6 now recognizes the condition through that accessor rather than by matching a string.
  Card 27 gains the accessor's own coverage in the owner package's test tier (dirty reason alone, another reason alone, and the dirty reason alongside others), which is what pins the accessor to the constant; the producer's own tier asserts it branches on the accessor.
  Batch 4's `verify:` and its `## Batch Tests` section now include the owner package, since the batch edits it, and the overview's Batch Index and All Files Touched were updated to match.

- `[NIT:consistency]` "roadmap.md's phase-machine scaffolding Done entry is already stale and this task doesn't touch it" — accurate; that entry states a twelve-row list and enumerates seven stubbed rows, both stale before this task and made further wrong by it, since two of the rows it names stop being stubs here.
  Card 39 now corrects the row count and the stubbed-row list in the same file it already edits, with the reason recorded: leaving it would contradict the module-table row count this plan corrects one card later.

## Pushed Back

_None. All three findings verified accurate against the source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope._
