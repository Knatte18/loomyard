# Plan-fix report — round 2

Source review: `_mill/reviews/20260710-073314-plan-review-r2.md` (holistic, REQUEST_CHANGES,
1 BLOCKING + 2 NIT).

## Fixed

- `20260710-073314-plan-review-r2.md` — [BLOCKING] "Required-subcommand set omits
  has-session/kill-session": the engine provably calls both (`has-session` at `overlay.go:91`,
  `kill-session` at `lifecycle.go:142`/`:415`), so added them to card 6's `requiredSubcommands`,
  card 9's godoc contract subcommand list, and card 10's integration-test exercised set — the
  drift-canary now covers the engine's real dependency set.
- `20260710-073314-plan-review-r2.md` — [NIT] "Newly-unused `strings` import not removed
  (cards 12, 18)": added explicit import-cleanup instructions — card 12 removes `strings` from
  `command.go` when dropping `pwshSingleQuote` leaves it unused; card 18 removes `strings` from
  `launchers.go` once the backslash-conversion moves into `launcher_content.go` (each confirmed
  with `go build`).
- `20260710-073314-plan-review-r2.md` — [NIT] "Card 12 commit leaves package non-compiling":
  folded the `Prepare` call-site update (`claudeengine.go`) into card 12 so its commit compiles
  atomically — card 12 now edits both `command.go` and `claudeengine.go`; card 13 is reduced to a
  tests-only card (`command_test.go`) that builds on top of card 12's already-compiling package.
  Adjusted both cards' Context/Edits/commit lines accordingly.

## Pushed Back

_None — every finding was factually accurate and its fix carried no demonstrable harm._
