MILL_REVIEW_BEGIN
# Review: Rename loom CLI run/drive/step for verb/engine symmetry, plus rename ly-supervise — holistic

```yaml
duration_s: 33807.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-09-19
```

## Findings

### [BLOCKING:scope] Three Context-only files carry unaddressed rename hits
**Location:** batch 1, card 2 (`## Rename mechanic` + Requirements)
**Issue:** `internal/loomcli/wiring.go`, `sharedbootstrap.go`, and `seedinput.go` are listed only under card 2's `Context:`, and never appear in any card's `Edits:` across the whole plan (verified: each name occurs exactly once, in that `Context:` line). Yet each contains real verb/filename hits: `wiring.go` line ~175, "The verbs that actually build producers -- run and drive -- deliberately keep the full wire()"; `sharedbootstrap.go`'s file header ("holds the blocks `run` and `step` share"), its quoted refusal text ("run \"lyx loom run\"" at line ~137), its contrast sentence "`run` spawns `drive`, which ensures the directory itself" (line ~101), and three stale `run.go`/`drive.go` filename citations (lines ~55, ~158, ~177, ~225) that describe pre-move file locations; `seedinput.go`'s two `drive.go` filename citations (lines ~56, ~59) and its "drive-refuses-an-unrecorded-parent" decision name (line ~66). None of this is named in card 2's Requirements, so a literal implementer leaves all of it untouched.
**Fix:** Add these three files to a card's `Edits:` (card 2 or a new card in batch 1), with `Requirements:` classifying and rewriting each hit per the plan's own bootstrap-sense/contrast-as-sentence/stale-filename rules.

### [BLOCKING:scope] drive.go and bootstrap.go each carry one more hit than their cards name
**Location:** batch 1, cards 2 and 7
**Issue:** `internal/loomcli/drive.go` (move target `run.go`)'s `reflectFriction` doc comment reads "...the run lock reads as free and a second \"lyx loom run\" spawns a second driver" — a bootstrap-sense hit identical in wording to the one card 10 fixes in `loomengine/config.go`'s `LoomFrictionLock` doc comment — but card 2's Requirements name only `shouldReflectFriction`'s doc comment, not `reflectFriction`'s own. Separately, `bootstrap.go`'s file header says "the verb body in run.go is assembly over judgment that is already under test" (a stale reference to the pre-move bootstrap file, destined for `start.go`), but card 7's Requirements enumerate exactly three other sentences in that file and stop there.
**Fix:** Extend card 2's `run.go` Requirements to cover `reflectFriction`'s doc comment (→ "lyx loom start"), and card 7's `bootstrap.go` Requirements to cover the header's "run.go" → "start.go".

### [BLOCKING:scope] main.go's module list still names the retired root alias
**Location:** batch 1, card 2
**Issue:** `cmd/lyx/main.go`'s root command `Long` string ends "Available modules: board, config, ide, reed, fabric, selfreport, shuttle, burler, webster, stencil, loom, run, quarry." — naming the bare-root alias as "run". Card 2's Requirements for `main.go` cover only the `loomcli.RunAliasCommand()` → `StartAliasCommand()` call site and its four-line comment, not this sentence, so after the rename the root help text keeps calling the alias "run" even though `RunAliasCommand`'s own `Use` becomes `"start"` — a direct hit against the no-back-compat-aliases decision's "old names survive nowhere in the tree" clause.
**Fix:** Add this sentence to card 2's `main.go` Requirements, renaming "run" to "start" in the module list.

### [NIT:consistency] friction_test.go's test name still says "Drive"
**Location:** batch 1, card 7
**Issue:** `TestDriveEnsuresAbsentFrictionDir` keeps the retired verb name in its own Go identifier even though card 7 states this file "carr[ies] no renamed identifier in an executable position" — true for call sites, but the test name itself still narrates the old verb.
**Fix:** Either rename it to `TestRunEnsuresAbsentFrictionDir` in card 7's Requirements, or note the identifier as a deliberate exception.

## Verdict

REQUEST_CHANGES
Three verified scope gaps leave real rename hits (in wiring.go/sharedbootstrap.go/seedinput.go, drive.go, bootstrap.go, main.go) unaddressed by any card.
MILL_REVIEW_END
