# Plan fix — round 3

Source review: `_mill/reviews/20260814-113908-plan-review-r3.md` (holistic, sonnetxhigh, APPROVE, 0 BLOCKING / 3 NIT).

## Fixed

- r3 "[NIT:design] runTriage/runTargeting new param position unspecified" — verified accurate: card 22 said both functions "gain the parameter" without stating where, while every other signature change in the plan states leading or trailing explicitly.
  Specified **leading** for both, spelled out the full before-and-after signature of each, and recorded why leading is the right choice rather than merely the consistent one: both functions already end in a run of same-typed `string` scalars (`model, effort`), so a trailing `stencilsDir string` would leave a mis-ordered call site still compiling.
  Also made the corresponding `run.go` call-site instruction say "leading argument".

- r3 "[NIT:consistency] render.go's new fabricengine import not called out" — verified accurate: `grep -c fabricengine internal/websterengine/render.go` returns 0, and cards 18, 23, 24, and 27 all carry an explicit import-addition line where card 26 did not.
  Added the instruction to card 26, stated as fact rather than conditionally ("it does not carry one today") since the absence was verified.

- r3 "[NIT:consistency] seam_enforcement_test.go's prose goes stale after the allowlist edit" — verified accurate: the file's header comment at lines 1-5 and the `t.Errorf` parenthetical at line 86 both enumerate the allowlist by hand, and neither would mention `internal/stencilstore` after card 23's map edit.
  Extended card 23 to update both spots in the same commit as the map entry, quoting each one's current text so the implementer can locate them without searching.
  Added why the failure message matters more than the comment: a stale enumeration there tells the next person who trips the test that `stencilstore` is not allowed, which is the opposite of the truth.

## Pushed Back

_None. Every finding was factually accurate and no fix caused demonstrable harm._

DAG re-validated and the validator gate re-run after the edits: 9 batches, 0 findings.
Convergence gate satisfied — round 3 meets `min_rounds` (1) and this round's findings carry no ceiling demotion — so the plan is approved.
