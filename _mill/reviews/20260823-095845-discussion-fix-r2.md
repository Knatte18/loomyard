# Discussion fix — round 2

Source review: `_mill/reviews/20260823-095749-discussion-review-r2.md` (APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- `20260823-095749-discussion-review-r2.md` — "[NIT:consistency] \"Forced\" registration test is a superset check": verified at `internal/loomcli/cli_test.go:35-48` — the test marks only names already in its four-entry map, so two extra verbs pass unchanged. Both `## Technical context` and `## Testing` now state that zero tests are forced, and the discussion adopts the reviewer's explicit-decision prompt: tighten `TestCommand_AllFourVerbsRegistered` into a genuine exact-set assertion rather than extending the map and preserving the blind spot.
- `20260823-095749-discussion-review-r2.md` — "[NIT:design] Retained tests need the required-heading list they can no longer see": `required-sections-move-unexported` now carries a decision on fixture sourcing — a hardcoded fixture constant of the shape `internal/loomshed/discussionvalidate_test.go`'s existing `validDecisionRecord` already uses, with the per-heading iteration case (`discussionvalidate_test.go:112`) moving into `internal/discussionparser` where the unexported list is in scope. No export needed.
- `20260823-095749-discussion-review-r2.md` — "[NIT:scope] Non-not-exist I/O case is only reachable on one of the two files": the `internal/discussionparser` case list now names the decision record as the only file that can produce a non-not-exist read error, and adds an explicit case for a support-log path that is a directory (`os.Stat` succeeds, so it counts as present), flagged as behaviour preserved deliberately rather than tightened.

## Pushed Back

_None._
