# Discussion fix — round 2

Source review: `_mill/reviews/20260913-105932-discussion-review-r2.md` (verdict APPROVE, reviewer `opus`, effort medium, 0 BLOCKING / 5 NIT — one NIT demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- "Stale `-HUB` hub after the documented re-clone" (`[NIT:decision]`, **demoted-from BLOCKING**) — the substantive finding of the round.
  Verified against source before fixing: `CloneHub` derives `hubPath = HubPath(cwd, name)` in both argument forms (`internal/fabricengine/clone.go:158,205`), and both the collision guard (`os.Stat(hubPath)` → `"hub already exists at %s"`, lines 165 and 215) and the `--reset` teardown (`resetHub(rec, cwd, hubPath)`, lines 161 and 210) key on that one derived path.
  After the value swap they address `<name>-LYXHUB` only, so a pre-existing `<name>-HUB` is neither refused nor reset and a re-clone silently stands up a second parallel hub for the same warp.
  Added a **Disposition of the old directory** bullet to Decision *No physical rename* giving the explicit procedure (push or abandon outstanding work, `lyx reed down`, remove the directory by hand, then clone) and stating that `--reset` is not the tool for this.
  Added a companion bullet recording *where* the procedure is documented — `docs/sandbox-hub.md` and the `CONSTRAINTS.md` note, deliberately **not** the `lyx fabric clone` Cobra help text, since that text outlives the retired suffix and a permanent migration paragraph there is clutter.

- "\"costs one re-clone and nothing else\" understates the sandbox case" (`[NIT:consistency]`).
  Accurate and the same mechanism: `build.cmd -reset` resolves through the renamed `hubName`, so it reaches `lyx-test-LYXHUB` only.
  Rewrote the sentence in Decision *Sandbox fixture hub is renamed* to "a re-clone **plus one manual deletion**", naming the surviving old hub and any live reed server on its socket, and pointing at the disposition bullet above.

- "`tools/sandbox` is stated to have no Go tests" (`[NIT:consistency]`).
  Verified: `main_test.go`, `suite_test.go`, `report_test.go`, `resolve_test.go`, `pathresolve_guard_test.go` all exist;
  `grep -c -- '-HUB'` returns 0 for every one, and they build hub paths from the `hubName` constant (`main_test.go:21,49,54-55`, `report_test.go:238`).
  The premise was false as written.
  Replaced the Testing bullet with the accurate statement — tests exist, self-update via the constant, owe no literal sweep, and must still run under `go test ./...` rather than be skipped.

- "CONSTRAINTS.md note has no settled home" (`[NIT:decision]`).
  Accurate — the Scope bullet said "under **Hub Containment Invariant** (or a new line)", leaving the choice to the plan writer.
  Settled it: a new top-level `## Hub Suffix Invariant` section placed immediately after `## Hub Containment Invariant` (line 52) and before `## gitkit Leaf Invariant`.
  Added a full Decision section giving the exact claim to be recorded (blockquoted, three bullets) and the rationale — Hub Containment is about junctioning, not naming, so a bullet there would misfile a machine-enforced invariant under an unrelated one.

- "Stale round-1 instruction block" (`[NIT:consistency]`).
  Accurate — the section directed polish items to round 1, which had already passed, so it read as open while its own last line closed it.
  Retitled `### Open — scope extension` to `### Resolved — scope extension` and collapsed it to the conclusion: round 1 raised no scope additions, the opportunity has passed, and mill-plan should plan exactly this scope.

## Pushed Back

None.
