# Fixer report: plan-fix round 3

Source review: `_mill/reviews/20260912-114351-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NITs).

## Fixed

- r3 BLOCKING:scope — "Card 7's Context omits two files its own Requirements name".
  Accurate.
  Card 7's Requirements name `loomengine.RenderAnomalyBody` and `selfreportengine.DefaultLabels()` as bare mentions with no signature inlined, while neither declaring file appeared in the card's `Context:`/`Edits:` — so the implementer, who reads only what `Context:` allows, could not see either shape.
  The reviewer's distinction is right: `selfreportengine.CreateIssue` is named in the same card but has its full signature inlined in the same sentence, so that mention is self-contained and needs no entry.
  Added `internal/loomengine/anomalybody.go` and `internal/selfreportengine/selfreport.go` to card 7's `Context:`.

- r3 NIT:consistency — "\"four documentation edits\" but three files are listed".
  Accurate — the prose said four, the parenthetical listed three, and card 9 edits three.
  Corrected to three in the overview's `docs-land-with-the-observable-change` decision.

- r3 NIT:consistency — "Batch 3's Batch Tests section overstates what its cards touch".
  Accurate: no card in batch 3 creates or edits a `loomengine` file, so calling it one of "the two packages this batch's code touches" was wrong.
  Reworded to name `internal/loomcli` as the package whose files this batch's cards create or edit, and `internal/loomengine` as one it consumes by import — kept in the verify list deliberately, so a mismatch between the seams batch 2 declared and the way batch 3 calls them surfaces at the batch gate rather than at the repo-wide done gate.

## Pushed Back

_(none — all three findings verified accurate and were fixed)_
