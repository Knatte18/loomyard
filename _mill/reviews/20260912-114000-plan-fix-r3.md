# Fixer report: plan-fix round 3 — self-report-tier2

Source review: `20260912-113700-plan-review-r3.md` (holistic, APPROVE, 0 BLOCKING / 1 NIT).

## Fixed

- `20260912-113700-plan-review-r3.md` — "[NIT:consistency] `WarnIfMarkerAbsent`'s third parameter is misleadingly named".
  Verified accurate: card 1 declared `WarnIfMarkerAbsent(template []byte, stencilName, notePath string)`, while every call site in batches 4, 5, and 6 passes the composed **directive** text, so the declared name matched no real caller.
  Fixed by renaming the parameter to `directive` in card 1's signature and stating in the same card why: an empty directive is the "nothing would have been rendered anyway" case, which covers both Tier 2 being off and a `friction.Directive` read error the composer swallowed under round 2's Shared Decision, and neither should warn about a missing marker.
  Card 1 additionally now requires the parameter's own doc comment to say callers pass the directive, so a future caller cannot pass a note path and silently invert the gate.
  Card 5's test bullet, card 14's explanatory sentence, and card 21's helper instruction were each updated to the new name and the widened rationale.

## Pushed Back

_None._
The finding was factually accurate, and the fix broke nothing, conflicted with no documented decision, and destabilised nothing outside this task.
