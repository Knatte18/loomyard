# Plan fix — round 3

Source review: `_mill/reviews/20260813-150046-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 3 NITs).

## Fixed

- `20260813-150046-plan-review-r3.md` — "[BLOCKING:design] add.go's HEAD-probe site misclassified as a plain merge".
  Verified against the code: the guard is `if exitCode != 0 || strings.TrimSpace(stdout) == "HEAD"`, and the second disjunct fires on a *successful* git call with a nil error, so `default-merge-rule` — which is written for a site whose exec and exit paths are two separate guards — cannot express it.
  A mechanical merge would misattribute an exec-level failure to "detached HEAD or unborn branch" and has no error to wrap on the success arm at all.
  Card 20 now specifies the three-way migrated shape explicitly: a non-`*GitError` error keeps the `rev-parse abbrev-ref HEAD` wrapper, a recovered `*GitError` returns the detached-HEAD message, and a nil error falls through to the separate `stdout == "HEAD"` check returning that same message.
  Both operator-visible strings survive unchanged.
  The card's site accounting is corrected with it: two of `add.go`'s six sites need non-merge shapes, not one, and four are plain merges.

- `20260813-150046-plan-review-r3.md` — "[NIT:consistency] card 12's rationale contradicts card 7's HasUnpushed migration".
  Accurate: card 7 migrates `HasUnpushed` to `r.runChecked`, so only `Pull` and `Fetch` still call `r.run`, and the blind-spot figure is ten of twelve rather than nine.
  Card 12's rationale now says that and cross-references card 7.
  The guard code the card requires is unchanged — the union key and the twelve pinned names were already correct.

- `20260813-150046-plan-review-r3.md` — "[NIT:design] card 35 asks to replace prose that isn't in CONSTRAINTS.md today".
  Verified: the "exactly one `gitexec.` occurrence" phrasing appears only in `cmd/lyx/gitrepoboundary_test.go`'s header comment, which card 12 already corrects, and nowhere in `CONSTRAINTS.md`.
  Card 35 now says to *state* the two-call-expression assertion rather than replace anything, and records where the phrase actually lives so the implementer does not go hunting for it in the wrong file.

- `20260813-150046-plan-review-r3.md` — "[NIT:design] wrapProbeError's dead nil-cause branch after migration".
  Accurate: after migration all seven call paths invoke the helper from inside an `err != nil` branch, so the nil-cause arm is unreachable by construction and `op` reads only there.
  Took the reviewer's first offered remedy — keep the branch and label it — because the second (drop it and simplify the signature) would mean dropping `op`, and the two-parameter `wrapProbeError(weftURL, op string, cause error)` shape is what `_mill/discussion.md`'s `error-constructing-helpers-are-re-signatured` decision settles on.
  Card 23 now requires the godoc to say the arm is deliberately defensive and unreachable by construction, so it is not left looking live.

## Pushed Back

_None._
