# Plan fix — round 1

Source review: `_mill/reviews/20260728-095420-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT, reviewer `sonnetmax`).

## Fixed

- r1 "[BLOCKING] Batch 2 verify omits `-tags integration` for the integration-tagged file it edits" — verified accurate: `internal/hubgeometry/hubgeometry_test.go` begins with `//go:build integration`, so a plain `go test` excludes it from the build and cards 4 and 5 would have added assertions to a file their own batch never compiles. Added `-tags integration` to batch 2's `verify:` in both `02-hubgeometry-pattern-surface.md` and `00-overview.md`'s Batch Index, and rewrote batch 2's `## Batch Tests` to state why the flag is mandatory even though the batch creates no tagged file, that `cmd/lyx`'s guards text-scan rather than compile and so cannot compensate, and why the new assertions stay in `hubgeometry_test.go` beside their siblings rather than being relocated to the untagged file (batch 5's card 15 must edit that same file anyway). Swept the remaining batches for the same mismatch: `internal/stencil` and `internal/pattern` have zero integration-tagged test files so batch 1 is correct as written, and batches 3, 4, 5 and 7 already carried the flag — but **batch 6 had the same defect for the same reason**, naming `./internal/hubgeometry/...` and `./cmd/lyx/...` under an untagged verify, so it was fixed identically and its `## Batch Tests` reworded to keep the accurate half of its original claim (the batch creates no tagged file, and `internal/pattern` stays Tier 1) while dropping the false inference that the verify therefore needs no flag.
- r1 "[NIT] Card 28's 'drive it through PlanSpec the way the existing tests there do' undersells the fixture change needed" — verified accurate: `internal/loomengine/plan_test.go` builds `&hubgeometry.Layout{WorktreeRoot: filepath.Join("home", "user", "repo")}`, a path that never exists, and `pattern.Directive` does a real `os.Stat`, so the non-empty-directive assertion would have passed vacuously. Card 28 now states explicitly that this case needs a `t.TempDir()`-backed Layout with a real `_pattern/PATTERN.md` seeded on disk, calls out that it is the only test in batch 7 that touches the filesystem at all (cards 24–27 inject the marker value directly through `stencil.FillOptional` and never stat anything), and records that `t.TempDir()` is not a banned token so the file stays untagged.

## Pushed Back

None. Both findings were verified against source — the build constraint on `hubgeometry_test.go` and the fake-path Layout fixture in `plan_test.go` were each confirmed by reading the files — and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope. The BLOCKING fix widens two `verify:` scopes and changes no card content; the NIT fix adds a fixture requirement to one card's `Requirements:` prose.
