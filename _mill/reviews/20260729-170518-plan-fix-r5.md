# Plan fix report — round 5

Source review: `_mill/reviews/20260729-170023-plan-review-r5.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT). Both findings verified accurate; both fixed per `mill-receiving-review`'s fix-everything default; all edits confined to `_mill/plan/` files. Zero pushbacks.

## Fixed

- [BLOCKING] "ensureServer's native-fallback branch has zero test coverage" — verified: the flip's step (3) (supervised fails for a non-toolchain reason → `ensureNative` attempted → its result returned) is exercised by no card; the toolchain-failure test short-circuits at step 1 and card 13's integration test only proves the success path. Fixed by adding a native-fallback unit test to card 12 (added `internal/codeintelengine/ensureserver_test.go` to card 12's `Edits:`, and `toolchain_test.go` + `supervised_test.go` to its `Context:`). Note: the reviewer's proposed mechanism (make the fallback's `resolveGoToolchain` also fail) can't reach the fallback — a toolchain failure returns at step 1 before `ensureSupervised`/`ensureNative`. The correct, deterministic, **process-free** approach specified instead, grounded in `toolchain.go`'s fast path (`resolveGoToolchain` only `os.Stat`s `binPath`, never checks the executable bit): a `withFakeInstaller` that succeeds by writing a **non-executable** stub `gopls` (so step 1 passes), a pre-held daemon spawn lock via `lock.AcquireWriteLock(Layout.CodeintelDaemonLock("go"))` (so `ensureSupervised` returns `ErrServerSpawnTimeout`), and the fallback `ensureNative`'s `newLSPClient` failing at `exec.LookPath` **before** any `cmd.Start` (no subprocess) — then asserting `ensureServer` returns `connKindNative` and a non-nil error that is **not** `ErrServerSpawnTimeoutSentinel`, proving both that the fallback fired and that native's result superseded supervised's timeout.
- [NIT] "buildOptions unit test doesn't guard the actual call-site wiring" — verified: comparing `buildOptions(...)` to itself with the same args is tautological and cannot catch a call site passing the wrong local. Reframed card 8: the **DRY collapse of the six literal sites into six `buildOptions` calls** is the structural regression guard (a batch closure can no longer silently omit `WorktreeRoot`), and the `cli_test.go` assertion is reframed as documenting `buildOptions`'s field-threading contract, not as the batch-vs-single-arg guard. Also updated batch 3's Batch Tests summary to match (a real cross-arg same-daemon check would need a live daemon and belongs to the integration tier).

## Pushed Back

- None.
