# Discussion-fix report — round 1

Source review: `_mill/reviews/20260715-072018-discussion-review-r1.md` (verdict APPROVE, 0 blocking, 1 NOTE).

## Fixed

- `[NOTE] Swallow depends on has-session exit-1 for server-gone` (r1 review) —
  added an explicit exit-code-grounding note to the `classify-via-hassession-reprobe`
  decision and required the new `doc.go` assumptions bullet to state the
  `has-session` exit-1 → `(false, nil)` dependency (overlay.go:90), the same
  exit-1 the reproduction showed from `listPanes`, plus a note that the
  integration test's success assertion pins it against real tmux.

## Pushed Back

_None._
