# Plan fix — round 1

Source review: `_mill/reviews/20260828-095427-plan-review-r1.md`

## Fixed

- r1 "[BLOCKING:design] Card 20's header-tail tests panic on a nil `*Engine`" — verified against `internal/reedcli/header.go`: `RunE` calls `c.eng.HeaderText()` before the `if blocking` branch, and `HeaderText` (`internal/reedengine/header.go:11`) dereferences `e.cfg` immediately. Card 20 now mandates `&reedCLI{eng: reedengine.New(reedengine.Config{}, reedengine.Geometry{RepoName: ..., HubPath: t.TempDir()})}`, names the `cmd.SetOut`/`SetArgs`/`Execute` drive shape, records that `clihelp.ShouldAbort`/`SetExit` are nil-safe on a bare context, and requires the file-header comment's now-false "never runs RunE" claim be reworded.
- r1 "[BLOCKING:scope] Card 11 breaks an existing lifecycle test the batch claims stays green" — verified: `newTestEngine` (`internal/reedengine/lock_test.go:45`) builds its `Config` without `Watchdog`, and `TestUp_BadHeaderTemplateFailsBeforeAnyTmuxContact` sets only `DebugLog`/`Mouse`. Card 11 now carries `internal/reedengine/lock_test.go` in `Edits:` and `internal/reedengine/lifecycle_test.go` in `Context:`, and requires `Watchdog: "on"` be added to the shared fixture's `Config` literal — one line covering every caller — with the second, local `Config` literal in the same file explicitly left alone.
- r1 "[BLOCKING:design] Card 5 asks for a both-GOOS assertion the package's own template mechanism cannot produce" — verified: `template_posix.go` is `!windows` and `template_windows.go` is `windows`, so `ConfigTemplate()` exposes one file per build. Card 5 now names that limit explicitly, permits reading both `.yaml` files by relative path with `os.ReadFile` in this one test, prefers a plain byte assertion over a second resolution mechanism, and adds a separate `ConfigTemplate()` assertion so the shipped accessor is covered too.
- r1 "[BLOCKING:scope] Card 9's godoc requirement names `requiredSubcommands`, absent from its Context" — verified: `requiredSubcommands` is declared in `internal/reedengine/probe.go:32`. Added `internal/reedengine/probe.go` to card 9's `Context:`.
- r1 "[NIT:scope] Card 21's doc bullet cites two ungiven files" — verified: the `testing.Testing()` bullet's rationale needs `headerLaunchLine`'s `underTest` mechanic. Added `internal/reedengine/headerpane.go` and `internal/reedengine/lifecycle.go` to card 21's `Context:`.

## Pushed Back

_None. Every finding was factually accurate and no fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task._
