# Plan fix — round 4

Source review: `_mill/reviews/20260828-103021-plan-review-r4.md`

## Fixed

- r4 "[BLOCKING:design] reapplyLayout re-probes the hook on every signal-mode apply" — verified: card 9 called `hookInstalledLocked()` unconditionally, so card 16's `mode == watchModePoll` guard suppressed only the mode transition, not the `show-options` round trip, contradicting `discussion.md`'s `hook-availability-decides-poll-fallback` ("signal-driven mode never re-probes") and card 16's own restatement of it.
  Took the reviewer's first option rather than weakening the rule: `reapplyLayout` now takes a `probeHook bool`, the probe is issued only when it is true, and `probeHook == false` yields `HookInstalled: false, HookKnown: false` — "not asked", the same undecided shape a deferral produces.
  Poll mode passes `true` (re-probing each cycle is what allows promotion after an attach installs the hook, and costs nothing extra in a mode already making a round trip per cycle); signal mode passes `false`.
  Batch 2's external-interface line, card 9's requirements and godoc, both call sites in card 16, and card 13's probe assertions were all updated, and card 13 gained a `probeHook: false` case asserting no `show-options` round trip is issued at all.

- r4 "[NIT:scope] LivePane's defining file is never listed in any card's Context" — verified: `LivePane` is declared in `internal/reedengine/parse.go`, absent from every card's lists while cards 7 and 9 write new `[]LivePane`-typed signatures.
  Added `internal/reedengine/parse.go` to the `Context:` of cards 7, 9, 12, 13, and 18 — the cards that introduce or assert against those signatures.

## Pushed Back

_None. Both findings were factually accurate and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task._
