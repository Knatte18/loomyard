# Plan fix — round 2

Source review: `_mill/reviews/20260725-134927-plan-review-r2.md`

## Fixed

- [BLOCKING] "Card 26 edits template.yaml but omits it from Edits" — moved `internal/websterengine/template.yaml` from `Context:` to `Edits:` in card 26 (batch 7) and added it to `00-overview.md`'s `All Files Touched` union. The card's Requirements already instructed updating the template's yaml tags; `config_test.go`'s tag-coverage assertion now has a declared edit target.
- [NIT] "Batch 5 carries a Rename mechanic section with no Moves" — removed the `## Rename mechanic` section from batch 5 (all cards have `Moves: none`; the convention reserves that section for batches with a non-empty `Moves`). The copy-not-move rationale it carried already lives in the batch's `## Batch Scope`.

## Pushed Back

_None — the BLOCKING finding was accurate and fixed; the NIT (which the reviewer marked acceptable-as-is) was fixed anyway for idiomatic cleanliness per the receiving-review default rule._
