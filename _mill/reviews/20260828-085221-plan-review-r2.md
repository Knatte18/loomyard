MILL_REVIEW_BEGIN
# Review: reed: attach's layout computation scales header pane height with terminal height — holistic

```yaml
duration_s: 314.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-28
```

## Findings

None.

Verified against source: `render/rules.go`, `height.go`, `layout.go`, `types.go`, `policy.go`, `focus.go`, `checksum.go`, `apply.go`, `attach.go`, `windowsize.go`, `overlay.go`, `lock.go`, `state.go`, `parse.go`, `config.go`, `probe.go`, `doc.go`, `template_posix.yaml`, and every test file listed in the manifest, plus `manifest/roadmap.md`'s watchdog-daemon item.

- The extraction target (`Rules`'s current body, the `placement` struct with only `id`/`height` today, `stackHeights`'s `isStrip[i]` computation) matches Card 1's description exactly, including the "six direct call sites" claim in `height_test.go` (counted: 6).
- `apply.go`'s current `planLayout`/`applyLayoutLocked` and `attach.go`'s `AttachArgv` match Cards 3/5/6's described mapping and exact install-point locations (`select-layout` success → before `select-pane`; `planLayout` success → before `chained` assignment) verbatim.
- `attach_test.go`'s existing `attachRecorder`/`newAttachHook` and `apply_test.go`'s lack of any recorder fixture match Card 7's "built from scratch, not extended" / "model on attach_test.go" claims.
- `doc.go`'s current closing sentence ("did not grow for any of this … no new psmux risk") is exactly the sentence Card 10 requires rewritten, and every load-bearing bullet Card 10 must not touch (Silent layout rescale, The chained attach) is present and unchanged elsewhere in the plan.
- `manifest/roadmap.md`'s watchdog-daemon item's resize clause (rejected `set-hook`+`run-shell`, resize job claimed as the daemon's) is exactly what Card 11 narrows.
- Batch Index DAG is acyclic, all three `file:` entries exist, `depends-on` is monotonic (1→2→3), verify strings match between `00-overview.md` and each batch file.
- Global step numbering is sequential 1–11 with no gaps across the three batches, matching each batch's declared `cards:` count.
- `## All Files Touched` in `00-overview.md` is the exact union of every card's `Edits:`/`Creates:` across all three batches (14 entries, checked pairwise) — no `Moves:` entries exist anywhere (all `none`), so the Moves/rename-mechanic criteria are vacuously satisfied.
- Context completeness checked for every card that references a cross-file identifier (`render.Pin`/`FixedHeightPins` in `rules.go`, `toRenderInputs`/`fixedHeightPins` in `apply.go`, `installResizePinsLocked`/`resizePinHookArgvs` in `windowsize.go`, `waitUntil` in `contract_integration_test.go`, `newIntegrationEngine` in `mouse_boot_integration_test.go`): each consuming card's `Context:`/`Edits:` list carries the declaring file.
- All `Requirements:` name concrete identifiers (struct/field/function names) rather than vague prose; no BLOCKING vagueness found.
- Every Shared Decision (pins-are-render-placed-heights, one-mapping-site, hook-body-is-one-array-entry-per-pin, the-clear-is-unconditional, hook-failure-is-non-fatal, guard-skip-leaves-a-stale-array, set-hook/resize-pane stay optional, install-points-are-two-named-statements) is faithfully and consistently implemented across the cards that claim it.

## Verdict

APPROVE
Plan is internally consistent, source-grounded, and faithfully implements every Shared Decision with no gaps found.
MILL_REVIEW_END
