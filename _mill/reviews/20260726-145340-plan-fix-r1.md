# Plan fixer report — round 1

Source review: `_mill/reviews/20260726-145218-plan-review-r1.md`

## Fixed

- r1 review, "[BLOCKING] Deleting treadle.md leaves dangling links in
  shed.md/hardener.md" — verified: four `[treadle.md](treadle.md)` links in
  `manifest/designs/shed.md`, four in `manifest/designs/hardener.md`. Card 14
  now lists both files under Edits (hardener.md moved out of Context) with a
  grep-verified dangling-link sweep requirement, including rewording the
  anchor-dependent sentence; overview All Files Touched updated.
- r1 review, "[NIT] Model-spec registry loaded twice per perch invocation" —
  fixed via an additive export: card 12 adds
  `LoadConfigWithRegistry(baseDir, module, reg)` carrying the resolution
  logic while `LoadConfig` keeps its exact signature (delegating), so the
  byte-identical-API decision is honored; card 13's `cli.go` loads the
  registry once and reuses it for both the config load and `decodeProfile`.
- r1 review, "[NIT] treadleengine missing an 'Other docs' entry in
  overview.md" — verified the `## Other docs` convention
  (codeintelengine/tokenvocab bullets); card 14 now adds the treadleengine
  bullet in the same as-built style.

## Pushed Back

- (none)
