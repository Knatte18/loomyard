# Plan fix — round 8

Source review: `_mill/reviews/20260811-170428-plan-review-r8.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 3 NIT).

## Fixed

- `20260811-170428-plan-review-r8.md` — "[BLOCKING:scope] `verbs_test.go`'s `vc.Run` call site is nowhere in the plan".
  Verified: `internal/fabricengine/fabrictest/verbs_test.go` calls `err := vc.Run(t, h, fixture)` inside `TestVerbCases_CleanState`, and card 30's arity change breaks it.
  Added to card 30's `Edits:` and to `## All Files Touched`, with the repoint stated as `_, err := vc.Run(...)` and the reason discarding is correct there (that test asserts error shape, not the record) written down so it does not read as a missed opportunity.
- `20260811-170428-plan-review-r8.md` — "[BLOCKING:scope] `reconcile.go`'s own `writeWarpBinding` has no recording site".
  Verified: `writeWarpBinding` has two production callers — `clone.go` and `reconcile.go`'s backfill branch — and only the clone one was recorded.
  Card 21 now records the reconcile site too, gated on the branch actually reaching the call, using the `Reconcile` verb's own recorder.
- `20260811-170428-plan-review-r8.md` — "[NIT:consistency] `rollbackAdd`'s recorder parameter has two claimed owners and an incomplete requirement".
  Verified: `rollbackAdd` reaches six gate-bound calls — four through card 13's helpers, two of its own through card 15.
  Both cards now state the split explicitly and name all six, with the consequence spelled out: passing `nil` at any of them silently drops the rollback record and **no** later assertion catches it, because `Add`'s mint-then-rollback nets to zero in the manifest diff and the commission direction exempts exactly that shape.
  This was the round's most valuable finding — a genuine silent-failure path in the plan's own division of labour.
- `20260811-170428-plan-review-r8.md` — "[NIT:consistency] Card 13 says "three" test-caller helpers and names two".
  Verified: two is correct.
  Changed, keeping the grep-is-the-authority sentence.
- `20260811-170428-plan-review-r8.md` — "[NIT:consistency] Cards 25/26 hand a `*Mutations` to helpers declared over a `Mutations` value".
  Verified: card 23's helpers take a value, the CLI-layer recorders are pointers, and Go does not auto-dereference.
  Cards 25 and 26 now state `rec.Snapshot()` at every CLI-layer emission, matching the `res.Mutated()` value form the engine-verb handlers use.

## Pushed Back

_None._
Every finding was factually accurate and confirmed against the source before fixing.
