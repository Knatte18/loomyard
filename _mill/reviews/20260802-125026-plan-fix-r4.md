# Plan fixer report — round 4

Source review: `_mill/reviews/20260802-124832-plan-review-r4.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 1 NIT). All three findings verified accurate → all FIXED, none pushed back.

## Fixed

- **[BLOCKING] Batch 6 card numbers run out of order (26, 30, 27, 28, 29)** (`20260802-124832-plan-review-r4.md`) — The test-suite/commitWeft-migration card was physically placed right after card 26 (the unexport) but numbered 30, so numeric order diverged from the compile-safe file order, risking a multi-card broken-build window if mill-go schedules by ascending number. Renumbered batch 6 so physical and numeric order agree: the test-suite card became **card 27** (immediately after the unexport, closing the `weftgit_exclude_test.go` compile window), and the doc/design/sandbox cards shifted to **28/29/30**. Updated the two internal cross-references (card 26's "handled in card 27"→"card 28"; Batch Tests "Card 30's migration"→"Card 27's").
- **[BLOCKING] `fabriccli/fabric.go`'s "14-verb" doc comment goes stale, never fixed** (`20260802-124832-plan-review-r4.md`) — Confirmed `fabric.go:10` says "the flat 14-verb `lyx fabric` tree"; card 22 adds `diff` (making it 15) but no card edited the file. Moved `internal/fabriccli/fabric.go` from Context to Edits in card 22 with a requirement to update the verb-count prose (14→15), and added it to All Files Touched.
- **[NIT] `CoalescePush`'s doc comment keeps a false "exported because…" rationale after unexport** (`20260802-124832-plan-review-r4.md`) — Confirmed `coalesce.go:27-29` says "exported because boardengine … calls it directly"; false after both board→`Bolt.Sync` (card 2) and the unexport (card 4). Extended card 4's requirement to reword the rationale sentence (unexported because `Bolt`/`CoalescePushBothAt` is now the sole coalescing entry point), not merely recase the identifier.

## Pushed Back

(none)
