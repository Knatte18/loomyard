# Plan-fix report — round 1

Source review: `_mill/reviews/20260615-104300-plan-review-r1.md` (verdict APPROVE, 3 NITs)

## Fixed

- [r1 NIT] "Untouched consumer tests rely on root-collapse invariant" — added a
  note to batch 02 scope that `add_test.go`/`remove_test.go` resolve from hub root
  (`RelPath == "."`), are unaffected by the mirror, and must stay green unedited.
- [r1 NIT] "Card 6 prune guard wording risks an off-by-one read" — rewrote card 6
  to evaluate the boundary guard (`filepath.Rel(stop, cur)`) at the loop top
  before any `os.Remove`, collapsed the duplicated `cur == stop` / Rel `.`
  condition into one, and pinned that `stop` is never a removal candidate.
- [r1 NIT] "Card 8 menu MkdirAll vs. never-clobber ordering left implicit" —
  specified that `MkdirAll` runs only in the menu-absent branch after the
  never-clobber `os.Stat` early-return, so it does not run on every
  `writeLaunchers` call.

## Pushed Back

(none — all three NITs were accurate clarifications applied as written)
