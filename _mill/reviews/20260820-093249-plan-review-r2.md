MILL_REVIEW_BEGIN
# Review: shedengine: per-producer bounce budget + explicit OnDone routing — holistic

```yaml
duration_s: 392.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (Sonnet 5, Anthropic)
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [BLOCKING:scope] Batch 2 cards 9 and 11 omit shed.go despite naming its identifiers
**Location:** batch 2, cards 9 and 11
**Issue:** Card 9's Requirements name `RunDone` and `Result.HaltedProducer` (both declared in `internal/shedengine/shed.go`), and card 11's Requirements name the `Producers` field (declared on `Shed` in the same file), but neither card lists `shed.go` in `Context:`. Sibling cards 5, 6, 8, and 10 in the same batch all correctly include `shed.go` for the identical need.
**Fix:** Add `internal/shedengine/shed.go` to card 9's and card 11's `Context:` lists.

### [BLOCKING:scope] Batch 3 card 13 omits fixture_test.go despite naming testLandingDeps
**Location:** batch 3, card 13
**Issue:** Card 13's Requirements say "Do not redeclare `fakeAlwaysDoneProducer`, `testDeps`, `testLandingDeps`, or `wantProducerTable`" — `testLandingDeps` is declared in `internal/loomshed/fixture_test.go`, which is not in card 13's `Context:` (card 14, in the same batch, does list it).
**Fix:** Add `internal/loomshed/fixture_test.go` to card 13's `Context:` list.

## Verdict

REQUEST_CHANGES
Two batch-2/batch-3 cards name identifiers from `shed.go`/`fixture_test.go` without listing those files in `Context:`.
MILL_REVIEW_END
