# Plan fixer report — round 1

Source review: `_mill/reviews/20260802-134834-plan-review-r1.md` (holistic).

## Fixed

- `[BLOCKING] recoverbatch.go's own doc comments still claim "the SAME fork prompt"` — expanded Card 3's `recoverbatch.go` bullet to sweep the package-level header comment (lines 1–13) and `recoverSpawn`'s doc comment (~172–173) to name `RenderRecoveryPrompt` and drop the "SAME fork prompt" claim; expanded the `doc.go` bullet to also fix the stale "Plan consumption" sentence near line 17 that names `RenderForkPrompt` as a plan-level-section reader (now an unused `plan` param). Verified accurate against the discussion's own quote of recoverbatch.go's header and the `doc.go` grep hits; no harm.
- `[NIT] fork-prefix.md's cited source range includes the marker it must exclude` — narrowed Card 2's fork-prefix citation to "lines 20–27 EXCEPT line 24", explicitly dropping the `{{.pattern_directive}}` marker on line 24. Verified line 24 of `fork-template.md` is that marker.
- `[NIT] implementer-body.md's cited source range omits required sections` — widened Card 2's implementer-body citation from "lines 53–82" to "lines 43–82" so the FRESH-READ rule and `## Prior-batch context` section fall inside the cited range.

## Pushed Back

- (none)
