# Discussion fix — round 2

Source review: `_mill/reviews/20260702-164504-discussion-review-r2.md` (verdict APPROVE, 2 NOTEs).
Fixer: internalmux. Both NOTEs passed the `mill-receiving-review` decision tree (factually
accurate, fix causes no harm) → FIX. No push-backs.

## Fixed

- r2 NOTE1 — "Remainder-row assignment among co-equal full panes undefined": added a deterministic
  rule to the render **Derived height policy** decision — the integer-division remainder among ≥2
  co-equal full panes is assigned to the **active/bottom pane** (others get the floor), mirroring
  muxpoc's single-bottom-pane absorbing the leftover — so "heights + dividers exactly fill" stays a
  total, deterministic golden invariant. Added a matching Testing invariant (remainder determinism
  with ≥2 full panes).
- r2 NOTE2 — "Single mux.lock acquisition point unspecified (self-deadlock risk)": pinned the mux
  operation lock to **exactly one layer** in the Cross-process concurrency decision — acquired once
  at each **engine op** entry (composing from unexported unlocked helpers); **CLI verbs never take
  the lock**. Rationale recorded: `internal/lock` (gofrs/flock) is non-reentrant across separate
  handles even in-process on Windows, so a CLI-locks-then-engine-locks path would self-deadlock;
  one acquisition point removes that while still serializing the full cycle. Added a matching
  Testing assertion (single-layer acquisition, no CLI double-acquire / self-deadlock).

## Pushed Back

(none)
