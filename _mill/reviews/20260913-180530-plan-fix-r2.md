# Plan fix — round 2

Source review: `_mill/reviews/20260913-180312-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- r2 holistic — "[BLOCKING:scope] Card 20 points the new SpecsDir guard's negative-path test at the wrong file".
  Verified: `internal/shedrecipe/entries_simple_test.go`'s own header states it is table-driven over the nine value-only entries, and neither Bouncer nor BurlerRound is among them — no row there validates a stencils directory at all.
  The real analogues are the blank-stencils-directory subtest in `internal/shedrecipe/entries_bouncer_test.go` and the rubric-stencil-only one in `internal/shedrecipe/entries_burler_test.go`.
  Dropped `entries_simple_test.go` from card 20's `Edits:` (it needs no change — the value-only entries read neither guard) and added the two real files, with a requirement to mirror each existing blank-stencils-directory subtest for the new field, plus the companion the burler side needs: a blank specs directory with a LITERAL rubric must still construct cleanly, since that guard sits inside the stencil branch and must never fire on the literal route.
  Added an explicit instruction not to put these rows in the value-only table, so the same mistake is not re-made.
- r2 holistic — "[NIT:consistency] Card 20 omits the now-unused-import check it gives card 19 for the analogous file".
  Verified: lines 203, 208, and 211 of `internal/shedrecipe/entries_burler.go` are the only uses of `internal/stencilstore` and `internal/stencil` in that file, so replacing the pair leaves both imports unused and fails the build.
  Added the caveat to card 20, noting it applies to two packages there rather than card 19's one.

## Pushed Back

_None._
Both findings verified as factually accurate against the cited files, and neither fix breaks existing functionality, conflicts with a documented decision in `_mill/discussion.md`, or destabilises work outside this task.
