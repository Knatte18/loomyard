MILL_REVIEW_BEGIN
# Review: Add typed file-ops to lyx's plan-format — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-07-12
```

## Findings

### [NIT] `root: "."` degenerate case undocumented in plan-format.md
**Location:** `internal/builderengine/plan.go:544-553` (normalizeCardPath); `docs/modules/plan-format.md:288-305` ("Card path resolution: root: and //")
**Issue:** The parser special-cases `root: "."` to leave a card path unchanged (avoiding an unclean `"./path"`), which the fixture (`testdata/plan-valid/01-json-flag.md`, frontmatter `root: .`) and `plan_test.go`'s `` `root: "." joins to raw unchanged...` `` subtest deliberately exercise — but this is a fourth case beyond the three the `normalize-at-parse` Shared Decision text and the doc's "Card path resolution" section describe ("otherwise join `<root>/<path>` when `root:` is non-empty").
**Fix:** Add one sentence to plan-format.md's root:/`//` section noting the `root: "."` degenerate case resolves to the raw path unchanged (matching the code comment already at `normalizeCardPath`).

## Verdict

APPROVE
Implementation matches the plan end-to-end across all five batches: parser, all 18 named checks, templates, help text, and docs are consistent, tested, and cross-verified.
MILL_REVIEW_END
