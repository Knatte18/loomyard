# Plan fix — round 1

Source review: `_mill/reviews/20260805-130738-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 1 NIT).

## Fixed

- `20260805-130738-plan-review-r1.md` — "[BLOCKING] Card 3 deletes `Layout.Repo` but tells tokenvocab to keep reading it". Verified: card 3's `Edits:` contains both `tokenvocab.go` and `tokenvocab_test.go`, and `tokenvocab.go:25` reads `c.Layout.Repo`, so deleting the field would have broken batch 1's own green-tree gate. Card 3 now **keeps** the `Repo` field and pulls the `reponame-derivation` change forward into this card — `resolveCore` sets `Repo` to `strings.TrimSuffix(filepath.Base(hub), HubSuffix)`. That is what actually frees `Prime` to leave: `deriveRepo`'s `filepath.Base(prime)` branch loses its source, and its `worktreeRoot` branch is superseded, so the function still has no caller and is still deleted. The behaviour note about non-hub layouts moved to card 3's commit message, where the change now happens. The trailing sentence about `tokenvocab` was rewritten to say why the read is left alone (the field survives under its old name) and to name cards 11 and 17 as where it is renamed.
- `20260805-130738-plan-review-r1.md` — "[BLOCKING] Card 5's Moves list has a self-referential pair for anchor.go". Verified: the pair read `` `internal/lyxcwd/anchor.go` -> `internal/lyxcwd/anchor.go` ``, a `git mv` of a path that does not exist yet, and no other Moves entry captured the real rename. Cause was a path-retarget pass that rewrote cards 6 and 7 onto post-rename paths and also hit card 5's Moves *source*. Corrected to `` `internal/hubgeometry/anchor.go` -> `internal/lyxcwd/anchor.go` ``.
- `20260805-130738-plan-review-r1.md` — "[BLOCKING] Cards 6-7 reference pre-rename paths and the old type name after Card 5 already ran". Verified all three: card 6's `Edits:` named `internal/hubgeometry/hubgeometry.go`, its `ResolveWithAnchor` signature returned `*Layout`, and card 7's `Edits:` named `internal/hubgeometry/anchor_test.go`. Fixed to `internal/lyxcwd/lyxcwd.go`, `*Location`, and `internal/lyxcwd/anchor_test.go`. Also corrected the one remaining prose reference to the gate's location (`hubgeometry.go:118-127` now given as `internal/lyxcwd/lyxcwd.go`, with the pre-rename line range retained for locating it), and dropped the now-stale `internal/hubgeometry/anchor_test.go` entry from the overview's All Files Touched.
- `20260805-130738-plan-review-r1.md` — "[NIT] Batch 2 mixes a single directory-level move with 17 per-file Move pairs". Verified: the `## Rename mechanic` boilerplate instructs `git mv <old> <new>` per pair, which reads as seventeen commands. Card 5's Requirements now open by stating the rename is **one** `git mv internal/hubgeometry internal/lyxcwd` plus three follow-up `git mv` calls for the files whose basename also changes, that the per-file pairs document that operation's effect rather than being a command list, and that the Rename-mechanic per-pair instruction is satisfied by the directory move for the fourteen unchanged basenames.

## Pushed Back

_None. All four findings were factually accurate, and no fix broke existing functionality, conflicted with a documented design decision, or destabilised work outside this task._
