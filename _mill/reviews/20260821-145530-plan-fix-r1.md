# Plan fix — round 1

Source review: `_mill/reviews/20260821-144932-plan-review-r1.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 6 BLOCKING + 4 NIT).

Every finding was verified against source before acting.
All ten are accurate;
none met a harm-check exception.
All ten fixed, none pushed back.

## Fixed

- r1 `[BLOCKING:design]` "Roadmap-mandated sole-parser invariant dropped" — verified `manifest/roadmap.md:17` states this item is where the recipe-format sole-parser invariant belongs, "added to `CONSTRAINTS.md` in that same commit as a review obligation", premature until the first production recipe and first real consumer ship, which this task does.
  Card 23 now makes three edits, the third adding a Recipe-Format Sole-Parser Invariant modelled on the Planparser Sole-Parser Invariant (`CONSTRAINTS.md:518`), marked a review obligation with a candidate future guard.
  Batch 6's batch-local decision, which asserted no new invariant was needed, is rewritten;
  its old rationale conflated the coverage guard's row-name↔engine property with sole-parsing.
- r1 `[BLOCKING:scope]` "`shedrecipe.Env`'s declaring file is in no card's Context" — verified `Env`, `Constructor`, and `Config` are declared in `internal/shedrecipe/recipe.go:21-34`, not `env.go`, which holds only `requireAbsRoot`/`requireSeam`.
  Added `internal/shedrecipe/recipe.go` to the `Context:` of cards 3, 5, 7, 15, 16, and 18, and `internal/shedbuild/build.go` to card 7.
- r1 `[BLOCKING:design]` "\"`go build ./...` proves the test files compile\" is false" — accurate and the most consequential finding: `go build` never compiles `_test.go` files, so the claimed boundary check could not catch a stale test-side reference.
  The module-wide `verify:` changes from `go build ./...` to `go vet ./...`, which typechecks test files and runs clean on the current tree in 1.9s (measured).
  Batch 5's `verify:` gains `./internal/loomrecipe/...` — the package holding nine of the moved `New` sites, and precisely where a residual `loomshed.New`/`Deps` reference would have survived undetected to the done gate.
  Batch 6's `verify:` gains `./internal/preflightshed/ ./internal/shedrecipe/ ./internal/shedcheck/ ./internal/shedbuild/`, the packages its comment sweep edits.
  Every Batch Tests paragraph asserting the old premise is restated.
- r1 `[BLOCKING:scope]` "`internal/shedbuild/fixture_test.go` names two files this task moves" — verified at `internal/shedbuild/fixture_test.go:104-106`, naming both `internal/shedrecipe/coverage_guard_test.go` and `internal/loomshed/fixture_test.go`.
  Added the file to card 26's `Edits:` and to the overview's All Files Touched, with requirements naming both stale paths and instructing that the reference to the deleted `coverageGuardLandingDeps` be dropped rather than repointed.
  Card 26's sweep is widened from production files to every `.go` file, since two of its five sites are test files.
- r1 `[BLOCKING:scope]` "Falsified `loomshed.go:137-151` claims not covered by any sweep" — verified both `manifest/designs/shed-recipe.md:10` and `manifest/roadmap.md:14` carry the line-range reference, and that card 24's sweep tokens could not match it.
  Card 24 now names shed-recipe.md's "The idea" paragraph explicitly and the roadmap's section intro, and its sweep token set gains `loomshed.go:137-151` and `Go literal`.
- r1 `[BLOCKING:design]` "`contracts/recipes` has no home in the module tree" — verified `docs/overview.md`'s tree enumerates `cmd/lyx/` and `internal/*` only, closing with "everything else is in `internal/`", and that `contracts/stencils` is likewise absent despite being a production Go package.
  Card 25 now states the decision explicitly: add `internal/loomrecipe` to the tree, document `contracts/recipes` in prose only following the `contracts/stencils` precedent, and do not open a `contracts/` region — restructuring that tree would take both `contracts/` packages at once and is a larger doc change than this task should make as a side effect.
- r1 `[NIT:consistency]` "Row-1 substitution site count is off by one" — verified card 6's `TestNew_PassesShedValidation` calls `Run`, making eleven `Run`-driving `New` sites, not ten.
  Restated in both batch 2's Batch Tests and card 9, attributing the counts to cards 6, 8, and 9.
- r1 `[NIT:consistency]` "Card 14 miscounts `internal/shedbuild`'s surviving test files" — verified `fixture_test.go` survives and is consumed by `build_test.go`/`build_engines_test.go`.
  Card 14 and batch 3's Batch Tests now say seven, with `fixture_test.go` enumerated and a pointer to card 26 for its stale comment.
- r1 `[NIT:consistency]` "Self-contradicting sentence in card 6" — accurate;
  the sentence asserted a false premise and retracted it in the next line.
  Rewritten to state the requirement once.
- r1 `[NIT:scope]` "`RunAliasCommand`'s doc comment still names the deleted `deps` field" — verified at `internal/loomcli/run.go:303`.
  Card 17 now repairs that comment alongside its two path repoints in the same file, and its pre-commit grep gains a bare `deps` check across the four files it edits.

## Pushed Back

_None._
All ten findings verified accurate against source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope.
