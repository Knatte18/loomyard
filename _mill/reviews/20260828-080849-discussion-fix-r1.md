# Discussion fix report — round 1

Source review: `_mill/reviews/20260828-080812-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `20260828-080812-discussion-review-r1.md` — "[NIT:scope] Hook install site list omits the zero-live-pane / unknown-size attach gap": verified accurate against `apply.go`'s `len(live) < 2` early return and `attach.go`'s `cols <= 0 || rows <= 0` pre-lock return; recorded as a second accepted limitation under the `pins-are-a-snapshot-refreshed-at-every-apply` Decision, with the reason neither branch should install a hook (the single-pane case has no fixed-height budget to pin, since `render.Rules`' sole-cell branch gives the header the whole box; the no-size attach return is lock-free by contract) and the self-healing path out of it, plus a cross-reference from the Scope bullet naming the two install sites.

## Pushed Back

_None._
