# Plan fix — round 2

Source review: `20260912-111904-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `[NIT:scope]` "Card 13's loom.md edit site is underspecified" — verified accurate: `manifest/designs/loom.md` has no unified verb list analogous to `docs/overview.md`'s module bullet.
  Its only structured enumeration is the Module decomposition table (`| Piece | Form | Notes |`), whose rows include `` `loom` (`lyx loom run`) ``, `` `lyx loom status` ``, and `/ly-*` skills; every other verb mention is scattered prose across the Graceful-pause and state-contract sections.
  Card 13 now names that table as the edit site, specifies the new row's placement (immediately after the `` `lyx loom status` `` row), and pins its three cell values.

- Same defect, sibling card, fixed under the default fix-everything rule: batch 5 card 15 said to add `interrupt_policy` "wherever the `lyx loom status` envelope's keys are described".
  `loom.md` describes the status **file**'s fields (line 321) but never the CLI **envelope**'s key set, so that instruction named no site either.
  Card 15 now targets the same Module decomposition table's existing `` `lyx loom status` `` row, extending its Notes cell, and records why that is the site rather than a key list.

## Pushed Back

_None._
