# Discussion fix — round 1

Source review: `_mill/reviews/20260702-104026-discussion-review-r1.md` (verdict: APPROVE, 2 NOTE findings).

## Fixed

- **"Preserve path idempotency unspecified"** (Decisions → set-preserve-and-warn /
  Testing) — added an explicit idempotency guarantee to `set-preserve-and-warn`
  (unconditional `HeadComment` set, never appended, plus same-source-subtree
  re-grafting on every run) and a new idempotency test case to Testing (feed round-1
  `Merged` output back in as round-2 `existing`, assert byte-identical output).
- **"Non-flat orphan behavior not pinned"** (Decisions → flat-keys-only) — redesigned
  preservation from leaf-path granularity to root-key (top-level) granularity,
  renamed the decision to `root-key-preservation`, and updated Scope/Technical
  context/Testing accordingly. This closes the gap the reviewer identified (and is
  worse than stated: `configengine.Edit` validates only YAML syntax, not shape, so a
  hand-edited nested orphan is reachable today, not just hypothetical) without any
  special-case detection logic — every top-level orphan, flat or nested, is now
  preserved as a whole subtree.

## Pushed Back

None — both findings verified accurate on inspection, both fixes additive with no
conflicting design constraint or existing-functionality risk.
