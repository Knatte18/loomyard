# Plan-fix report — round 3

Source review: `_mill/reviews/20260710-074026-plan-review-r3.md` (holistic, APPROVE,
0 BLOCKING + 2 NIT). NITs applied under the default fix-everything rule before approving.

## Fixed

- `20260710-074026-plan-review-r3.md` — [NIT] "Card 3 Context omits config.go for `e.cfg.Psmux`":
  added `internal/muxengine/config.go` to card 3's `Context:`, matching card 6's treatment of the
  same `Config.Psmux` field access.
- `20260710-074026-plan-review-r3.md` — [NIT] "Card 6 leaves psmux `list-commands` support
  unverified on host": extended card 5's requirements so the implementer also runs
  `psmux list-commands` against the on-box binary and confirms it succeeds and emits the required
  subcommand names, halting if the installed psmux lacks `list-commands` — de-risking a card-7
  probe that would otherwise brick every `mux up` on Windows without batch 2's verify catching it.

## Pushed Back

_None — both NITs were factually accurate and their fixes carried no demonstrable harm._
