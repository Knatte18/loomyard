# Plan fix — round 4

Source review: `_mill/reviews/20260810-171705-plan-review-r4.md` (holistic, 1 BLOCKING, 0 NIT).

## Fixed

- `20260810-171705-plan-review-r4.md` — "[BLOCKING:scope] Card 8's Context omits clone.go, source of the `RemoveAll` seam it must call".
  Accurate: card 8 names the seam by identifier, and at the point card 8 runs the seam is still declared in `clone.go`, since card 10 relocates it later in the same batch.
  The implementer would have had to read a file its own allowlist did not grant.
  `internal/fabricengine/clone.go` added to card 8's `Context:`, and the requirement now states the ordering explicitly — call the seam by its bare identifier, do not qualify it, and do not move it, because card 10 owns the relocation.
  That second half matters as much as the Context entry: without it the natural reading of "call the package's seam" while holding `destroy.go` open is to move the declaration there, which would collide with card 10.

## Pushed Back

_None._
The finding is accurate and the fix breaks nothing, conflicts with no documented decision, and destabilises no other work.
