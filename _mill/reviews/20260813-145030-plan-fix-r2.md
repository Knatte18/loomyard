# Plan fix — round 2

Source review: `_mill/reviews/20260813-144745-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 1 NIT).

## Fixed

- `20260813-144745-plan-review-r2.md` — "[BLOCKING:design] Card 20's \"six wrong-string paths\" premise is stale against add.go's actual source".
  Verified against the code: `"cwd is not a valid git worktree"` appears nowhere in `internal/fabricengine/add.go`, and its only occurrences in the tree are in `internal/fabricengine/add_rollback_adopt_test.go` — once in a past-tense comment ("Six paths in Add **used to** answer…") and once as a negative assertion in `TestAdd_GitFailureCarriesGitsOwnReason`.
  `add.go`'s failure paths already carry git's own stderr.
  The stale clause is dropped from card 20, replaced by a statement that every remaining site there is a plain two-message merge plus an instruction not to reword the existing messages beyond what the merge requires — which preserves the out-of-scope boundary the stale clause was serving without asserting a false premise.
  Note for the record: the same stale claim sits in `_mill/discussion.md`'s `## Scope` "Out" list and in `manifest/designs/gitexec-error-shape.md`.
  Neither was edited — Plan Review fixes apply only to files under `_mill/plan/`, and the design doc is deleted outright by card 37 — but the plan no longer inherits the claim from either.
  This is a live instance of the discussion's own working rule: do not take a design decision from a doc file, read the code.

- `20260813-144745-plan-review-r2.md` — "[NIT:consistency] \"deliberate-discards-migrate-as-discards\" overstates how many sites carry `//nolint:errcheck`".
  Verified: only `prune.go`'s two sites carry the comment;
  `reconcile.go` and `remove.go` already discard via the bare `_, _, _, _ =` form.
  The Shared Decision in `00-overview.md` now says exactly that.
  Batch 4's card 22 was already correct in count but said "both" while covering three files, so it now names `prune.go` as where the pair sits and states that `remove.go` has nothing to remove.

## Pushed Back

_None._
