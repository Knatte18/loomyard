MILL_REVIEW_BEGIN
# Review: shedengine: per-producer bounce budget + explicit OnDone routing — holistic

```yaml
duration_s: 529.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [NIT:consistency] Card 7's `linearChain` signature contradicts its own next sentence
**Demoted-from:** BLOCKING
**Location:** batch 2 / card 7 **Issue:** The card states the signature as `linearChain(names []string, producers []ShedProducer) []ProducerDef`, then two sentences later requires "take a `*testing.T` as its first parameter, call `t.Helper()`, and `t.Fatalf` on a mismatch" — a parameter the given signature omits entirely. Every other helper in `testsupport_test.go` (`newTestShed(t *testing.T)`, `seedStatus(t *testing.T, ...)`, etc.) takes `t` first, so the intended signature is almost certainly `linearChain(t *testing.T, names []string, producers []ShedProducer) []ProducerDef`, but the card as written hands the implementer two contradictory instructions. **Fix:** Restate the signature to include `t *testing.T` as the first parameter, matching the file's existing helper style.

### [NIT:consistency] Card 19's "following item" cross-reference claim is inaccurate
**Location:** batch 4 / card 19 **Issue:** Card 19 says to leave the remaining Planned items untouched, "including the following item's cross-reference to this one by bold name." The item immediately following (`shedadapters: Burler-round producer`, `roadmap.md:24`) refers back only as "(previous item)," never by bold name; the actual bold-name backreference to `**shedengine: per-producer bounce budget + explicit \`OnDone\` routing**` sits several items later, at `roadmap.md:46`, inside `loom: Discussion-Review producer` in a different subsection. Since the card's actual instruction is just "don't touch other items" (which the plan doesn't ask to violate), this is a narrative inaccuracy rather than an edit risk. **Fix:** Correct the card's own reasoning to point at `roadmap.md:46`'s actual backreference, or drop the specific claim.

### [NIT:consistency] `parallel-work.md` still lists the shipping task under "Can start now" after card 19's own edit
**Location:** batch 4 / card 19 **Issue:** Card 19 corrects the `internal/shedengine`-only scope claim on the `parallel-work.md` bullet for this very task, but in the same commit the companion `roadmap.md` edit moves this task to Done — leaving the corrected bullet still sitting under "## Can start now, no caveats" for a task that has just shipped. The file explicitly disclaims durability ("delete or regenerate freely"), so this is not blocking, but the card could note the staleness rather than silently leave it. **Fix:** Either drop the bullet or note in the commit message that `parallel-work.md`'s snapshot is now stale for this entry, consistent with the file's own regeneration policy.

## Verdict

APPROVE
Fix card 7's contradictory `linearChain` signature before landing; the two NITs are optional polish.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
