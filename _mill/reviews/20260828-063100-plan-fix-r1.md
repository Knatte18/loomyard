# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260828-062348-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 5 NIT).

## Fixed

- r1 · "[BLOCKING:design] Card 12 says \"Remove\" the roadmap item; the file's own convention says \"Move to Done\"" — verified against `manifest/roadmap.md`'s own `## Maintenance` note, which states verbatim "Move an item from Planned or Someday to Done, with a link to its module doc if one exists, when it ships". Card 12 now instructs a move into `## Done`, rewritten to Done-entry length, pointing at `contracts/specs/final-summary-spec.md`, with the literal `1.` list marker; its `Commit:` message changed from "mark … complete" to "move … to Done".
- r1 · "[BLOCKING:scope] docs/overview.md's \"Other docs\" entry for webster-spec.md goes stale, untouched by Card 11" — verified at `docs/overview.md:445-446`, which describes `webster-spec.md` as covering "the `summary.md` artifact Finalize consumes". Card 11 now names three edit sites instead of two: the third rewrites that entry to what `webster-spec.md` still covers and adds a sibling `final-summary-spec.md` entry to the same list naming both consumers.
- r1 · "[NIT:consistency] Batch-2 verify in 00-overview.md drops the integration-tagged vet pass" — verified: the batch file's own header carried `go vet -tags integration ./...` and the overview's authoritative `batches:` block did not. Added to the overview's batch-2 verify line so the two copies match.
- r1 · "[NIT:consistency] Card 10 fixes one Finalize/Publish mislabel in webster-spec.md but leaves an identical one two lines above" — verified at `contracts/specs/webster-spec.md:41` ("It is Finalize's PR-text source"). Card 10's Requirements now instruct correcting that clause too, pointing it at `final-summary-spec.md` for both consumers rather than naming either one wrongly, while keeping the Master-oversight reason.
- r1 · "[NIT:consistency] Card 6 names the wrong RecordBatch deps type" — verified at `internal/websterengine/recordbatch.go:46`: the type is `RecordDeps`. Both occurrences in card 6 corrected.
- r1 · "[NIT:scope] Card 6 undercounts the `wantPath` assertion sites in webster_test.go" — verified: `internal/shedadapters/webster_test.go` has two, at lines 51 and 232. Card 6 now says "each occurrence" and names both enclosing tests.
- r1 · "[NIT:consistency] Card 5's claim that the \"fourteen fields\"/\"fifteenth field\" comments \"stay accurate\" is false today" — verified by counting `internal/landingshed/deps.go`: `Deps` carries fifteen fields, so both comments are already stale. Card 5's false claim is replaced with an explicit statement of the pre-existing drift, and the card now corrects both counts as a drive-by: `internal/loomcli/landingdeps_test.go` was already in its `Edits:` list, and `internal/shedrecipe/recipe.go` was promoted from `Context:` to `Edits:` for the one-word fix, with an explicit note that nothing else in that file changes. `internal/shedrecipe/recipe.go` was added to the overview's All Files Touched accordingly.

## Pushed Back

_None. Every finding verified as factually accurate against the cited source, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task's scope._

## Re-validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate`: OK.
`_plan_validate.run` with an empty `skip_checks`: 0 findings.
