MILL_REVIEW_BEGIN
# Review: Rename the fabric host vocabulary to warp, and name the composite repo Fabric — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-09
```

## Findings

### [BLOCKING] Card 8 dry-run count says "six", lists and yields five
**Location:** batch 03-go-sweep, card 8
**Issue:** The card instructs the implementer to "confirm the report shows ... an AMBIGUOUS (unresolved) bucket of exactly six entries" but then enumerates only five paths (`drift.go:3`, `hostclean.go:1`, `hostjunction_test.go:1`, `hostlayout.go:1`, `lyxtest.go:128`). Independently re-deriving the tool's own boundary rule (cards 3–4) against every file in card 8's `Edits:` set confirms five AMBIGUOUS occurrences exist, not six — card 9's `Edits:`/Requirements list matches five, not six, so the "six" is the outlier, not an omitted sixth file.
**Fix:** Change "exactly six entries" to "exactly five entries" in card 8.

## Missing context
(not applicable — verdict is REQUEST_CHANGES, not NEED_CONTEXT)

### [NIT] Card 10 pre-verified occurrence counts are inaccurate
**Location:** batch 03-go-sweep, card 10
**Issue:** Card 10 states `hostPath` (47) merges into `warpPath` (321) and `hostBare` (29) merges into `warpBare`. Grepping `internal/fabricengine` today gives `hostPath`=44/6 files, `warpPath`=312/22 files, `hostBare`=25/2 files, `warpBare`=6/1 file — all lower than claimed. The load-bearing conclusion (zero files contain both names, so no shadowing) still holds under the real counts, but the cited numbers are wrong.
**Fix:** Re-grep and correct the four occurrence counts, or drop the specific numbers and keep only the "verified zero overlapping files" claim.

## Verdict

REQUEST_CHANGES
Fix card 8's ambiguous-entry count; card 10's stale counts should be corrected too.
MILL_REVIEW_END
