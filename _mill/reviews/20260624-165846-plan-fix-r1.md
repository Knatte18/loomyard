# Plan fix — round 1

Source review: `_mill/reviews/20260624-165835-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING, 1 NIT).

## Fixed

- `20260624-165835-plan-review-r1.md` — [BLOCKING] "Cards 5/7/11/13 orphan path/filepath import" (partial — the 3 genuinely-affected cards): verified by grep that `internal/configcli/configcli_test.go` (Card 5), `internal/configsync/configsync_test.go` (Card 7), and `internal/update/update_test.go` (Card 13) have **zero** surviving `filepath.` references after conversion (every site is a combined-form `_lyx/config` or `*.yaml` construction). Updated all three cards to instruct removal of the now-unused `path/filepath` import (keep `os`). Added a "`path/filepath` orphan rule" to the overview Shared Decision naming exactly these three files and explaining why bare-`_lyx`→`paths.LyxDirName` files keep the import while combined-form files do not.
- `20260624-165835-plan-review-r1.md` — [NIT] "Batches 2-4 sweep exceeds the cited constraint and the task": added a new Shared Decision ("batch 1 fixes the bug; batches 2-4 are a deliberate operator-requested sweep") clarifying that batches 2-4 are a sanctioned consistency sweep grounded in the operator instruction and the extended `CONSTRAINTS.md` rule, not a build-enforced constraint fix — so reviewers do not read them as scope creep.

## Pushed Back

- `20260624-165835-plan-review-r1.md` — [BLOCKING] portion naming **Card 11 (`cmd/lyx/main_test.go`)** as orphaning `path/filepath`: **factually inaccurate.** `main_test.go` has 8 `filepath.` sites; three of them are `lyxDir := filepath.Join(cwd, "_lyx")` (lines 47/83/187), which the recipe converts to `filepath.Join(cwd, paths.LyxDirName)` — these **retain** a `filepath.` reference, so the `path/filepath` import is still used and removing it would itself break the build. Grep confirms 3 surviving `filepath.` refs post-conversion. Card 11 correctly keeps `path/filepath`; clarifying note added to the card so it is not re-flagged. (The reviewer overlooked that the bare-`_lyx` substitution keeps `filepath.Join`.)
