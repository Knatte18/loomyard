MILL_REVIEW_BEGIN
# Review: Audit the remaining leaf and seam import invariants — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-08
```

## Findings

None. Verified against the plan card-by-card:

- Card 1: `internal/lyxtest/leaf_enforcement_test.go` converted from denylist to allowlist, matches `internal/pattern/leaf_enforcement_test.go`'s shape exactly (stdlib test, `go/parser`/`ImportsOnly`, `filepath.WalkDir`, single trailing `t.Errorf`). `allowedImports` holds exactly the three required entries. Confirmed against `lyxtest.go`/`hermetic.go`/`reexecguard.go` that this is the package's true non-stdlib import closure (only `lyxtest.go` imports `configengine`/`lyxcwd`/`weftname`; the other two are stdlib-only). Function renamed to `TestLeafInvariant_AllowlistOnly`; file-header and function-doc comments rewritten off the denylist framing and both retain the test-build-cycle reason in one clause. `CONSTRAINTS.md`'s lyxtest "Enforced by" line now names the function, matching the sibling-section shape (Modelspec, Treadle). `internal/shuttleengine/seam_enforcement_test.go`'s cross-reference retargeted to `TestLeafInvariant_AllowlistOnly`, still citing lyxtest as origin, unchanged otherwise.
- Card 2: `internal/lyxtest/doc.go`'s Leaf Invariant paragraph rewritten to an allowlist statement mirroring the corrected `CONSTRAINTS.md` sentence, names `TestLeafInvariant_AllowlistOnly`, keeps the test-build-cycle clause, leaves the `SeedConfig` sentence and the Hermetic Git Test Environment section untouched.
- Card 3: `internal/treadleengine/doc.go`'s geometry-blindness section and `engine.go`'s `Engine` doc both correctly scope the claim to "never imports lyxcwd directly," state the direct-import ban is a discipline rather than an isolation guarantee, and leave the fabricengine claim and the `runDir`/`Profile`/`GateDir` mechanism prose unchanged. No bare `weft`/`warp` token or fabric-sense `host` phrase introduced.
- Card 4: `internal/modelspec/leaf_enforcement_test.go`'s file header no longer contrasts against lyxtest's mechanism; the ALLOWLIST payload sentence is kept verbatim; the first sentence naming the invariant and allowed set is untouched.
- Card 5 (zero-diff gate): confirmed untouched — `internal/perchengine/doc.go`, `internal/perchengine/engine.go`, `internal/burlerengine/doc.go`, `internal/treadleengine/seam_enforcement_test.go`, `internal/tokenvocab/doc.go` all read as the plan describes (already-accurate, no isolation overstatement). No scoutengine path appears anywhere in the reviewed set. "All Files Touched" in the overview matches exactly the files actually changed — no out-of-plan files present.
- Cross-batch/single-batch contract: this is a single-batch plan, so no producer/consumer seam to check across batches; the allowlist shape is consistently reused (pattern → lyxtest) per the Shared Decision, and no helper is duplicated beyond the established sibling-file convention already used by modelspec/pattern/treadleengine's own enforcement tests.
- Card 1(e)/(f) (negative control, commit body content) cannot be verified by a Read/Grep/Glob-only reviewer with no git access; nothing in the source contradicts the plan's requirements, so this is not treated as a finding.

## Verdict

APPROVE
All five cards match the plan and shared decisions; no out-of-plan files, no constraint violations, no stale isolation claims remain.
MILL_REVIEW_END
