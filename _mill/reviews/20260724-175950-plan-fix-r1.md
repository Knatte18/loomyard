# Plan-fix report — round 1

Source review: `_mill/reviews/20260724-175805-plan-review-r1.md` (verdict APPROVE, 3 NITs).

## Fixed

- [NIT] "PushCoalesced loop can spin vs board's bounded loop" — Batch 2 / Card 6: replaced the
  unbounded `hasUnpushed()` loop with a **single guarded push** under the lock (`hasUnpushed()` →
  `pushWithRebaseRetry()` once). `git push` sends all ahead-of-upstream commits atomically and
  cross-process coalescing is handled by the lock queue, so no loop is needed — and the loop could
  spin forever when a push succeeds without configuring an upstream. Updated Card 6 Requirements
  and the batch-02 scope paragraph.
- [NIT] "Roadmap Done entry should not link, per Maintenance convention" — Batch 4 / Card 11:
  changed the requirement to reference `` `internal/gitrepo` `` as an inline code-span in the
  existing Done-entry style, not a markdown link, per roadmap.md's Maintenance convention.
- [NIT] "HermeticGitEnv defined in hermetic.go, not the listed lyxtest.go" — Batch 1 / Card 4:
  added `internal/lyxtest/hermetic.go` to the card's `Context:` allowlist.

## Pushed Back

_None — all three NITs were factually accurate and applied under the fix-everything default._
