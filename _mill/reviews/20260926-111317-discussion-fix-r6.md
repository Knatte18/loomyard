# Discussion fix — round 6

Source: `20260926-111303-discussion-review-r6.md`.

## Fixed

- "Recipe header's escalate-set ignores new row" — the row-count decision now says the yaml header gains a sentence placing `Friction-Reflect` outside the escalate set (no `on_stuck`, but always `Done`), keeping "eight" true.
- "Lock-wait test has no named home" — the test is placed in `internal/loomcli` over a minimal `shedengine.Shed` wrapping the real row-path closure, with fast-failing Reflect deps, and must assert `done` persistence.

## Pushed Back

(none)
