# Plan fix — round 2

Source review: `_mill/reviews/20260815-095934-plan-review-r2.md`

## Fixed

- `[NIT:consistency]` "Card 7/Card 8 disagree on whether card 7 calls `persist`" — verified accurate: card 7 required `go build ./internal/shedengine/` to succeed at its own end while also directing the pause exit to be written "against [`persist`'s] signature", a method card 8 does not create until the next card.
  Card 7's step-3 requirement now says the pause exit's write is implemented **inline** as a direct `state.UpdateJSON` call and explicitly forbids calling a `persist` method;
  card 7's closing build note now names that as the reason;
  card 8's follow-up now reads as a refactor of that inline write into the shared method, deleting the call it replaces.
  Added `internal/shedengine/activity.go` to card 7's `Context:`, since the inline write composes `activity`.

- `[NIT:consistency]` "Batch 3 doesn't pin the external-writer mechanism batch 4 pins explicitly" — verified accurate: batch 4's local decision pins `state.UpdateJSON` against a lenient map on the shared `StatusLockPath`, while batch 3's card 14 mutated `pause_requested` mid-`Call` with no mechanism named, leaving a bare unlocked write open to an implementer.
  Batch 3's Batch Scope now carries the identical local decision, scoped to status-file mutations and explicitly excluding context cancellation (not a status-file write);
  card 14's requirement names the mechanism at the point of use;
  and `internal/state/state.go` is added to card 14's `Context:`.

## Pushed Back

_None._
