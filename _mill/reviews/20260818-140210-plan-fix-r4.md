# Fixer report: plan-fix round 4 — the standalone CLI path

```yaml
round: 4
review: 20260818-140210-plan-review-r4.md
fixed: 1
pushed_back: 0
converged: false
```

## Fixed

- `20260818-140210-plan-review-r4.md` — "[NIT:consistency] wiring_test.go's pre-existing standalone cases never get LOCALAPPDATA" (demoted from BLOCKING by the stage ceiling): extended batch 3 card 11's Requirements to add `t.Setenv("LOCALAPPDATA", t.TempDir())` alongside the existing `XDG_STATE_HOME` redirect in every `wireStandalone`-reaching case in `internal/webstercli/wiring_test.go`.
  Verified accurate.
  This is the same gap round 3 closed for card 12's `cli_integration_test.go` in the same package, left open one file over: round 2's fix reached the integration file and round 3's reasoning never revisited the untagged one.
  The named cases are `TestWire_HubPresentFalseSelectsStandaloneMode`, the three standalone subtests of `TestWire_PlanDirResolution`, `TestWire_StandaloneRootsResolveToTarget`, and `TestWire_MatcherNeverNilOpenerNilOnlyInStandalone`'s `StandaloneMode` subtest.
  The card instructs the implementer to re-derive that set against the file rather than trust the enumeration, since the rule — every case passing `preflight.ModeStandalone` — is what actually determines membership.
  Also required the file header's own `XDG_STATE_HOME` sentence to name both variables, so the file documents what it does.

## Pushed Back

_None._

## Convergence

Round 4's verdict is `APPROVE` with `blocking_count: 0`, but the round's single finding carries `demoted: true`, so the convergence gate evaluates to `converged = false`:
the stage's blocking-class ceiling turned what the reviewer judged a BLOCKING into a NIT, and the gate exists precisely so a ceiling-demoted BLOCKING cannot terminate the loop early.
`round` is 4 and `max_review_rounds` is 7, so the NIT fix, this report, and the DAG plus validator re-runs all happen — real work either way — while the terminal actions (flipping `approved: true`, breaking to Handoff) are deliberately withheld.
The loop continues to round 5.
