# Discussion fix — round 1

Source review: `_mill/reviews/20260713-090034-discussion-review-r1.md` (APPROVE, 2 NOTEs).

## Fixed

- "tierpurity rationale overstates spawn removal" — reworded the
  tierpurity-evasion-left-alone rationale: the four remaining untagged
  `execGateCommand` tests still spawn cheap `go` processes through the wrapper;
  claim is now "no expensive/real-time spawn remains", not "no spawn".
- "boardtest seeded-count knob has hidden coupled literals" — the
  boardtest-bounded-shrink decision and Technical context now steer the bounded
  attempt to the uncoupled `writes = 50` knob first and document the
  `GetTask("task-50")` / count-stays-100 lockstep coupling on the seeded `100`.

## Pushed Back

- None.
