MILL_REVIEW_BEGIN
# Review: fabric: fold snapshot-tracking into the Warp-SHA trailer — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (claude-sonnet-4-5), per the harness's own stated model ID "claude-sonnet-5" — I self-identify as the Sonnet family model currently serving this session
reviewed_file: plan/
date: 2026-07-31
```

## Findings

### [BLOCKING] Card 12 mentions Topology.Checkout, whose file is not in Context
**Location:** batch 3 (snapshot-reader) / card 12
**Issue:** The requirement text instructs "Do not reach for `Topology.Checkout` here" and explains its `*hubgeometry.Layout` requirement — `Topology.Checkout` is defined in `internal/fabricengine/checkout.go`, which is not listed in card 12's `Context:` (snapshot.go, index.go, index_integration_test.go, trailer.go, weftgit.go, testmain_test.go).
**Fix:** Add `internal/fabricengine/checkout.go` to card 12's `Context:` list. Low practical risk since the instruction is self-contained (full rationale given inline, no exploration needed to comply), but it is a literal Context-completeness gap per the review criteria.

## NITs

### [NIT] ErrInvalidSHA doc-fix in card 2 leaves a pre-existing undercount
**Location:** batch 1 (retire-ref-mechanism) / card 2
**Issue:** Card 2 says `ErrInvalidSHA`'s doc "names `SetSnapshotSHA` as one of its two returners (`ChangedFilesSince` survives and is the remaining one)" — but `gitrepo.go`'s `ErrInvalidSHA` comment already omits two other real returners today, `CheckoutDetached` (gitrepo.go) and `ResetHard` (reset.go), a pre-existing staleness unrelated to snapshot but touched by this exact edit.
**Fix:** Since this card already rewrites this comment, enumerate all current returners (`ChangedFilesSince`, `CheckoutDetached`, `ResetHard`) rather than leaving the doc undercounting by two.

### [NIT] "three other call sites" miscounts forcePackIndexFreeze's survivors
**Location:** batch 1 (retire-ref-mechanism) / card 1
**Issue:** Card 1 says to "keep `forcePackIndexFreeze` and its three other call sites" in `parity_test.go`, but the helper has exactly 3 call sites today total, one of which (inside `TestSetSnapshotSHA_MixedBackend_RepackBetweenCommitAndCanonicalization`) is deleted by this same card — leaving 2 surviving call sites, not 3.
**Fix:** Correct to "its two other call sites."

### [NIT] GIT_COMMITTER_DATE instruction references a nonexistent env-override convention
**Location:** batch 3 (snapshot-reader) / card 13
**Issue:** Card 13 says to set `GIT_COMMITTER_DATE` and "read how the existing fixtures spawn git before adding an environment override, and follow that convention" — but neither `lyxtest.MustRun` nor `gitexec.RunGit` supports per-call env vars, and no existing test in this package sets a git env var; there is no override convention to follow. A careless implementation (package-level `os.Setenv`) would leak into concurrently-running `t.Parallel()` tests in the same package (several exist in `index_integration_test.go`/`weftgit_pathspec_integration_test.go`).
**Fix:** State explicitly that the fixture must bypass `lyxtest.MustRun`/`gitexec.RunGit` for this one commit and set `cmd.Env` scoped to that single `exec.Command` call (mirroring `syncweft_integration_test.go`'s existing raw-`exec.Command` helpers), never a process-wide `os.Setenv`.

### [NIT] Card 18's TDD instruction conflicts with its position after cards 15/16
**Location:** batch 4 (empty-commit-rule) / card 18
**Issue:** Card 18 says "write both before cards 15 and 16 are complete, watch them fail, then make them pass" — but card 18 is numbered/ordered after cards 15 and 16 in the same batch, so if cards commit in listed order, 15/16's implementation is already landed by the time card 18 is reached and the tests cannot literally be observed failing at that point.
**Fix:** Clarify that the "watch them fail" step happens by drafting/running the two tests in the working tree before committing 15/16's changes, then committing the test file itself last, as card 18 — or explicitly describe the revert-and-confirm pattern already used elsewhere in this codebase's crucible rounds.

## Verdict

REQUEST_CHANGES
One Context-completeness gap (card 12/Topology.Checkout); otherwise an exceptionally well-verified plan with only minor NITs.
MILL_REVIEW_END
