# Plan fix — round 1

Source review: `_mill/reviews/20260809-062003-plan-review-r1.md`

## Fixed

- r1 `[BLOCKING] Card 8 dry-run count says "six", lists and yields five` — accurate;
  batch `03-go-sweep` card 8 now reads "exactly five entries", matching its own enumeration, card 9's five-entry `Edits:` list, and the pre-plan grep that predicted exactly five `host`+lowercase occurrences surviving batch 2's rewords.
- r1 `[NIT] Card 10 pre-verified occurrence counts are inaccurate` — partially accurate, fixed via the finding's own second remedy.
  The reviewer's replacement numbers are themselves wrong (`hostPath` is 47 repo-wide, not 44;
  `hostBare` is 29 repo-wide, not 25 — the reviewer appears to have scoped its grep to `internal/fabricengine` only), but its core claim holds: `warpPath` is 338 repo-wide / 317 in `fabricengine`, never the 321 the card cited.
  Rather than substitute one drifting number set for another, card 10 now drops all four counts and keeps only the load-bearing property — zero files contain both names of a merged pair — which I re-verified independently across `internal/`, `cmd/` and `tools/`.
  A sentence was added recording why no count is pinned: counts drift with every commit and would fire as a false tripwire, exactly the reasoning `_mill/discussion.md` already applies to the ambiguity report ("treat the report as the work list, not as a checksum").

## Pushed Back

_(none — both findings were fixed)_
