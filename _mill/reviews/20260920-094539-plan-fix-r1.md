# Plan fix — round 1

Source review: `_mill/reviews/20260920-094408-plan-review-r1.md`

## Fixed

- `[BLOCKING:scope]` "Card 14's signature change breaks `internal/loomcli/arm.go`'s own call site" — verified accurate: `internal/loomcli/arm.go:363` destructures three values from `seedAndCommitBootstrap`, and card 14 grows its arity. Added `internal/loomcli/arm.go` **and** `internal/loomcli/start.go` to card 14's `Edits:`, with a requirement that both call sites are updated in card 14's own commit. The reviewer named only `arm.go`; `start.go:85` is a second three-value destructure with the same breakage, and deferring it to card 15 would leave card 14's own commit non-compiling, so both are pulled into this card.
- `[NIT:consistency]` "Card 14 grows a return value that card 15 never consumes" — verified accurate: card 14's stated rationale and card 15's `ReadSeed` requirement contradicted each other. Resolved in card 14's favour, since it is the better design: card 15 now consumes the returned driver and is explicitly forbidden from adding a second `shedrun.ReadSeed` call.
- `[NIT:scope]` "Card 10's 30000 bytes fact lives in a file outside its Context" — verified accurate: `maxLaunchPromptBytes = 30000` is declared in `internal/shuttleengine/claudeengine/command.go` and only enforced in `claudeengine.go`. Added `command.go` to card 10's `Context:` and replaced both bare-number citations with the constant name plus its declaring and enforcing files.

## Pushed Back

_None._
