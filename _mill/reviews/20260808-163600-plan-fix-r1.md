# Plan fix — round 1

Source review: `_mill/reviews/20260808-163000-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING / 3 NIT).

## Fixed

- `20260808-163000-plan-review-r1.md` — "[BLOCKING] Card 9 contradicts itself on line 199's pattern.DirName".
  Verified: line 199 is `junctions := HostJunctions(loc, "slug", []string{"_lyx", pattern.DirName})`, inside the `no_raddle_names` sub-test body (192-205).
  Resolved by splitting the sub-test's two halves explicitly: batch 3 card 9 retargets line 199's junction-name **input** now (it carries no `_raddle` claim), and only the `_raddle` **assertion** at lines 200-204 plus the sub-test's title are deferred.
  Added `internal/fabricengine/hostjunction_test.go` to batch 4 card 21's `Edits:` with the deferred half spelled out, so the deferral now has a named home instead of dangling.
- `20260808-163000-plan-review-r1.md` — "[BLOCKING] Card 29's `_raddle` comment-block instruction is self-contradictory".
  Verified: lines 277-280 are one shared block covering `_portals`, `_launchers`, and `_raddle`, feeding rows 281-283.
  Card 29 now states that block is **rewritten** (drop the `_raddle` clause, keep the `_portals`/`_launchers` rationale), and that only row 283, row 299, and the `_pattern`-exclusive block at 293-298 are deleted outright.
- `20260808-163000-plan-review-r1.md` — "[BLOCKING] `pattern.DirName`/`PathspecFile` referenced without `internal/pattern/pattern.go` in Context".
  Verified against each card's Context block.
  Added `internal/pattern/pattern.go` to `Context:` for cards 9, 10, 13, 14, 15, 16, 22, and 26.
- `20260808-163000-plan-review-r1.md` — "[NIT] Card 8 overcounts `pattern.DirName` occurrences in `pull_integration_test.go`".
  Verified: exactly two occurrences exist (lines 254 and 288), so one remains after `wantPath`.
  Card 8 now names line 254 specifically, classifies it as a PATTERN-path use rather than a junction name, and requires the file to end with zero `pattern.DirName` references.
- `20260808-163000-plan-review-r1.md` — "[NIT] Card 40 has two off-by-one line citations" (the `fabric.go` half).
  Verified: the pollution-scan sentence runs 191-194 while 188-190 is the `junction_healthy`/`junction_reason` sentence.
  Card 40 now cites the two sentences separately with their correct ranges.
- `20260808-163000-plan-review-r1.md` — "[NIT] Card 13's `_extra`-seeding fallback may not be achievable for `TestRunCLI_CloneEndToEnd`".
  Verified: the fixture clones from an empty bare weft, so there is no `weft:main` config to seed pre-clone.
  Replaced the seeding fallback with a concrete instruction: assert `{lyxdirs.LyxDirName, lyxdirs.DotLyxDirName}`, which preserves the check's real content (more than one junction wired) and is true both before and after batch 4, because `.lyx` is structural and wired by every clone regardless of `pathspec`.

## Pushed Back

- `20260808-163000-plan-review-r1.md` — "[NIT] Card 40 has two off-by-one line citations" (the `weft_verbs.go` half only).
  Factually wrong: the finding claims the "pathspec names only `_pattern`" mention is at line 94, but line 93 reads `repo's pathspec names only _pattern, so a raw-Dirs() sync would silently stop syncing` — the `_pattern` token is on line 93, which is what the card already cites.
  Line 94 begins `_lyx entirely. The routing set can never name .lyx either`.
  The comment sentence spans 91-95; the card's citation lands on the exact line carrying the token being changed, so no shift is correct.
  The `fabric.go` half of this same finding was accurate and is fixed above.

## Re-validation

`_plan_dag.validate` — OK.
`_plan_validate.run` — 0 findings.
