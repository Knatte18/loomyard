# Plan fix report — round 1

## Fixed

- `20260626-070136-plan-review-r1.md` — "[BLOCKING] Card 8 deletion drops mispointed-junction coverage": converted card 8 from delete to **fold** — the wrong-target assertion is added as a third sequential step in `TestPairInSync_BrokenJunction` (repoint to decoy, re-check), then the standalone test is removed. Build saving preserved, coverage retained.
- `20260626-070136-plan-review-r1.md` — "[BLOCKING] Card 10 deletion drops report-only pollution coverage": converted card 10 from delete to **fold** — the `_codeguide` `ReportOnly=true`/empty-`Remedy` assertions are added as a second sequential phase of `TestStatus_LyxPollutionDetected` on one fixture, then the standalone test is removed.
- `20260626-070136-plan-review-r1.md` — "[NIT] Card 4 mischaracterizes a table row as a test function": reworded card 4 to remove the `HardRequireWeftRepo` **row** from the `TestWeftPrechecks` table, leaving the `RejectExistingWeftWorktree` row.
- `20260626-070136-plan-review-r1.md` — "[NIT] Card 6 drops the only missing-parent rollback trigger": kept the genuine `ForkPointMirrorsHost` subset-delete, but converted `TestWeftMissingParentBranch` from delete to **fold** — its missing-parent trigger + zero-residue assertions are added as a second sequential scenario in `TestWeftRollbackOnPostHostCreateFailure`.
- `20260626-070136-plan-review-r1.md` — "[NIT] Card 14 merge does not fit the single-call table body": specified that the merged `HostDirty`/`WeftDirty` cases become standalone `t.Run` sequential subtests beside the `TestRemove` table (which issues one `Remove` per row), leaving `HappyPath`/`NonexistentSlug` as rows.
- `20260626-070136-plan-review-r1.md` — "[NIT] Card 21 drops subpath-collision assertion": converted card 21 from delete to **fold** — the `link1 != link2` two-subpath assertion is added as a sequential step in `TestCreatePortal`, then the standalone test is removed.

## Pushed Back

(none)
