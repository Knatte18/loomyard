# Plan fix report — round 1

Source review: `_mill/reviews/20260702-072035-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING).

## Fixed

- `20260702-072035-plan-review-r1.md` — "[BLOCKING] Wrong filepath.Dir walk-up count for repo root": corrected Batch 2 / Card 4 step (b) from "two `filepath.Dir` walk-ups" to **three**, matching the authoritative code at `cmd/lyx/registration_test.go:71` (`filepath.Dir(filepath.Dir(filepath.Dir(testFile)))`) and spelling out the `cmd/lyx/… → cmd/lyx → cmd → repo root` walk. Verified: `registration_test.go:71` uses three calls; its inline comment saying "two" is stale, and following the stale count would resolve to `.../cmd` and break the doc read the new test depends on.

## Pushed Back

_(none)_
