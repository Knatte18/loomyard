MILL_REVIEW_BEGIN
# Review: fabric: close the weft-visibility leak (slice 8) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude Opus 4.x-class model (Anthropic), running in tool-use mode
reviewed_file: plan/
date: 2026-08-06
```

## Findings

### [BLOCKING] Card 14 leaves fabricengine tests non-compiling
**Location:** batch 04 / card 14 (and card 15)
**Issue:** `internal/fabricengine/fabric_test.go` is `package fabricengine_test` and calls `fabricengine.New(...)` at `:25,:44` plus `.Warp`/`.Weft`; card 14 unexports `New`/`Warp`/`Weft` but does not list `fabric_test.go` in `Edits:`, hedging "only if the compiler forces it" — the compiler *always* forces it, so card 14's commit fails its own `go test -tags integration ./internal/fabricengine/` verify and breaks the Shared Decision "every card-level commit compiles and passes its package tests".
**Fix:** add `internal/fabricengine/fabric_test.go` to card 14's `Edits:` with a mechanical retarget onto the `export_test.go` shim, leaving card 15's `Open`-based restatement as the follow-up (or merge cards 14 and 15).

### [BLOCKING] configsync comments make batch 07 red on first activation
**Location:** batch 06 / card 24, batch 07 / card 26
**Issue:** Card 26 states `configsync` is an owner for string literals only — "a `configsync` identifier or comment hit still fails" — but `internal/configsync/configsync.go` carries `warp`/`weft` in *comments* at `:21`, `:28`, `:36-37`, `:46`, `:160`; no card edits that file (it is absent from `## All Files Touched`), and card 24's gate filters "`configsync`'s two string literals" wholesale, so the stragglers are never surfaced.
**Fix:** either add `internal/configsync/configsync.go` to card 22's sweep (rewording the legacy `warp.yaml`/`weft.yaml` prose) or widen the carve-out in the decision, card 24's gate, and card 26's predicate to cover its comments.

### [NIT] Card 24 has `Commit: none` but prescribes a commit
**Location:** batch 06 / card 24
**Issue:** The card is declared diff-free with `Commit: none`, yet its requirements say "fold the fix into a follow-up commit amending the responsible card's file" — contradictory, and `amend` conflicts with the repo's commit-per-fix discipline.
**Fix:** state that any straggler found becomes its own new commit on this card, and give the card a commit message (or declare it explicitly no-diff and forbid fixes here).

### [NIT] Shared Decision undercounts the deliberate double-visits
**Location:** `00-overview.md` / decision `comments ride with code edits`
**Issue:** The decision names four double-visited files; `internal/webstercli/cli_test.go` is a fifth (card 8 in batch 03, card 23 in batch 06). It is DAG-safe (batch 06 depends on 03), but the decision text as written is inaccurate and a validator reading it literally would flag the file.
**Fix:** add `internal/webstercli/cli_test.go` to the enumerated double-visit list with its DAG-ordering note.

### [NIT] `builder-contract.md:167` weft mention not enumerated
**Location:** batch 08 / card 29
**Issue:** Card 29 names `docs/reference/builder-contract.md:22,24` but `:167` also reads "every weft-synced batch"; the file sits outside the machine check, so an unenumerated line survives the sweep.
**Fix:** add `:167` to card 29's named line list (same reword class as `README.md:62` and `docs/overview.md:133`).

### [NIT] `lyxcwd/anchor_test.go` missing from card 23's list
**Location:** batch 06 / card 23
**Issue:** `internal/lyxcwd/anchor_test.go:203` carries "fabricengine's `hostLayoutFor`" — a fabric-geometry `host` identifier in prose — and the file is not in card 23's edit list; card 24's grep gate ("every surviving test hit must be an owner-API reference or a named carve-out") will surface it unclassified.
**Fix:** either add the file to card 23 or name `hostLayoutFor` as an owner-symbol carve-out in card 23's KEEP list.

## Verdict

REQUEST_CHANGES
Two blocking gaps: card 14 breaks the test compile; configsync comments fail batch 07.
MILL_REVIEW_END
