# Plan fix — round 2

Source review: `_mill/reviews/20260714-082141-plan-review-r2.md` (verdict APPROVE, 1 NIT).

## Fixed

- `20260714-082141-plan-review-r2.md` — "[NIT] Card 4 fixture Add uses SkipPush; reconcile_test uses SkipGit": card 4 now creates the extra host+weft pairs via `w.Add(f.Layout, slug, AddOptions{SkipGit: true})`, matching `reconcile_test.go` line 54. Verified against `add.go`: `SkipGit` (line 20–21) suppresses all weft-side git ops including the host push, whereas `SkipPush` (line 22–24) still runs local git; `SkipGit` avoids the network round-trip in the guard's setup phase.

## Pushed Back

(none)
