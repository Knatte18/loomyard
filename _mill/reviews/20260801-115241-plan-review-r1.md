MILL_REVIEW_BEGIN
# Review: Formalize the Tier 1/2 substrate rule and re-tier mis-tagged tests — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (Sonnet 5 / claude-sonnet-5 per system context)
reviewed_file: plan/
date: 2026-08-01
```

## Findings

### [BLOCKING] Card 6's quoted surgical-edit phrase does not match the source file
**Location:** batch `scoutengine-scout-tag`, Card 6 (`ensureserver_integration_test.go`)
**Issue:** Card 6 instructs replacing the phrase `"it is run separately with `-tags integration`. Even though ensureNative itself"` (cited as line 8). The actual file text at lines 7-8 reads `"...verify (the Test Tier Purity Invariant) and run\nseparately with `-tags integration`. Even though ensureNative itself"` — i.e. preceded by "and run", not "it is run" ("it is" appears earlier, at line 6, attached to a different clause: "it is excluded from the"). A literal find for the quoted phrase will not match.
**Fix:** Correct the quoted target phrase to the text actually present, e.g. `"...and run\nseparately with `-tags integration`. Even though ensureNative itself"`, or just target `"separately with `-tags integration`. Even though ensureNative itself"` (line 8) without the false "it is run" prefix. (Note: the sibling Cards 7, 8, 9 quote their equivalent phrases correctly against their own files — verified character-for-character — this is isolated to Card 6.)

### [NIT] Card 14 cites the wrong batch-1 card number
**Location:** batch `substrate-rule-docs-and-sweep`, Card 14
**Issue:** Card 14 says "Card 3 in batch 1 already edited this bullet's wording" when describing the Statement bullet's known-tags-list wording — but Card 3 in batch 1 creates `cmd/lyx/tiersleep_test.go` and never touches `CONSTRAINTS.md`; the Statement-bullet edit is actually Card 5.
**Fix:** Change "Card 3 in batch 1" to "Card 5 in batch 1" in Card 14's Requirements text.

## Verdict

REQUEST_CHANGES
Fix Card 6's misquoted phrase (surgical-edit target text doesn't exist verbatim in the file) and Card 14's wrong card-number citation.
MILL_REVIEW_END
