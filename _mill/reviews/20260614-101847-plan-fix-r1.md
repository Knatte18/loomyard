# Plan-fix report — round 1

Source review: `_mill/reviews/20260614-101732-plan-review-r1.md` (holistic, APPROVE, 0 blocking, 3 NITs).

## Fixed

- `20260614-101732-plan-review-r1.md` — "[NIT] muxpoc cli_test.go not in card 15 Context/Edits": added `internal/muxpoc/cli_test.go` to card 15 Context and required the no-/unknown-subcommand usage paths to keep writing to STDERR (not `output.Err` to stdout) so the existing `out.Len() == 0` dispatch-error assertions still hold.
- `20260614-101732-plan-review-r1.md` — "[NIT] Smoke test worktree-root vs rev-parse normalization": card 18 now mandates the IDENTICAL raw temp-dir string for both `cfg.WorktreeRoot` and the `LoadState` assertion, never routing either side through `paths.Resolve`, so the Windows `%TEMP%`-vs-`--show-toplevel` normalization mismatch cannot split them.
- `20260614-101732-plan-review-r1.md` — "[NIT] socketName basename collision across sibling worktrees": card 18 now requires a `state_test.go` comment documenting that socket identity is the worktree-root basename, so sibling worktrees with colliding leaf names share a socket (inherent to the basename scheme — documented, not fixed here, per the reviewer's own "no code change required").

## Pushed Back

(none)
