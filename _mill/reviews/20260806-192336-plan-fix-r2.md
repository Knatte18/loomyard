# Plan fix — round 2

Source review: `_mill/reviews/20260806-192115-plan-review-r2.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 4 NIT).

## Fixed

- r2 BLOCKING "`weftCommit` rename leaves 6 call sites outside batch 03" — verified by grep: `buildercli/spawnbatch.go:165`, `buildercli/poll.go:339`, `webstercli/beginbatch.go:135`, `webstercli/recordbatch.go:135`, `webstercli/recoverbatch.go:155,189`, `webstercli/cli_test.go:137,155`.
  Renaming the helper without them leaves both packages non-compiling, contradicting the `expand-migrate-contract sequencing` Shared Decision.
  Moved all of them into cards 7 and 8 (`Edits:` plus explicit `Requirements:` naming the `weftCommit` → `fabricSync` and `weftErr` → `syncErr` retargets) and removed them from batch 06 card 21, whose scope is now stated as "the residual cli-package files cards 7-9 do not own".
- r2 BLOCKING "Cobra `Long` text and operator error strings misclassified as comments" — verified: `webstercli/status.go:31`, `awaitbatch.go:33`, `recoverbatch.go:55,58,59`, `run.go:45`, and `buildercli/run.go:40` are `Long` bodies;
  `webstercli/beginbatch.go:136`, `recordbatch.go:136`, `recoverbatch.go:156,190` are `output.Err` operator strings.
  Enumerated every one of them explicitly in cards 7 and 8 under the CLI/Cobra help-accuracy obligation and `diagnostics-say-fabric-detail-says-weft`, matching how card 10 already handles `configcli.go:233`, and added a re-read-the-`Long`-end-to-end requirement.
  Card 21's "everything else in these files is comments" claim is replaced with an instruction to check for operator-visible text before assuming a hit is a comment.
- r2 BLOCKING "perchcli card omits the `weftErr` local rename" — verified at `perchcli/run.go:322` (`weftWorktree`) and `:341,344,345,347,359,365,367` (`weftErr`).
  Added both renames to card 9's `Requirements:` in the same shape cards 7 and 8 use, plus the two diagnostics at `:360,367` that embed the local.
- r2 NIT "Documentation deferred to batch 08, not the code commit" — accurate: the discussion's `documentation` decision and CLAUDE.md both say same-commit, and the plan deviates.
  Added a Shared Decision `docs land in batch 08, not inside each code commit` recording the deviation, why the content cannot be written earlier (it must name symbols that batches 01/04/05/07 create), why the rule's intent still holds (batch 08 is the DAG's terminal batch, not an optional follow-up), and the carve-out that per-card CLI help-text fixes ride with their own code commit rather than waiting.
- r2 NIT "`builderengine/runlevel_test.go` has no owning card" — verified at `:800` ("the same host repo").
  Added the file to card 23's `Edits:` with the reword named, and to `## All Files Touched`.
- r2 NIT "Rule (3) scope vs the `*_test.go` exclusion is ambiguous" — verified: `internal/lyxcwd/geometry_test.go:13` imports `internal/weftname` to test `weftname.SiblingPath`.
  Card 26 now states the test-file exclusion applies to all three rules, rule (3) included, and cites that file as the reason it is load-bearing rather than editorial.
- r2 NIT "Shared-Decision overlap exception list is incomplete" — verified: `loomengine/preflight_integration_test.go` (cards 5 and 23) and `websterengine/audit_test.go` (cards 12 and 23) are both double-visited.
  Added both to the exception list in the `comments ride with code edits` decision, noting all four exceptions are DAG-ordered.

## Pushed Back

_(none — every finding verified accurate and no fix caused demonstrable harm.)_
