MILL_REVIEW_BEGIN
# Review: Reed attach dot-fill render artifact on resize and cross-client mouse move — holistic

```yaml
duration_s: 466.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (per system-declared identity; distinct from the assigned reviewer_model label "sonnetxhigh")
reviewed_file: plan/
date: 2026-08-31
```

## Findings

### [BLOCKING:scope] Card 13 Context omits every production file its tests assert against
**Location:** 03-repaint-entry.md, Card 13 **Issue:** Requirements write tests against `repaintHookCommand`, `resizeRepaintHookCommand`, the new 4-arg `resizePinHookArgvs`, `installResizePinsLocked`, the `Shell.ForEachLine`/`LineVarRef` members, and `shell.Posix()`/`shell.ForGOOS()` — all defined in `internal/shell/shell.go`, `posix.go`, `pwsh.go`, `internal/reedengine/watchdog.go`, `windowsize.go`, and `apply.go` (`SkipFocus`). None of these six production files appear in Card 13's `Context:` (only their `_test.go` counterparts are in `Edits:`), so the implementer has no way to see the exact new signatures/bodies Card 12 just wrote when composing byte-exact assertions against them. **Fix:** Add `internal/shell/shell.go`, `internal/shell/posix.go`, `internal/shell/pwsh.go`, `internal/reedengine/watchdog.go`, `internal/reedengine/windowsize.go`, and `internal/reedengine/apply.go` to Card 13's `Context:`.

### [BLOCKING:scope] Card 15 Context omits windowsize_test.go, which its Requirements name by test
**Location:** 04-attach-multi-client-warning.md, Card 15 **Issue:** Requirements say to add a table test "mirroring `TestParseWindowSize`'s shape" — that test lives in `internal/reedengine/windowsize_test.go`, which is in neither Card 15's `Context:` (`windowsize.go`, `overlay.go`) nor its `Edits:` (`attach.go`, `attach_test.go`). **Fix:** Add `internal/reedengine/windowsize_test.go` to Card 15's `Context:`.

### [NIT:scope] Cards 14 and 16 cite hookInstalledLocked from a file outside their Context
**Location:** 03-repaint-entry.md Card 14; 04-attach-multi-client-warning.md Card 16 **Issue:** Both name `reapply.go`'s `hookInstalledLocked` as the model for a new per-entry-match helper, but `reapply.go` is absent from either card's `Context:`; low-impact since each requirement fully spells out the match rule itself ("failing unless some entry equals `want` exactly") in the same sentence. **Fix:** Add `internal/reedengine/reapply.go` to both cards' `Context:`, or drop the by-name cross-reference since the behavior is already self-contained.

### [NIT:consistency] "the geometry section" of doc.go is not an actual named section
**Location:** Shared Decision `the-measurement-record-lives-in-doc-go`; Cards 11 and 18 **Issue:** `doc.go` has no heading titled "geometry section" — the resize-pin/hook/window-size material sits inside one continuous "Load-bearing behavioral assumptions" bullet list interleaved with unrelated bullets (split failures, pane-id generations, etc.), so "placed in the geometry section" overstates the file's actual structure. **Fix:** Reword to "beside the resize round-robin and hook bullets" (the phrasing both cards already use elsewhere) rather than implying a distinct labeled section exists.

## Verdict

REQUEST_CHANGES
Two Context-completeness gaps (Cards 13, 15) leave new production signatures invisible to their own test-writing cards.
MILL_REVIEW_END
