# Discussion fix — round 2

Source review: `_mill/reviews/20260818-084313-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, none demoted).
Both fixed; none pushed back.

## Fixed

- `20260818-084313-discussion-review-r2.md` — "[NIT:design] lyxcwd removal has no machine guard, only a grep step".
  Verified: `internal/pattern/leaf_enforcement_test.go` and `internal/shuttleengine/seam_enforcement_test.go` exist; `reedengine`, which made this same conversion in wave 1, has neither and still names `lyxcwd` in `server.go:3-4`, `geometry.go:13` and `doc.go:21-24`.
  Added a Decision stating the disposition outright: no enforcement test in T6, deferred to T10, which already owns the question and answers it uniformly — its brief lands the three-tier invariant *"with its enforcement basis named honestly (an import-allowlist test per producer package where one exists, review obligation where none does)"*.
  The Decision also records why a bolted-on grep-style guard would be actively wrong here: `reedengine`'s surviving prose mentions of `lyxcwd` are correct and would trip it.
- `20260818-084313-discussion-review-r2.md` — "[NIT:decision] shedadapters doc comment left as 'may need a wording touch'".
  Verified: `perch.go:34`'s comment is about `PerchFactory` and `Options.PauseRequested` consumption at construction time, naming no geometry.
  Replaced the conditional wording in both Scope→Out and Technical context with an unconditional "no change at all", stating why the comment stays true.

## Pushed Back

None.

## Incidental

While confirming T10's scope for the first finding, noticed T10's `Files` list names `manifest/roadmap.md` and its brief says "move the roadmap entries to Done".
Extended the roadmap Decision to name T10 as the backstop sweeper alongside the wave-closing task — this is exactly the "or to T10" clause of T7's rule that this discussion adopted, now cited rather than paraphrased.
Not a review finding; recorded here for the audit trail.
