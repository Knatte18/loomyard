# Plan fix — round 2

Source review: `_mill/reviews/20260814-112758-plan-review-r2.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 2 BLOCKING / 2 NIT).

## Fixed

- r2 "[BLOCKING:design] Root pre-run seeding forces a git spawn on every lyx invocation" — verified accurate: `internal/lyxcwd/lyxcwd.go:149` spawns `gitexec.Run(["rev-parse","--show-toplevel"], cwd)` before any gate check, every parent group is Runnable via `RunE: clihelp.GroupRunE`, and `cmd/lyx/exitcode_test.go:47` drives exactly the `{"lyx board (no subcommand)"}` case the review names.
  Split card 14's `seedStencils` into a `testing.Testing()`-guarded pre-run wrapper and a `seedStencilsAt(hub, worktree)` worker, mirroring the `if !testing.Testing()` guard on the trace-arming block it sits beside.
  Recorded the residual production cost explicitly rather than leaving it implicit: outside tests a real invocation pays one `git rev-parse --show-toplevel` even for a command needing no geometry, which most commands already pay inside their own module pre-run.

  Follow-on the review did not name, fixed in the same pass: the guard makes card 16's `stencilenvelope_integration_test.go` assertion — "a non-`stencil` command's envelope key set is unchanged **by a run that seeded**" — unsatisfiable in-process, since `seedStencils` is now a no-op under `go test`.
  Retargeted that test to three assertions reachable through the split: seeding driven directly via `seedStencilsAt`, the envelope-key assertion against a read-only command, and a new assertion that `seedStencils` is a no-op under test, which pins the guard against removal.
  Added to card 14 that neither function references `internal/output` or any envelope key, making the no-widening rule structurally true rather than asserted.

- r2 "[BLOCKING:scope] template_test.go's RenderIntegrationPrompt call sites are left stale" — verified accurate: `internal/websterengine/template_test.go:662` and `:677` both call `RenderIntegrationPrompt(plan, "/reports/integration.yaml", "/worktree")`, whose signature card 26 changes.
  Added both sites to card 28's requirements by line and by enclosing test name (`TestRenderIntegrationPrompt_InjectsVerifyText`, `TestRenderIntegrationPrompt_EmptyVerifyErrors`), and stated that the other three `Render*Prompt` wrappers keep their signatures so their call sites in that file need no change.

- r2 "[NIT:consistency] Batch 9's restated verify omits its own go vet clause" — verified accurate.
  Restated batch 9's full verify string in its `## Batch Tests` section and explained why the clause exists (card 40 edits `integration_test.go`, which is `//go:build integration` and invisible to the untagged clause) and why it is `go vet` rather than `go test -tags integration`.
  Applied the same fix to batch 4, whose `## Batch Tests` section had the identical omission for its `go vet -tags smoke` clause — the review found one instance of a defect that existed twice.

- r2 "[NIT:scope] Several stated test call-site counts are inaccurate" — verified inaccurate, and independently unreproducible: a fresh recount disagreed with both the plan's numbers and the review's corrections, because the counts vary with whether one counts matching lines or matching occurrences.
  Took the review's second suggested remedy rather than its first: dropped every specific count from cards 18, 22, and 24 in favour of "every call site", and added an explicit instruction not to work from a count, since `go build` and each batch's own `verify:` enumerate the real sites.
  Recounting would have reintroduced a number that drifts the moment any test file changes and that reads as a completeness claim the plan cannot make.

## Pushed Back

_None. Every finding was factually accurate and no fix caused demonstrable harm._

DAG re-validated and the validator gate re-run after the edits: 9 batches, 0 findings.
