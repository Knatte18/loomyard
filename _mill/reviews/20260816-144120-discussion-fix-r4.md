# Discussion fix — round 4

Source review: `_mill/reviews/20260816-144032-discussion-review-r4.md` (APPROVE, 0 blocking, 2 NIT).

## Fixed

- r4 — "Return matrix contradicts the banner-strip decision": the lazy-read matrix's success row now reads `(stencil.StripLeadingComment(string(content)), nil)` and points at the strip decision. A real internal contradiction, introduced in round 1 when the strip decision was added without updating the matrix — a plan writer following the matrix alone would have shipped the stamp leak.
- r4 — "Design-doc correction list misses a fourth false claim": the design-doc edit scope is now four corrections, adding line 57's "Related" bullet, which calls `docs/shared-libs/stencil.md` the contract "these stencil files render through" — false, since these are the first stencils never passing through `Fill`. Updated in both §Decisions and the §Q&A log.

## Pushed Back

None.
