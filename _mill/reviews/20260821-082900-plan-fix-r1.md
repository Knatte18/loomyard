# Plan fix — round 1

Source review: `_mill/reviews/20260821-082748-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `20260821-082748-plan-review-r1.md` — "[NIT:decision] Commit-body instruction for the Segment decision has no card home".
  Added a `**Commit body.**` paragraph to batch 1 card 2's `Requirements:` in `_mill/plan/01-shedcheck-package.md`, assigning the overview's `Segment` Shared Decision conclusion to that card's commit: `CONSTRAINTS.md` gains no new section, `internal/shedengine/seam_enforcement_test.go`'s allowlist is untouched, and `internal/loomshed/seam_enforcement_test.go`'s `loomshedAllowedImports` is untouched, each with its reason.
  Card 2 is the commit that lands the module's own exported surface, which is why it is the one that carries the conclusion rather than card 1 or card 3.
  Added `internal/shedengine/seam_enforcement_test.go`, `internal/loomshed/seam_enforcement_test.go`, and `CONSTRAINTS.md` to that card's `Context:` in the same edit, since the new paragraph names all three as backtick paths and `Context:` is an allowlist — without them the `context-completeness` validator check would fire on the fix itself.

## Pushed Back

_None._
