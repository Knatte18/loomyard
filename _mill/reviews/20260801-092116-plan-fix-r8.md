# Plan fix report — round 8

Source review: `_mill/reviews/20260801-091921-plan-review-r8.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 2 NIT; reviewer noted the plan is "otherwise exceptionally well-grounded"). All four VERIFIED accurate and FIXED. No deep/architectural findings this round — all four are DAG-ordering / Context-completeness / action-field mechanics.

## Fixed

- [BLOCKING] "Batch 4 is missing a dependency on batch 2" — CONFIRMED: clone (batch 4) creates a hub whose `fabric.yaml` lives only at the repo-wide `BoardDir`, but `reconcile`/`checkout`/`remove`/`PairInSync` read the per-pair base until batch 2 card 7 migrates them; with batch 4 gated only on [1,3] and batch 2 on [1], a scheduler could land batch 4 first and break those verbs package-wide on every clone. Fixed by adding `2` to batch 4's `depends-on` (now `[1, 2, 3]`, mirroring batch 5's `[1, 2]` added in round 5) in both `00-overview.md` and the batch frontmatter, with a load-bearing-dependency note in the batch scope. DAG re-validated (topo order clean, no cycle).
- [BLOCKING] "Card 23's Context omits fabric.go, which its Requirements reference" — CONFIRMED: card 23 calls `New`/`EnvSyncOptions`/`ScopedPathspec` (declared in `internal/fabricengine/fabric.go`) but that file was absent from its Context allowlist. Fixed by adding `internal/fabricengine/fabric.go` to card 23's Context.
- [NIT] "Stale-removal in Reconcile leaves Action stale when only a junction was removed" — CONFIRMED: an otherwise-healthy pair with a removed stale junction would still report `action: "already_healthy"`. Fixed card 9 to require setting `pr.Action` to the converged/changed value (reuse the repoint action, or add a dedicated `ReconcileActionStaleRemoved` registered wherever action values are enumerated/asserted) when stale-removal actually deletes a junction; unchanged when nothing was removed.
- [NIT] "Card 30's Context omits anchor.go for the `.fabric-anchor` filename" — CONFIRMED: card 30 asserts `<BoardDir>/.fabric-anchor` exists but `hubgeometry.FabricAnchorName` (in batch 1's `anchor.go`) was not in Context, risking a hardcoded string. Fixed by adding `internal/hubgeometry/anchor.go` to card 30's Context.

## Pushed Back

(none — all four findings were factually accurate and fixing them caused no harm.)
