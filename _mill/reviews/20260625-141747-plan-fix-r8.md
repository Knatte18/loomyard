# Plan fixer report — round 8 (APPROVE with NITs)

Source review: `_mill/reviews/20260625-141709-plan-review-r8.md` (verdict APPROVE, 0 BLOCKING + 3 NITs).

## Fixed

- `20260625-141709-plan-review-r8.md` — [NIT] "cli.go arg-parse omitted from clone-fold context" — card 4 now states the JSON `board` field carries `cloneHub`'s resolved board URL (default-derived from the weft URL via `deriveBoardURL` when the arg is omitted), matching current behaviour.
- `20260625-141709-plan-review-r8.md` — [NIT] "init no-pairing early-return changes lyx init contract silently" — card 14 now documents the intended contract: in the weft-overlay model `_lyx` lives in the weft via junction, so `lyx init` is the activator run only inside a warp-hub worktree with a weft pairing; scaffolding into an unpaired host would violate the host-pristine invariant, so the no-pairing path reports by design (not a regression), and the adapted `TestRunInit_*` tests encode it.
- `20260625-141709-plan-review-r8.md` — [NIT] "configreg package doc comment update under-specified" — card 9 now names both `worktree` doc-comment sites in `configreg.go` (the package-doc registry line and the `Module.Name` field example) to be renamed `worktree`→`warp`.

## Pushed Back

(none — all three NITs were accurate clarifications and were applied under the default fix-everything rule.)
