# Fixer report: plan-fix round 4 — reed-pane-reap-consistency

Source review: `_mill/reviews/20260829-055335-plan-review-r4.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 5 NIT, one ceiling-demoted from BLOCKING).
Every finding was verified against the real source files before acting;
all six were accurate, and all six were acted on.

## Fixed

- r4 holistic, `[BLOCKING:scope]` "Requirements name symbols from files absent from Context" — confirmed for all four cards. `SaveState` and `toRenderStrands` live in `internal/reedengine/state.go:170,178`, which card 6 named but did not list;
  card 7 names `addStrandLocked` without listing `strand.go`;
  card 8 names `AddStrand`/`UpdateStrand`/`RemoveStrand`, the `state.go` remedy, and `adoptPaneGenerationLocked` without listing any of the three files;
  card 9 names `adoptPaneGenerationLocked` without listing `generation.go`. Added `state.go` to card 6, `strand.go` to card 7, `strand.go` + `state.go` + `generation.go` to card 8, and `generation.go` to card 9. Worth noting the validator's own `context-completeness` check could not have caught this class: it matches path-like tokens, and these were bare symbol names.
- r4 holistic, `[NIT:consistency]` (demoted) "Card 13 contradicts itself on which `up` reaps" — confirmed, and the later paragraph was the correct one. Traced through `lifecycle.go`'s `Up`: `ensureHeaderPaneLocked` runs before `reconcileApplyPersistLocked`, so on the *first* `up` the header is already alive with zero strands and the reap kills the session's initial pane there. The test then reaches its foreign `split-window` with the header as its only pane, and the *second* `up` reaps the foreign pane alone. Neither `up` kills both. Card 13's opening now states the per-`up` behaviour explicitly and flags that the tightened assertion passes under either reading, so a backwards comment would go uncaught.
- r4 holistic, `[NIT:consistency]` "Card 8 says \"three passages\", then enumerates four" — confirmed, and self-inflicted in round 2 when the `!anyPlacedStrand` passage was appended without updating the count. Changed to "four passages", with an explicit instruction not to stop at three because the fourth is the one passage neither sweep reaches.
- r4 holistic, `[NIT:scope]` "Card 12 under-enumerates the second sweep's survivors" — confirmed: the sweep also hits `reconcile.go`'s header-exemption comment above the dead-pane kill loop and `clearConflictingPaneBindings`' "planReconcile's deterministic untracked reap kills it" sentence. Both remain true post-fix and are deliberately preserved (card 2 changes neither the dead-pane loop nor its comment; card 10 rewrites only the adoption clause of that doc comment). All three second-sweep survivors are now enumerated with the reason each stands.
- r4 holistic, `[NIT:scope]` "`headerAlive`'s stale-header-id branch is untested" — confirmed: `headerAlive` is false three ways (empty id, present-but-dead, and id set but absent from `live`), and only the first two were covered. The third is reachable on the `add` path after an operator kills the header pane outright, since nothing but `up`/`resume` rebuilds it. Added the case to card 2's add-list with that reachability argument.

## Pushed Back

- r4 holistic, `[NIT:consistency]` "Batch 3 depends on the higher-numbered batch 4" — **resolved via the finding's own stated alternative, not by dismissal.** The finding's `Fix:` offers two branches: swap the numbering, "or leave as-is if mill-go's validator accepts a non-monotonic DAG". That condition is satisfied and verified — `_plan_dag.validate` accepts the edge, the reviewer itself confirms the DAG is acyclic with every `file:` present, and mill-go schedules from the edges rather than the filename ordinals.
  The swap branch was declined on harm-check category (c), destabilising work outside the finding's own scope: renumbering the two batches requires renumbering all eleven of their cards to keep card numbers monotonic too, and rewriting roughly thirty cross-references ("card 12's sweep", "cards 13-16", "batch 4 owns them") spread across all five plan files. `_plan_validate` would catch a broken DAG edge or a duplicate card number, but it cannot catch a stale prose reference pointing at the wrong card — and a card 6 that cites "card 12" when the sweep has become card 18 misdirects the implementer far more concretely than a backward edge the scheduler never reads.
  What the finding is really about — a reader inferring execution order from the numbers — is fixed directly instead: the overview's Batch Index now carries an explicit "read the edges, not the numbers" note stating that batch 3 runs last, why the backward edge is deliberate, and the resulting order (1 → 2 → 4 → 3).

## Post-fix validation

`_plan_dag.validate` passes.
`_plan_validate.run` returned zero findings with an empty `skip_checks`.
