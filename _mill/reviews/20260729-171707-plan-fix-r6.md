# Plan fix report — round 6

Source review: `_mill/reviews/20260729-171256-plan-review-r6.md` (verdict REQUEST_CHANGES, 3 BLOCKING + 0 NIT). All three verified accurate; all fixed per `mill-receiving-review`'s fix-everything default; edits confined to `_mill/plan/`. Zero pushbacks.

## Fixed

- [BLOCKING] "Supervised daemon anchoring is cwd-fragile outside a lyx hub" — verified: `cli.go` leaves `worktreeRoot` `""` when `hubgeometry.Resolve(cwd)` fails (the supported outside-a-hub → `BuiltinRegistry()` path), which is inert under native but, post-flip, makes `ensureSupervised` anchor at a cwd-**relative** `.lyx/codeintel/go/` — colliding/littering across cwds, defeating the session-long-daemon goal for a supported mode. Added a "Supervised daemon anchoring outside a lyx hub" Shared Decision and extended card 8 (batch 3, before the flip) to fall `worktreeRoot` back to the **absolute `--target-dir`** (`filepath.Abs(dir)`) via a small testable `resolveWorktreeRoot(cwd, targetDir)` helper, with a `cli_test.go` assertion that outside a hub the resolved root is the absolute target dir, never `""`. This gives per-target stability with a one-line fallback that lands before batch 4's flip.
- [BLOCKING] "Card 1 Context omits lspclient.go for a cited function" — verified: card 1's `KillPID` doc comment contrasts it with `lspClient.kill()`, defined in `lspclient.go`, absent from card 1's `Context:`. Added `internal/codeintelengine/lspclient.go` to card 1's `Context:`.
- [BLOCKING] "Card 12 Context omits lspclient.go for a cited function" — verified: card 12's native-fallback test's process-free claim ("`newLSPClient` fails at `exec.LookPath` before `cmd.Start`") depends on `newLSPClient`'s behavior in `lspclient.go`, absent from card 12's `Context:`. Added `internal/codeintelengine/lspclient.go` to card 12's `Context:`.

## Pushed Back

- None.
