# Discussion fix — round 5

Source review: `_mill/reviews/20260918-193642-discussion-review-r5.md`

## Fixed

- `[NIT:decision]` (demoted from BLOCKING) "Third AddStrand caller undispositioned" — verified `internal/shuttleengine/run.go:252`. Added a `shuttle-inherits-the-self-heal` decision stating it is intended and in scope behaviourally, with the standalone/detached runner explicitly included, and rewrote the `shuttleengine/reed.go:19` technical-context bullet from a signature-compatibility note into a third-entrypoint note.
- `[NIT:consistency]` (demoted from BLOCKING) "Superseded `Status()` → `Up()` swap line" — rewrote the `internal/reedcli/attach.go:55` bullet as "`EnsureSession()` inserted ahead of the retained `Status()` call", and noted the header comment must now say two pre-flight steps can abort.
- `[NIT:scope]` (demoted from BLOCKING) "reed API design doc missing from the doc list" — verified the frozen count at `manifest/designs/reed-fabric-standalone-api.md:124`, `:130`, `:169`. Added the file to the same-commit doc list, naming all three sites, the 17→18 change and the method enumeration; noted `:112`'s line count is a measurement, not a contract.
- `[NIT:scope]` "Zero-pane husk test has no stated construction" — verified no existing fixture builds a zero-pane session. Marked the test best-effort, named the likeliest construction route, and added an explicit instruction to drop the test rather than weaken the predicate if it proves unconstructible.

## Pushed Back

None.
