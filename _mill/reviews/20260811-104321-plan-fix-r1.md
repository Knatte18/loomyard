# Plan fix — round 1

Source review: `20260811-104222-plan-review-r1.md` (holistic, sonnetxhigh).

## Fixed

- `20260811-104222-plan-review-r1.md` — "[BLOCKING:scope] UnwireJunctions containment surface cited to the wrong file, and `junction.go` missing from Context".
  Verified against source before fixing: `fabricengine.UnwireJunctions` is defined at `internal/fabricengine/junction.go:368` and reaches the link executor through `unseedLyxJunction` (`junction.go:392`) into `unseedJunctionRecords` (`junction.go:420`), whose `pathRequest` and `removeLink` call sit at `junction.go:474-483`;
  `unwire.go:143-152` is `unwireBoardLink`, called only from `unwire.go:79` and `remove.go:99`, never from `UnwireJunctions`.
  Corrected the citation in all three places the finding names — `00-overview.md`'s cell-enumeration-and-omissions decision, batch 4 card 13's Requirements, and batch 6 card 16's omission-table paragraph — each now naming `junction.go:474-483` and stating explicitly that `unwire.go:143-152` is a different function, so the wrong citation cannot be reintroduced from memory.
  Added `internal/fabricengine/junction.go` to card 16's `Context:`, and swapped `internal/fabricengine/unwire.go` for `internal/fabricengine/junction.go` in card 13's `Context:` — card 13 no longer references `unwire.go` at all after the correction, while card 16 keeps both because `Remove` legitimately reaches `unwireBoardLink` at `remove.go:99`.

## Pushed Back

_None._
