# Discussion fix — round 5

Source review: `_mill/reviews/20260918-193213-discussion-review-r5.md` (APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r5 — "removeWeftWorktree has two callers, not one": verified against `internal/fabricengine/add.go:264` and `remove.go:132`.
  `rollbackAdd` does reach the `alsoDeleteBranch` arm (it passes `!weftBranchAdopted`).
  Corrected the Technical-context caller enumeration and the Scope → In bullet, and recorded that `rollbackAdd` passes `remote: false` while still having to compile against the widened signature.
- r5 — "Rollback carve-out's \"never pushed\" premise is weaker than stated": verified — `Add` pushes the warp branch at `add.go:218` and the weft branch at step (12), so a rollback can face a pushed branch.
  Kept the exclusion, replaced the false factual premise with the real reason (an unattended best-effort rollback must not make a network-visible destructive change), and corrected the matching Q&A-log entry.
- r5 — "removeWeftWorktree reporting shape left as a two-way fork": chose the changed return shape, `(weftTeardownResult, error)`, with the rationale for rejecting the out-parameter alternative recorded.

## Pushed Back

(none — all three findings were accurate and none met a Decision-Tree harm category.)
