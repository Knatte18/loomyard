MILL_REVIEW_BEGIN
# Review: preflight: split into two Shed rows -- a generic one, and loom's own — holistic

```yaml
duration_s: 231.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-20
```

## Findings

No findings. The implementation was checked batch-by-batch against every card's `Requirements:` and cross-checked against the plan's Shared Decisions and CONSTRAINTS.md.

- Batch 1 (`internal/preflightshed`): doc.go/ctx.go/preflight.go match cards 1-3 verbatim in shape and content; no `weft`/`warp`/host-phrase tokens present. Tier-1 test (card 4) and the moved Tier-2 wrapper test (card 5, `setupPreflightWrapperFixture` drops the `loomshed.Seed` call and keeps the add+commit, retitled "seed junctions") and the relocated `TestMain` (card 6) all match.
- Batch 2 (`loomengine.CheckSeed`/`checkCoherence`): `coherence.go`'s three rewritten prose blocks, `seed.go`'s pre-emption-rule doc comment (all four kept verdicts named and justified), `preflight.go`'s transitional two-literal `checkCoherence` call, and the extended Tier-1 tables in `coherence_test.go`/`seed_test.go` all match cards 7-11 exactly, including the deliberately-told (never `p.name`) call convention.
- Batch 3 (wiring the 13th row): `loomshed.go`'s `NameLoomPreflight` constant placement, the row-1/row-2 insert, the renumbered doc comments (1-13 enumeration, escalate-only rationale extended for Loom-Preflight), `loompreflight.go`'s unexported `newLoomPreflight` constructor and its `Call` passing constants directly, `wiring.go`/`cli.go`'s repoint onto `preflightshed.NewPreflight(loomshed.NamePreflight, cwd)`, and every touched test file (`loomshed_test.go`, `sequence_test.go`, `resume_test.go`, `fixture_test.go`, `loompreflight_test.go`, `wiring_test.go`) all match cards 12-20, including the one deliberate divergence (loompreflight_test.go's hand-written fixture vs. `Seed`'s own shape) being correctly documented and implemented.
- Batch 4 (deletion): `internal/loomengine/preflight.go`, `export_test.go`, `preflight_integration_test.go`, and `testmain_test.go` are all gone; no residual reference to `checkResolved`, `runCheck4`, `check3BlocksSeed`, `loomengine.Preflight`, or `CheckResolvedForTest` remains anywhere in the tree. The migrated Tier-2 sub-cases in `internal/preflight/preflight_integration_test.go` (5-case `TestCheckResolved_Dirty`, the 2x3 junction matrix, the two migrated orphans) match cards 21-22's union requirement. `cmd/lyx/cwdmutation_test.go`'s subject-set removal and comment (card 23), `report.go`'s five-alias deletion with the three-type-alias retention and per-constant doc rewording (card 25), `status.go`'s rewritten package doc plus the confirmed absence of any remaining unqualified `Preflight` reference (card 26), `smoke_test.go`'s repoint onto `preflight.Check`/`preflight.CheckWorktreeClean` and the "five of thirteen" fix (card 27), and the four `fabricengine` comment repoints (card 28) are all present and correct.
- Batch 5 (docs): `manifest/designs/loom.md`'s producer table insert, row renumbering (13→14 rows, all four scattered row-number references fixed), and module-decomposition table two-part entry; `docs/overview.md`'s module-tree line placement and precondition-paragraph extension; `CONSTRAINTS.md`'s tier-3 bullet retarget; `contracts/specs/loom-status-spec.md`'s three pinned rewordings (including the deliberate seed-vs-checklist divergence sentence) with the seed schema/worked-example left untouched; and `manifest/roadmap.md`'s Done-section move (correct past tense, no "13 rows to 14" wording, empty subsection heading removed) all match their cards precisely.

The one prior non-blocking item (test-name mismatch at `internal/preflight/preflight_integration_test.go:266`) no longer applies: the function at that location is `TestCheckResolved_BrokenJunction`, matching the plan's own card 21 naming, not `TestPreflight_JunctionBroken` — nothing to escalate.

## Verdict

APPROVE
Every batch matches its plan cards, shared decisions, and CONSTRAINTS.md; no drift, no out-of-plan files, no coverage gaps found.
MILL_REVIEW_END
