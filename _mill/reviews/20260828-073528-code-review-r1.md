MILL_REVIEW_BEGIN
# Review: Audit internal/logger coverage across spawn/hard-error paths — holistic

```yaml
duration_s: 412.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-28
```

## Findings

No findings.

Verification performed across all five batches:

- `manifest/designs/logger-coverage.md`: every table row (spawn-sites, hard-error-return) cross-checked
  against actual source (`internal/gitexec/gitexec.go`, `internal/gitkit/gitkit.go`,
  `internal/githubclient/token.go`, `internal/hubforge/hub.go`, `cmd/testtiming/main.go`,
  `tools/deploy/main.go`, `tools/sandbox/main.go`, `tools/sandbox/suite.go`) — call counts (1/3/1/1/1/3/2/2 = 7
  dev-tooling), verdicts, and the three doc-comment-only `exec.Command` mentions
  (`internal/githubclient/doc.go`, `internal/reedengine/doc.go`, `internal/reedengine/attach.go`) all match
  exactly. Uses backticked paths throughout, per the `audit-doc-uses-backticked-paths` decision.
- `CONSTRAINTS.md`'s `## Live-Substrate Spawn Observability` body is byte-identical to card 2's mandated
  text; nothing else in the file changed; no test file or guard is named there.
- Batch 2 (`singlellm.go`, `mergeresolve.go`, `runlevel.go`): field sets, message wording, and
  `cancelErr`-then-log ordering match every card's requirement precisely; `mergeresolveAllowedImports`
  gained `internal/logger` in the same commit shape as the import; the four `runlevel.go` Warn lines are
  correctly left untested per the plan's stated decision.
- Batch 3 (`integration.go`, `gate.go`, `edit.go`, `boardengine/spawn.go`,
  `vscode/launch_{linux,windows}.go`, `proctree_windows.go`, and card 12's three sites in
  `fabricengine/spawn.go`, `reedcli/attach.go`, `loomcli/run.go`): Info/Warn/Debug placement, level choice
  (Debug for the two Windows polling probes and `overlay.go`'s pre-existing `TmuxCmd` tracing), and
  teardown-vs-detached-spawn-only distinctions all match. The existing `fabricengine/spawn.go` Warn's
  `"error"` key (not `"cause"`) is correctly left untouched, as the card explicitly requires.
- Batch 4 (`selfreport.go`, `publish.go`): Warn field sets and the deliberate
  `reportStuck`/new-Warn duplication in `publish.go` match the `publish-warn-overlaps-reportstuck`
  decision; `landingshedAllowedImports` already admitted `logger`, confirmed by reading the map.
  `internal/landingshed/stuck.go` and `internal/selfreportengine`'s own seam are unmodified as required.
- Batch 5 (`cmd/lyx/spawnobservability_test.go`, `cmd/lyx/tierpurity_test.go`): AST-based detection
  (not substring), the five-entry allowlist with exemption-vs-not-governed framing, the 200-file vacuity
  floor, `filepath.ToSlash` normalization, and the vacuity self-test (including the aliased-import and
  doc-comment-only cases) all match card 15's requirements. `tierpurity_test.go`'s fourteenth
  `allowedSpawners` entry text matches the required wording exactly.
- Spot-checked every "covered" hard-error-table claim against source
  (`burlerengine/engine.go`, `shedadapters/burler.go`, `shedadapters/bouncer.go`,
  `treadleengine/run.go`, `judge.go`, `targeting.go`) — all accurate, including
  `burlerengine/engine.go`'s deliberately unlogged normal-loop-event return.
- No out-of-plan files found; the `## All Files Touched` union in `00-overview.md` matches every file
  actually present.
- No global-utility duplication, no cross-batch contract mismatches, no constraint violations found.

## Verdict

APPROVE
Every batch's cards are realized precisely as specified; audit-doc claims verified against source; no findings.
MILL_REVIEW_END
