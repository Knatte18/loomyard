# Plan fix — round 4

## Fixed

- `20260826-184313-plan-review-r4.md` — "[NIT:consistency] Stale \"dirties the weft\" claim survives in a sibling test's doc comment" (demoted from BLOCKING): verified `TestSmokeBootstrap_OriginRecordSelfHealsAfterCrashBetweenWriteAndCommit`'s doc comment at `internal/loomcli/smoke_test.go:747-752` carries the same now-false claim card 6 fixes in the sibling case, and cross-references the renamed test by its old name. Card 6 now rewrites that comment too — replacing the stated reason with the surviving one (the case scopes its cleanliness assertion to the origin record because that is the only tracked path the scenario is about) and updating or dropping the dangling cross-reference.
- `20260826-184313-plan-review-r4.md` — "[NIT:consistency] Card 9 names a nonexistent `phase` field": verified the table struct at `contracts/stencils/discussiontemplate_test.go:27` declares `phrase string`, not `phase`. Corrected the identifier in card 9's Requirements.

## Pushed Back

_None. Both findings were factually accurate against source, and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task's scope._
