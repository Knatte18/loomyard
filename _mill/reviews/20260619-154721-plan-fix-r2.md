# Plan fix — round 2

Source review: `_mill/reviews/20260619-154633-plan-review-r2.md` (verdict APPROVE, 4 NIT).

## Fixed

- review-r2 [NIT] "Card 2 declares white-box but package is black-box" — Card 2 changed to `package paths_test` (black-box), matching the existing `paths_test.go`/`helpers_test.go`; all eight methods are exported so no unexported access is needed.
- review-r2 [NIT] "Card 6 LoadConfig double-suffixes _lyx path" — Card 6 now specifies that `config.Load`'s `FindBaseDir` raises the generic `not initialized: _lyx/ directory not found` error, that weft's `LoadConfig` wraps the "not initialized" case into a clear `weft worktree or its _lyx is missing at <base>` message (board/worktree wrap precedent), and that the test asserts that wrapped message.
- review-r2 [NIT] "Card 18 test uses os.Readlink contra card 9/14" — Card 18's resolution assertion now uses `filepath.EvalSymlinks` (mode-bit check via `os.Lstat`), matching the production helpers in cards 9/14 and `links.go`.
- review-r2 [NIT] "Card 13 status bullet contradicts existing overview text" — Card 13 now quotes the exact sentence "**Weft has no Go code yet** — portals are still the live mechanism." as the replacement target, removing edit ambiguity.

## Pushed Back

(none — all four NITs were factually accurate, harmless to fix, and consistent with the plan's own decisions and the existing codebase conventions.)
