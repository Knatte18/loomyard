MILL_REVIEW_BEGIN
# Review: Reed and Fabric as standalone modules: public API design — holistic

```yaml
duration_s: 176.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-06
```

## Findings

### [NIT:consistency] Mid-line semicolons left unbroken despite card 11's own verification pass
**Location:** `manifest/designs/reed-fabric-standalone-api.md:61,105,172,194,197,207,271,487,488`
**Issue:** `tools/mdreflow/reflow.go`'s `findCuts` unconditionally cuts at every semicolon (the `semi`/`sp2` branch has no guard, unlike the comma+conjunction branch), and CLAUDE.md's rule names a semicolon as one of the two clause-boundary triggers, yet these nine lines join two independent clauses on one line with a mid-line `;` — e.g. line 61 ("...is one file, `internal/hubforge/seed.go`; every other reference...") and line 172 ("...gate alone; the Someday items are a secondary note..."). Card 11 explicitly required reviewing the finished document against `tools/mdreflow`'s rules before landing, and running the actual tool over the file would reflow all nine.
**Fix:** Break each cited line after its semicolon, continuing the clause on the next line, matching the convention already used elsewhere in the same document (e.g. line 22's "so it satisfies that rule..." break pattern).

## Verdict

REQUEST_CHANGES
Content, measurements, plan coverage, and constraints are sound; nine semicolon-joined lines violate the semantic-line-break convention card 11 was required to enforce.
MILL_REVIEW_END
