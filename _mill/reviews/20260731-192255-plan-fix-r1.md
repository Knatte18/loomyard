# Plan fix report — round 1

Source review: `_mill/reviews/20260731-192122-plan-review-r1.md` (verdict REQUEST_CHANGES, blocking_count 2, nit_count 1)

## Fixed

- "Card 44 targets a scoutengine teardown site that does not exist" — verified by reading `ensureserver.go` and `refs.go`: the daemon's own doc comment confirms it "ends on its own" with no observable clean-exit event, and `teardownConnection`'s `connKindSupervised` branch is a deliberate bare `return`. Renamed Card 44 to "Missing spawn observability", dropped the invented teardown-search instruction, and added an explicit note that Card 43's already-converted wedged-kill `Warn` is the only teardown event scoutengine's own code can ever observe — no further call site exists to add.
- "Card 37's run.go:447-448 citation contradicts its own 'retries once' framing" — verified against the current `run.go`: lines 447-448 are `RunAttempt`'s own Go-error path (no retry, any attempt), while the actual died/timeout retry fall-through is at lines 486-490. Repointed the bullet at 486-490, reworded to describe the silent `continue` on `attempt == 1`, and explicitly excluded 447-448 from a new call since its error already wraps `round` context per `adoption-scope`'s negative case.
- "cmd/lyx/main.go line citations are stale by ~13 lines" — refreshed against the current file: Card 27's `PersistentPreRunE` body citation "lines 82-85" → "lines 95-98"; Card 28's `main()` citation "lines 39-44" → "lines 39-47" and `run()` citation "lines 46-52" → "lines 55-63". `newRoot()`'s "lines 70-133" and `main.go:113`'s `cobra.EnableTraverseRunHooks` citation were both confirmed still accurate and left unchanged.

## Pushed Back

(none)
</content>
