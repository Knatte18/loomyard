MILL_REVIEW_BEGIN
# Review: reed: attach doesn't reconcile session geometry with the terminal — holistic

```yaml
duration_s: 467.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (claude-sonnet-5 per system context)
reviewed_file: plan/
date: 2026-08-26
```

## Findings

None.

Verification performed this round (all four batches plus the overview against live source):
- Confirmed `planLayout`'s current signature, `applyLayoutLocked`'s guard order and doc comment wording ("skip both tmux calls entirely"), and the exact four `e.planLayout(st, live)` call sites in `apply_test.go` that card 4 must update.
- Confirmed `overlay.go`'s `TmuxCmd.run`/`output`/`execHook` shape, `exactSessionTarget`/`exactSessionWindowTarget`, and that `requiredSubcommands` (probe.go) already lists `display-message`, `select-layout`, `set-option`, `list-panes` but not `attach-session` — matching the no-probe-change decision.
- Confirmed `lifecycle.go`'s `ensureServerAndSessionLocked` mouse pin is immediately before `return true, stripped, nil`, matching card 3's insertion point, and that `remain-on-exit`/`mouse` do fail the boot (contrasted correctly against the new non-fatal pins).
- Confirmed `loadOrInitStateLocked` (spawn.go) makes no disk write itself, matching card 6's "never call SaveState" read-only claim; confirmed `reconcileApplyPersistLocked` calls `applyLayoutLocked(st, live)` with an unchanged 2-arg signature.
- Confirmed `internal/reedcli/attach.go`'s and `internal/loomcli/bootstrap.go`'s current `attachArgv` duplicates, both `TestAttachArgv` tests (cli_test.go, bootstrap_test.go) pinning the bare 5-element argv, and that `internal/loomcli` already imports `internal/reedengine` (via `findStatusStrand`/`resolveStatusStrandAction`) — all match cards 9–11's deletion/convergence claims.
- Confirmed `go.mod` has `golang.org/x/sys v0.45.0` direct and no `golang.org/x/term` line, while `go.sum` already carries `golang.org/x/term v0.42.0` with both `h1:` and `/go.mod` hashes — card 8's claims are accurate.
- Confirmed `docs/overview.md:298` and `:318` wording ("hands the operator's stdio to a `tmux attach-session` child") stays true after the chained-argv change, and that the Shared Decision correctly and explicitly scopes `lyx reed attach`'s own `Long` as in-inventory (card 9) while `docs/overview.md`/`lyx loom run`'s `Long` are named out — no inconsistency.
- Confirmed `render/height.go`'s `clampToFit` last-resort branch can legitimately leave the active pane's debt unrepaid (floored at 1 without full reclaim), supporting card 5's "cell heights may sum to more than box.H" note.
- Confirmed `newIntegrationEngine`/`waitUntil`/`seedReedConfig` (contract_integration_test.go, mouse_boot_integration_test.go) match card 14's harness-reuse description, and that no `smoke`-tagged test references the old `attachArgv` builders.
- Cross-checked `## All Files Touched` against the union of every card's `Edits:`/`Creates:` — exact match, no omissions or extras.
- Batch Index DAG (1→2→3→4) is acyclic, every `file:` exists, dependencies match the real dependency order (windowsize.go before attach.go before CLI convergence before docs/proof).
- Global card numbering (1–14) is sequential with no gaps across the four batches; every card has Creates/Edits/Context/Moves/Requirements/Commit; every `Moves:` is `none` (no rename-mechanic section required, correctly absent).
- Context completeness spot-checked across all 14 cards (windowsize.go, attach.go, spawn.go, lock.go, render/{types,rules,height}.go, config.go, etc.) — every named identifier resolves to a file already in that card's own `Context:`/`Edits:`.
- Integration test reachability: batch 4's `verify:` explicitly runs `-tags integration -run TestAttachGeometry`, and the task's `done_gate` runs the full `integration` tag separately — reachable both ways.

No BLOCKING or NIT findings identified. The plan is unusually well-grounded against the actual source tree and internally consistent across its Shared Decisions, batch scopes, and card requirements.

## Verdict

APPROVE
Plan is accurate, complete, and internally consistent against source; no findings this round.
MILL_REVIEW_END
