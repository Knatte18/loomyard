# Plan fix report — round 1

```yaml
slug: yamlengine
round: 1
source_review: _mill/reviews/20260624-081708-plan-review-r1.md
verdict_addressed: REQUEST_CHANGES (1 blocking, 1 nit)
date: 2026-06-24T08:18:18Z
```

## Fixed

- [r1 BLOCKING] "main_test.go board fixtures break under strict Load" — added a
  requirement to batch 6 Card 21 to rewrite the `board.yaml` fixtures in
  `TestRunDispatchesToBoard` and `TestRunBoardErrorPropagatesExitCode` from
  `path: board` to a template-complete config (all four keys), preserving each
  test's intent, so batch 6's `./cmd/lyx/` verify passes under strict Load. Added
  `internal/board/template.go` to Card 21 Context. Verified against `cmd/lyx/main_test.go:54,88`.
- [r1 NIT] "Card 15 ide tests are behind //go:build integration" — added a note to
  batch 5 Card 15 that `menu_test.go` is `//go:build integration`, so the batch
  verify only confirms compilation; the updated assertions are validated by review
  and the integration tier, and the existing integration tests must be adjusted to
  expect the new hard load-error. Verified the build tag on `internal/ide/menu_test.go`.

## Pushed Back

(none)
