# Discussion fixer report — round 1

Source review: `_mill/reviews/20260630-183642-discussion-review-r1.md` (verdict APPROVE, 4 NOTEs).
All four NOTEs passed the `mill-receiving-review` decision tree (factually accurate, no harm) → fixed.

## Fixed

- `20260630-183642-discussion-review-r1.md` "[NOTE] -loomyard flag: placement undecided + %~dp0 quoting" — Decision: scratch-destination now pins `-loomyard` as a **top-level** flag on `fs` (alongside `-parent`/`-reset`), threaded into `runSuite`, and documents the `%~dp0` trailing-backslash gotcha with the `"%~dp0."` + `filepath.Clean` remedy. Scope bullet and Technical-context main.go note updated to match.
- `20260630-183642-discussion-review-r1.md` "[NOTE] Fetch-helper dest argument is described inconsistently" — pinned the helper signature to `(hostRepoDir, loomyardRoot string, info binaryInfo) error` in Technical context and Testing; `dest` = loomyard root, helper joins `.scratch` and `MkdirAll`s it. Removed the ambiguous "dest `.scratch` dir" wording.
- `20260630-183642-discussion-review-r1.md` "[NOTE] Stale prior sandbox-report.json not cleaned before launch" — added Decision: clean-slate-report (pre-launch `os.Remove` of any prior `<host>/sandbox-report.json`), a Scope bullet, and a Testing case asserting a stale file is removed and a no-write run surfaces as missing-report.
- `20260630-183642-discussion-review-r1.md` "[NOTE] \"Present items array\" check needs pointer/RawMessage" — Decision: validation-strictness now specifies decoding `Items` as `*[]Item` (nil pointer = absent → reject; non-nil possibly-empty = present → accept), with `json.RawMessage` noted as the equivalent. Added an "Absent `items` key" Testing case distinct from "Empty findings".

## Pushed Back

_None._
