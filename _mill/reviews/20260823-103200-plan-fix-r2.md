# Plan fix — round 2

Source review: `_mill/reviews/20260823-102821-plan-review-r2.md`

## Fixed

- r2 `[BLOCKING:scope]` "Card 5's Context omits the loomshed files it cites by name" — added `internal/loomshed/discussionvalidate.go` and `internal/loomshed/planvalidate.go` to batch 3 / card 5's `Context:`, since its Requirements justify both `RunE` call sequences by naming `discussionValidate.Call` and `planValidate.Call` verbatim.
- r2 `[BLOCKING:design]` "Card 3's new cancellation case can't reach the path it claims to cover" — verified against `internal/loomshed/ctx.go`: `entryErr` and `cancelErr` both read the identical `ctx.Err()`, and `entryErr` is `Call`'s first statement, so a pre-cancelled context never reaches `nonDoneExit`'s check.
  The requirement's premise was false.
  Replaced the "add a second cancellation case" instruction with an explicit instruction to add none, stating the reachability reasoning so a later reader does not read the single case as an oversight, and added `internal/loomshed/ctx.go` to card 3's `Context:`.
  Also corrected batch 2's `## Batch Tests` wording, which still described "the extra cancellation case".
- r2 `[NIT:consistency]` "docs/overview.md tree-placement guidance has no real precedent to follow" — verified `internal/planparser` has no module-tree entry in `docs/overview.md`, only a module-list bullet.
  Replaced card 9's tree-entry instruction with an explicit instruction to add none (adding one only for the new package would make the tree inconsistent with the package it mirrors), and pinned the module-list bullet to a concrete anchor: immediately after the existing `planparser` bullet.
  The discussion's "module table gains `internal/discussionparser`" obligation is discharged by that bullet.

## Pushed Back

_None._
