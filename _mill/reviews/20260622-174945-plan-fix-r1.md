# Plan fix — round 1

Source review: `20260622-174913-plan-review-r1.md` (verdict APPROVE, 0 BLOCKING, 2 NITs).

## Fixed

- `20260622-174913-plan-review-r1.md` — [NIT] "Card 13 leaves drop-vs-fold choice to
  implementer": made Card 13 deterministic — instruct **drop**
  `TestMenuZeroWorktreeMessage` (not fold), since `TestMenuRequiresLyxDir` is the keeper
  covering the `_lyx`-filter path; keeps the name-map diff unambiguous.
- `20260622-174913-plan-review-r1.md` — [NIT] "Card 17 'before' counts hard-coded
  alongside derivation rule": reworded Card 17 so the `_mill/plan/baseline/<pkg>.txt`
  files are the single source of truth for "before" counts (re-read the files; the literals
  are illustrative-as-captured), and changed the name-map line for
  `TestMenuZeroWorktreeMessage` from "if it was dropped rather than folded" to the
  definite "(dropped — covered by `TestMenuRequiresLyxDir`)" to match the Card 13 fix.

## Pushed Back

_None._
