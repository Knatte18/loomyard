# Plan fix — round 1

Source review: `_mill/reviews/20260811-152746-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 8 NIT).

## Fixed

- `20260811-152746-plan-review-r1.md` — "[BLOCKING:scope] Signature changes break callers the plan never inventories".
  Verified and worse than reported: `WireJunctions` has ~50 call sites across 15 test files, plus `internal/configcli` and `internal/loomengine`.
  Resolved by splitting rather than churning — card 14 now specifies `WireJunctionsWith(rec, l, slug, names)` carrying the body and `WireJunctions(l, slug, names)` as a thin nil-recorder wrapper, so no existing call site changes;
  a new batch-4 scope decision states that the compiler-enforced-parameter rule binds the eight gate executors, not every function on the path to one.
  `(*Fabric).ResetHard`'s two extra callers in `warpforward_integration_test.go` added to card 12's `Edits:`, and the three in-package helper test call sites (`portallauncher_test.go`, `weftwiring_test.go`, `export_test.go`, `clone_reset_guard_test.go`, `junction_test.go`) added to cards 13 and 14.
  Batch 4's verify widened from `go vet -tags integration ./internal/fabricengine/...` to `./...` so the out-of-package breaks cannot survive to the done gate.
- `20260811-152746-plan-review-r1.md` — "[BLOCKING:design] `branch_pushed` has no branch name at two of its three sites".
  Verified: `PushWeft`/`PushWarpAt` are bare `PushCoalesced()` calls, and `pushRebaseFreeLogged` maps `gitrepo.ErrPushRejected` to a `nil` return.
  Card 19 now names the exact resolution (`(*gitrepo.Repo).CurrentBranch()`) and the exact success predicate (`HasUnpushed()` sampled before and after, recording only on a `true` → `false` transition), which excludes both the nothing-to-push and the swallowed-rejection paths.
  The same defect existed at card 24's `Bolt.Push` sites, which the review did not reach: those now record nothing, with the reason written into the plan and required as a code comment.
- `20260811-152746-plan-review-r1.md` — "[BLOCKING:scope] Card 32 must table twelve result types it cannot read".
  Verified.
  Added the eleven declaring files to card 32's `Context:`.
- `20260811-152746-plan-review-r1.md` — "[NIT:consistency] Batch Index verify drops every tagged run".
  Verified.
  The `batches:` entries for batches 4, 5 and 6 are now byte-identical to their batch files' `verify:`.
- `20260811-152746-plan-review-r1.md` — "[NIT:consistency] `Len()` cannot be both value-receiver and nil-safe".
  Verified.
  Card 1 declares `Len()` on `*Mutations`, and states the `res.Mutated().Len()` non-addressability consequence for `errWithRecord`.
- `20260811-152746-plan-review-r1.md` — "[NIT:consistency] Card 21's package and its fixture source disagree".
  Verified: `remove_guard_integration_test.go` is `package fabricengine_test`.
  Card 21 now names `package fabricengine_test` and notes every identifier it asserts is exported.
- `20260811-152746-plan-review-r1.md` — "[NIT:consistency] The "fixed key set" invariant is false as planned".
  Verified.
  The Shared Decision now carves out the pre-flight failure class explicitly and requires card 22's doc comment and card 33's `CONSTRAINTS.md` wording to carry the carve-out in the same breath.
- `20260811-152746-plan-review-r1.md` — "[NIT:consistency] Enumeration counts contradict the source".
  Verified all three counts: 14 `Run` literals, 8 `return fabricengine.CloneResult{}, err` sites.
  Corrected, the uniform-shape claim dropped, and `pullCase`/`unwireJunctionsCase`'s differing shape called out by name.
- `20260811-152746-plan-review-r1.md` — "[NIT:scope] Two cards list edits their own requirements make unnecessary".
  Verified both.
  `manifest.go` dropped from card 28's `Edits:` (same package, so the unexported helper is reachable directly) and moved to `Context:`;
  `internal/fabriccli/spawn.go` dropped from card 25's `Edits:`.
- `20260811-152746-plan-review-r1.md` — "[NIT:consistency] Card 23 both routes and exempts `runReconcile`".
  Verified.
  Removed from the bullet list, with one sentence saying card 24 owns it.
- `20260811-152746-plan-review-r1.md` — "[NIT:consistency] `repo_advanced` contradicts the plan's own vocabulary decision".
  Verified the enforcement reads only `host`-compounded phrases, so the kind does not break the build.
  Card 1 now requires `KindRepoAdvanced`'s doc comment to state that "repo" is deliberately side-agnostic and the `Target` tells the sides apart.

## Pushed Back

_None._
Every finding was factually accurate and none of the fixes broke existing functionality, conflicted with a documented decision, or destabilised work outside this task.
