MILL_REVIEW_BEGIN
# Review: Extract scout into its own standalone repo — holistic

```yaml
duration_s: 2603.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [NIT:consistency] Batch 5's `-count=1` rationale isn't in the mechanized `verify:`
**Demoted-from:** BLOCKING
**Location:** 00-overview.md batch 5 `verify:` / 05-quarry-live-and-equivalence.md `verify:` + Batch Tests
**Issue:** The batch's own prose says `-count=1` "matters here" because these tests spawn real servers and a cached pass "would prove nothing," and card 34's own manual command includes it — but the machine-read `verify:` field in both the batch file and 00-overview.md's Batch Index is `go -C .../quarry test -tags lsp ./...`, with no `-count=1`. Since card 34 already populated the build cache with a passing run moments earlier, the actual gate that fires after the batch can report a stale cached "ok" without re-exercising anything, undermining the exact guarantee the batch exists to provide.
**Fix:** Add `-count=1` to the `verify:` field in both files, or state explicitly why the mechanized gate is allowed to differ from card 34's own invocation.

### [NIT:consistency] Shared Decision overclaims what batch 4 does with the sixth tagged file
**Demoted-from:** BLOCKING
**Location:** 00-overview.md, "one opt-in test tag, lsp" Shared Decision vs. 04-port-cli.md Batch Scope / Card 24
**Issue:** The Decision states "the five `//go:build scout` files and the one `//go:build integration` file collapse onto it [lsp] during the port." Verified against source: there are indeed 5 `//go:build scout` files (all in scoutengine) and exactly 1 `//go:build integration` file (`scoutcli/cli_integration_test.go`). But batch 4's own Batch Scope and Card 24 explicitly say that one integration file is **not** ported at all — its subject (`lookupContext`'s hub branch) is deleted, and Card 31 replaces it with an **untagged** `resolve_test.go`. Only 5 of the 6 files actually collapse onto `lsp`; the sixth doesn't survive in any tagged form.
**Fix:** Reword the Decision to say 5 of 6 collapse onto `lsp` and the sixth (`cli_integration_test.go`) is dropped and replaced by an untagged test because its subject (the hub branch) is deleted, not carried forward under any tag.

### [NIT:consistency] Card 17's line-number/description pairing for ensureserver.go is shifted by one
**Location:** 03-port-engine.md, Card 17
**Issue:** Card 17 names six lyx-mentioning lines in `ensureserver.go` — "40, 160, 257, 260, 357, and 496" — paired with five descriptions. Verified against source: those six line numbers are exactly right (confirmed via grep), but the prose pairing is off by one starting at the second entry: line 160 actually reads "finalizeConnection failure lyx makes no second spawn attempt" (ensureNative's retry note, undescribed), while "a lyx-owned...daemon" actually sits at line 257, "this worktree's own future lyx invocations" at 260, and ".lyx/scout/<lang>/" at 357 — each one slot later than the card's list implies. The broad "rewrite every occurrence" instruction still covers line 160, so this is low-impact, but a reader cross-checking descriptions against lines will be confused.
**Fix:** Re-pair the five descriptions against the correct line numbers, and add the missing sixth description for line 160's actual content (ensureNative's no-retry note).

### [NIT:consistency] Card 43 mislabels a roadmap "Done" entry as "Someday"
**Location:** 06-lyx-removal.md, Card 43 (manifest/roadmap.md)
**Issue:** Card 43 calls the item to delete "The Someday `scout` item itself — the LSP-backed code intelligence entry... since the thing it describes has shipped and left." Verified against `manifest/roadmap.md`: that exact entry ("scout: LSP-backed code intelligence — V1 Go-only...") sits at line 278, inside the `## Done` section (180-308), not `## Someday` (73-179) — consistent with it having shipped. The quoted content is specific enough that an implementer will likely still find it, but the section name is wrong, and deleting a `## Done` ledger entry (rather than the file's usual convention of Done entries persisting, pointing at package docs) is a real convention deviation the card doesn't acknowledge.
**Fix:** Say "the Done `scout` item" instead of "Someday," and note explicitly that this is a deliberate exception to Done-entries-persist because the module's own package documentation (the thing Done entries point at) is also leaving the repo.

## Verdict

APPROVE
Two consistency contradictions (verify cache gap, decision-vs-batch mismatch) plus two low-impact labeling nits.
_Note: 2 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
