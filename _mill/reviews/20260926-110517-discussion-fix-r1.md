# Discussion fix — round 1

Source: `20260926-110410-discussion-review-r1.md`

## Fixed

- r1: "`run_dir` is filled on one of the three step entry points only" — `RunDir` now filled at each module's single `Spec` construction site in `arm.go`; tests assert all three entry points.
- r1: "The repair verb set does not cover the crash windows this task absorbs, and the discussion never says so" — coverage list per absorbed window added; narrow trace-gated stranded-branch exception decided and recorded against the Fabric Git Invariant.
- r1: "Trace for kind-less pre-run refusals is left to the plan" — decided: `trace_dir` plus per-step `LYX_TRACE_ID`; `shedcli` pre-run untouched.
- r1: "\"The file whose name carries that id\" is not unique" — now the set of files carrying the id, ordered by UTC timestamp.
- r1: "Recipe-name tripwire needs word boundaries" — whole-word, case-insensitive match.
- r1: "Merge ordering with #23 loom-done-after-friction" — ordering note added under `step-cap-dropped`.

## Pushed Back

None.
