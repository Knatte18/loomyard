MILL_REVIEW_BEGIN
# Review: Migrate planparser.Card to Edits/Uses fields — holistic

```yaml
duration_s: 500.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-24
```

## Notes

Verified against source across all four batches, not just plan-internal consistency:

- Batch 1 (planparser core): current `plan.go`/`parse.go`/`validate.go`/`normalize.go` match every field/function named in Cards 1-7's `Requirements:` exactly (label constants, `noneSentinel`, `parseFileOpField`/`parseMovesField`, `createsUnion`/`movesTargetsUnion`, the 15 current check IDs). The new 16-check set in Card 5 sums correctly (`checkFormatAndApproval` alone emits 2 IDs, giving 15 named functions → 16 IDs), matching the `sixteen-checks-counted-by-id` Shared Decision.
- Card 8's golden-fixture path arithmetic ("seven distinct paths… two deliberately absent") checks out exactly against the stated per-card target/Uses/Pairs shapes and the type-conditional `path-missing` rule in Card 5.
- Batch 2/3 test-file rewrites (parse_test.go, sections_test.go, normalize_test.go, validate_test.go, template_test.go, runlevel_test.go, loomshed/loomrecipe fixtures, loomcli validate_test.go + parity_test.go, webstercli validate.go/cli_test.go) all cite real, currently-existing test/helper names, and every delete/keep/retarget list matches what's actually in each file today.
- Batch 4 doc/stencil/spec rewrites (loom-plan-spec.md, loom-template-plan.md, webster-body-implementer.md, SANDBOX-WEBSTER-SUITE.md, plan-card-format.md status banner + Open-items, scout-plan-symbol-fields.md, roadmap.md Wave 2/3/Someday structure, websterengine/doc.go's `HasSymbolFields()`/deviation-union passages) all target real current text, and Card 24/25's roadmap repointing (stale-doc ownership to the Someday items, Wave 2→Done, Wave 3 dependency reword) lines up with the roadmap's actual current structure.
- `All Files Touched` is a complete, exact union of every card's `Edits:`/`Creates:`/Move-target path across all 26 cards — no omission, no extra entry. Global card numbering (1-26) is sequential with no gaps. Batch DAG (`depends-on: [1]/[2]/[3]`) is acyclic and file-accurate.
- No card's `Moves:` field is non-empty anywhere in this plan itself (it correctly stays on the current format-3 shape, since this plan predates its own migration), so the "Rename mechanic section required" criterion doesn't engage — consistent, not an omission.
- `internal/batcher` and `internal/loomshed/planvalidate.go`'s "reads no Card field" claims, and the roadmap correction that no non-test websterengine code beyond `render.go` ever read a file-op field, are all confirmed by source inspection.

No constraint violations, no context-completeness gaps, no vague requirements, no over-engineering, no unreachable integration tests found.

## Verdict

APPROVE
Every cross-batch claim and numeric assertion checked against source confirms correct; no findings.
MILL_REVIEW_END
