# Plan fix — round 1

Source review: `_mill/reviews/20260809-091032-plan-review-r1.md`

## Fixed

- r1 `[BLOCKING:scope]` "Card 15's `unbindHub` has no committing mechanism in Context" — added `internal/fabricengine/bolt.go` to card 15's `Context:`, specified that `newClonedHubFixture` ends with a stage-all `fabricengine.NewBolt(res.BoardDir).Commit(...)` so the board is clean, and that `unbindHub` commits the deletion the same way; added an explicit prohibition on using `commitFileOnBranch` for it.
- r1 `[NIT:consistency]` "Card 5's usage string names a flag not yet registered" — recorded the premature mention as deliberate in card 5, with the reason (avoids rewriting the same line in card 8; batches squash to one commit on merge, so no released build advertises an unregistered flag).
- r1 `[NIT:consistency]` "Card 14 tells the implementer to leave a nonexistent `runStatus` alone" — corrected to `runPairs`, verified as the function backing `lyx fabric pairs` at `internal/fabriccli/fabric.go:407`.
- r1 `[NIT:consistency]` "Card 10's rationale for `--force-bootstrap` misreads its own fixture" — verified `makeCLICloneWeftBare` creates a zero-commit bare repo (`internal/fabriccli/cli_test.go:493-499`); dropped the flag from card 10's two end-to-end clones and from card 16's fixture clone, and stated the unborn-HEAD reason at both sites.

## Pushed Back

_(none)_
