# Plan fix — round 4

Source review: `_mill/reviews/20260819-093709-plan-review-r4.md` (holistic, sonnet/xhigh, APPROVE, 0 BLOCKING + 1 NIT, none demoted).

## Fixed

- r4 "Card 6 requirement names a test not in its Context/Edits" — accurate;
  card 6's rename requirement cites `destructivegaps_integration_test.go`'s `TestOwnership_WarpCheckoutKind` as the reason the exported seam name stays unchanged, but the file was absent from every field.
  Added `internal/fabricengine/destructivegaps_integration_test.go` to the card's `Context:` (read-only — the card still does not edit it), keeping the citation rather than weakening it to a bare invariant statement.

## Pushed Back

(none)

## Validation

- `_plan_dag.validate`: 6 batches, DAG OK.
- `_plan_validate.run`: 0 findings.

## Convergence

Round 4: `APPROVE`, `blocking_count: 0`, and no finding carries `demoted: true`, so the convergence gate is satisfied (`round 4 >= min_rounds 1`).
Plan approved.
