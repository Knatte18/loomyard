# Discussion fix — round 2

Source review: `_mill/reviews/20260820-084020-discussion-review-r2.md` (APPROVE, 3 NIT).

## Fixed

- r2 NIT:scope "Doc-falsification list misses the \"in what order\" family" — replaced the hand-enumerated closed list with a stated grep sweep (eight search terms over `internal/**/*.go`, `manifest/designs/*.md`, `contracts/specs/*.md`) plus a committed per-hit disposition; added the specific hits at `shed.md:11`/`:43`, `loom.md:22`/`:55`, `doc.go:5`, and noted the "in what order" family may survive with a qualification rather than a rewrite.
- r2 NIT:decision "`loomshed.Deps.MaxBounces` field doc has no stated disposition" — named `loomshed.go:50-52` as an in-scope same-commit edit in both the Scope bullet and the doc-falsification inventory, and corrected the "`Shed.MaxBounces` keeps its name" decision, which had claimed `Deps.MaxBounces` and `wiring.go` were untouched.
- r2 NIT:consistency "`ProducerDef`'s struct doc says \"the two things\"" — named `producer.go:34`'s struct-level comment as in scope alongside the three new field docs, in both the Technical context entry and the inventory.

## Pushed Back

(none)
