# Plan fix — round 3

Source review: `_mill/reviews/20260820-114720-plan-review-r3.md` (holistic, `sonnetxhigh`, APPROVE, 0 BLOCKING + 1 NIT, none demoted).

## Fixed

- r3 — "[NIT:consistency] Card 22's rationale for dropping the internal/clihelp banned-import entry is wrong":
  the finding is correct on the Go semantics. `seam_enforcement_test.go` tests `strings.Contains(importPath, "/internal/") && strings.HasSuffix(importPath, "cli")`, and `github.com/Knatte18/loomyard/internal/clihelp` ends in `help`, not `cli`, so that rule never matched the `clihelp` entry — the two checks are independent, and the separate `internal/clihelp` entry existed precisely because the suffix rule could not catch a package carrying cobra without the `cli` suffix.
  Card 22 now states the real reason the entry can go: quarry has no package by that name at all, because `clihelp`'s used surface was absorbed into `internal/cli` in batch 2, so the entry would match nothing and would only mislead a later reader into thinking a real package is being guarded against.
  The instruction itself (drop the entry) was already right;
  only its justification was wrong, and a wrong justification in a plan is what a later reader copies.

## Pushed Back

_None. The finding verified accurate against `internal/scoutengine/seam_enforcement_test.go`, and the fix touches only prose in the card's `Requirements:`._

## Convergence

Converged: round 3 is at or past `min_rounds` (1), the verdict is APPROVE with `blocking_count` 0, and no finding in this round carries `demoted: true`.
The overview's `approved:` flips to `true` and the review loop ends here, at round 3 of 7.

## Verification

- `_plan_dag.validate` — OK.
- `_plan_validate.run` with `skip_checks={"out-of-worktree-target"}` — 0 findings.
