# Plan fix — round 3

Source review: `20260716-200821-plan-review-r3.md` (REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- [BLOCKING] "AuditForks workdir is WorktreeRoot, must be Cwd" — Card 5 point 3 now
  passes `run.runner.layout.Cwd` and requires both the call-site comment and
  `AuditForks`' doc comment to state that the workdir is the pane's actual process cwd
  (mux launches panes with `-c e.layout.Cwd`; Cwd diverges from WorktreeRoot when
  invoked from a subdirectory).
- [NIT] "Card 9 leaves template_test.go 'eight markers' comments stale" — Card 9's
  requirement now names template_test.go's stale marker-count prose (lines ~4-5,
  ~42-43, ~58-59) and adding `cluster_rules` to the hard-coded required-marker list
  (~line 67).

## Pushed Back

(none)
