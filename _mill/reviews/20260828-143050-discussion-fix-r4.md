# Discussion fix — round 4

Source review: `_mill/reviews/20260828-142833-discussion-review-r4.md`

## Fixed

- `[NIT:scope]` "Sandbox suite spec asserts the reaped behaviour" — correct, and the most consequential of the four: `tools/sandbox/SANDBOX-REED-SUITE.md` is the document M16 and M22 were graded against, so leaving it stating the pre-fix expectation would make the next sandbox pass re-report the fixed behaviour as a regression.
  Added it to Scope → In with a per-scenario disposition, and amended the `no-new-CONSTRAINTS-entry` Decision so the doc inventory names it alongside `doc.go`.
  Checking the file also showed **M22's spec is falsified too**, which the finding did not name: its *Watch* expects the rebuilt header "at the very top of the window with the strand stack below it", but the orphaned strand pane is now reaped along with the old header, so the correct expectation is the header alone.
  Both scenarios' *Watch* and `FAIL` wording are now in scope.
- `[NIT:consistency]` "Adoption vocabulary survives in a live error string" — correct;
  the round-3 in-scope list covered comments only and missed an operator-facing string.
  `planPaneTarget`'s `"session has no panes to adopt or split"` becomes `"session has no panes to split"`, and both verbatim quoting sites (`smoke_lifecycle_test.go` ~line 217, `SANDBOX-REED-SUITE.md` ~line 285) are named as moving with it.
- `[NIT:scope]` "One adoption-framed smoke test has no disposition" — correct;
  the stated sweep hits `TestSmokeRemoveLastStrandThenAddRunsTheNewCommand` and the round-3 disposition list omitted it.
  Added as comment-and-skip-message-only, with the reason its mechanics still hold (the sole pane is corpsed, the corpse is not the header, and the next `add` splits instead of adopting) and an explicit note that what it asserts must stay asserted.
- `[NIT:design]` "Chokepoint ordering pinned only by real tmux" — correct, and a genuine tier gap: the chokepoint is this task's central structural fix, yet was guarded only by a smoke regression that proves an outcome on one backend and does not run on every `go test`.
  Added an untagged Testing subsection using the existing `execHook` fake to assert the ordering directly — `kill-pane` before `split-window`, a re-`list-panes` between them, and a split target that is not the reaped pane — plus a nothing-to-reap companion case asserting no `kill-pane` and no redundant re-enumeration.

## Pushed Back

_None._
