# Plan fix — round 1

Source review: `_mill/reviews/20260913-174953-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 2 NIT).

## Fixed

- r1 holistic — "[BLOCKING:scope] CommitSeededStencils signature change misses a third caller".
  Verified: `internal/fabricengine/stencilhistory_integration_test.go:34` calls the verb at the old four-argument arity.
  Added that file to batch 3 card 11's `Edits:` and a requirement paragraph naming the single call site and what it must pass, plus a note that card 9's leave-alone instruction covers the production history file, not this test.
- r1 holistic — "[BLOCKING:scope] PlanSpec/composePlanPrompt signature change breaks plan_test.go's own build".
  Verified, and wider than reported: 68 call sites across `internal/loomengine/plan_test.go`, `internal/loomengine/config_test.go`, and `internal/loomcli/wiring_test.go`.
  Added the two loomengine test files to batch 5 card 21's `Edits:` and the loomcli one to card 23's, with an explicit requirement that a real non-empty specs directory be threaded through every site via a shared test helper — never an empty-string arity patch, which would compile now and fail at run time once batch 6 inserts the marker.
- r1 holistic — "[BLOCKING:scope] RenderForkPrompt/RenderRecoveryPrompt signature change breaks template_test.go".
  Verified: 34 calls in `internal/websterengine/template_test.go`; the neighbouring render-helpers test file carries none.
  Added that file to batch 5 card 22's `Edits:` with the same real-value requirement.
  The same check also showed card 32 named the wrong file for its renderer assertions, so card 32's `Edits:` and prose were retargeted onto `template_test.go` as well.
- r1 holistic — "[NIT:consistency] sync's second (specs) commit-failure path is unspecified".
  Verified.
  Batch 4 card 14 now states the specs half's failure posture explicitly: a force-refresh failure returns a bare error envelope, a commit failure returns the record-carrying envelope with the snapshot accumulated so far (which by then already holds the stencils half's mutations), both returning early and leaving the stencils commit landed and reported as partial.
- r1 holistic — "[NIT:scope] New SpecsDir geometry field ships with no direct assertion".
  Verified: both geometry tests compare field by field, so the batch verify would pass with the field left empty.
  Added `internal/hubgeom/webstergeom_test.go` and `internal/standalonegeom/standalonegeom_test.go` to batch 2 card 8's `Edits:`, requiring the assertion beside each existing stencils-directory check — including the standalone side's second, shared-helper-equality assertion that pins the one-construction-site property.

## Fixed beyond the review's findings

- Same class as the three BLOCKINGs, found while verifying them: batch 5 card 20's new `SpecsDir` absolute-root guards break every existing `Env` fixture that has no such field — `internal/shedrecipe/fixture_test.go`, `internal/shedrecipe/entries_simple_test.go`, `internal/loomrecipe/fixture_test.go`, and `internal/loomrecipe/shape_test.go`.
  `internal/loomrecipe` was in no card's scope and in no batch's verify, so the break would have surfaced only at the repository-wide done gate.
  Added all four files to card 20's `Edits:`, added a missing-root table row for the new field alongside the existing one, and added `./internal/loomrecipe/...` to batch 5's verify command (synced in both the batch frontmatter and the overview's Batch Index).

## Pushed Back

_None._
All five findings verified as factually accurate, and none of the fixes breaks existing functionality, conflicts with a documented decision in `_mill/discussion.md`, or destabilises work outside this task.
