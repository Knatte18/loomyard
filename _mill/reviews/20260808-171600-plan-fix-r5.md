# Plan fix — round 5

Source review: `_mill/reviews/20260808-171049-plan-review-r5.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 0 NIT).

## Fixed

- `20260808-171049-plan-review-r5.md` — "[BLOCKING] Card 18 misdescribes template_test.go's pathspec fixture and gives a wrong target value".
  Verified: `TestConfigTemplate_PathspecResolvesToPattern` holds a single-element `want := []string{"_pattern"}` at line 93 and asserts the raw resolved `ConfigTemplate()` pathspec value, not `pathspecNames()`'s routing set.
  Directing the implementer to assert `{_lyx}` there would have contradicted template.yaml's own trailing comment ("`_lyx` and `.lyx` are structural and injected in code ..., never read from here") and the card's own `len(cfg.Dirs()) == 0` assertion.
  Card 18 now separates the two files: `configcli_integration_test.go` line 67 is an explicit `WireJunctions` name slice retargeted to `{"_lyx", lyxdirs.DotLyxDirName}`, while `template_test.go`'s `want` becomes an empty slice with the test renamed off "...ResolvesToPattern" and its doc comment rewritten.
  An explicit "do not assert `{_lyx}` for this raw value" instruction was added with the reason.
- `20260808-171049-plan-review-r5.md` — "[BLOCKING] No card actually fixes the dangling link to the deleted design doc".
  Verified: `manifest/roadmap.md:41` is `See [designs/pattern.md](designs/pattern.md).`, and card 37 — the only card mentioning link repair — declares `Edits: none`, so it could not have performed the fix it described.
  Ownership moved to the cards that already edit the containing files: card 34 now retargets roadmap line 41 to `internal/pattern`'s package godoc.
  Card 37's instruction was rewritten from "grep and fix" to "grep and verify", with a stop-and-report rule if a third inbound link appears in a file no card in the batch edits — a dangling link is a worse end state than a late deletion.
  **Beyond the finding:** the grep also surfaced a second dangling link the review did not report, `manifest/designs/fabric-unified-view.md:194` (`- [pattern.md](pattern.md) — hand-authored weft content;`).
  That file is already in card 36's `Edits:`, so card 36 now retargets it the same way.
  Both links are named explicitly in card 37's verification step.

## Pushed Back

None.
Both findings were factually accurate, and neither fix breaks existing functionality, conflicts with a documented decision, or destabilises work outside this task.

## Re-validation

`_plan_dag.validate` — OK.
`_plan_validate.run` — 2 `context-completeness` findings raised by the card-37 rewrite (it now names `manifest/roadmap.md` and `manifest/designs/fabric-unified-view.md` in its Requirements); both files added to card 37's `Context:`, re-run clean at 0 findings.
