# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260912-113502-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- r2 BLOCKING:design — "`DetectCrashResume`'s `slug` parameter is unsourced and duplicates `EntryObservation.Slug`".
  Accurate, and the parameter was a leftover: it predates the decision to carry `Slug`/`Parent` on `EntryObservation` and was never removed, so card 5's own `DetectAnomalies` text already said the crash-resume's slug comes from `entry`, contradicting its own signature.
  Card 7's `selfreportDeps` carries no independent slug either, so the sole call site could only have filled it from `entry.Slug`.
  Dropped the parameter — the signature is now `DetectCrashResume(entry EntryObservation) (Anomaly, bool)` — and card 5 now states why it takes none: `entry` already carries `Slug` and `Parent`, which is exactly what makes the cancelled-context call site work with no final status and no decoded product in hand.

- r2 NIT:consistency — "Collapse's \"first occurrence\" tie-break is unreachable by construction".
  Accurate: only recurring-finding anomalies carry a non-zero `Round`, and one detector call returns at most one crash-resume and at most one halt-kind anomaly whose title shapes collide neither with each other nor with a recurring-finding title, so two same-titled non-recurring anomalies cannot appear in one pass.
  Kept the branch rather than dropping it, and card 7 now marks it unreachable-by-construction with its reason, following the in-repo convention `selfreportengine.CreateIssue`'s own `targetRepo` guard sets — a defensive guard so a future detector change fails predictably instead of silently.

## Pushed Back

_(none — both findings verified accurate and were fixed)_
