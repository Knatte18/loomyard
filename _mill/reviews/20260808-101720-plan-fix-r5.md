# Plan fix — round 5

Source review: `_mill/reviews/20260808-101651-plan-review-r5.md` (holistic, APPROVE, 0 BLOCKING + 1 NIT).

Round 5 was the first review round after the prerequisite-merge revision (main merged in;
card 29 collapsed to the AnchorPath base swap;
batch 1's allowlist amendment retargeted from scoutengine to lyxtest;
the drift/health consumer recorded in batch 8).
The reviewer approved with a single wording NIT, fixed under the default rule.

## Fixed

- r5 holistic — "[NIT] Card 47's weft-path guidance offers two options, one of them wrong": verified against `internal/fabricengine/junction.go` — `HostJunctions` builds `Target: filepath.Join(WeftWorktreePath(l, slug), l.AnchorRel, name)`, so a record's `Target` parent equals the weft worktree root only at `AnchorRel "."` and a subdirectory otherwise.
  Card 47 now names `WeftWorktreePath(l, slug)` as the sole resolution and explicitly forbids the Target-parent derivation, stating why it is wrong in subpath-anchored repos.

## Pushed Back

_None._
The finding was accurate.

## Post-fix validation

`_plan_dag.validate` passes;
`_plan_validate.run` reports 0 errors.
`plan/00-overview.md`'s `approved:` flipped to `true`.
