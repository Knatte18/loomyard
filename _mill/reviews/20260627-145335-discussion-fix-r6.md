# Discussion fix report — round 6

Source review: `_mill/reviews/20260627-145317-discussion-review-r6.md` (verdict APPROVE, 1 NOTE).

## Fixed

- [r6 NOTE] "weft bypass is not wholly a PersistentPreRunE move" — clarified the weft entry
  in the shared-pre-dispatch gotcha (Technical context): the `--weft-path` bypass is split
  across two homes — the `PersistentPreRunE` holds only the resolution-skip and the
  non-push-rejected gate, while the push `RunE` itself branches on the flag (bypass →
  `Push(weftPath, SyncOptions{})` directly vs. normal `Commit`+`Push`). Explicitly stated
  the push-only call must NOT be folded into the PreRunE.

## Pushed Back

(none)
