# Plan fix: round 3

Source review: `20260823-111148-plan-review-r3.md` (holistic, round 3, envelope verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- [NIT:consistency] "Card 10 misdescribes the existing test's Location-building pattern" — reworded card 10 to describe the file's actual pattern (a fixed synthetic path literal, no `t.TempDir()`), matching `TestLoomStatusFile_EqualsAnchorPathJoinedWithLoomStatusRel`'s real construction.
- [NIT:consistency] "Card 15's 'every scalar argument' phrasing doesn't clearly cover the `cfg` struct parameter" — card 15 now explicitly instructs building `cfg` with at least one non-zero field, and explains why a zero-value `cfg` would produce a spurious drift-guard failure.

## Pushed Back

(none)
