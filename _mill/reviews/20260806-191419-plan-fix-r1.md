# Plan fix — round 1

Source review: `_mill/reviews/20260806-191138-plan-review-r1.md` (holistic, REQUEST_CHANGES, 5 BLOCKING + 2 NIT).

## Fixed

- r1 BLOCKING "Card 14 omits in-package tests broken by the unexport" — verified: six `package fabricengine` test files reference the renamed symbols (`index_integration_test.go:92,94`, `commit_gating_integration_test.go:35,57`, `commit_partial_integration_test.go:84,103`, `pull_integration_test.go:125,128`, `snapshot_integration_test.go:532,557`, `weftgit_pathspec_integration_test.go:5`).
  Added all six to card 14's `Edits:` and to `## All Files Touched`, with a requirement stating they retarget onto `newPaired`/`f.warp`/`f.weft` directly (in-package, no shim).
- r1 BLOCKING "Card 16's 'zero readers' claim is false in-package" — verified at `commit_partial_integration_test.go:54,57,58,150` and `commit_integration_test.go:870`.
  Added both files to card 16's `Edits:`, restated the claim as "zero readers *outside the package*", named the two comment mentions at `:27,120`, and added an explicit guard that `CommitResult`'s identically-named exported fields must NOT be renamed (those same files read them heavily).
- r1 BLOCKING "Two production `host repo` sites belong to no batch" — verified at `internal/builderengine/runlevel.go:399` and `internal/burlerengine/profile.go:29`.
  Assigned `runlevel.go` to card 11 and `profile.go` to card 19 with the exact reword, noted the `FixScope` value `"source"` is unchanged, and added both to `## All Files Touched`.
- r1 BLOCKING "Test sweep omits the two heaviest non-owner test files" — verified: `loomengine/preflight_integration_test.go` (40 hits, `TestPreflight_WeftWorktreeRemoved` at `:302,305`) and `websterengine/audit_test.go` (38 hits, `TestWeftReferencePattern` at `:28,39`).
  Added both to card 23 with explicit test-rename and prose requirements.
  Consequential DAG fix: batch 06 now `depends-on: [2, 3]` rather than `[3]`, because card 5 (batch 02) also edits `preflight_integration_test.go` — without the edge the two batches were parallel writers to one file (`parallel-modifies-overlap`).
- r1 BLOCKING "Card 3 `Context:` misses the file declaring `ScopedPathspec`" — verified: declared at `internal/fabricengine/fabric.go:125`.
  Added `fabric.go` to card 3's `Context:`.
- r1 NIT "Card 5 leaves drift.go's now-false comments" — verified: `drift.go:18-25` documents `Returns (true, "", nil)` and `:61-66` states the reason string must keep the substring `junction`.
  Both become false after typing.
  Added to card 5(a) as a staleness fix, with the distinction stated explicitly: `drift.go` keeps warp/weft vocabulary as an owner file, but may not keep a wrong statement.
- r1 NIT "`docs/overview.md:133` 'weft-synced' left unreworded" — verified at `:133`.
  Added the reword to card 28, classified under `doc-vocabulary-split` as consumer-behaviour prose (loom's observable behaviour, not fabric's mechanism), same class as `README.md:62`.

## Pushed Back

_(none — every finding verified accurate and no fix caused demonstrable harm.)_
