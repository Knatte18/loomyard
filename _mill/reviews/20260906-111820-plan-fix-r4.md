# Plan fix — round 4

Source review: `_mill/reviews/20260906-111434-plan-review-r4.md`

## Fixed

- r4 "[BLOCKING:design] Batch 5 existing fixtures use non-repo worktree roots" — confirmed: both `parity_test.go` and `planvalidate_test.go` build fixtures over a bare `t.TempDir()`. Cards 23, 24 and 25 now require `language: none` in every pre-existing fixture overview, so the resolve pass opens nothing and each existing cell keeps its recorded verdict; the two new resolve-backed fixtures and the non-repository case declare `language: go` explicitly, which is what makes the seventh case distinguishable from the four sharing the same root. Added as a batch-local decision so the rule is stated once.
- r4 "[BLOCKING:design] Create arrow bullet is unreachable at the named seam" — confirmed: `parseRefField` calls `stripBackticks` before returning each payload, so the arrow form could never match. Card 9 now gives `createLabel` its own `parseCreateField`, mirroring `parseRenameField`, which already matches against the unstripped payload for exactly this reason, and names the capture field `CreateRaw` on both `TargetGroup` and `Card`; card 10's `checkHandleMalformed` now reads that field by name.
- r4 "[BLOCKING:scope] Card 5 never names the TargetGroups fields to canonicalize" — confirmed: `normalizeCard` walks card-level and group-level fields independently, and four checks read group `Refs`. Card 5 now enumerates the identical field set and states the failure mode it prevents — `createTargetsUnion` holding pre-canonical strings while `checkPathMissing` compares canonical ones.
- r4 "[BLOCKING:design] Extensionless bare filenames become unfixable hard findings" — confirmed against `classify.go`'s own doc comment, which names `Makefile` as the example. `classifyRef` gains a fourth rule sending a dot-free, slash-free token to `refKindPath`, and `directory-target` is narrowed to entries containing a `/`. The narrower coverage that leaves — a slash-free extensionless directory at the repository root — is stated in the check's own comment rather than papered over.
- r4 "[BLOCKING:scope] Batch 6 verify does not run the tests card 26 adds" — confirmed. `./internal/planglyph/` added to batch 6's verify in both the batch file and the overview, and the Batch Tests paragraph that justified the exclusion replaced with one stating why the package is in scope.
- r4 "[NIT:consistency] README prerequisite bullet is in the wrong section" — confirmed: `- Go 1.26+` is in `## Requirements`, and `## Building` is a fenced shell block. Card 2 retargeted, with the distinction stated so the implementer does not re-find it.
- r4 "[NIT:consistency] Batch Tests prose contradicts the declared verify commands" — all four Batch Tests openings now quote their own yaml `verify:` verbatim, batch 5's package count corrected to four, and batch 7's `beginbatch_test.go`/`recordbatch_test.go` moved to the tagged-half sentence where they actually run.
- r4 "[NIT:scope] Card 27 omits two hand-maintained registration lists" — confirmed: `longlist_test.go` checks `root.Long`'s "Available modules:" line and `helptree_test.go` carries a separate `requiredModules` slice. Card 27 now names both edits explicitly, and the incorrect "alphabetical-by-module" claim is replaced with the actual order plus an instruction to append rather than reorder.

## Pushed Back

_None. All eight findings verified accurate against the cited source and none met a harm-check escape._
