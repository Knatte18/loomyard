# Plan fix — round 4

Source review: `20260813-061803-plan-review-r4.md` (holistic, REQUEST_CHANGES, 2 BLOCKING, 2 NITs).

## Fixed

- `20260813-061803-plan-review-r4.md` — "[BLOCKING:scope] NewPairedForTest call-expression count is wrong, and the counting method it claims avoids the error actually reproduces it": verified accurate and fixed.
  Confirmed by reading `internal/fabricengine/weftgit_exclude_test.go` lines 70/72 and 95/97: two real calls, each paired with a `t.Fatalf("fabricengine.NewPairedForTest(%q, %q): %v", …)` whose format string contains the literal text the grep pattern matches.
  `warpforward_integration_test.go`'s own `t.Fatalf` uses the paren-less `"fabricengine.NewPairedForTest: %v"` and is therefore not double-counted — which is precisely why the grep-only figure looked internally consistent while being wrong, and is now stated in the plan so the next reader does not repeat it.
  Batch 8's `## Batch Scope` now gives nine call expressions (5 + 2 + 1 + 1), eight migrating and one staying, and says the count comes from reading each occurrence rather than from any grep.
  Card 50's "the four … calls" corrected to "the two … call expressions".
  Card 51 already said five and one and needed no change.
- `20260813-061803-plan-review-r4.md` — "[BLOCKING:scope] The lyxtest/fabrictest-prose sweep leaves several production/test files with stale references that card 69's zero-hit grep gates require to be gone": verified accurate, and the gap is wider than the finding reported.
  A full sweep found nine `.go` files carrying `lyxtest`/`fabrictest` prose that no card in the plan named at all (`internal/batcher/config_test.go`, `internal/fabricengine/destroy_test.go`, `internal/fabricengine/refusalof_test.go`, `internal/gitrepo/keyvalidation_test.go`, `internal/lyxcwd/anchor.go`, `internal/perchcli/cli_test.go`, `internal/perchcli/run_test.go`, `internal/weftname/weftname.go`, `internal/weftname/weftname_test.go`), plus `internal/lyxcwd/lyxcwd.go` and `internal/fabricengine/fabric_test.go`, which appeared only as `Context:` and so would never have been edited.
  Fixed at the mechanism rather than by patching card 69: batch 1 card 2's sweep is now a **bare-word** rewrite (`s{\blyxtest\b}{gitkit}g`) rather than an import-path-plus-selector one, and its `Edits:` list grew from 116 to 125 files to cover every file carrying the token.
  The card now states why the bare-word form is load-bearing (it is what makes card 69's zero-hit gate satisfiable) and adds a follow-up instruction for the three production files whose only change is prose: re-read each comment after the substitution, because a sentence saying `gitkit` builds synthetic hubs is factually wrong once `hubforge` builds real ones — rename the owner, correct the claim.
  Batch 2 card 12 gets the symmetric treatment for `fabrictest`: three more files added to `Edits:` (`destroy_test.go`, `refusalof_test.go`, `mutation_record_integration_test.go`), each carrying bare-word prose and no `fabrictest.` selector, and the card now requires `grep -rln '\bfabrictest\b' --include=*.go internal cmd` to come back empty before it is done.
- `20260813-061803-plan-review-r4.md` — "[NIT:consistency] Batch 8 card 52 miscounts its own batch's commit-bearing cards": corrected "twelve commit messages" to "ten" — batch 8 has 11 cards and card 52 itself carries `Commit: none`.
- `20260813-061803-plan-review-r4.md` — "[NIT:consistency] Batch 4's coverage-gap count is off by one": corrected "five call sites" to "six" in batch 4's `## Batch Tests` — cards 25 and 26 migrate 2 and 4 smoke-tagged sites respectively.

## Pushed Back

_None._
