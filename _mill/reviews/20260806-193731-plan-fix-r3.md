# Plan fix — round 3

Source review: `_mill/reviews/20260806-193058-plan-review-r3.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 4 NIT).

## Fixed

- r3 BLOCKING "Card 14 leaves fabricengine tests non-compiling" — verified: `fabric_test.go` is `package fabricengine_test` and calls `fabricengine.New` at `:25,44,66` plus `f.Warp`/`f.Weft` at `:70-74`.
  The hedge "only if the compiler forces it" was wrong — it always does.
  Added the file to card 14's `Edits:` with a mechanical retarget onto the `export_test.go` shim, leaving card 15's `Open`-based contract restatement as a behaviour-level follow-up on an already-compiling file.
- r3 BLOCKING "configsync comments make batch 07 red on first activation" — verified: `configsync.go` carries `warp`/`weft` comments at `:21,28,36-37,46,160`, and no card covered the file.
  Resolved by widening the carve-out rather than rewording: the discussion's justification for the string literals (they name on-disk legacy filenames the migration must read) applies verbatim to the comments documenting them, and a token scan cannot separate the two.
  Updated the overview's owner-set decision, card 26's owner map, and card 26's predicate sub-test to "string literals and comments, never identifiers".
  Added `configsync.go` to card 22 to reword the one genuine leak, `:28`'s illustrative example list, as a review obligation rather than a machine-enforced fix.
- r3 NIT "Card 24 has `Commit: none` but prescribes a commit" — accurate and self-contradictory as written, and "amending" also conflicts with commit-per-fix discipline.
  Card 24 now states a straggler becomes its own new commit with a message, and that `Commit: none` describes the expected no-diff case.
- r3 NIT "Shared Decision undercounts the deliberate double-visits" — verified: `webstercli/cli_test.go` is a fifth (card 8, card 23).
  Added to the enumerated list.
- r3 NIT "`builder-contract.md:167` weft mention not enumerated" — verified.
  Added `:167` to card 29's named line list.
- r3 NIT "`lyxcwd/anchor_test.go` missing from card 23's list" — verified at `:203` ("fabricengine's `hostLayoutFor`").
  Added the file to card 23, classified as an owner-symbol reference to keep spelled correctly, so card 24's gate has an owner rather than an unexplained hit.

## Pushed Back

_(none — every finding verified accurate and no fix caused demonstrable harm.)_

## Additional: deterministic symbol and vocabulary sweep

Not a review finding.
Three consecutive rounds produced blocking findings of one mechanically-checkable class (a file referencing a changed symbol, or carrying policed vocabulary, that no card listed), so that class was swept exhaustively rather than left to a fourth sampling pass.
Cross-checked every renamed/unexported symbol's references against the union of every card's `Edits:`/`Creates:`/`Moves:` targets: `fabricengine.New`, `Fabric.Warp`/`Weft`, `weftCommit`, `weftErr`, `WarpResetter`, `WarpBisector`, `ClassWeftReference`, `weftReferencePattern`, `CheckWeftPairing`, `CheckWeftSync`, the `"weftCommitted"` JSON key, `PartialCommitError`'s fields, `Healthy`, and `WeftWorktree`;
then swept all policed vocabulary across production `.go`/`.md`, all non-owner test files, and all repo prose.
Result: zero gaps in code and tests.

The sweep did surface one gap review had not: 20 repo-prose docs carrying the vocabulary, none classified by the plan.
Two are the same class the discussion already scoped in and were added to card 29 — `docs/reference/status-schema.md` (`:33` names loom preflight's "weft pairing in sync" precondition, which this task renames, so it would go factually stale) and `docs/reference/plan-format.md:212` ("commits per card to the **host** repo", builder-contract's twin).
The other eighteen are mechanism, historical, or on-disk-name docs;
they are enumerated and justified in the new `repo-prose sweep boundary` Shared Decision so a future reader sees they were classified, not missed.
