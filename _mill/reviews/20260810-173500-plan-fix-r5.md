# Plan fix — round 5

Source review: `_mill/reviews/20260810-173031-plan-review-r5.md` (holistic, APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260810-173031-plan-review-r5.md` — "[NIT:consistency] Batch 1's error-collapse site list omits pull.go and remove.go".
  Accurate: `pull.go`'s probe and both of `remove.go`'s carry the same two-form structure, and cards 2 and 3 already instruct the same collapse at each of them, so only the scope summary undercounted.
  Took the reviewer's second option rather than widening the list: the rule now states that the collapse applies to every migrated probe that distinguishes the two forms, and names the single exception (`prune.go`, whose probe deliberately has one failure path).
  An enumeration is what went stale in the first place, so the fix removes it rather than lengthening it.
- `20260810-173031-plan-review-r5.md` — "[NIT:design] `resolveBranchOwnership`'s `repoDir` parameter is unused by the only branch kind".
  Accurate: every predicate the one branch-shaped kind runs resolves from the Location it carries, so the parameter was dead.
  Dropped it rather than justifying it — a speculative parameter for a kind that does not exist is the weaker of the two options the reviewer offered — and added a sentence saying to add one back when a kind needs it.

## Pushed Back

_None._
Both findings verified accurate against the plan and the tree;
neither fix breaks existing functionality, conflicts with a documented decision, or destabilises other work.
