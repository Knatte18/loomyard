# Plan fix — round 4

Source review: `_mill/reviews/20260807-134948-plan-review-r4.md` (holistic, REQUEST_CHANGES, 0 BLOCKING + 1 NIT).

Round 4 returned zero BLOCKING findings, so this is the convergence round: the NIT is fixed under the default rule, the plan is approved, and no round 5 is run.

## Fixed

- r4 holistic — "[NIT] scoutcli card 29 misdescribes assert-no-callers' existing worktreeRoot resolution": confirmed against `internal/scoutcli/cli.go`.
  `assertNoCallersCommand` resolves its root inline inside an `if layout, resolveErr := lyxcwd.Resolve(cwd); resolveErr == nil { ... worktreeRoot = layout.WorktreePath() }` block, never calling the shared `resolveWorktreeRoot` helper the other three verbs use, and leaves `worktreeRoot` empty outside a hub rather than falling back to `filepath.Abs(dir)` — so card 29's blanket "each verb also needs `anchorRoot := resolveAnchorRoot(cwd, dir)` alongside its existing `worktreeRoot := resolveWorktreeRoot(cwd, dir)` line" described a line that does not exist in that fourth verb.

  Card 29 now scopes that instruction to the three `buildOptions`-driven verbs and describes `assertNoCallersCommand`'s actual inline shape verbatim, instructing the implementer to add `var anchorRoot string` beside `worktreeRoot` and set `anchorRoot = layout.AnchorPath()` **inside the same `if` block**, reusing the `layout` already in scope rather than calling `resolveAnchorRoot` a second time, then pass `AnchorRoot: anchorRoot` in the hand-built `baseOpts` literal.
  That satisfies the finding's requirement — the anchor is threaded in this verb too — while matching the verb's existing code shape instead of imposing the helper pattern on it.
  The card also now states explicitly that the verb's divergent inline resolution and its missing out-of-hub fallback are pre-existing and deliberately left alone, and names the consequence the fix prevents: `assert-no-callers` would otherwise keep resolving a pre-slice-9 daemon identity in a subpath-anchored repo while the other three verbs resolve the new one.

## Pushed Back

_None._
The finding was accurate.
The one deviation from its literal suggested fix — reusing the in-scope `layout` instead of calling `resolveAnchorRoot(cwd, dir)` — meets the same requirement with one fewer `lyxcwd.Resolve` call and is not a reduction in scope.

## Post-fix validation

`_plan_dag.validate` passes;
`_plan_validate.run` reports 0 errors.
`plan/00-overview.md`'s `approved:` flipped to `true`.
