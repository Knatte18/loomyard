# Plan fix — round 1

Source review: `_mill/reviews/20260906-104523-plan-review-r1.md`

## Fixed

- r1 "[BLOCKING:design] quarrycli root resolution breaks in standalone mode" — card 26 now branches on the returned `Mode` and never dereferences the location unconditionally, taking the root from `cwd` in standalone mode and from the location's worktree-path accessor in hub mode, citing `internal/webstercli/wiring.go`'s existing precedent; that file is added to the card's `Context:`.
- r1 "[BLOCKING:scope] canonicalizeCard's `lang` argument has no stated source inside ParsePlan" — card 5 now states that `lang` is derived from `fm.Language` before the cards loop, the same pattern `root` already uses, and that `planLanguage` is for the post-parse `validate.go` callers rather than for use inside the loop.
- r1 "[NIT:consistency] Plan.SurfaceRefs's type contradicts its own per-card keying rule" — `SurfaceRefs` is now `map[string]map[string]string`, keyed card identity first and canonical string second, with the reason for the two-level shape stated; card 12's consumption is rewritten to rewrite one card file at a time and look each key up in that card's own inner map, and both cards gain a test case for two cards spelling one canonical string differently.
- r1 "[NIT:scope] loom-plan-spec.md's deviation-union section is left stale" — card 8's spec rewrite scope now includes the `## Deferred / forward-compat` section, restating the deviation union over glyphs and naming the mechanical guard, while keeping that section's informational-never-blocking statement and its exclusion of `Uses:`.

## Pushed Back

_None. Every finding verified accurate against the cited source and none met a harm-check escape._
