# Discussion fix report — round 6

Source review: `_mill/reviews/20260817-092129-discussion-review-r6.md` (APPROVE, 0 blocking, 4 NIT, reviewer `fable` at high effort).
All four NITs verified accurate against the tree before fixing;
none met a pushback condition.

## Fixed

- r6 `[NIT:scope]` "Root pre-run stencil seeding keeps the tier-1-only trigger" — confirmed at `cmd/lyx/main.go:97` (`cobra.EnableTraverseRunHooks = true`), `main.go:87` (`seedStencils`) and `stencilseed.go:51-56` (bare `Resolve` trigger). T6 now gates `seedStencils` on the same tier-1-AND-tier-2 check, and the doc states plainly that this is a live defect today rather than an artefact of the standalone work.
- r6 `[NIT:design]` "Wired-but-broken hub silently degrades to standalone" — T6 now decides the `(resolved, hub-damaged)` row explicitly: `fabricengine.BoardDir(filepath.Dir(worktreeRoot))` discriminates a damaged hub from a plain git repo, and tier 2 failing with a `_board` present is a hard error naming `lyx fabric reconcile`, never a silent degrade.
- r6 `[NIT:consistency]` "`--stencils-dir` is only ever read vs bootstrap-on-first-use" — bootstrap scope pinned to the standalone default `<state>/_lyx/stencils` only; an explicitly-told directory is read, never written, in either mode, which makes the read-only rationale in the `--target-dir` ruling literally true.
- r6 `[NIT:consistency]` "Stencil Ownership reword scope misses the seed-pass bullet" — T6's `CONSTRAINTS.md` edit now names both falsified bullets, with the seed-pass bullet's load-bearing half (*never lazily inside `stencilstore.Read`*) called out as preserved.

## Pushed Back

None.
