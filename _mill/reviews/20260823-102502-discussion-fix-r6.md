# Discussion fix — round 6

Source review: `_mill/reviews/20260823-102250-discussion-review-r6.md` (REQUEST_CHANGES, 1 blocking, 3 NITs).

Round 6 is an operator-authorised extension past the configured cap of 5 (`roles.discussion-review.holistic.rounds`), run at Opus/high, with handoff directed to follow regardless of verdict.

## Fixed

- r6 BLOCKING "Stale/prunable parent entry has no disposition" — verified: `parseWorktreePorcelain` reads only `worktree`/`HEAD`/`branch`/`detached`/`bare` and silently drops `prunable`, so a deleted-but-unpruned worktree keeps a real branch value and matches.
  Added the decision `prunable-entries-are-skipped-and-resolve-failures-name-the-branch`, covering two distinct failure modes: a new `WorktreeEntry.Prunable` field parsed from the dropped porcelain line, with the matcher treating prunable as no-match;
  and an `OpenParent` error wrap naming the branch and path, covering the list→resolve TOCTOU the flag cannot.
  Extended the "no live pair" vs "pair exists but is broken" distinguishability rule to cover the new case, and added three scenarios to the integration-test list plus one to the parser's own coverage.
- r6 NIT "`Deps.PushBranch` field-doc correction unlisted" — the `deps.go` entry now names three corrections rather than two, explicitly including the "the layer that names it is the caller" phrase, with a note that the field doc's conclusion (the closure stays) must survive the rewording.
- r6 NIT "Package-doc list omits `Fabric.PushBranch`" — added, along with `WorktreeEntry.Prunable`, and noted that `PushBranch`'s doc comment must carry the vocabulary rationale rather than only its delegation target.
- r6 NIT "\"Why now\" understates the current breakage" — verified against `contracts/recipes/loom-recipe.yaml` (both rows live) and `entries_simple.go` (constructors called at build time).
  The Problem section now states that `lyx loom drive` fails at `loomrecipe.New` on every invocation today, rather than implying a run must first reach the landing rows.

## Pushed Back

None.
All four findings verified accurate against source;
none met the harm-check criteria for pushback.
