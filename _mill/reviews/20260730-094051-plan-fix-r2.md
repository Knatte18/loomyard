# Plan-fix round 2 — fabric-commit-api

Source review: `_mill/reviews/20260730-093159-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 5 NIT). Each finding was run through the `mill-receiving-review` decision tree; all fixes land only in files under `_mill/plan/`.

## Fixed

- [BLOCKING] "Context completeness gaps in several cards" — VERIFIED accurate: Requirements name symbols whose defining file was absent from Context/Edits (the `context-completeness` validator only catches path-shaped *tokens*, not bare symbol references, so it passed while the allowlist principle was still under-served). Fixed by adding the defining production files to each flagged card's Context: Card 3 (batch 1) +`internal/gitrepo/gitrepo.go` (StageAndCommit) +`internal/lock/lock.go` (AcquireWriteLock/Release); Card 5 (batch 2) +`internal/fabricengine/weftgit.go` (PushWeftAt); Card 7 (batch 3) +`internal/lock/lock.go`; Cards 8 and 9 (batch 3) +`internal/fabricengine/index.go` (WeftSHAForWarpSHA/RebuildIndex); Card 13 (batch 4) +`internal/fabricengine/weftgit.go` +`internal/fabricengine/syncweft.go` (CommitWeft/ensureWeftLockDir/SyncWeft).
- [NIT] "classifyPaths output path normalization unspecified" — VERIFIED accurate (round-1 edit specified both-sides `ToSlash` for the *decision* but not what the output slices carry). Fixed in `01-commit-foundations.md` Card 1: the output slices carry the original, unmodified input string; `ToSlash` is used only for the classification decision.
- [NIT] "Fabric.Commit spawns a push child even on a fully no-op call" — VERIFIED accurate against `03-fabric-commit.md` Card 7 (unconditional `spawnDetachedPushFn` after the commit step). Fixed: gated the push on `result.WarpCommitted || result.WeftCommitted` (Card 7), consistent with the decision's own "push whatever landed" language, and added a no-op-no-push assertion to Card 8. This breaks no planned test (every partial-failure/skip-git case that expects a push has `WarpCommitted` or `WeftCommitted` true; only the pure no-op is newly skipped).
- [NIT] "`.gitrepo-push.lock` is git-excluded only on the weft side" — VERIFIED accurate: `PushCoalesced` (`internal/gitrepo/push.go:135`) creates `.gitrepo-push.lock` in the repo's worktree root, and `lock.FileLock.Release` (`internal/lock/lock.go:65-70`) unlocks *without deleting*, so the file lingers; fabric seeds a weft-side `.git/info/exclude` but does not (and by ownership boundary should not) manage the host repo's git config. Fixed by documenting the warp/host-side asymmetry as a known characteristic in `05-docs.md` Card 14's doc.go pass, rather than seeding a host-repo exclude (which would violate fabric's "owns only the weft's git config" boundary).
- [NIT] "Shared Decision 4's 'no-ops ... no upstream' claim contradicts hasUnpushed() and Card 6's own text" — VERIFIED accurate against `internal/gitrepo/push.go:152-156` (`hasUnpushed` returns true with no upstream, so the push proceeds). Fixed in `00-overview.md` Shared Decision `async-push-both-sides-via-detached-child`: rationale reworded to "no-ops when nothing is ahead of upstream; performs a harmless tracking-establishing first push when no upstream is configured."
- [NIT] "SyncOptions.SkipGit field doc not updated for Fabric.Commit's narrower semantics" — VERIFIED accurate against `internal/fabricengine/fabric.go:85`. Fixed in `03-fabric-commit.md` Card 7: added `internal/fabricengine/fabric.go` to Edits with a requirement to add a one-line cross-reference on the `SyncOptions.SkipGit` field comment noting Fabric.Commit's weft-scoped narrowing (added `internal/fabricengine/fabric.go` to the overview's All Files Touched). This complements the round-1 doc.go narration in Card 14.

## Pushed Back

_None. Every finding was factually accurate and passed the harm check. The one finding with a plausible "faithful reporting is correct" counter-argument (`.gitrepo-push.lock` surfacing in warp Status) was still addressed — documented as a known asymmetry — rather than dismissed, per the default-fix rule._
