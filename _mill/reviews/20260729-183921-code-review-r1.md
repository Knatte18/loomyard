MILL_REVIEW_BEGIN
# Review: Give codeintel a persistent, session-long daemon — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-29
```

## Findings

### [NIT] ErrServerTimeout.Phase doc comment omits new "documentSymbol" phase
**Location:** `internal/codeintelengine/errors.go:129-133`
**Issue:** The doc comment states `Phase is one of "initialize", "references", or "workspace/symbol"`, but card 3 adds a `client.documentSymbol` call that reports `ErrServerTimeout{Phase: "documentSymbol"}` (and `"definition"` was already a possible phase pre-existing this task) — the enumerated list is now incomplete.
**Fix:** Extend the enumeration to include `"documentSymbol"` (and `"definition"`) or soften the wording to "e.g." rather than an exhaustive list.

### [NIT] Stale "batch N" references in unedited ancestor files
**Location:** `internal/codeintelengine/toolchain.go:6`, `ensureserver.go:142`, `daemonstate.go:8,71`, `refs.go:11,167`, `detect.go:3`
**Issue:** These comments cite batch numbers (5, 6, 7, 8, 2, 3) from an earlier plan numbering that no longer corresponds to this task's batches 1-5; pre-existing debt, not introduced by this task, but worth a future cleanup pass since it can mislead a reader cross-referencing against this plan's batch list.
**Fix:** Replace batch-number references with either the concrete file/function name or drop the numbering entirely (out of scope for this task's cards, no action required now).

## Verdict

APPROVE
Implementation closely tracks every card and Shared Decision across all five batches; only doc-staleness NITs found.
MILL_REVIEW_END
