# Discussion fix report — round 4

Source review: `_mill/reviews/20260826-062240-discussion-review-r4.md` (APPROVE, 0 blocking, 3 nits).

## Fixed

- `[NIT:scope]` "Defect-2 enumeration method is file-scoped and misses doc.go" — the defect-2 inventory's method now names `internal/shedadapters/doc.go` alongside `bouncer.go` and the recipe yaml, states why the package doc is in scope despite the replay path being private to `Bouncer.Call`, and adds a Rewrite row for `doc.go:42-48` ("Outcome mapping"). Verified the claim at that site: it restates both the four-mode branch and "A parsed APPROVED verdict maps to `Done`". The Scope In-list's docs bullet now names both `doc.go` sections, not just the round-artifact convention.
- `[NIT:consistency]` "Problem overstates defect 2's in-run reachability" — the Problem section now gives a per-segment reachability breakdown: `Plan-Review` in-run via `Plan-Revalidate`; `Discussion-Review` only cross-invocation or through the crash window, since nothing downstream of `Discussion-Bouncer` routes back; `Webster-Review` not reachable at all today. "Why now" was reworded to match. Added a Q&A log entry.
- `[NIT:consistency]` "Scope 'In' file list omits two inventoried edit sites" — `internal/loomcli/wiring.go` and `internal/shedrecipe/recipe.go` added as an explicit In bullet, plus a sentence stating once that the inventory tables extend the In list rather than restate it.

## Pushed Back

(none)
