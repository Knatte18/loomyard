# Plan fix — round 1

Source review: `20260819-182604-plan-review-r1.md`

## Fixed

- `[BLOCKING:scope]` "loom.md's 4-step bootstrap diagram is never updated" — added the diagram rewrite to batch 5 Card 23 (the run verb's own card, so it lands in the same commit as the behaviour per the Documentation Lifecycle) rather than to batch 2 Card 10, which keeps owning only the run-launcher paragraph; the requirement names the three pre-steps and the handshake the diagram must gain.
- `[BLOCKING:scope]` "loom-status-spec.md line 9 misattributes Shed.Run to the wrong verb" — added to batch 5 Card 25's enumerated corrections, phrased to re-attribute the status-shell-rewriting loop to the driver verb and to fix every occurrence of that claim, not only the first.
- `[NIT:consistency]` "batch 4's depends-on [1, 3] is not reflected by any of its cards" — verified accurate: batch 4 reaches none of batch 1's record functions and none of batch 3's new accessors or sentinel.
  Batch 4's `depends-on` is now `[]` and batch 5's is `[1, 2, 3, 4]`, with both batches' Scope prose stating the reason.
  The added edge on batch 2 is not from the finding: it is required by `parallel-modifies-overlap`, since batches 2 and 5 now both edit `manifest/designs/loom.md` and would otherwise be parallel-eligible on the same file.

## Pushed Back

_(none)_
