# Plan fix — round 2

Source review: `_mill/reviews/20260831-122434-plan-review-r2.md`

## Fixed

- `[BLOCKING:scope]` "Card 7 Context omits watchdog.go, source of tmuxQuoteValue" — added `internal/reedengine/watchdog.go` to card 7's `Context:` in `02-measurement-gate.md`, mirroring card 8, which already carries it.
- `[NIT:consistency]` "AttachArgv's own doc comment enumeration not updated for the new pre-flight step" — card 16 in `04-attach-multi-client-warning.md` now requires the documentation in two named places: the rationale on `warnMismatchedClientsLocked` itself, and an extension of `AttachArgv`'s own precondition enumeration so the file's index of the pre-flight names the client listing, with the note that the listing gates nothing and can never suppress the chain.
- `[NIT:scope]` "Card 9 cites hookInstalledLocked without reapply.go in Context" — added `internal/reedengine/reapply.go` to card 9's `Context:` in `02-measurement-gate.md`, keeping the named citation rather than dropping it.

## Pushed Back

_(none — every finding was verified accurate and none tripped the harm check.)_
