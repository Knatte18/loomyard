# Plan fix — round 1

Source review: `_mill/reviews/20260821-085552-plan-review-r1.md`.

## Fixed

- r1 `[BLOCKING:scope]` "Card 10's Context omits the landingshed files its own Requirements cite" — added `internal/landingshed/publish.go`, `internal/landingshed/finalize.go`, and `internal/landingshed/deps.go` to batch 3 card 10's `Context:`.
- r1 `[NIT:consistency]` "Publish/Finalize entries skip the error-text-prefix decision" — batch 3 card 7 now requires both entries to surface their constructor's error wrapped with the `shedrecipe: Publish: ` / `shedrecipe: Finalize: ` prefix, and card 10 now asserts that prefix in both failure-path subtests.
- r1 `[NIT:consistency]` "Card 24 mischaracterizes the roadmap's dependency phrasing" — batch 6 card 24 now names each stale reference separately: the loader/builder item's "Depends on the engine-registry item above" and the loom-convert item's "Depends on all three items above", the latter also carrying a now-wrong count.
- r1 `[NIT:consistency]` "Card 21's 'seven tests' count is already wrong before this task's edit" — batch 6 card 21 now forbids a mechanical increment and requires a recount, stating the correct total is ten once `internal/shedrecipe` joins the enumeration.

## Pushed Back

_None._
