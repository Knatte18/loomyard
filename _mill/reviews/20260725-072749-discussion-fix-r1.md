# Discussion-fix report — round 1

Source review: `_mill/reviews/20260725-072656-discussion-review-r1.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- [NOTE] "Input existence not pre-flighted" (r1) — added a "Pre-flight / cleanup is NOT this
  task's job" bullet to Technical context → shuttle contract, stating that verifying
  `decision-record.md` exists before spawning is the loom phase machine's job, not `PlanSpec`'s.
- [NOTE] "Re-run collision on shared PlanDir" (r1) — same new bullet documents that `_lyx/plan/`
  is shared with builder artifacts and shuttle `validate` rejects a pre-existing `00-overview.md`;
  stale-plan-dir cleanup is deferred to the phase machine, so the plan writer adds no stat/cleanup
  to `PlanSpec`.
- [NOTE] "LoadConfig godoc/struct scope" (r1) — extended the Scope `loom.yaml config` bullet to
  require refreshing `config.go`'s `Config` type godoc and `LoadConfig`'s grammar-validation
  comment to name the `plan` key in the same edit (same-commit docs rule).

## Pushed Back

_None._
