# Plan fix — round 2

Source review: `_mill/reviews/20260816-151504-plan-review-r2.md`

## Fixed

- `20260816-151504-plan-review-r2.md` — "[BLOCKING:scope] Card 1 Context omits the files its own Requirements name".
  Verified accurate: card 1's `Requirements:` asks for banner prose that states what `stencilstore.Read`, `stencilstore.Validate`, `stencil.StripLeadingComment`, and `stencil.Fill` each do, and none of the four defining files was on the card's `Context:` allowlist.
  Added `internal/stencilstore/reconcile.go`, `internal/stencilstore/stencilstore.go`, `internal/stencilstore/validate.go`, and `internal/stencil/stencil.go` to card 1's `Context:`.
- `20260816-151504-plan-review-r2.md` — "[NIT:consistency] Card 10 asks doc.go to remove wording it doesn't contain".
  Verified accurate against the file: `internal/pattern/doc.go`'s only "constant" mention is in its "Why the pointer stays relative" section, which the card's first instruction already covers;
  the returns-constants wording lives in `internal/pattern/pattern.go`'s header, which card 3 rewrites.
  Reworded card 10's second instruction to say so explicitly and to scope the header/opening-paragraph edit to accounting for the new read path rather than striking a claim that is not there.

## Pushed Back

_(none)_
