# Plan fix — round 2

Source review: `_mill/reviews/20260919-134053-plan-review-r2.md` (holistic, REQUEST_CHANGES, 4 BLOCKING + 4 NIT).

Every finding was verified against the real source before acting;
all eight were factually accurate and none triggered a HARM CHECK exception.

## Fixed

- r2 holistic, "Batch 4 breaks test compilation it never declares" — confirmed by grep that `internal/loomcli/cli_test.go` uses the `(*loomCLI).runCmd`/`stepCmd`/`statusCmd`/`pauseCmd` method expressions, `status_test.go` calls `renderStatusLine`/`statusUnavailableLine`/`printStatusLinesOnChange`/`c.statusCmd()`, `step_test.go` calls `stepEnvelope`/`stepKinds`/`stepKindBusy`/`c.stepCmd()`, and `internal/lifecyclecli/run_test.go` (five sites) plus `lifecycle_integration_test.go` (one) call `c.runCmd()`. Those files are now in cards 22, 23 and 26's `Edits:` with explicit retargeting requirements, and card 27's no-other-change rule carves the retargeting out as mechanical compilation repair. Where batch 3 card 18 already asserts the same closed set inside `shedverbs`, the cards allow deleting the duplicate rather than retargeting it, recorded in the commit message.
- r2 holistic, "Card 25 declares one Arm where card 21 proved two are needed" — card 25 now mirrors card 21's split exactly: an unexported `(c *lifecycleCLI) arm(...)` worker plus a thin exported `Arm` wrapper. Verified `wire` is a method and the hooks close over `c.env`/`c.shedPaths`/`c.abandonedSession`; the card now also states that the worker performs the `c.location`/`c.slug` assignments, which a package-level `Arm` would have silently dropped.
- r2 holistic, "Lifecycle's never-run-slug status cannot reach `found: false`" — verified in source: `lock.AcquireReadLock` is `flock.New(path).RLock()`, which creates the lock file but never its parent, and `state.ReadJSONStrict` adds no `MkdirAll`, so against a never-created `LifecycleDir` the read fails in lock acquisition before `found` is produced. Confirmed this is pre-existing, not introduced here — every existing `internal/lifecyclecli` test anchors its status paths in an already-existing `t.TempDir()`, so no shipped test covers the never-created case. Added the `lifecycle-absent-file-needs-an-existing-directory` Shared Decision recording that, and reworded card 27's `--watch` and `pause` cases to create the per-slug directory first. Card 18's assertion was rewritten to prove the told boolean both ways over a non-existent lock parent — created and read proceeding when true, still absent and the read failing in lock acquisition when false — which is satisfiable and tests exactly the boolean's effect.
- r2 holistic, "Batch 6 verify never runs the Markdown Link Integrity checker" — confirmed the checker is `internal/lyxcwd/docslink_test.go`. Batch 6's `verify:` now includes `./internal/lyxcwd/...` in both the batch file and the Batch Index, the file is named in card 39 and added to its `Context:`, and the manual locate-and-run step is gone — the links card 38 adds are now gated rather than merely asserted.
- r2 holistic, "ensureStatusLockDir arity differs between cards 13 and 15" (NIT) — card 15's call site now passes both arguments, matching card 13's `ensureStatusLockDir(prefix, statusLockPath)`.
- r2 holistic, "Batch 4 names smoke-tagged suites as its proof but never runs them" (NIT) — confirmed all four carry `//go:build smoke`. Dropped from the Batch Scope proof list, from card 28's `Context:` and from its confirmation list, with a stated reason: the smoke tier spawns real sessions and is not run per-batch anywhere in this pipeline. No `-tags smoke` run was added, since that would put session spawning inside every batch gate.
- r2 holistic, "The sixth hook PostStep has no Shared Decision" (NIT) — added the `poststep-is-a-sixth-hook` Shared Decision to `00-overview.md`, recording the deviation from the discussion's five-hook set, the `recordStepHandoff` placement rationale, and why `InterruptPolicyFor` cannot host it.
- r2 holistic, "lifecycleVerbTexts' step fields have no stated disposition" (NIT) — card 26 now states that lifecycle leaves the `step` texts at their zero values and never registers the returned `step` command, so the blank `Short` never reaches the live tree `cmd/lyx/drift_test.go` walks.

## Pushed Back

None.
