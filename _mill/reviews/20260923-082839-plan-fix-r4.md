# Plan fix — round 4

## Fixed

- `20260923-082808-plan-review-r4.md` — "Run.attached and RunState.Started docs go stale": card 1 now rewords `Run.attached`'s doc in `run.go` and `RunState.Started`'s doc in `rundir.go` (moved from Context to Edits) to name `AwaitStarted`.
- `20260923-082808-plan-review-r4.md` — "awaitStartedTickCap rationale says the probe 'spawns nothing'": card 1's required doc sentence now says each tick runs tmux through reed, so the count cap is the clause's required bound.

## Pushed Back

