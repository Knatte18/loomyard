# Plan fixer report — round 2

Source review: `_mill/reviews/20260802-121713-plan-review-r2.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 3 NIT). All five findings verified accurate → all FIXED, none pushed back.

## Fixed

- **[BLOCKING] Card 4 omits bolt.go from Edits despite mandating its edit** (`20260802-121713-plan-review-r2.md`) — Card 4's Requirements retarget `Bolt.Sync`/`Bolt.Commit`'s calls in `bolt.go` to the unexported names, so `bolt.go` must be an Edit target or the package fails to compile. Moved `internal/fabricengine/bolt.go` from Context to Edits in batch 1 card 4.
- **[BLOCKING] Card 11 omits bolt.go from Edits despite mandating its edit** (`20260802-121713-plan-review-r2.md`) — Same compile-break: card 11 retargets `Bolt.Push`'s `PushWeftAt` call in `bolt.go`. Moved `internal/fabricengine/bolt.go` from Context to Edits in batch 2 card 11.
- **[NIT] HostClean mention in drift.go never scheduled for repair** (`20260802-121713-plan-review-r2.md`) — Confirmed `drift.go:13` names both `PairInSync` and `HostClean` on one line. Card 17 already edits `drift.go` for the `PairInSync`→`Healthy` rename; extended card 17's Requirements to also fix the `HostClean`→`Clean` wording on that same header line (card 16's rename).
- **[NIT] Stale PairInSync mention in hubgeometry.go never scheduled for update** (`20260802-121713-plan-review-r2.md`) — Confirmed `hubgeometry.go:957` (`HostJunctionsHere`'s doc comment) names `PairInSync`. Added `internal/hubgeometry/hubgeometry.go` to card 17's Edits (comment-only one-line fix, explicitly noted as not touching geometry tokens so the Hub Geometry Invariant enforcement test is unaffected), widened the grep instruction to the `hubgeometry` package, and added the file to the overview's All Files Touched.
- **[NIT] StageAndCommit's own doc comment goes stale after force-add removal** (`20260802-121713-plan-review-r2.md`) — Confirmed `gitrepo.go:142/159-160` describe the `:(exclude)`/`-f`/`hasPathspecMagic` behaviour being deleted. Extended batch 3 card 14's Requirements to rewrite `StageAndCommit`'s doc comment to the plain `git add -- <files>` shape (golang-comments) with no reference to the removed magic/force-add path.

## Pushed Back

(none)
