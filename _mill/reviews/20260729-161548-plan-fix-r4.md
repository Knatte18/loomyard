# Plan-fix report — round 4

Source review: `_mill/reviews/20260729-161548-plan-review-r4.md` (reviewer opus · xhigh; verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT). Verification round on the R3-fixed plan; both findings verified accurate against source.

## Fixed

- [BLOCKING] "Card 12 test 3 will fail: weft not on suffixed branch" — verified `drift.go:69-72`: `PairInSync` checks weft-branch correspondence (`weftBranch == WeftBranchName(hostBranch)`) BEFORE the junction loop, and `lyxtest.CopyPairedLocal` leaves the weft prime on `main` (not `main-weft`), so `TestPairInSync_NarrowPathspecIsHealthy` as specified would get `(false, "host on main, weft on main (want main-weft)")` and fail under batch-3 `verify:`. Card 12 test 3 now requires `lyxtest.MustRun(t, fixture.WeftPrime, "git", "checkout", "-b", fabricengine.WeftBranchName("main"))` before wiring — the established pattern (`junction_pattern_integration_test.go:~400`, `add_rollback_adopt_test.go:56`, `hook_test.go:176`). Tests 1/2 (WireJunctions/UnwireJunctions only, no branch check) correctly need no such setup.
- [NIT] "Card 3 Context omits hubgeometry.go" — verified card 3 references `IsReservedHubName`/`HostJunctions`/`HostJunctionsHere` and needs the generic Link/Target composition, all defined in `internal/hubgeometry/hubgeometry.go`, which was in neither `Context:` nor `Edits:`. Added `internal/hubgeometry/hubgeometry.go` to card 3's `Context:`.

## Pushed Back

(none — both findings were factually accurate; applied under the fix-everything default.)
