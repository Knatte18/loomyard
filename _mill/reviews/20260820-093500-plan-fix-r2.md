# Fixer report — plan-fix round 2

Source review: `_mill/reviews/20260820-093249-plan-review-r2.md` (holistic, round 2).

## Fixed

- `[BLOCKING:scope]` "Batch 2 cards 9 and 11 omit shed.go despite naming its identifiers" — verified accurate.
  `RunDone` and `Result.HaltedProducer` are declared in `internal/shedengine/shed.go:31` and `:44`, and the `Producers` field on `Shed` at `:11`; cards 5, 6, 8, and 10 in the same batch already list that file for the identical need, so the two omissions were inconsistent within their own batch.
  Added `internal/shedengine/shed.go` to both cards' `Context:`.
- `[BLOCKING:scope]` "Batch 3 card 13 omits fixture_test.go despite naming testLandingDeps" — verified accurate.
  `testLandingDeps` is declared at `internal/loomshed/fixture_test.go:46`, and card 14 in the same batch already lists that file.
  Added `internal/loomshed/fixture_test.go` to card 13's `Context:`.

Beyond the two findings, the same defect class was swept across all twenty cards: every identifier named in a `Requirements:` block was checked against the declaring file's presence in that card's own `Context:`/`Edits:`.
One further instance was found and fixed — card 13 names the `*shedengine.Shed` value `New` returns, whose type is declared in `internal/shedengine/shed.go`, so that file was added to the same card's `Context:`.
No other card had a gap.

## Pushed Back

_None._
