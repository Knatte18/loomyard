# Discussion fix report — round 5

Source review: `_mill/reviews/20260829-104821-discussion-review-r5.md` (APPROVE, 4 NITs, 0 BLOCKING; 1 demoted from BLOCKING).

## Fixed

- `20260829-104821-discussion-review-r5.md` — "GitHub Auth Invariant never disposed of" (demoted from BLOCKING): a genuine miss — the earlier Constraints section dismissed `CONSTRAINTS.md` by a 13-item partial list, having read only its first 80 lines, and omitted the one invariant that names `gh` directly. The section is rewritten to quote the GitHub Auth Invariant (line 251) and dispose of it on three verified grounds: `cmd/lyx/ghguard_test.go` scans non-test `.go` files only (verified by reading its walk, which notably does **not** skip `plugins/`, so the scoping claim is about file type, not directory); a standalone plugin shell script in a separate nested module cannot import `internal/githubclient`, so the invariant's letter is unsatisfiable rather than merely inconvenient; and `github-repo-explorer/SKILL.md` already instructs direct `gh api` calls today, so this task narrows the `gh` surface rather than introducing it. Conclusion recorded explicitly, including that no `CONSTRAINTS.md` amendment is warranted and why a plan writer must not re-derive this silently. The invariant count is corrected from an implied 13 to the actual 44.
- `20260829-104821-discussion-review-r5.md` — "Fixture-reuse rationale contradicts the two-level fixture design": the one-fixture-serves-both-queries claim is dropped. The stub keys fixtures by full endpoint **including query string**, so `git/trees/{sha}` and `git/trees/{sha}?recursive=1` are necessarily distinct bodies — which is exactly what the truncated-fallback tests depend on, since a truncated recursive response and its complete non-recursive counterpart differ in both `truncated` and entry count.
- `20260829-104821-discussion-review-r5.md` — "Superseded \"without buffering\" clause in the Q&A log": the sort Q&A entry's rationale no longer claims tests avoid buffering. It now rests on determinism plus the fact that a `sort` would discard git's non-byte-lexicographic tree order, and carries an explicit pointer that the listing *is* buffered per the all-or-nothing decision.
- `20260829-104821-discussion-review-r5.md` — "SKILL.md rewrite does not say how `[path]` reaches the script": the SKILL.md decision now specifies that the rewritten block shows **both** invocation forms (whole-repo and scoped), and states the path-vs-question test: a second token is forwarded as `<path>` only when it matches the accepted `^[A-Za-z0-9._/-]+$` set and contains no whitespace; anything else is the `[question]` and is never forwarded. The disambiguation is the skill's judgement, made before invocation — the script takes two positional arguments and never sees a question. The same whitelist bounding the script's input is what makes the test unambiguous.

## Pushed Back

(none)
