# Discussion fixer report — round 5

Source review: `_mill/reviews/20260802-112834-discussion-review-r5.md` (verdict APPROVE, 1 NOTE).

## Fixed

- **[NOTE] Never-force-add enforcement left as "consider"** (`20260802-112834-discussion-review-r5.md`) — Revised the Constraints "Never force-add (NEW invariant)" bullet to decide the enforcement posture explicitly: the invariant is enforced *structurally* (the `hasPathspecMagic`/`-f` branch is removed, so no `-f` code path exists), and the plan additionally adds a machine-checked grep regression test (no `"-f"` in any `git add` argv under `internal/gitrepo`/`internal/fabricengine`) as a named CI guard — no longer worded "consider."

## Pushed Back

(none)
