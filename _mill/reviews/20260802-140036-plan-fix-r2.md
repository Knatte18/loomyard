# Plan fixer report — round 2

Source review: `_mill/reviews/20260802-135716-plan-review-r2.md` (holistic).

## Fixed

- `[BLOCKING] SourcePath token construction violates Planparser Sole-Parser Invariant` — verified accurate: `hubgeometry.PlanDir`'s doc comment says "Per the Hub Geometry Invariant, no other package may construct this path," and no relative accessor existed, so planparser hardcoding `path.Join(hubgeometry.LyxDirName, "plan", …)` constructed the `_lyx/plan` path itself. Fix: Card 1 now adds an exported `hubgeometry.PlanDirRel()` accessor (returns the relative `_lyx/plan` token), adds `internal/hubgeometry/hubgeometry.go` to Card 1's Edits, and has `parse.go` build `SourcePath` as `path.Join(hubgeometry.PlanDirRel(), cardFileName(...))` (no hardcoded `"plan"`/`"_lyx"`). Updated the `00-overview.md` Shared Decision, Card 1 batch scope/commit, batch-1 verify (adds `./internal/hubgeometry/...`), and All Files Touched. Consistent with the `card-source-identity-in-planparser` decision (planparser still owns the field + `NN-slug.md` filename; hubgeometry owns the `_lyx/plan` token) — no design conflict.
- `[NIT] Stale doc comments left outside the plan's edited-file set` — verified accurate: `beginbatch.go:66-69` claims `RenderForkPrompt` does a "PATTERN active check" (false after thinning) and `beginbatch.go` was in no Edits; `recoverbatch.go:54-55` `RecoverDeps.Plan` comment names `RenderForkPrompt`/"plan-level context" and Card 3's sweep only named the header + `recoverSpawn`. Fix: moved `beginbatch.go` from Card 3 Context to Edits with a bullet fixing the `BeginDeps.Layout` comment; broadened Card 3's recoverbatch sweep to cover EVERY stale `RenderForkPrompt`/"plan-level context" reference including `RecoverDeps.Plan` (lines ~54-55); added `beginbatch.go` to All Files Touched.
- `[NIT] Ambiguous test-merge instruction` — verified: the flip bullet did not state whether the two tests stay separate. Fix: Card 3's `template_test.go` bullet now states the two functions remain distinct (each keeping its own inverted focus, optionally renamed to `…OmitsSharedDecisions`/`…OmitsRenameMechanic`), and BOTH additionally assert the relative `SourcePath` pointer.

## Pushed Back

- (none)
