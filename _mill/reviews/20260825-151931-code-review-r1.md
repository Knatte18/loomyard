MILL_REVIEW_BEGIN
# Review: loom: Discussion-Burler Fabric Git Invariant fix — holistic

```yaml
duration_s: 214.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-25
```

## Findings

None.

Verified end-to-end against the plan:

- `contracts/recipes/loom-recipe.yaml`: `Discussion-Burler.profile.fix-scope` flipped to `overlay`, `Discussion-Bouncer.config.commit_seam: discussion` added; `run_subdir`, `artifact_paths`, `rubric_stencil`, `max_bounces`, `on_stuck` edges, target paths, `fasit`, `tool-use` all untouched. All five comment sites (Discussion-Burler fix-scope, Discussion-Bouncer commit_seam, Plan-Burler fix-scope, Webster-Burler fix-scope, Webster-Bouncer no-seam) rewritten exactly as card 1 step 4 specifies, cross-checked against the corresponding Plan-row comments they now mirror.
- `internal/loomrecipe/overlay_seam_guard_test.go`: `assertOverlayBurlerCommitSeams` correctly walks `BurlerRound` rows, derives overlay paths via `lyxdirs.LyxDirName` (never a literal), applies the structural-exemption skip for no-profile/no-target/no-paths rows, straddle-check-before-seam-lookup ordering matches spec, both `t.Errorf` halves report independently so a single run surfaces both failures, and `capturingT`'s non-halting `Fatalf` is safe because every malformed-shape branch already `continue`s before reaching the two helper functions that do naked type assertions on `Config["profile"]`. Confirmed against `yaml.v3`'s actual decode shape (`map[string]any`/`[]any` for nested config, per `shedbuild/parse.go`'s use of `gopkg.in/yaml.v3`) and against `FixScopeOverlay = "overlay"` in `internal/burlerengine/profile.go`. The seven synthetic cases (5 reject, 2 accept) match the card's required case list exactly.
- `internal/loomrecipe/sequence_test.go`: `commitDiscussionCalls != 2` with rewritten comment/message mirroring the neighbouring `commitPlanCalls != 2` block's shape, as required; `wantSequenceOrder`, the three-round/three-judge-call assertions, and all else are unchanged.
- `internal/loomcli/wiring.go` / `internal/shedadapters/bouncer_commit_test.go` (card 2): both are comment-only edits, verified no code/assertion changes; `CommitDiscussion`'s doc comment now states the two-caller idempotence mirroring `CommitPlan`'s; `TestBouncer_Commit_NilIsNotAnError`'s doc now correctly retargets to `Webster-Bouncer`.
- `manifest/designs/loom.md` "The gate" section: the black-box sentence rewritten (not supplemented) to add fix-scope and commit-seam as per-phase axes alongside profile, black-box claim intact; new "Which segment fixes what, and commits how" paragraph added, citing the Fabric Git Invariant, kept to the cross-segment picture with no per-row duplication. Semantic line breaks correctly applied (comma+and and semicolon boundaries broken; the one "while"-joined clause left on one line is consistent with CLAUDE.md's rule, since "while" is not a listed coordinating conjunction).
- `manifest/roadmap.md`: Planned item split as required — a Done entry (new content, one sentence, correctly formatted) with a `See` line matching the two existing Done entries' shape, and a Planned item retaining the two folded defects with dropped first paragraph; all list markers are literal `1.`.
- No out-of-plan files found; the union in `00-overview.md`'s "All Files Touched" matches the delivered file set exactly. `internal/shedrecipe/entries_bouncer.go`, `entries_burler.go`, `internal/burlerengine/prompt.go`, and `internal/loomrecipe/shape_test.go` are unmodified, as card 1 step 6 requires — cross-checked their actual field/config shapes (`shedbuild.Row.Config map[string]any`, `burlerRoundProfile`'s `target`/`fasit`/`fix-scope` key handling) against the new guard's assumptions and found them consistent.

## Verdict

APPROVE
Implementation matches the plan precisely across all three cards; no design, scope, decision, or consistency defects found.
MILL_REVIEW_END
