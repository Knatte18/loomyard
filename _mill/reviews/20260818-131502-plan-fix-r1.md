# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260818-131502-plan-review-r1.md`

## Fixed

- `20260818-131502-plan-review-r1.md` — "[BLOCKING:scope] Card 10's closing grep gate undercounts its own survivor set".
  Verified accurate: `grep -rn "Location" internal/scoutengine/*.go` excluding `lyxcwd` returns 49 hits, all of them the package's own LSP wire-type family (`lspLocation`, `formatLocation`, and the `Location` field on LSP result structs), none of which this task touches and none of which the card's survivor list named.
  Narrowed card 10's gate pattern from `grep -rn "layout\|Location" ...` to `grep -rnE "lyxcwd\.Location|\blayout\b" ...`, and added an explicit paragraph stating why the pattern is narrow, that the LSP wire types are out of scope, and that the pattern must not be widened back to bare `Location`.
  The file enumeration the reviewer's finding used was deliberately not reproduced verbatim in the card: naming those seven source files in `Requirements:` would pull six of them into the card's `Context:` allowlist for a zero-diff gate that never reads them, so the paragraph names the LSP symbol family instead.

## Pushed Back

_(none — the single finding was accurate and the fix carries no harm.)_
