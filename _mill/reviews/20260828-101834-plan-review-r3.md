MILL_REVIEW_BEGIN
# Review: reed: watchdog daemon — holistic

```yaml
duration_s: 397.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-28
```

## Findings

### [BLOCKING:scope] Card 20's Context omits the files its literal fixture needs
**Location:** batch 4 / card 20 (tier-1 tests for the header tail) **Issue:** Requirements prescribes `&reedCLI{eng: reedengine.New(reedengine.Config{}, reedengine.Geometry{RepoName: "test-repo", HubPath: t.TempDir()})}` and cites `Config.Header.Template`'s fallback and `tokenvocab.Ctx`'s field consumption, but `Context:` lists only `header.go, cli.go, cli_test.go, logger.go, CONSTRAINTS.md` — `reedengine.New`/`Engine` (lock.go), `Config`/`HeaderConfig` (config.go), `Geometry` (geometry.go), and `tokenvocab.Ctx` are all outside it. **Fix:** Add `internal/reedengine/lock.go`, `internal/reedengine/config.go`, and `internal/reedengine/geometry.go` to card 20's `Context:` (compare card 5, which correctly lists `lock.go`/`geometry.go` for the identical `New`/`Geometry` fixture pattern).

## Verdict

REQUEST_CHANGES
One Context-completeness gap in card 20; everything else checked against source verifies accurate.
MILL_REVIEW_END
