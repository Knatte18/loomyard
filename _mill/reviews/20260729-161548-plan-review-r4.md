MILL_REVIEW_BEGIN
# Review: fabric: config-driven junction list — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude (Anthropic), Opus-class model; exact build not verifiable from inside the sandbox
reviewed_file: plan/
date: 2026-07-29
```

## Findings

### [BLOCKING] Card 12 test 3 will fail: weft not on suffixed branch
**Location:** Batch 3 / Card 12 (`TestPairInSync_NarrowPathspecIsHealthy`)
**Issue:** `PairInSync` checks branch correspondence (`weftBranch == WeftBranchName(hostBranch)`) BEFORE any junction check (drift.go:69-72); raw `CopyPairedLocal` leaves both host and weft prime on `main` (buildWeftPrime→initRepo), so `PairInSync(l)` returns `(false, "host on main, weft on main (want main-weft)")` and the asserted `ok == true` never holds — the test fails under batch-3 `verify:`. Tests 1/2 in the same card need no such setup, so the single "mirror the fixture pattern" instruction does not cleanly cover test 3.
**Fix:** Have card 12 test 3 also run `git checkout -b fabricengine.WeftBranchName("main")` on the weft prime (as `TestPairInSync_JunctionDriftShapes:404` / `newFabricFixture` do) or reuse `newFabricFixture`.

### [NIT] Card 3 Context omits hubgeometry.go
**Location:** Batch 1 / Card 3
**Issue:** Requirements name `hubgeometry.IsReservedHubName`/`HostJunctions`/`HostJunctionsHere` (defined in `internal/hubgeometry/hubgeometry.go`), and the new 3-name assertion needs the generic Link/Target composition (`filepath.Join(WorktreePath(slug), RelPath, name)` etc.) that is only stated in card 2 / the source, not restated here; that file is in neither `Context:` nor `Edits:`.
**Fix:** Add `internal/hubgeometry/hubgeometry.go` to card 3's `Context:`.

## Verdict

REQUEST_CHANGES
Plan is sound and constraint-clean; one proof test as specified fails verify, plus a minor Context gap.
MILL_REVIEW_END
