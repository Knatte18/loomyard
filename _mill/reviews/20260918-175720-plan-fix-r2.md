# Plan fix — round 2

Source review: `_mill/reviews/20260918-174948-plan-review-r2.md`

## Fixed

- `20260918-174948-plan-review-r2.md` — "[BLOCKING:scope] batch 6 misses an existing reedUp call site that will fail to compile": added `internal/webstercli/cli_test.go` to card 43's `Edits:` and a requirement retargeting its `c.reedUp = func() error { … }` assignment in `TestRecoverBatchCmd_BootsStandaloneReedSessionFirst` to the widened signature, extending that test's existing one-bring-up assertion to also assert it received `watch: false`. Verified accurate against source — `grep -rn "c\.reedUp = " internal/` returns exactly three sites, and `internal/webstercli/cli_test.go:620` was the one no card covered.
- `20260918-174948-plan-review-r2.md` — "[NIT:consistency] geometry.go's leading comment keeps a stale file reference after the rename": added the fix to card 12 (the card that performs the rename) rather than card 8, and moved `internal/reedengine/geometry.go` from card 12's `Context:` to its `Edits:` so the card owns the file it now writes. Verified accurate — `internal/reedengine/geometry.go:2-3` names `header.go` in its file list.
- `20260918-174948-plan-review-r2.md` — "[NIT:consistency] batch 04's own scope text undercounts the set-option calls it specifies": normalized every count to **seven** across `04-status-line-pins.md` (Batch Scope and Batch Tests) and `00-overview.md` (both Windows Shared Decisions, which carried the discussion's own stale count of four), and `07-docs-smokes-and-residue.md`'s card 44. Also tightened a related imprecision the finding surfaced: card 24 and card 26 said a `StatusLineText` error skips "the four text-carrying options", but only `status-left` and `status-left-length` derive from the text, so both now say those two are skipped while the other five are still issued.

## Pushed Back

_None._
