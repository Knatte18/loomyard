# Plan fix — round 6

Source review: `_mill/reviews/20260811-163518-plan-review-r6.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 6 NIT).

## Fixed

- `20260811-163518-plan-review-r6.md` — "[BLOCKING:scope] Exclude-file target is not in scope at card 17's recording site".
  Verified: `resolveGitExcludePath` runs inside `mutateGitExclude` and its result never leaves the function;
  `seedGitExclude` discards the `changed` bool with `_`.
  Card 17 now widens `mutateGitExclude`'s return to `(excludePath string, changed bool, err error)` and updates its three in-package callers, and requires `seedGitExclude` to capture `changed`.
  Chose the widening over the in-scope alternative (worktree path as `Target`, exclude file in `Detail`) and said why: the alternative puts a `Target` on the record that is not the thing written.
  `internal/fabricengine/gitexclude.go` moved from card 17's `Context:` to its `Edits:`.
- `20260811-163518-plan-review-r6.md` — "[BLOCKING:design] Card 21's launcher entries contradict record-only-on-observed-effect".
  Verified: `os.MkdirAll` succeeds on an existing directory, the menu launcher returns early when present, and `removeLaunchers` leaves the menu in place so the repair path always hits that early return.
  Worse than reported, and fixed too: the two scripts inside the directory are rewritten byte-identically on the repair path, so an unconditional entry for them fails commission as well.
  Card 21 now states a predicate per entry — stat-before-`MkdirAll` for the directory, a read-and-compare for each script, and the menu entry only on the branch that reaches its `os.WriteFile` — and names itself the constructive-side instance of the record-only-on-observed-effect Shared Decision: "on success" is not the predicate, "the filesystem changed" is.
- `20260811-163518-plan-review-r6.md` — "[NIT:consistency] Card 22's `res.Mutated().Entries()` cannot compile".
  Verified.
  Fixed at the root rather than per call site: card 1 now splits receivers explicitly — pointer and nil-safe for `Append`/`AppendRef`/`Extend`/`Snapshot` (the recorder-pointer methods), value for `Entries`/`Len`/`MarshalJSON` (the result-value methods) — so `res.Mutated().Entries()` and `res.Mutated().Len()` both compile.
  This also retires round 1's "`Len()` cannot be both value-receiver and nil-safe" finding properly: `Len` never needed nil-safety, only `Snapshot` did.
  Card 7's nil-safety list and card 2's test list updated to match.
- `20260811-163518-plan-review-r6.md` — "[NIT:consistency] Card 27's reserved-key assertion is unsatisfiable for `okWithRecord`".
  Verified: `output.Ok` injects only `ok` and never touches `error`.
  Card 27 now asserts reserved keys per helper — `ok`/`mutations`/`partial` for `okWithRecord`, plus `error` for `errWithRecord` only.
- `20260811-163518-plan-review-r6.md` — "[NIT:decision] `runReconcile`'s error returns have no stated disposition".
  Verified, including that card 24's carve-out justification is false for this handler because `ReconcileFabricAt` runs before `LoadConfig`.
  The carve-out is restated as "before any CLI-layer mutation" rather than "before the verb call", card 25 enumerates all three `runReconcile` returns as `errWithRecord`, and `runCloneWithReset`'s post-`CloneAndWire` return is assigned too, with its two genuinely pre-mutation returns left bare.
- `20260811-163518-plan-review-r6.md` — "[NIT:scope] Card 21 names helpers whose files are absent from Context, and ignores a conditional write".
  Verified.
  `internal/fabricengine/boardweft.go` and `internal/fabricengine/warpbinding.go` added to card 21's `Context:`, and the `.lyx-anchor` entry qualified to the create branch (the adopt branch writes no marker), with `writeWarpBinding`'s entry likewise qualified to the `writeRecord` branch.
- `20260811-163518-plan-review-r6.md` — "[NIT:scope] Card 25's "import already established" premise is wrong".
  Verified.
  The false premise is gone, the `configengine` import is stated as new for both production files, and `internal/fabricengine/junctionnames.go` added to `Context:` for `BoardDir`.
- `20260811-163518-plan-review-r6.md` — "[NIT:consistency] Card 10 prescribes a recorder placement card 14 immediately rewrites".
  Verified.
  Card 10 now places the assignment at its final position — after each `hubPath = HubPath(cwd, name)`, ahead of that branch's `if opts.Reset` — and card 14 relies on it rather than moving it, with a note that a nil recorder at `resetHub` means card 10 was implemented wrongly.

## Pushed Back

_None._
Every finding was factually accurate and confirmed against the source before fixing.
