# Discussion fix — round 4

Source review: `_mill/reviews/20260828-055654-discussion-review-r4.md` (APPROVE, 0 BLOCKING, 4 NIT — 2 demoted from BLOCKING by the stage's `blocking_classes` ceiling).

All four NITs verified against source before fixing, per `mill-receiving-review`.
PUSH BACK is unavailable under `--orch`; none was warranted in any case.

## Fixed

- r4 `[NIT:scope]` "mergeresolve import allowlist edit missing" — verified `internal/mergeresolve/seam_enforcement_test.go:27-33` is a strict membership allowlist holding only `fabricengine`, `shuttleengine`, `modelspec`, `stencilstore`, `stencil`. Added the allowlist entry as an explicit Scope In deliverable and recorded the reasoning as a new `mergeresolve-allowlist` decision, which states why this is the opposite call from `gitkit-leaf`/`githubclient-leaf`: a Told-Geometry *seam* list versus a *minimality* leaf. CONSTRAINTS.md's Treadle Runner-Seam allowlist, which already admits `logger`, is the in-repo precedent.
- r4 `[NIT:consistency]` "Amended invariant text is violated on landing" — verified `internal/hubforge/hub.go:371`, `cmd/testtiming/main.go`, and seven `tools/` spawns are real, unlogged, and not structurally barred, so the exemption bullet did not reach them and "every production code path" convicted them. Rewrote the replacement text to key on "reachable from a `lyx` command" and added an out-of-scope bullet for fixture machinery, standalone harnesses, and `tools/`. Also stated the deliberate asymmetry between *barred* (inside the rule, owes a written reason) and *out of scope* (never governed, owes nothing).
- r4 `[NIT:consistency]` "Hard-error selector filters comments only in `doc.go`" — verified comment matches outside `doc.go` at `shedadapters/burler.go:38` and `burlerengine/engine.go:47,66,89`, plus a non-comparison `string(shuttleengine.OutcomeDone)` at `treadleengine/run.go:218`. Replaced the grep-plus-prose-exception form with an AST selector (`*ast.SwitchStmt` on an outcome-valued tag, or `*ast.BinaryExpr` with `==`/`!=` on a `shuttleengine.Outcome*` selector), matching the spawn selector r3 already made AST-based.
- r4 `[NIT:consistency]` "Yield count stated as nine and ten" — the r2 Q&A entry said "nine-site yield" while `error-universe` says ten with ten rows. Dropped the count from the Q&A entry rather than restating it, since the tables are already declared non-authoritative snapshots.

## Pushed Back

None.
