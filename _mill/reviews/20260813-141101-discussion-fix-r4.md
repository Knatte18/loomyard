# Discussion fix report — round 4

Source review: `_mill/reviews/20260813-140930-discussion-review-r4.md` (APPROVE, 0 BLOCKING, 4 NIT).
Verdict was APPROVE but the convergence gate did not pass — two findings carry `Demoted-from: BLOCKING`, so round 5 follows.

## Fixed

- `20260813-140930-discussion-review-r4.md` — "Scope carries pre-r3 gitrepo counts": Scope bullet restated as 19 checked / 2 raw with a pinned count of 3; the Testing Tier 2 bullet updated to 19 and given the `HasUnpushed` regression note; both stale Q&A figures marked superseded rather than left as second live numbers.
- `20260813-140930-discussion-review-r4.md` — "`isWeftWorktree` does not exist in the tree": verified by grep over `internal/fabricengine` (zero occurrences) and by reading `weftwiring.go`, which declares `weftRepoExists` (`rev-parse --is-inside-work-tree`) and `weftBranchExists`. Replaced at all three sites in `discussion.md` and both probes now named explicitly where the `internal/fabricengine` pinned count of 2 is stated.
- `20260813-140930-discussion-review-r4.md` — "prune.go's fallback message deferred to a closed list": `prior-call-diagnostic-exception` widened from two live instances to three, adding `prune.go`'s fallback-cleanup-also-failed message and its sibling, and stating that both fill their `%d`/`%s` from the `*GitError` that shape (D) has already recovered via `errors.As` in order to reach the fallback branch at all.
- `20260813-140930-discussion-review-r4.md` — "Pinned-map semantics for an unlisted package": stated that a missing map key means pinned zero and that a raw site in an unlisted package is a guard failure with a message naming the package, so a brand-new package reaching for `RunGit` is caught rather than silently passing.

## Pushed Back

(none — all four findings verified accurate against the code, and no fix was in conflict with existing functionality, a documented design decision, or work outside this task's scope)
