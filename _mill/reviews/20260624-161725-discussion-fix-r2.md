# Discussion-fix report — round 2

Source review: `20260624-161634-discussion-review-r2.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- `[NOTE] Board-URL rewrite undefined for non-`.git` weft URL` — defined the derivation
  rule in `board-url-derivation-default-weft-wiki`: strip a single trailing `.git` if
  present, then append `.wiki.git`, so both `…/<repo>.git` and `…/<repo>` yield
  `…/<repo>.wiki.git`. Added a matching test scenario.
- `[NOTE] Teardown-failure path not addressed` — added a teardown-failure clause to
  `board-is-essential-strict-abort`: on `os.RemoveAll` failure, report the residual Hub path
  and exit non-zero rather than masking it. Added a matching test scenario.
- `[NOTE] proposal-weft-repo.md not readable from this worktree` — updated Technical
  context → Reference to state the proposal lives in the daemon-owned wiki (absent from the
  worktree) and is background-only; every decision needed to plan is captured in
  `discussion.md`.

## Pushed Back

(none — all three NOTEs were accurate and fixed.)
