# Plan-fix report — round 3

Source review: `_mill/reviews/20260729-154526-plan-review-r3.md` (reviewer sonnet · xhigh; verdict REQUEST_CHANGES, 2 BLOCKING + 2 NIT). Final round of the operator's three-reviewer benchmark. All findings verified accurate against source; none warranted pushback.

## Fixed

- [BLOCKING] "Card 10 missing config.go in Context" — verified `Config.Dirs()` is defined in `internal/fabricengine/config.go`, absent from Card 10's `Context:`/`Edits:`. Added `internal/fabricengine/config.go` to Card 10's `Context:` so the implementer can read `Dirs()`'s whitespace-split behavior.
- [BLOCKING] "New PairInSync config-load reason bypasses preflight's check3BlocksSeed classification" — verified against `preflight.go:125-148`: the check-3 classifier sets `check3BlocksSeed=true` only when `strings.Contains(reason, "junction")`, and its own godoc (`:125-131`) mandates any reworded PairInSync reason keep the `"junction"` substring or classification "silently reverts to CheckWeftSync" (splitting one root cause into a phantom `CheckSeedMissing`). Card 8's config-load reason changed from `"cannot load fabric.yaml: <err>"` to `"host junction check unavailable: cannot load fabric.yaml: <err>"` — contains `"junction"` (correct classification, seed correctly blocked) AND names the config fault. Card 9's `checkJunctionHealth` reason updated to the identical wording (documented twins). This keeps `loomengine/preflight.go` untouched (honoring the discussion's loomengine-untouched decision) rather than adding a classifier case. Overview no-fallback Shared Decision updated to record the wording and the preflight substring contract.
- [NIT] "Card 12 test (3) names an unexported function from an external test package" — verified the new file is `package fabricengine_test` (external) and `checkJunctionHealth` is unexported. Card 12 test (3) reworded to assert health only through exported surfaces (`PairInSync`, `Topology.Status`/`Reconcile`); the direct `checkJunctionHealth` call was removed.
- [NIT] "weftwiring.go needs a new path/filepath import for Card 7" — verified `weftwiring.go`'s imports are `errors, fmt, os, fslink, gitexec, hubgeometry` (no `path/filepath`). Card 7 now states the implementer must add the `"path/filepath"` import for its new `filepath.Join(...)` call.

## Pushed Back

(none — all four findings were factually accurate; applied under the fix-everything default.)
