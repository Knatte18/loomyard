MILL_REVIEW_BEGIN
# Review: Launch ly-supervise and orchestrator via lyx reed add — holistic

```yaml
duration_s: 319.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (per system-provided model ID; self-assessed)
reviewed_file: plan/
date: 2026-09-18
```

## Findings

### [BLOCKING:scope] Card 6's WriteConfig signature change leaves a third call site unaddressed
**Location:** Batch 2 / Card 6
**Issue:** `WriteConfig` changes from `(worktreeDir, relpath, slug, color string)` to a 6-arg signature (`+lyxPath, claudePath`). `internal/vscode/config_test.go` has three existing positional call sites (verified: lines 21, 128, 169 — `TestWriteVSCodeConfigCreatesFilesWhenAbsent`, `TestWriteVSCodeConfigDoesNotClobber`, `TestWriteVSCodeConfigRegistersInGitignore`). Card 6's Requirements name only the first two by function name; `TestWriteVSCodeConfigRegistersInGitignore` is never mentioned, so an implementer following the card literally can leave its call on the old 4-arg signature, breaking `go build`/`go test ./internal/vscode/...` — batch 2's own `verify:`.
**Fix:** Add one sentence to Card 6's Requirements naming `TestWriteVSCodeConfigRegistersInGitignore` and instructing its call site be updated to the new signature (its own assertions are unrelated to the two new params and stay unchanged).

### [NIT:scope] Card 2's engine-level no-mutation test only spells out the alive-no-op branch
**Location:** Batch 1 / Card 2
**Issue:** Requirements say to "drive the no-op branches [plural] through the engine helpers so the no-mutation guarantee is pinned," but the only concrete assertions given (`Display` untouched despite `Focus: true`; `Cmd`/`ResumeCmd`/`Parent` as persisted) are described solely against "a no-op against an alive candidate" (`ifAbsentNoOpAlive`). `ifAbsentNoOpHidden`'s no-mutation behavior at the engine-call level (as opposed to the pure classifier, which Card 1 does cover per-row) is left unspecified.
**Fix:** Repeat the same no-mutation assertion list for the hidden-strand no-op case, or state explicitly that one shared case covers both branches.

## Verdict

REQUEST_CHANGES
Card 6's WriteConfig signature change leaves one existing test call site unaddressed, breaking batch 2's own verify.
MILL_REVIEW_END
