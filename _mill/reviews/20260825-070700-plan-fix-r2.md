# Fixer report: plan-fix round 2

Source review: `20260825-070400-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 3 NIT).

## Fixed

- `[BLOCKING:scope]` "Card 12 Context omits webstergeom.go; its claim is also wrong" — verified both halves.
  `WebsterGeometry` lives in `internal/hubgeom/webstergeom.go`, which card 12 did not list.
  And the claim was wrong: `BurlerGeometry`'s doc comment in `internal/hubgeom/hubgeom.go` makes no comparison at all, while `WebsterGeometry`'s documents its divergence against `ReedGeometry`, not against `BurlerGeometry`.
  The claim was inherited verbatim from `_mill/discussion.md` and is a defect there too.
  Card 12 now lists `internal/hubgeom/webstergeom.go` in `Context:`, tells the implementer to read the divergence off each function's body rather than hunt for a comment pairing the two, and states outright that neither doc comment names the other.
  The substantive instruction is unchanged and is now correctly grounded: `WebsterGeometry`'s own stated reason for anchoring on `AnchorPath()` — converging it with the neighbouring `ReedGeometry` would silently change behaviour in a subpath-anchored hub — is what makes converging Burler onto it wrong too.
- `[NIT:consistency]` "Card 6 doesn't update burlerRoundProfile's stale key-count doc comment" — verified: `burlerRoundProfile`'s doc comment in `internal/shedrecipe/entries_burler.go` says "recognising exactly six keys" and enumerates them, which the seventh makes false.
  Card 6 now requires updating that comment to seven keys, adding `rubric_stencil` to the enumeration, and recording the mutual-exclusivity rule there, since that comment already explains which burler-profile keys are deliberately absent and why.
- `[NIT:consistency]` "Card 12 miscounts the fields it fills as six" — verified: the sentence said six and enumerated eight.
  Corrected to eight;
  the enumeration itself was already complete and needed no change.
- `[NIT:consistency]` "Card 22 leaves a two producers not yet built claim stale" — verified at `manifest/designs/loom.md`: the discussion-detail section's introductory sentence calls `Discussion-Validate` and `Discussion-Review` "two producers not yet built", and `Discussion-Validate` was already real before this task started.
  Card 22 now requires dropping or rewriting the clause, with an explicit instruction that the sentence's actual point — a validator checklist and a review rubric are not what the writing agent needs to read — must survive the edit.

## Pushed Back

_(none — all four findings were factually accurate, and no fix broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task's scope.)_
