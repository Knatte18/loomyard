MILL_REVIEW_BEGIN
# Review: Audit and overhaul engine test suites — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-01
```

## Findings

None. All three batches match their cards, the shared decisions apply consistently, and no out-of-plan files are present.

Verified specifically:
- `internal/githubclient/token.go:64-68` — `ghAuthTokenTimeout` is now `var`, value unchanged (`5 * time.Second`), doc comment appended per Card 1, `resolveToken()` (line 124) untouched otherwise.
- `internal/githubclient/githubclient_test.go:524-562` — save/override(`10ms`)/`t.Cleanup`-restore seam added mirroring `withFakeGHAuthToken`'s shape; `slack` shrunk to `200 * time.Millisecond` with the rationale comment the batch-local decision called for; all other assertions (`errors.Is`, lower-bound check, fake body, cache/env setup) untouched.
- `internal/webstercli/verbs_test.go:411-413` — `NoReport_WindowElapses` now passes `--wait 1ns`, matching the file's existing `recoverBatchCmd` convention (lines 548, 582); `ReportPresent` subtest (422-435) correctly left unchanged.
- `internal/webstercli/verbs_test.go:400-407` — doc comment's stale `PollWaitS = 1s` claim replaced with an accurate description of the `--wait` flag vs. `websterengine.DefaultAwaitWaitS`; statelessness/two-envelope content preserved.
- `internal/webstercli/verbs_test.go:452-530` — `TestRecordBatchCmd_Envelope` table-drives the two original scenarios with the exact fixture setup, table fields (`writeReport`/`wantSubstrs`/`wantTerminal`/`wantDigestSet`), and the per-row-conditional `head_sha` check the plan specified; both original functions (`DigestEnvelope`/`NoReportEnvelope`) are fully removed, no leftover duplicate.
- `docs/benchmarks/test-suite-timing.md:117-138` — new `### 2026-08-01 — timeout/window seams shrunk` block sits immediately above the `githubclient + webstercli now the floor` block it supersedes (newest-first, no heading collision), reuses the same-machine Machine/Method lines, and its Cause section explicitly attributes the drop to both fixes with matching per-package deltas. The residual gap vs. the 2026-07-13 pre-regression floor (~1.03s/~4.97s vs. measured ~3.86s/~6.48s) is investigated and explained (new `internal/scoutengine` module plus pre-existing `cmd/lyx`/`buildercli` real-time tests), satisfying Card 4's "investigate before writing" requirement rather than silently recording a mismatched number. All earlier blocks are left frozen/untouched (append-only preserved).
- No product-code behavior changed outside the `const`→`var` declaration-kind change; `internal/githubclient`'s leaf-import allowlist (GitHub Auth Invariant) and the Test Tier Purity/Hermetic Git invariants are unaffected — `githubclient_test.go` stays untagged with no new spawns, `webstercli`'s existing `TestMain`/`HermeticGitEnv` wiring is untouched.
- `## All Files Touched` in `00-overview.md` matches exactly the four files delivered; no surprise files.

## Verdict

APPROVE
Implementation matches every batch's cards, shared decisions, and constraints; no blocking issues found.
MILL_REVIEW_END
