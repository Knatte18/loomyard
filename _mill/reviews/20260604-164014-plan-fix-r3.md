# Plan Fix — round 3

Source review: `_mill/reviews/20260604-163844-plan-review-r3.md`

## Fixed

- **[BLOCKING] Home.md header newline conflict** — clarified Card 9: `lines = ["# Tasks", ""]` produces `"# Tasks\n"` for empty list; test (a) (`"# Tasks\n"`) is correct and unchanged.
- **[BLOCKING] WIKI_SKIP_PUSH suppresses pull** — fixed Card 15 step (2): pull is gated only on `WIKI_SKIP_GIT != "1"`; `WIKI_SKIP_PUSH=1` no longer skips pull.
- **[NIT] BriefTask struct location ambiguous** — pinned to `store.go`.
- **[NIT] Card 11 Context over-includes task.go** — removed `internal/wiki/task.go` from Card 11 Context.

## Pushed Back

_(none)_
