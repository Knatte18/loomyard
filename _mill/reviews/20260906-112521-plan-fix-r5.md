# Plan fix — round 5

Source review: `_mill/reviews/20260906-112316-plan-review-r5.md`

## Fixed

- r5 "[BLOCKING:scope] CanonicalizeHandles is defined with no caller" — confirmed: no card named an invoker, and `resolvePass` carried no plan directory to pass it. Card 16 now states that `resolvePass` takes the plan directory from `plan.Dir` — the field `ParsePlan` already stamps — rather than adding a parameter to either exported entry point, which would break the `planparser.Validate(plan, worktreeRoot)` shape the parity pair mirrors. Card 19 now names the caller explicitly and pins the ordering: `CanonicalizeHandles` runs first inside `resolvePass`, before cards 17, 18 and 20's passes, because they must see canonical handle spellings rather than draft ones; card 20's wiring sentence updated to match, and an infrastructure error propagates as card 21's wrapped error.
- r5 "[NIT:decision] Rename to-side handle is never computed" — confirmed and load-bearing: card 11 required a handle on the to-side precisely so it could be computed, but no card computed it, so the untrusted draft spelling survived to the end. Card 19's batch now draws declarations from two sources: `Create` sub-bullets, and every `Rename` pair whose `New` is a handle — for those, the resolved `Old`'s declaration is read from the same batched `Resolve` answer, the identifier swapped for the one the draft handle carries, and the result added to the same single `quarry.Name` call. Only the identifier comes from the draft; the spelling is what `Name` computes. A pair whose `Old` does not resolve `found` produces a new blocking `rename-old-unresolved` finding rather than silently contributing nothing. Card 11's forward reference retargeted from batch 7 to card 19.
- r5 "[NIT:scope] The record-batch Delta call site is named only in Batch Scope" — confirmed: three cards consumed the delta and none created it. Card 34 now owns the single `planglyph.Delta(deps.Geom.WorktreeRoot, bs.StartSHA, report.HeadSHA)` call, states its position in the sequence, threads the one answer into cards 35 and 36, forbids a second call, and records why the SHAs can be trusted at that point.
- r5 "[NIT:consistency] Card 29's RunCLIIn recount yields no textual change" — confirmed: the sentence is wrong today (eleven modules, ten `RunCLIIn`) but becomes correct post-task (twelve and eleven), so it coincides with the existing text. Card 29 reframed from "correct it" to "verify it and leave it byte-identical unless the derivation disagrees", with the commit-message note reworded so an unchanged line does not read as an overlooked one.
- r5 "[NIT:consistency] New language check displaces plan-unapproved from position two" — confirmed: wiring the new check after `checkFormatRecognized` puts it ahead of the `requireApproved` block. Card 3 now lists `Validate`'s "plan-unapproved at position two" sentence among the doc-comment text to update, alongside the counts.

## Pushed Back

_None. All five findings verified accurate against the cited source and none met a harm-check escape._
