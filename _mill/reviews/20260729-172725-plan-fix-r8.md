# Plan-fix report — round 8 (first short-review round on the revised hybrid seam)

Source review: `_mill/reviews/20260729-172725-plan-review-r8.md` (reviewer opus · xhigh; verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT). This is the first review of the re-planned HYBRID seam. Significantly, R8 found NO design breakage in the new seam — both findings are a scope oversight and a correctness refinement. Both verified accurate.

## Fixed

- [BLOCKING] "Batch-2 authoritative verify omits two migrated packages" — verified: I had updated the batch-2 FILE header verify and the overview's "Go verify commands" Shared Decision to span four packages, but the AUTHORITATIVE `batches:` DAG block in `00-overview.md` (which mill-go actually reads) still listed only `fabricengine`/`initengine`. Appended `./internal/configcli/ ./internal/loomengine/` to batch 2's `verify:` in the `batches:` block, so the compiler-forced migration of the `configcli`/`loomengine` test call sites is actually verified.
- [NIT→correctness] "Reconcile re-wire and health-check name-sets can diverge" — verified a real multi-pair bug: `Reconcile` iterates over many host worktrees and `Remove(l, slug)` can target a non-acting slug, each with its own `pathspec`, so sourcing wire-names from the acting worktree's `t.cfg` (as the first revision said) could re-wire the wrong set and loop `JunctionRepointed` against the per-pair health check. Changed the design so every mutating-primitive prod caller sources names from the TARGET pair's OWN weft base via `junctionNames(...)` (matching `checkJunctionHealth`/`PairInSync` exactly), never `t.cfg`: updated the overview hybrid Decision + batch-2 batch-local note + card 5 (filter is now internal to `junctionNames`/`WiredNames`) + card 6 (checkout uses `l.WeftWorktreePath(slug)` base, reconcile uses `hostLayout.WeftWorktree()` base) + card 7 (remove uses the removed slug's weft base, surfacing a load error before any destructive step). `Add`'s reserved-name check keeps `t.cfg.Dirs()` (it validates a new slug against the current worktree's reserved set — correct).

## Pushed Back

(none — both findings were accurate; the NIT was in fact a latent multi-pair correctness bug and was fixed as such.)
