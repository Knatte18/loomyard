# Plan fix report — round 1

Source review: `_mill/reviews/20260628-141536-plan-review-r1.md` (verdict APPROVE, 3 NITs, 0 blocking).

## Fixed

- 20260628-141536-plan-review-r1.md — "[NIT] JSON number decodes as float64, not int": Batch 1 / Card 3 number-parsing requirement now states the test must compare against `float64(123)` (or a typed struct), not an `int` literal.
- 20260628-141536-plan-review-r1.md — "[NIT] gh-not-found branch also catches generic exec failures": Batch 1 / Card 1 `createIssue` now distinguishes the cause via `errors.Is(err, exec.ErrNotFound)` ("gh not found on PATH" vs. generic "failed to run gh"), rather than keying off `exitCode == -1`.
- 20260628-141536-plan-review-r1.md — "[NIT] overview.md \"Module dispatch\" is stale": Batch 2 / Card 8 now also refreshes the pre-cobra `switch module`/`RunCLI` dispatch prose in `docs/overview.md` to reflect the cobra root in `newRoot()`, since the implementer already edits that file.

## Pushed Back

(none)
