MILL_REVIEW_BEGIN
# Review: builder: delete internal/builderengine and internal/buildercli, retire builder-contract.md as a reference — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5), best-effort self-assessment
reviewed_file: plan/
date: 2026-08-09
```

## Verification performed

Read the overview and all five batch files in full, then cross-checked plan claims against live source for every card:

- Code sites (batch 1): `cmd/lyx/main.go`, `configreg.go`/`configreg_test.go`, `configcli_test.go`, `notransients_test.go`, `constructoranchoring_test.go`, `rawgitmutation_test.go` (var/func doc-comment quotes verified verbatim), `.gitattributes`, `tools/sandbox/{suite.go,main.go}`, `SANDBOX-CORE-SUITE.md`'s S9 span and code-fence `---` distinction.
- Batch 1 cards 2–4: `fabricengine` fixture files, `loomengine/coherence.go` phase enum, `scoutcli/cli.go` + `cli_test.go`'s `TestFilterWithin` fixture rewrite — independently re-derived the substituted paths and confirmed the plan's own flagged `crossPackage`-collision fix is logically necessary and correctly resolved.
- Batch 2: verified the `rawGitMutationMinScannedFiles` 24-file count claim by listing `internal/websterengine`'s non-test `.go` files (24, confirmed).
- Batch 3–5 docs: `status-schema.md`, `model-spec.md`, `docs/overview.md`, `README.md`, `manifest/roadmap.md`, `manifest/designs/loom.md`, `finalize.md`, `discussion-format.md`, `hardener.md`, `raddle.md`, `review-finding-classification.md`, `self-report.md`, `plan-format-v3.md`, `sandbox-howto.md`, `sandbox-hub.md`, `SANDBOX-WEBSTER-SUITE.md`, `shed-followups.md`, `CONSTRAINTS.md` — grepped each file case-insensitively for `builder` and confirmed every occurrence is named in the corresponding card's `Requirements:`, with no unaddressed residue.
- Repo-wide `builder`-hit sweep (224 files) beyond the plan's own file list: every non-`strings.Builder` hit outside plan scope (`sandbox/build.cmd`'s "Hub builder:", `weftname.go`'s "fixture builders", `parity_test.go`'s line-wrapped "fixture / builders beyond...", `claudeengine/command_test.go`'s "the same builder produces", `reedcli/cli.go`'s "xCmd() builder", `fabricengine/launchers*.go`'s "content builder", `plugins/prowler/scripts/run.sh`'s "a builder that died" / "concurrent builders'") matches an enumerated card-18 exclusion token exactly.
- `## All Files Touched` union re-derived by hand from every card's `Edits:`/`Creates:` and matched byte-for-byte against the overview's list.
- Batch Index DAG (linear 1→2→3→4→5, no cycle, no forward dep), global card numbering (1–18, sequential, no gaps), and the `Moves:`/`Rename mechanic` criteria (no card has a non-empty `Moves:`, so N/A) all check out.

No `Moves:` anywhere in the plan, so the rename-mechanic and no-full-rewrite criteria are vacuously satisfied.
No BLOCKING or NIT findings resulted from this pass — every requirement I checked names a real, currently-existing site with an accurate quote, every Context/Edits list covers what its own Requirements reference, and the acceptance-sweep exclusion list in card 18 is independently verifiable against the live tree rather than merely asserted.

## Verdict

APPROVE — every checked requirement is source-grounded and complete; no constraint violations, scope gaps, or unaddressed `builder` residue found.
MILL_REVIEW_END
