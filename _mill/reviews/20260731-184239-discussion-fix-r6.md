# Discussion fix report — round 6

Source review: `_mill/reviews/20260731-184118-discussion-review-r6.md` (verdict APPROVE, 3 NOTE findings)

## Fixed

- "Count-bound 'newest 50' sort key not stated" — `retention` decision now states the count bound ranks by the same filename timestamp segment the age bound reads, never mtime.
- "Liveness-skip vs. count budget interaction unspecified" — added a `retention` bullet: live-skipped files are kept unconditionally and do not consume one of the newest-50 slots; the count bound's ranking is computed only over the dead-pid-eligible set. Updated the "Retention liveness rule" testing bullet with a concrete assertion for this.
- "Header worktree-root field under the test seam" — added a `sink-open-triggers` bullet: the untagged test seam never resolves geometry (Test Tier Purity), so the header's worktree-root field is left empty on that path; only production/integration paths populate it. Updated the "Sink naming and lazy open" testing bullet and the "Retention sweep" testing bullet (ranking key) to match.

## Pushed Back

(none)
