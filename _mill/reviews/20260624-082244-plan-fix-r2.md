# Plan fix report — round 2

```yaml
slug: yamlengine
round: 2
source_review: _mill/reviews/20260624-082156-plan-review-r2.md
verdict_addressed: REQUEST_CHANGES (1 blocking, 3 nits)
date: 2026-06-24T08:22:44Z
```

## Fixed

- [r2 BLOCKING] "Batch 6 batch-depends omits batch 5" — added `5` to batch 6's
  `depends-on` in both `00-overview.md` and `06-lyx-update-init.md` frontmatter
  (now `[1, 3, 4, 5]`). Card 19's fresh-init strict-Load assertions and card 21's
  template-complete fixtures require batch 5's engine-backed wrappers; the dep
  ensures batch 6 is scheduled/verified after batch 5. DAG re-validated, no cycle.
- [r2 NIT] "Card 17 write-condition precedence" — reworded to the explicit
  `apply && (fileAbsent || len(added)+len(removed) > 0)`.
- [r2 NIT] "Card 5 init_test.go header comment drift" — Card 5 now also updates the
  file's top doc comment so it no longer claims the templates are "fully commented".
- [r2 NIT] "Migration gap for pre-existing weft worktrees" — added a migration note
  to batch 7 Card 22 (docs): `lyx update --apply` run from the host worktree
  reconciles `weft.yaml` via the host `_lyx`→weft junction, so no separate
  weft-side command is needed; existing commented-format files and weft worktrees
  lacking `weft.yaml` are migrated by `lyx update --apply`.

## Pushed Back

(none)
