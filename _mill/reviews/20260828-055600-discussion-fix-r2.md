# Discussion fix — round 2

Source review: `_mill/reviews/20260828-055451-discussion-review-r2.md` (verdict APPROVE, 5 NITs — 1 demoted from BLOCKING by the blocking-class ceiling).
Every finding was verified against source before being applied; none were pushed back.

## Fixed

- `20260828-055451-discussion-review-r2.md` — "[NIT:decision] landingshed import allowlist has no disposition": confirmed `internal/landingshed/seam_enforcement_test.go` lists `internal/websterengine` in `landingshedAllowedImports`. Added a Scope bullet stating the entry is dropped and `internal/summaryparser` added, a Technical-context subsection explaining why a left-behind entry still passes, and a Testing bullet marking it a plan-level checklist item.
- `20260828-055451-discussion-review-r2.md` — "[NIT:consistency] New invariant not scoped to production code": confirmed bare `"summary.md"` literals at `websterengine/recordbatch_test.go:156` and ~10 sites in `websterengine/runlevel_test.go`. Scoped the invariant to production code in both the Decision and the Constraints bullet, cited the Lyxdirs Single-Declarer precedent, and stated explicitly that no declarer-scan test is added.
- `20260828-055451-discussion-review-r2.md` — "[NIT:scope] Message lands on both pair sides, not just the parent commit": confirmed `fabricengine/mergelifecycle.go` computes one `effectiveMsg` and passes it to `MergeConclude` for warp and weft alike. Added an accepted-consequence bullet to the `message-set-unconditionally` Decision, with the reason suppressing it is out of scope.
- `20260828-055451-discussion-review-r2.md` — "[NIT:consistency] \"matching Publish\" overstates the parity": confirmed `publish.go:98` returns `Done` before the parse when the parent is not in `RequirePRToBase`, and `publish.go:168` gates the parse on an empty PR query. Rewrote the `finalize-parse-fails-loud` rationale as a deliberate tightening, keeping only the failure *disposition* as borrowed from `Publish`, and added the rejected alternative of gating on `RequirePRToBase`.
- `20260828-055451-discussion-review-r2.md` — "[NIT:decision] Told-Geometry bound-package list membership unstated": confirmed `CONSTRAINTS.md`'s list contains `planparser` but not `discussionparser`. Added an explicit Decision that `summaryparser` is not added, because its stdlib-only leaf rule is strictly stronger and is test-enforced.

## Pushed Back

_None._
