# Plan fixer report — round 5

Source review: `_mill/reviews/20260726-155555-plan-review-r5.md`

## Fixed

- r5 review, "[BLOCKING] Test-relocation leaves cross-package helpers
  undefined" — verified all three helper dependencies (`writeFile` in
  state_test.go:418 called by run_test.go; `stringSlicesEqual` in
  roundfiles_test.go:156 called by run_test.go; `intSlicesEqual` in
  profile_test.go:268 called by state_test.go). Card 3 now carries an
  explicit cross-package test-helper fallout list duplicating each helper
  verbatim on the side that loses it, and run_test.go's mechanical-edit
  list gained item (c) receiving the duplicated helpers.
- r5 review, "[BLOCKING] buildRoundProfile's param type moves; its test
  isn't verbatim" — verified (`buildRoundProfile` takes
  `roundArtifactPaths`; the test builds its fixture via `artifactPaths`).
  Card 2 now pins the post-extraction signature
  (`buildRoundProfile(p Profile, reviewPath, fixerReportPath string,
  priorReviews, priorFixerReports []string) burlerengine.Profile`) and
  card 3 now states the extracted test keeps its assertion structure but
  rewrites its fixture to literal path strings against that signature —
  no longer claimed verbatim.
- r5 review, "[NIT] Card 13 fixture example covers only one of two
  key-pairs" — verified the fixtures carry `model: sonnet` +
  `effort: high` too. Card 13 now names both key-pair migrations
  explicitly (`model: "sonnet[effort=high]"`).

## Pushed Back

- (none)
