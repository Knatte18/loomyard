# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260825-065246-plan-review-r1.md`

## Fixed

- `20260825-065246-plan-review-r1.md` — "[BLOCKING:design] Card 8 breaks the build; its \"no external callers\" premise is false" — verified against `plugins/prowler/reddit.go:41-43`, which returns `fetchOldRedditHTML`'s result directly. Added `plugins/prowler/reddit.go` to card 8's `Edits:`, required a one-line interim adaptation (`out, err := ...; return out, err == nil`) in that card, and corrected batch 3's `## Batch Scope` claim that the function has no external callers.
- `20260825-065246-plan-review-r1.md` — "[BLOCKING:scope] Card 8 leaves three fetch_test.go subtests set up to nil-panic" — verified against `plugins/prowler/fetch_test.go:287-325`; `non_2xx_fails`, `transport_error_fails`, and `unsupported_content_encoding_fails` each build a raw `fetcher{do: ...}` literal. Card 8 now names all three by sub-test name and requires `doNoRedirect` to be set on each, plus on `reddit_test.go`'s raw literal.
- `20260825-065246-plan-review-r1.md` — "[NIT:scope] Reply-level maxTopComments cap is specified but never exercised by the fixture" — verified; the fixture requirement only demanded "at least one" nested reply. Card 5's fixture now requires one comment with more than `maxTopComments` nested replies and one comment whose reply itself carries replies, and `TestFormatRedditThread` now asserts the rendered reply count for the over-capped comment.

## Pushed Back

_(none)_
