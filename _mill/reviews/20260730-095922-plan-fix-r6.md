# Plan-fix report — round 6

Source review: `_mill/reviews/20260730-095752-plan-review-r6.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 0 NIT). Finding verified accurate and new; fixed; none pushed back.

## Fixed

- **[BLOCKING] "Reddit JSON structs omit the nested listing/children shape"** — Card 5's struct spec was internally inconsistent (its own test bullets referenced `children` a field the flat struct never named) and gave a cold implementer no source for the nested shape. Card 5 now defines the full shape: `redditThing{ Kind, Data redditData }` and `redditData{ …flat fields…, Children []redditThing \`json:"children"\` }`, states that a post response is `[]redditThing` (two Listings) and a subreddit response is a single Listing object, and pins the exact JSON path each formatter reads — `formatRedditPost`: post fields from `arr[0].Data.Children[0].Data`, comments from `arr[1].Data.Children` filtered `Kind=="t1"` (cap 20); `formatRedditSubreddit`: the single listing's `Data.Children` (`Kind=="t3"` post things).

## Pushed Back

None.
