# Plan fix report — round 1

Source review: `_mill/reviews/20260729-072809-plan-review-r1.md` (verdict: REQUEST_CHANGES, 4 BLOCKING, 2 NOTE/NIT).

## Fixed

- `_mill/reviews/20260729-072809-plan-review-r1.md` — "[BLOCKING] CloneHub test call sites break on the new 2-value return": Batch 2 Card 9's instructions for all three existing `CloneHub` call sites in `clone_adopt_test.go` said only "change the call to 3 args," never mentioning the destructured return-value count also has to shrink from 3 to 2. Fixed by spelling out the exact before/after destructuring for each of the three call sites (`hubPath, _, err :=` → `hubPath, err :=` twice; `_, _, err :=` → `_, err :=` once).
- `_mill/reviews/20260729-072809-plan-review-r1.md` — "[BLOCKING] No verify: actually runs the integration tests this plan adds": every batch's `verify:` was a bare `go test ./...`-shaped Tier-1-only command, which never runs the `//go:build integration` files Batches 1, 2, 3, 4, and 5 each add or edit (`docs/benchmarks/running-tests.md` confirms Tier 1 excludes tagged files entirely). Fixed by adding a second `go test -tags integration ./...` clause to every non-null `verify:` field (both `00-overview.md`'s Batch Index and each batch file's own frontmatter, for batches 1-5).
- `_mill/reviews/20260729-072809-plan-review-r1.md` — "[BLOCKING] Batch 5's verify: never targets internal/fabriccli": batch 5's frontmatter `verify:` was `go test ./cmd/lyx/...` only, contradicting its own "Batch Tests" section's claim of `go test ./cmd/lyx/... ./internal/fabriccli/...` — Card 27 adds a test to `internal/fabriccli/cli_test.go` that this verify command never reached. Fixed by adding `./internal/fabriccli/...` to batch 5's `verify:` in both the overview and the batch file (alongside the integration-tag fix above).
- `_mill/reviews/20260729-072809-plan-review-r1.md` — "[BLOCKING] Card 15 Context omits store.go": Batch 3 Card 15's Requirements reference `*Store` and several of its methods throughout, but Context listed only `task.go`. Fixed by adding `internal/boardengine/store.go` to Card 15's Context.
- `_mill/reviews/20260729-072809-plan-review-r1.md` — "[NIT] Card 32 leaves dead links to the deleted design doc": confirmed via direct grep that `manifest/designs/fabric-unified-view.md`, `raddle.md`, `host-visibility.md` (×2), `pattern.md` (×2), and `internal/fabricengine/cleanup.go`'s comment all reference `board-weft-storage` and would dangle once the file is deleted. Fixed by adding all five files to Card 32's Edits and spelling out the exact sweep for each reference, plus a substantive (not just dead-link) fix to `cleanup.go`'s adjacent "the board repo is excluded entirely" comment, which is now factually wrong (board has no separate repo) beyond just naming a deleted file — found independently while verifying this finding. Also added a one-sentence fold-in of the design doc's forward-looking "Consequence" note (this repo's own manifest/mill-wiki eventual fold-in) into `board.go`'s package doc, which the review separately flagged as dropped with no preservation.
- `_mill/reviews/20260729-072809-plan-review-r1.md` — "[NIT] README.md keeps the stale `_board` description Card 29 fixes elsewhere": confirmed README.md line 45 carries the identical stale topology-diagram line `docs/overview.md` gets fixed in Card 29, with no card touching README's own copy. Fixed by extending Card 30 (README.md's git-version-bump card, in this plan's numbering) to also correct README's topology diagram line.

## Pushed Back

(none — all six findings were factually accurate and fixed as recommended)
