# Plan fix — round 3

Source review: `_mill/reviews/20260810-170521-plan-review-r3.md` (holistic, 1 BLOCKING, 1 NIT).

## Fixed

- `20260810-170521-plan-review-r3.md` — "[BLOCKING:scope] Card 26's Edits list omits the two files it requires editing".
  Accurate, and re-verified against the tree rather than taken on trust: `removeWeftWorktree` has exactly two production callers, one in `remove.go` and one in `add.go`, both inside `Topology` methods.
  Adding a parameter without editing them leaves the package non-compiling, so the card's declared scope was genuinely short by two files.
  Both added to card 26's `Edits:`, with the requirement now naming them, stating that each passes its receiver's configured branch prefix, and stating that they are this card's own work rather than a side effect of the other cards that happen to touch those files.
  No new cross-batch conflict: batch 5 already depends on batch 3, so the shared files are never edited in parallel.
- `20260810-170521-plan-review-r3.md` — "[NIT:consistency] Card 31's roadmap.md edit under-specifies how to split a combined 4-slice bullet".
  Accurate: the four slices share one Planned bullet, so "move slice 12 to completed" had more than one defensible reading.
  Card 31 now specifies the split explicitly — a new short Done bullet in the register the existing Done entries use, the Planned bullet rewritten to cover slices 13 to 15, the dependency statements kept but reworded from "goes first" to "has landed", and only slice-12-subject text dropped, since the build-order argument it carries is already recorded in the design doc.

## Pushed Back

_None._
Both findings verified accurate against the plan and the tree;
neither fix breaks existing functionality, conflicts with a documented decision, or destabilises other work.
