# Discussion fix — round 5

Source review: `_mill/reviews/20260818-193155-discussion-review-r5.md` (APPROVE, 0 blocking, 2 NIT, none demoted).

## Fixed

- `20260818-193155-discussion-review-r5.md` — "[NIT:consistency] Enforcement/audit set delta omits two packages": the `doc.go` audit decision now states the delta in both directions — `internal/shedengine` and `internal/treadleengine` are machine-enforced and bound, but were never in the converted set, each having arrived at told geometry through its own earlier seam invariant, so both sit deliberately outside the audit.
- `20260818-193155-discussion-review-r5.md` — "[NIT:consistency] \"do not mention it at all\" is false for configengine": verified `internal/configengine/config_test.go:678,693,708` carry the bare token in comments. The verified-state bullet is now scoped to production files, names those three test hits, and the re-verification instruction says to exclude `_test.go`.

## Pushed Back

None. Both findings verified accurate against the cited source;
neither fix breaks existing content, conflicts with a documented decision, or destabilizes work outside this task.
