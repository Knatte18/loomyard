# Plan fix report — round 1

```yaml
round: 1
source_review: _mill/reviews/20260622-111409-plan-review-r1.md
verdict_handled: APPROVE (2 NIT findings)
date: 2026-06-22
```

## Fixed

- [NIT] "Card 1 Context omits junction_windows.go" (r1 review) — added
  `internal/worktree/junction_windows.go` to Batch 1, Card 1 `Context:`, matching the
  Requirements text that cites it as a `prepareLink` port source.
- [NIT] "seedLyxJunction edge-case check order changes" (r1 review) — reordered Batch 2,
  Card 7 `seedLyxJunction` requirements to check the missing-target case first
  (`EvalSymlinks(target)` error → "weft _lyx directory does not exist …"), then the
  idempotent match, then the catch-all "host repo already contains a real _lyx …",
  mirroring the original order so the real-dir+absent-target edge message stays
  byte-identical per the `preserve-behaviour-and-messages` decision.

## Pushed Back

(none)
