# Plan fix — round 1

Source review: `20260826-123326-plan-review-r1.md`

## Fixed

- `20260826-123326-plan-review-r1.md` — "[BLOCKING:scope] Card 7 Context omits parse.go despite naming parsePaneList": added `internal/reedengine/parse.go` to card 7's `Context:`.
- `20260826-123326-plan-review-r1.md` — "[BLOCKING:scope] Card 4 Context omits strand_test.go despite citing its shape": added `internal/reedengine/strand_test.go` to card 4's `Context:`.
- `20260826-123326-plan-review-r1.md` — "[NIT:consistency] applyLayoutLocked's new live-box query bypasses its own documented skip guards": took the reviewer's first option.
  Card 2 now requires `applyLayoutLocked` to be restructured guards-first — `len(live) < 2` and `!anyPlacedStrand(...)` move above the plan, and `liveBoxLocked()` is called beneath them — so no `display-message` round trip fires on the paths the doc comment promises to skip, and `reconcileApplyPersistLocked`'s per-launch `Resume` loop pays nothing extra there.
  The card now also states the one behavioural delta (a plan error is no longer returned once a skip guard has fired), names the two existing tests that pin the nil return and keep passing, and forbids weakening them.
  `internal/reedengine/apply_test.go` and `internal/reedengine/spawn.go` were added to card 2's `Context:` for the two identifiers that reasoning cites.

## Pushed Back

_None — every finding was accurate and no fix caused demonstrable harm._
