# Plan fix — round 3

Source review: `_mill/reviews/20260808-063522-plan-review-r3.md` (holistic, APPROVE, 0 BLOCKING + 1 NIT).

## Fixed

- r3 holistic, "[NIT] Card 3's temporary lspclient.go rename is underspecified" — card 3 now requires the temporary name to keep a `.go` suffix, with an example and the reason.
  Accurate: a non-`.go` name drops the file from the build, so every other file in the package referencing `lspClient` and its neighbours fails to compile.
  That is a package-wide compile failure, not the isolated `t.Fatal` the check exists to observe — the guard test would never run to reach its own missing-target branch.

## Pushed Back

_None._
