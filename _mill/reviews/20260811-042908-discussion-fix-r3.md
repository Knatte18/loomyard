# Discussion fix — round 3

Source review: `_mill/reviews/20260811-042808-discussion-review-r3.md` (verdict APPROVE, 0 blocking, 4 NITs).
All four verified against source before applying;
none pushed back.

## Fixed

- `20260811-042808-discussion-review-r3.md` — "Doc site 5 leaves the contradicting sentence": site 5 widened from `internal/websterengine/doc.go:25–26` to `:23–29`, naming both stale claims (the config-key clause and the "Batching is 100% webster's own execution-policy decision" sentence at `:27`) and stating that `:27` is reworded in the same words site 1 uses for batcher's own doc, while `:28–29`'s "never the plan's / never an LLM's" clauses stay.
- `20260811-042808-discussion-review-r3.md` — "planparser/doc.go's \"webster's batcher\" has no disposition": added as doc site 9, in scope, with the reword named ("the batcher, webster's master, and fork prompt rendering") and the reason for listing rather than sweeping it.
- `20260811-042808-discussion-review-r3.md` — "The nil-Batcher \"typed error\" is unnamed": sentinel decided as `ErrNilBatcher = errors.New("webster: RunDeps.Batcher not populated")`, placed in `runlevel.go` beside `ErrRunBusy` (`:45`), with the seven existing webster sentinels cited as the convention it follows.
- `20260811-042808-discussion-review-r3.md` — "configreg entry's SeedOnly flag not stated": recorded `SeedOnly` as deliberately omitted (`false`) with the closed-key-set reason, plus the note that `configreg_test.go:32`'s `TestModules_SeedOnly` passes unchanged because its `want` is name-computed.

## Pushed Back

None.
