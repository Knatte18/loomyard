# Plan fix report — round 7

Source review: `_mill/reviews/20260801-090212-plan-review-r7.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT). First round with the operator-switched `sonnethigh` reviewer, which caught a genuine architectural blocker the prior six `sonnetxhigh` rounds missed. Both findings VERIFIED and FIXED.

## Fixed

- [BLOCKING] "Batch 4 introduces a fabricengine↔configsync import cycle" — CONFIRMED via `grep`: `configsync` imports `configreg` (configsync.go:13), `configreg` imports `fabricengine` (configreg.go:12,51 for `fabricengine.ConfigTemplate`), and `fabricengine` does NOT import `configsync` today. Cards 15/16 had `fabricengine.CloneHub` call `configsync.ReconcileFabricAt`/`ReconcileAll`, closing `fabricengine → configsync → configreg → fabricengine` — a hard Go compile error; batch 4 would not build. (`initengine`, today's orchestrator, imports both safely because it sits above them.) Fixed via the reviewer's option (a): restructured batch 4 so `fabricengine.CloneHub` stays git/geometry-focused (clone, checkout, board worktree, anchor resolve/validate, write `.fabric-anchor` to disk) and returns a `CloneResult{HubPath, Anchor, BoardDir, WeftBase, PrimeCwd}`; the `internal/fabriccli` clone handler (which imports both without cycling, exactly as `initengine` did) drives the config-materialization + weft:main commit + wiring + reconcile sequence. Rewrote cards 14-19 and the Batch Scope, added an explicit "Import-cycle constraint" section and a partial-failure-recovery decision (`lyx fabric reconcile` completes a clone whose post-git orchestration failed, rather than tearing down a good clone). "Clone does everything" stays true at the command level.
- [NIT] "Card 23's ported snippet silently drops an error the source checks" — CONFIRMED: `undo.go:145-148` checks the `New(...)` error; card 23's snippet wrote `f, _ := New(...)`. Fixed card 23 to check and return the error (`f, err := New(...); if err != nil { return result, err }`), matching "verbatim in behavior".

## Pushed Back

(none — both findings were factually accurate and fixing them caused no harm.)
