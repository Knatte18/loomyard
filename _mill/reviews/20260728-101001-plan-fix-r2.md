# Plan fix — round 2

Source review: `_mill/reviews/20260728-101001-plan-review-r2.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 1 NIT, reviewer `sonnetmax`).

## Fixed

- r2 "[BLOCKING] Card 28's inclusion of plan-template.md doesn't fit the batch's own stated criterion" — verified accurate against source: `internal/loomengine/plan.go`'s package doc comment states *"Like the Discussion producer (discussion.go), the Plan producer is not a Go module — it is a prompt/profile fed to shuttle.Run, one shuttle.Run producing one artifact"*, so the two producers are structurally identical and batch 7 was including one while excluding the other under a three-clause criterion that literally covers neither. Added a fourth clause to the criterion — **(d) authors the typed file-op instructions a later code-writing agent executes near-verbatim** — and stated the distinction it rests on: the Plan producer emits plan-format-v3 cards whose `Edits:`/`Creates:`/`Moves:`/`Requirements:` fields a builder implementer or webster fork executes near-verbatim, so an invariant violated at planning time is carried into every card rather than reconsidered, whereas the Discussion producer's decision record is re-derived from by the Plan producer against the codebase and therefore still has a gate after it — the plan prompt, which now carries the directive. Plan is the last authoring point before code; Discussion is not. Also recorded why clause (d) makes `RoleImplementer` the right variant for `plan-template.md` rather than a fourth role: the block addresses the agent that writes code, which is precisely the audience the plan's `Requirements:` prose is written for, so the directive travels through the plan to the agent that executes it. Restated builder's orchestrator exclusion against all four clauses rather than three.
- r2 "[BLOCKING] Card 14 widens the pathspec default but doesn't say to fix the now-stale exact-match test" — verified accurate: `internal/fabricengine/template_test.go`'s `TestConfigTemplate_PathspecResolvesToLyx` asserts `if pathspec != "_lyx"` against the resolved template default, so batch 4's own `verify:` would have gone red on a test card 14 never named. This was the one place in the plan where the "name the stale pre-existing assertion, update it, never delete it" pattern used by cards 8, 15 and 19 had lapsed. Card 14 now names the test explicitly and requires its exact-equality check rewritten to split the resolved value on whitespace and assert exactly `_lyx` and `_pattern` in order — **and** requires the test's *name* and doc comment rewritten too, since both encode the old single-value expectation in their own text (`…PathspecResolvesToLyx`, "resolves to `_lyx` regardless of environment") and would otherwise misdescribe what the test checks.
- r2 "[NIT] Card 29 overclaims that the discussion resolved every Open Question in pattern.md" — verified accurate by reading `manifest/designs/pattern.md`'s `## Open questions` section: it carries five entries, and exactly one — *Detail-submap layout*, whether `_pattern/<topic>/` has a fixed structure or is free-form — is a question about PATTERN's own content and is settled by nothing in these seven batches, since content migration is explicitly out of scope. Card 29 point (6) now enumerates the four that **are** settled and names the decision each resolves to (template set, helper home, directive wording, optional-marker surface), and requires the detail-submap question to be kept in the section marked as deferred to the content migration rather than silently dropped.

## Pushed Back

None. All three findings were verified against the cited source before acting — the `plan.go` doc comment, the `TestConfigTemplate_PathspecResolvesToLyx` body, and the five-entry `## Open questions` list were each read directly. No fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope: the first two add stated rationale and an explicit test-update instruction to existing cards, and the third narrows a card's doc-editing instruction from "replace all" to "replace four, carry one forward".
