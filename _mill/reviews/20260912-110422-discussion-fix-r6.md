# Discussion fix report — round 6

Source review: `_mill/reviews/20260912-110254-discussion-review-r6.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, 1 of them demoted from BLOCKING by the stage's blocking-class ceiling).

Round 6 is the configured round cap (`roles.discussion-review.holistic.rounds: 6`).
The convergence gate is not satisfied — the round carries a demoted finding, and round 5's verdict was `REQUEST_CHANGES` rather than the second consecutive `APPROVE` `--orch` requires — so this round proceeds to Handoff as a **round-cap implicit approval**, which the commit message records.
The operator additionally instructed, mid-session, that reaching the cap should end in approve + handoff regardless of which verdict the capped round returned.

## Fixed

- `20260912-110254-discussion-review-r6.md` — "[NIT:consistency] Context skip loses the crash-resume permanently" (demoted from BLOCKING).
  Verified accurate: the cancellation arm persists `StatePaused`, so the next `drive`'s entry read sees `paused`, never `running`, and the in-memory crash observation is unrecoverable — the identical permanent loss `### detection-site` already refuses to accept on the error path.
  Took the first of the two offered dispositions rather than the "record it as imprecision" escape: a detected crash-resume is now exempt from the done-context skip and filed before that skip returns, with everything else still skipped.
  Dropped the false "skipping loses nothing" claim, scoping it to triggers 2–5, and added the two paired Tier-1 cases (cancelled context with and without a crash-resume in the entry observation).

- `20260912-110254-discussion-review-r6.md` — "[NIT:consistency] `false` skips everything vs. the unconditional entry step".
  Verified accurate: the knob was a told input of the extracted function only, while the entry lock probe and the extra status decode sat unconditionally in `drive`'s closure — so `selfreport: false` still paid the cost the rationale claimed it avoided.
  `drive` now guards the entry step on the same bool, named as the one deliberate place the closure branches on it, and the Config test asserts the entry step is skipped too.

- `20260912-110254-discussion-review-r6.md` — "[NIT:consistency] Trigger-5 title has no occurrence discriminator".
  Verified accurate: `<bouncer-row> — <ledger-key>` is identity-keyed, so a key recurring after a `resolved` round — including across an `archiveRunDir` generation boundary — is suppressed permanently, which contradicts the section's per-occurrence premise as written.
  Recorded as the deliberate exception it is: trigger 5 is identity-deduped, one issue per `(row, key)` pair per task, with the reasoning stated and a test case pinning it as intended behaviour rather than a latent bug.

## Pushed Back

None.
All three findings were verified accurate against source before being fixed, and `--orch` mode has no push-back path available in any case — there is no operator in this worker's own conversation to escalate a disputed finding to.
