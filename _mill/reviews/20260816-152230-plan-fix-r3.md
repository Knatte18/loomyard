# Plan fix — round 3

Source review: `_mill/reviews/20260816-152120-plan-review-r3.md`

## Fixed

- `20260816-152120-plan-review-r3.md` — "[BLOCKING:scope] Card 11's Context omits the two files whose functions its Requirements name".
  Verified accurate: card 11's requirement 3 asks the implementer to state what `stencilstore.Read`, `stencilstore.Reconcile`, and `stencil.StripLeadingComment` each do, and neither declaring file was on that card's `Context:` allowlist.
  Added `internal/stencilstore/reconcile.go` and `internal/stencil/stencil.go` to card 11's `Context:`.
- Same finding, applied plan-wide rather than only where it was reported.
  Rounds 1, 2, and 3 each surfaced one instance of the identical defect on a different card, so the remaining cards were swept for it in this pass instead of waiting for a fourth round to name the next one.
  Card 4's `Requirements:` asserts which internal packages `internal/stencil` and `internal/stencilstore` do and do not import, so it gained `internal/stencil/stencil.go`, `internal/stencilstore/stencilstore.go`, and `internal/stencilstore/reconcile.go`.
  Card 5 names `stencilstore.Reconcile` and `stencil.Fill`, so it gained `internal/stencilstore/reconcile.go` and `internal/stencil/stencil.go`.
  Card 6 names `stencilstore.Read`, `stencil.Fill`, `stencil.StripLeadingComment`, `ApplyStamp`, and the three embedded defaults, so it gained those two plus `internal/stencilstore/stencilstore.go` and `stencils/stencils.go`.
  Card 9 names `fabricengine.StencilsDir`, so it gained `internal/fabricengine/junctionnames.go`, which declares it.
  Card 10 names `stencilstore.Read`, `stencil.StripLeadingComment`, and `stencil.Fill`, so it gained `internal/stencilstore/reconcile.go` and `internal/stencil/stencil.go`.

## Pushed Back

_(none)_
