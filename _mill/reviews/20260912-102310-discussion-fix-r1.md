# Discussion fix — round 1

Source review: `_mill/reviews/20260912-101954-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `[NIT:design]` "Skill loop's 30-step cap has no stated derivation" — derived the cap from loom's real producer list: seventeen rows (`internal/loomshed`'s `Name*` constants) plus three review rounds in each of the three review segments at two steps per round (`Bouncer` + `Burler`) = 35, rounded to 40. Recorded explicitly that it sits below the ~77-step mechanical ceiling implied by `internal/loomcli/wiring.go` leaving `ShedPaths.MaxBounces` zero (inheriting `shedengine`'s default of ten per episode), and that it is an adjustable safety margin rather than a computed bound — a healthy long run can reach it, and hitting it halts and hands back without touching loom state.

## Pushed Back

None.
