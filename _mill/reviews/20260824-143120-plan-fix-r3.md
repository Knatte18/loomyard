# Fixer report: plan-fix round 3

Source review: `_mill/reviews/20260824-142848-plan-review-r3.md` (holistic, round 3, APPROVE, 0 BLOCKING / 1 NIT).

## Fixed

- r3 holistic, "[NIT:scope] Two cards name functions from a file outside their own Context" — verified against the tree: `shuttleengine.Result` is declared at `internal/shuttleengine/run.go:91` and `OutcomeDone` at `internal/shuttleengine/engine.go:15`, neither of which card 16 listed; `fabricengine.NewMutations` is at `internal/fabricengine/mutation.go:114` and `EnvSyncOptions` at `internal/fabricengine/fabric.go:104`, neither of which card 21 listed. `Context:` is an allowlist, so a symbol named in `Requirements:` whose declaring file is absent is a plan defect regardless of how fully the call is spelled out. Added `internal/shuttleengine/run.go` and `internal/shuttleengine/engine.go` to card 16's `Context:`, and `internal/fabricengine/mutation.go` and `internal/fabricengine/fabric.go` to card 21's.

## Pushed Back

_None. The single finding was verified accurate and fixed._
