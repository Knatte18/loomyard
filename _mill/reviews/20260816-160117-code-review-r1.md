MILL_REVIEW_BEGIN
# Review: Shed engine adapters: SingleLLMProducer, perch, Webster — holistic

```yaml
duration_s: 198.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-16
```

## Findings

No findings. Verified end-to-end across all four batches:

- **Batch 1** (`ctx.go`/`ctx_test.go`, `archive.go`/`archive_test.go`, `singlellm.go`/`singlellm_test.go`): `entryErr`/`cancelErr` shapes, prefixes, and `%w`-wrapping match card 1 exactly; `archiveStaleOutputs`/`firstFreeArchivePath` match card 2's rename/collision/no-op semantics; `SingleLLMProducer.Call`'s five-step order (entry check → spec source → absolute-path guard → archive → seam call → outcome mapping with empty-`OutputFiles` guard and `cancelErr` on every non-success path) matches card 3 precisely, verified against `shuttleengine.Spec`/`Result`/`Outcome` in `internal/shuttleengine/spec.go` and `engine.go`.
- **Batch 2** (`perch.go`/`perch_test.go`): `PerchRunner`/`PerchFactory` seam shapes, constructor validation order, and `resolveRunID`'s hash8-namespaced `<prefix>-<hash8>-<N>` scheme (highest-N-not-first-gap, terminal-only advancement, mandatory `MkdirAll(scratchDir)` before `TerminalOutcome`) match card 5/6 exactly, cross-checked against `perchengine.ProfileHash`/`TerminalOutcome`/`ValidRunID` (`identity.go`) and `treadleengine.TerminalOutcome` (`state.go`). `Call`'s per-Call fresh-bridge construction and outcome mapping (`OutcomeApproved`→empty-pointer `Done`, `OutcomeStuck`→`Stuck` w/ log, `OutcomePaused`→error) match card 6. Test coverage (card 7) hits every required row: mapping table, bridge-reflects-context-state, run-id advancement (terminal/non-terminal/highest-N), profile-hash namespacing, absent-scratch-sibling, corrupt state.json, constructor validation, nil-runner-from-factory.
- **Batch 3** (`webster.go`/`webster_test.go`): func-typed `WebsterRunner` seam, `Fresh: false` fixed at the call site, the three local outcome literals (`"done"`/`"stuck"`/`"paused"`) verified byte-identical to `websterengine`'s own unexported `outcomeDone`/`outcomeStuck`/`outcomePaused` constants (`outcome.go`), and the asking-error special-case (`errors.Is`/`errors.As` against `ErrMasterAsking`/`*MasterAskingError`) all match card 8. Test coverage (card 9) hits the outcome table, the error table (asking/died/timeout/busy/fingerprint/nil-batcher/plain-unmatched), the `Fresh`-always-false safety assertion, context rows, and the no-bridge assertion via `websterengine.PauseRequested`.
- **Batch 4** (`doc.go`, `manifest/designs/shed.md`, `docs/overview.md`, `manifest/roadmap.md`): `doc.go`'s sectioned godoc covers the three outcome-mapping tables, told-never-derived discipline, the perch run-id scheme, the shared cancellation rule, and all three named limitations (no-reattach, no-mid-run-bridge for SingleLLM/Webster, the standalone perch pause verb's no-op against an adapter-driven run — the last independently confirmed against `internal/perchcli/pause.go`, which writes a flag only `PauseRequested`-as-flag-check reads, never the context-bridge callback `PerchProducer` installs). All five `shed.md` corrections (banner, the "not designed yet" line, the reattach parenthetical, the "what Shed does not provide" bullet, the `SingleLLMProducer` parameterization line) are applied exactly as specified, with every other line of the file left untouched. `docs/overview.md`'s three edits (repo-tree line, module-bullet extension, Planned→implemented correction) and `manifest/roadmap.md`'s three edits (Planned-item removal + Done entry, the dangling "above" reference reworded, the Shed-skeleton Done entry's two false claims corrected and pointed at `shed.md`'s own banner) are all present and accurate.

No out-of-plan files: every file in the "Files included" manifest that this task touches appears in some batch's `Creates:`/`Edits:` list, and the union matches `01-...md`'s "All Files Touched" section exactly. No cross-batch helper duplication beyond the one explicitly named and justified in the plan (`firstFreeArchivePath`, re-implemented because `websterengine`'s own copy is unexported and the no-new-engine-surface rule forbids exporting it). No adapter calls `lyxcwd`/`os.Getwd`/git or writes `_lyx`/`.lyx` literals. Every test file is untagged, in-package, fake-driven, with no `gitexec.Run`/`exec.Command`/`gitkit.Copy*`/`hubforge.NewHub` calls (Test Tier Purity honored); the one `time.Sleep` (`ctx_test.go`, 1ms) is well under the 1s guard threshold.

## Verdict

APPROVE
Implementation matches the plan precisely across all four batches, with no constraint violations or gaps found.
MILL_REVIEW_END
