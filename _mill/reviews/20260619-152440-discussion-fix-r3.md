# Discussion fix — round 3

Source review: `_mill/reviews/20260619-152343-discussion-review-r3.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- review-r3 [NOTE] "Detached `lyx weft push` cwd-resolution unspecified" — `detached-coalesced-push` now states the detached worker is spawned with an explicit `--weft-path <abs>` arg (mirroring board's `spawnSync --board-path`), so the spawn is cwd-independent; direct `lyx weft push` still resolves from cwd.
- review-r3 [NOTE] "Host pristine vs. worktree's own config load not reconciled" — `host-pristine-enforced` gained a caveat: `lyx worktree add` loads its own `branch_prefix` via `LoadConfig(cwd)`, which requires a resolvable host `_lyx` junction at the operator's cwd; seeding that junction in the Prime is the hub-creator's responsibility, consistent with hard-require.
- review-r3 [NOTE] "`status` resilience covers broken junction, not missing weft worktree" — `weft-config-pathspec-only` now bounds the guarantee: "status still runs" applies to a broken junction with an intact weft worktree; a *missing* weft worktree fails `config.Load`/`FindBaseDir` and `status` errors loudly with a "weft worktree missing" message (the correct, distinct outcome).

## Pushed Back

(none — all three NOTEs were factually accurate, harmless to fix, and consistent with existing decisions.)
