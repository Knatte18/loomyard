MILL_REVIEW_BEGIN
# Review: fabric: clone doesn't commit written module configs — holistic

```yaml
duration_s: 339.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 ("Sonnet 5")
reviewed_file: plan/
date: 2026-08-26
```

## Findings

### [BLOCKING:scope] Card 2 Context omits clone.go despite naming CloneAndWire
**Location:** Batch 2 (fixture-empty-stage-tolerance), Card 2, `Requirements:`.
**Issue:** The `SeedConfig` doc-comment addition text names `fabriccli.CloneAndWire` ("once `fabriccli.CloneAndWire` commits the module configs it materialises, the base clone leaves the weft prime clean..."), but `internal/fabriccli/clone.go` — the file that declares `CloneAndWire` — is absent from Card 2's `Context:` list (only `pushbypass_integration_test.go` from that package is listed).
**Fix:** Add `internal/fabriccli/clone.go` to Card 2's `Context:` list.

### [NIT:consistency] Card 2 commit scope omits preflightshed
**Location:** Batch 2, Card 2, `Commit:` field.
**Issue:** The commit message `test(hubforge,preflight): allow an empty stage at the weft-prime fixture commits` edits three packages — `internal/hubforge`, `internal/preflight`, and `internal/preflightshed` — but the scope names only the first two.
**Fix:** Either extend the scope to `test(hubforge,preflight,preflightshed): ...` or drop the per-package scope list.

## Verdict

REQUEST_CHANGES
Card 2's Requirements name `fabriccli.CloneAndWire` without its declaring file in Context; everything else checks out.
MILL_REVIEW_END
