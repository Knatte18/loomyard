MILL_REVIEW_BEGIN
# Review: Custom-typed plan cards skip path-missing checks — holistic

```yaml
duration_s: 550.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-26
```

## Findings

### [NIT:consistency] Rename-group zero-bullet Refs nil guarantee is unenforced
**Location:** batch 1, card 2 (parse-time population) / card 3 (normalization)
**Issue:** Card 2's blanket claim "a type label present with zero bullets under it yields a group whose Refs is a non-nil empty slice" is guaranteed for the six non-Rename labels via `parseRefField`, but the Rename branch is told only to set `Refs` from pair endpoints (`Old` then `New`), which naturally leaves `Refs` nil rather than non-nil-empty when a Rename label carries zero (or zero well-formed) bullets — no instruction closes this gap for the Rename case specifically.
**Fix:** Either state the Rename-branch exception explicitly (Refs may be nil on zero pairs) or add an explicit "initialize to a non-nil empty slice first" instruction to card 2's renameLabel-branch text.
**Note:** Verified benign — `checkCardFieldEmpty` (card 6) keys off `len(Refs)==0`, and `slices.Equal`-based tests don't distinguish nil vs empty, so no test or check in this plan actually depends on the stated guarantee holding for Rename groups; this is a self-consistency gap in the plan's own stated invariant, not a functional defect.

## Verdict

APPROVE — plan is internally consistent, fully grounded in the current source, and its file-touch/check-count/renumbering arithmetic all verify correctly.
MILL_REVIEW_END
