MILL_REVIEW_BEGIN
# Review: fabric: collapse external API surface onto Commit — stop leaking warp/weft — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-02
```

## Findings

### [BLOCKING] Stale CommitWeftAt/PushWeftAt doc references in files this task edited
**Location:** `internal/boardengine/board.go:11-13`, `internal/gitrepo/doc.go:132`, `internal/fabricengine/coalesce.go:98`
**Issue:** These package/method doc comments still name `fabricengine.CommitWeftAt`/`PushWeftAt` — symbols batch 1 (card 4) and batch 2 (card 11) unexported to `commitWeftAt`/`pushWeftAt`. `board.go`'s package doc is the worst case: it says "the detached sync path talks to git through fabricengine.CommitWeftAt and fabricengine.PushWeftAt," but card 2 of this very batch rewrote `sync.go` to route through `fabricengine.Bolt` instead — the doc now misdescribes the mechanism this task itself replaced. `board.go` and `gitrepo/doc.go` were both directly edited by card 20/21 (for the SyncWeft/RevertWithWeft cascade) yet these adjacent stale mentions in the same files were missed; `coalesce.go` was edited by card 4 itself.
**Fix:** Update `board.go`'s package doc to describe the Bolt-routed commit/push path (not the retired direct-call shape), and fix the exported-casing mentions in `gitrepo/doc.go:132` and `coalesce.go:98` to `commitWeftAt`/`pushWeftAt`.

### [NIT] Stale RevertWithWeft mention in a still-open design-doc passage
**Location:** `manifest/designs/fabric-unified-view.md:84`
**Issue:** "So rebase-recovery = re-anchor the correspondence (the `RevertWithWeft` "nearest-older" logic is the building block)…" still names the now-deleted `RevertWithWeft` in present tense as the building block; card 29 only updated the two lines it named (~113, ~130), missing this one. The surviving building block is `resolveRevertTarget`.
**Fix:** Reword to name `resolveRevertTarget` (the kept resolver) instead of the deleted `RevertWithWeft`.

### [NIT] Doc-comment cascade left untouched in files outside the plan's edit lists
**Location:** `internal/fabricengine/spawn.go:7,75,77`, `internal/fabricengine/gogit.go:147` (gitrepo package), `internal/fabriccli/cli_test.go:431-436`, `internal/websterengine/audit.go:217`
**Issue:** These files still cite the old exported `PushWeftAt`/`CommitWeftAt` casing. None of them appear in any batch's Edits list, and the golang-comments Shared Decision deliberately scopes comment trims to touched files only, so this is consistent with the plan's own accepted scope — flagged here only because it's the same root cause as the BLOCKING item above and may be worth a follow-up sweep.
**Fix:** Optional follow-up grep-and-fix pass across the wider tree for `CommitWeftAt`/`PushWeftAt` exported-casing mentions, if a future task wants full cascade closure.

## Verdict

REQUEST_CHANGES
Implementation matches the plan end-to-end; fix the stale Bolt-contradicting doc comment in board.go before merge.
MILL_REVIEW_END
