MILL_REVIEW_BEGIN
# Review: fabric: clone doesn't commit written module configs — holistic

```yaml
duration_s: 103.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-26
```

## Findings

No findings. All three batches were checked end-to-end against the plan, the Shared Decisions, and CONSTRAINTS.md:

- Batch 1 (`configengine.ConfigFileRel`): per-segment join (`lyxdirs.LyxDirName`, `configDirName`, `module+".yaml"`), godoc matches the required content, unit test covers both module names, non-absolute assertion, and the `ConfigFile == Join(base, ConfigFileRel)` lockstep assertion. Doc entry placed correctly between `ConfigFile` and `FindBaseDir`. File stays untagged (no gitexec/exec/gitkit/hubforge calls).
- Batch 2 (`--allow-empty` fixture tolerance): all three sites (`hubforge/seed.go`, `preflight_integration_test.go`, `preflightshed/preflight_integration_test.go`) got the flag inserted before `-m` with the preceding `add` call unchanged, and each carries the required comment extension. `pushbypass_integration_test.go` was correctly left untouched.
- Batch 3 (the fix): `fabriccli/clone.go`'s `CloneAndWire` builds `relPaths` inside the existing `ReconcileAll` loop (append after `rec.Append`, as required), calls `fabricengine.CommitAnchoredPaths(rec, l, relPaths, "fabric clone: record module configs", fabricengine.SyncOptions{})` with no extra guard and no duplicate recording (`CommitWeftPaths` already appends `KindCommitCreated` at its own success site). Header comment and `CloneAndWire`'s doc comment both now enumerate the per-worktree config commit. `hubforge.Hub.Mutations` is added and populated verbatim from `res.Mutated()` in both `NewHub` call sites review-checked (the second, `TestNewHub_TeardownRemovesJunctionsKeepsTargets`, legitimately omits it since it isn't card 4's subject). `hub.go`/`doc.go` doc-comment updates describe the post-clone committed state accurately, matching what `SeedConfig`'s `--allow-empty` paragraph in `seed.go` says.
- `cloneconfigcommit_integration_test.go` (card 4) derives its module set from `configreg.Modules()` filtered to non-`"fabric"`, matches the five required tests exactly, and its mutation-record-shape assertion correctly reads the tail of the record (accounting for the fabric-config/board-commit/junction entries that precede the per-worktree loop) and cross-checks `Target` against `configengine.ConfigFileRel` and the hub-relative weft-worktree path — consistent with `mutation.go`'s `Append`/`hubRelativeTarget` conversion semantics.
- `hubforge/seed_test.go` (card 5) is in-package, untagged `TestMain` reused correctly (no duplicate `TestMain` added), and exercises both the byte-identical-seed and genuinely-different-seed halves as required.
- No out-of-plan files: every file in the `## Files included` manifest is accounted for by some batch's `Context:`/`Edits:`/`Creates:` list.
- No global utility duplication observed; `CommitAnchoredPaths`/`CommitWeftPaths` are the single commit path used, and `ConfigFileRel` has exactly one call site pattern reused everywhere it's needed.
- Cross-batch contracts hold: batch 3 consumes `ConfigFileRel` (batch 1) and depends on the `--allow-empty` tolerance (batch 2) exactly as the DAG states, and the fixture files read as already carrying the tolerance before the clone-commit's stricter cleanliness requirement would otherwise break them.

## Verdict

APPROVE
Implementation matches the plan, Shared Decisions, and CONSTRAINTS.md across all three batches with no gaps found.
MILL_REVIEW_END
