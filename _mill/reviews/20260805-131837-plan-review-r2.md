MILL_REVIEW_BEGIN
# Review: fabric: shrink hubgeometry to the minimal illusion primitive (slice 7) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (Anthropic), self-identified as "Sonnet 5" per system context
reviewed_file: plan/
date: 2026-08-05
```

## Findings

### [BLOCKING] New integration tests are never executed by any batch's verify
**Location:** batch 7 card 40 (`internal/fabricengine/boardjunction_integration_test.go`); batch 2 card 7's stale-`.fabric-anchor` addition to `clone_adopt_test.go`
**Issue:** Every `verify:` in this plan pairs `go vet -tags "integration smoke scout" ./...` (type-check only) with an *untagged* `go test`; no batch, and not even the stated `pipeline.done_gate` (`go test ./...`), ever runs `go test -tags integration`. New runtime assertions — the `_board` junction wiring/health/exclusion behavior and the stale-marker hard-error path — are therefore only ever type-checked, never executed, by anything this plan's own gates do.
**Fix:** Add at least one batch (7 or 8) whose `verify` includes a `-tags integration` execution of the touched packages (e.g. `go test -tags integration ./internal/fabricengine/...`), or explicitly document in the plan why running Tier 2 is out of scope and where/when it is expected to run before merge.

### [NIT] Batch 7's own "Batch Tests" section concedes the reachability gap without resolving it
**Location:** batch 7, `## Batch Tests`
**Issue:** The text states card 40's coverage "is reached by the tagged `go vet` … and by `-tags integration` runs, not by the untagged `go test`" — accurately describing the gap above but treating it as settled rather than flagging it as a residual risk to close.
**Fix:** Either fold this into the same fix as the BLOCKING finding above, or add a one-line TODO/handoff note naming who runs Tier 2 before this slice merges.

## Verdict

REQUEST_CHANGES
No batch ever executes the new `_board`/stale-anchor integration tests it adds; add a `-tags integration` run somewhere in the gate chain.
MILL_REVIEW_END
