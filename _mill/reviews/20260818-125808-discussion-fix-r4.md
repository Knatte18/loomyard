# Discussion fix — round 4

Source review: `_mill/reviews/20260818-125732-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260818-125732-discussion-review-r4.md` — "[NIT:decision] seeded servers.yaml must satisfy validateEntry".
  Verified against `internal/scoutengine/registry.go:110-124`: `validateEntry` requires non-empty `Markers`, `Match` ∈ {`all`,`any`}, non-empty `Command`, non-empty `InstallHint`, and `LoadRegistry` decodes under `KnownFields(true)`.
  The Testing section now names all four required keys with their YAML tags, flags the `install_hint` snake_case tag, notes the unknown-key strictness, and points at `builtins()`'s `"go"` entry (`registry.go:64-67`) as the shape to copy.
- `20260818-125732-discussion-review-r4.md` — "[NIT:scope] test-comment enumeration misses refs_integration_test.go:196".
  Verified: line 196 reads "// layout is an isolated temp dir so the supervised daemon this".
  Added to the known-sites list, and the list is now explicitly marked non-exhaustive with the closed rule plus pre-commit grep named as the authority.

## Pushed Back

None.
Both findings verified accurate against current source, and neither fix breaks existing functionality, conflicts with a documented design decision, nor destabilises work outside this task's scope.
