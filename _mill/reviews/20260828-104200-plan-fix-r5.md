# Plan fix — round 5

Source review: `_mill/reviews/20260828-103946-plan-review-r5.md` (verdict APPROVE, 0 BLOCKING, 1 NIT)

## Fixed

- r5 "[NIT:scope] doc.go named in card 6 Requirements but absent from Context" — verified: card 6's rationale paragraph names `apply.go`, `attach.go`, and `doc.go` as the files whose existing `liveBoxLocked` doc comments stay accurate under an in-place widening, but only `attach.go` was in its `Context:` (`apply.go` is one of its own `Edits:`).
  Added `internal/reedengine/doc.go` to card 6's `Context:`.
  No edit to `doc.go` is required by card 6 — the widened signature does not invalidate its name-only reference — so it stays out of `Edits:`; batch 4's card 21 remains the only card that edits it.

## Pushed Back

_None. The finding was factually accurate and the fix breaks nothing._
