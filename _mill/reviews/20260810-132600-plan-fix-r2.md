# Plan fix — round 2

Source review: `_mill/reviews/20260810-132120-plan-review-r2.md` (holistic, 1 BLOCKING, 1 NIT).

## Fixed

- `20260810-132120-plan-review-r2.md` — "[BLOCKING:design] Card 3's `dirtyReason` treatment rests on an unachievable premise".
  Accurate and verified against both the card and the tree: `dirtyReason` carries a spawn-failure format and a distinct nonzero-exit format, while card 1 defines the shared probe to return one consolidated error for both, so "keep its two error formats" could not be satisfied.
  Card 3 now makes the same concession card 2 already makes for the two sites above it — keep the spawn-failure wording, let the exit code arrive inside the wrapped error — and explicitly forbids the tempting alternative of widening the probe's return shape for one caller.
- `20260810-132120-plan-review-r2.md` — "[NIT:consistency] Batch 1's blanket \"preserved verbatim\" claim contradicts Card 2's own text".
  Accurate: the scope paragraph's blanket claim and card 2's stated collapse could not both be true, and an implementer reading the scope first would have taken the wrong one as authoritative.
  The scope paragraph now claims verbatim preservation for refusal *messages* only, states the error-path collapse as the single uniform exception, and names all four sites it applies to.

## Pushed Back

_None._
Both findings verified accurate;
neither fix breaks existing functionality, conflicts with a documented decision, or destabilises other work.
