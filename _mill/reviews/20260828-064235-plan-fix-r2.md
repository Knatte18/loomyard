# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260828-063937-plan-review-r2.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260828-063937-plan-review-r2.md` [BLOCKING:scope] "\"covered\" verdicts conflate file-level import with per-call spawn logging" — verified accurate at every site the finding names. `internal/fabricengine/spawn.go`'s `SpawnDetachedPush` logs a `Warn` on `Start` failure and announces no spawn; `internal/reedcli/attach.go`'s only `logger` call is an unrelated terminal-size warning above its tmux-attach `exec.Command`; `internal/loomcli/run.go` logs its `loom drive` spawn at `logger.Info("loom: spawned detached driver", …)` but leaves its tmux-attach spawn unlogged. Each would violate card 2's own sharpened invariant text on landing, and batch 5's file-level guard structurally cannot see any of them. Took the finding's first branch (give them an `add` verdict) rather than its second (record an out-of-scope exemption) — a "pre-existing, out of scope" note would leave the task shipping an invariant three of its own rows contradict. Added card 12 to batch 3 covering all three sites, re-verdicted the three rows in card 1's spawn table, added a **What "covered" means here** subsection to card 1's required document structure, added the `covered-is-a-per-call-claim` decision to the overview's Shared Decisions, extended batch 3's `verify:` with `./internal/fabricengine/ ./internal/reedcli/ ./internal/loomcli/`, and added the three files to All Files Touched. Cards 12-14 in batches 4 and 5 renumbered to 13-15 to keep global card numbering monotonic across batches.
  Also verified the finding's implicit question about the remaining `covered` rows rather than only the three it named: `internal/reedengine/lifecycle.go`'s `spawnSession` logs both an `Info` on spawn and a `Warn` on `Start` failure, and `internal/reedengine/overlay.go`'s `TmuxCmd.run`/`TmuxCmd.output` each log the argv at `Debug` immediately before spawning. Both are genuinely covered per-call; card 1 now records why `Debug` is the right level for the `overlay.go` pair so a later reader does not re-litigate it.
- `20260828-063937-plan-review-r2.md` [NIT:consistency] "Wrong enclosing function named for mergeresolve's Warn insertion" — verified accurate: the `!= shuttleengine.OutcomeDone` branch and the `abortAndStuck` call are inside `(*Resolver).resolveConflicts`, not `(*Resolver).Resolve`. Corrected the named enclosing function in card 4 of `02-hard-error-warn-lines.md`.

## Pushed Back

_None._
Both findings were factually accurate against the cited source. Neither fix breaks existing functionality, and the scope growth in the BLOCKING fix is required by `_mill/discussion.md`'s own `error-universe` instruction to regenerate the tables from a re-run rather than trust the transcribed rows — the discussion's `spawn-site-verdicts` table is explicitly a snapshot, not the authority.
