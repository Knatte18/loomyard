# Discussion fix — round 3

Source review: `_mill/reviews/20260812-174134-discussion-review-r3.md` (APPROVE, 3 NITs, 0 BLOCKING).

## Fixed

- r3 "`CopyRepo` caller set stated two ways" — the Decision bullet said `internal/lyxcwd` alone while its own Rationale two lines later said `lyxcwd` and `gitrepo`, and the already-applied CONSTRAINTS text said the fixtures "serve `internal/gitrepo` and `internal/lyxcwd` only".
  Reconciled to one statement in both files: the `CopyRepo` guard allowlist is `internal/lyxcwd` alone;
  `gitkit`'s other helpers are unpinned, and `internal/gitrepo` is a `MustRun` consumer with no fixture call.
- r3 "Build-tag disposition of the merged `hubforge` files unstated" — added a Decision: production untagged (as `internal/lyxtest/lyxtest.go` is today), git-spawning tests `//go:build integration`, one untagged `doc.go` per `fabrictest`'s own existing pattern.
  Records why untagged production is safe (tier purity bans by token, not build tag) and that all 132 current `Copy*` sites already sit in tagged files.
- r3 "`SeedFabricConfig` commit behaviour left open" — stated that it commits via `fabricengine.NewBolt(BoardDir).Commit(...)`, the same path `CloneAndWire` uses at `internal/fabriccli/clone.go:57-58`, and why leaving the board dirty would change verb outcomes in fabric's live-state cells.

## Pushed Back

None.
