# Plan fix — round 1

## Fixed

- `20260825-141224-plan-review-r1.md` — "[NIT:consistency] Guard's fix-scope/commit_seam failures not pinned to non-fatal reporting": both the `fix-scope` and the `commit_seam` halves of card 1's guard rule now name `t.Errorf` explicitly and state why non-fatal reporting is required (step 2 needs one run to surface both failures).
- `20260825-141224-plan-review-r1.md` — "[NIT:consistency] \"CommitPlan ... immediately below\" is spatially inaccurate in wiring.go": verified against `internal/loomcli/wiring.go` — the `PlanSpec` closure sits between `CommitDiscussion` and `CommitPlan`. Card 2 now says "further down in the same struct literal" and names `PlanSpec` as what sits between them.

## Pushed Back

_None. Both findings verified accurate against source and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task._
