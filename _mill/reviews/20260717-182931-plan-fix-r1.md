# Plan-fix report — round 1

Source review: `_mill/reviews/20260717-182838-plan-review-r1.md` (verdict APPROVE, 2 NITs).

## Fixed

- `20260717-182838-plan-review-r1.md` — **[NIT] Card 10 cites builderengine/spawn.go not in Context**: changed Card 10's Requirements to cite "the exact mapping documented verbatim in `builderengine/roles.go`'s godoc" (dropped the uncited `spawn.go` reference; `roles.go` — already in Context — documents the `Model/Effort/Version` mapping).
- `20260717-182838-plan-review-r1.md` — **[NIT] Card 6 mixes an integration pattern with a Tier-1 intent**: rewrote Card 6 to require an explicitly **untagged** Tier-1 unit test with inline `t.TempDir()` seeding (`MkdirAll(_lyx/config)` + `WriteFile(loom.yaml, ConfigTemplate())` + `LoadConfig`), and to NOT mirror `builderengine/config_test.go` (which is `//go:build integration` and would escape the batch's plain `go test` verify). Swapped `builderengine/config_test.go` out of Context for `internal/configengine/config.go` (the baseDir-layout reference).

## Pushed Back

- None.
