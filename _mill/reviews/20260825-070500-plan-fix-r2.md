# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260825-070103-plan-review-r2.md`

## Fixed

- `20260825-070103-plan-review-r2.md` — "[BLOCKING:scope] Card 2's Context omits fetch_test.go" — verified; `redditLikeHTMLWithComments` and `readableArticleHTML` are both declared in `plugins/prowler/fetch_test.go`, which card 2 never listed. Added it to card 2's `Context:` and reworded the requirement to name the file explicitly.
- `20260825-070103-plan-review-r2.md` — "[NIT:consistency] Fetch's stale doc comment not scheduled for update" — verified against `plugins/prowler/reddit.go:29-30,39-41`. Card 9 now requires updating `redditAdapter.Fetch`'s doc comment and the `redditAdapter` type doc comment alongside the package comment.
- `20260825-070103-plan-review-r2.md` — "[NIT:consistency] Batch 3's regression-coverage claim overstates its own `-run` filter" — verified; `TestFormatRedditThread` and `TestFetchRedditOAuthThread` were unmatched. Took the reviewer's stronger option: both names are now in batch 3's `verify:` filter (in the batch file and in the overview Batch Index), and the `## Batch Tests` prose is reworded to state what the filter actually covers.

## Pushed Back

_(none)_
