# Plan fix — round 5

Source review: `_mill/reviews/20260814-174312-plan-review-r5.md` (REQUEST_CHANGES, 2 BLOCKING, 0 NITs).

## Fixed

- r5 / "[BLOCKING:design] Card 13 deletes `wireBoardLink` before card 14 removes its three call sites" — verified against source: `clone.go:407`, `add.go:205` and `reconcile.go:397` all still call `wireBoardLink`, and none is touched until the call-site card. Deleting the definition first yields a commit that fails to compile with "undefined: wireBoardLink". Reordered so the call-site removal is card 13 and the definition deletion is card 14, and stated the constraint in card 14's own Requirements with the reason the reverse order is safe (Go accepts an unused package-level function).
- r5 / "[BLOCKING:design] Card 15 deletes `unwireBoardLink`/`BoardJunctionRemoved` before cards 16-17 remove the remaining references" — verified against source: `remove.go:104` calls `unwireBoardLink` and `internal/fabriccli/unwire.go:34` reads `res.BoardJunctionRemoved`, so deleting the definitions first breaks two packages across two intervening commits. Reordered so `remove.go` is card 15 and the `fabriccli` envelope key is card 16, with the definition deletion last at card 17; card 17's Requirements now state the constraint and why the reverse order is safe (Go accepts both an unused package-level function and an unread struct field). Added `remove.go` and `fabriccli/unwire.go` to card 17's Context, since its Requirements now name them.

Cross-references updated to match the renumbering: card 16's own note about its asserting test, card 20's "fails without card 15", card 21's "envelope key card 16 removes", and both references in `## Batch Tests`. Cards 18-25 keep their numbers.

## Pushed Back

_(none — both findings were factually accurate against source, and neither fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task.)_
