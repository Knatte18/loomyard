# Plan fix — round 2

Source review: `20260813-055640-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 0 NITs).

## Fixed

- `20260813-055640-plan-review-r2.md` — "[BLOCKING:design] Batch 4 card 27 gives no per-site resolution for perchcli's three ad-hoc SeedConfig sites": verified accurate and fixed in `04-small-consumers.md` card 27, which now carries an explicit resolution per site rather than the general triage rule.
  Both sites hand-build the anchor a real hub records for itself, so both resolve the same way — build at the real anchor, delete the scaffolding:
  - `TestRunCLI_Pause_NestedInitAnchorsRunDirsAtCwd` builds with `hubforge.NewHub(t, "nested")`;
    its `os.MkdirAll` of the nested dir, its `os.MkdirAll` of `BoardDir`, and its hand-written `.lyx-anchor` are all deleted, and the local `nested` variable becomes `h.Location.AnchorPath()`.
  - `TestRunCLI_Run_FabricCommitExcludesLockFiles_NestedRelPath` builds with `hubforge.NewHub(t, "wts/some-task")`;
    the local `seedFabricAnchor` helper is deleted outright (this test is its only caller), `warpSubdir` and the `t.Chdir` argument become `h.Location.AnchorPath()`, and the run-dir join off `WeftPrime` becomes `h.WeftBase`.
  - Both `gitkit.SeedConfig` calls pass three plain registered templates (`shuttleengine`/`reedengine`/`perchengine` `ConfigTemplate()`), so both are triage outcome 1 — deleted, not retargeted.
  - The finding also surfaced the adjacent `seedRepoWideFabricConfig` helper (five call sites in the same file), which the review named as the same class of hack.
    It writes a genuine override (`branch_prefix: ""`, `pathspec: _lyx`), so it is triage outcome 3: all five calls become `hubforge.SeedFabricConfig(t, h, …)` and the helper is deleted.
    Card 27 records why this matters beyond tidiness — the hand-rolled helper never commits, leaving the `weft:main` checkout dirty, which `Fabric.Commit` (this file's own subject) observes.
  - Verified `lyxcwd.ValidateAnchorRel` accepts a multi-segment relative anchor with no existence requirement, so `"wts/some-task"` is a legal `Subpath`;
    card 27 states the `os.MkdirAll(h.Location.AnchorPath(), 0o755)` fallback and why it is ordinary arrangement rather than stand-in-hub scaffolding.

## Also fixed (same finding's traceability half, not separately raised)

- `00-overview.md`'s three-way `SeedConfig` triage decision now names where each of the three ad-hoc sites is resolved, so a reader of the decision is pointed at the per-site cards instead of being left to discover that the general rule does not cover them.
- `05-reedcli.md` card 33's `sibling` site was resolved from a conditional ("if the base is X … if the base is Y") to a definite instruction, having verified the base: `internal/reedcli/smoke_test.go` builds it two lines earlier as a second clone via `gitkit.MustRun(… "git", "clone" …)`, so it is a plain repo and `gitkit.SeedConfig` stays exactly as written;
  only the clone's source argument moves onto `h.WarpBare`.

## Pushed Back

_None._
