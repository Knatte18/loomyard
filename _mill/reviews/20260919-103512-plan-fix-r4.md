# Fixer report: plan-fix r4

Source review: `20260919-102858-plan-review-r4.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 2 BLOCKING + 1 NIT).

## Fixed

- r4 holistic — `[BLOCKING:design] ReapSession's throwaway Geometry literal bypasses the sole-constructor rule` — verified accurate on both halves: CONSTRAINTS.md's Told-Geometry Invariant names `internal/hubgeom`/`internal/standalonegeom` as the only `Geometry`-struct constructors, and a repo grep confirms the only production `Geometry{` literals live in those two packages (`hubgeom/hubgeom.go`, `hubgeom/webstergeom.go`, `standalonegeom/reedgeom.go`, `standalonegeom/burlergeom.go`, `standalonegeom/webstergeom.go`); every other hit is a `_test.go` fixture.

  Took neither remedy the finding offered. Instead of routing through a sanctioned constructor (unreachable — `hubgeom.ReedGeometry` needs a `*lyxcwd.Location`, which `reedengine` may not import and which cannot exist for a deleted worktree) or carving out a CONSTRAINTS.md exemption, the literal is removed outright: `descendantClosurePIDs` reads no geometry on either platform. Its Linux body touches no `Engine` field at all, and its Windows body reads only `e.cfg.Shell`. Card 2 now specifies `&Engine{cfg: Config{Shell: shellPath}}` — one field, no `Geometry` value of any kind, not even a zero one — and states in the doc-comment requirement why. This leaves the invariant with zero new exceptions rather than one documented exception.

- r4 holistic — `[BLOCKING:scope] Card 13 calls Engine.ShellPath() without lock.go in Context` — verified accurate: `Engine.ShellPath` is added by card 1 to `internal/reedengine/lock.go`, card 13's updated call sites invoke it, and card 12 already lists that file for the same reason while card 13 did not. Added `internal/reedengine/lock.go` to card 13's `Context:`.

- r4 holistic — `[NIT:consistency] Card 18's Windows-degradation rationale doesn't hold on the platform the test runs on` — verified accurate: `internal/reedengine/proctree_linux.go`'s `descendantClosurePIDs` never reads `e.cfg.Shell`, so on the platform this suite runs an empty shell changes nothing about the closure. Card 18's rationale reworded: the roots-only assertion is now justified as keeping the case portable — asserting the guarantee the empty-shell configuration makes on every platform rather than one that only happens to hold on this one — with the Linux behaviour stated correctly and the degradation attributed to the Windows body alone. Added `internal/reedengine/proctree_linux.go` to card 18's `Context:` to back the claim.

## Pushed Back

_None. All three findings verified accurate against the repository, and no fix triggered a harm-check escape._
