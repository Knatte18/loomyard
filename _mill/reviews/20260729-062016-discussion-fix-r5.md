# Discussion-fix round 5 — codeintel-v1

Source review: `_mill/reviews/20260729-061939-discussion-review-r5.md` (verdict: APPROVE, 2 NOTEs).

## Fixed

- `_mill/reviews/20260729-061939-discussion-review-r5.md` — "Stale unix-socket cleanup on supervised restart": added a sentence to `supervised-reconnect-transport` stating the kill-and-restart path `os.Remove`s the old socket file before rebinding the same recorded path (avoiding `EADDRINUSE`), and that reusing the same path keeps the state file's `address` field stable across restarts.
- `_mill/reviews/20260729-061939-discussion-review-r5.md` — "Zero-valued (no-daemon) mode teardown not spelled out": added a sentence to `ensure-server-call-site` confirming the "never close the daemon-owned connection" rule is `supervised`/`native`-only; the zero-valued (Python/C#/TypeScript/Rust) path never goes through `EnsureServer`'s daemon machinery and keeps today's caller-owned close/kill unchanged.

## Pushed Back

None.
