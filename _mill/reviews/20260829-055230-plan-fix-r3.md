# Fixer report: plan-fix round 3 — reed-pane-reap-consistency

Source review: `_mill/reviews/20260829-054240-plan-review-r3.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 4 NIT).
Every finding was verified against the real source files before acting;
all six were accurate, and all six were fixed.

Note on this round's dispatch: the first agent-mode attempt died on a raw API error before producing any verdict, and was re-dispatched once per the Agent-mode transient-error rule.
No review file was written by the failed attempt and no round was consumed by it.

## Fixed

- r3 holistic, `[BLOCKING:scope]` "Card 3's log test is unnamed; verify filter can miss it" — confirmed: batch 1's `verify:` filters on `-run 'TestPlanReconcile|TestReconcileLocked'`, and `go test -run` exits 0 when its pattern matches nothing, so an unnamed test could have landed outside the filter and left the new `logger.Info` line with no gate whatsoever. Card 3 now names it `TestReconcileLocked_LogsTheUntrackedPanesItReaps` and states why the prefix is load-bearing rather than stylistic, matching how cards 7, 15 and 16 already name theirs.
- r3 holistic, `[BLOCKING:scope]` "Card 12's Context omits a file its own sweep hits" — confirmed at `internal/reedengine/reconcile_test.go:136`: the second sweep's `untracked reap` term hits the `DeadHeaderPaneKeptNotKilled` comment, which card 12 must disposition but could not read. Added `internal/reedengine/reconcile_test.go` to card 12's `Context:`, and named that comment as a legitimate second-sweep survivor — it describes the header's exemption from the dead-pane kill, which this task does not change and card 2 preserves.
- r3 holistic, `[NIT:consistency]` "Card 2's fifth new case duplicates an existing one" — confirmed: the requested "alive header alongside a bound strand, third foreign pane reaped" shape is exactly `HeaderPaneNeverReapedAsUntrackedWhileStrandBound` (`reconcile_test.go:114-122`), which card 1 preserves unchanged. Restated from "add" to "confirm it still passes rather than adding a duplicate of it".
- r3 holistic, `[NIT:consistency]` "Card 7 names the wrong model test for argv capture" — confirmed: `TestEnsureHeaderPaneLocked_RebuildRejectsSilentSplitFailure` scripts the hook but captures no argv;
  the `splitArgs` capture lives in `TestEnsureHeaderPaneLocked_SplitsWithPaneCwdNotAnchorPath`. Card 7 now cites both, one per half — hook scripting from the first, argv capture from the second.
- r3 holistic, `[NIT:scope]` "Card 17 misses the third adoption-named site on the same line" — confirmed at `internal/reedcli/smoke_panecwd_test.go:60`: the `--name` argument's value is the string `"adopted"`, a live strand name rather than a comment, on the same line as the `adopted` local. Card 17 now enumerates three sites and flags that this one is code, not prose, so card 12's grep reaches it.
- r3 holistic, `[NIT:scope]` "Card 12's survivor list omits spawn.go's generation-probe call" — confirmed: after cards 4 and 6, `spawn.go`'s only remaining "adopt" hit is the `e.adoptPaneGenerationLocked(st)` call in `loadOrInitStateLocked`. Added to the enumerated survivor list, so the hit reads as justified against the list rather than only against the card's governing rule.

## Pushed Back

_None._
All six findings were factually accurate, and no fix broke existing functionality, conflicted with a documented decision in `_mill/discussion.md` or `CONSTRAINTS.md`, or destabilised work outside this task's scope.

## Post-fix validation

`_plan_dag.validate` passes.
`_plan_validate.run` returned zero findings with an empty `skip_checks`.
