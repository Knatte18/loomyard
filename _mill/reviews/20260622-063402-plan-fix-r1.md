# Plan fixer report — round 1

Source review: `20260622-063328-plan-review-r1.md` (verdict APPROVE, 3 NITs).

## Fixed

- `20260622-063328-plan-review-r1.md` — **[NIT] Card 6 fallback trigger understates the
  tasks.json mismatch**: rewrote Card 6's fallback paragraph from "only if" to "likely needed
  — evaluate first", spelling out that `CopyWeft` seeds `_lyx/config.yaml` but not
  `tasks.json` and that `TestSyncCleanTreeIsNoOp` assumes a clean tree after the board
  `.gitignore` commit, so the `CopyBoardRepo` path is the expected outcome if assertions
  diverge. Helps the implementer not waste cycles forcing `CopyWeft`.
- `20260622-063328-plan-review-r1.md` — **[NIT] Card 8 RunCLI reachability vs CopyHostHub
  layout**: added a note to Card 8 that `RunCLI` calls `paths.Resolve(cwd)` and that
  `CopyHostHub.Hub` has no `_lyx`, so `CopyPaired.Hub` (full `Layout`) is the next choice,
  with the gated-but-unmigrated fallback if neither reaches spawn-dispatch.

## Pushed Back

- `20260622-063328-plan-review-r1.md` — **[NIT] Batch 4 verify scope excludes boardtest**:
  no change. The finding itself states the `verify: go test ./internal/board` scope is
  "intentional and correct" for the Tier-1 unit folds and is "covered by batch 5's whole-repo
  two-tier gate". Adding `/...` would re-run the relocated `boardtest` integration tests on
  every board-prune fixer round for no benefit (board-prune touches only `render_test.go`/
  `store_test.go`), violating the per-batch verify-scoping principle. Batch 5's
  `go test ./... -count=1 && go test -tags integration ./... -count=1` is the whole-suite
  gate. Legitimate pushback: fix conflicts with a documented design decision (per-batch verify
  scoping) and the reviewer's own assessment.
