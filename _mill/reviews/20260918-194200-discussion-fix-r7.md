# Discussion fix — round 7

Source review: `_mill/reviews/20260918-194053-discussion-review-r7.md` (APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r7 — "Testing contradicts absent-ref substring decision": the Testing section still said "cover both stderr spellings git uses" after round 6 settled on one pinned substring.
  Rewrote the line to match the decision — one case, one substring (`remote ref does not exist`), asserted against a real `git push --delete` so it acts as a tripwire on a future git rewording.
- r7 — "No-origin pre-check's gating on `remote` is unstated": added the explicit `remote == true` guard to the decision, with the reason — the fields maps emit their keys unconditionally, so an unguarded pre-check would make a plain `cleanup --apply`/`remove` report a non-empty `remote_skipped_reason` on a path this task does not otherwise touch.
- r7 — "Mixed/local-only synthesised error string left unformatted": pinned the local-only and combined `fmt.Errorf` formats the same way the remote-only and `remove` formats were already pinned, since the local-only exit is newly observable output this task introduces.

## Pushed Back

(none — all three were internal inconsistencies introduced by earlier rounds' own fixes, and none met a Decision-Tree harm category.)
