# Discussion fix — round 4

Source review: `_mill/reviews/20260918-193543-discussion-review-r4.md` (APPROVE, 0 BLOCKING, 3 NIT, 2 demoted).

## Fixed

- r4 review, "[NIT:scope] shedbuild's every-engine fixture unaccounted for" — verified in source: `internal/shedbuild/build_engines_test.go` drives `TestBuild_EveryRegisteredEngineBuilds` off `shedrecipe.Names()`, and `fixture_test.go`'s `newTestEnv` doc comment states a new entry with a new required seam fails it until the fixture covers it. Added `internal/shedbuild`'s `newTestEnv` to Scope as an in-scope edit and added a "The second registry-wide test" paragraph to Technical context naming the four seams to fill and the `engineMinimalConfig` follow-on.
- r4 review, "[NIT:consistency] Non-prime refusal test placed in Tier 1, but it spawns git" — verified: the check reaches `gitexec.Run` via `PrimeName` → `List`, and `lyxcwd.Resolve` shells out to `git rev-parse --show-toplevel`. Moved the case to the `integration` tier in Testing, and split the refusal *decision* into a pure helper so the table coverage stays Tier 1.
- r4 review, "[NIT:decision] `PrimeName` error disposition unstated" — verified: `PrimeName` returns `(string, error)` and fails on an unreadable list or a missing main entry. Recorded the disposition in `lifecycle-driver-runs-from-prime`: refusal envelope on both verbs, failing closed, with a note on why `refusePrimeSlug`'s opposite treatment of the same failure is deliberate rather than an inconsistency.

## Pushed Back

None. All three findings were factually accurate and no fix triggered a harm-check escape.
