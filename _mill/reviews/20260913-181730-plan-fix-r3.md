# Plan fix — round 3

Source review: `_mill/reviews/20260913-181500-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 0 NIT).

## Fixed

- r3 holistic — "[BLOCKING:scope] Batch 4 breaks an existing integration test its own verify never runs".
  Verified: `internal/stencilcli/cli_integration_test.go:118` asserts the returned entry count equals the stencils registry's own name count, so card 14's widening of `list` breaks it — and batch 4's tagged verify deliberately excluded that package, which would have hidden the regression until the repository-wide done gate.
  Added the file to card 14's `Edits:` with a requirement to change the assertion to the sum of both registries' name counts and to extend the per-name loop so the specs names are checked rather than silently ignored, plus one new assertion the widening makes worth having: every row's kind label matches, and each label's row count matches its own registry's name count, so a future change that files one registry's rows under the other's label fails there.
  Widened batch 4's tagged verify to include `./internal/stencilcli/...`, synced in both the batch frontmatter and the overview's Batch Index, and rewrote that batch's Batch Tests paragraph, which previously asserted the package had no integration-tagged test in scope — no longer true.

## Pushed Back

_None._
The finding verified as factually accurate against the cited line, and the fix breaks no existing functionality, conflicts with no documented decision in `_mill/discussion.md`, and destabilises nothing outside this task.
