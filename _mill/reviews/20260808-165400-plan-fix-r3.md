# Plan fix — round 3

Source review: `_mill/reviews/20260808-164956-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 3 NIT).

## Fixed

- `20260808-164956-plan-review-r3.md` — "[BLOCKING] Card 13's line-464 requirement is self-contradictory".
  Accurate, and it is residue from round 1's own fix: the r1 edit appended the corrected instruction without removing the superseded `"_extra"` clause from the sentence above it, leaving two conflicting targets for the same line.
  The first sentence now names `[]string{lyxdirs.LyxDirName, lyxdirs.DotLyxDirName}` as the sole target, and the following paragraph explains why `_extra` is wrong here rather than restating a second target — an `_extra` assertion would name a junction nothing ever wires, since the fixture clones from an empty bare weft and batch 4's default is empty.
- `20260808-164956-plan-review-r3.md` — "[NIT] Card 20 mislabels a line as 'the file header comment'".
  Verified: `junctionnames_test.go`'s header is lines 1-8; line 19 sits inside `TestJunctionNames_NoFallbackOnLoadFailure`'s doc comment ("never silently defaulted to `_lyx`/`_pattern`").
  Card 20 now describes it correctly.
- `20260808-164956-plan-review-r3.md` — "[NIT] Card 38 attributes a two-line quote to a single line number".
  Verified: the phrase runs across `doc.go` lines 84-85, with `_raddle`) closing on 85.
  Card 38 now cites lines 84-85.
- `20260808-164956-plan-review-r3.md` — "[NIT] Card 36 misnames the mechanism behind finalize.md's line-29 example".
  Verified: line 28 attributes the call to `fabric.CommitWeft`'s pathspec parameter; `ScopedPathspec` is a different helper.
  Card 36 now calls it the `fabric.CommitWeft` pathspec example and adds an explicit instruction not to rename the mechanism while editing the literal.

## Pushed Back

None.
All four findings were factually accurate and none of the fixes breaks existing functionality, conflicts with a documented decision, or destabilises work outside this task.

## Re-validation

`_plan_dag.validate` — OK.
`_plan_validate.run` — 0 findings.
