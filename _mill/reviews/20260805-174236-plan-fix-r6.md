# Plan fix — round 6

Source review: `_mill/reviews/20260805-174236-plan-review-r6.md` (holistic, REQUEST_CHANGES, 4 BLOCKING + 2 NIT, reviewer `fablehigh` — self-review round: the fixer is the reviewer, so every finding was re-verified against the tree before being applied, and the coordinator independently confirmed all four blockers). All six findings processed through `mill-receiving-review`'s decision tree: all accurate, none hit a harm category, all six fixed, none pushed back.

## Fixed

- "[BLOCKING] Card 2 deletes config symbols with the module's own unit test uncovered". Re-verified every site: `hubgeometry_unit_test.go` is in-package and untagged, calling `ConfigDir` (`:23`), `ConfigFile` (`:36`), `DotEnv` (`:96`) and the exported `LyxDirName` at `:24,37,49,61,73,85,109`. Added the file to card 2's `Edits:` with a per-sub-test coverage decision, checked against the destination packages' existing tests as instructed: `configengine/config_test.go` exercises the config path only **indirectly** through `Load` (its tests are `TestLoad_*`/`TestFindBaseDir_*` — no direct path-math assertion exists), so the `ConfigDir`/`ConfigFile` sub-tests and `TestLyxDirNameConstant` **move** there rather than being deleted; `envsource/envsource_test.go` covers `.env` parsing and OS overlay via locally-joined paths (`TestBuild_DotEnvParsing` builds `filepath.Join(tmpDir, ".env")` itself) but never pins `DotEnv(baseDir)`'s join, so the `DotEnv` sub-test **moves** there — `envsource_test.go` added to card 2's `Edits:` and to All Files Touched (the round's only new union path). The four surviving sub-tests' want-expressions retarget onto the private transitional `lyxDirName` card 2 itself declares — the file is in-package, so no import and no cycle question.
- "[BLOCKING] Card 3's fallout enumeration misses two sub-tests in files it lists". Re-verified `geometry_test.go:180` (`Prime:` literal) and `:191-197` (`WeftRepoRoot()` parity assertion) inside `TestWeftLayoutMethodParity`, and `hubgeometry_unit_test.go:151-192` (`TestDeriveRepo` calling the deleted `deriveRepo`). Card 3's "four test files" corrected to six, with both dispositions named: the parity test drops `Prime:` and deletes only its `WeftRepoRoot` block (its `WeftWorktreePath`/`WeftWorktree` halves survive until card 31, whose lift covers the file), and `TestDeriveRepo` is deleted whole with its subject, the `-HUB`-trim replacement being asserted by `hubgeometry_test.go`'s rewritten `:63-70` block.
- "[BLOCKING] Card 34 privatizes BoardDir/IsReservedHubName with two lyxcwd test files unlisted". Re-verified `anchor_test.go:26` (external `hubgeometry_test` package, integration-tagged) and `lyxcwd_test.go:786-791` (`TestIsReservedHubName_Pattern`). Both files added to card 34's `Edits:` with rewrites named: `anchor_test.go`'s `writeAnchor` helper takes `fabricengine.BoardDir(hub)` — legal from an external-package lyxcwd test, and the geometry guard skips `_test.go` files (verified at `enforcement_test.go:410`), so no literal is needed; `TestIsReservedHubName_Pattern` plus `geometry_test.go`'s `TestBoardDir` (`:48-76`), `TestHubPath` (`:79-109`) and `TestIsReservedHubName` (`:208-289`) — the previously undispositioned sub-tests — move into `fabricengine/junctionnames_test.go` (in-package, untagged, already in the card's `Edits:`), preserving the coverage with the symbols.
- "[BLOCKING] Card 35 deletes HostPatternLink(Here) with two caller files unlisted". Re-verified `reconcile_stale_removal_test.go:113,273,380` and `remove_junctions_integration_test.go:69`, plus the accessor bodies at `hubgeometry.go:502-545` so the prescribed joins are exact. Both files added to card 35's `Edits:` with the generic-join rewrites named: the Here-form becomes `filepath.Join(hostLayout.WorktreePath(), hostLayout.AnchorRel, pattern.DirName)` and the slug form `filepath.Join(fabricengine.WorktreePath(nestedLayout, slug), nestedLayout.AnchorRel, pattern.DirName)` — both files are `package fabricengine_test`, served by the exported `WorktreePath` from batch 1 card 4, both gaining the `internal/pattern` import.
- "[NIT] Card 18 renames the lyxtest Leaf Invariant instead of correcting it". Card 18's CONSTRAINTS.md instruction now restates the lyxtest invariant as the banned-imports list it actually is, with the true post-batch-1 import set (stdlib + `lyxcwd` + `weftname` + `configengine`) — verified against `leaf_enforcement_test.go:36-46`.
- "[NIT] Batch-5/6 relocations leave four comments naming symbols lyxcwd no longer has". All four fixed in the card that relocates each symbol, each with its line named: `builderengine/state.go:8` → card 21 (`builderengine.Dir`), `perchengine/identity_test.go:77` → card 23 (`perchengine.RunsDir`), `fabricengine/weftgit.go:82-86` → card 22 (`websterengine.PromptsDir`; the `:94-96` exclude patterns already read `configengine.LyxDirName` from card 2 and are called out as untouched), `remove_junctions_integration_test.go:76` → card 34's comment-only list (the file's `:69` call stays card 35's).

## Found while fixing, not in the review

The coordinator's instruction to sweep every deleted/moved symbol's callers again turned up the same defect classes at more sites; all folded into existing cards, no renumbering:

- **Card 21 also missed the `BuilderReportsDir` sub-test.** `lyxcwd_unit_test.go:80-90` calls `BuilderReportsDir(baseDir)`; card 21 named only the `BuilderDir` sub-test. Both are now named for deletion (and the `BuilderDir` range corrected `:68-76` → `:68-78`).
- **The stale-comment class had three more members in files already owned by the right card, but unnamed.** `websterengine/state.go:10` (`WebsterDir`) — file added to card 22; `websterengine/template_test.go:183` (`PlanDirRel()`) and `buildercli/testdata_test.go:93` (`PlanDir(hub)`) — both files already in card 20's `Edits:`, corrections now named there (`planparser.PlanDirRel()` / `loomengine.PlanDir`). The full comment-line sweep (every `// …hubgeometry.<symbol>` outside the module, mapped to its relocating card) found no other unowned member.
- **`modelspec`'s leaf allowlist goes over-wide at card 2 and no card shrank it.** `load.go:23`'s `ConfigFile` call is modelspec's only `hubgeometry` use, so card 2 removes the import — but left the allowlist entry, and card 18 would then have renamed a dead entry to `internal/lyxcwd`, exactly the "over-wide allowlist stops enforcing" defect card 18's own rationale names. Card 2 now drops the entry (and corrects `leaf_enforcement_test.go:3`'s doc comment); card 18's rename list drops modelspec's map with a verify-do-not-re-add note; card 18's CONSTRAINTS edit writes the Modelspec Leaf Invariant's true post-card-2 list (stdlib, `internal/configengine`, `gopkg.in/yaml.v3`).
- **`scoutengine` has the same allowlist problem one batch later.** `ensureserver.go:300`'s synthetic `Location` is scoutengine's last `lyxcwd` use and card 24 deletes it, so card 24 now also drops `internal/lyxcwd` from `scoutengine/leaf_enforcement_test.go`'s allowlist, updates the Scoutengine Leaf Invariant line in `CONSTRAINTS.md` in the same commit (card 18's version keeps `lyxcwd`, still true at batch 4), and re-corrects `scoutengine/doc.go:25`'s import-ceiling enumeration and `:217`'s constructor attribution that card 12's batch-3 wording leaves one move behind. Three files added to card 24's `Edits:` (`CONSTRAINTS.md`, `scoutengine/doc.go`, `scoutengine/leaf_enforcement_test.go`).
- **`fabricengine/doc.go`'s deferred residue lines are now named in card 34.** Card 18 defers `doc.go` to card 34 by design, but card 34 named only the `BoardDir` wording; its `:64` `internal/hubgeometry` aside and `:66`/`:68` `HubReservedNames()`/`HostJunctions` attributions are now explicitly listed.
- **Card 1's `geometry_test.go` retarget was implicit.** The file holds ~17 `hubgeometry.WeftSiblingPath`/`WeftSuffix` references and was in card 1's `Edits:` with no instruction; one sentence now names the `weftname.SiblingPath`/`weftname.Suffix` retarget. Verified no other in-module file references the pair (only `geometry_test.go` and `hubgeometry.go` match).

Sweeps that came back clean, for the next reviewer's benefit: card 2's full caller set (`ConfigDir`/`ConfigFile`/`LyxDirName`/`DotEnv`, 56 files) diffs empty against its `Edits:`; `HubSuffix` has no qualified external caller; `LyxDir()`/`DotLyxDir()` callers match card 27's list exactly; the `Weft*`, host-junction, portal/launcher and `BoardDir`/`HubPath`-family caller sets match cards 30-34 as amended.

## Pushed Back

None. All six findings re-verified and held; no fix hit a harm category. The one judgement call — moving vs deleting card 2's sub-tests — was resolved by checking the destination packages' existing coverage as the coordinator instructed, and every sub-test moved because no destination test pins the same property.

## Validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` → `DAG OK: 8 batches`. `_plan_validate.run` (root=None, wiki_root=git_root=repo, skip_checks=∅, max_cards_per_batch=20, max_batch_context_tokens=700000, parent_branch=main) → `findings: 0`. All Files Touched regenerated programmatically (`_parse_edits_only` ∪ `Creates:` ∪ move targets): **286 paths**, overview matches the union exactly (`missing: []`, `extra: []`) — round 5's 285 plus `internal/envsource/envsource_test.go`, the only file this round adds to the union.
