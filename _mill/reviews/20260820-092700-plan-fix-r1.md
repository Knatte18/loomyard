# Fixer report — plan-fix round 1

Source review: `_mill/reviews/20260820-092423-plan-review-r1.md` (holistic, round 1).

## Fixed

- `[BLOCKING:scope]` "Card 13's new test names `Seed` from a file absent from Context/Edits" — verified accurate against `internal/loomshed/seed.go:56-62`, whose `UpdateJSON` mutate hardcodes `CurrentProducer: NamePreflight` and `State: StateRunning` with no parameter reaching either, so the card's asked-for seed shape is unreachable through `Seed` alone.
  Resolved by removing the requirement rather than by adding the missing `Context:` entry and the two-step patch mechanism: the property the new test would have asserted is already covered by the pre-existing `TestNew_PassesShedValidation`, so the file was never needed.
- `[NIT:consistency]` "Card 13 collides with the pre-existing `TestNew_PassesShedValidation`" — verified accurate at `internal/loomshed/loomshed_test.go:191-220`.
  Card 13 now names that test explicitly, forbids adding a second test of the same property, and forbids renaming or deleting any existing test in the file.
  It instead requires the existing test's explanatory comment to be re-read against per-producer semantics: the comment currently describes a single shared budget of three exhausted between `Discussion-Write` and `Discussion-Validate`, and the budget now belongs to the bouncing producer alone.
  The card's title and `Commit:` message were updated to match the narrowed scope.
- `[NIT:design]` "New OnStuck/Segment check's ordering vs. the existing OnStuck-existence check is unspecified" — verified accurate: card 3 ordered the two `OnDone` rules but said nothing about the same-`Segment` rule's position relative to the pre-existing not-found check in the same loop.
  Card 3 now requires the same-`Segment` rule to run after it, and states the concrete failure mode — an absent name has no `segmentByName` entry, so the lookup yields the zero value and the rule either misfires with a misleading message or fails to fire at all, depending on the bouncing producer's own `Segment`.

## Pushed Back

_None._
