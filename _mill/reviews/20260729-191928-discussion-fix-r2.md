# Discussion fix report — round 2

Source review: `_mill/reviews/20260729-191724-discussion-review-r2.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- `20260729-191724-discussion-review-r2.md` — **[NOTE] Prose calls DotLyxDir the "_lyx" tree; it is ".lyx"**: verified accurate against `internal/hubgeometry/hubgeometry.go:463-474` (`LyxDir()` → weft-synced `_lyx`; `DotLyxDir()` → machine-local, non-weft-synced `.lyx`). Corrected every scaffolding/run-dir reference in the discussion prose from `_lyx` to `.lyx`/`DotLyxDir()` (Problem, Scope, both materialization Decisions, Technical context, Constraints, Q&A), while leaving the legitimate weft-path-prohibition references (`weft _lyx/... output path`) and the Hub Geometry Invariant quote untouched. Added an explicit `DotLyxDir` vs `LyxDir` contrast so the same-commit doc.go text inherits the correct name.
- `20260729-191724-discussion-review-r2.md` — **[NOTE] Existing TestTemplate_HasClusterRulesSection relocation unstated**: verified accurate — `template_test.go`'s `TestTemplate_HasClusterRulesSection` pins `## Cluster rules` + `{{.cluster_rules}}` against the monolith bytes. Added a Testing-section bullet naming it in the relocation list (re-home to `instruction-2-review-template.md` bytes), so no plan writer leaves it asserting against the deleted monolith var.

## Pushed Back

_None._
