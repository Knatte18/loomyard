# Discussion fix — round 4

Source review: `_mill/reviews/20260828-142615-discussion-review-r4.md` (APPROVE, 0 blocking, 5 NIT).

## Fixed

- r4 · "[NIT:consistency] \"status refuses before a lock helper\" is false" — verified against `lifecycle.go:1165`: `Status` does acquire `withOpLock`, and the refusal fires inside it. Restated the evidence item on its true ground (a CLI `status` cannot leak because `PersistentPreRunE` resolves it a fresh geometry) and dropped the "before a lock helper" claim.
- r4 · "[NIT:consistency] Verb-correlated timestamps vs. the 2s poll model" — picked the poll model explicitly in `### the-leak-is-continuous-not-per-event`, stated that it wins over the report's verb-exact timestamp, and rewrote both the Problem paragraph and the Q&A #1 evidence list so the surviving items support it.
- r4 · "[NIT:consistency] Validator name/rationale still says AnchorPath" — renamed the section and the proposed function to `validateToldWorktreeRootLive`, rewrote the rationale around the field it actually checks, and recorded the old name as explicitly rejected so it cannot drift back.
- r4 · "[NIT:consistency] \"nothing MkdirAlls stateDir in standalone\" is overstated" — verified `stencilstore.Reconcile(standalonegeom.StencilsDir(stateDir))` at `burlercli/wiring.go:139` / `webstercli/wiring.go:159`. Narrowed the claim: `withOpLock`'s `MkdirAll` is the sole materializer only on the `--stencils-dir` path, and noted the `WorktreeRoot` decision does not rest on the overstated version.
- r4 · "[NIT:scope] Fixture sweep does not mention build-tagged files" — the Testing/Fixture section now requires the sweep and the verification run to cover the `integration` and `smoke` tags, and names the tag-gated inline-`Geometry` sites.

## Pushed Back

(none)
