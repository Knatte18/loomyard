MILL_REVIEW_BEGIN
# Review: Reed attach dot-fill render artifact on resize and cross-client mouse move — holistic

```yaml
duration_s: 239.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-31
```

## Findings

None.

## Verification notes

- Confirmed batch 3 correctly took **Branch C** (no repaint candidate accepted): `internal/shell/{shell,posix,pwsh}.go` carry no `ForEachLine`/`LineVarRef` additions, `internal/reedengine/watchdog.go` is untouched beyond context, `resizePinHookArgvs` keeps its pre-task 3-argument signature, and `installResizePinsLocked`'s doc comment records the no-candidate disposition with a pointer to `doc.go`'s Measurement record, exactly per card 12/13.
- `internal/reedengine/windowsize_test.go` adds exactly the one Branch-C test (`TestResizePinHookArgvs_NoRepaintEntryShips`); `apply_test.go`, `reapply_test.go`, `watchdog_test.go`, `shell_test.go` carry no dot-fill-related changes, matching card 13's Branch C "make no change" instruction.
- `internal/reedengine/doc.go`'s Measurement record and the nine follow-on bullets (mechanism, stale-paint/uncovered split, window-size latest rationale, repaint entry disposition, acceptance criteria, watchdog independence, Windows, multi-client warning, requiredSubcommands exemption) all match card 18's required content and voice, correctly written for the no-candidate branch.
- Batch 4's `attach.go`/`attach_test.go` (client-list parser, `warnMismatchedClientsLocked`, wiring position ahead of `pinGeometryOptionsLocked`, `exactSessionTarget` form, argv-invariance tests) match cards 15–17 exactly, including the `SuppressedChainStillWarns` case.
- Batch 1/2/3 smoke files (`smoke_dotfill_test.go`, `smoke_dotfill_measure_test.go`) implement every required helper (`newDotFillHarness`, `pollPaneHasDotRun`, `paneStaysCleanOfDotRun`, `windowResizedEntries`, `rewriteWindowResizedArray`, `pinOnlyEntries`, `assertOnlyPinEntries`, the candidate composers, the measurement scenario, `TestSmokeDotFillResizeTreatment` inverted per Branch C) and cross-reference helpers that genuinely exist in `smoke_test.go`/`smoke_attach_test.go` (`harnessOnlyPaneID`, `tmuxBinaryPath`, `harnessShellBinaryPath`, `buildLyxBinary`, `addStrand`, `smokeMarkerLaunchCmd`, `deferHubRelease`, `socketAndSession`, `reapHarnessServer`, `capturePane`, `pollPaneContains`, `sendKeysLine`, `smokeAttachInvokeLine`) and `captureLogOutput` (`logcapture_test.go`).
- `dotFillGlyphs` widening to include U+00B7 alongside ASCII `.` is a documented deviation from card 1's literal `strings.Repeat(".", ...)` text, but it is justified by the batch's own recorded round-1/round-3 failure history (tmux 3.6 emits the middle-dot glyph) — not an undisclosed scope change.
- `requiredSubcommands` (`probe.go`) and `resizePinHookArgvs`'s signature are unchanged, consistent with the no-candidate branch; no stray `ForEachLine`/`repaintHookCommand`/`resizeRepaintHookCommand` symbols exist anywhere in the tree (repo-wide grep).
- Sandbox doc (`SANDBOX-REED-WATCH-SUITE.md`) W5 update matches card 19: mechanism summary, the ~1s-smear expectation (no repaint shipped), cross-client repro steps, the "expected/OK" disposition for the larger-observed case, and the attach-time warning as the searchable trace.
- Re-checked the prior non-blocking item (`isolatedCriterionResult`'s bare zero-value `testing.T`, `smoke_dotfill_measure_test.go:222-241`): no new diff or reproducible failure surfaced this round; not escalated.

## Verdict

APPROVE
Branch C wiring, tests, and docs are internally consistent and match every batch's cards and shared decisions.
MILL_REVIEW_END
