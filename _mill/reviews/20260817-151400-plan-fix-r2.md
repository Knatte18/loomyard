# Plan fix — round 2

Source review: `_mill/reviews/20260817-150606-plan-review-r2.md`

## Fixed

- `20260817-150606-plan-review-r2.md` — "[NIT:design] Validate's worktreeRoot rationale misstates current call sites".
  VERIFY: accurate. `internal/webstercli/run.go` sets `RunDeps{..., WorktreeRoot: c.layout.AnchorPath()}`, which is the same value `internal/webstercli/validate.go` passes directly, so the two production callers of `planparser.Validate` agree today and there is no live divergence.
  HARM CHECK: none — the decision itself (leave the parameter untouched) is unaffected; only its stated reason was wrong.
  Rewrote the overview's Shared Decision rationale to rest on the parameter's *name* inviting a future diverging caller rather than on a claimed present-day disagreement, and recorded explicitly that `_mill/discussion.md` carries the same misstatement.
  The discussion file itself is deliberately not edited: Phase: Plan Review's guardrail confines fixes to files under the plan directory.

- `20260817-150606-plan-review-r2.md` — "[NIT:consistency] notransients_test.go's PlanDir/PlanOverview rows get no weakening annotation".
  VERIFY: accurate. The rewritten `durableSet` rows build `l.AnchorPath()` in the test itself, where the previous `loomengine.PlanDir(l)` form anchored internally and so faithfully reflected whatever production resolved — the same weakening already annotated for `constructoranchoring_test.go` and `cli_test.go`.
  HARM CHECK: none.
  Widened the overview's Shared Decision from two annotated sites to three (adding `cmd/lyx/notransients_test.go`) and sharpened its rationale to state why the weakening is newly introduced by this task rather than pre-existing.
  Added a fourth change to batch 2 card 10 requiring the one-line weakening comment above the two rewritten rows, naming where the anchoring proof actually lives, plus the three Context entries and the Batch Tests line that change implies.

## Pushed Back

_None._
