# Discussion fix report — round 4

Source review: `_mill/reviews/20260731-201156-discussion-review-r4.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- [NOTE] "`unwire` junction enumeration under repo-wide pathspec" — pinned `lyx fabric unwire` as **per-host-worktree** and specified it enumerates junctions by **scanning on-disk link entries (minus `HubReservedNames()`)**, so a full deactivation removes every fabric junction present including any stale one absent from `pathspec`. Updated `init-dissolves-to-fabric-verbs` decision and the Testing `lyx fabric unwire` bullet.
- [NOTE] "Interaction of unwire with the repo-wide weft:main records" — stated explicitly that `unwire` touches only the per-worktree weft `_lyx` and **leaves the repo-wide `.fabric-anchor` and `<BoardDir>/_lyx/config/fabric.yaml` intact** for a later re-wire. Updated the same decision and Testing bullet.

## Pushed Back

(none — both NOTEs were factually accurate and fixing them caused no harm.)
