# Discussion fix — round 5

Source review: `_mill/reviews/20260809-084246-discussion-review-r5.md` (verdict APPROVE, 3 NITs).

## Fixed

- r5 NIT — "Re-point conflict has no stated remedy": added a `conflict-rule` decision requiring the differing-URL error to name the remedy (edit `.lyx-warp` in `<hub>/_board` and commit), with the wording pinned; added a test-list entry asserting the message names both URLs and the remedy.
- r5 NIT — "Derived-URL provenance absent from error wording": added a `conflict-rule` decision that on the derive path every error surfacing the warp URL states it came from the recorded `.lyx-warp` binding, naming `internal/fabricengine/clone.go:92` and the step-5 clone failure as the two sites.
- r5 NIT — "Probe reorders the offline failure surface": added an ordering decision to `pre-hub-probe` making the hub-exists check and `--reset` teardown run **before** the probe in the two-argument form (preserving today's offline `hub already exists`) and after it in the one-argument form, where the hub name is unknowable without the remote; updated the `reset-folding` decision to match, and added a test asserting no probe directory is created on the two-argument existing-hub path.

## Pushed Back

None.
