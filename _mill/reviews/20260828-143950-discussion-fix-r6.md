# Discussion fix — round 6

Source review: `_mill/reviews/20260828-143727-discussion-review-r6.md`

## Fixed

- `[NIT:decision]` "Scrubbed-reed.json remedy contract has no disposition" — correct, and the most operator-visible miss in the whole review series.
  `state.go`'s `unreadableStateError` justifies its "delete `reed.json` by hand to keep the session" remedy with the exact behaviour this task removes (*"planReconcile's untracked reap does not fire (it needs a bound present pane)"*), and its live error string makes the same promise to the operator.
  Added `state.go` to Scope → In with the disposition: the remedy stays offered and stays valid — the panes do keep running, and `attach` never reconciles, so the operator can still reach their work — but both the comment and the string must state the new limit, that the panes survive only until the next `up`/`resume`/`add`/`remove`.
  Recorded why this is a correctness bug rather than a doc nicety: that string is what someone reads while holding running work they can no longer see.
- `[NIT:scope]` "Doc-surface sweep keyed on \"adopt\" only, Go files only" — correct on both counts.
  The task falsifies two premises, and the round-5 sweep only covered one of them, over Go files only.
  Replaced it with two greps — the adoption term and the reap-gate premise (`untracked reap` / `bound present pane` / `reap.*does not fire`, prose that contains no "adopt" at all) — over a file set that now includes `tools/sandbox/*.md`, under the same rewrite/leave-alone disposition rule.
  Also named the sandbox doc's **M13** (~line 249) as a known third hit beyond M16/M22: its `FAIL` diagnosis attributes a strand flipping to `live: false` to having *"adopted a dead leftover pane"*, which becomes an impossible cause once adoption is gone.
  Its symptom and `FAIL` condition still stand; only the causal explanation is rewritten.
- `[NIT:design]` "Logger-capture unit test as specified captures nothing" — correct and verified against `internal/logger`: the stderr half defaults to the Warn threshold and the durable half is disabled under `testing.Testing()` (`sink.go`'s `ensureDurableSink`), so an `Info` record reaches neither sink and the test I specified would have passed vacuously or failed inexplicably.
  The Testing section now names `logger.SetVerbosity(1)` as part of the capture helper, restored alongside the previous output in the same `t.Cleanup`.

## Pushed Back

_None._
