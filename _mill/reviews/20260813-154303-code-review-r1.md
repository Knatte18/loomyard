MILL_REVIEW_BEGIN
# Review: Unblock t.Parallel on hub-fixture tests that currently t.Chdir — holistic

```yaml
duration_s: 210.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-13
```

## Findings

None. The implementation was checked card-by-card against all three batch files and the shared decisions.

Verified in detail:

- **Batch 1** (`internal/lyxcwd/cwdcontext.go`, `internal/clihelp/exec.go`, `cmd/lyx/seamsignature_test.go`): `WithCwd`/`CwdFrom` match the panic/absolute-path/fallback contract exactly; `RunRootCtx`/`ExecuteIn`/`WrapRunCtx` mirror their non-Ctx siblings byte-for-byte apart from the context threading; the eleven-`RunCLI` compile-time assertion is present and correctly scoped to batch 1 (no `RunCLIIn` slice yet at that point — now both slices exist, ten and eleven entries respectively, matching the batch-1/batch-2 split decision).
- **Batch 2** (seven `PersistentPreRunE` modules, `scoutcli`, `fabriccli`, `configcli`, `loomengine.Preflight`): all seven modules (`reedcli`, `shuttlecli`, `perchcli`, `webstercli`, `idecli`, `boardcli`, `burlercli`) apply the identical `lyxcwd.CwdFrom(ctx)` swap and identical `RunCLIIn` sentinel-branch pattern. `scoutcli` rebases all four `--target-dir` defaulting points and both `parseQuery`/`inFileQuery` onto an explicit `base`, with the required new `TestRunCLIIn_TargetDirResolvesAgainstInjectedSeamCwd` test present. `fabriccli.resolveWarpLocation` takes `ctx`, all nine handler functions gained the leading `ctx` parameter, all nine registrations moved to `WrapRunCtx`. `--into` is threaded correctly (destination resolution happens in `runCloneWithReset` before `CloneAndWire`, never inside it) and the usage string is updated in all three required places. `configcli`'s `runReconcile`/`runConfig` take `ctx`, and the nested `fabriccli.RunCLIIn(cwd, …)` call correctly avoids re-deriving cwd. `loomengine.Preflight(cwd string)` matches the signature change with both call sites updated in the same card.
- **Batch 3** (test migrations, `cmd/lyx/cwdmutation_test.go`, docs): all eight named files plus `internal/fabricengine/coalesce_integration_test.go` are chdir-free except the allowlisted file; the guard's subject set, banned tokens, and allowlist match `CONSTRAINTS.md`'s new bullet verbatim. `t.Parallel()` was added only to the three designated files (`reedcli`, `loomengine`, `perchcli/cli_integration_test.go`); the pre-existing `t.Parallel()` calls on cwd-independent tests in `fabriccli/cli_test.go` and `idecli/cli_test.go` (bare listing/help paths that never touched chdir) are consistent with "add no new t.Parallel()" rather than a violation. `restoreCwd` and both `os.Chdir` calls are gone from `preflight_integration_test.go`. `docs/benchmarks/test-suite-timing.md` and `running-tests.md` carry the required trend row and `LYX_TRACE` disposition, honestly stated as "no regression, no fix," with semantic line breaks throughout.
- **Cross-cutting**: `internal/selfreportcli` correctly has no `RunCLIIn`; `cmd/lyx/main.go` is untouched; no out-of-plan files were found — every edited file in the manifest maps to a card's `Edits:`/`Creates:` list, and every context-only file (`hubforge/hub.go`, `fabricengine/clone.go`, `perchengine/identity.go`, `ideengine/spawn.go`, `scoutengine/ensureserver.go`/`refs.go`, `logger/sink.go`, `output/output.go`) is correctly unmodified. `CONSTRAINTS.md`'s two new bullets (cwd-injection seam, cwdmutation guard) and the amended CLI/Cobra Invariant Seam/Enforced-by lines match the plan's required content precisely.

## Verdict

APPROVE
Implementation matches the plan across all three batches with no deviations, no out-of-plan files, and no constraint violations found.
MILL_REVIEW_END
