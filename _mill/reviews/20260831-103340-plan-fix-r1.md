# Plan fix — round 1

## Fixed

- r1 holistic — "[BLOCKING:scope] Card 1 Requirements name apply.go:200 but omit apply.go from Context": verified the Requirements step (4) prohibition names `apply.go:200` while `internal/reedengine/apply.go` appears in neither `Context:` nor `Edits:`, leaving the implementer no told path to confirm it. Took the finding's second remedy — dropped the file:line reference and kept the boundary generic ("do not reword any other occurrence of this same measurement elsewhere in the package"), so the intent survives without adding a file the implementer has no reason to read.

## Pushed Back

_None._
