# Discussion fix report — round 6

Reviewer: `opushigh`. Verdict: `GAPS_FOUND` (2 blocking, 2 nits). All four verified true against code before acting; all four fixed. No push-back.

Round 6 is the **final** round by operator decision, taken before dispatch: stop after R6 unless a finding changes a *decision* rather than an execution detail. None did — the two gaps were a scope clarification and an unbudgeted mechanical volume, the two nits were enumeration omissions. The remaining budget (R7) is deliberately unspent; plan review is the next verification stage.

## Fixed

### [GAP] Out-of-package test call sites unenumerated

Verified: 228 hits of `hubgeometry.{ConfigFile,ConfigDir,LyxDirName,DotEnv}` across 34 `*_test.go` files (`configsync_test.go` ×31, `configengine/config_test.go` ×21, `configcli_test.go` ×20 leading); ~70 hits of `BoardDir`/`FabricAnchorName`/`WeftSiblingPath` across 23 files; three out-of-package test files calling symbols privatized in batch 3.

Added a per-batch test-call-site table to `batching` with the measured counts, plus a correction to `config-path-move`, whose "the 115/85 grep counts were inflated by tests" was true about the production surface and misleading about the edit. Recorded that batch 1's mechanical volume is roughly five times the previously stated ~60 struct literals, and that every relocation list in the document is production-only unless it says otherwise.

### [GAP] `_board` re-wire skips reconcile's missing-weft branch

Verified: `reconcileMissingWeft` (`reconcile.go:164-197`) wires no junctions at all — both the recreate and raw-adopt paths return without calling `WireJunctions`.

Resolved by **excluding the missing-weft branch on purpose** rather than hoisting the call. `_board` follows the convention the pathspec junctions already follow on that path, and the deferral is already the documented operator contract there (`createDormantWeftForRawHost`'s message at `reconcile.go:189` says to re-run reconcile). Hoisting would produce a worktree with a board link but no working junctions — a half-wired state that reads healthier than it is and that `Healthy` cannot correct, since `_board` is unmonitored by design. Reworded "unconditionally" to "unconditionally with respect to junction health" and added the test row (absent after `WeftRecreated`/`RawAdopted`, present after the next pass).

### [NOTE] Two moving symbols named in no destination list

Verified: `WeftWorktreePath` has 9 production call sites, all inside `fabricengine`; `HostLyxLinkHere` has zero production callers outside the module and does not match the `Host*Link` glob.

Both added to `weft-junction-move` explicitly, with the call-site counts, since every other list in the document is exhaustive and a reader is entitled to treat that one the same way.

### [NOTE] `fabriccli` is an unlisted `weftname` consumer

Verified: `fabriccli/fabric.go:45,67,78` builds cobra `Long` help text from `hubgeometry.WeftSuffix`.

Corrected the importer count from two to **four**: `fabricengine`, `lyxtest`, `fabriccli`, and `websterengine/audit.go:89` — the last being the one `seven-leak-fixes` call site whose replacement is `weftname.WeftSuffix` rather than a `fabricengine` accessor. Noted that neither new consumer needs a leaf-allowlist edit.

## Pushed Back

None.
