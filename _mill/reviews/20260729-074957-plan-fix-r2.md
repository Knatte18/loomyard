# Plan-fix round 2 — codeintel-v1

Source review: `_mill/reviews/20260729-074525-plan-review-r2.md` (verdict: REQUEST_CHANGES, 1 BLOCKING, 3 NIT).

## Fixed

- `_mill/reviews/20260729-074525-plan-review-r2.md` — "Context omits the file defining a referenced identifier": added `registry.go` to batch 02 card 9's Context; `lspclient.go` to batch 05 card 20's and batch 08 card 29's Context; `ensureserver.go` to batch 05 card 21's Context; `internal/clihelp/exec.go` to batch 09 card 37's Context (and, on independent verification of the same pattern, batch 10 card 42's Context too — both use `clihelp.NewExitContext`/`exitState.Code()`).
- `_mill/reviews/20260729-074525-plan-review-r2.md` — "[NIT] New Entry fields not reflected in the seeded servers.yaml template": batch 01 card 1 now also edits `internal/codeintelengine/template.yaml`, adding `pinned_version: v0.23.0`/`has_native_daemon: true` to its `go:` block (a real fix, not just a doc callout, since `LoadRegistry` whole-replaces on override and a stale seed would silently regress an operator's Go entry off the native daemon path).
- `_mill/reviews/20260729-074525-plan-review-r2.md` — "[NIT] Batch 2's leaf-invariant allowlist edit lands one card after the import it permits": folded the `internal/lock` leaf-invariant amendment into batch 02 card 6's own commit (the card that introduces the import), matching batch 04 card 13's correct ordering. Card 7 is now solely the Sandbox Suite Coverage backport.

## Pushed Back

- `_mill/reviews/20260729-074525-plan-review-r2.md` — "[NIT] No verify: in the plan ever exercises the integration tier: land a CI workflow (or the suggested pipeline.done_gate change)": partially fixed, partially pushed back. Fixed: batch 11 card 47 now states the `pipeline.done_gate` recommendation as an explicit, named operator recommendation in its final report (not a buried "consider" aside), with the exact command. Pushed back on actually *making* that `mill-config.yaml` edit from within this plan: `done_gate` is a hub-wide setting, hardlinked across every worktree in this hub — silently changing it from inside one task's plan would alter every other concurrent or future task's Handoff gate too, including tasks with no relationship to `gopls`/codeintel and no guarantee of network access for a `-tags integration` run. This is a concrete "destabilizes other work" risk (per `mill-receiving-review`'s legitimate-pushback category), not an "out of scope" dismissal — this task genuinely cannot see or reason about every other task sharing this hub. Landing a new CI workflow was not pursued either, for the same reason plus genuine scope: this task's job is codeintel V1, not this repo's CI topology. The recommendation is preserved and strengthened for the operator to act on deliberately, hub-wide.
