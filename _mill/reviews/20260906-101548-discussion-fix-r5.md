# Discussion fix — round 5

Source review: `_mill/reviews/20260906-101424-discussion-review-r5.md` (reviewer `opus`, effort medium, verdict REQUEST_CHANGES — 1 BLOCKING, 3 NIT after the blocking-class ceiling demoted one).

Every finding was verified against the tree before acting. All four were accurate; none were pushed back.

## Fixed

- r5 — "Plan-Revalidate is not a per-card-merge boundary" (`[BLOCKING:design]`) — verified `contracts/recipes/loom-recipe.yaml` places the `Plan-Revalidate` row once, after the review segment, with `on_done: Batchifier`, whose own `on_done` is `Webster`; it therefore runs strictly before any card merge exists.
  The claim "one batched `Resolve` over the remaining plan after each card merge" was carried over from issue #226's intent and is false for loomyard's actual shed.
  Added a **Boundary correction** paragraph to `drift-boundaries`: `Plan-Revalidate` is restated as a one-shot pre-Webster baseline, and the per-merge `Resolve` folds into `record-batch`, which already fires after each sequentially-run card holding `StartSHA` and the verified `HeadSHA`.
  In loomyard's structure the issue's per-merge boundary and its done-check boundary are the same boundary, so this needs no new row — consistent with this task adding none.

- r5 — "Surface lexeme per ref contradicts the ref-model decision" (`[NIT:consistency]`, demoted from BLOCKING) — accurate: round 4's `rewrite-write-path` keyspace fix decided to retain surface lexemes per ref, while `parse-time-canonicalization` had rejected "storing both forms per ref", with no storage shape stated either way.
  Added a **Storage shape** bullet: surface lexemes live in one `Plan`-level canonical→surface map, and `Targets`/`Uses`/`Pairs` stay `[]string` of canonical glyphs, unchanged in type, so `websterengine.deriveEdges`/`refsIntersect` are untouched.
  Reworded `parse-time-canonicalization`'s rejected alternative to reject the *struct-per-ref* shape specifically, and said so explicitly, so the two decisions no longer read as contradicting each other.

- r5 — "Interim window where the two path checks go silent" (`[NIT:scope]`) — accurate: `path-missing` and `card-path-malformed` are both `isPathRef`-gated, so they match nothing once refs canonicalize to glyphs, and deferring their repair to late `UnitPath()`-gated cards would land merges with two checks dead.
  Added an **Interim posture** bullet to `quarry-unitpath-precondition`: the classifier rewrite and the disk-check repair land in the same card, which carries the accessor precondition — so if the accessor is unmerged, that one card blocks while every other card proceeds.

- r5 — "Facade import path stated as the module root" (`[NIT:consistency]`) — accurate: the module is `github.com/Knatte18/quarry` with no package at its root, so the facade's import path is `github.com/Knatte18/quarry/quarry`.
  Corrected the Problem section's wording and spelled both import paths verbatim in Technical context, including `github.com/Knatte18/quarry/glyph`.

## Pushed Back

None.

## Gaps auto-picked

One BLOCKING finding, resolved by auto-picking the recommended option per the operator's standing instruction. Logged in `discussion.md`'s `## Q&A log` with the `[auto-pick]` marker and rationale.
