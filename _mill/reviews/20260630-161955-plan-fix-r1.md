# Plan-fix report — round 1

Source review: `_mill/reviews/20260630-161855-plan-review-r1.md`

## Fixed

- [BLOCKING] "Verify never compiles the integration test files it edits" — added
  `&& go vet -tags integration ./...` to the batch-1 `verify` in both `00-overview.md`
  (Batch Index) and `01-code-rename.md` (frontmatter), and documented the rationale in
  `01-code-rename.md` `## Batch Tests`. The three `//go:build integration` files
  (`configcli_integration_test.go`, `clone_integration_test.go`,
  `weft_integration_test.go`) are now compile-checked; `go vet` type-checks them without
  running the heavy integration suites.
- [NIT] "Card 10 loom.md instruction misses the line-60 reference" — card 10 now
  instructs replacing **every** `internal/paths` in `loom.md` (line 60 and line 256), not
  only the anchor-link occurrence.
- [NIT] "Card 10 overview.md sweep scoped to the invariant section only" — card 10 now
  directs sweeping **every** `internal/paths` in `overview.md` (directory tree ~line 177,
  shared-modules list ~line 242, plus the heading section), backstopped by the
  comprehensive-sweep grep.

## Pushed Back

(none)
