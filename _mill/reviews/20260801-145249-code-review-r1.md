MILL_REVIEW_BEGIN
# Review: Formalize the Tier 1/2 substrate rule and re-tier mis-tagged tests — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-01
```

## Findings

### [NIT] Stale "integration or smoke" phrasing survives outside the two locations Card 2 fixed
**Location:** `cmd/lyx/tierpurity_test.go:71` and `cmd/lyx/tierpurity_test.go:133`
**Issue:** `TestTierPurity_UntaggedTestsSpawnNothing`'s own doc comment ("mentioning \"integration\" or \"smoke\"") and its failure-message text ("move it behind `//go:build integration` (or `smoke`)") still hardcode only two of the three now-recognized tags, even though Card 1/2 generalized `isTierTagged` and its own doc comment to the full `knownTierTags` list. Card 2's scope named only the file-header block and `isTierTagged`'s doc comment, missing this third/fourth occurrence.
**Fix:** Generalize both strings to reference `knownTierTags` (or name all three tags) for consistency with the rest of this batch's stated purpose.

## Verdict

APPROVE
Plan and shared decisions are fully and precisely realized across all three batches with no blocking issues found.
MILL_REVIEW_END
