# Plan fix report — round 3

Source review: `_mill/reviews/20260729-081019-plan-review-r3.md` (verdict: REQUEST_CHANGES, 1 BLOCKING, 2 NIT).

## Fixed

- `_mill/reviews/20260729-081019-plan-review-r3.md` — "[BLOCKING] `ensureBoardWorktree`'s orphan git invocation likely misbinds its args": confirmed against the actual installed git (2.53.0, `git worktree add --help`) that `--orphan` alone names no branch — the branch name must come from a separate `-b <new-branch>` flag; the plan's `git worktree add --orphan <hostBranch> <boardPath>` form would either error or silently name the orphan branch `basename(<boardPath>)` (`"_board"`) instead of the host's real default branch. Fixed by changing batch 2 Card 6's instruction to `git worktree add --orphan -b <hostBranch> <boardPath>`, with the git synopsis and the failure mode spelled out so the mistake isn't repeated.
- `_mill/reviews/20260729-081019-plan-review-r3.md` — "[NIT] `help_test.go`'s new table entries imply a struct-shape change that isn't stated": confirmed the existing `tests` table struct has `verb string`, not `args []string`, and the loop calls `runHelp(t, tt.verb)`. Fixed by stating explicitly in batch 4 Card 24 that the struct field renames to `args []string` (every existing entry becoming a one-element slice) and the loop's call site changes to `runHelp(t, tt.args...)`.
- `_mill/reviews/20260729-081019-plan-review-r3.md` — "[NIT] `renderManifestSection`'s zero-notes output is unspecified": fixed by stating explicitly in batch 3 Card 12 that an empty `notes` slice produces `""` (no bare "# Manifest" heading), and that `Render` skips the blank-line separator between sections when the Manifest section is empty.

## Pushed Back

(none — all three findings were factually accurate and fixed as recommended; the BLOCKING finding was independently confirmed against the real installed git binary before fixing)
