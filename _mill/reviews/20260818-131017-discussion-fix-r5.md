# Discussion fix — round 5

Source review: `_mill/reviews/20260818-130909-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 4 NIT).

## Fixed

- r5 · "Byte-identity list omits webster's changed classification" — added webster's reclassification as the fourth named deviation in the byte-identity decision, and repointed the Scope "Out" line and the closing "any fifth deviation" rule to match.
- r5 · "\"one line\" understates the `webstercli/cli.go` edit" — reworded both the Scope "In" bullet and the two files-table rows to "probe swap + refuse branch + doc-comment repoint", and named `wire`'s `hubPresent bool` → `mode preflight.Mode` parameter change.
- r5 · "\"the design forbids importing fabricengine into either CLI\"" — scoped the sentence to reaching `Ready`, and listed the retained hub-mode `fabricengine` call sites in both CLIs so it cannot be read as a blanket import ban.
- r5 · "perch `pause`'s envelope has no stated disposition" — recorded `lyx perch pause` as deliberately unchanged, with the reason (its envelope already reports an absolute `pauseFile`, which names `<state>` by construction).

## Pushed Back

None.
All four findings verified accurate against the current tree, and none met a harm category under the `mill-receiving-review` decision tree.
