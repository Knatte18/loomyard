# Plan fix — round 1

Source review: `_mill/reviews/20260702-172053-plan-review-r1.md` (verdict REQUEST_CHANGES,
2 BLOCKING + 2 NIT). Fixer: mill-plan. All four findings passed the `mill-receiving-review`
decision tree (factually accurate, fix causes no harm — no conflict with a documented design
decision, breaks nothing, destabilizes nothing) → FIX. No push-backs. DAG + `_plan_validate`
re-run clean (0 errors) after the edits.

## Fixed

- BLOCKING "attach references unexported socketName + no layout" (batch 6 card 27 / batch 5
  card 16) — added exported accessors `func (e *Engine) Socket() string` and `func (e *Engine)
  SessionName() string` to card 16 (`internal/muxengine/lock.go`), so `muxcli` builds the
  attach invocation from `eng.Socket()` + `eng.SessionName()` instead of the unexported
  `muxengine.socketName`. Card 27's Requirements now name the accessors explicitly; its Context
  swaps `naming.go` for `lock.go` (where the accessors live). This avoids capturing `layout` in
  the PreRunE closure entirely — the Engine already holds it.
- BLOCKING "Card 17 forward-references cards 18/19 helpers" (batch 5 ordering) — reordered
  batch 5 so the composable helpers precede their consumer: 16 (Engine+lock) → 17 (reconcile)
  → 18 (apply) → 19 (strand mutation, which composes `reconcileLocked`+`applyLayoutLocked`) →
  20 (lifecycle). No card now references a symbol that a lower-numbered card has not yet
  created, so per-card build stays green. Added a "Card order matters" note to the batch scope.
  Also dropped the spurious `Edits: lock.go` from the mutation and lifecycle cards (their ops
  are methods on `*Engine` defined in their own new files — no edit to `lock.go` is needed).
- NIT "Card 15 config_test references lyxtest, not in Context" (batch 4 card 15) — added
  `internal/lyxtest/lyxtest.go` to card 15's Context allowlist.
- NIT "Card 15 LoadConfig has a dead `module` param" (batch 4 card 15) — changed the
  Requirements to `configengine.Load(baseDir, module, ...)` (threading the `module` argument
  through, mirroring warpengine) instead of the hardcoded `"mux"` literal.

## Pushed Back

(none)
