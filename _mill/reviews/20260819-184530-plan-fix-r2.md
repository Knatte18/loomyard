# Plan fix — round 2

Source review: `20260819-183619-plan-review-r2.md`

## Fixed

- `[BLOCKING:design]` "Origin-commit's KindCommitCreated omission is unjustified" — verified accurate on both counts: the oracle's own observable-kind table classifies the commit kind as a git-state kind and exempts it from the commission direction, and both existing commit sites in the package record that kind at their success sites.
  Took the reviewer's first option, the one that fixes the behaviour rather than only the prose: `CommitWeftPaths` now takes a `*Mutations` recorder and appends the commit entry itself, conditionally on the commit having landed (batch 1 card 3); `Add` threads its own recorder through and no longer discards the record (batch 2 card 5); the batch-5 caller passes a throwaway recorder, matching what the origin write already does there.
  Also applied the reviewer's second option where it was still needed rather than treating the options as exclusive: batch 2 card 9 now states outright that the oracle cannot check the commit entry and that card 8's integration assertion is its only guard, so the card's verification promise is no longer vacuous.
  Card 8 gained that assertion plus a skip-git counterpart, and batch 1 card 4 gained empty-recorder assertions on both early-return paths.
  The overview's Shared Decision was rewritten accordingly and now records, rather than hides, that this plan adds a recorder parameter the discussion's own sketch of the helper's signature omits.

## Pushed Back

_(none)_
