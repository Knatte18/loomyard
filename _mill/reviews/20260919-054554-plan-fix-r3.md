# Plan fix — round 3

Source review: `_mill/reviews/20260919-054440-plan-review-r3.md` (holistic, sonnetxhigh, APPROVE, 0 BLOCKING, 2 NIT).

Both NITs fixed under the default rule — an APPROVE verdict does not make them optional.

## Fixed

- r3 holistic, `[NIT:design] Card 9's new launcher-filename test may not exercise the real write path` — verified: `internal/fabricengine/launcher_content_test.go` drives only `launcherScript` and `launcherExt`, neither of which constructs a path;
  `runPath` is built by `filepath.Join(launcherDir, "run"+ext)` inside `writeLaunchers`.
  Every test that calls `writeLaunchers` today is `//go:build integration` (`launchers_containment_integration_test.go`, `add_rollback_adopt_test.go`, `livestate_mutationoracle_test.go`, `livestate_doc_test.go`), so no untagged test in the package touches the real write path.
  The card's previous "same shape as the file's existing cases" instruction would therefore have produced a tautological assertion that could not catch a rename to `start<ext>` — the exact failure the scenario exists to prevent.
  Rewrote card 9's requirement to call `writeLaunchers` directly against a hand-built `*lyxcwd.Location` rooted at a `t.TempDir()`, asserting on the real filesystem result (a `run<ext>` file whose bytes contain `loom start`, and no `start<ext>` beside it).
  Confirmed the call stays Tier 1: `writeLaunchers` performs only `os.OpenRoot`, `MkdirAll` and file writes — no `exec.Command`, no `gitexec`, no `hubforge.NewHub`.
  Recorded its one tail hazard, the menu-launcher branch's `PrimeName(l)` call, with the pre-seed escape (`menuLauncherPath(l)`) that reaches the never-clobber early return before `PrimeName` runs.
- r3 holistic, `[NIT:consistency] Batch 4's dependency rationale argues for the opposite order` — verified: the stated rationale claimed the skill should exist under its new name before anything else refers to it, while the DAG has batch 3 (which writes the `ly-drive` comment) land before batch 4 (which creates the path).
  Replaced the rationale rather than swapping the edge: the task lands as one squashed commit, the single cross-reference is a Go comment with no link resolution and no test asserting on it, and the chain order is arbitrary here.
  The new text says so explicitly, so the next reader does not mistake the edge for a constraint.

## Note

Fixing the first NIT introduced a `context-completeness` finding — the new requirement cited `internal/loomcli/wiring.go` as a hand-built-Location precedent, a path outside card 9's own lists.
Rephrased to name the pattern without citing a cross-package file.
Validator and DAG both clean afterwards.

## Pushed Back

_(none — both findings verified accurate against the tree, and neither fix broke existing functionality, contradicted a recorded decision, or destabilised work outside this task.)_
