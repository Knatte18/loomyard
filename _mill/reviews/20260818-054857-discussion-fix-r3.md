# Discussion fix report — round 3

Source review: `_mill/reviews/20260818-054745-discussion-review-r3.md` (verdict APPROVE, 0 blocking, 5 NIT).

## Fixed

- r3 — "CONSTRAINTS.md contended with T4, denied in same doc": verified against `producers-standalone.md:337` that T4's Files list does include `CONSTRAINTS.md`. Replaced the over-broad "must not touch files T4 touches" claim in § Constraints with an explicit three-part disposition: `CONSTRAINTS.md` is contended but the edits are disjoint regions (T4 rewords Pattern Leaf, T5 appends two new sections) resolved by mechanical rebase; `internal/stencilstore/stencilstore.go` recorded as a third T5-owned file, verified absent from T4's list; the `internal/loomengine` adjacency restated as different files.
- r3 — "\"three entry points\" heading and stale Q&A row": renamed the decision to `preflight-exposes-four-entry-points` and appended a supersession parenthetical to the Q&A row enumerating the entry points, matching the marker style the adjacent gate row already carries.
- r3 — "Injected `goos` does not make the Windows path row testable": accurate — `filepath.Join`/`Clean` take the separator from the compile-time host, not the injected `goos`. Scoped the "both rows testable everywhere" claim to branch selection plus case folding, and stated that the Windows-row assertion is built with the same `filepath.Join` rather than a literal backslash string.
- r3 — "Gate-split deviation has no durable record": routed the `Wired`/`HubPresent` split into `internal/preflight/doc.go` as a required content item, the same way the `StencilMode()` deviation is routed into `internal/buildinfo/doc.go`, with the concrete failure mode named (a later reader "fixing" the seed back onto `Wired` and reintroducing the `_board` regression).
- r3 — "hash8 collision behaviour unstated": added an explicit accept-and-document disposition — 32 bits can collide, `Derive` does not detect it, the width is inherited from the design doc rather than chosen here, a colliding pair is wrong-but-not-corrupting, and the widening path is a one-line change because every consumer takes the value from `Derive`.

## Pushed Back

None. All five findings verified accurate against current source before fixing; none met a Decision Tree escape (factually wrong / breaks existing functionality / conflicts with a documented design decision / destabilizes other work).
