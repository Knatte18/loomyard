# Plan-fix report — round 4

Source review: `_mill/reviews/20260725-081609-plan-review-r4.md` (verdict APPROVE, 2 NITs).

## Fixed

- [NIT] "resolveLyx runs before the clone no-op path" — Card 9 revised: `decideClone` keeps its
  original `(hubPath, reset)` signature and resolves the binary **internally at the clone step
  only** (after the Hub-exists/no-`-reset` no-op early-return and the reset `removeAll`, right
  before `cloneRun`), rather than resolving unconditionally in `run()` and threading a
  `lyxPath` parameter. This preserves the current behaviour where the no-op path succeeds even
  when neither a dev binary nor a PATH `lyx` is resolvable. Card 12's test Requirements updated
  to match (stub `resolveLyx` seams in the clone-path test; keep the no-op test unstubbed to
  prove it never resolves).

## Pushed Back

- [NIT] "All Files Touched omits the deleted design doc" — rejected per the overview template's
  documented convention: `## All Files Touched` is the union of `Creates:`/`Edits:`/`Moves:`
  **target** paths only, and "Move source paths are excluded — they disappear, like `Deletes:`
  tokens." `manifest/designs/dev-test-binary.md` is a `Deletes:` (Card 16), so it is
  deliberately excluded. The round-1 `all-files-touched-mismatch` validator confirmed this — it
  flagged the missing `main_test.go` (a Creates target) but never the deleted design doc,
  proving the validator does not expect Deletes in the list; adding it would contradict the
  template and risk a reverse mismatch. The deletion is already recorded in Card 16's `Deletes:`
  field and the roadmap-close card.
