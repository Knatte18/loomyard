MILL_REVIEW_BEGIN
# Review: reed: pane reap isn't applied consistently across up/add's mutating paths — holistic

```yaml
duration_s: 202.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-29
```

## Findings

### [NIT:consistency] HeaderPaneID field comment keeps a clause card 9 didn't commission
**Location:** `internal/reedengine/state.go:41-49`
**Issue:** Card 9 (state.go) is explicitly `doc.go`'s "twin" and specifies the HeaderPaneID comment's replacement enumeration as "excluded from strand accounting, from being the preferred split target, and from both halves of reconcile's kill schedule" — a full replacement of the old list. The implementation keeps a trailing "and from every layout path a Strand would otherwise be subject to" that doc.go's parallel sentence (doc.go:39-42) does not carry, so the two "twin" passages now diverge in content the plan intended to be identical.
**Fix:** Drop the trailing "and from every layout path…" clause from state.go's HeaderPaneID comment so it matches doc.go's restated three-item list exactly.

### [NIT:consistency] unreadableStateError's doc comment inverts the pre-fix header/reap relationship
**Location:** `internal/reedengine/state.go:154-157`
**Issue:** The rewritten justification reads "...but only until the next mutating verb (up, resume, add, or remove) reaps them, since an alive header no longer suffices to spare an untracked pane once one of those verbs runs." Before this task, header aliveness was never part of the untracked-reap gate at all (only `anyBoundPresent` was), so an alive header never "used to suffice to spare" anything — the phrasing implies a prior protective role for the header that didn't exist, inverting the actual before/after story (before: nothing authorized the reap with zero strands, regardless of header state; after: an alive header now authorizes it). The operator-facing error string a few lines below states the real limit correctly, so this is a self-contained doc-comment accuracy slip, not a behavioral or operator-facing defect.
**Fix:** Restate the clause as "since an alive header now authorizes reaping every other pane the moment one of those verbs reconciles" (or similar), rather than describing the header as having previously "sufficed to spare."

## Verdict

APPROVE
Implementation faithfully realizes every batch's cards, shared decisions, and constraints; only two minor doc-prose nits found.
MILL_REVIEW_END
