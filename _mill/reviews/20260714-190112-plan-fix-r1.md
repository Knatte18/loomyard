# Plan fix — round 1

Source review: `_mill/reviews/20260714-185853-plan-review-r1.md` (REQUEST_CHANGES,
0 BLOCKING, 4 findings — NIT-only convergence path).

## Fixed

- [MAJOR] "Card 6 smoke test targets an ambiguous 'fixture hub' dir" — card 6 now
  instructs computing the log dir as the engine does
  (`filepath.Join(filepath.Dir(fixture.Hub), ".lyx", "logs")`) and explains the
  `PairedFixture.Hub` (worktree root) vs `Layout.Hub` (container) distinction.
- [MINOR] "Card 5 Context omits config.go for `e.cfg.DebugLog`" — added
  `internal/muxengine/config.go` to card 5's Context.
- [NIT] "Card 4 template.go godoc classifies debug_log wrongly" — card 4 now pins
  `debug_log` to the `${env:...}`-resolved key group in template.go's file comment.
- [NIT] "Server cwd move vs Windows teardown belt" — card 5's prose-reconciliation
  requirement now notes the server cwd lives in the hub container (outside every
  worktree) and that worktree-scoped Windows teardown belts no longer see it.

## Pushed Back

(none)
