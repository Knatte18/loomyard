# Discussion fix — round 5

Source review: `_mill/reviews/20260919-094147-discussion-review-r5.md` (verdict APPROVE, 3 NITs, reviewer `opusmedium`).

## Fixed

- "reed-header-selvage.md has no disposition" — added a Decision under `docs-in-the-same-commit`: `manifest/designs/reed-header-selvage.md:41`'s file attribution is repointed at `selvagepane.go` in this same commit, because both functions it names by file move. Verified line 41 asserts exactly that as shipped fact, and that line 13's "~230 lines across apply/reconcile/spawn/lifecycle" is historical framing for the pre-split *header* pane — so it is explicitly recorded as staying untouched.
- "Audit enumeration omits the two YAML templates" — scoped the audit method explicitly to `internal/reedengine`'s non-test `.go` files, and named `template_posix.yaml:14` and `template_windows.yaml:14` (one `Selvage` line each, the `height_rows` comment) as expected-unchanged so a whole-directory re-run does not read them as drift. Also stated in `enforcement-test-is-the-teeth` that the check reads only non-test `.go` files, so it sees neither those templates nor the `render` leaf.
- "Allowlist granularity for state.go/config.go unstated" — pinned the mechanism as a plain file set (`selvagepane.go`, `state.go`, `config.go`), matching `internal/cliwire/bannedecl_enforcement_test.go` rather than inventing a file+identifier pair set, with the reason (both forms pass identically today, since those two files' only Selvage identifiers are the declarations they are allowlisted for) and the instruction to record what each file is allowlisted *for* in the test's own doc comment.

## Pushed Back

None.
