# Discussion fix — round 1

Source review: `_mill/reviews/20260811-040523-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 6 NIT).
All six fixed under `mill-receiving-review`'s fix-everything default; none met a pushback criterion.

## Fixed

- r1 — "Batchifier row keeps `plan.md`, criterion 3 forbids it" — added Decision `batchifier-row-hands-to-E`, added the row to Scope "Out", and narrowed Testing item 3 to the rows this task owns (new rows 2–8).
- r1 — "`Discussion-Review-Gate` occurrence set is wrong and unreachable" — added Decision `discussion-review-gate-name-disposition` (sweep `roadmap.md:47`, leave `shed-followups.md`'s nine occurrences as the spec archive plus one `**Override recorded**` note), and rewrote Testing items 1–2 into satisfiable, `_mill/`-excluded, per-file greps.
- r1 — "No disposition for inserting the new producer into `shed.md:13,41`" — added the insertion to Scope "In" and to the Technical-context file table, citing `shed-followups.md:423–424`, which assigns it to this task by name.
- r1 — "Stale line references in the 'already verified' facts" — corrected `loom.md:188` → `:186` and `:91–94`/`:187` → `:91–92`/`:185`, each with a note recording the spec's own off-by-N so it is not propagated.
- r1 — "`Plan-Review-Gate` count stated as 7, actual is 8" — restated as 8 occurrences on 8 lines in 4 files, and named the zero-hit grep as the authoritative criterion instead of the count.
- r1 — "Insertion widens `:75`'s second open question, unacknowledged" — extended Decision `repair-loom-75-open-question`'s hand-off to record that the thin-Output question now covers `Plan-Validate` and `Discussion-Validate` as well as `Preflight`/`Finalize`.

## Pushed Back

None.
