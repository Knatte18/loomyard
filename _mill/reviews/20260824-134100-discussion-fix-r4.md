# Discussion fix report — round 4

Source review: `_mill/reviews/20260824-133436-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r4 "Stencil self-check line assigned here, never dispositioned" — added a new `stencil-self-check` Decision committing the rewritten stencil to a closing `lyx loom validate-discussion` step with a fix-what-it-reports instruction, citing the shipped `manifest/roadmap.md:355` Done entry that assigns the obligation to this item by name; extended the Scope stencil bullet to list it; recorded the consequence for `blind-revalidate-bounce` (bounce frequency drops, disposition unchanged).
- r4 "\"One legitimate bounce path\" is false against the recipe" — qualified `no-on-stuck`'s claim to the one *live* path, noting `Discussion-Review`'s already-declared `on_stuck: Discussion-Write` is inert while row 5 is a Stub.
- r4 "Sibling timeout comment goes stale by the same standard" — extended `timeout-comment-only` to correct `plan_timeout_min`'s "autonomous, shorter than the interview" comment in the same edit.

## Pushed Back

None.
