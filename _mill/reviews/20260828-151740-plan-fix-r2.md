# Fixer report: plan-fix round 2 — reed-pane-reap-consistency

Source review: `_mill/reviews/20260828-150858-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 6 NIT, two of them ceiling-demoted from BLOCKING).
Every finding was verified against the real source files before acting;
all eight were accurate, and all eight were fixed.

## Fixed

- r2 holistic, `[BLOCKING:scope]` "Card 12 sweeps reedcli files its Context does not list" — confirmed: card 12's grep spans `internal/reedcli/*.go` while its `Context:` named only `up.go`, so the implementer could not read the three smoke files carrying the actual hits. Added `smoke_lifecycle_test.go`, `smoke_panecwd_test.go` and `smoke_teardown_test.go` to card 12's `Context:`.
- r2 holistic, `[BLOCKING:scope]` "Card 14 misses a fourth adoption site in the same test" — confirmed at `internal/reedcli/smoke_lifecycle_test.go:350`: `TestSmokeHeaderPaneSurvivesUpAddRemoveAndReconcile`'s function doc comment says the header is "never adopted/reaped as a strand's pane", which card 14's three-site enumeration did not cover. Card 14 now enumerates four sites, doc comment first, with the replacement claim stated.
- r2 holistic, `[NIT:consistency]` (demoted) "Overview's batch-4 verify drops the R4-F4 guard" — confirmed, and self-inflicted in round 1: the `TestSmokeUpSurvivesAScrubbedStateFileWhileTheSessionIsUp` addition landed in batch 4's own frontmatter but not in the overview's Batch Index entry. The overview now mirrors the batch file's filter verbatim.
- r2 holistic, `[NIT:decision]` (demoted) "planPaneTarget's `strands` parameter left with no disposition" — confirmed: the `anyBound` loop card 4 deletes is `strands`' only reader, leaving exactly the vestigial seam the Shared Decision argues against for `adoptID`. Resolved by dropping the parameter: the signature is now `planPaneTarget(live []LivePane, headerPaneID string)`. The Shared Decision states the reasoning, card 4 makes the drop and the call-site change explicit, and card 5 drops the table's `strands` field. Dropping it collapses two case pairs into literal duplicates (`FreshSession_*` with `AllStrandsPaneless_*`, and `OneStrandHoldsAPane_*` with `TinyActiveBand_*`, each pair differing only in its strand table) — card 5 now requires each pair merged rather than left as identical rows, names which comment to carry forward, and closes with a no-duplicate-fixtures rule.
- r2 holistic, `[NIT:consistency]` "Card 2 calls a case falsified that is not" — confirmed: `UntrackedPanesUntouchedWhenNothingBound` sets no `headerPaneID`, so `headerAlive` is false and its expectation is unchanged;
  it *is* the absent-header case card 2 separately asked to add. Card 2 now treats the two existing cases oppositely and explicitly — comment-only for that one, expectation-and-comment for `HeaderAloneNeverMakesAnyBoundPresentTrue` — and the duplicate new case was dropped from the add-list.
- r2 holistic, `[NIT:consistency]` "Card 6's mandated safety comment overstates the Resume path" — confirmed: `planResumeLaunches` selects on absence from `aliveIDSet`, so a strand bound to a dead-but-present pane reaches `launchStrandLocked` still bound, making the dictated "appended with `PaneID == \"\"`" claim non-universal. Card 6 now requires the comment stated per path, with the Resume case named and its harmlessness argued (the binding names a corpse; reconcile kills or spares it, and `s.PaneID` is overwritten either way).
- r2 holistic, `[NIT:scope]` "Card 12's survivor enumeration is incomplete" — confirmed: the grep also returns generation-probe prose in `attach_test.go:106`, `reapply_test.go:50`, `contract_integration_test.go:472` and `spawn_test.go:279`. All four are now named survivors, and the three not already present were added to card 12's `Context:`.
- r2 holistic, `[NIT:scope]` "doc.go's !anyPlacedStrand bullet carries the premise card 9 narrows" — confirmed at `internal/reedengine/doc.go:369-373`: it calls the case "reachable for good via the operator remedy state.go documents, which deletes reed.json while the session keeps running untracked", the same durability claim card 9 corrects, and neither sweep term reaches it. Added as card 8's fourth enumerated passage, with the correction scoped to the durability clause and the paragraph's own conclusion left standing.

## Pushed Back

_None._
All eight findings were factually accurate, and no fix broke existing functionality, conflicted with a documented decision in `_mill/discussion.md` or `CONSTRAINTS.md`, or destabilised work outside this task's scope.

## Post-fix validation

`_plan_dag.validate` passes.
`_plan_validate.run` returned zero findings with an empty `skip_checks`.
