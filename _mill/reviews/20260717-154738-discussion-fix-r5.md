# Discussion-fix report — round 5 (loom-preflight)

Source review: `_mill/reviews/20260717-154614-discussion-review-r5.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- **[NOTE] "Schema doc 'Parse discipline' still says KnownFields(true)"** — extended the
  status-json-typed-and-strict reconciliation note (and the Scope→In doc-update bullet) so the
  mill-go doc change realigns **both** `status-schema.md`'s validation-checklist item 1 **and**
  its "Parse discipline" paragraph from the `yaml.Decoder` `KnownFields(true)` idiom to
  `json.Decoder.DisallowUnknownFields()` for the JSON seed.
- **[NOTE] "Lock-acquire error is a third mode outside the two sentinels"** — reworded the error
  classification in both strict-read-mechanism and seed-read-path to a single rule: only
  `errors.Is(err, state.ErrDecode)` → `seed-incoherent` (Report); **every other** non-nil error
  (`state.ErrRead`, a `lock.AcquireReadLock` failure, anything else) → escalate as `error` per
  result-error-contract. This closes the previously-unspecified lock-acquire branch.

## Pushed Back

_None — both NOTEs were factually accurate, harmless to fix, and improved the contract's
precision._
