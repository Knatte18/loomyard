MILL_REVIEW_BEGIN
# Review: .lyx hygiene -- relocate transients out of _lyx, fix .lyx junction geometry (slice 9) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (Anthropic; self-reported, not independently verifiable)
reviewed_file: plan/
date: 2026-08-07
```

## Findings

### [BLOCKING] Batch 4 card 29 omits scoutengine test files that break on the new ensureServer/ensureSupervised parameter
**Location:** batch 04-dotlyx-group-reanchor-and-logger-sink, card 29
**Issue:** Card 29 requires `acquireConnection` to pass `opts.AnchorRoot` into `ensureServer`, which passes it into `ensureSupervised`, while explicitly keeping `worktreeRoot` flowing through both functions too ("Keep worktreeRoot flowing through both functions unchanged for its existing uses") — this is a new parameter, not a rename, since `DaemonStateFile`/`DaemonLock` calls are the *only* use of `worktreeRoot` inside either function and both move to `anchorRoot`. `internal/scoutengine/ensureserver_test.go` and `internal/scoutengine/supervised_test.go` are both untagged files that call `ensureServer(...)`/`ensureSupervised(...)` positionally with the current arity (verified via Read/Grep), so they fail to compile under this card's own Edits list, which names only `daemonstate.go`, `refs.go`, `ensureserver.go`, `scoutcli/cli.go`. This breaks batch 4's own `verify: go test ... ./internal/scoutengine/...` (untagged, no build tag needed). `ensureserver_integration_test.go` and `supervised_integration_test.go` (`//go:build integration`) call the same functions and have the identical problem, surfacing later at the module-wide `go vet -tags integration ./...` gate instead.
**Fix:** Add `internal/scoutengine/ensureserver_test.go` and `internal/scoutengine/supervised_test.go` (and `ensureserver_integration_test.go`, `supervised_integration_test.go`) to card 29's Edits list, with a requirement to update their `ensureServer`/`ensureSupervised` call sites to the widened signature; also correct the "existing uses" rationale for keeping `worktreeRoot`, since none remain in either function once `anchorRoot` takes over path construction.

## Verdict

REQUEST_CHANGES
Batch 4 card 29's Edits list omits scoutengine test files whose compile breaks under its own re-signaturing.
MILL_REVIEW_END
