# Fixer report: plan-fix round 5 — reed-pane-reap-consistency

Source review: `_mill/reviews/20260829-060612-plan-review-r5.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 2 NIT, one ceiling-demoted from BLOCKING).
Every finding was verified against the real source files before acting;
all five were accurate, and all five were fixed.

## Fixed

- r5 holistic, `[BLOCKING:design]` "Card 13's empty-layout coverage claim is false post-reap" — confirmed by reading `internal/reedengine/apply.go:211-215`: the `len(live) < 2` return precedes the `anyPlacedStrand` guard. Post-reap both of that test's `up` calls reach apply holding exactly one pane (the first `up` reaps the initial pane, the second reaps the foreign one, and `reconcileApplyPersistLocked` re-enumerates before applying), so the `anyPlacedStrand` branch is unreachable from this fixture. Card 13 had ordered the implementer to write a doc comment asserting the test still pins the zero-cell hazard — which would have replaced one false premise with another. It now forbids that claim, states the mechanism, gives the honest remaining purpose of the test, and requires the comment to name `apply_test.go`'s `TestApplyLayoutLockedOpts_GuardSkipsReturnZeroResult` (verified present at `apply_test.go:225`) as where that coverage now lives. `internal/reedengine/apply_test.go` added to card 13's `Context:`.
- r5 holistic, `[BLOCKING:scope]` "No card rewrites spawn_test.go's kept adopt-vs-split comment" — confirmed: card 5 kept `SoleCorpseUnbound_NeverAdopted_SplitOffTheCorpse` "with their current expectations" and spoke only to case *names*, leaving its comment asserting "the next add must split, not adopt" as a live decision, and leaving `SeveralUntrackedAlivePanes_...`'s "Adoption picked it" narrative in the present tense. Card 12 greps both and reports rather than fixes, so no card owned them. Card 5 now names both comments explicitly with the replacement framing for each — the corpse case's real reason (a corpse is never a valid target for anything but a split), and the R4-F5 provenance restated in the past tense as the finding that removed the seam.
- r5 holistic, `[BLOCKING:scope]` "Card 2's Context omits internal/reedengine/strand.go" — confirmed: card 2's Requirements name `AddStrand`/`UpdateStrand` and assert those paths never call `ensureHeaderPaneLocked`, a claim the implementer must encode into the rewritten gate comment, while `strand.go` appeared in neither `Context:` nor `Edits:`. Added. This is the second round in which the same class surfaced (r4's blocker covered cards 6/7/8/9), which is a consequence of the validator matching path tokens rather than symbol names.
- r5 holistic, `[NIT:consistency]` (demoted) "Card 12's survivor list contradicts prose cards 4 and 15 mandate" — confirmed, and a genuine self-contradiction in the plan: cards 4, 5 and 15 all *require* new prose that records why adoption was removed, which cannot be written without naming adoption, while card 12's rule declared any hit not meaning something-other-than-pane-adoption a missed rewrite. Card 12 now recognises two survivor classes rather than one, names the three cards whose commissioned prose falls in the second class, and gives the discriminator: judge by tense and claim, not by presence of the word — adoption described as a removed seam is the intended end state, adoption asserted as something `planPaneTarget` still does or still decides against is a missed rewrite.
- r5 holistic, `[NIT:consistency]` "Card 16 leaves the scrubbed-state guard's new vacuity undispositioned" — confirmed: with the recovering `up` now leaving one pane, `TestSmokeUpSurvivesAScrubbedStateFileWhileTheSessionIsUp`'s `pane_top == 0` assertion is trivially satisfied, so that half of the R4-F4 guard stops guarding while card 16 cited its continued passing as reassurance. Card 16 now states the vacuity plainly, identifies which half remains load-bearing (the recovering `up` exiting 0 — the actual wedge — plus the following `add`), and requires the confirmation note to record it so a later reader does not mistake a trivially-true assertion for live coverage.

## Pushed Back

_None._
All five findings were factually accurate, and no fix broke existing functionality, conflicted with a documented decision in `_mill/discussion.md` or `CONSTRAINTS.md`, or destabilised work outside this task's scope.

## Post-fix validation

`_plan_dag.validate` passes.
`_plan_validate.run` returned zero findings with an empty `skip_checks`.
