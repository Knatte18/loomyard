# Discussion fix report — round 2

## Fixed

- `20260626-053743-discussion-review-r2.md` — "[NOTE] New guard test build tag unspecified": added that the lyxtest golden-copy guard test is `//go:build integration`-tagged (home `internal/lyxtest/lyxtest_test.go`), so it adds no git spawns to the untagged regression run.
- `20260626-053743-discussion-review-r2.md` — "[NOTE] New fixture struct shape left as 'e.g.'": stated that extending `PairedFixture` with the new slug/branch/worktree-path fields is backward-compatible and is the preferred shape; the plan picks one concrete struct + helper name as a fixed contract.

## Pushed Back

(none)
