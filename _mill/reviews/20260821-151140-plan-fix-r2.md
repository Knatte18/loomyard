# Plan fix — round 2

Source review: `_mill/reviews/20260821-150315-plan-review-r2.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 3 BLOCKING + 5 NIT).

Every finding was verified against source before acting.
All eight are accurate;
none met a harm-check exception.
All eight fixed, none pushed back.

## Fixed

- r2 `[BLOCKING:scope]` "shedengine's ProducerDef file in no Context list" — verified `ShedProducer` and `ProducerDef` are declared in `internal/shedengine/producer.go:30,36`, and that only `shedengine/shed.go` ever appeared in a `Context:`.
  Added `internal/shedengine/producer.go` to cards 3, 6, 7, and 12, and `internal/shedengine/shed.go` plus `internal/shedcheck/check.go` to card 6, which prescribed `shed.Producers[0].Producer = …`, a six-field per-row assertion, and a `shedcheck.Check` call while listing no `shedengine` file at all.
- r2 `[BLOCKING:scope]` "Card 23 names shedbuild symbols with no shedbuild file in Context" — accurate;
  the card's `Context:` held five `_test.go` files and nothing else, while its requirements assert the `shedbuild.Recipe` model, `internal/shedbuild`'s own package doc, and the registry's two accessors.
  Added `internal/shedbuild/doc.go`, `internal/shedbuild/parse.go`, `internal/shedbuild/recipe.go`, and `internal/shedrecipe/registry.go` to its `Context:`.
- r2 `[BLOCKING:design]` "Split status-path duplication gets no guard or stated disposition" — the round's most substantive finding, and accurate.
  `loomshed.Deps` carried one `StatusPath` field feeding both `NewLoomPreflight` and `shedengine.Shed`, so the two could not disagree;
  splitting it into an `Env` copy and a `ShedPaths` copy makes a divergent fill possible for the first time, with a silent consequence — `Shed` persisting status to one file while `Loom-Preflight` reads another, surfacing as a broken resume rather than any error.
  Took the reviewer's first option: card 3 now requires `loomrecipe.New` to error when `env.StatusPath != paths.StatusPath` or `env.StatusLockPath != paths.StatusLockPath`, naming both sides' values.
  Card 7 gains two test cases built by taking the coherent fixture output and overwriting one side, so they cannot pass by accident.
  Card 15 additionally requires each pair be filled from a single evaluation of its `loomengine` accessor rather than two calls.
  This does not re-litigate the duplication `discussion.md` says not to collapse — both copies stay;
  it is the guard the duplication needs, and `New` is the only place in the tree where both copies are visible at once.
- r2 `[NIT:consistency]` "Card 5 cites the wrong card for the resume tests" — accurate;
  card 8 moves `sequence_test.go`, card 9 moves `resume_test.go`.
  Reference corrected to card 9.
- r2 `[NIT:consistency]` "Batch 6's Batch Tests contradicts its own verify and card 26" — accurate on both counts.
  The `-run` sentence was left over from the pre-r1 verify command and is deleted;
  the batch now runs the whole `internal/lyxcwd` package, which is stated as deliberate.
  The edit count is six throughout after this round's own smoke-test addition.
- r2 `[NIT:consistency]` "Card 23's title says two edits, its body says three" — accurate;
  retitled to "The three `CONSTRAINTS.md` edits".
- r2 `[NIT:consistency]` "Shared Decision name says three packages, body names four" — accurate.
  Renamed to `no-production-change-to-the-consumed-packages` and both citing references updated.
- r2 `[NIT:scope]` "A falsified smoke_test.go comment is outside every card's reach" — accurate, and it uncovered a second defect the finding did not name.
  Verified `internal/loomcli/smoke_test.go`'s header carries the stale parenthetical "loom's own producer table (`internal/loomshed`)", written in the slashless form no sweep token matched.
  Added the file to card 26's `Edits:` and to `## All Files Touched`, added the slashless `internal/loomshed` spelling to the sweep tokens, and scoped the repair to the parenthetical alone — the five stub rows and their unconditional Done are unchanged.
  **Second defect, found while fixing:** the file's tag is `//go:build smoke`, not `integration` as I had written, and `pipeline.done_gate`'s `go test -tags integration ./...` does not enable `smoke` — so with the file merely added to `Edits:`, no gate in this plan would ever have compiled it.
  Batch 6's `verify:` therefore gains a chained `&& go vet -tags smoke ./internal/loomcli/`, verified green on the current tree.
  `go vet` rather than `go test` is deliberate: the edit is comment-only, so typechecking is the whole requirement, and running the smoke suite at a batch boundary would spawn tmux and git for no gain.

## Pushed Back

_None._
All eight findings verified accurate against source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope.
