# Discussion fix — round 2

Source review: `_mill/reviews/20260817-144048-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, none demoted).

## Fixed

- r2 — "[NIT:consistency] Scope says eleven test files, lists ten".
  Verified by recounting the enumeration: ten files, and the four-symbol grep confirms ten is the complete set.
  Changed the count word to "ten"; the list itself was already correct and is unchanged.

- r2 — "[NIT:consistency] `hubgeom` filed under `docs/shared-libs` without a classification".
  Verified against `docs/shared-libs/README.md:7-9`, whose admission line is "a shared lib does one mechanical thing … It carries *no* domain logic", and whose every entry is a leaf sitting below its consumers.
  `hubgeom` fails both tests: it encodes reed-specific derivation (domain knowledge) and imports `reedengine` + `fabricengine`, sitting above them.
  Added a new Decision, "hubgeom is a hub-mode adapter, not shared infrastructure", pinning documentation to the `docs/overview.md` package tree plus its own `doc.go`.
  Removed `docs/shared-libs/README.md` and the `docs/overview.md:314` shared-infrastructure sentence from the Scope docs bullet and from the Q&A entry — this reverses the earlier answer, which had filed it as shared infrastructure without testing it against that document's stated line.

- r2 — "[NIT:design] Trust rule stated for `SocketKey` only, not the other six fields".
  Verified: `os.MkdirAll(logsDir, …)` at `lifecycle.go:257` takes `LogsDir` unchecked, and `stateDir()` at `lifecycle.go:44` joins onto the anchor value, so an empty field there yields a relative path.
  Renamed the decision to "Geometry is trusted verbatim — all seven fields, validated by nobody" and restated it over the whole struct, naming each field's consumer and both concrete failure modes the reviewer cited.
  Added the caller's obligation to the `Geometry` type's doc comment as well as `New`'s, so T7/T8 inherit an explicit rule rather than an inference.
  Added a third rejected alternative — validating `SocketKey` alone — since that was the shape of the original decision.

## Pushed Back

(none)
