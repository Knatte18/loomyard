# Plan fix — round 4 (APPROVE with NITs)

Source review: `_mill/reviews/20260725-135919-plan-review-r4.md` (verdict APPROVE, 2 NITs)

## Fixed

- [NIT] "Golden-fixture clean case needs referenced target files present" — card 9 (batch 2) now states explicitly that the `t.TempDir()` worktreeRoot for the clean/zero-findings case must materialize every `Edits:`/`Context:` path and every `Moves:` SOURCE the golden fixture references (e.g. `internal/boardcli/list.go`, `internal/boardengine/rows.go`, `list_test.go`, the `//`-escaped `internal/output/envelope.go` / `cmd/lyx/helptree_test.go`) while deliberately NOT creating the `Moves:` DESTINATION (`rowsjson.go`) or any `Creates:` target — so `path-missing`/`move-source-missing`/`move-target-collision` all pass; each triggering case flips exactly one file.
- [NIT] "Card 25 does not call out updating stale state.go header doc" — card 25 (batch 7) now instructs reconciling `state.go`'s godoc in the same edit: drop the "deferred-verify chain rollback anchor SHA" (`ChainStartSHAs`) and `builderengine.Digest` carry-forward prose, and document `BatchState.Digest` as the webster-local `*Digest` and `CardSHAs` as the per-card resume/bisect trail.

## Pushed Back

_None — both NITs were factually accurate and low-harm; fixed per the receiving-review default rule._
