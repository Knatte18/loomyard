# Plan fix — round 2

Source review: `_mill/reviews/20260811-050421-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING + 2 NITs).

## Fixed

- r2 holistic, `[NIT:design]` "Card 12's doc-count rationale is arithmetically off" — verified against `internal/configcli/configcli_test.go`'s aggregate-print test, which seeds `board` and leaves the rest absent, so "the other N modules" means total minus one.
  With today's nine registered modules "other" is eight, making the existing word "nine" a pre-existing off-by-one;
  batch 1's registration takes the total to ten and "other" to nine, so the word becomes correct untouched and card 12's instruction to bump it would have *introduced* the error.
  Removed `internal/configcli/configcli_test.go` from card 12's `Edits:`, replaced the sub-instruction with an explicit do-not-edit note carrying that arithmetic, moved the file to card 12's `Context:` so the reference stays resolvable, dropped `./internal/configcli/...` from batch 3's `verify:` and its `## Batch Tests` paragraph, corrected batch 3's file count and card 12's title, and rewrote the overview's `doc-site-ownership` decision to record this as a candidate examined and rejected rather than a site found — so a later reader does not re-discover it and assume it was missed.
- r2 holistic, `[NIT:consistency]` "Batch 2's \"atomic compile unit\" framing overstates the compile coupling" — verified against `internal/websterengine/runlevel_test.go`'s `newRunFixture` (its `websterengine.Config{}` literal sets `SelfFixCap`/`MasterTimeoutMin`/`PollWaitS` only, never `Batcher:`) and `internal/webstercli/verbs_test.go` (its only `Config.Batcher` mention is prose in a comment;
  its `Config{}` literal likewise omits the field).
  Neither tagged file breaks the compiler when the field is deleted, so card 5 alone does compile under both build tags.
  Rewrote batch 2's `## Batch Scope` to claim an atomic *correctness* unit instead, naming the two behaviours that actually break at that intermediate point — every `TestRun_*` hitting `ErrNilBatcher` with an unpopulated `RunDeps.Batcher`, and `verbs_test.go`'s `strings.Replace` silently no-opping against the removed `batcher: ""` literal so the gate pair asserts nothing — and narrowed the card-5 compile claim to the production files plus the untagged `config_test.go`, which is where it is true.
  Card grouping and sequencing are unchanged, as the finding noted.

## Pushed Back

_(none — both findings verified accurate against source, and neither fix broke existing functionality, conflicted with a documented decision, or destabilized work outside this task.)_
