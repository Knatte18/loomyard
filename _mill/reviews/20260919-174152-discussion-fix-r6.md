# Discussion fix — round 6

Source review: `_mill/reviews/20260919-174118-discussion-review-r6.md` (verdict APPROVE, 4 NITs, 1 demoted from BLOCKING).

## Fixed

- r6 `[NIT:consistency]` "Probe refusal names a go-path-only artifact" — the probe's refusal now names the shuttle run directory and the strand GUID, with `loomengine.LoomDriverLog` called out as the `go` branch's artifact that this path never writes.
- r6 `[NIT:consistency]` "AskUserQuestion deny is not the backstop claimed" — the SKILL.md edit is now stated as the sole enforcement; the tool deny is recast as defence-in-depth against a future edit.
- r6 `[NIT:scope]` "`shedrun` ephemeral accessor assumed, not inventoried" — the Lyxdirs bullet now says this task adds the `.lyx/shed/<run-id>/` accessor to `shedrun` if the rebase shows none shipped, matching the hedge already used for the bootstrap-lock relocation.
- r6 `[NIT:design]` "\"Cap sentence\" is an unpinned test target" — the test is anchored on the `## Autonomous driver` heading plus the fixed `autonomous step cap: <N>` phrase, so the file's existing `40` and "near a hundred steps" cannot satisfy it.

## Pushed Back

None.

## Note

Round 6 is the configured round cap (`roles.discussion-review.holistic.rounds: 6`).
The convergence gate was not satisfied on its own terms — one finding in this round was demoted from BLOCKING by the stage's blocking-class ceiling, and this is the first APPROVE round under `--orch`'s two-consecutive-APPROVEs rule — so this round is taken as an implicit approval at the cap, per the operator's explicit instruction to approve and hand off on reaching it.
