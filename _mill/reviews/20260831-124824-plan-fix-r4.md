# Plan fix — round 4

Source review: `_mill/reviews/20260831-124723-plan-review-r4.md`

## Fixed

- `[BLOCKING:scope]` "Cards 12/13 call `e.TmuxPath()` but `lock.go` is absent from Context" — verified: `func (e *Engine) TmuxPath() string` is declared at `internal/reedengine/lock.go:70` and is named by both cards' Requirements. Added `internal/reedengine/lock.go` to card 12's and card 13's `Context:` in `03-repaint-entry.md`.
- `[NIT:consistency]` "Card 2's \"two strands\" rationale doesn't match `FixedHeightPins`" — verified against `internal/reedengine/render/rules.go:144`: `FixedHeightPins` emits the header pin whenever `plan.hasHeader` holds and `plan.soleHeader` does not, so a single strand already yields a non-empty pin set and the pin-count rationale was simply wrong. Card 2 in `01-dotfill-repro-harness.md` now states the correct reason — two strands give the resize round-robin a taller stack to distribute rows across, so the mid-relayout region is larger and the artifact reproduces more reliably, and it keeps the scenario clear of `AttachArgv`'s `len(live) < 2` guard boundary rather than sitting on it — and explicitly instructs the implementer to write that reason into the helper's comment rather than the pin-set one. Also added `internal/reedengine/render/rules.go` and `internal/reedengine/attach.go` to card 2's `Context:` so both claims are verifiable without leaving the allowlist.

## Pushed Back

_(none — both findings were verified accurate and neither tripped the harm check.)_
