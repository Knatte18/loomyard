# Discussion fix report — round 5

Source review: `_mill/reviews/20260729-062612-discussion-review-r5.md` (verdict: APPROVE, 2 NOTE findings, 0 blocking gaps).

## Fixed

- `_mill/reviews/20260729-062612-discussion-review-r5.md` — "[NOTE] 'two gitrepo.New calls' miscounts current sync.go": reworded the Weft-git-routing decision to state `sync.go`'s `Sync()` constructs exactly one `gitrepo.New(boardPath)` handle, reused for both `commitDirty`'s `StageAllAndCommit` and `Sync`'s own `PushCoalesced` — the fix swaps those two method calls for `CommitWeftAt`/`PushWeftAt`, not "two `gitrepo.New` calls."
- `_mill/reviews/20260729-062612-discussion-review-r5.md` — "[NOTE] `_board` adopt-path branch state after clone is imprecise": replaced the "adopt origin as a tracking local branch" framing with the precise git mechanics — `git clone` already creates and checks out a local `<hostBranch>` ref on any non-empty remote (both the first-ever-setup and `--reset` re-clone cases hit this identical code path), so `_board`'s worktree-add is a plain `git worktree add <path> <hostBranch>` against an already-existing local branch, no `-b`/`--track` flag; only a genuinely empty weft remote (no local `<hostBranch>` ref at all) needs the separate orphan-creation path.

## Pushed Back

(none — both findings were factually accurate wording/precision corrections against the actual codebase, fixed as recommended)
