# Plan fix — round 2

Source review: `_mill/reviews/20260820-112541-plan-review-r2.md` (holistic, `sonnetxhigh`, APPROVE, 0 BLOCKING + 4 NIT, 2 of them demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r2 — "[NIT:consistency] Batch 5's `-count=1` rationale isn't in the mechanized `verify:`" (demoted from BLOCKING):
  added `-count=1` to batch 5's `verify:` field in both `05-quarry-live-and-equivalence.md`'s frontmatter and `00-overview.md`'s Batch Index, so the machine-read gate matches card 34's own invocation and the batch's stated rationale.
  The finding is exactly right about the mechanism: card 34 runs the live tier and populates the test cache, so without `-count=1` the batch-boundary gate firing moments later could report a cached pass without re-spawning a single language server — the one guarantee this batch exists to provide.
- r2 — "[NIT:consistency] Shared Decision overclaims what batch 4 does with the sixth tagged file" (demoted from BLOCKING):
  rewrote the "one opt-in test tag, `lsp`" Shared Decision to say five of the six tagged files collapse onto `lsp`, and that the sixth, `internal/scoutcli/cli_integration_test.go`, does not survive in any tagged form — batch 4 drops it because its subject is `lookupContext`'s in-hub branch, and card 31 replaces it with an untagged `resolve_test.go`.
  The Decision and batch 4's own Batch Scope contradicted each other; batch 4 was right.
- r2 — "[NIT:consistency] Card 17's line-number/description pairing for ensureserver.go is shifted by one":
  re-paired the descriptions against the correct source lines and supplied the missing sixth.
  The six line numbers were right and the five descriptions were right, but they were zipped together one slot apart from line 160 onward.
  Card 17 now names one description per line: 40 (dialling a daemon lyx spawned to outlive the call), 160 (after a `finalizeConnection` failure lyx makes no second spawn attempt), 257 (a lyx-owned supervised daemon behind a worktree-scoped advisory lock), 260 (this worktree's own future lyx invocations as reconnecting callers), 357 (the `.lyx/scout/<lang>/` directory), 496 (breaking away from a Windows Job Object lyx itself created).
- r2 — "[NIT:consistency] Card 43 mislabels a roadmap 'Done' entry as 'Someday'":
  corrected the label and acknowledged the convention deviation the card was silently making.
  Verified against the tree: `manifest/roadmap.md`'s `## Done` section spans lines 180-308 and the scout entry sits at line 278, so it is a Done entry, not a Someday one.
  Card 43 now says Done, states that removing it deviates from the file's convention that Done entries persist, gives the reason — a Done entry earns its place by pointing at the durable documentation of what shipped, and that documentation is `internal/scoutengine`'s package doc, which card 38 deletes — and requires the deviation to be named in the commit message.

## Pushed Back

_None. All four findings verified accurate against the tree, and no fix broke existing plan structure, conflicted with a documented decision, or destabilised another batch._

## Convergence

Not converged this round: two of the four findings carry `demoted: true`, so the convergence gate's predicate fails even though the verdict is APPROVE and `blocking_count` is 0.
Per the gate, the NIT fixes above are applied and committed, `approved:` is left `false`, and the loop continues to round 3 (round 2 of 7).

## Verification

- `_plan_dag.validate` — OK.
- `_plan_validate.run` with `skip_checks={"out-of-worktree-target"}` — 0 findings.
