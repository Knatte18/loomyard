# Discussion fix — round 3

Source review: `_mill/reviews/20260906-122224-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, 2 demoted from BLOCKING).
Auto mode: every finding is FIX by rule;
PUSH BACK is unavailable with no operator present.

## Fixed

- "[NIT:consistency] pattern/stencilstore import sites are misattributed" (demoted from BLOCKING).
  Verified accurate: `grep -ln "loomyard/internal/pattern" $(ls internal/fabricengine/*.go | grep -v _test)` returns `pull.go` alone, and the same command for `stencilstore` returns `stencilhistory.go` alone;
  `stencilcommit.go` imports neither.
  The `fabric-verdict-do-not-extract-and-say-why-precisely` decision now names both real sites with line numbers, names the producing command, and explicitly warns off the false "`stencilcommit.go`/`stencilhistory.go`" framing.
  A new bullet draws the consequence the finding asked for: because `pull.go` sits on the pair-kernel side of the measured partition, the kernel is **not** `pattern`-free — the edge exists to populate `PullResult.PatternResidue`, and `internal/pattern` itself pulls `lyxdirs`, `stencilstore` and `stencil` in behind it.
  The `fabric-recommendation-is-an-in-repo-seam-not-a-repo-boundary` rationale was corrected to claim only that the split isolates the stencil-versioning coupling, and to state that cutting the `pattern` edge is separate, named work (making residue reporting a caller-supplied predicate).
  The Testing scenario list gained a matching check.

- "[NIT:consistency] Logger prep item (a) collides with a shipped enforcement test" (demoted from BLOCKING).
  Verified accurate: `cmd/lyx/spawnobservability_test.go:59` pins `spawnObservabilityLoggerImportPath` to the literal `github.com/Knatte18/loomyard/internal/logger` and `:64`'s `spawnObservabilityAllowedSpawners` requires a written structural reason per exemption;
  `internal/reedengine/{lifecycle,overlay,attach}.go` all carry real `exec.Command` calls.
  The `logger` decoupling was removed from the cheap-in-repo-prep list, which is now two items rather than three, and the `reed-logger-decoupling-is-an-injected-slog-logger` decision now scopes the change **standalone-only**, retracts the incorrect "the invariant requires that spawns are logged, not which package logs them" claim for the in-repo case, records the real in-repo price (a `CONSTRAINTS.md` amendment plus three allowlist entries, and Scope bars the former), and quotes the allowlist's own `gitexec` entry because it documents the `logger`→`lyxcwd`→`gitexec` cycle at issue.
  The Constraints section now lists Live-Substrate Spawn Observability as an active bound on a recommendation rather than a merely cited fact, and the Q&A entry was reworded to carry the standalone-only scoping.

- "[NIT:consistency] `render` import claim is incomplete".
  Verified accurate: `internal/reedengine/render/policy.go` imports `sort`.
  The `reed-standalone-layout-mirrors-quarry` decision now states the measured import set (`fmt`, `sort`, `strings`), keeps the substantive claim (stdlib-only, zero internal dependencies), explicitly warns off the wrong two-import phrasing, and names the producing command.

## Pushed Back

None.
