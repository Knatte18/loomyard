# Plan fix — round 5

Source review: `_mill/reviews/20260821-153456-plan-review-r5.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 3 NIT, one of the NITs demoted from BLOCKING by the stage's blocking-class ceiling).

Every finding was verified against source before acting.
All four are accurate;
none met a harm-check exception.
All four fixed, none pushed back.

## Fixed

- r5 `[BLOCKING:scope]` "Card 6 writes a file it declares no Edits for" — accurate, and a direct consequence of round 4's own fix.
  Verified card 6's `Edits:`, `Creates:`, and `Deletes:` were all `none`, its only `Moves:` pair was `loomshed_test.go -> shape_test.go`, and `internal/loomrecipe/fixture_test.go` appeared only in `Context:` — while its requirements told the implementer to write the `fakeAlwaysDoneProducer` declaration into that file.
  Rounds 3 and 4 settled *which* card owns the declaration without moving the file into that card's `Edits:`.
  Added `internal/loomrecipe/fixture_test.go` to card 6's `Edits:` and reworded the requirement to say plainly that card 5 moved the file into place and this card edits it.
  `## All Files Touched` needs no change — the path is already there as card 5's move target.
- r5 `[NIT:decision, demoted-from BLOCKING]` "Roadmap's emptied \"Shed recipe\" Planned section has no stated disposition" — verified `manifest/roadmap.md`'s `### Shed recipe: declarative producer lists` Planned section holds exactly one item, the conversion item card 24 moves to Done, leaving a heading plus intro with zero items.
  Card 24 now states the disposition explicitly: delete the heading and its intro, since all four pieces of the group have shipped and the four Done entries carry the record.
  It also catches the knock-on the finding implied — `### loom: real LLM producers`' own "Sequenced after the \"Shed recipe\" group above" sentence would then name a section that no longer exists — and requires it repointed at the Done entries with its ordering rationale intact.
  The new parent-fabric item's home is now named rather than left as a relative "sequence it after this conversion item", which stopped being locatable once that item left Planned: its own `###` section under Planned, placed ahead of `### loom: real LLM producers`, since it is not an LLM-producer task.
  Its blocking claim is scoped honestly — loom cannot complete an end-to-end run until it lands, but the five LLM-producer tasks add rows and are developable independently, so they are not asserted to be blocked on it.
- r5 `[NIT:consistency]` "Card 15 says \"ten fields\" and lists nine" — accurate;
  the tenth is `Landing`, deliberately left unfilled in the following paragraph.
  Reworded to "nine of the ten fields loom's thirteen rows read; `Landing` is the tenth and stays unfilled", which also matches card 5's own ten-item list.
- r5 `[NIT:consistency]` "Card 25's commit message contradicts its own requirement" — accurate;
  the card forbids adding `contracts/recipes` to the module tree while its `Commit:` subject claimed exactly that.
  Reworded to `docs(overview): add loomrecipe to the module map, recipes in prose`.

## Pushed Back

_None._
All four findings verified accurate against source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope.
