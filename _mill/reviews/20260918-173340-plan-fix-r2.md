# Plan fix — round 2

Source review: `20260918-173022-plan-review-r2.md`

## Fixed

- `[BLOCKING:scope]` "Card 7 Context omits internal/ideengine/spawn.go" — verified: card 7's Requirements name `Spawn` and `CodeLauncher`, both defined in `internal/ideengine/spawn.go`, which card 6 edits in the same batch, while card 7's own `Context:` listed only `internal/vscode/config.go` and `internal/lyxcwd/lyxcwd.go`.
  Added `internal/ideengine/spawn.go` to card 7's `Context:`.
- `[NIT:consistency]` "New Info-level spawn log only on the relaunch branch" — verified: no `logger.Info` sits beside any of the three sibling `launchStrandLocked` call sites today.
  Took the finding's second remedy rather than its first: the discussion's Constraints section states outright that the relaunch branch logs its spawn at `Info` under the Live-Substrate Spawn Observability invariant, so dropping the log would contradict a settled decision, while recording the exception does not.
  Card 2 now states that this `Info` log is a deliberate, scoped exception, names the three sibling paths that rely on the generic tmux `Debug` trace instead, requires a comment beside the call saying so, and puts any change to the siblings' logging out of this plan's scope.

## Pushed Back

_(none — both findings were accurate, and each was fixed by a remedy the finding itself offered.)_
