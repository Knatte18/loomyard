MILL_REVIEW_BEGIN
# Review: Move <hub>/.lyx into <hub>/_board — holistic

```yaml
duration_s: 381.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: claude-sonnet-5 (Sonnet 5, per this session's own system prompt)
reviewed_file: plan/
date: 2026-08-14
```

## Findings

### [BLOCKING:design] Card 13 deletes `wireBoardLink` before card 14 removes its three call sites
**Location:** batch `board-junction-deletion` / cards 13-14.
**Issue:** Card 13's sole edit is deleting `wireBoardLink` from `junction.go`. Verified against the current source: `clone.go` (`wireBoardLink(rec, l, filepath.Base(warpWorktreePath))`), `add.go` (`wireBoardLink(rec, l, slug)`), and `reconcile.go` (`wireBoardLink(rec, warpLayout, slug)`) all still call it, and none of those three files are touched until card 14. The commit card 13 produces alone fails to compile with "undefined: wireBoardLink" in `package fabricengine`.
**Fix:** Merge cards 13 and 14 into one card, or reorder so the three call sites are removed first and the now-dead function is deleted last (Go does not error on an unused package-level function, only on unused imports/locals) — mirroring the "any intermediate commit in the other order is broken" discipline the plan itself states for batch 1's card 3→4 ordering.

### [BLOCKING:design] Card 15 deletes `unwireBoardLink`/`BoardJunctionRemoved` before cards 16-17 remove the remaining references
**Location:** batch `board-junction-deletion` / cards 15-17.
**Issue:** Card 15 (edits only `unwire.go`) deletes `unwireBoardLink` and the `UnwireVerbResult.BoardJunctionRemoved` field. Verified against current source: `remove.go`'s `boardRemoved, boardErr := unwireBoardLink(rec, l, slug)` call (fixed only in card 16, a different file) and `fabriccli/unwire.go`'s `"board_junction_removed": res.BoardJunctionRemoved` read (fixed only in card 17, a different package) both still reference the deleted symbols. The commit card 15 produces alone breaks two packages (`internal/fabricengine` via `remove.go`, `internal/fabriccli` via `unwire.go`) across two intervening commits.
**Fix:** Land the `remove.go` and `fabriccli/unwire.go` reference removals in the same card as, or a card before, the deletion of `unwireBoardLink`/`BoardJunctionRemoved` — same fix shape as the card 13/14 finding above.

## Verdict

REQUEST_CHANGES
Two intermediate-commit compile breaks in batch 2 (cards 13→14, 15→16/17); everything else checked out exactly as stated against source.
MILL_REVIEW_END
