# Plan fix — round 2

```yaml
task: 'plan-format: drop the v3 suffix and sweep every reference by script'
slug: plan-format-drop-v3-suffix
round: 2
source_review: 20260809-132511-plan-review-r2.md
```

## Fixed

- `20260809-132511-plan-review-r2.md` — "[NIT:consistency] Sweep leaves the renamed doc's own H1 title hyphenated inconsistently with the rest of the repo".
  Verified: `docs/reference/plan-format-v3.md:1` is `# Plan format v3 — flat card list` (space, not hyphen), so the sixth pattern's replacement leaves `# Plan format — flat card list` two lines above a blockquote that reads `**plan-format**`.
  Confirmed no markdown anchor anywhere in the repo targets that heading and no Go test asserts it, so the rewrite is free of consequence beyond the naming itself.
  Added as item 7 of batch 2 card 5's numbered rewrite list, renumbering the former item 7 (the "Dropped from v2" block deletion) to item 8.
- `20260809-132511-plan-review-r2.md` — "[NIT:consistency] Override note's "four citations" count for shed-followups.md's stale path is off by one".
  Verified: `grep -c 'plan-format-v3\.md' manifest/designs/shed-followups.md` returns **5** (lines 53, 74, 120, 192, 214), not the 4 the discussion's `exclusion-set` decision states — the discussion's tally omits `:120`.
  Adopted the reviewer's proposed remedy rather than hardcoding 5: batch 4 card 13 now instructs the implementer to run that grep while writing the note and use the verified number, with an explicit warning not to carry the discussion's "four" forward.
  Also removed the same unverified count from the three other places it had been restated — batch 4's `## Batch Scope`, card 13's "do not repair" line, and card 14's gate 9 — plus batch 2 card 8's parallel claim.

## Pushed Back

_None._
Both findings were factually accurate on inspection, and neither fix broke existing functionality, contradicted a documented decision, or destabilised work outside this task's scope.

## Incidental

Two numeric prose counts elsewhere in the plan ("seven rewritten sentences", "seven hand-written prose edits") became stale as a direct result of the first fix adding a rewrite site.
Both were made non-numeric rather than re-incremented, since the exact count carries no information for the implementer and had already drifted once when `roadmap.md:18` came into scope.
