# Discussion fixer report — round 2

Source review: `_mill/reviews/20260625-120042-discussion-review-r2.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- `20260625-120042-discussion-review-r2.md` — [NOTE] "cleanup flag composition (--apply vs --force) under-specified" — added an explicit flag matrix to both the Scope `warp cleanup` bullet and the `cleanup-codeguide-gate-conservative` decision: no flag → dry-run/report; `--apply` → delete non-gate-protected orphans; `--apply --force` → additionally delete gate-protected task branches; `--force` requires `--apply` (does not imply it).
- `20260625-120042-discussion-review-r2.md` — [NOTE] "lyx init activation on a missing/dormant pairing unspecified" — added a "Missing-pairing behavior" line to the `junction-relocation-dormant-then-init` decision: `lyx init` activates an existing dormant pairing only; on a host worktree with no weft sibling it reports "no weft pairing — run `warp add`/`warp clone`" and never creates topology (init→warp-only direction; raw-worktree adoption is `warp reconcile`'s).
- `20260625-120042-discussion-review-r2.md` — [NOTE] "post-checkout hook worktree-identification mechanism deferred" — added an explicit validation criterion to the Technical-context hook item: the plan must confirm the cwd-based worktree identification resolves the correct deterministic `<base>-weft` sibling for both prime (`<PrimeName>-weft`) and child (`<slug>-weft`) worktrees before the hook ships.

## Pushed Back

(none — all three NOTEs were accurate, harmless clarifications and were applied under the default fix-everything rule.)
