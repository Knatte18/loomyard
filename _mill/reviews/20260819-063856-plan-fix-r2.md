# Plan fix — round 2

Source review: `20260819-063754-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 0 NIT).

## Fixed

- `20260819-063754-plan-review-r2.md` — "[BLOCKING:scope] Batch 4 omits the pre-existing repo-tree gap for `internal/standalonegeom`".
  Verified accurate: the fenced repo-tree diagram in `docs/overview.md`'s `### Status` subsection carries an `internal/hubgeom/` line and an `internal/preflight/` line but no `internal/standalonegeom` line anywhere in the file, and batch 4 capped itself at three edits that never revisit the diagram.
  Added card 15 to batch 4 — one line for `internal/standalonegeom/`, placed immediately after the `internal/hubgeom/` line, with the diagram's existing prefix and description-column mechanics stated explicitly.
  Renumbered batch 5's single card from 15 to 16 to keep global card numbering unique and sequential, bumped batch 4's frontmatter `cards:` from 3 to 4, and rewrote batch 4's scope paragraph, which claimed three edits.
  DAG re-validated;
  the full validator gate re-ran clean.

## Pushed Back

_(none)_
