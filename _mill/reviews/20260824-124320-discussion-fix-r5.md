# Discussion fix — round 5

Source review: `_mill/reviews/20260824-124217-discussion-review-r5.md` (APPROVE, 0 BLOCKING, 4 NIT).
Auto mode: every NIT treated as FIX per `mill-receiving-review`'s default, with PUSH BACK unavailable.

Round 5 is the configured round cap.
Two findings were demoted from BLOCKING, so the convergence gate's `demoted` predicate is not satisfied;
per the gate's round-cap branch this round is treated as an implicit approval and the commit message records the shortfall.

## Fixed

- `20260824-124217-discussion-review-r5.md` — "**Old `**verify:**` spelling has no disposition**".
  Verified: `parse.go:307` defines `cardVerifyLabel = "**verify:**"` and `isCardLabelLine` matches case-sensitively via `strings.HasPrefix`, so recapitalizing the card field to `**Verify:**` would let a stale lowercase line fall through `default: i++`.
  `retired-label-disposition` now carries **eight** retired labels, the eighth being `**verify:**` → `**Verify:**`, and records the deliberate asymmetry that the plan-level `## verify:` heading stays lowercase because `sections.go` is unchanged.
- `20260824-124217-discussion-review-r5.md` — "**`Custom` + `path-missing`: targets only, or `Uses:` too?**".
  The exempt bullet now reads "`path-missing` **on its own targets only**", with `Custom`'s `Uses:` paths explicitly still checked, so exactly one reading survives.
- `20260824-124217-discussion-review-r5.md` — "**`webster-parallel-execution.md` mis-attributed to Wave 3**".
  Verified against `manifest/roadmap.md`: line 62 sits under `## Someday` (line 56), and the Wave 3 DAG item at line 31 does not name the doc.
  Both the Scope (Out) bullet and the design-doc banner disposition now cite `roadmap.md:14` and the Someday item at `:61-62`, with an explicit instruction not to encode the Wave 3 attribution.
- `20260824-124217-discussion-review-r5.md` — "**`Intent` → `Summary` rename touches one render function, not two**".
  Technical context corrected to name `RenderBatchIndex` (`render.go:268`) alone;
  `RenderProgress` reads `Number`/`Slug` only.

## Pushed Back

None.
Auto mode makes PUSH BACK unavailable, and all four findings verified as factually accurate against the cited files in any case.
