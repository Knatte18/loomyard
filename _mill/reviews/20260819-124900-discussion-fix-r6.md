# Discussion fix report — round 6

Source review: `_mill/reviews/20260819-124604-discussion-review-r6.md` (verdict APPROVE, 0 blocking, 3 NIT, reviewer `fable` at high effort).

## Fixed

- r6 — "landingshed cannot reference PushWarpRebaseFreeAt": verified against `internal/lyxcwd/enforcement_test.go`'s `fabricVocabularyHits`, which flags a bare vocabulary token in any `*ast.Ident` (line 712-715) for every non-owner production file.
  Added the `landing-never-names-fabric-vocabulary` decision: the push moves behind a `PushBranch func() error` closure filled by the CLI layer, `Deps` drops its path field for a told `OriginURL`, and both packages' docs describe one repo.
  Recorded that the merge verbs are unaffected because their names are already vocabulary-free.
- r6 — "Round-4 Q&A entry still names PushWarpAt": annotated the round-4 entry with an explicit supersession note naming what replaced it and instructing that the rejected verb not be implemented.
- r6 — "ScratchDir creation unassigned": assigned creation to whichever component writes into it, on every write path, with the Told-Geometry justification and the `shedengine` lock-parent precedent cited.

## Pushed Back

None.
