# Plan fix — round 1

Source review: `_mill/reviews/20260814-111444-plan-review-r1.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 2 BLOCKING / 2 NIT).

## Fixed

- r1 "[BLOCKING:scope] Pre-existing unit tests calling changed signatures are never listed in any card" — verified accurate against source (32 `composePrompt(` sites in `internal/burlerengine/prompt_test.go`; 7 `New(shuttle, …)` sites in `internal/burlerengine/engine_test.go`; both smoke files `//go:build smoke`; 11 `runTriage(` and 11 `judgeInputs{` in `internal/treadleengine/judge_test.go`; 3 `runTargeting(` in `internal/treadleengine/engine_test.go`; 39 `e.Run(p, …)` sites in `internal/perchengine/run_test.go`).
  Added each file to the `Edits:` of the card that changes the signature it calls, with a Requirements paragraph naming the call-site counts and the threading work: card 18 (burler's four test files), card 22 (treadle's two), card 24 (`internal/perchengine/run_test.go`).
  Corrected the reviewer's card assignment for `internal/treadleengine/engine_test.go`: it holds the `runTargeting` calls, whose arity changes in card 22, so it belongs to card 22 rather than card 24.
  Recorded in card 22 that `treadleengine.New`'s own arity is unchanged — `StencilsDir` arrives as an `Options` field — so the 34 existing `New(` calls in treadle's tests need no edit.

- r1 "[BLOCKING:scope] Stale-reference sweep (card 40) is scoped to `.md` only, missing two `.go` comment hits" — verified accurate; the sweep also missed a third hit the review did not name, `internal/websterengine/runlevel.go:752`.
  Extended card 40's grep scope to `internal/**/*.go`, added all three files to its `Edits:`, and listed each hit by path and line in the Known-hits list.
  Noted that `runlevel.go` is also edited by batch 6 card 27, which touches a call argument and not this comment, so the two edits do not overlap.
  Separately added `render.go:193`'s `RenderMasterPrompt` doc comment to card 26, which already owned that file's header comment.

- r1 "[NIT:consistency] Card 18's import claim is factually wrong for both named files" — verified accurate: `grep -n fabricengine internal/burlercli/cli.go internal/perchcli/cli.go` returns nothing.
  Reworded card 18 to instruct adding the import to **both** files.

- r1 "[NIT:consistency] Several batch-3 line-number citations have drifted from current source" — verified all three against source.
  Corrected card 12's `board.go:112` to `board.go:109`, and card 13's `commit.go:182` and `weftgit.go:256` to `commit.go:186` and `weftgit.go:260`.
  Fixed rather than deferred to implementation time as the review's own suggested remedy allowed: a wrong citation in a plan is a wrong citation, and correcting three integers costs nothing.

## Pushed Back

_None. Every finding was factually accurate and no fix caused demonstrable harm._

## Validator follow-up

Adding the two `//go:build smoke` files and one `//go:build integration` file to `Edits:` tripped three `verify-excludes-edited-tagged-test` findings on the self-run validator gate.
Fixed mechanically per the fix table by appending a matching `-tags` gate to each batch's `verify:`: `go vet -tags smoke ./internal/burlerengine/...` on batch 4 and `go vet -tags integration ./internal/websterengine/...` on batch 9.
`go vet` rather than `go test` deliberately — those tagged files need to compile, and running the smoke or integration suite on every implementer and fixer round would cost minutes per round for no added signal.

DAG re-validated and the validator gate re-run after every edit: 9 batches, 0 findings.
