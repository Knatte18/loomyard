# Plan fix — round 2

Source review: `_mill/reviews/20260814-171928-plan-review-r2.md` (REQUEST_CHANGES, 2 BLOCKING, 0 NITs).

## Fixed

- r2 / "[BLOCKING:design] Batch 1 card order breaks fabricengine's test compile mid-batch" — verified against source: `internal/fabricengine/clone_test.go` is `package fabricengine` and imports `internal/reedengine` for `TestReedHubLogsDir_MkdirAllIdempotentAgainstFabricCreatedDotLyx`, so any commit in which `reedengine` already imports `fabricengine` while that import survives fails to compile with "import cycle not allowed in test". The import-cycle-breaking card is now card 3, immediately before card 4 (the `HubLogsDir` re-point that introduces the production edge); the former cards 3-6 shift to 4-7 and cards 1, 2, 8-12 keep their numbers. Both cards now state the ordering constraint explicitly, and card 3 records that the idempotency test is absent between card 3 and card 8, which re-creates it — a deliberate gap that closes inside the same batch.
- r2 / "[BLOCKING:scope] Context omits files whose functions Requirements name" — verified each declaration site: `NewBolt`/`(*Bolt).Commit` at `internal/fabricengine/bolt.go:17,23`; `mutateGitExclude` at `internal/fabricengine/gitexclude.go:59`; `containedWorktreeAdd`/`createExclusiveDir` in `internal/fabricengine/destroy.go`. Added `bolt.go` to card 2's Context, `gitexclude.go` to card 9's Context, and `destroy.go` to card 10's Context.

## Pushed Back

_(none — both findings were factually accurate against source, and neither fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task.)_
