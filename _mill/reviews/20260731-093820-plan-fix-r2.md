# Plan fix — round 2

Source review: `_mill/reviews/20260731-093615-plan-review-r2.md`

## Fixed

- `20260731-093615-plan-review-r2.md` — "[BLOCKING] Card 1 references snapshot.go identifiers without listing it in Context/Edits". Verified: Card 1's Requirements names nine identifiers defined in that file and asks the implementer to grep-confirm callers of two of them, but the file appeared only under `Deletes:`, and the Context allowlist grants implicit-read status to `Edits:` files only. Added `internal/gitrepo/snapshot.go` to Card 1's `Context:`.
- `20260731-093615-plan-review-r2.md` — "[NIT] Card 2's gogit.go location description is imprecise". Verified against source: the `refs/loomyard/snapshot/*` mention sits at `gogit.go:40`, inside the `# Why EnableDotGitCommonDir, and why not PlainOpen or DetectDotGit` section, not in any fingerprint-gate rationale. Reworded the pointer to name that section. Also recorded something the finding exposes but does not state: this is the **same** worked example as the `PlainOpen` case already listed for `doc.go`, so Card 2 now says to give both the same restatement rather than treating them as two unrelated hits.

## Pushed Back

_None._
