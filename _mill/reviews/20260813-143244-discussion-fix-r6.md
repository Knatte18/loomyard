# Discussion fix report — round 6

Source review: `_mill/reviews/20260813-143152-discussion-review-r6.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).
Round 6 was an operator-authorised extension past the configured `rounds: 5` cap, requested to verify round 5's unreviewed fixes.

## Fixed

- `20260813-143152-discussion-review-r6.md` — "[NIT:design] ExecuteIn's own contract for cwd == \"\" is unstated".
  Verified accurate: `WithCwd` panics on an empty dir, so a uniform `RunCLIIn → ExecuteIn` delegation would panic on every existing `RunCLI` call.
  `RunCLIIn` now documented as branching on the sentinel — `Execute` when `cwd == ""`, `ExecuteIn` otherwise — with `ExecuteIn` never receiving `""`.
- `20260813-143152-discussion-review-r6.md` — "[NIT:consistency] \"eight packages / roughly fourteen files\" undercounts and overcounts".
  Verified against the tree: eleven packages gain a seam change (10 `RunCLIIn` modules plus `loomengine`), and the untouched non-smoke chdir-using files in those packages number exactly eleven — the enumerated list was already correct at eleven items, only the prose disagreed.
  Both numbers corrected.
- `20260813-143152-discussion-review-r6.md` — "[NIT:consistency] Clone's usage string is duplicated and needs `--into` in both places".
  Verified, and found to be **three** sites rather than two: `fabriccli/fabric.go:64` (cobra `Use:`), `fabriccli/clone.go:125` (usage-error literal), and the descriptive comment at `fabric.go:61`.
  All three named as commit 2's help-text obligation, along with the flag registration's own description and a `Long` example.

## Pushed Back

None. All three findings were verified accurate against the live tree and fixed under the `mill-receiving-review` default rule;
none triggered a harm category.

## Note on an earlier round's pushback

For the audit trail: one round-5 finding was partially pushed back and is recorded in the discussion's Q&A log rather than here.
The reviewer's smoke-tier correction (12 files / 33 occurrences, not 11 / 30) was upheld and applied;
its wider claim of "12 further files / 33 total / 111 occurrences" was not, because a comment-filtered repo census gives 37 files / 118 occurrences (smoke 12/33, integration 13/54, untagged 12/31), leaving "seventeen further files outside the twenty named" correct as written.
Round 6 independently re-verified that census as exact.
