# Plan fix report — round 6

Source review: `_mill/reviews/20260729-084951-plan-review-r6.md` (verdict: APPROVE, 1 NIT, 0 blocking).

## Fixed

- `_mill/reviews/20260729-084951-plan-review-r6.md` — "[NIT] Card 19's 'calling UpsertTask directly' is ambiguous between two same-named methods": confirmed `Board.UpsertTask` and `Store.UpsertTask` both exist in scope and both are accessible from `board_test.go`'s package; `Board.UpsertTask` would actually be rejected by Card 15's own cross-store slug-uniqueness check (the slug already exists in `notesStore` at that point in the test), so only `Store.UpsertTask` (constructed directly via `NewStore`, mirroring `PromoteNote`'s own documented bypass pattern from Card 16) produces the intended crash-simulation state. Fixed by naming `Store.UpsertTask` (via `NewStore(<tasks.json path>)`/`Load`/`Save`) explicitly in Card 19's Requirements, removing the ambiguity.

## Pushed Back

(none — the finding was factually accurate and fixed as recommended)
