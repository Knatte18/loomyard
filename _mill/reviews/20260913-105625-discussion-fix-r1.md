# Discussion fix — round 1

Source review: `_mill/reviews/20260913-105545-discussion-review-r1.md` (verdict APPROVE, reviewer `orchestrator`, 0 BLOCKING / 1 NIT).

## Fixed

- `20260913-105545-discussion-review-r1.md` — "File-inventory total is off by one against its own enumeration" (`[NIT:consistency]`).
  Verified independently before fixing: `grep -rl -- '-HUB'` returns 54 files, of which three are `_mill/` task-state (`status.md`, `discussion.md`, the round-1 review file), leaving 51 product-source files;
  the discussion's own three enumeration groups sum to exactly 51 (7 production + 31 test + 13 docs).
  The header's "52" double-counted `_mill/status.md`, the very file the same header claimed to exclude.
  Rewrote the `### File inventory` heading and added a short preamble stating the 54→51 derivation, the per-group sums, and — per the reviewer's first suggested option — an explicit note that `CONSTRAINTS.md` carries no `-HUB` literal today and so gains new content rather than a substitution, bringing the total files touched to 52.

## Pushed Back

None.
