# Discussion fix — round 6 (final, round cap)

Source review: `_mill/reviews/20260913-172221-discussion-review-r6.md` (verdict REQUEST_CHANGES, 1 BLOCKING, 4 NIT — 2 demoted).

Round 6 is `roles.discussion-review.holistic.rounds`'s cap.
Per the Convergence gate's round-cap path, this round is treated as an implicit approval and the phase proceeds to Handoff;
the `discussed` commit carries the `(min_rounds/demoted-predicate not satisfied by round cap)` note so the shortfall is auditable.
All five findings were fixed — none were carried forward unresolved.

## Fixed

- `20260913-172221-discussion-review-r6.md` — "[BLOCKING:design] CONSTRAINTS.md sites are outside the scanner's prefix set": the rejection rationale in `background-citations-lose-the-path` rested on the enforcement test flagging the two unguarded `CONSTRAINTS.md` sites, which it structurally cannot — `CONSTRAINTS.md` is a repo-root token under none of the four scanned prefixes. Corrected to state that outright, rejected simply widening the prefix set (`CONSTRAINTS.md` is a legitimate target-repo path; the defect is the missing "if present" guard, not the token), and added a narrow companion assertion that genuinely closes the class: any stencil body mentioning `CONSTRAINTS.md` must carry "if present" in the same sentence.
- `20260913-172221-discussion-review-r6.md` — "[NIT:scope] loom.md's Webster-Review rubric record not in collateral": added `manifest/designs/loom.md:276` as a third determinate collateral site for `code-comment-conventions-is-misscoped`, with its disposition. Verified: that line is the rubric's durable transcription record, explicitly "kept in step with the stencil," and carries the `[code-comment-conventions.md](../../docs/code-comment-conventions.md)` link that Markdown Link Integrity binds.
- `20260913-172221-discussion-review-r6.md` — "[NIT:consistency] Enforcement-test failure count is arithmetically wrong": replaced "13 occurrences across 12 rows" with the correct figures — 13 rows, 15 flaggable occurrences — and added the per-row breakdown, since two rows cite two paths each. Recorded that the two `CONSTRAINTS.md` rows contribute zero flaggable occurrences and are covered by the companion assertion instead.
- `20260913-172221-discussion-review-r6.md` — "[NIT:consistency] \"four call sites\" contradicts the three-site decision": corrected to three, a survival from the superseded four-site draft that R4 and R5 had already corrected elsewhere.
- `20260913-172221-discussion-review-r6.md` — "[NIT:consistency] Reciprocal cross-references point the wrong way": fixed both directions between the `sourceDir` decision and the per-verb table.

## Pushed Back

None.
