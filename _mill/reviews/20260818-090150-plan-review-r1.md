MILL_REVIEW_BEGIN
# Review: burlerengine + perchengine told-geometry — holistic

```yaml
duration_s: 469.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude (Sonnet-class); harness-reported build id "claude-sonnet-5" ("Sonnet 5"), unverified beyond that system-provided label
reviewed_file: plan/
date: 2026-08-18
```

## Findings

### [BLOCKING:scope] Card 8 misses a live `e.layout` field access in engine_test.go
**Location:** Batch 2, Card 8 (`internal/burlerengine/engine_test.go`) **Issue:** `TestEngine_Run_PatternDirectiveReachesInstruction1` (source line 538) reads `e.layout.AnchorPath()` directly off the constructed `*Engine` — a site outside the "eight `New(...)` call sites" Card 8 enumerates. After Card 6 renames `Engine`'s field from `layout *lyxcwd.Location` to `geom Geometry` (whose `AnchorPath` is a plain string field, not a method), this line fails to compile twice over: `e.layout` no longer exists, and even a renamed `e.geom.AnchorPath()` would still fail since `AnchorPath` takes no parens. Verified: this is the only occurrence of `.layout` in the package outside the sites Cards 6/8 already cover (checked via grep across `internal/burlerengine`). **Fix:** Add this site to Card 8's Requirements — rewrite line 538 to `e.geom.AnchorPath` (field access, no call parens).

### [NIT:consistency] Card 15's stated call-site count is off (33 claimed, 31 actual)
**Location:** Batch 3, Card 15 (`internal/perchengine/run_test.go`) **Issue:** Requirements says "update all thirty-three call sites of the helper" (`newTestLayout`), but the file contains 31 (`layout := newTestLayout(t)` occurs 31 times, verified by grep), not 33. The instruction to rename every occurrence is otherwise unambiguous, so this does not block correct execution. **Fix:** Correct the count to 31, or drop the specific number and rely on "every call site."

### [NIT:consistency] Card 21 / batch-3 grep note undercounts and mis-locates hits
**Location:** Batch 3, Card 21 and the Batch 3 "Batch Tests" manual-check note **Issue:** Card 21 says "all five" `perchengine.RunsDir(l)`/`ScratchDir(l)` sites in `cmd/lyx/notransients_test.go`, but there are 6 lines (7 call expressions, since the `mirroredPairs` row carries both `RunsDir` and `ScratchDir` on one line) — verified by grep. Separately, the batch-end manual `grep -rn lyxcwd internal/perchengine/` note says "Cards 12, 13, and 14 are the three places a hit could survive," but `doc.go` (Card 14) never contains the literal string "lyxcwd" today — only `engine.go` (12) and `identity.go` (13) do (verified by grep). **Fix:** Correct both counts/claims; the enumerated edit locations themselves are otherwise accurate and complete, so no work is actually missed.

## Verdict

REQUEST_CHANGES
Card 8 omits a compile-breaking `e.layout` reference in engine_test.go; two other cards carry minor miscounts that don't drop work.
MILL_REVIEW_END
