# Plan fix — round 3

Source review: `_mill/reviews/20260814-172825-plan-review-r3.md` (APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- r3 / "repairPairWiring's doc comment goes stale after card 14" — verified against source: `internal/fabricengine/reconcile.go:360-361` summarises the function as re-wiring broken junctions and "always re-wires the operator-convenience `_board` link", and `:369` opens a paragraph explaining why that re-wire is unconditional with respect to junction health. Card 14 deletes exactly that call, so both would have been left describing code that no longer exists in the function they document. Card 14 now names `repairPairWiring`'s own doc comment alongside the two comments it already listed: drop the `_board` clause from the summary sentence, and remove the unconditional-re-wire paragraph.

## Pushed Back

_(none — the finding was factually accurate against source, and the fix breaks nothing, conflicts with no documented decision, and destabilizes no work outside this task.)_
