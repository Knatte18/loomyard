MILL_REVIEW_BEGIN
# Review: Scoutengine: rewrite CONSTRAINTS.md as a seam rule, convert leaf test to banned-list, add LSP guard — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-08
```

## Findings

### [BLOCKING] Card 1 Context missing the file it renames and surgically edits
**Location:** batch 1 / card 1
**Issue:** Requirements names concrete identifiers from `internal/scoutengine/leaf_enforcement_test.go` (`allowedImports`, `TestLeafInvariant_AllowlistOnly`, the `firstSegment`/`isStdlib` block, the exact current `t.Errorf` string, the exact catch-all line) that the implementer can only know by reading that file, but the `Moves:` source is not listed in `Context:` (only `internal/shuttleengine/seam_enforcement_test.go` and `CONSTRAINTS.md` are).
**Fix:** Add `internal/scoutengine/leaf_enforcement_test.go` to card 1's `Context:` list.

### [BLOCKING] Card 3 Context missing the file its non-vacuity proof must edit
**Location:** batch 1 / card 3
**Issue:** The requirement "Prove the seam test's non-vacuity assertion fires — either by temporarily pointing its directory scan at an empty directory or by asserting the scanned-file count directly" requires opening and temporarily editing `internal/scoutengine/seam_enforcement_test.go` (card 1's output), but that file is absent from card 3's `Context:` (`probe.go`, `lspclient.go`, `lspclient_guard_test.go` only) and `Edits:` is `none`.
**Fix:** Add `internal/scoutengine/seam_enforcement_test.go` to card 3's `Context:` list.

### [NIT] Batch-1 scope rationale doesn't match the two-commit card structure
**Location:** batch 1 / Batch Scope
**Issue:** The stated reason for keeping cards 1–2 in one batch — avoiding "an intermediate commit where the [isStdlib] heuristic exists in neither place or in both" — isn't actually achieved, since card 1 and card 2 each carry their own separate `Commit:` line, so that exact intermediate state (heuristic in neither file) still exists in git history between the two commits.
**Fix:** Either note that the transient state is harmless (nothing tests for the heuristic mid-batch) or drop/reword the rationale sentence.

## Verdict

REQUEST_CHANGES
Two Context-completeness gaps (cards 1 and 3) need the referenced files added before implementation.
MILL_REVIEW_END
