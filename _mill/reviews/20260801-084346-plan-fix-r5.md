# Plan fix report — round 5

Source review: `_mill/reviews/20260801-083707-plan-review-r5.md` (verdict REQUEST_CHANGES, 2 BLOCKING). Both VERIFIED accurate against the code and FIXED. Both concern broken intermediate states with no verify coverage — the class the earlier rounds' fabriccli fixes established, now closed inside fabricengine and for initengine.

## Fixed

- [BLOCKING] "Pre-existing fabricengine fixtures never migrated to repo-wide BoardDir config" — CONFIRMED: `newFabricFixture` (reconcile_stale_registration_test.go:106) seeds `lyxtest.SeedConfig(t, fixture.WeftPrime, …)`, and 10 other fabricengine `*_test.go` files seed fabric config per-pair; `CopyPaired`/`CopyPairedLocal` never materialize `_board`. After card 7's read migration these fail with "cannot load fabric.yaml" at batch 2's/5's own verify. Fixed by: (a) adding an edge so **batch 5 depends on batch 2** (so batch 2 owns the shared fixture migration and batch 5 inherits it, avoiding a parallel same-file conflict); (b) extending card 10 to migrate the shared `newFabricFixture` helper + the six known non-add fixture files to seed config at `hubgeometry.BoardDir(fixture.Hub)`, with a grep-sweep instruction and the whole-package verify as the completeness gate; (c) extending card 22 to migrate the add-path fixtures (`add_rollback_adopt_test.go`, `add_branch_exists_test.go`). Added all affected test files to All Files Touched.
- [BLOCKING] "Batch 3 breaks the not-yet-deleted initengine test suite with no verify coverage until batch 6" — CONFIRMED: card 11's fabric skip makes `initengine.Init` unable to materialize a per-worktree `fabric.yaml`, breaking `init_test.go:93-107`'s module-count and `LoadConfig` assertions (and `undo_test.go`), with no batch verify between batches 3-6 catching it. Fixed card 11 to `t.Skip` the superseded initengine tests in the same batch (with a reason pointing to the batch-6 deletion; `Init`/`Undo` are dead code, not fixed), added `init_test.go`/`undo_test.go` to its Edits, and widened batch 3's verify to `-tags integration ./internal/configsync/... ./internal/initengine/...` so the skip is confirmed at the batch boundary — closing the exact verify gap the review flagged.

## Pushed Back

(none — both findings were factually accurate and fixing them caused no harm.)
