# Discussion Fix Report — round 2

Source review: `_mill/reviews/20260618-182702-discussion-review-r2.md`

## Fixed

- **[NOTE] Prose "container" left after Container→Hub rename** — Updated CONSTRAINTS.md row in Technical context to specify that prose occurrences of "container" (lines 5, 18) are replaced by "hub", not just the identifier swap.
- **[NOTE] init.go .gitignore vs .git/info/exclude tension unstated** — Added explicit line in Weft model decision: `.git/info/exclude` mechanism is task 006's concern; task 005 leaves existing `lyx init` `.gitignore` block untouched (`.lyx/`, no conflict with config-path migration).
- **[NOTE] init.go production-change scope under-specified** — Updated init.go row to explicitly name the `_lyx/config/` mkdir step and both `os.WriteFile` path retargets (lines 61 and 82).

## Pushed Back

_(none)_
