# Plan fix report — round 6

Source review: `_mill/reviews/20260801-085301-plan-review-r6.md` (verdict REQUEST_CHANGES, 3 BLOCKING). All VERIFIED accurate and FIXED. All three are shallow mechanical/consistency gaps (two are fallout from this loop's own earlier-round edits).

## Fixed

- [BLOCKING] "Card 17 migration leaves `weftBaseDir` a dead variable" — CONFIRMED: `weft_verbs.go:122` declares `weftBaseDir` solely to feed the `LoadConfig(weftBaseDir)` at :124; switching that call's base to `BoardDir(l.Hub)` (round-3/4 fix) orphans it → "declared and not used" compile error. Fixed card 17 part (a) to explicitly remove the `weftBaseDir` declaration and its comment.
- [BLOCKING] "`doc.go`'s stale `lyx init` mentions are never swept" — CONFIRMED: `doc.go:34,36,38,42` name `lyx init` as a live remedy; card 28's sweep lists every other file but not `doc.go`, and card 32 only appended. Fixed by extending card 32 (the sole batch-7 editor of `doc.go`, keeping the file in one batch) to reword all four mentions to `lyx fabric reconcile`/eager-wiring.
- [BLOCKING] "Batch 3's own 'Batch Tests' section contradicts its `verify:` field" — CONFIRMED: the round-5 fix widened batch 3's `verify:` to `-tags integration ./internal/configsync/... ./internal/initengine/...` but left the Batch Tests prose describing the old `go test ./internal/configsync/...` ("no -tags needed"). Fixed the Batch Tests paragraph to match the real command and explain why `initengine` + `-tags integration` are included (to run the card-11 skip-neutralized tests).

## Pushed Back

(none — all three findings were factually accurate and fixing them caused no harm.)

## Note

Per the operator's mid-loop directive, plan-review was bumped from 6 to 8 rounds and the holistic reviewer switched from `sonnetxhigh` to `sonnethigh` (worktree-local override in `.millhouse/config.local.yaml`). The loop continues to round 7 within the new cap; no max-rounds escape fired.
