# Plan-fix round 4 — fabric-commit-api

Source review: `_mill/reviews/20260730-100932-plan-review-r4.md` (holistic, reviewer sonnetxhigh, **APPROVE**, 0 BLOCKING + 3 NIT). Each NIT was run through the `mill-receiving-review` decision tree; all fixes land only in files under `_mill/plan/`. The plan is approved (`approved: true`).

## Fixed

- [NIT] "CommitWeft's stale doc comment not relocated" — VERIFIED accurate: Card 3 extracts `CommitWeft`'s body into `commitWeftLocked` but never directed moving its detailed doc comment, so the public wrapper would keep a doc describing internals it now delegates and gain no `snapshotTags` mention. Fixed in `01-commit-foundations.md` Card 3: added a doc-comment-hygiene requirement to relocate the detailed narration onto `commitWeftLocked` and leave `CommitWeft` a short wrapper doc that mentions the `snapshotTags ...string` variadic.
- [NIT] "Untested 'tags silently dropped on warp-only commit' behavior" — VERIFIED accurate: Card 7 documents that a warp-only `Fabric.Commit` silently drops `snapshotTags`, but Card 8's assertions had no warp-only + non-empty-`snapshotTags` case. Fixed in `03-fabric-commit.md` Card 8: added assertion (9) — a warp-only `Fabric.Commit` with non-empty `snapshotTags` returns no error, commits the warp side, and the warp commit carries no `Snapshot:` (nor `Warp-SHA`) trailer.
- [NIT] "opts.SkipPush ignored by Fabric.Commit's async push, not called out in docs" — VERIFIED accurate: `SpawnDetachedPush` gates only on `WEFT_SKIP_GIT`/`WEFT_SKIP_PUSH` env vars, and `Fabric.Commit` passes no opts to it, so `opts.SkipPush` is not consulted for the async push. Honoring it would contradict the documented helper-internal-gating decision (`async-push-both-sides-via-detached-child`), so the correct fix is to document. Fixed in `05-docs.md` Card 14: the doc.go requirement now states `opts.SkipPush` is also not consulted by the async push (env-var gating only), mirroring the existing `opts.SkipGit` sentence.

## Pushed Back

_None. All three NITs were factually accurate and passed the harm check (two doc/test additions and one documentation clarification; the SkipPush fix documents rather than changes behavior, staying consistent with the design decision rather than conflicting with it)._
