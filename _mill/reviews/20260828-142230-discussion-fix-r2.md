# Discussion fix — round 2

Source review: `_mill/reviews/20260828-141854-discussion-review-r2.md`

## Fixed

- `[BLOCKING:design]` "Corpse header as anchor on the launch path is undecided" — the finding is correct: the reap-before-allocate chokepoint makes the gate fire from `add`/`update`, which never call `ensureHeaderPaneLocked`, so the "a corpse is healed on the next `up`/`resume`" argument that justified presence does not hold there.
  Disposition taken: **require an alive anchor, uniformly** — the decision is renamed `reap-gate-accepts-an-alive-header-as-survival-anchor` and the gate is now `anyBoundPresent || headerAlive`.
  Added the reasoning for one rule rather than a per-call-site anchor strength (`Up`/`Resume` always have an alive header at that point, so aliveness costs nothing where the reap is needed), an explicit statement of the corpse-header-on-`add` behaviour (reap deferred, `planPaneTarget` splits the alive foreign pane, `validateSplitCreatedNewPane` still guarantees a fresh pane, tail reconcile converges within the same verb), and a note that the header's *exemption from being killed* stays keyed on presence.
  Rejected-alternatives list and Q&A entry updated to match.
- `[NIT:consistency]` "Non-regression claim misstates when the reap fires" — correct: under the new gate the reap fires on that test's second `up`, so `smoke_lifecycle_test.go`'s own comments become false while its loose `len(panes) == 0` assertion still passes.
  Replaced the "must keep passing unchanged" claim with an explicit instruction to rewrite the comments to the new post-`up` pane set and tighten the `up` assertion to the exact expected set, while keeping what the test exists to pin (no zero-cell layout string).
- `[NIT:decision]` "Reap log line: optional distinction vs mandatory identifiability" — correct: `planReconcile` returns one merged `panesToKill`, so "identifiable in the log" is unsatisfiable without a distinction.
  Made the distinction required and left only its mechanism to mill-plan, and stated that `planReconcile`'s return shape changing to carry the two kill reasons apart is in scope.
- `[NIT:scope]` "Adoption-describing comments outside the file list" — correct.
  Named `spawn.go`'s file-header comment and `strand.go`'s `RemoveStrand` kill-loop comment as in-scope doc surface, called out `doc.go`'s dead-pane-adoption load-bearing bullet and the header-exclusion-seams sentence specifically, and amended the Out list so `RemoveStrand` reads "logic untouched" rather than implying its comments are out of scope.

## Pushed Back

_None._
