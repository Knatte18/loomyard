# Discussion fix — round 5

Source review: `_mill/reviews/20260816-144507-discussion-review-r5.md` (APPROVE, 0 blocking, 3 NIT, none demoted — convergence gate satisfied).

## Fixed

- r5 — "Webster error-path test site left unassigned": now names **both** `RenderRecoveryPrompt` and `RenderMasterPrompt` as required sites, with the reason — each needs its own hoist-plus-error-check, and a dropped check at one site is invisible to a test at the other.
- r5 — "`stencilstore`'s allowlist rationale unstated": the two allowlist entries now carry explicitly different justifications. `internal/stencil` is a zero-import leaf (the `lyxdirs` argument); `internal/stencilstore` is not a leaf and rests on shared-infrastructure-not-feature-package plus a verified-acyclic closure. Confirmed `stencilstore` imports `internal/stencil` and `internal/logger`, so the leafness wording would have been false for it.
- r5 — "Strip-only vs CRLF claim slightly overstated": reworded as accepted parity with `Fill` rather than as CRLF being eliminated, naming the operator-edits-board-copy case that `Classify` reports untouched because `BodyHash` normalises and `Read` does not.

## Pushed Back

None.
