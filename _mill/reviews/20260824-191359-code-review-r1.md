MILL_REVIEW_BEGIN
# Review: webster: DAG-derived card sequencing — holistic

```yaml
duration_s: 264.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-24
```

## Findings

No findings. The implementation matches the plan across all three batches:

- `internal/websterengine/sequence.go` implements exactly the four-step mechanism (vertex keys, `deriveEdges`, `stronglyConnected` Tarjan, `orderComponents` Kahn) the plan specifies, with the exact import list (`container/heap`, `sort`, `strings`, `batcher`), purity/length-preservation/no-op properties, and `Cycle.Warning()` as sole renderer. `sequence_test.go` covers every scenario the plan's card 2 and `discussion.md`'s testing section enumerate (symbol/path matches, shared-`Targets` tie-break, self-overlap, rename endpoints, multi-card batch intra-batch edges, acyclic/no-op/producer-before-consumer ordering, 2/3-card and disjoint cycles, structural guarantees, nil/empty input, `Cycle.Warning`).
- All five batch-computation sites (`Run`, and webstercli's `begin-batch`/`await-batch`/`record-batch`/`recover-batch`) call `SequenceBatches` per the `sequencing-applied-at-every-batch-computation-site` decision, each with the required "every batch-computation site sequences" comment.
- `render.go`'s `RenderBatchIndex`/`RenderProgress`/`RenderMasterPrompt` signatures changed exactly as card 3 specifies (`[]batcher.Batch` in place of `*planparser.Plan`, `plan` param dropped entirely), and `RenderIntegrationPrompt` correctly stays on `*planparser.Plan`.
- `runlevel.go`'s `Run` re-binds `batches` through `SequenceBatches` right after the zero-batch refusal, adds `RunResult.Cycles`, prepends cycle warnings ahead of integration warnings, and corrects `accumulatedCardSHAs`' doc comment to "execution order" — matching card 4 exactly, including the documented non-done-outcome cycle-visibility limitation.
- `predecessorDigestLine` in `beginbatch.go` is shared verbatim by `recoverbatch.go` (card 6), with both call sites and doc-comment updates matching cards 5–6, and both new `beginbatch_test.go`/`recoverbatch_test.go` reordering sub-tests (card 10) and `runlevel_test.go`'s three new Run-level tests (card 11) are present and exercise exactly the scenarios specified.
- `webstercli/run.go`'s `cycles` envelope key defaults to a non-nil empty slice and is documented in `Long` help text, matching card 7.
- The master template reword (card 8) hits every required point: "Drive it STRICTLY in order" kept verbatim, ordering now explicitly means listed order not ascending number, both retired clauses ("no DAG here to reorder around", "batch N assumes...") are gone and replaced by one clean statement, the loop-section phrase updated, no `oversized`/`chain`/`## Scope`/host-sense/weft-warp tokens introduced. `template_test.go` (card 9) adds matching coverage including `TestMasterTemplate_OrderingRuleMeansListedOrderNotAscendingNumber`'s explicit `requireNotContains` on both retired clauses.
- `doc.go`'s dead-DAG-seam section is fully retitled and rewritten (card 12) with `HasSymbolFields()` removed, the opening paragraph's "sequential...declared card order" corrected without weakening the sequential/one-fork-at-a-time claim, and the `RenderProgress` parenthetical corrected to "batch numbers, which are equally positive."
- Doc batch (cards 13–16): `loom-plan-spec.md`'s two false claims are corrected per the plan's literal replacement instructions (the "Plan vs. schedule" sentence's replaced clause is inserted exactly where specified, preserving the surrounding "potentially wave-based..." tail); `docs/overview.md`'s webster bullet gains the sequencing/cycle clause while the batcher bullet and link line are untouched; `manifest/roadmap.md` moves the item to `## Done` with all the required content and repoints the Someday item's dependency pointer to "(Done, above)"; `internal/planparser/validate.go`'s banner sentence is corrected as a comment-only edit with no other change to the file.
- `internal/batcher` (`batcher.go`, `identity.go`, `doc.go`) and `internal/planparser` (`plan.go`) are confirmed unmodified beyond validate.go's one sentence, per the `no-planparser-and-no-batcher-change` decision.
- No out-of-plan files, no duplicated helpers across batches (predecessor-digest logic lives once, in `beginbatch.go`, reused by `recoverbatch.go` via same-package call), and cross-batch signatures line up (batch 2's `render.go`/`runlevel.go` changes compile against batch 1's `SequenceBatches`/`Cycle`, batch 3's doc edits accurately describe batch 2's shipped mechanism).

## Verdict

APPROVE
Implementation fully realizes the plan across all three batches with no deviations, gaps, or constraint violations found.
MILL_REVIEW_END
