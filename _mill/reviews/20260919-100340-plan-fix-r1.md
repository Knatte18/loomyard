# Plan fix — round 1

Source review: `20260919-100115-plan-review-r1.md` (holistic, sonnetxhigh).

## Fixed

- `20260919-100115-plan-review-r1.md` — "Card 7 Context omits Status()'s defining file": added `internal/reedengine/lifecycle.go` to card 7's `Context:`, which is where `func (e *Engine) Status() (StatusResult, error)` is declared.
- `20260919-100115-plan-review-r1.md` — "Card 11 cites awaitRunLock from an un-Contexted file": added `internal/loomcli/bootstrap.go` to card 11's `Context:`, which is where `awaitRunLock` and its four injected seams are declared.
- `20260919-100115-plan-review-r1.md` — "Card 14 names three fixtures only smoke_test.go defines": added `internal/loomcli/smoke_test.go` to card 14's `Context:`, which declares `probeReedEngine`, `statusStrandCount` and `tmuxBinaryPath`.
- `20260919-100115-plan-review-r1.md` — "Card 13's cited test precedent doesn't reach a receiver": the finding is correct that neither `Command()` nor `StartAliasCommand()` exposes its constructed `*loomCLI`, and that an independently-built replica would not catch the nil-field defect. Both halves of the card were restated rather than only the test. Card 11 now introduces an unexported `newLoomCLI()` factory carrying both field initialisations and makes it the only production site that builds a `loomCLI` value, which designs the two-constructor gotcha out instead of testing for it after the fact. Card 13's assertion splits to match: a direct field assertion over `newLoomCLI()`'s return value, plus a source scan pinning that no production file in the package builds a `&loomCLI{` literal outside the factory's own file — the same scan shape `internal/burlercli/wiring_test.go` already uses. No accessor is added to either constructor. Card 13's `Context:` gained `internal/burlercli/wiring_test.go` as the cited scan precedent, and card 11's commit message was updated to name the factory.
- `20260919-100115-plan-review-r1.md` — "[NIT] Card 15 references a \"process-kill helper\" that doesn't exist as such": reworded card 15's requirement to name the find-then-`proc.KillPID` pattern `registerBootstrapTeardown` actually applies, rather than a helper function that does not exist.

## Pushed Back

_None. Every finding was accurate and every fix was applied._
