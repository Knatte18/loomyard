# Discussion fix — round 6 (final round, round cap)

Source review: `_mill/reviews/20260906-101914-discussion-review-r6.md` (reviewer `opus`, effort medium, verdict REQUEST_CHANGES — 1 BLOCKING, 2 NIT after the blocking-class ceiling demoted one).

Every finding was verified against the tree before acting. All three were accurate; none were pushed back.

This is round 6 of a configured maximum of 6. The operator pre-authorized approve-and-handoff at the cap, so these fixes are applied and the phase proceeds to Handoff rather than opening a seventh round.

## Fixed

- r6 — "No disposition for a quarry call that errors" (`[BLOCKING:design]`) — accurate and a real hole: `quarry.Open`, `(*Repo).Resolve` and `(*Repo).DeltaGit` each return an `error`, and every prior decision covered only the four per-target *statuses*.
  Added a new decision `quarry-error-disposition` giving a per-boundary policy: block at `Plan-Revalidate` and `begin-batch`, block the done-checks at `record-batch`, degrade only the scope guard to informational, and never report an infrastructure error as a plan finding.
  Cited quarry's own contract, which draws the same line — the failure envelope's `ok` key "marks that quarry could not answer at all, and never that the answer is negative".
  Named the specific disaster this prevents: because `create-target-verdict` makes `not_found` a **pass**, a transport failure read as a clean `not_found` would silently mark every `Create` card done.

- r6 — "Parity pair's shared function left unnamed" (`[NIT:consistency]`, demoted from BLOCKING) — accurate: moving the resolve pass into `planglyph` means the two plan pairs can no longer call `planparser.Validate`, which is what CONSTRAINTS.md's parity bullet literally spells.
  Named both entry points in `package-ownership` — `planglyph.ValidateFormat` and `planglyph.Validate`, mirroring `planparser`'s own pair — and assigned them to the two parity pairs explicitly.
  Noted that under `language: none` both still run and simply skip the resolve pass, so the parity pair is one function in every mode rather than branching by plan language.
  Added the parity bullet as a fourth CONSTRAINTS.md edit and updated the Constraints and Testing sections to match.

- r6 — "CLI/Cobra module-count line not in the edit list" (`[NIT:consistency]`) — accurate: the invariant's "eleven of twelve also carry `RunCLIIn`" line goes stale when `quarrycli` is registered.
  Folded it into the CLI/Cobra edit, which is now explicitly a two-place edit (package-naming deviation line plus module-count line), and recorded that `quarrycli` does carry `RunCLIIn` because its root resolution is cwd-dependent per `planglyph-root-resolution`.

## Pushed Back

None.

## Gaps auto-picked

One BLOCKING finding, resolved by auto-picking the recommended option per the operator's standing instruction. Logged in `discussion.md`'s `## Q&A log` with the `[auto-pick]` marker and rationale.

## Round-cap note

Round 6 returned REQUEST_CHANGES, so the Convergence gate's implicit-approval branch (which applies only to an APPROVE round) did not fire. The phase proceeds to Handoff under the operator's explicit pre-authorization to approve and hand off at the cap, with all of round 6's findings fixed first rather than carried forward unaddressed.
