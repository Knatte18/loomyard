# Plan fixer report — round 6

Source review: `_mill/reviews/20260625-135251-plan-review-r6.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 2 NITs).

## Fixed

- `20260625-135251-plan-review-r6.md` — [BLOCKING] "Card 14 breaks existing initcli tests (no-weft path)" — card 14 now requires adapting `TestRunInit_FirstRun`/`TestRunInit_Idempotent`: they run in a bare `git init` tmpDir with no `<base>-weft` sibling, which now hits the no-pairing early-return and skips `configsync.ReconcileAll`; the card now says to seed a `<base>-weft` weft-sibling pairing first so the activation+reconcile path runs and their config-creation/idempotency assertions still hold, with the new activation / missing-pairing cases added separately.
- `20260625-135251-plan-review-r6.md` — [NIT] "Card 10 underspecifies de-qualifying external-test files" — card 10 now distinguishes the two test package flavours: the internal `package worktree` files become `package warp` (unqualified); the three external `package worktree_test` files (`config_test.go`, `cli_test.go`→`warp_test.go`, `list_test.go`) become `package warp_test`, drop the `internal/worktree` self-import (use `internal/warp`), and re-qualify `worktree.X` → `warp.X`.
- `20260625-135251-plan-review-r6.md` — [NIT] "Card 14 slug derivation needs an explicit Layout resolve" — card 14 now states `RunInit` must build the layout via `l, err := paths.Resolve(cwd)` (today it only calls `paths.Getwd()`) before deriving the slug/sibling.

## Pushed Back

(none — all three findings were factually accurate with no harm in fixing; applied under the default fix-everything rule.)
