# Plan fix — round 2

Source review: `_mill/reviews/20260624-170153-plan-review-r2.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260624-170153-plan-review-r2.md` — [NIT] "Card 1 cites cli.go default case but omits it from Context": reworded Card 1's Requirements to drop the `internal/worktree/cli.go` file-path citation. The behavioural note (`UnknownSubcommand` keeps passing because `bogus` is rejected as an unknown subcommand once config loads) is retained as explanation without naming a file the implementer must read — the edit remains a pure RHS path substitution in `cli_test.go`, so no Context entry is needed.

## Pushed Back

- `20260624-170153-plan-review-r2.md` — [NIT] "Top-level overview verify is null": left as-is. The `plan-overview.md` template intentionally ships `verify: null` at the overview level; mill-go schedules and runs the per-batch `verify:` commands (all four are populated), not the overview-level one. Setting a top-level command would document a gate that is never executed and deviate from the template's documented design (legitimate-pushback category: conflicts with documented design). The reviewer explicitly marked this NIT optional ("leave as-is or set it to the union").
