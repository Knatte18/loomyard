# Plan fix report — round 1

Source review: `20260628-195902-plan-review-r1.md` (verdict APPROVE, 0 blocking, 2 NITs).

## Fixed

- `20260628-195902-plan-review-r1.md` — "[NIT] Cards 2 and 3 are mutually compile-dependent": reordered the batch so `suite.go` is now Card 2 (before the dispatch card, now Card 3) and moved the `hostDirName` const into `suite.go`; each commit now leaves `go build ./tools/sandbox` green (a package-level `runSuite` with no caller compiles fine). Updated the batch-local conventions note and the Card 3 cross-reference (`runSuite` added in Card 2) accordingly.
- `20260628-195902-plan-review-r1.md` — "[NIT] `//go:embed` into a string var needs the `embed` import": Card 2 Requirements now state `suite.go` must add a blank `import _ "embed"` alongside the `//go:embed test-scheme.md` directive.

## Pushed Back

(none)
