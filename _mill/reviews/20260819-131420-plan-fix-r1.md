# Plan fix — round 1

Source review: `_mill/reviews/20260819-131248-plan-review-r1.md` (holistic, `sonnetxhigh`).

## Fixed

- `[BLOCKING:design]` "landingshed.Deps has no way to obtain a Shuttle for the resolver" — accurate; card 20's field list omitted the session-runner seam that the resolver's own constructor rejects a nil value for, so neither producer could have built a resolver.
  Card 20 now carries a told `Shuttle mergeresolve.Shuttle` field and declares the unexported one-method resolver seam both producers hold their resolver behind, with a compile-time satisfaction assertion.
  Cards 23 and 24 now state that each constructor builds the resolver at construction time from the told values and returns a construction failure rather than deferring it, and explain why eager construction does not conflict with the laziness rule the two pair-opener closures carry — the pair constructor stat-checks a layout that may not be wired yet, whereas the resolver's constructor performs no I/O.
  Card 26 now asserts the constructor rejects a nil session-runner seam and records that both unit tiers live in-package so they can substitute the unexported seam; card 27 records the same placement reason.
  Card 34 in batch 5 now requires the shared fixture helper's landing passthrough to carry every field the constructors require, including the session-runner seam.

- `[NIT:scope]` "shed.md link-count claim off by one" — accurate; the status-banner reference names the file inside a code span rather than in link syntax, so it is prose the link guard never enforces.
  Card 38's inventory line for that file now reads three links plus one prose mention, names which three are links, and tells the implementer not to force the banner's code span into link syntax it never had.

## Pushed Back

_None. Both findings verified accurate, and neither fix breaks existing functionality, conflicts with a documented decision, nor destabilizes work outside this task's scope._
