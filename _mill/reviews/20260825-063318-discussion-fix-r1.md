# Discussion fix — round 1

Source review: `_mill/reviews/20260825-063253-discussion-review-r1.md` (APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `20260825-063253-discussion-review-r1.md` — "Credential-provisioning step has no named owner or step, despite gating \"done\"" (`[NIT:scope]`). Added a "Manual operator prerequisite" paragraph to `## Scope` naming the operator as owner, `https://www.reddit.com/prefs/apps` as the registration point, a script-type app as the app kind, and the operator's own shell (not CI — this repo runs no CI job for the nested `plugins/prowler` module) as where the two env vars must reach; stated that the plan must schedule it as an explicit gating step rather than a footnote, and restated the never-committed/never-logged handling. Added a cross-reference from `## Testing`'s live-credentialed-smoke-test bullet back to that prerequisite so the dependency is visible from both ends.

## Pushed Back

None.
