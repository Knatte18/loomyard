MILL_REVIEW_BEGIN
# Review: Extract scout into its own standalone repo — holistic

```yaml
duration_s: 2129.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 4.5 (Anthropic), self-assessed
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [BLOCKING:scope] Card 11's Context omits the ~20 test-file sources the port actually needs
**Location:** batch 03-port-engine, Card 11
**Issue:** Requirements says "run the port program ... for all 34 files listed above," but `Context:` lists only 14 non-test `.go` files plus `tools/port/main.go` (15 entries). The 34 figure only appears in `Creates:` (34 destination paths); the corresponding 20 `_test.go`/`_integration_test.go` source filenames (including the two renamed ones, `scoutdaemon_test.go`→`quarrydaemon_test.go` and `supervised_scout_test.go`→`supervised_lsp_test.go`) are never listed anywhere in `Context:`.
**Fix:** Add the 20 missing scoutengine test-file paths to Card 11's `Context:` so the source filename list is self-contained, matching the pattern Card 24 (batch 04) correctly follows by listing both `cli.go` and `cli_test.go`.

### [BLOCKING:scope] Card 17's lyx-vocabulary sweep Edits list omits two production files it needs to cover
**Location:** batch 03-port-engine, Card 17 (and its own completion gate)
**Issue:** `ensureserver.go` carries 6 "lyx" mentions in doc comments (lines 40, 160, 257, 260, 357, 496) untouched by Card 14 (which only rewrites the anchorRoot→stateDir rename and the file header), and `toolchain.go` carries a "lyx" hit via `internal/lyxcwd`/"Cwd Resolution Invariant" (line 10) untouched by Card 16 (which only fixes the two path joins, one comment, and the discussion.md citation). Neither production file is in Card 17's `Edits:` list, and neither is among the cards-18-through-22 files Card 17 explicitly exempts from its own "confirm every remaining hit is zero" gate — so that gate, as specified, cannot pass.
**Fix:** Add `ensureserver.go` and `toolchain.go` to Card 17's `Edits:` list (or explicitly widen its exemption), and have it sweep the specific lines identified above.

### [NIT:consistency] Card 46 says it may fix files and record in a commit, but declares Edits: none and Commit: none
**Demoted-from:** BLOCKING
**Location:** batch 06-lyx-removal, Card 46
**Issue:** Requirements instructs "either fix it or record in the batch's final commit message why it stays" for any surviving `scout` grep hit — but the card's own fields are `Edits: none`, `Creates: none`, and `Commit: none`, and it is explicitly the last card in the batch, so there is no commit left for a fix or a justification to land in.
**Fix:** Either give Card 46 a real `Commit:` (and drop "none") to hold the sweep's justification note, or rescope its Requirements to report-only (no "fix it" branch) and push any needed fix back into the specific card that owns that file.

### [NIT:consistency] Batch 04's frontmatter says cards: 9, but it contains 10 cards
**Location:** batch 04-port-cli.md, header
**Issue:** `cards: 9` in the batch's yaml frontmatter, but the file has Cards 24 through 33 inclusive — 10 cards. Global step numbering (24-33) is itself correct and sequential; only the declared count is wrong.
**Fix:** Change `cards: 9` to `cards: 10`.

### [NIT:consistency] "dependency budget is exactly four external modules" names five modules in its own body
**Location:** 00-overview.md, Shared Decision "dependency budget is exactly four external modules"
**Issue:** The decision's title says four, but its body names five distinct module paths: `cobra`, `yaml.v3`, `flock` (direct) plus `pflag`/`mousetrap` (cobra's indirects). Card 1's own operative text ("no fourth direct external dependency") is unambiguous and correctly implemented downstream; only the shared-decision heading's count is off.
**Fix:** Retitle the decision (e.g. "three direct dependencies plus cobra's own indirects") or otherwise reconcile the heading with the five modules its body lists.

## Verdict

REQUEST_CHANGES
Two scope gaps would make the plan's own stated completion gates fail as written, plus a self-contradictory no-op card.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 2._
MILL_REVIEW_END
