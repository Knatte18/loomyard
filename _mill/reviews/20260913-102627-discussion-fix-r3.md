# Discussion fix — round 3

Source review: `_mill/reviews/20260913-102454-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 5 NIT, none demoted).

## Fixed

- `20260913-102454-discussion-review-r3.md` — "[NIT:consistency] Check order (unknown-key before duplicate) only implied": the duplicate-key Decision gains an explicit ordering bullet stating that canonicalization/unknown-key rejection must move ahead of the duplicate check (currently `parse.go:146-148` then `:149-151`), with the reason and the `opus[v=4.8,version=4.8]` consequence; Technical context's check-list now cites both line ranges and states the two checks swap.
- `20260913-102454-discussion-review-r3.md` — "[NIT:consistency] \"four named files\" contradicts the five-file Scope list": Technical context and the Q&A entry now say "the five files named in Scope"; the remaining Q&A mention is re-worded to "the four files the task brief names", which is what that question was about.
- `20260913-102454-discussion-review-r3.md` — "[NIT:scope] Load-time validators are the real operator-facing break surface": the `version=`-removal Decision gains a bullet naming `internal/websterengine/config.go:71`, `internal/landingshed/config.go:52`, and `internal/loomengine/config.go:255,259,263,270` as the load-time sites where the hint surfaces, with the wrapped error shape an operator sees.
- `20260913-102454-discussion-review-r3.md` — "[NIT:scope] `internal/modelspec/template.yaml` disposition unstated": added to Scope "Out" as explicitly unchanged (canonical `effort:` defaults key, no bracket string), together with a matching Out bullet for the three load-time validators.
- `20260913-102454-discussion-review-r3.md` — "[NIT:scope] Duplicate-error message format has no test assertion": the reject-case list now pins a segment-quoting assertion on `opus[high,effort=max]` and a new `opus[v=4.8,v=4.5]` case, adds `opus[v=4.8,version=4.8]` → `unknown param key` as the check-order regression guard, and notes the single-`wantSubstr` table row must gain a second substring (widened literal or optional field).

## Pushed Back

None.
