# Discussion NIT fixes — round 3

Source review: `_mill/reviews/20260821-115206-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 4 NIT).
Auto mode: every NIT is fixed under `mill-receiving-review`'s default rule;
pushback is unavailable, since no operator is present to escalate to.

## Fixed

- `[NIT:scope]` "Twelve-engine fixture omits `Env.Landing`" — verified against `internal/shedrecipe/fixture_test.go`'s `newTestEnv` (leaves `Env.Landing` zero) and `internal/landingshed/publish.go`'s `NewPublish` (rejects nil `OpenFabric`/`PushBranch`). Testing § `Build` now names `Env.Landing` as a twelfth required field, points at `coverage_guard_test.go`'s `coverageGuardLandingDeps`/`coverageGuardNilFabricOpener`/`coverageGuardFakeMergeShuttle` as the ready-made pattern, and corrects the "eleven things"/"does the whole job" wording to "covers most of the job" plus an explicit not-sufficient-on-its-own sentence.
- `[NIT:consistency]` "Coverage-guard helpers described as an `Env` pattern" — Technical context reworded: the four helpers are now stated to build `loomshed.Deps`-shaped values only, with `internal/shedrecipe/fixture_test.go`'s `newTestEnv` named as the `Env`-side pattern and the `Env.Landing` gap flagged forward to the Testing section.
- `[NIT:consistency]` "`config` mapping check assigned to two owners" — "`config` a mapping if present" removed from `Parse`'s own-check list in the validation-split decision, with an explicit sentence recording that it is decoder-enforced because `Row.Config` is typed `map[string]any`.
- `[NIT:design]` "Missing `version` is indistinguishable from `version: 0`" — both the `version` decision and the `Parse` test list now state that an absent `version` and a literal `version: 0` collapse into one `unsupported recipe version 0` message, with the `int`-over-`*int` choice and its consequence recorded; empty/comment-only input is given its own `shedbuild: recipe is empty` contract, translating `yaml.Decoder.Decode`'s `io.EOF`.

## Pushed Back

None.
All four findings verified accurate against the cited code before fixing, and none met the decision tree's harm criteria.
