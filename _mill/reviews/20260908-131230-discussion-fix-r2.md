# Discussion fix — round 2

Source review: `_mill/reviews/20260908-130750-discussion-review-r2.md` (APPROVE, 0 BLOCKING, 6 NIT).

## Fixed

- r2 review, "Derive test call-site enumeration is incomplete" — corrected six to eight, adding `webstercli/wiring_test.go:165` and the second `standalonegeom/reedgeom_symlink_integration_test.go` call at :49.
  Verified against source: eight call sites, exactly as the finding states.
- r2 review, "`ResolvePlanDir` is a method but violates the stated method rule" — moved it to a package-level function beside the other pure helpers, with the reason stated (infallible, no message, reads no descriptor field).
- r2 review, "`StandaloneRequest` field semantics left implicit" — stated that the request carries raw flag values and `ResolveStandalone` resolves them itself, that both `wire` methods keep resolving for their own hub branch, and that the resulting double resolution on the standalone path is harmless because `ResolveToldDir` is idempotent on an absolute input.
- r2 review, "Missing-plan refusal text shape unspecified" — named the shape: `MissingPlanRefusal func(planDir, defaultPlanDir string) string`, chosen over a format string so the two-path argument order is a compile-time fact rather than a comment.
- r2 review, "`internal/preflight` not named among rejected homes" — added it to the rejected-homes list, disqualified on its import set (`lyxcwd`, `fabricengine`), with the mode-selection-stays / post-mode-work-moves split stated.
- r2 review, "\"Only webstercli and burlercli are standalone-capable\" is slightly overbroad" — narrowed to "the only CLIs that derive standalone state geometry" and named `quarrycli/cli.go:58-73` as the `ResolveMode` caller that is correctly out of scope.

## Pushed Back

None.
All six findings were verified accurate against the source before fixing.
