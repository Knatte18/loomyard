# Plan fix — round 6

Source review: `_mill/reviews/20260821-154725-plan-review-r6.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 3 NIT).

Every finding was verified against source before acting.
All four are accurate;
none met a harm-check exception.
All four fixed, none pushed back.

## Fixed

- r6 `[BLOCKING:scope]` "Moved sequence_test doc comment is never repaired" — verified `internal/loomshed/sequence_test.go:13` says the list is asserted literally "so a reordering in loomshed.go's producer table is a test failure rather than a silently-agreeing derivation".
  Card 8 moves that file into `internal/loomrecipe` and batch 5 then deletes the table it names, and card 26's sweep tokens are all path- or symbol-shaped, so none matches the bare `loomshed.go` spelling.
  Card 8 now restates the comment against `contracts/recipes/loom-recipe.yaml`'s row order — the property survives unchanged, only its subject moves — and keeps the rest, including why the sequence stops at `Publish` and why the real row 2 passes against this fixture.
  Added `contracts/recipes/loom-recipe.yaml` to card 8's `Context:`, and added the bare `loomshed.go` token to card 26's sweep list as the backstop.
- r6 `[NIT:scope]` "Card 8 scopes `Name*` qualification too narrowly" — verified `TestSequence_FullRunBlocksAtPublish` carries four bare `NamePublish` references outside `wantSequenceOrder` (`sequence_test.go:58`, `:59`, `:71`, `:89`, `:90`).
  Dropped the "in `wantSequenceOrder`" qualifier so the instruction covers the whole file, matching card 9's unrestricted wording, and named the out-of-var references explicitly so the count is not left to inference.
- r6 `[NIT:consistency]` "Card 13's \"expect zero hits\" grep contradicts itself" — accurate, and the real count is three rather than the two the finding cites: `internal/shedrecipe/seam_enforcement_test.go` names `loomshed` at lines 6 and 12 in its header prose and at line 36 as the `shedrecipeAllowedImports` entry.
  Restated the expectation as "hits only inside `seam_enforcement_test.go`", enumerated the three legitimate ones, and stated the actionable form: any hit outside that file means a test still reaches for `loomshed` and card 12 missed it.
  Also simplified the following paragraph, which framed leaving that file alone as conditional on an allowlist entry having gone stale — none has.
- r6 `[NIT:consistency]` "Card 24 both corrects and deletes the same paragraph" — accurate;
  three instructions applied to `manifest/roadmap.md:12-14`, two of them moot once the third deletes those lines.
  Removed the "close out the remaining-work framing" clause and the falsified-`loomshed.go:137-151` correction clause as they applied to the section intro, and stated the deletion once as that paragraph's whole disposition, with an explicit note that both other clauses apply to text the deletion removes.
  The same `loomshed.go:137-151` correction still stands for `manifest/designs/shed-recipe.md`'s "The idea" paragraph, which is not deleted.

## Pushed Back

_None._
All four findings verified accurate against source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope.
