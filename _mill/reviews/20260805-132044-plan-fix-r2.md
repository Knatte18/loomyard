# Plan fix — round 2

Source review: `_mill/reviews/20260805-131837-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260805-131837-plan-review-r2.md` — "[BLOCKING] New integration tests are never executed by any batch's verify". Verified and accurate: `go vet -tags "integration smoke scout"` type-checks tagged files but executes nothing, `go test` without tags does not compile them at all, and `pipeline.done_gate` was likewise untagged — so card 40's `_board`-junction assertions and card 7's stale-`.fabric-anchor` hard error would have been compiled and never run. Every gate that *can* run Tier 2 now does: batches 1 and 4 append `go test -tags integration ./...`; batches 5, 6 and 7 append `go test -tags integration` over the same package trees as their untagged run; and `pipeline.done_gate` becomes `go test ./... && go test -tags integration ./...`. Batches 2 and 3 gain nothing — their trees do not compile, which is why their gates are `go build`/`go vet` only — and batch 8 gains nothing because its sole code change is to an untagged file, with the extended done_gate closing the repo-wide tagged tier immediately after. The overview's Batch Index was regenerated from the batch files so the two cannot drift. Added a Shared Decision, "every gate that can run Tier 2 does, because this slice's new tests are all Tier 2", recording the split, the ~128 s per-run cost from `docs/benchmarks/running-tests.md`, and the fact that the vet stage still earns its place as the only check available in batches 2 and 3. The repo's own guidance supports this: `running-tests.md:85` says to reach for `-tags integration` precisely when changing fabric / hubgeometry / board / ide git behaviour, which is this whole task.
- `20260805-131837-plan-review-r2.md` — "[NIT] Batch 7's own `Batch Tests` section concedes the reachability gap without resolving it". Verified: the text described the gap accurately and then treated it as settled. Rewritten to state that `verify` now **executes** the tagged tests as its third command, and to say why that matters — a `go vet -tags` pass only type-checks, so without the tagged execution the link's wiring, its absence from `filterHubReserved`/`ScopedPathspec`, and its invisibility to `Healthy` would all be compiled and never checked. No TODO or handoff note was added, because the fix removes the residual risk rather than deferring it.

## Pushed Back

_None. Both findings were factually accurate, and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task. The added cost (~128 s per tagged run) is the documented price of Tier 2 and is exactly what the repo's testing guidance prescribes for a change of this shape._
