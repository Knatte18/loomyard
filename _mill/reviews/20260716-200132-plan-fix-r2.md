# Plan fix — round 2

Source review: `20260716-200010-plan-review-r2.md` (REQUEST_CHANGES, 1 BLOCKING + 1 LOW).

## Fixed

- [BLOCKING] "AuditForks widens Engine; 5 other doubles unpatched" — Card 5's Edits now
  list all five external Engine doubles (`shuttlecli/cli_test.go`,
  `builderengine/poll_test.go`, `builderengine/spawn_test.go`,
  `buildercli/poll_test.go`, `buildercli/spawnbatch_test.go`) with an explicit
  stub-requirement (return `ForkAudit{}, nil`, one-line comment), batch 2's verify
  widened to `./internal/shuttleengine/... ./internal/shuttlecli/
  ./internal/builderengine/ ./internal/buildercli/` (overview Batch Index updated to
  match), and All Files Touched gained the five files. Kept the core-interface
  approach over capability-splitting: compile-time enforcement on every implementer is
  the repo's seam posture, and the stubs are one line each.
- [LOW] "template.go marker-count doc goes stale" — `internal/burlerengine/template.go`
  added to Card 9's Edits; requirement now names updating the "eight markers" counts in
  both `template.go`'s header and `prompt.go`'s comments to nine. All Files Touched
  updated.

## Pushed Back

(none)
