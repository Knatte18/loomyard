MILL_REVIEW_BEGIN
# Review: Rename loom CLI run/drive/step for verb/engine symmetry, plus rename ly-supervise — holistic

```yaml
duration_s: 223.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

## Findings

### [BLOCKING:scope] Stale `run.go`/`drive.go` citation survives the rename
**Location:** `manifest/designs/reed-fabric-standalone-api.md:143`
**Issue:** This design doc (not listed in any batch's `Edits:`/`Context:`, and not in this round's file manifest) cites `internal/loomcli/run.go:145-320` and `drive.go:64` as where `loomcli` uses reed's `Up`/`Status`/`AddStrand`/`RemoveStrand`/`TmuxPath`/`AttachArgv`. After batch 1's moves those names/line ranges no longer describe the cited surface: the old `run.go` (bootstrap) is now `start.go`, and `drive.go` no longer exists (its content is now `run.go`). The doc's own header states every figure was grep-verified at a pinned commit; this citation is now false and untraceable.
**Fix:** Add this file to batch 5's sweep (or a follow-up card) and retarget the citation to `start.go`/`run.go` with corrected line numbers, per the same read-and-classify rule the rest of the batch follows.

## Verdict

REQUEST_CHANGES
One stale filename/line citation in an unswept design doc; all sampled Go, test, skill, and other manifest/docs files are correctly renamed.
MILL_REVIEW_END
