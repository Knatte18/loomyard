# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260831-154556-plan-review-r1.md`

## Fixed

- `20260831-154556-plan-review-r1.md` — "[BLOCKING:design] emit()/output unreachable in the --children branch": card 2's Requirements now hoists `output=()` out of the walk's three-line preamble and declares `emit` immediately after it, leaving only `queue=()`/`head=0` inside the recursive-only arm, and states that this ordering is what makes both available to card 3's branch. Card 3's Requirements now states that its branch point sits below that hoisted initialisation and declaration.
- `20260831-154556-plan-review-r1.md` — "[BLOCKING:design] Real private repo names committed via CAPTURE.md": the two literal private repository names are removed from card 8 entirely and replaced by a `gh repo list --visibility private` selection step plus a mandatory `<private-repo>` placeholder in both the card's own prose and the capture record, with the redaction reason stated. `CAPTURE.md` additionally opens with a one-line note recording that the redaction is deliberate. The fix is applied one step wider than the review asked: the plan file itself is committed to this public repository too, so leaving the names in card 8's Requirements would have disclosed them even with `CAPTURE.md` genericized. A new Shared Decision in `00-overview.md` binds the rule across every batch, so the docs batch cannot reintroduce a literal name either.
- `20260831-154556-plan-review-r1.md` — "[NIT:scope] Card 6 omits the stub-gh context it depends on": `plugins/prowler/scripts/testdata/github-tree/bin/gh` added to card 6's `Context:`, matching card 5.

## Pushed Back

_(none — every finding verified accurate with no harm from fixing.)_
