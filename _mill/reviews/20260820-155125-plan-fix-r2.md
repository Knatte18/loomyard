# Plan fix — round 2

Source review: `20260820-155046-plan-review-r2.md`

## Fixed

- `20260820-155046-plan-review-r2.md` — "[BLOCKING:design] Card 6's ShedProducer assertion precedes Card 8's Call, breaking intermediate builds": moved `var _ shedengine.ShedProducer = (*BurlerProducer)(nil)` out of card 6 and into card 8 alongside the `Call` implementation, so cards 6 and 7 each leave the package compiling.
- `20260820-155046-plan-review-r2.md` — "[NIT:consistency] Card 8's \"fresh profile copy\" rationale misattributes the reason": restated the rationale as needing fresh per-round `ReviewPath`/`FixerReportPath`/`PriorReviews`/`PriorFixerReports`/`ClusterExclude` values, and kept the by-value `Run` fact as a separate note rather than as the reason.

## Pushed Back

_None._
