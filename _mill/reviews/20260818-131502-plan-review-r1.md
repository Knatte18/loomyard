MILL_REVIEW_BEGIN
# Review: scoutengine told-geometry (optional uniformity pass) — holistic

```yaml
duration_s: 445.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (Sonnet 5)
reviewed_file: plan/
date: 2026-08-18
```

## Findings

### [BLOCKING:scope] Card 10's closing grep gate undercounts its own survivor set
**Location:** Batch 1, Card 10 (`01-told-anchor-root-conversion.md`).
**Issue:** The prescribed `grep -rn "layout\|Location" internal/scoutengine internal/scoutcli` also matches every occurrence of `internal/scoutengine`'s own LSP wire type — `lspLocation`, `LocationLink`, `formatLocation`, `BareLocationObject`, the `Location` field on LSP result structs — across `position.go`, `position_test.go`, `lspclient.go`, `lspclient_test.go`, `refs.go`, `symbol.go`, and `definition.go`. None of these is on the card's "known-intentional survivors" list (which names only the toolchain-cache paragraph, the live `lyxcwd.Resolve`/`CwdFrom` calls, `refs_integration_test.go`'s path-string literals, and `cli_test.go`'s degrade-outside-hub comments).
**Fix:** Either narrow the pattern (e.g. `\*lyxcwd\.Location` instead of bare `Location`) or add the LSP-`Location` family to the survivor list, so an implementer running this zero-diff gate doesn't treat scoutengine's own unrelated LSP-protocol naming as an incomplete conversion and start "fixing" it.

## Verdict
REQUEST_CHANGES
Card 10's closing verification gate needs its grep pattern or survivor list corrected before the batch can be trusted as zero-diff.
MILL_REVIEW_END
