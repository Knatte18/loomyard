# Plan fixer report — round 4

Source review: `_mill/reviews/20260625-134024-plan-review-r4.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260625-134024-plan-review-r4.md` — [BLOCKING] "Batch-4 verify can't reach card-12's configcli edit" — added `./internal/configcli/` to batch 4's `verify` list (overview Batch Index + batch file frontmatter + Batch Tests prose), so card 12's edit to the integration-tagged `configcli_integration_test.go` (rewiring to explicit `WireJunctions` after `Add` goes dormant) is compiled and run at the batch that makes the change, not deferred to batch 9.
- `20260625-134024-plan-review-r4.md` — [NIT] "Card 20 under-specifies the weft junction-only tests" — card 20 now states the two junction-dedicated tests (`TestStatus_Junction`, `TestStatus_JunctionOk_Windows`) are **removed entirely** (not just their assertions stripped) and the surviving `TestStatus` is updated to the trimmed `Status(weftRepo, []string{"_lyx"})` call signature.

## Pushed Back

(none — both findings were factually accurate with no harm in fixing; applied under the default fix-everything rule.)
