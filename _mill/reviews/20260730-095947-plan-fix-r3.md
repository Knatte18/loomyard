# Plan-fix round 3 — fabric-commit-api

Source review: `_mill/reviews/20260730-095738-plan-review-r3.md` (holistic, reviewer sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 1 NIT). Each finding was run through the `mill-receiving-review` decision tree; all fixes land only in files under `_mill/plan/`.

## Fixed

- [BLOCKING] "Card 4 Context missing gitrepo.go for gitrepo.New reference" — VERIFIED accurate: Card 4's Requirements call `gitrepo.New(warpPath).PushCoalesced()`; `New` is defined in `internal/gitrepo/gitrepo.go`, which was absent from Card 4's Context (only `internal/gitrepo/push.go` was listed). Fixed in `02-async-push-plumbing.md` Card 4: added `internal/gitrepo/gitrepo.go` to Context.
- [NIT] "Card 13's .gitrepo-push.lock exclusion assertion is vacuous" — VERIFIED accurate: `seedWeftArtifactExcludes` (`internal/fabricengine/weftgit.go:131`) genuinely seeds both `.weft/` and `gitrepo.PushLockFileName` into the weft `.git/info/exclude`, but `SyncWeft` pushes via `f.Weft.Push()` (`internal/fabricengine/syncweft.go:78` = `Repo.Push`), which never creates `.gitrepo-push.lock` — only `PushCoalesced` does (`internal/gitrepo/push.go:135`). So in Card 13's `CommitWeft`/`SyncWeft` flow the file never exists and asserting its absence passed trivially. Fixed in `04-unified-diff-status.md` Card 13: the test now explicitly creates a `.gitrepo-push.lock` file in the weft worktree root before calling `Status` (the exclude entry genuinely exists, so the assertion becomes real), scopes the artifact-exclusion assertion to the weft side only (the warp/host side is the documented known limitation), and added `internal/gitrepo/push.go` to Card 13's Context for the `gitrepo.PushLockFileName` reference.

## Pushed Back

_None. Both findings were factually accurate and passed the harm check (a Context addition and a test-strengthening edit, neither destabilizing any other batch)._
