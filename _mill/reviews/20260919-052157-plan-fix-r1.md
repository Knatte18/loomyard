# Plan fix — round 1

Source review: `_mill/briefs/review-plan-holistic-r1.out.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 3 BLOCKING + 1 NIT).

## Fixed

- r1 holistic, `[BLOCKING:scope] Three Context-only files carry unaddressed rename hits` — verified against the tree: `wiring.go:175` names both renamed verbs in `wire()`'s doc comment and carries stale `run.go`/`drive.go` citations at 334 and 401;
  `sharedbootstrap.go` carries the verb pair in its header and at 3, 30, 101, the quoted refusal at 137, and stale filename citations at 55, 158, 177, 225;
  `seedinput.go` carries `drive.go` citations at 56 and 59 plus the `drive-refuses-an-unrecorded-parent` decision name at 66.
  All three were `Context:`-only on card 2 and appeared in no card's `Edits:`.
  Added them to card 7's `Edits:` with per-file `Requirements:` classifying each hit class, and added the three paths to the overview's `All Files Touched`.
  Card 7 is the right home: every hit is comment or doc-string text, which is what that card already owns.
- r1 holistic, `[BLOCKING:scope] drive.go and bootstrap.go each carry one more hit than their cards name` — verified: `drive.go:218-233`'s `reflectFriction` doc comment carries its own "a second \"lyx loom run\" spawns a second driver" sentence, distinct from `shouldReflectFriction`'s at 207;
  `bootstrap.go`'s header names `run.go` as the location of the verb body.
  Extended card 2's Requirements to cover `reflectFriction`'s doc comment and card 7's to cover the `bootstrap.go` header citation.
- r1 holistic, `[BLOCKING:scope] main.go's module list still names the retired root alias` — verified at `cmd/lyx/main.go:75`: the root `Long`'s closing sentence lists `... stencil, loom, run, quarry.`.
  Card 2 covered only the `RunAliasCommand()` call site and its comment.
  Extended card 2's `main.go` Requirements to rename that list entry to `start`, citing the no-back-compat-aliases decision.
- r1 holistic, `[NIT:consistency] friction_test.go's test name still says "Drive"` — verified at `internal/loomcli/friction_test.go:108`.
  Renamed to `TestRunEnsuresAbsentFrictionDir` in card 7's Requirements, and corrected that card's own now-false claim that it changes no code — the test function is the card's one executable change, package-local with no other caller.
  Also retitled card 7 to "loomcli comment and filename-citation sweep" and changed its `Commit:` message, since the card's scope is no longer bootstrap-and-friction comments alone.

## Pushed Back

_(none — all four findings verified accurate against the tree, and no fix broke existing functionality, contradicted a recorded decision, or destabilised work outside this task.)_
