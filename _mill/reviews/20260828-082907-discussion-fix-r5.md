# Discussion fix — round 5

Source review: `_mill/reviews/20260828-082826-discussion-review-r5.md` (holistic, APPROVE, reviewer `opusmedium`).

## Fixed

- `[NIT:consistency]` "Seed-gate ordering assertion has no named seam" — dropped the ordering assertion.
  The `cmd/lyx` pin is now stated in full as the predicate's skip/proceed behaviour plus the annotation's presence on `reed header`;
  source-order placement is still required but explicitly recorded as not independently testable, with the optional seam named for any plan that wants it observable.
- `[NIT:scope]` "`internal/clihelp` absent from the Scope \"In\" list" — added `internal/clihelp` (one exported annotation-key constant, no behaviour change), and noted the `seedStencils` signature change under the `cmd/lyx` bullet.
- `[NIT:design]` "`ED 3` support in tmux/psmux is assumed, never stated as assumed" — recorded the asserted-not-verified disposition alongside the existing psmux command-form caveat, and named the one observation that confirms it: run B before the source fixes land, while the noise is still being written.

## Pushed Back

_None._
