# Discussion fix — round 1

Source review: `_mill/reviews/20260919-092217-discussion-review-r1.md` (verdict APPROVE, reviewer `orchestrator`).

Note on routing: the round-1 envelope parsed `nit_count: 0` because the finding's heading carries a doubled-bracket class form, `### [NIT][:consistency]`, rather than `### [NIT:consistency]`.
The finding was read off the review file per Phase: Discussion Review step 4a's file-confirmation rule and fixed under the 4b path.

## Fixed

- `20260919-092217-discussion-review-r1.md` — "Audit numbers and cited line ranges have drifted from current HEAD": re-counted every per-file figure with the audit's own method (case-sensitive matching lines for `Selvage`) and replaced the case-insensitive numbers in "Why now"; named the method explicitly, recorded that a case-insensitive count is a different measurement rather than a correction, and flagged the one real drift since `d39b30648` (`lifecycle.go` 57 → 58). Re-cited every entry in "Technical context" by its verified declaration line at HEAD, with an explicit instruction to locate each by name via `grep` rather than by number. Disambiguated `generation.go:151` as the `st.SelvagePaneID = ""` clear *inside* `adoptPaneGenerationLocked` (`generation.go:126`), which the reviewer had read as a claim about the function's own line. Added the method-restating requirement to both the Technical-context baseline note and the Testing section's post-extraction audit step, so the after-state is published as comparable to a stated before-state.

## Pushed Back

None.
