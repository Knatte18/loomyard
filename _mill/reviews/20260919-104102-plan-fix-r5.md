# Fixer report: plan-fix r5

Source review: `20260919-103748-plan-review-r5.md` (holistic, sonnetxhigh, APPROVE, 0 BLOCKING + 1 NIT).

## Fixed

- r5 holistic — `[NIT:consistency] watchdog.go's file header still claims reedengine gains "exactly one" function` — verified accurate against `internal/reedcli/watchdog.go` lines 1-10, whose header ends "internal/reedengine gains exactly one new engine-less function (ListSessions) and learns nothing about the daemon's existence". Card 9 adds `dispatchReap`, this file's first caller of `ReapSession`, which falsifies the first half.

  Placed the fix on card 9 rather than card 10 or 11: card 9 is the one that introduces the call, so the stale claim and its correction land in the same edit. The card now requires the sentence reworded to name both functions while keeping its second half — `reedengine` still learns nothing about the daemon, which is the claim the surrounding told-geometry paragraph actually rests on. Noted in the card that this mirrors card 2's correction to `ListSessions`' own doc comment: both stale claims assert "the one", and both are falsified by the same addition.

## Pushed Back

_None. The finding was verified accurate against the repository and the fix triggered no harm-check escape._
