# Plan fix — round 1

Source review: `_mill/reviews/20260919-132644-plan-review-r1.md` (holistic, REQUEST_CHANGES, 6 BLOCKING + 4 NIT).

Every finding was verified against the real source before acting;
all ten were factually accurate and none triggered a HARM CHECK exception.

## Fixed

- r1 holistic, "Arm's fixed signature leaves the module receiver unwired" — cards 21 and 25 now declare a two-function split: an unexported `(c *loomCLI) arm(cwd, verb, args)` the pre-run calls on its own receiver, and a thin exported `Arm` wrapper that constructs a fresh receiver for `shedcli`'s table. Verified `internal/loomcli/start.go` and `validate.go` do depend on a populated receiver, so the pre-run must keep wiring its own `c`.
- r1 holistic, "`--parent` has no route from Command() into loom's PreStep" — card 22 now names the carrier (a `parentFlag` field on `loomCLI`, bound by `Command()`, read by the hook) and states the `lyx shed step --recipe loom` disposition: no flag registered there, so the value is the empty string, which is what `lyx loom step` already passes when the flag is omitted.
- r1 holistic, "loom's run would perform the fabric block twice" — verified `sharedbootstrap.go`'s `buildLoomShed` already performs the whole fabric block plus the `Landing` assignment before calling `loomrecipe.New`. Card 21 now fills `BuildShed` per verb: `buildLoomShed` for `step`, `loomrecipe.New(c.env, c.shedPaths)` for `run`, whose own `PreRun` keeps the inline block it performs today.
- r1 holistic, "Batch 1 omits nine ShedPaths call sites" — confirmed all nine by repo-wide grep, including the three bare in-package `ShedPaths{` literals. Card 4's `Edits:` and the overview's All Files Touched now carry them, and the card's instruction is now a grep for three patterns rather than a hand list, with the found sites stated as the floor.
- r1 holistic, "Batch 2 omits four Env.LoomRun construction sites" — confirmed all four plus the two `lifecycle_integration_test.go` field overrides. Added to card 7's `Edits:`; `recipe_test.go`'s engine-name list assigned unambiguously to card 8, and card 9's contradicting "change no existing assertion" sentence narrowed accordingly.
- r1 holistic, "revisit batch 4's 'deliberately does not depend on batch 2' claim" (same finding) — batch 4's `depends-on` is now `[1, 2, 3]` in both the batch file and the overview's Batch Index, since both batches edit `lifecyclecli/run_test.go` and `lifecycle_integration_test.go`.
- r1 holistic, "Card 31's parity tests cannot be tier 1, and batch 5 runs no tagged tier" — verified `lyxcwd.Resolve` spawns git through `gitexec` and that the cited precedent's own header says it stays tier 1 precisely by not calling `RunCLIIn`. Card 31 now tags `parity_test.go` `integration`, creates `internal/shedcli/testmain_integration_test.go` with `gitkit.HermeticGitEnv()` per the Hermetic Git Test Environment Invariant, builds its hub through `hubforge`'s fabric fixture, and batch 5's `verify:` chains `go test -tags integration ./internal/shedcli/...` in both the batch file and the Batch Index.
- r1 holistic, "The NewShed hoist double-prefixes every recipe error" (NIT, demoted from BLOCKING) — card 1 now has `NewShed` return parse and build errors unwrapped, leaving the two delegating `New` bodies as the only prefix layer, so today's `loomrecipe: <err>` envelope text is preserved exactly. Card 5 gains an assertion that no `shedbuild: ` prefix is present, so a later addition fails there rather than silently rewording both packages' envelopes.
- r1 holistic, "lifecycle's found-status envelope is seven keys, not six" (NIT) — card 26 corrected to seven, with the seven names enumerated and an explicit instruction not to pin a six-key closure assertion.
- r1 holistic, "loomcli.ensureStatusLockDir is left with no caller" (NIT) — confirmed `status.go` and `pause.go` are its only two callers. Card 23 now deletes it with the deleted verbs, adds `sharedbootstrap.go` to its `Edits:`, and points at batch 3 card 13 as the new home for its crucible history. Card 13 was extended in the same pass: `shedverbs`' own `ensureStatusLockDir` now composes its error from the told `DecodeErrPrefix`, which reproduces loom's existing `loom: create the status lock's directory %s: %w` text byte-for-byte instead of leaving the wording to the implementer.
- r1 holistic, "Card 34 is a no-file card that instructs a file to be written" (NIT) — the broken-config lightweight-wiring case moved into card 31's own `Requirements:`, in the integration tier alongside the rest; card 34 is now purely confirmatory and states why that case specifically must be confirmed rather than assumed.

## Pushed Back

None.
