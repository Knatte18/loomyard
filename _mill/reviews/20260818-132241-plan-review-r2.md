MILL_REVIEW_BEGIN
# Review: scoutengine told-geometry (optional uniformity pass) — holistic

```yaml
duration_s: 400.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (model id claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-18
```

## Findings

### [NIT:design] Card 11's no-build-tag rationale is factually wrong
**Location:** batch 2 / card 11 **Issue:** The card justifies leaving `testmain_test.go` untagged by claiming `cmd/lyx/hermeticenv_test.go` "would not see a tagged TestMain as satisfying the requirement" — but that guard's own doc comment (line ~90) and implementation read every `*_test.go` file's raw content via `os.ReadFile`/`strings.Contains` with no build-tag filtering at all, so a `//go:build integration`-tagged `testmain_test.go` would be seen identically. The instructed action (leave the file untagged) is still correct — it matches `internal/perchcli/testmain_test.go`'s precedent exactly — only the stated mechanical justification is false. **Fix:** Reword the rationale to cite the perchcli precedent / TestMain-must-cover-the-whole-package-binary reasoning instead of the guard's (non-existent) tag-sensitivity.

## Verdict

APPROVE
Plan is precise, fully cross-checked against source, complete in scope, and internally consistent; only one cosmetic rationale inaccuracy found.
MILL_REVIEW_END
