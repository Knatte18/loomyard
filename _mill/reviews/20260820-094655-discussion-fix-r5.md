# Discussion fix — round 5

Source review: `_mill/reviews/20260820-094523-discussion-review-r5.md` (sonnetxhigh, APPROVE, 0 BLOCKING / 1 NIT, none demoted).

Convergence gate satisfied on this round: `round >= min_rounds` and no finding carries `demoted: true`.

## Fixed

- r5 "\"Exact used surface\" table's call-site counts don't match source" — the finding is correct on every number. Root cause identified: the original measurement grepped the bare form `package.Symbol` rather than the call form `package.Symbol(`, so it counted comment mentions alongside real calls. `lyxcwd.CwdFrom` was the worst case — 8 claimed, 4 real, the other 4 being the repeated `// lyxcwd.CwdFrom(ctx) resolves the seam cwd` comment at `cli.go:132,267,377,555`.

  Re-measured every row with an explicit methodology (call form `Symbol(`, on lines not starting with `//`, production and test tiers counted separately) and rewrote the table with both columns. Corrected values: `SetExit` 21 (not 22), `Execute`/`ExecuteIn` 1 each (not 2), `output.Err`/`Ok` 14/7 (not 17/9), `logger.Warn`/`Info` 6/1 (not 7/1), `lyxcwd.CwdFrom`/`Resolve` 4/1 (not 8/2), `proc.KillPID`/`DetachBreakaway` 1 each (not 6/2), `configengine.ConfigFile` 1 prod + 2 test (not 3).

  Added the methodology statement the finding asked for, plus an explicit note that the numbers describe the port's surface and are not a checklist to assert against — `go build`/`go test` is the real gate.

  Propagated the corrections to the two other sites quoting the same figures: `exact-replacement-shapes`' "22 call sites" for `SetExit` and its "8 call sites" for `logger`.

  **One substantive consequence beyond the numbers.** The re-measurement showed `lyxcwd.WithCwd` has **zero** call sites in scout — the single occurrence at `cli.go:930` is a comment. `WithCwd` is called by `clihelp.ExecuteIn`, so it arrives with the `clihelp` replacement rather than as a separate lyxcwd port. Corrected `exact-replacement-shapes`, which described it as a directly-used symbol to port, and TDD candidate 4, which implied a standalone seam package to test.

  Also verified two non-call forms the old grep had mishandled: `clihelp.GroupRunE` is used as a value (`RunE: clihelp.GroupRunE`, `cli.go:55`), and `lyxdirs.DotLyxDirName` is a constant with 2 production uses (`daemonstate.go:40,48`) — both now labelled as such in the table.

## Pushed Back

None.
