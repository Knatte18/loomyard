# Plan fix report — round 2

Source review: `_mill/reviews/20260826-115258-plan-review-r2.md`

## Fixed

- `20260826-115258-plan-review-r2.md` — "[BLOCKING:design] Card 4 test 5's hubforge.NewHub fallback reads a record that does not exist": accurate. `hubforge.Hub` carries no `Mutations` field or accessor, and `NewHub` discards `CloneAndWire`'s `res.Mutations` entirely, so the documented fallback was unimplementable. Took the reviewer's second option — card 3 (which already edits `internal/hubforge/hub.go`) now adds a `Mutations fabricengine.Mutations` field to `Hub`, populated verbatim from `res.Mutated()` in `NewHub`'s returned literal, and card 4's test 5 reads that field. Card 4 keeps `Edits: none`. Also recorded in card 4's Requirements why the direct-`CloneAndWire`-against-bare-repos route was rejected: `hubforge` does not export its bare-template builder, `gitkit.CopyRepo` is documented as callable from one package alone and this is not it, and re-cloning from an already-cloned hub's `WarpBare`/`WeftBare` takes the adopt path where `configsync.ReconcileAll` reports `Applied` false and no config commit is produced at all.
- `20260826-115258-plan-review-r2.md` — "[BLOCKING:scope] Card 1 references `fabricengine.OriginRecordRel` outside its Context list": accurate. `OriginRecordRel` is declared in `internal/fabricengine/origin.go`, which was absent from card 1's `Context:` allowlist. Added it, between `internal/fabricengine/commitweftpaths.go` and `CONSTRAINTS.md`.

## Pushed Back

_None._
