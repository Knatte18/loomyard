# Plan fixer report — round 4 (APPROVE with NITs)

Source review: `_mill/reviews/20260802-142037-plan-review-r4.md` (holistic, verdict APPROVE).

## Fixed

- `[NIT] RecoverDeps.Plan becomes fully dead code but is left in place` — resolved via the reviewer's own second offered remedy (document the field as intentionally kept), not the prune sub-option. Rationale for declining the prune: `RecoverDeps.Plan`'s only out-of-batch populate site is `internal/websterengine/recoverbatch_test.go`, which is `//go:build integration` (Tier-2 — real scratch git repo, shuttle doubles). Editing it triggers the `verify-excludes-edited-tagged-test` validator (confirmed live: the prune attempt produced exactly that error), which would force `-tags integration` into batch 2's `verify:` and pull the whole Tier-2 integration suite into every implementer/fixer verify round — contradicting mill-plan's scoped-verify discipline (HARM category c: destabilizes/burdens the batch gate). Verified `BeginDeps.Plan` stays live (`beginbatch.go:153` reads `deps.Plan.Dir`), so the two structs' shape-symmetry keep is genuine. Fix applied: Card 3's recoverbatch sweep bullet now instructs KEEPING `RecoverDeps.Plan` and rewording its stale doc comment to state it is retained for `RecoverDeps`/`BeginDeps` shape symmetry and is currently unread in `recoverbatch.go` (rather than naming `RenderForkPrompt`/"plan-level context"). The underlying concern — a dead field left silently undocumented — is fully addressed.
- `[NIT] doc.go bullet's parenthetical misdescribes the RenderForkPrompt signature change` — verified: the parenthetical said "`plan` is now an unused parameter" while the render.go bullet drops the parameter entirely. Fix: reworded to "(`plan` is no longer a parameter of `RenderForkPrompt` at all, per the `render.go` bullet above)".

## Pushed Back

- (none — the prune sub-option of NIT 1 was declined in favor of the reviewer's own alternative remedy, which fully resolves the finding; this is not a rejection of the finding itself.)
