MILL_REVIEW_BEGIN
# Review: Spawned agent panes resolve the spawning lyx binary — holistic

```yaml
duration_s: 165.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-21
```

## Findings

None. Verified against source across all three batches:

- Batch 1 (`internal/shell/shell.go`, `posix.go`, `pwsh.go`, `shell_test.go`): `ExportEnv`/`PrependPathEntry`/`Chain` implemented exactly to spec on both dialects, `chainStatements` shared helper declared once and delegated to by both `Chain` methods, `CONSTRAINTS.md`'s `## Shell Mechanics Seam` parenthetical reworded to "illustrative... not exhaustive" per the card. `WithEnv`, `Touch`, `Quote`, `Invoke`, `ReadFile`, `ForGOOS`, `Pwsh`, `Posix` untouched. Table tests cover ordinary/space/quote cases and assert the live-`PATH`/unset-guard substrings by name.
- Batch 2 (`internal/reedengine/panebin.go`, `spawn.go`, `doc.go`, `panebin_test.go`, `spawn_test.go`, `panebin_enforcement_test.go`, `emptycmd_integration_test.go`): `panebin.go` declares exactly the four named things; `composePaneLaunchLine` degrades to `launchCmd` unchanged on an `executablePath()` error with a named `logger.Warn`, mirroring `spawnwatchdog.go`'s own handling as the Shared Decision requires. `launchStrandLocked` in `spawn.go` composes the line via `shell.ForGOOS()` at the one `send-keys -l` call site, leaves `split-window`'s argv unchanged (verified: no trailing shell-command arg, ends in `-F "#{pane_id}"`), and the `new-session`/Selvage splits in `lifecycle.go`/`selvagepane.go` still carry `e.cfg.Shell` as before. The AST enforcement test's allowlist (`spawn.go`, `selvagepane.go`, `probe.go`) matches the only three non-test files in `internal/reedengine` containing a `"split-window"` literal — confirmed by grep. `doc.go`'s new paragraph is inserted at the exact seam the card specifies. `emptycmd_integration_test.go` is repointed to the prelude-only payload and its own decision citation, per card 7. `CONSTRAINTS.md`'s new `## Pane Binary Resolution` section sits exactly between `## Live-Substrate Spawn Observability` and `## Sandbox Suite Coverage`, with semantic line breaks, and its content matches the card's five sub-clauses verbatim.
- Batch 3 (`crucible/README.md`, `docs/sandbox-howto.md`, `tools/sandbox/SANDBOX-REED-SUITE.md`, `SANDBOX-SHUTTLE-SUITE.md`): the crucible bullet, the sandbox-howto sentence pair, and both suites' item-1 nested checks are present, worded per the cards, with the `claude`-resolution check correctly scoped to the shuttle suite only and a pointer from the reed suite. No `**Covers:**` tags touched. `SANDBOX-REED-WATCH-SUITE.md` left alone as instructed.

Cross-batch: batch 2's `Shell.ExportEnv`/`PrependPathEntry`/`Chain` are consumed exactly as batch 1 exposes them; no signature drift. No out-of-plan files found — every file touched matches the overview's "All Files Touched" union. No duplicated helper: the executable-path-error-degrades pattern in `panebin.go` intentionally mirrors `spawnwatchdog.go`'s existing two-line shape rather than being extracted, which the Shared Decision itself calls out by name rather than leaving accidental.

## Verdict

APPROVE
Implementation matches the plan precisely across all three batches with no constraint violations found.
MILL_REVIEW_END
