# Plan fix — round 1

Source review: `_mill/reviews/20260810-130855-plan-review-r1.md` (holistic, 1 BLOCKING, 2 NIT).

## Fixed

- `20260810-130855-plan-review-r1.md` — "[BLOCKING:design] `createGitWorktree`'s literal return signature mis-declares its first value".
  Accurate: `(createdToken, exitCode int, ...)` parses as one name-group of type `int`, shadowing the type inside the function body so the token literal cannot compile.
  Batch 2 card 9 now spells the first return `tok createdToken` and states the parse trap so a later edit does not undo it.
- `20260810-130855-plan-review-r1.md` — "[NIT:consistency] `branchRequest` gains an undeclared `branchPrefix` field".
  Accurate on both halves: the pinned shape did not carry the field, and the discussion's "each check's inputs travel with the check" rule places it on the ownership kind rather than on the request.
  Took the reviewer's second option: the constructor is now `ownedManagedBranch(l *lyxcwd.Location, branchPrefix string)`, `branchRequest` gains no field, and the overview's Shared Decisions now pins both request shapes literally, including the explicit statement that `branchRequest` carries no `branchPrefix`.
  Batch 2 cards 5 and 6 and batch 5 cards 23, 24, 25 and 26 all updated to the constructor form, plus a batch-5 scope bullet stating the rule once.
- `20260810-130855-plan-review-r1.md` — "[NIT:scope] Card 27 omits `_mill/discussion.md` and `doc.go` from Context despite relying on both".
  Accurate: the card asserts a fact about the package doc's contents and tracks the discussion's guard section closely.
  Both paths added to card 27's `Context:`.

## Pushed Back

_None._
Every finding verified accurate against the plan and the tree, and none of the three fixes breaks existing functionality, conflicts with a documented decision, or destabilises other work.
