# Plan fix report — round 2

Source review: `_mill/reviews/20260729-161410-plan-review-r2.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 2 NIT). Every finding was verified accurate against source and fixed per `mill-receiving-review`'s fix-everything default; all edits are confined to `_mill/plan/` files (the Plan Review guardrail). Zero pushbacks.

## Fixed

- [BLOCKING] "Card 11 escalation lacks a deadline check between re-dial failure and kill+respawn" — verified against `supervised_test.go`: `TestEnsureSupervised_UncontendedLockWithUndialableHealthyStateReturnsErrServerSpawnTimeout` (300ms timeout, uncontended lock, healthy-but-undialable state) is exactly the escalation's trigger, and the un-bounded ~500ms re-dial would overshoot the deadline and fall into a real `KillPID`+respawn (failing at `cmd.Start()` on the test's placeholder command). Card 11 now **requires** bounding `reconnectUnderLock`'s retry by the overall `deadline` **and** an explicit `if time.Now().After(deadline) { return &ErrServerSpawnTimeout{...} }` guard immediately after `reconnectUnderLock` returns `healthy == false`, before any `KillPID`/respawn. Moved `supervised_test.go` from card 11's `Context:` to `Edits:` and required updating that test's now-inaccurate `step1->step3`-spin doc comment to describe the deadline-bounded escalation it now exercises (the guard fires before any kill, so the held fixture PID is never touched and the assertion stays green).
- [BLOCKING] "Card 4 Requirements reference `formatLocation` from a file absent from Context/Edits" — confirmed `formatLocation`, `lspRange`, `lspPosition`, `lspLocation`, and `SelectionRange` all live in `position.go`, which cards 4 and 6 name in Requirements but omit from `Context:`. Added `internal/codeintelengine/position.go` to both cards' `Context:`.
- [NIT] "`refs_test.go`'s stale call-chain comment after the supervised flip" — confirmed `TestReferences_HasNativeDaemonRoutesThroughEnsureServer`'s doc comment and its assertion failure-message both name the pre-flip `... -> ensureServer -> ensureNative -> resolveGoToolchain -> the fake installer` chain, which the flip makes stale (the assertion still passes since `ensureServer` calls `resolveGoToolchain` directly). Added `internal/codeintelengine/refs_test.go` to card 12's `Edits:` with an explicit requirement to correct both prose spots to the post-flip chain without changing the assertion.
- [NIT] "Batch 2's deferred `--in-file` integration coverage is not actually created anywhere in the plan" — the discussion's Testing section lists this end-to-end `--in-file` integration test (including the same-name-in-two-types ambiguity case) as a deliverable, so per `mill-receiving-review` it is created, not reworded to out-of-scope. Added **card 14** (`TestReferences_InFile_Integration` in `refs_integration_test.go`, real gopls, both the single-match and the ambiguity case) and rewrote batch 2's Batch Tests to point at it. Because card 14's edit of an integration-tagged test would otherwise force batch 4's fast untagged `verify:` to carry `-tags integration` (the `verify-excludes-edited-tagged-test` validator invariant, which surfaced on re-validation) — pulling gopls into the per-round gate against the repo's out-of-band convention — cards 13 and 14 (the only integration-tagged edits) were moved into a new **batch 5** (`engine-integration-tests`, depends-on `[4]`) whose `verify: go test -tags integration -run=^$ ./internal/codeintelengine/...` compiles the tagged tests without executing them (no gopls). Batch 4 shrank to cards 10–12 and keeps its fast untagged gate. The overview Batch Index, All Files Touched, and cross-references were updated accordingly; the full 5-batch plan re-validates clean (DAG + `_plan_validate`).

## Pushed Back

- None.
