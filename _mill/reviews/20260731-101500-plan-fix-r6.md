# Plan fix — round 6

Source review: `_mill/reviews/20260731-100933-plan-review-r6.md`

## Fixed

- `20260731-100933-plan-review-r6.md` — "[BLOCKING] Card 12 mentions Topology.Checkout, whose file is not in Context". Verified. Added `internal/fabricengine/checkout.go` back to Card 12's `Context:`. This reverses the removal made in round 1's fix, deliberately: round 1's substance was *which mechanism the test drives* (a weft-side branch switch, not the coordinated checkout), and that stands unchanged — but the card still names `Topology.Checkout` to tell the implementer what not to reach for, and listing the file lets them verify the `*hubgeometry.Layout` claim instead of taking it on faith. Chose this over deleting the identifier because a vague negative ("do not use the coordinated checkout API") invites exactly the exploration the allowlist exists to prevent.
- `20260731-100933-plan-review-r6.md` — "[NIT] ErrInvalidSHA doc-fix in card 2 leaves a pre-existing undercount". Verified against source: `ErrInvalidSHA` is returned by `ChangedFilesSince` (`gitrepo.go`), `CheckoutDetached` (`gitrepo.go`, from the same `validSHA` gate), `ResetHard` (`reset.go`), and the doomed `SetSnapshotSHA`, while the comment names only two of them. Card 2 now requires enumerating all three survivors rather than just dropping the dead name — a doc that is accurate about the deletion and wrong about everything else is not an improvement.
- `20260731-100933-plan-review-r6.md` — "[NIT] 'three other call sites' miscounts forcePackIndexFreeze's survivors". Verified: `parity_test.go` has exactly three call sites (657, 687, 764), and 764 sits inside `TestSetSnapshotSHA_MixedBackend_RepackBetweenCommitAndCanonicalization`, which this same card deletes — so two survive, not three. Corrected. Worth recording that this miscount was inherited verbatim from `discussion.md` and survived all seven discussion review rounds.
- `20260731-100933-plan-review-r6.md` — "[NIT] GIT_COMMITTER_DATE instruction references a nonexistent env-override convention". Verified and the finding is exactly right: `lyxtest.MustRun(tb, dir, args...)` takes no environment, `gitexec.RunGit` takes none, and a grep for `cmd.Env` across the package's tests returns nothing — there is no convention to follow, so "follow that convention" was an instruction to imitate something that does not exist. Card 13 now says so explicitly and specifies the mechanic: a raw `exec.Command` for that one commit with `cmd.Env` scoped to the single call, mirroring the raw-command helpers `syncweft_integration_test.go` already uses. Added the `t.Parallel()` leak reasoning the reviewer raised, and one point it did not: the raw command must append to `os.Environ()` rather than replace it, or it drops the hermetic git settings `TestMain` installs.
- `20260731-100933-plan-review-r6.md` — "[NIT] Card 18's TDD instruction conflicts with its position after cards 15/16". Verified: cards commit in listed order, so "write both before cards 15 and 16 are complete" is not executable as written once card 18 is reached. Replaced the slogan with the actual mechanic — draft and run the two tests against the unmodified files while working card 15, implement and commit 15/16, then commit the test file as card 18 — plus a stash-or-revert recovery for the case where that ordering has already been lost, since an unobserved failing run is not evidence and these two cases exist only for the before/after difference.

## Pushed Back

_None._
