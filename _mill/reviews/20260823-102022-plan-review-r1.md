MILL_REVIEW_BEGIN
# Review: loom: self-checkable mechanical gates — holistic

```yaml
duration_s: 457.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (per environment metadata; best-effort self-assessment)
reviewed_file: plan/
date: 2026-08-23
```

## Findings

### [BLOCKING:scope] Card 6's Context omits planparser/validate.go
**Location:** batch 3 / card 6. **Issue:** Requirements names `commit-subject-mismatch` (the exact `Check` string from `checkCommitSubjectMismatch` in `internal/planparser/validate.go`) to explain why the plan-fixture's `Commit:` line must match a specific prefix form, but `validate.go` is not in card 6's `Context:` (only `cli_test.go`, `validate.go` (loomcli), `wiring.go`, `discussionparser/validate.go`, and the two testdata files are). **Fix:** Add `internal/planparser/validate.go` to card 6's `Context:` so the implementer can confirm the required `"%d: "` prefix rather than relying solely on copying the testdata fixture's shape.

### [NIT:consistency] `zero-argument-verbs` decision has no locatable source in the plan
**Location:** batch 3 / card 5. **Issue:** Card 5 cites "per the `zero-argument-verbs` decision" as if it were a plan-level Shared Decision, but no such decision exists in `00-overview.md`'s `## Shared Decisions` — it only exists in `_mill/discussion.md`. Batch 4/card 8's analogous citation of `parity-tests-per-gate` correctly labels it "the discussion's," but card 5 doesn't, so a reader working only from the plan files can't locate the decision. **Fix:** Reword to "per the discussion's `zero-argument-verbs` decision," matching card 8's phrasing.

### [NIT:consistency] go-conventions-and-verify-scope's stated rule doesn't cover its own CONSTRAINTS.md trigger
**Location:** `00-overview.md` / Shared Decisions. **Issue:** The Decision text says the extra `./internal/lyxcwd/...` verify scope triggers when "a batch['s cards] edit a `.md` file under `manifest/` or `docs/`" — but batches 1 and 4 add that scope because they edit `CONSTRAINTS.md`, a repo-root file outside both roots; each batch's own local rationale is correct (a `manifest/`/`docs/` link could point at a `CONSTRAINTS.md` anchor), but the Shared Decision itself never states this trigger. **Fix:** Extend the Decision's wording to also name "or `CONSTRAINTS.md`" as a trigger for the `internal/lyxcwd/...` scope addition.

## Verdict

REQUEST_CHANGES
One BLOCKING context-completeness gap in card 6; two NIT wording/consistency gaps.
MILL_REVIEW_END
