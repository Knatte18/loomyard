# Discussion fixer report — round 5

Source review: `_mill/reviews/20260811-145554-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).
Target: `_mill/discussion.md`.

Round 5 is the configured round cap (`roles.discussion-review.holistic.rounds: 5`).
One finding carried `demoted: true`, so the convergence gate's demoted-predicate was not satisfied; per the gate's round-cap branch this round is treated as an implicit approval and the phase closes here.

## Fixed

- `20260811-145554-discussion-review-r5.md` — "[NIT:decision] `loom.md:70–72` listed as an edit site, no disposition".
  Verified: `:70` states the two-part contract with Input as "a *pointer* to the format-contract file … never a restated copy", which the thin-Input carve-out contradicts outright since `Discussion-Write` has no Input; `:71` restates the pointer rule in full.
  Gave both an explicit disposition — qualify `:70` to admit the thin-Input/thin-Output cases pointing at `shed.md`, and reduce `:71` to a pointer.
  Added a clarification that the forcing rule is the authority split, **not** the new `CONSTRAINTS.md` invariant, whose subject is instruction files rather than design docs — so a plan writer does not over-apply the invariant.
  Also added `:70–72` and `:29` to Scope's `loom.md` list, which had omitted both.

- `20260811-145554-discussion-review-r5.md` — "[NIT:decision] `shed.md:8`'s own disposition left implicit".
  Verified: `:8` was described only as the claim the carve-out scopes, never as edited, while its `loom.md:44` mirror was explicitly told to gain a pointer.
  Stated the disposition: qualified in place, symmetrically with `:44`, because `:8` sits in the `## What it is` summary 14 lines above the contract section and a reader meets the unqualified claim first.

- `20260811-145554-discussion-review-r5.md` — "[NIT:consistency] `finalize.md` line citation disagrees between sections".
  Verified: the tree has "never released and re-acquired partway through" on `finalize.md:37`; `:38` is the `raddle.md` pointer.
  The Q&A entry still said `:38` after round 4 corrected the decision body to `:37`. Both now read `:37`.

## Pushed Back

None.
