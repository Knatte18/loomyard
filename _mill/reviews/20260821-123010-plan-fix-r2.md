# Plan fix — round 2

Source review: `20260821-122746-plan-review-r2.md`

## Fixed

- `20260821-122746-plan-review-r2.md` — "[NIT:consistency] Duplicated implementation-detail claim across two doc comments".
  Verified: batch 1 card 1 and batch 2 card 6 both enumerated which of the three registry constructors creates a run directory and which probes a stencil, giving a future change to those constructors two independent doc sites to update.
  Reworded card 6 so `Build`'s own doc comment states only the pass-through property and points at the package doc for the enumeration, leaving `internal/shedbuild/doc.go` as the single site that lists which constructor produces which effect.
  Added `internal/shedbuild/doc.go` to card 6's `Context:` to match the new cross-reference.
  Batch 2 card 10's mention of the same effects was left as it is: it is a test-file comment explaining why that one test cannot be filesystem-free, which is a different claim from the package's own contract.

- `20260821-122746-plan-review-r2.md` — "[NIT:scope] Roadmap Done-entry insertion point unspecified".
  Verified: card 18 said to move the item into the Done group but named no insertion point, while the group's two already-shipped siblings sit at the top of Done.
  Added an explicit placement instruction — adjacent to those two entries — and noted that the file's own Maintenance section makes the numbering itself cosmetic, so adjacency is the only property that matters.

## Pushed Back

_(none — both findings were factually accurate and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this plan.)_
