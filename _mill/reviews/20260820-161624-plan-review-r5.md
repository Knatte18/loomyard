MILL_REVIEW_BEGIN
# Review: shedadapters: Burler-round producer — holistic

```yaml
duration_s: 477.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [BLOCKING:scope] Card 14's roadmap cross-reference cleanup miscounts and misattributes its own targets
**Location:** batch 4 / card 14, "Third" instruction, editing `manifest/roadmap.md`
**Issue:** Card 14 claims "four cross-references" go stale after the lifecycle move, then itself enumerates 2 (in the Bouncer item) + 3 (the "Depends on the two…" lines) = 5, and the "later" phrase it quotes for the Bouncer item — `"an instance of `shedadapters: Burler-round producer` above"` — does not exist there at all: it is verified in the source at `manifest/roadmap.md:38`, inside the **Discussion-Review producer** item, not the Bouncer item (whose own line 26 instead reads "see `shedadapters: Burler-round producer`'s own cluster-fan-trimming note above"). A further stale reference the card never names is `manifest/roadmap.md:95` (Someday, `Tenter + Hardener`): "a round producer … equivalent of the Planned `shedadapters: Burler-round producer` item" — also false once the item moves to Done. Card 14's closing clause ("Change no other Planned or Someday item beyond these four cross-references…") then actively forbids fixing line 38's real location and line 95, since neither is among the four named.
**Fix:** Recount and correctly locate all stale post-move references before writing card 14's Requirements: fix `manifest/roadmap.md:22` and `:26` (Bouncer item), `:38` (Discussion-Review producer item, the actual site of the "an instance of … above" phrase), `:39`/`:48`/`:53` (the three "Depends on the two…" lines), and `:95` (Someday `Tenter + Hardener`) — seven sites total, not four — and update the card's own enumeration and its "change no other item" scope clause accordingly.

## Verdict

REQUEST_CHANGES
Card 14's roadmap cross-reference repair is self-contradictory and misses real stale references it then forbids fixing.
MILL_REVIEW_END
