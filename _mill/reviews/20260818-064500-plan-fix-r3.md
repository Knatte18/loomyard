# Plan fix — round 3

Source review: `_mill/reviews/20260818-064117-plan-review-r3.md`

## Fixed

- `20260818-064117-plan-review-r3.md` — "[NIT:scope] `preflight.Wired`'s true-case branch is never asserted": verified — card 13's scenario list drove `Wired` only through the `<hub>/_board` divergence row, where it is expected false, so the predicate's true branch had no direct coverage and no consumer in this task to exercise it. Card 13 now requires a positive-path row asserting both `Wired(cwd)` and `HubPresent(cwd)` return a non-nil `*lyxcwd.Location` and true against the fixture's ordinary healthy pair, stating why the row exists.

## Pushed Back

_None._
