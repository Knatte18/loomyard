# Discussion fix — round 3

Source review: `_mill/reviews/20260828-055952-discussion-review-r3.md` (verdict APPROVE, 3 NITs — 1 demoted from BLOCKING by the blocking-class ceiling).
Every finding was verified against source before being applied; none were pushed back.

## Fixed

- `20260828-055952-discussion-review-r3.md` — "[NIT:consistency] Weft-conclude \"accepted consequence\" is false": the finding is correct and it corrects round 2's own fix. Confirmed `internal/fabricengine/merge.go:449-460` writes `WeftOutcome: mergeOutcomeAlreadyUpToDate` into the merge-state record's initial literal, and `mergelifecycle.go`'s weft arm guards on `st.WeftOutcome != mergeOutcomeAlreadyUpToDate`, so the arm is skipped on every record this binary produces — its header comment says so verbatim ("not because a weft conclude can still be produced"). Replaced the false accepted-consequence bullet in `message-set-unconditionally` with a warp-only reach statement citing both sites, and carried forward the reviewer's instruction that this must not migrate into `final-summary-spec.md`.
- `20260828-055952-discussion-review-r3.md` — "[NIT:scope] Finalize's unconditional parse breaks existing fixtures": confirmed nine `&Finalize{` struct literals in `internal/landingshed/finalize_test.go`, and `landingshed.Deps` fixtures at `internal/shedrecipe/entries_simple_test.go:43`, `internal/shedbuild/fixture_test.go:113`, `internal/loomrecipe/fixture_test.go:160`. Added a Testing subsection naming all four sites and stating which need an artifact on disk versus only a non-empty path string.
- `20260828-055952-discussion-review-r3.md` — "[NIT:scope] docs/overview.md edit left unspecified": confirmed the kept-specs enumeration at `docs/overview.md:98` and the module list around `docs/overview.md:303-306`. Replaced the bare "docs/overview.md updated" Scope bullet with both named sites and what each gains.

## Pushed Back

_None._
