MILL_REVIEW_BEGIN
# Review: loom: session bootstrap — holistic

```yaml
duration_s: 599.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 4.5 (self-assessed; harness reports "claude-sonnet-5" but I cannot independently confirm the version number)
reviewed_file: plan/
date: 2026-08-19
```

## Findings

None.

## Verification performed

Cross-checked all seven batches and the overview against source: `internal/fabricengine/{add.go,fabric.go,weftwiring.go,weftgit.go,commit.go,mutation.go,launchers.go,launcher_content.go,refscanner.go,open.go}`, `internal/lyxcwd/lyxcwd.go`, `internal/loomengine/config.go`, `internal/loomshed/{seed.go,loomshed.go,preflight.go}`, `internal/state/state.go`, `internal/shedengine/{shed.go,run.go,validate.go}`, `internal/websterengine/runlevel.go`, `internal/webstercli/{cli.go,wiring.go}`, `internal/reedcli/{cli.go,attach.go}`, `internal/reedengine/{headerpane.go,render/types.go}`, `internal/shell/shell.go`, `internal/lock/lock.go`, `internal/proc/proc_linux.go`, `cmd/lyx/{main.go,seamsignature_test.go,helptree_test.go,longlist_test.go,sandbox_coverage_test.go}`, `manifest/designs/loom.md`, `contracts/specs/loom-status-spec.md`, `docs/overview.md`.

- Batch Index DAG: 7 batches declared, files present, no cycles, no forward deps (5→[1,2,3,4], 6→[2,5], 7→[6]); the batch-2/batch-5 co-edit of `manifest/designs/loom.md` is correctly serialized via the dependency edge.
- All Files Touched (overview) matches the union of every card's Creates/Edits/move-targets exactly — no omissions, no extras.
- Global step numbering: cards 1–33, sequential, no gaps; each batch's `cards:` count matches its card list.
- Every `Moves:` field is `none` across all 33 cards — no Rename mechanic section is required, correctly absent.
- Function/type signatures cited in Requirements verified against source and found accurate: `lyxcwd.Location`/`AnchorPath`/`AnchorRel`, `fabricengine.{WeftWorktree,WeftWorktreePath,ensureWeftLockDirAt,ScopedPathspec,commitWeftAt,Mutations.Append,KindFileWritten,KindCommitCreated}`, `state.{ReadJSON,WriteJSON,UpdateJSON,ReadJSONStrict}`, `shedengine.Shed{StatusPath,LockPath,StatusLockPath}` and its `validate()` equal-lock-paths check, `loomshed.Deps`/`New`/`NewPreflightProducer`, `websterengine.RunDeps`/`MasterStarter`, the `runnerMasterStarter` 12-line adapter precedent in `webstercli/cli.go`, `shell.Shell`/`headerLaunchCmd` precedent for `statusStrandCmd`, `reedcli.attachArgv`, `proc.IsAlive`/`Detach`, `lock.AcquireWriteLock`/`TryAcquireWriteLock`.
- Card 1's placement claim (new origin step lands after `WireJunctionsWith`, before the warp push) matches `add.go` exactly; `rollbackAdd`'s signature and the "adopted branch commit retained, created-branch path removes it with the worktree" rollback semantics check out against the current gate/lock code.
- Card 6/7's launcher-set edits match `writeLaunchers`/`removeLaunchers`/`launcherScript` call shapes precisely, including the `writeLauncherScriptIfChanged(rec, root, l.HubPath, ...)` argument order.
- Card 8's failure-injection instruction ("force the failure at the warp push, using the seam the existing rollback tests already use") correctly identifies the broken-origin-remote technique already present in `add_rollback_adopt_test.go`'s `TestAddRollback_UnwiresJunctionsOnPostWiringFailure`, which fails exactly at the step after the new origin-record step lands.
- Card 10/23's design-doc edits target real, currently-stale text: the existing `.lyx/lyxrun.cmd` paragraph and the existing 4-step diagram in `manifest/designs/loom.md` are both present verbatim as described.
- Card 25's status-spec edits target real, currently-deferred/stale text in `contracts/specs/loom-status-spec.md` (the "pinned when that command lands" deferral, and both "before `lyx loom run` has executed" occurrences).
- Card 31's "12-row" vs "13-row" producer-list overview staleness is a genuine, verified discrepancy (`docs/overview.md` says 12, `loomshed.go`'s own header comment already says 13) that the plan correctly catches and fixes.
- CLI/Cobra Invariant mechanics (seam counts, alias-as-second-root-child, `RunCLIIn` eleven/twelve exception) and the sandbox-coverage/help-tree/long-list guards' actual implementations all match how the plan proposes to extend them, including that the `run` alias is itself enumerated by `newRoot().Commands()` and therefore correctly needs its own sandbox-coverage allowlist entry (not a scenario) and its own long-list-prose mention.
- `resolveParentBranch`'s five-row decision table (card 21) is complete and non-overlapping across (found, recorded-non-empty, flag-non-empty).

No constraint violations, no context-completeness gaps (every function named in a card's Requirements is either in that card's Context or in its own Edits/Creates list), no vague Requirements, and no over-engineering found. This is a mature, thoroughly cross-referenced plan with no outstanding issues at this round.

## Verdict

APPROVE
No findings — plan is accurate, complete, and internally consistent against source.
MILL_REVIEW_END
