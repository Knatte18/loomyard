# Discussion fix — round 6

Source review: `_mill/reviews/20260820-152411-discussion-review-r6.md` (APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- `20260820-152411-discussion-review-r6.md` — "[NIT:consistency] Seed fallback trigger stated two ways".
  Pinned one rule: the seed fallback keys on the *file's* state after the spawn returns (absent or unparseable), never on the spawn's outcome — the seed-side twin of harvest.
  A spawn that wrote a valid focus file and then failed keeps that file.
  Reworded the seed-failure test scenario accordingly and added a seed-side harvest scenario asserting the agent's file survives byte-identical.
- `20260820-152411-discussion-review-r6.md` — "[NIT:scope] `manifest/designs/shed.md` edit left unspecified".
  Added a "The `shed.md` amendment" subsection to Technical context naming both falsified claims by line (the one-adapter-per-engine-type rule at l.302 and its restatement, and the per-engine bullet list at l.303-307), what each becomes, and the `perch`-bullet supersession note.
  Scope now points at it rather than saying "a note".
- `20260820-152411-discussion-review-r6.md` — "[NIT:design] `ResolveRound`'s non-ENOENT stat error unspecified".
  Pinned that only `fs.ErrNotExist` means "absent" and every other stat error is returned, with the `errors.Is` classification named explicitly, plus a matching test case.

## Pushed Back

None.
All three findings verified accurate against the cited sources, and no fix conflicted with an existing decision, broke existing functionality, or reached outside this task's scope.
