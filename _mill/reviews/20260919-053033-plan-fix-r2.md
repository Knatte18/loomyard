# Plan fix — round 2

Source review: `_mill/reviews/20260919-052851-plan-review-r2.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 4 BLOCKING, 0 NIT).

## Fixed

- r2 holistic, `[BLOCKING:scope] cli.go struct comments miss four drive/run hits` — verified: `internal/loomcli/cli.go`'s `loomCLI` struct carries hits on `cfg` ("Batch 5's run/bootstrap verb reads"), `registry` and `runner` (both "so drive.go can pass it to landingDeps"), and `landingCfg` ("not only inside drive's detached driver log"), beyond the three fields card 2 named.
  Replaced card 2's three-field enumeration with a whole-struct sweep naming all seven fields as the minimum.
- r2 holistic, `[BLOCKING:scope] step.go header and a RunE comment miss the rename` — verified: the file header says step bootstraps idempotently exactly as `` `run` `` does and that unlike `` `run` `` it spawns no detached driver, and two `RunE`-body comments name `drive` (the producer-hard-error mirror at ~201 and the next-`drive` Tier-1 observation at ~210 — the reviewer named one; the sweep found the second).
  Card 3's `step.go` requirement now sweeps the file with those as landmarks.
- r2 holistic, `[BLOCKING:scope] smoke_test.go prose and test names retain drive/run` — verified: the header doc at 10 and 12, `newWiredPairFixture`'s doc at ~146, and three `TestSmokeDriveStandalone_*` function names, none covered by card 6;
  plus the flagged `smoke_bootstrapwiring_test.go` citation naming `run.go` and `runCmd`.
  Card 6 now sweeps both files whole, with the three test-function renames called out explicitly as identifier changes.
- r2 holistic, `[BLOCKING:scope] ly-drive SKILL.md Self-report section keeps drive` — verified at SKILL.md lines 116 and 121.
  Card 13 now sweeps the whole relocated file, with a note that this pair inverts the skill's own instruction if the wrong verb is named, since the sentences turn on which verb the self-report tiers hang off.

## Systemic fix

Both review rounds produced only `scope` findings, every one the same shape: a card enumerated some of a file's hits and would have left the rest.
The cause is that the cards named sites where the discussion names a *rule* — its Scope section states scope as an enumeration rule over a grep set precisely because, at this hit count, "a whitelist cannot be both complete and maintainable, and a silently-short one is worse than no list because it reads as authoritative".

Added a `## Per-file sweep rule` section to batches 1, 3, and 5, enumerating the four hit classes (verb names, pre-move filename citations, identifiers including test function names, and the skill name) and the disposition-2 leave list (the noun sense of "run"; "driver"/"drives" as ordinary English).
Added the `cards-name-landmarks-not-boundaries` Shared Decision to the overview recording that a card's `Requirements:` names landmarks, never boundaries.
Batch 4's single card carries the same instruction inline.

## Pushed Back

_(none — all four findings verified accurate against the tree, and no fix broke existing functionality, contradicted a recorded decision, or destabilised work outside this task.)_
