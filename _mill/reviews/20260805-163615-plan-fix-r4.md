# Plan fix — round 4

Source review: `_mill/reviews/20260805-163615-plan-review-r4.md` (holistic, REQUEST_CHANGES, 7 BLOCKING + 2 NIT, reviewer `fablehigh` — self-review round: the fixer is the reviewer, so every finding was re-verified against the tree before being applied, under the same standard as a stranger's).

Five of the seven blocking findings are round 3's defect class continued — a card that deletes a symbol or renames a file without listing every live call site, this time in the module's own test files, a guard's filename literal, and a const whose readers sit one batch downstream. The fixes follow the two structural rules the plan already carries: a card that declares or deletes a geometry-token literal moves its own ownership row in the same card (the `_pattern` fix extends the `_lyx` row's transitional-co-owner shape rather than reverting to cross-batch staging), and a card that deletes a symbol lists every live caller in its own `Edits:`. No cards were added and none renumbered; all work folded into the cards the review cited.

## Fixed

- "[BLOCKING] Card 3's Prime deletion leaves three test files unfixed". Re-verified all sites: `weft_test.go` `Prime:` literals at :86,166,181,199,281,318 and `WeftRepoRoot()` at :90 and :189-206, `pattern_test.go:24`, `mouse_boot_integration_test.go:50-55`. Added all three files (plus `launchers.go`, next finding) to card 3's `Edits:` and stated each substitution: `Prime:` dropped from every literal; `weft_test.go`'s `WeftRepoRoot` assertions deleted with the `wantWeftRepoRoot` column — an in-package `hubgeometry` test cannot import `fabricengine`, and the equivalence property is held by the `fabricengine` test callers the card already retargets. Applying structural rule 2 to the same card surfaced more unstated fallout in a file that *was* listed: `hubgeometry_test.go`'s `Prime`/`Repo` assertions at :63-70 (must move to the `-HUB`-trim derivation), the `PrimeName` sub-test at :158-161 (deleted with its subject), and the `MenuLauncherRel` sub-tests at :469-499 (now pass an explicit prime name) — all now spelled out.
- "[BLOCKING] `MenuLauncherRel` signature change misses its only caller". Re-verified: `writeLaunchers` at `launchers.go:63` is the sole production caller and was in no batch-1 card. Added `internal/fabricengine/launchers.go` to card 3's `Edits:` and specified the shape: the call becomes `l.MenuLauncherRel(primeName)` with the name sourced via the new in-package `PrimeName(l)`, error propagated — `writeLaunchers` already returns one, and a launcher pointing at an unresolved prime is worse than a failed wire.
- "[BLOCKING] Batch 2's verify cannot pass — lyxtest still imports hubgeometry". Re-verified the import chain: `testmain_test.go`, `anchor_test.go` and `hubgeometry_test.go` import `internal/lyxtest`, and `lyxtest.go` names `hubgeometry` at :267 (`PairedFixture.Layout`) and :473/:530 (`Resolve`) after batch 1's cards retarget its other uses. Fixed by pulling `lyxtest.go`'s retarget forward from card 10 into card 5 (added to `Edits:`, requirement states it is qualifier-and-type only since the file reads no `Layout` fields), rather than weakening the verify to build-only — the vet gate is the only thing in batch 2 that proves the module's own tests still type-check. Card 10 now carries a deliberate-absence note so the sweep does not re-touch it, and batch 2's `## Batch Tests` explains why `lyxtest` is the one out-of-module package the gate reaches. The same fix made explicit what was previously implicit: the moved test files' own `Location` conversion happens in card 5, because no batch-4 sweep card covers `internal/lyxcwd`'s tests and the vet gate type-checks them.
- "[BLOCKING] raddle guard's filename skip breaks at the rename". Re-verified: `raddle_guard_test.go:48` compares `d.Name() == "hubgeometry.go"` and `_raddle` sits in `hubgeometry.go` at :317 and :518, leaving only in batch 6 (cards 31 and 34). Card 5's requirements now name the file and instruct updating the skip literal (and the :45 comment) to `"lyxcwd.go"`; the file is already in card 5's `Moves:`, so no `Edits:` entry was needed.
- "[BLOCKING] Batch 5 deletes constructors with lyxcwd's own tests unlisted". Re-verified every site in `lyxcwd_unit_test.go` (`PerchRunsDir` :44-52, `PlanDir` :56-64, `BuilderDir` :68-76, `DotLyxDir`/`LyxDir` :123-131, `TestHubLogsDir` :135-147) and `lyxcwd_test.go` (`LyxDir()` at :121-125 and :521-530). Distributed per owning card: cards 20, 21, 23, 26 and 27 each add the file(s) to `Edits:` and delete the sub-test whose subject they delete, with the superseding coverage named each time — card 28's anchoring table plus the relocated per-module test files. `lyxcwd_test.go`'s two `LyxDir` assertions are deleted surgically, leaving the surrounding tests' other assertions intact.
- "[BLOCKING] Card 25 deletes `PatternDirName` a batch before its consumers move". Re-verified the readers: `pull.go:299` (production) plus `fabriccli/cli_test.go:416`, `pull_integration_test.go:258,292`, `unwire_test.go:65,71` and nine sites in `junction_pattern_integration_test.go` — none in batch 5's card set. Chose the keep-transitionally shape over retargeting-in-card-25, because it extends the sanctioned `_lyx` two-owner pattern and keeps card 25 one-subsystem: card 25 now retains the const and registers `_pattern` as dual-owned (`internal/pattern` + transitional `internal/lyxcwd`); card 35 deletes the const, cuts every reader over in the same card (`pull.go` to its own `patternDirName`, the tests to `pattern.DirName`), and — per the declares-its-row-in-the-same-card rule, since it both declares `fabricengine`'s literal and deletes `lyxcwd`'s — swaps the row to `internal/pattern` + `internal/fabricengine` itself, with `internal/lyxcwd/enforcement_test.go` added to its `Edits:`. Card 36 now leaves `_pattern` untouched, and card 29's zero-diff gate asserts the new dual-owner shape. All of card 35's reader files were already in its `Edits:`; only the guard file was missing.
- "[BLOCKING] Lifted HostJunctions tests keep pattern-accessor calls card 35 deletes". Re-verified: `lyxcwd_test.go:615-619,663,705` assert the `_pattern` rows via `HostPatternLink`/`WeftPatternDirFor`/`WeftPatternDir`, and those tests are lifted by card 32 before card 35 deletes the accessors. Fixed at the lift, which is the cleaner of the two options the review offered: card 32 now requires the `_pattern` rows be rewritten against the generic join (`filepath.Join(WorktreePath(l, slug), l.AnchorRel, pattern.DirName)` and the weft-sibling equivalent), importing `internal/pattern` for the name, so `hostjunction_test.go` is never reachable by card 35's deletion — which card 35 now states as a verify-do-not-re-edit obligation. This is also what those rows should have asserted all along, by card 35's own argument.
- "[NIT] `.fabric-anchor` survives in help text and a comment no card touches". Re-verified `fabriccli/fabric.go:266` (cobra `Long`) and `fabricengine/unwire.go:5` (godoc). Both files added to card 7's `Edits:` with the correction named, the help text flagged as a CLI-invariant accuracy obligation.
- "[NIT] Red-tree Shared Decision cites the wrong batch-2/4 verify commands". Re-verified against the Batch Index. The decision now quotes the actual `-tags integration` batch-2 gate and describes batch 4's three-command gate; no verify command changed, only the prose describing it.

## Found while fixing, not in the review

- **The guard-staging Shared Decision was still describing the pre-round-3 staging.** It said batch 4 "registers `-weft` and `_lyx`" and batch 6 registers `_pattern` — both restaged by round 3 into cards 1, 2 and 25. Rewritten to the actual per-card staging including this round's `_pattern` transitional co-owner, and `module-owned-constructors` added to its `Applies to:` line. Card 19's forward-reference sentence in batch 4 was corrected to match.
- **Batch 2's `## Batch Tests` claimed the module has five test files.** It has sixteen; the sentence's real point (two are integration-tagged, so the `-tags` flag is load-bearing) survives with the false count removed.

## Pushed Back

_None. All nine findings were re-verified against the source and held; the two structural rules from the coordinator were compatible with every fix, and in the `_pattern` case rule 1 dictated a stronger fix than the review's first-listed option (card 35 moves the ownership row itself rather than leaving it to card 36)._

## Validation

`_plan_dag.extract_batch_index` + `validate` → DAG OK (8 batches). `_plan_validate.run` (max_cards_per_batch=20, max_batch_context_tokens=700000, parent_branch=main) → 0 findings. The overview's All Files Touched was regenerated programmatically from the batch files (`_parse_edits_only` ∪ `compute_creates_union` ∪ move targets): 283 paths, byte-identical to the round-3 list — every file this round added to an `Edits:` list was already in the union through another card, which is itself a small confirmation that the misses were sequencing gaps, not scope gaps.
