# Discussion-fix report — round 2

Source review: `_mill/reviews/20260717-150053-discussion-review-r2.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- **[NOTE] "Callgraph root/entry-point selection unspecified"** — added a **Callgraph roots**
  paragraph to Decisions → transitive-impact-in-scope: RTA/VTA build the SSA callgraph from
  seed roots, so the author selects and records the root set (baseline: `cmd/lyx` `main` +
  package `init`s, plus test mains), and RTA/VTA caller sets are reported relative to it.
- **[NOTE] "No fallback stated if gopls cannot be installed"** — added a **Load-bearing arm &
  fallback** clause to Technical context → Dependencies: the in-process `go/packages` arm is
  load-bearing (no external binary); the gopls-subprocess comparison degrades to a docs-based
  characterization if `gopls` can't be installed here, as an accepted non-blocking outcome.

## Pushed Back

_None — both NOTEs were factually accurate, harmless to apply, and improved the plan's
self-containedness._
