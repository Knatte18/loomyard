# Discussion fix — round 1

Source review: `_mill/reviews/20260906-094802-discussion-review-r1.md` (reviewer `orchestrator`, verdict APPROVE after the blocking-class ceiling demoted one finding).

## Fixed

- r1 — "Semver tag precondition has no owner or timing" (`[NIT:decision]`, demoted from BLOCKING) — added a **Status and owner** bullet to the `quarry-version-pin` decision: the operator cuts the tag on quarry `main`, preferably after `glyph-unitpath` merges so one tag serves both preconditions; the two-tag case is stated as the alternative; and the initial `go.mod` card must name an existing tag, never a pseudo-version stopgap.
- r1 — "Resolve-backed pass's composition rule unstated" (`[NIT:design]`) — added a **Composition, not duplication** bullet to `package-ownership`: `planglyph`'s resolve-backed entry point calls `planparser`'s pure `ValidateFormat`/`Validate` and adds only resolve findings on top, with the Gate Self-Check Parity Invariant given as the reason no check may be implemented twice.
- r1 — "cgo retreat path dropped from the record" (`[NIT:design]`) — reworded `cgo-posture`'s flat rejection into **Not chosen now, recorded as the named retreat**, noting that the `package-ownership` seam is what keeps the swap cheap.

## Pushed Back

None. All three findings named real gaps in the document.

## Notes on provenance

Two findings attributed their substance to an operator rider that was not stated in this task's discussion: the composition rule and the cgo-retreat framing were both answered bare (`Q4: 1`, `Q6: 1`) with no rider attached. The defects themselves are real, so both were fixed on their own merits and written without the attribution. The third finding's suggested wording ("a one-package refactor") was also not adopted verbatim: the retreat swap touches `internal/planglyph` **and** the `lyx quarry` verb group, so the text states that scope accurately instead.
