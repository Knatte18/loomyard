# Plan fix — round 4

Source review: `20260716-201306-plan-review-r4.md` (APPROVE, 2 NITs).

## Fixed

- [NIT] "hubgeometry.ConfigFile named without hubgeometry in Context" — added
  `internal/hubgeometry/hubgeometry.go` (defines `ConfigFile`) to Card 6's Context.

## Pushed Back

- [NIT] "muxengine.CleanClaudeEnv / lifecycle referenced from doc-comment prose" — no
  change, per the finding's own fix text ("None needed — these are descriptive facts to
  embed in comments, not APIs the implementer reads or calls; leave as-is").
