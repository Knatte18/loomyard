# Discussion fix — round 1

Source review: `_mill/reviews/20260918-163957-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- r1 `[NIT:design]` "dependsOrder: sequence may not actually stop on failure" — the `sequenced-tasks-not-shell-operators` rationale no longer claims VS Code aborts the chain on a non-zero exit; it now relies on `dependsOrder: sequence` for ordering only and flags the abort behaviour as something to verify at implementation time. The `fail-loud-on-no-hub` decision gained the finding's own point: the safety comes from `AddStrand`'s `requireSessionLocked` pre-flight and `attach`'s `c.eng.Status()` pre-flight, not from the task runner, so the end state holds either way and the implementation adds no compensating guard.

## Pushed Back

_None._
