MILL_REVIEW_BEGIN
# Review: reed: AddStrand and attach self-heal a cold worktree — holistic

```yaml
duration_s: 381.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (Sonnet 5, tool-use mode)
reviewed_file: plan/
date: 2026-09-19
```

## Findings

### [NIT:consistency] Card 6's RemoveStrand cross-reference premise is wrong
**Location:** batch 03-comment-sweep.md, Card 6 (strand.go) **Issue:** The card claims "UpdateStrand's and RemoveStrand's doc comments both describe their pre-flight by cross-reference to AddStrand," but `internal/reedengine/strand.go`'s current `RemoveStrand` doc comment already reads "Pre-flights the session's existence (mirroring Status)" — it cross-references `Status`, not `AddStrand`; only `UpdateStrand`'s comment says "(like AddStrand/RemoveStrand)". **Fix:** Correct the premise to note RemoveStrand already cites Status; the remaining ask (state explicitly that RemoveStrand keeps `requireSessionLocked` and does not self-heal) still stands and needs no "re-point."

## Verdict

APPROVE
Every card's mechanism claims verified against source; only one minor, non-blocking premise inaccuracy found.
MILL_REVIEW_END
