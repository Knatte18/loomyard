MILL_REVIEW_BEGIN
# Review: gitexec: add the checked entry point and migrate the call sites — holistic

```yaml
duration_s: 492.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-13
```

## Findings

### [NIT:consistency] Card 36's prose sweep contradicts the bounded-read Shared Decision
**Demoted-from:** BLOCKING
**Location:** batch 8, card 36 (`08-checked-call-invariant-and-docs.md`)
**Issue:** Card 36's `Context:` names six specific files, but its `Requirements:` instruct "re-run the prose regeneration sweep... grep for `RunGit`, `gitexec`, `exit code`, and `exitCode` across the markdown files and `doc.go` files under `docs/`, `manifest/`, and `internal/`, plus `CONSTRAINTS.md`... and read every hit" — an open-ended read across three whole directory trees. This directly contradicts the plan's own Shared Decision `no-source-file-reads-outside-a-card-s-lists` ("every card's `Context:` is an allowlist... never widens the read set beyond the card's `Context:` + `Edits:` + `Creates:`"), which is stated to apply to all batches. If the sweep turns up a genuinely falsified file beyond the ones the card already pre-confirms (`docs/overview.md`, `docs/shared-libs/lyxcwd.md`), the card also has no `Edits:` entry to place a fix in.
**Fix:** Either enumerate the actual candidate files the sweep is meant to check in `Context:` (so the read set is bounded as every other card's is), or drop the "grep and read every hit" instruction and rely solely on the hand-list the card already gives, explicitly deferring any further prose drift to a follow-up task.

## Verdict

APPROVE
Card 36 (batch 8) instructs an unbounded repo-wide read that contradicts the plan's own bounded-Context Shared Decision.
MILL_REVIEW_END
