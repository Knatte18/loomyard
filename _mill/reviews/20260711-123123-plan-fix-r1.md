# Plan fixer report — round 1

## Fixed

- `20260711-122913-plan-review-r1.md` — "[BLOCKING] strandLive cannot read liveness from persisted mux state": card 18 now sources liveness via `shuttleengine.MuxOps.Status()` scanning the returned `Strands` for the guid's `Live` field, with an explicit "never from `muxengine.LoadState`" note; card 28's CLI wiring updated to use the mux engine seam.
- `20260711-122913-plan-review-r1.md` — "[BLOCKING] turnEnded re-parses Claude event grammar in a provider-invariant package": card 18's `turnEnded` now delegates to `shuttleengine.Engine.ParseEvents` and checks for `Kind == shuttleengine.EventStop`; Provider-Seam Invariant note added; Context gains `internal/shuttleengine/engine.go` + `mux.go`; tests use fake Engine/MuxOps.
- `20260711-122913-plan-review-r1.md` — "[BLOCKING] SpawnBatch has no way to obtain the shuttle run dir": card 21's `SpawnDeps` gains `ShuttleCfg shuttleengine.Config` + `Layout *hubgeometry.Layout`; step 8 resolves `(RunState, runDir)` via `shuttleengine.FindRun(cfg, layout, guid)` and records run dir + `RunState.EventsPath`; `internal/shuttleengine/rundir.go` added to Context; card 12's `BatchState` gains `EventsPath string`.
- `20260711-122913-plan-review-r1.md` — "[NIT] Card 10 references builderengine.ConfigTemplate without listing its file": `internal/builderengine/template.go` added to card 10's Context (exists by card 10 — created in card 8, same batch).

## Pushed Back

(none)
