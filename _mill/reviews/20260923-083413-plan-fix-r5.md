# Plan fix — round 5

## Fixed

- `20260923-083330-plan-review-r5.md` — "Re-run after a live-strand refusal skips readiness": took the finding's third option, which matches the discussion's Out-of-scope entry on already-live driver strands. Card 3 now states the residual in the step-6 comment and in the `Long` text, and scopes `--no-attach` to a driver this invocation spawns.
- `20260923-083330-plan-review-r5.md` — "run.go header leaves AwaitStarted out of the Run surface": card 1 now adds `AwaitStarted` to `run.go`'s header sentence and the `Run` type doc.

## Pushed Back

