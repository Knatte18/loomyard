# Plan fix report — round 3

Source review: `_mill/reviews/20260826-120025-plan-review-r3.md`

## Fixed

- `20260826-120025-plan-review-r3.md` — "[BLOCKING:scope] Card 2 Context omits clone.go despite naming CloneAndWire": accurate. Card 2's `SeedConfig` doc-comment requirement names `fabriccli.CloneAndWire` as the cause of the clean weft prime, but `internal/fabriccli/clone.go` — its declaring file — was absent from the card's `Context:` allowlist, which listed only `internal/fabriccli/pushbypass_integration_test.go` from that package. Added `internal/fabriccli/clone.go` to card 2's `Context:`.
- `20260826-120025-plan-review-r3.md` — "[NIT:consistency] Card 2 commit scope omits preflightshed": accurate. The card edits `internal/hubforge`, `internal/preflight` and `internal/preflightshed`, but the conventional-commit scope named only the first two. Extended it to `test(hubforge,preflight,preflightshed): ...` rather than dropping the scope list, since the explicit package enumeration is what makes the three-site class disposition visible in git history.

## Pushed Back

_None._
