MILL_REVIEW_BEGIN
# Review: fabric: one ownership-and-dirtiness gate for all destruction (slice 12) — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-11
```

## Findings

### [NIT:scope] `warpforward_integration_test.go` omitted from the review manifest
**Location:** `internal/fabricengine/warpforward_integration_test.go`
**Issue:** This file is required editing per `00-overview.md`'s `All Files Touched` and `03-path-callsites.md`'s "Plan-gap fix" section (renaming/splitting `TestFabricWarp_ResetHardDiscardsCommitsAndWorktreeChanges`), but it is absent from this brief's `## Files included` manifest.
**Fix:** I read the file directly via the filesystem (outside the manifest) to verify it: it correctly implements the plan-gap fix — `TestFabricWarp_ResetHardDiscardsCommitsOnCleanWorktree` (clean-worktree discard, unaffected half) and `TestFabricWarp_ResetHardRefusesDirtyWarpCheckout` (new refusal, asserts both the later commit and the uncommitted edit survive) both exist and match the required assertions verbatim. No code defect; future manifests for this slice should include this file.

### [NIT:design] `rollbackAdd`'s refusal-surfacing has no visible destination
**Location:** `internal/fabricengine/add.go:139,143,151,156,161,173,177,186,193,197,204` (six `_ = t.rollbackAdd(...)` call sites) vs. `add.go:271-308` (`rollbackAdd`'s `surfaceRefusal`-wrapped accumulator)
**Issue:** `rollbackAdd` correctly wraps its own worktree-remove and branch-delete executor calls in `surfaceRefusal` and folds a refusal into `firstErr`, which it returns — satisfying the CONSTRAINTS.md invariant's literal text ("every such site wraps its executor call in `surfaceRefusal`... rather than swallowing it"). However, every one of `Topology.Add`'s six call sites discards `rollbackAdd`'s return value entirely (`_ = t.rollbackAdd(...)`), so a refusal that was carefully surfaced inside `rollbackAdd` is never logged, returned, or otherwise observable by an operator — functionally equivalent to the "keep discarding everywhere" alternative the discussion's `a-refusal-is-never-best-effort` decision rejects, one level removed.
**Fix:** Not blocking — this six-site discard pattern in `Add()` predates this slice and is not named in card 25's edit scope (`rollbackAdd`'s own internals only), so changing it would be an unplanned behaviour change under the "regression posture" decision. Worth a follow-up (e.g. `logger.Warn` at the `Add()` call sites) but out of this slice's stated scope.

### [NIT:scope] `destroy_test.go`'s containment coverage omits two named cases
**Location:** `internal/fabricengine/destroy_test.go:64-134` (`TestGate_Containment`)
**Issue:** Card 12 asks for containment coverage of "`..`, `../x`, `.`, an absolute path outside the container, a path equal to the container, and both platform separators." The test table covers `DotDot`, `DotDotSlashX`, `EqualToContainer`, `AbsoluteOutsideContainer` — four of six. `.` is implicitly covered by `EqualToContainer` (both yield `filepath.Rel` == `"."`), but there is no test exercising both `/` and `\` separator forms.
**Fix:** Not blocking — `refuseUncontainedPath` (`ancestors.go`) uses `filepath.Rel`/`filepath.Separator`, which are inherently platform-native, so a same-OS separator test adds little; genuine cross-platform coverage would need a GOOS-gated case. Low-value gap, noted for completeness.

## Verdict

APPROVE
Implementation matches the plan precisely across all seven batches, gate logic, tests, guard, and docs; only minor non-blocking observations found.
MILL_REVIEW_END
