# Plan fix report — round 3

## Fixed

- `20260626-071800-plan-review-r3.md` — "[NIT] Card 7 in-sync precheck placement is imprecise": card 7 now places the in-sync assertion after `WireJunctions` + `hostLayout` resolution (drift_test.go:~116-124), before `fslink.Remove` — not right after `Add` (which would observe a missing junction).
- `20260626-071800-plan-review-r3.md` — "[NIT] Card 9 omits the pre-drift Status() call it requires": card 9 now states two distinct `Status()` invocations — one before the drift for the field-population assertions, one after for the drift check.
- `20260626-071800-plan-review-r3.md` — "[NIT] Card 6 ForkPointMirrorsHost deletion drops the main-case positive assertion": rather than lose the positive merge-base==main-tip assertion, it is folded into card 2's HappyPath `extraAssert` (new item e: `git merge-base <weftBranch> main` in the weft repo equals the weft `main` tip). Card 6 updated to note the fold; SubtaskIsolation still exercises the fork-from-parent mechanism.

## Pushed Back

(none)
