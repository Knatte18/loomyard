# Plan Fix — round 1

Source review: `_mill/reviews/20260725-130350-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260725-130350-plan-review-r1.md` — "[BLOCKING] Batch 3 reuses batch 2 symbols but depends only on [1]":
  Card 10's `SetSnapshotSHA` reuses `read.go`'s `isStrictDescendant` (card 7) and
  snapshot-key validators (card 6), and `write.go`/`read.go` are the same package,
  so batch 3 cannot compile (nor pass its `go test -tags integration` verify)
  before batch 2 lands. Changed batch 3 `depends-on: [1]` → `depends-on: [1, 2]`
  in both `00-overview.md` (Batch Index) and `03-write-surface.md` (frontmatter).
  DAG re-validated: deps now `1→2→3→4` (4 keeps `[2, 3]`).
- `20260725-130350-plan-review-r1.md` — "[NIT] Batch 3 scope's 'shares no files → parallel' reasoning is unsound":
  Reworded `03-write-surface.md` Batch Scope to drop the parallel-with-batch-2
  claim and state the batch-2 dependency explicitly (Card 10's cross-card symbol
  reuse; cards 8/9 noted as self-contained but scheduled after batch 2 regardless
  since they co-inhabit one package). Also corrected the reciprocal parallel claim
  in `02-read-surface.md` Batch Scope for coherence (it asserted the same unsound
  file-disjointness parallelism from the other side).

## Pushed Back

_(none)_
