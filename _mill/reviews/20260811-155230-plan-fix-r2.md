# Plan fix — round 2

Source review: `_mill/reviews/20260811-154319-plan-review-r2.md` (holistic, REQUEST_CHANGES, 4 BLOCKING + 4 NIT).

## Fixed

- `20260811-154319-plan-review-r2.md` — "[BLOCKING:scope] Constructive inventory misses writes the oracle asserts".
  Verified: `writeLaunchers` writes three files under `_launchers/<anchor>/<slug>` from two callers, and `CloneHub` creates `<hub>/.lyx`, two clone roots, `<hub>/_board`, `.lyx-anchor` and `.lyx-warp` — none recorded.
  Fixed structurally rather than by extending the list, because a hand-written list is what failed here: a new Shared Decision ("record at the covering root, and derive the inventory rather than enumerating it") fixes recording at the coarsest covering root and requires the implementer to run a stated grep-and-classify derivation over `os.WriteFile(`/`os.MkdirAll(`/`os.Mkdir(`/`fslink.CreateDirLink(`/`cloneRepo(`, sorting every production hit into recorded-here / covered-by-an-enclosing-root / invisible-to-the-manifest and writing the classification into the batch's completion note.
  New batch-5 card 21 records `writeLaunchers`' directory as one entry and each of `CloneHub`'s six construction steps.
- `20260811-154319-plan-review-r2.md` — "[BLOCKING:scope] Card 17's `seedWeftArtifactExcludes` breaks callers not in Edits".
  Verified the chain through `ensureWeftLockDirAt`/`ensureWeftLockDir` and its five callers.
  Resolved by dropping the site rather than widening `Edits:`: it writes only under the `.git` metadata directory, which `CaptureManifest` excludes wholesale, so the record would buy the oracle nothing while forcing a parameter onto a five-caller chain.
  The weft lock directory it creates is covered by the enclosing `worktree_created`.
- `20260811-154319-plan-review-r2.md` — "[BLOCKING:scope] Two threaded helpers each have a caller outside their card's Edits".
  Verified `createWeftWorktree` at `add.go:149` and `reconcile.go:511`, `unwireBoardLink` at `unwire.go:79` and `remove.go:99`.
  Added `internal/fabricengine/reconcile.go` to card 18's `Edits:` and `internal/fabricengine/remove.go` to card 14's, and corrected both cards' prose to name both callers.
- `20260811-154319-plan-review-r2.md` — "[BLOCKING:scope] Card 21's Context omits `newFabricFixture`'s declaring file".
  Verified: declared in `internal/fabricengine/reconcile_stale_registration_test.go`, not in the guard file.
  Attribution corrected and the file added to what is now card 22's `Context:`.
- `20260811-154319-plan-review-r2.md` — "[NIT:consistency] Card 13 repoints `teardownHub` before card 14 declares it".
  Verified.
  The `export_test.go` repoint moved into card 14, which now lists the file in `Edits:`; card 13 keeps it in `Context:` and says explicitly that card 14 owns it.
- `20260811-154319-plan-review-r2.md` — "[NIT:consistency] Batch 4 states two different vet scopes".
  Verified.
  The `## Batch Tests` prose now states the authoritative `./...` form and says why the width is load-bearing.
- `20260811-154319-plan-review-r2.md` — "[NIT:consistency] Card 30 misdescribes `AssertNoUnpermittedChange`'s signature".
  Verified: it takes `(tb, before, after, permitted)` and diffs internally.
  Reworded to keep the existing call untouched and add exactly one `DiffManifest(before, after, nil)`.
- `20260811-154319-plan-review-r2.md` — "[NIT:decision] Card 24 leaves the `Bolt.Commit` entry's `Target` unstated".
  Verified.
  Both `Bolt.Commit` record sites now fix `Target` to the board worktree path, with the omission-coverage consequence stated.

Also corrected without a finding: card 34's `CONSTRAINTS.md` wording still asserted the unqualified fixed-key-set rule that round 1 carved out in the Shared Decision;
it now carries the pre-flight carve-out too.
Cards were renumbered so batch 5's new card is 21 and the later batches shift by one (old 21-35 → 22-36), keeping global numbering sequential.

## Pushed Back

_None._
Every finding was factually accurate;
two were resolved by removing work rather than adding it (the `.git`-only exclude write, and the `AssertNoUnpermittedChange` signature), which is a narrower change than the review proposed but satisfies the same requirement.
