# Discussion fix — round 1

Source review: `_mill/reviews/20260828-054638-discussion-review-r1.md` (verdict APPROVE, 1 NIT — demoted from `BLOCKING:consistency` by the blocking-class ceiling).

## Fixed

- `20260828-054638-discussion-review-r1.md` — "CommitMessage's Body-trimming is presented as decided, then reopened": added a `commitmessage-body-trim` Decision pinning the exact output (`Title` alone when `Body` is blank, otherwise `Title + "\n\n" + strings.TrimLeft(Body, " \t\r\n")`, trailing whitespace untouched), rewrote the Technical-context gotcha to point at that Decision instead of deferring to the plan, and expanded the `CommitMessage` test bullet to enumerate the cases the rule creates.

## Pushed Back

_None._
