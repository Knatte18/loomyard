# Plan fix — round 1

Source review: `_mill/reviews/20260811-035155-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 0 NIT).

## Fixed

- r1 `[BLOCKING:scope]` "Card 3's wiki-task body omits the Client Boundary Invariant guard fix" — verified accurate against `_mill/discussion.md`'s `guard-test-with-justification-comments` decision, which records all three Client Boundary assertions as breaking in the implementation commit as a fact rather than a risk.
  Card 3's body requirement is now a four-item list, with the Client Boundary Invariant's three broken assertions named as their own item and explicitly distinguished from the literal-token guards as a structurally distinct collision keyed on occurrence counts and method names.
  The invariant's enclosing test file is named in prose rather than as a backtick path, because Card 3's implementer must not read it — the card writes wiki text, and adding the file to `Context:` would widen the card's read allowlist for no purpose.

## Pushed Back

_None._
