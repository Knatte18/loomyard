# Plan fix — round 2

Source review: `_mill/reviews/20260730-181432-plan-review-r2.md` (verdict REQUEST_CHANGES, 1 BLOCKING, 2 NIT).

## Fixed

- `20260730-181432-plan-review-r2.md` — "[BLOCKING] Card 3 instructs keeping an import that becomes unused": corrected Card 3's retained-imports instruction — `strings`'s only users (`toRedditJsonUrl`, the JSON formatters, `selftextSnippet`) are all deleted by this card, so the final `reddit.go` imports only `context` and `regexp`; `strings` is now explicitly listed among the imports to drop (unused import = compile error).
- `20260730-181432-plan-review-r2.md` — "[NIT] `maxTopComments`'s doc comment references a function this batch deletes": added `maxTopComments`'s doc comment to Card 3's comment-rewrite list, redescribing it as the shared top-level-comment bound now reused by the Hacker News adapter.
- `20260730-181432-plan-review-r2.md` — "[NIT] GitHub tree-API truncation not addressed": added a line to Card 10 requiring the skill to check the tree response's `.truncated` field and fall back to non-recursive per-directory tree calls when true, so a large-repo browse is never silently partial.

## Pushed Back

- None.
