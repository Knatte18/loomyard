# Discussion fix report — round 5

Source review: `_mill/reviews/20260831-180936-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 3 NIT — one demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r5 review, "[NIT:consistency] F18 edit text unqualified on vantage point" (demoted from BLOCKING) — the `SANDBOX-FABRIC-SUITE.md:404` disposition row now requires the F18 edit to name the vantage point (the prime worktree holding the record) and to state that the field does not predict `remove`'s hub-wide `mergeSourceInFlight` refusal, mirroring the `Long`-text qualification the `field-is-this-pair-only` decision already demands. The finding was correct that the prescribed sentence, sitting beside F18's existing list of refusing verbs, reintroduced the exact ambiguity this task's own decision forbids.
- r5 review, "[NIT:scope] Parked-merge fixture shape underspecified" — the "Test scaffolding" bullet now separates `hubforge.NewHub` from the optional `hubforge.SeedFabricConfig`, records that the two cited exemplars differ (`cli_test.go:900-901` seeds; `merge_cli_integration_test.go:433` does not), and assigns a shape per scenario: scenario 1 seeds, scenario 2 does not, each matching the exemplar it copies. Verified both call sites.
- r5 review, "[NIT:consistency] Reflow disposition for `docs/overview.md:210` unstated" — the disposition row now says the edit is appended in place with no reflow, and gives the reason (a reflow would turn a one-sentence change into a whole-line rewrite that hides the edit; reflowing that bullet belongs to its own mechanical pass).

## Pushed Back

None. All three findings were factually accurate and no fix broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task's scope.
