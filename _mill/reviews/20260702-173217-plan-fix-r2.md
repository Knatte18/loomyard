# Plan fix — round 2

Source review: `_mill/reviews/20260702-173016-plan-review-r2.md` (verdict REQUEST_CHANGES,
2 BLOCKING + 2 NIT). Fixer: mill-plan. All four findings passed the `mill-receiving-review`
decision tree (factually accurate, fix causes no harm) → FIX. No push-backs. DAG +
`_plan_validate` re-run clean (0 errors) after the edits.

## Fixed

- BLOCKING "Batch 4: overlay card precedes its parsers" (04-muxengine-carrier, cards 10→11)
  — swapped the two cards so the pure parsers + `LivePane` land **first** (new card 10:
  `doc.go` + `parse.go` + `parse_test.go`, defining `LivePane` in `parse.go`), and the psmux
  overlay wrapper that calls them lands **second** (new card 11: `overlay.go`, Context now
  lists `parse.go`). Each card compiles standalone at its boundary — no forward reference.
- BLOCKING "Batch 3: height card forward-references focus + layout symbols" (03-render) —
  reordered so `focus.go` (now card 7) precedes `height.go` (now card 8); `stackHeights`
  consumes `placement` (card 5's `layout.go`) and `isAncestor` (card 7's `focus.go`), both of
  which now precede it. Added `render/layout.go` + `render/focus.go` to the height card's
  Context. Added a "Card order matters" note to the batch scope, and updated the Batch Tests
  note (the no-standalone-test cards are now 5 and 7).
- NIT "Rules named return shadows focusTarget function" (03-render, card 9) — renamed the
  second return value from `focusTarget` to `focus` (`func Rules(...) (layout string, focus
  string, err error)`) so the body can call the `focusTarget(ordered)` helper without
  shadowing it. Updated the batch scope's external-interface signature to match.
- NIT "Batch 4 depends-on lists logger unnecessarily" — resolved by **adding the intended
  logger consumer** (the reviewer's stated alternative) rather than dropping the dep: the
  overlay's `run`/`output` now emit a `logger.Debug("psmux", ...)` command trace (silent at
  the default `Warn` level, surfaced by `-vv`). Added `internal/logger/logger.go` to card
  11's Context. Batch 4's `depends-on: [1, 2, 3]` is now honest — dropping the dep would have
  left the logger a dead package in this task.

## Pushed Back

(none)
