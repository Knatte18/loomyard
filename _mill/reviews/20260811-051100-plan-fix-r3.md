# Plan fix — round 3

Source review: `_mill/reviews/20260811-050915-plan-review-r3.md` (APPROVE, 0 BLOCKING, 1 NIT, no demotions).

## Fixed

- r3 — "[NIT:consistency] Card 4's quoted anchor block is pre-edit text, not the quoted-text anchor the plan's own convention calls for" — the observation is accurate, and the reviewer's own stated remedy was "none required".
  Rewriting the fence to post-card-2 text was rejected as harmful rather than adopted: `_plan_validate`'s `requirements-quote-indent-drift` check requires a `Requirements:` fence to be a byte-exact substring of the target `Edits:` file as it stands on disk, so a fence quoting the post-card-2 wording would fail the validator gate at the next round.
  Fixed the underlying confusion instead: card 4 now states explicitly that the fence is a byte-exact anchor into the file **as committed**, not a prediction of the line's state, and directs the implementer to locate the line by the "a distilled digest, never raw prose" clause, which no earlier card touches.

## Pushed Back

_None._
The one finding was accurate and is addressed;
only the reviewer's literal remedy (rewrite the quoted text) was declined, with the harm cited — it would break the `requirements-quote-indent-drift` validator check — and replaced with a fix that resolves the same ambiguity without that consequence.
