# Discussion fix — round 1

Source review: `_mill/reviews/20260715-071948-discussion-review-r1.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- [r1] "[NOTE] Live config change needs a server restart to take effect" — Accurate against the code (`ensureServerAndSessionLocked` early-returns at lifecycle.go:223 when the session is already up, so `set-option` only runs on a fresh boot). Added the live-toggle-requires-server-restart consequence to Technical context (boot site) and to the Documentation Lifecycle constraint (docs must state it).
- [r1] "[NOTE] Empty-after-env handling diverges from debug_log precedent" — Accurate; "mirror debug_log" is ambiguous for the empty case because debug_log's own comment-vs-code contract conflicts. Made the empty-string contract explicit in Decision: validate-up-front (`mouseOption("")` errors, no silent default-to-off) and in the Testing value-helper scenario.

## Pushed Back

_None._
