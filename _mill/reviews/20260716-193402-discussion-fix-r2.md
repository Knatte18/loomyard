# Discussion fix — round 2

Source review: `20260716-193304-discussion-review-r2.md` (APPROVE, 2 NOTEs).

## Fixed

- [NOTE] "`default` fan name collides with 'never default'" — renamed the seeded fan
  `default` → `standard` throughout, and stated explicitly that every seeded fan is
  dormant until a round profile names it in `cluster-fan`.
- [NOTE] "Low-core concurrency cap vs exact-N and the N=2 smoke" — added to the
  cluster-timeout decision that serialization under the CC cap never breaks exact-N
  (only wall-time grows; surfaces as `OutcomeTimeout`, never fork-shortfall), and the
  smoke-test bullet now requires a generous explicit timeout so a slow runner reads as
  a timeout, not a shortfall.

## Pushed Back

(none)
