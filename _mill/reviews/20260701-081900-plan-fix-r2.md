# Plan fix — round 2

## Fixed

- Ref: `_mill/reviews/20260701-081550-plan-review-r2.md` — "dispatch signature change breaks integration test": added `internal/configcli/configcli_integration_test.go` to batch 01, card 5's `Edits` (was Context-only, but it's `//go:build integration`-tagged and calls `dispatch()` directly, so it needs editing, not just reading); pinned `setFlags` as the new LAST parameter of `dispatch()`/`runConfig()` so all 5 existing call sites (4 in `configcli_test.go`, 1 in `configcli_integration_test.go`) are fixed mechanically by appending one `nil` argument; added this to the overview's All Files Touched.
- Ref: `_mill/reviews/20260701-081550-plan-review-r2.md` — "boardengine git-stderr leaks left uncovered" [NIT]: added an explicit "Deliberately NOT applied to" note in the overview's Shared Decision, citing the three `boardengine` sites and `_mill/discussion.md`'s Scope → Out reasoning, so the plan is self-contained on this already-approved exclusion instead of relying on the reader having discussion.md open.

## Pushed Back

(none)
