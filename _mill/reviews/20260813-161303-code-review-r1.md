MILL_REVIEW_BEGIN
# Review: gitexec: add the checked entry point and migrate the call sites — holistic

```yaml
duration_s: 254.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-13
```

## Findings

### [NIT:scope] TestRun_StdoutOnError does not actually assert non-empty stdout
**Location:** `internal/gitexec/gitexec_test.go:140-163`
**Issue:** Card 3 (`01-gitexec-checked-entry-point.md`) requires a Tier 2 scenario proving "stdout is still returned alongside that error, using a command that writes to stdout and exits non-zero." The chosen command (`git branch -v --nonexistent-flag`) fails argument parsing before listing branches, so stdout is empty — the test's own comment concedes this ("this command's stdout is legitimately empty") and discards `stdout` unchecked. No test in the file positively asserts a non-empty `stdout` alongside a `*GitError`; `TestRun_NonZeroExit` punts identically (line 135).
**Fix:** Pick a command that genuinely emits stdout before failing (e.g. a formatted `git log`/`git branch --list` against a seeded repo, followed by a second failing arg) and assert `stdout != ""`, or narrow the doc comment/requirement to match what is actually proven.

## Verdict

APPROVE
Implementation matches the plan precisely across all 8 batches; only a minor test-assertion gap found.
MILL_REVIEW_END
