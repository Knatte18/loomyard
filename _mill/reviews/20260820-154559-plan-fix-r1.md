# Plan fix report — round 1

Source review: `_mill/reviews/20260820-154519-plan-review-r1.md`

## Fixed

- r1 `[BLOCKING:design]` "Card 6's ResolveRound hard-error path skips cancelErr" — batch 3 card 6's `ResolveRound`-error branch now consults `cancelErr` first and returns the resolve failure only when the context is not cancelled; batch 3's Batch Scope note states the same, so hard-versus-degraded governs which error is returned rather than whether cancellation is checked.
- r1 `[NIT:consistency]` "Stencils.go family-ordering rationale is factually wrong" — batch 2 card 4 no longer claims the file's family groups are alphabetical; the placement instruction now cites the file's existing physical ordering.

## Pushed Back

_None._
