# Fixer report: plan-fix round 1

Source review: `20260908-133249-plan-review-r1.md`

## Fixed

- `20260908-133249-plan-review-r1.md` — "[BLOCKING:scope] Card 7 silently deletes burler's R4-26 wiring composition test".
  Verified against `internal/burlercli/wiring_test.go:655-673`: the `SubdirectoryWiresOntoTheRootsOwnStateDir` sub-test drives `c.wire` and asserts `c.stateDir`, so it is composition coverage, not a pure-function test.
  Card 7 now removes `TestResolveStandaloneTarget_LiftsToRepositoryRoot` from the wholesale-delete list, deletes only its three pure-function sub-tests, and promotes the fourth to a retained top-level `TestWireStandalone_SubdirectoryOfRepositoryWiresLikeItsRoot`, matching the name card 6 keeps for webster's analog.
- `20260908-133249-plan-review-r1.md` — "[NIT:consistency] Import-drop lists in cards 6/7 omit likely-orphaned `fmt`/`path/filepath`".
  Verified by tracing each post-rewrite file's surviving bodies: `path/filepath` is orphaned in both files, and `fmt` is orphaned in burler's alone (its descriptor leaves `Plan` nil, so it builds no message) while surviving in webster's (the `MissingPlanRefusal` closure uses `fmt.Sprintf`).
  Both cards now state that the named set is a non-exhaustive starting point, that the whole import block must be walked and grep-verified entry by entry because an unused import is a hard compile error, and each card records its own `fmt` expectation.
- `20260908-133249-plan-review-r1.md` — "[NIT:consistency] `MissingPlanRefusal`'s second parameter name doesn't match its actual argument".
  Verified against card 4's own step 6: the argument is `planDir` itself whenever `overridden` is false.
  The field is now `MissingPlanRefusal func(planDir, recourse string) string`, card 3 documents why the name is branch-neutral, and card 4's step 6 records the same reason at the call site.

## Pushed Back

_None._
