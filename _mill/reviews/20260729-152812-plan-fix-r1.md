# Plan-fix report — round 1

Source review: `_mill/reviews/20260729-152812-plan-review-r1.md` (reviewer sonnet · xhigh; verdict REQUEST_CHANGES, 4 BLOCKING + 2 NIT). All findings verified accurate against code/files; none warranted pushback (all improve the plan; BLOCKING-4's graceful reason still honors the no-fallback rule — the failure is surfaced as a verdict reason, not defaulted).

## Fixed

- [BLOCKING] "Card 10 breaks existing test TestAdd_RejectsReservedHubNameSlug" — verified `add_test.go:124` constructs `NewTopology(Config{})` (empty) with a `{"LyxDir","_lyx"}` case; after card 1 drops `_lyx` from `HubReservedNames()`, `IsReservedHubName("_lyx", [])` is false and batch 2's own `verify:` fails. Card 10 now also edits `add_test.go` to construct the `Topology` with `Config{Pathspec: "_lyx _pattern"}`, landing the fix in batch 2 (not deferred to batch 3's card 14).
- [BLOCKING] "docs/shared-libs/hubgeometry.md left stale" — verified the file documents the old `HostJunctions`/`HostJunctionsHere` signatures and "two entries, `_lyx` first" at `:124-125`. Card 4 now also edits `docs/shared-libs/hubgeometry.md` (injected-`names` signatures, pathspec-driven order, new `HubReservedNames()` bullet); added to All Files Touched.
- [BLOCKING] "manifest/designs/fabric-unified-view.md not updated" — verified the design doc names this task as build-order slice 1 (`:104`) and lists the "Home of the junction-name config" open question (`:116`). Card 15 now also edits it (mark slice 1 done, resolve the open question); added to All Files Touched. `manifest/roadmap.md` deliberately untouched per the discussion.
- [BLOCKING] "PairInSync vs checkJunctionHealth diverge on config-load failure, breaking Preflight's contract" — verified `preflight.go:120` propagates PairInSync's `err` into `return Report{}, err`. Card 8 now returns `(false, "cannot load fabric.yaml: <err>", nil)` (a determinable unhealthy verdict reason, `nil` Go error) for parity with card 9's `checkJunctionHealth`, so a bad/absent config is not converted into a preflight infra-escalation. The overview no-fallback Shared Decision was updated to record the verdict-reason-vs-hard-error shape distinction and why it still honors no-fallback.
- [NIT] "Card 2's UnwireResult godoc citation points at the wrong file" — verified `UnwireResult.JunctionsRemoved` lives in `internal/fabricengine/junction.go`, not `hubgeometry.go` (card 2's only `Edits:`). Card 2 now retargets the edit to `HostJunctions`'s own godoc (~`hubgeometry.go:794-797`) and explicitly excludes the cross-package `UnwireResult` field comment (covered by batch 2).
- [NIT] "removeHostJunction config-load failure silently skips nested-junction cleanup" — verified `remove.go:91` discards the error and `fslink.RemoveLinksIn` only sweeps root-level links. Card 7's requirements now document this residual nested-junction (`RelPath != "."`) leak on unreadable config during forced removal as the conscious no-fallback trade-off; a best-effort `_lyx`/`_pattern` default sweep is explicitly rejected (would reintroduce the removed literals and violate no-fallback).

## Pushed Back

(none — all six findings were factually accurate and improved the plan; applied under the fix-everything default.)
