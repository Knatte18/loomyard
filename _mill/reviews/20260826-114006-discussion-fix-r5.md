# Discussion fix — round 5

Source review: `_mill/reviews/20260826-113834-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 5 NIT).

## Fixed

- r5 review, "plan-unapproved's position in Validate's order" (demoted from BLOCKING) — settled the contradiction in favour of preserving the spec's fixed order: `Validate` splices the approval finding at position two via an unexported `validate(plan, worktreeRoot, requireApproved bool)` shared by both exported wrappers, and `loom-plan-spec.md`'s ordered sixteen-row list is not renumbered.
  Stated in the same sentence that defines `Validate`.
- r5 review, "Rubric reword: fifteen over a contiguous range" — replaced the vague "reword the count" disposition with the exact replacement phrasing, keeping the `format-unrecognized`-through-`commit-subject-mismatch` range intact and carving out `plan-unapproved` as enforced downstream by `Plan-Revalidate`.
- r5 review, "gatefindings_test.go disposition left as an unchosen either/or" — picked: re-point the fixture to an approved, format-clean plan plus one unindexed orphan `.md` file so the single finding is `index-file-mismatch`, and keep the test in the default mode.
  Recorded the reason (the test's subject is log-line plumbing, and `planvalidate_test.go`'s mode table is where `require_approved` belongs) and noted it reuses the same verified corruption as the `revalidate_test.go` disposition.
- r5 review, "NewPlanValidate call-site count contradicts its own list" — dropped the false count, restated the scope as every `loomshed.NewPlanValidate` construction in the repo with the build as the enumerator, and added the two omitted sites (`internal/loomcli/parity_test.go:197`, the second `shape_test.go:52` construction).
- r5 review, "Plan-Burler's recipe instructions still claim upstream approval enforcement" — added `contracts/recipes/loom-recipe.yaml`'s `Plan-Burler` `fasit.instructions` to the in-commit edit list with its own Technical-context entry, and answered the open question the finding raised: the never-self-approve rule binds the `Plan-Burler` fixer as well as `Plan-Write`, stated as prose in the row's instructions.
  Also recorded that the neighbouring `Webster-Burler` instructions need no change, since they name both validating rows and those two together still enforce all sixteen checks.

## Pushed Back

(none — all five findings verified accurate against the source and none tripped a harm check.)
