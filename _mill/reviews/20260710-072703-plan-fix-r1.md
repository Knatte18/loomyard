# Plan-fix report — round 1

Source review: `_mill/reviews/20260710-072431-plan-review-r1.md` (holistic, REQUEST_CHANGES,
1 BLOCKING + 4 NIT).

## Fixed

- `20260710-072431-plan-review-r1.md` — [BLOCKING] "Batch 3 contract test is never executed by
  any verify": changed batch 3 `verify` (in both `00-overview.md` and `03-mux-contract-and-godoc.md`)
  from `go vet -tags integration ... && go test ...` to
  `go test ./internal/muxengine/... && go test -tags integration -run TestMultiplexerContract ./internal/muxengine/...`,
  so the contract test now executes against the on-box psmux (self-skips if absent). Updated the
  batch's `## Batch Tests` prose to match.
- `20260710-072431-plan-review-r1.md` — [NIT] "Card 15 targets an overview.md sentence that does
  not exist": reworded card 15 to add `internal/shell` to the real shared-infra sentence
  (`overview.md:272-274`, listing configengine/gitexec/lock/output/hubgeometry/state) and the
  directory tree, dropping the fictional proc/fslink/fsx "portability family" anchor (that phrasing
  lives in roadmap.md, not overview).
- `20260710-072431-plan-review-r1.md` — [NIT] "Card 4 self-contradicts on the `_windows.go`
  suffix": reworded card 4 so the "no `_linux` suffix" rule is scoped to the POSIX file only
  (explicit `//go:build !windows` on a non-suffixed `template_posix.go` to cover all non-Windows
  GOOS), while `template_windows.go` keeps the `_windows` suffix + explicit tag per the
  `proc_windows.go` convention.
- `20260710-072431-plan-review-r1.md` — [NIT] "Per-card Context omits same-batch producer files":
  added the producer file to each consuming card's `Context:` — card 3 (`proctree.go`), card 6
  (`version.go`), card 7 (`probe.go`), card 12 (`internal/shell/shell.go`), card 18
  (`launcher_content.go`), mirroring card 13's existing treatment of `shell.go`.
- `20260710-072431-plan-review-r1.md` — [NIT] "launchers.go package doc goes stale after the .sh
  branch": added to card 18's requirements an explicit instruction to update the `launchers.go`
  package/leading doc comment (currently "Windows-only / no-op elsewhere") to describe the new
  cross-platform `.cmd`/`.sh` behavior.

## Pushed Back

_None — every finding was factually accurate and its fix carried no demonstrable harm._
