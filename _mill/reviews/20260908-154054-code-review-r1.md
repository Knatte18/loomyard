MILL_REVIEW_BEGIN
# Review: Unify webster/burler CLI wiring into a shared module — holistic

```yaml
duration_s: 172.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-08
```

## Findings

### [NIT:consistency] Plan card literal `TargetRecourse` value would double text if taken verbatim
**Location:** `_mill/plan/02-rewire-clis.md:49,129` vs. `internal/webstercli/wiring.go:38`, `internal/burlercli/wiring.go:35`, `internal/cliwire/module.go:109-112`
**Issue:** Cards 6/7 literally specify `TargetRecourse: "Drive a target outside the state home"` / `"Review a target outside the state home"`, but `module.go`'s reverse-nesting format string already appends the literal suffix `" outside the state home, or point XDG_STATE_HOME..."` after `%s`. The implementer correctly used `TargetRecourse: "Drive a target"` / `"Review a target"` (no suffix) to avoid a duplicated phrase, matching `_mill/discussion.md`'s own byte-identical-message intent, and this is pinned verbatim by `TestWireModule_DescriptorIsVerbatim` in both packages plus `cliwire_test.go`'s fixtures.
**Fix:** None needed for correctness — the shipped messages are byte-identical to the pre-existing ones and are test-pinned. Note only for the plan record: card 6/7's literal quoted `TargetRecourse` values should be corrected to drop the redundant "outside the state home" suffix so the card text matches what a literal implementation must actually contain.

## Verdict

APPROVE
Implementation matches the plan, invariants, and shared decisions end-to-end; only a harmless plan-text/code wording mismatch noted.
MILL_REVIEW_END
