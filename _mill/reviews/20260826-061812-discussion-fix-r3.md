# Discussion fix report — round 3

Source review: `_mill/reviews/20260826-061700-discussion-review-r3.md` (APPROVE, 0 blocking, 3 nits).

## Fixed

- `[NIT:scope]` "Stale-assertion inventory covers defect 1 only" — added a second inventory, "Stale-assertion inventory — defect 2 (run-directory clearing)", with its own stated enumeration method and a per-site disposition for all four `internal/shedadapters/bouncer.go` sites the reviewer named, plus `contracts/recipes/loom-recipe.yaml:178-181`. Verified `shedengine.episodeStuckCount` returns at the first `Done` entry authored by the producer, confirming `NewBouncer`'s "its episode therefore never resets" becomes false. Extended the Scope In bullet for `bouncer.go` to name the doc comments.
- `[NIT:consistency]` "Scope's yaml bullet excludes a yaml edit the task makes" — the `loom-recipe.yaml` In bullet now reads "the sites the two inventories below name, including `Plan-Revalidate`'s `on_stuck` comment", and `loom-recipe.yaml:178-181` is a Rewrite row in the defect-2 inventory.
- `[NIT:scope]` "Cross-invocation fire case has no stated disposition" — the `clearing-trigger-is-the-approved-verdict-already-on-disk` Decision now states the cross-invocation fire explicitly as intended, distinguishes it from a resume (which restarts at the persisted `CurrentProducer` and never re-enters from the top), and Testing gains a row asserting it. Added a Q&A log entry.

## Pushed Back

(none)
