# Fixer report: plan-fix round 4

Source review: `_mill/reviews/20260912-115419-plan-review-r4.md` (holistic, REQUEST_CHANGES, 2 BLOCKING).

## Fixed

- r4 BLOCKING:scope — "New mandatory `selfreport` key silently poisons the existing smoke suite with real GitHub filings".
  Confirmed on every link of the chain.
  `fastDeadlineLoomConfig` (`internal/loomcli/smoke_test.go`) builds from `loomengine.ConfigTemplate()` with a single `strings.Replace`, so it inherits a new template key automatically.
  That file's own header states every verb in the suite goes through the real built `cmd/lyx` binary as a genuine subprocess, never `RunCLI` in-process, because the bootstrap spawns its driver via `os.Executable()` — so `selfreportengine.NewGitHubClient` cannot be swapped there.
  The header also states the suite's runs bounce Discussion-Write to the default bounce budget and then block, which is the bounce-budget-exhausted trigger verbatim.
  With warn-and-continue, a filed issue would not fail the test, so the failure mode is silent upstream issue spam on any machine with a resolvable token.
  Card 9 now edits `internal/loomcli/smoke_test.go` to add a second `strings.Replace` turning `selfreport: true` into `selfreport: false`, with a doc-comment note that it is the third member of the existing providerless/fast-deadline override pair and why it is load-bearing.
  Placed in card 9 rather than a new earlier card deliberately: card 9 is the card that first makes filing possible, so landing the override in that same commit leaves no window in either direction.
  Added a new overview Shared Decision, `no-test-fixture-may-file-a-real-issue`, so the rule binds any future test that boots the real binary rather than only this one suite.
  Checked the rest of the repo for the same exposure: `tools/sandbox/SANDBOX-CORE-SUITE.md` hand-writes a status.json fixture and never runs `lyx loom run`/`drive`;
  `internal/hubforge/seed_test.go` seeds a loom config but drives nothing;
  `internal/loomcli/wiring_test.go` writes the template verbatim but never calls `shed.Run`.
  The smoke suite is the only site.

- r4 BLOCKING:scope — "Card 4 omits updating four pre-existing hand-typed config fixtures that the new mandatory key breaks".
  Confirmed: `TestLoadConfig_DiscussionInteractiveTrue` and the three `TestLoadConfig_Malformed*Spec` tests each hand-type a full seven-key block through `seedLoomConfig`, none carrying `selfreport`, so all four would fail `MissingKeys` before `LoadConfig` reached `yaml.Unmarshal` or `modelspec.Parse` — breaking batch 1's own `verify:`.
  Card 4 now repairs all four in the same card, and takes the reviewer's second option rather than the first: switch them to the package's existing `writeLoomConfigWithKey` helper instead of hand-adding a `selfreport:` line to each block.
  That helper builds off `ConfigTemplate()` and its own doc comment says it exists precisely so a fixture never drifts from the template's key set, which is the drift that caused this;
  switching makes the four immune to the next template key too, where hand-adding a line would leave the same trap armed for the next task.
  Card 4's own new `selfreport: false` case now uses the same helper.

## Pushed Back

_(none — both findings verified accurate against the cited files and were fixed)_

## Follow-on from the fixes above (validator, not reviewer)

Adding `internal/loomcli/smoke_test.go` to card 9 tripped `verify-excludes-edited-tagged-test`: the batch edited a `smoke`-tagged file whose `verify:` had no matching `-tags` flag, so an untagged run would not have compiled the edited file at all.
Applied the fix-table's no-existing-`-tags` remedy as a ` && `-chained invocation rather than by widening the existing untagged run, and scoped it with `-run` to the two tests that drive the machine to a halt through `fastDeadlineLoomConfig` — the only place a filing attempt could fire — instead of the whole thirteen-test suite, which builds the `cmd/lyx` binary and spawns real tmux on every implementer and fixer round.
Both scoped tests skip when tmux is absent from `PATH`, so the check is real where the substrate exists and free where it is not.
Updated the command identically in the overview's Batch Index and in the batch file's own frontmatter, and recorded the scope justification in the batch's `## Batch Tests` section.
