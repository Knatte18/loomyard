# Plan fix — round 1

## Fixed

- `20260826-180506-plan-review-r1.md` — "[BLOCKING:scope] validate_test.go's hand-built Card fixtures desync from TargetGroups".
  Batch 2's `## Batch Scope` gains a batch-local decision requiring every hand-built `Card` fixture to set `Type`, `Targets`, and `TargetGroups` together.
  Card 4's `Requirements:` now mandates a `cardOfType(number, slug, typ, refs)` helper plus a sweep of every existing `validate_test.go` subtest that reassigns `card.Type`/`card.Targets`/`card.Pairs` on a `validCard`-derived baseline, naming the five affected test sites the reviewer traced, and explicitly exempting the subtests that mutate only `Uses`/`Intent`/`ImpactSummary`/`Commit`/`RetiredLabels`.
  Cards 5, 6, and 7 each gain a sentence requiring their new fixtures to be built through that helper (or, for multi-group fixtures, constructed with all three fields set at once) rather than by post-construction mutation.

## Pushed Back

_(none)_
