# Plan-fix report — round 1

Source review: `_mill/reviews/20260630-050724-plan-review-r1.md` (verdict: APPROVE, 3 NITs)

## Fixed

- [NIT] "Stale `path:` seed lines left in board test fixtures" — Card 15 now instructs stripping the `path:` seed line from `cli_test.go`'s `seedCwd` (~line 40) and from `TestLoadConfig_HappyPath`, and fixing any "all template keys" comment. Closes the hardcoded-fixture-drift the Path Invariant warns about.
- [NIT] "HappyPath test risks losing home/sidebar/proposal coverage" — Card 15 now says `TestLoadConfig_HappyPath` (~line 36) is **repurposed** (remove only the `path:` seed line + path assertion, keep home/sidebar/proposal_prefix + env coverage), not deleted; only the pure path-resolution cases (absolute, `../custom_board`, `${env:TEST_BOARD_PATH}`) are deleted.
- [NIT] "LoadConfig godoc becomes false after dropping resolution" — Card 13 now (a) sets the `Path` field tag to `yaml:"-"` (the real exclusion, since yaml.v3 lowercases field names) with the corrected rationale (Card 14's cli-overwrite is the functional guarantee), and (b) updates the `LoadConfig` function godoc ("Preserves relative-Path resolution …", ~lines 53–56) to state it no longer resolves a data-dir path.

## Pushed Back

(none)
