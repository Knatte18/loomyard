# Plan fix — round 2

Source review: `_mill/reviews/20260809-092528-plan-review-r2.md`

## Fixed

- `[BLOCKING:scope]` "finalize.md:9's Builder-escalation sentence is never swept" — verified: the merge-conflict paragraph in `manifest/designs/finalize.md` reads "escalating the same way Builder escalates a stuck batch to a fresh higher-capability model", and `manifest/designs/shed-followups.md` assigns that site to this task.
  Card 9's Requirements now include rewriting the clause on its own terms while keeping the existing pointer to `internal/websterengine`'s package documentation.
- `[BLOCKING:scope]` "plugins/prowler/scripts/run.sh:111 is an unenumerated bare-word hit" — verified: `run.sh` carries two distinct ordinary-English hits, "a builder that died" (already enumerated) and "concurrent builders'" (not).
  Added "concurrent builders'" to card 18's enumerated exclusions, added `plugins/prowler/scripts/run.sh` to the card's `Context:`, and recorded both plan-time additions to the discussion's derived exclusion list explicitly, per that list's own provenance rule.
- `[NIT:consistency]` "Card 18 exempts fabric-unified-view.md from editing but not from its own zero-hit grep" — verified: the file carries two bare-word hits and was named only in the confirm-these-checks paragraph, not in the exclusion clause.
  `manifest/designs/fabric-unified-view.md` is now a named path exclusion alongside the dated records, on the stated grounds that it is a record of an already-completed migration whose `BuilderDir` mention names a symbol absent from the tree.

## Pushed Back

_None._

## Coverage re-check

After applying the three fixes, the bare-word scan was re-run against the current tree with every enumerated token and path exclusion applied.
Every file it still reports is already an `Edits:` target of some card in this plan, and no file outside the plan's coverage remains.
