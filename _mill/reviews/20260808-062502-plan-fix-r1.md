# Plan fix — round 1

Source review: `_mill/reviews/20260808-062202-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 1 NIT).

## Fixed

- r1 holistic, "[BLOCKING] Card 1 Context missing the file it renames and surgically edits" — added `internal/scoutengine/leaf_enforcement_test.go` to card 1's `Context:`.
  Accurate: card 1's Requirements names `allowedImports`, `TestLeafInvariant_AllowlistOnly`, the `firstSegment`/`isStdlib` block, the catch-all line, and the exact current `t.Errorf` string, none of which the implementer can resolve without reading that file;
  the `Moves:` source declaration alone does not put it on the card's read allowlist.
- r1 holistic, "[BLOCKING] Card 3 Context missing the file its non-vacuity proof must edit" — added `internal/scoutengine/seam_enforcement_test.go` to card 3's `Context:`.
  Accurate: the non-vacuity proof temporarily edits card 1's output, and that path appeared in none of card 3's five reference fields.
- r1 holistic, "[NIT] Batch-1 scope rationale doesn't match the two-commit card structure" — reworded the `## Batch Scope` rationale.
  Accurate: cards 1 and 2 each carry their own `Commit:`, so the heuristic-in-neither-file state does exist in git history between them.
  The rewrite states the real reason the cards share a batch (card 3 cannot prove either guard red until both exist, and a batch split would put a verify gate and a review round inside one indivisible move) and records the transient state as harmless, since nothing in the repo references the heuristic and card 1's commit compiles and passes without it.

## Pushed Back

_None._
