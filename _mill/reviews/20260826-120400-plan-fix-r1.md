# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260826-115832-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING, 0 NIT).

## Fixed

- r1 holistic, "shape_test.go's testEnv() never gets ApprovePlan, breaks batch 6's own verify" — VERIFIED accurate against `internal/loomrecipe/shape_test.go:88-127`, whose `Env` literal carries `CommitPlan` and no `ApprovePlan`, and against `internal/shedrecipe/entries_bouncer.go`'s `requireSeam` guard, which the plan's own card 15 wires to the `plan` case.
  Added card 31 to batch 6 filling `testEnv`'s `Env` literal with a non-nil no-op `ApprovePlan` closure, landing in the same batch that sets `approve_seam: plan`.
  Raised the batch's `cards:` count to 6, and recorded in both the Batch Scope and the Batch Tests sections that this package carries two full `Env` builders rather than one.
  Confirmed by grep that `shape_test.go:88` and `fixture_test.go:578` are the only two full `shedrecipe.Env` literals in the package;
  the `loomcli` occurrences are partial `loomCLI{env: ...}` literals that never reach `New`.
- r1 holistic, "Card 6's six named helpers live in files absent from its Context" — VERIFIED accurate: `testBouncerConfig` is declared in `internal/shedadapters/bouncer_seed_test.go:41` and the other five in `internal/shedadapters/bouncer_judge_test.go` (`:38`, `:67`, `:71`, `:75`, `:81`), neither file being in card 6's `Context:`.
  Added both files to card 6's `Context:` and stated in its `Requirements:` which file declares which helper, and that both are read-only.

## Consequential renumbering

Adding a card to batch 6 tripped the `card-numbering` validator check, which requires sequential numbering within a batch.
The new batch-6 card took number 26, and batch 7's five cards moved from 26-30 to 27-31, including their `Commit:` prefixes and the three in-prose card references in batch 7's Batch Tests section.
No card's content changed in that move.

## Pushed Back

_None._
Both findings were factually accurate and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task's scope.
