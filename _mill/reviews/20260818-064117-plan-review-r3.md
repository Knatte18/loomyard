MILL_REVIEW_BEGIN
# Review: lift the orchestrator preflight out of loomengine, plus the shared standalone-CLI foundations — holistic

```yaml
duration_s: 726.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (model ID claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-18
```

## Findings

### [NIT:scope] `preflight.Wired`'s true-case branch is never asserted
**Location:** batch 3, card 13
**Issue:** Card 13's required scenarios exercise `Wired` only at the negative `<hub>/_board` case (the "`HubPresent` true / `Wired` false" assertion). No listed scenario asserts `Wired(cwd) == true` against an ordinary healthy paired worktree, so this new exported predicate — the hub-mode trigger T7/T8 will consume — ships with zero direct positive-path coverage; its "true" branch is only exercised indirectly, through `fabricengine.Ready` being called inside `Check`/`CheckResolved`.
**Fix:** Add one scenario asserting `Wired(cwd) == true` against `setupPreflightFixture`'s ordinary healthy pair, alongside the existing `Check`/`CheckResolved` assertions in the same test file.

## Verdict

REQUEST_CHANGES
One minor test-coverage gap on a new exported predicate; every other claim, signature, decision, and cross-file dependency checked against source is exact and internally consistent.
MILL_REVIEW_END
