# Plan-fix report — round 1

Source review: `20260622-175757-plan-review-r1.md` (holistic, APPROVE + 2 NITs).

## Fixed

- `20260622-175757-plan-review-r1.md` — "[NIT] spawn.go doc comment lines 20-22 left stale after rewire": added a Card 3 requirement to refresh the `Spawn` doc comment's numbered step list to name `vscode.PickColor` / `vscode.WriteConfig` instead of the deleted `pickColor` / `writeVSCodeConfig`.
- `20260622-175757-plan-review-r1.md` — "[NIT] cli.go package doc retains baked-value claim about palette/settings": added a Card 3 requirement to remove the "Mill values (palette, settings keys, cmd /c code) are baked" sentence from `ide`'s package doc and relocate it into the `// Package vscode ...` doc on `internal/vscode/config.go`; mirrored the relocation note into Card 1's package-doc requirement for self-consistency.

## Pushed Back

(none)
