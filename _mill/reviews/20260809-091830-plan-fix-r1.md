# Plan fix — round 1

Source review: `_mill/reviews/20260809-091541-plan-review-r1.md`

## Fixed

- `[BLOCKING:design]` "Card 4's scoutcli fixture rename creates an unaddressed path collision" — verified against `internal/scoutcli/cli_test.go`: `crossPackage` is `/repo/internal/websterengine/poll.go`, byte-identical to the renamed `inScope1` the card prescribed.
  Card 4's Requirements now rename `crossPackage` to `/repo/internal/perchengine/identity.go` in the same edit, and name `TestFilterWithin` explicitly.
- `[NIT:consistency]` "Batch 2 `verify:` diverges from the overview's DAG" — the mismatch was real and mine: I edited `02-comment-sweep.md`'s `verify:` during the pre-commit validator pass without mirroring it into `00-overview.md`'s batch index.
  The two strings are now byte-identical.
  See Pushed Back for the remedy divergence.

## Pushed Back

- `[NIT:consistency]` "Batch 2 `verify:` diverges from the overview's DAG" — **remedy only, not the finding**.
  The prescribed fix ("the integration clause … looks like a stray copy-paste … and should be dropped") rests on the stated premise that the batch "touches no integration-tagged test file".
  That premise is factually wrong: `internal/websterengine/beginbatch_test.go`, `internal/websterengine/recoverbatch_test.go` and `internal/webstercli/verbs_test.go` all carry the `integration` build tag, and `_plan_validate`'s `verify-excludes-edited-tagged-test` check emitted three findings against this exact batch until the clause was added.
  Dropping it would re-break the validator gate, so the divergence was resolved in the other direction — the overview now mirrors the batch file — and `02-comment-sweep.md`'s `## Batch Tests` section gained a sentence naming the three tagged files and why the tagged run is required.
