# Plan fix — round 5

Source review: `_mill/reviews/20260805-170544-plan-review-r5.md` (holistic, REQUEST_CHANGES, 5 BLOCKING + 2 NIT, reviewer `fablehigh` — self-review round: the fixer is the reviewer, so every finding was re-verified against the tree before being applied). All seven findings held under the receiving-review decision tree (accurate, no harm category); all seven fixed, none pushed back.

The fix pass surfaced a defect one level deeper than the review's own findings: cards 31-33 prescribed rewriting deleted-method call sites onto **unexported** `fabricengine` functions (`hostLyxLinkHere(l)`, `weftWorktreePath(l, slug)`, portal/launcher constructors), but roughly 20 of `fabricengine`'s ~50 test files are `package fabricengine_test` — external test packages where an unexported identifier does not compile — and most of the affected call sites live in exactly those files. The review's findings 1-3 (unlisted caller files) were therefore fixed together with their root cause: each relocated constructor with an external-package caller is now declared **exported**, with the caller enumerated in the card that relocates it, under the batch scope's own "private unless an in-scope caller needs otherwise" rule.

## Fixed

- "[BLOCKING] Card 31 privatizes Weft* with four caller files unlisted". Re-verified every site: `hook_test.go:233` (in-package, was in no batch-6 card), `junction_repoint_test.go:53,155`, `junction_pattern_integration_test.go:74,386,470`, `unwire_test.go:104` (all `package fabricengine_test`). Added all four to card 31's `Edits:` with each rewrite named. Because the last three are external-package files, `WeftWorktreePath` and `WeftLyxDirFor` are now re-declared **exported** (with `WeftHostSlug`, whose caller `branchname_test.go` is also external — resolving the card's previous keep-exported-only-if conditional to a definite answer). The `:386` Lyx-row `targetFor` goes onto card 30's exported `WeftLyxDir(l)`.
- "[BLOCKING] Card 31's 'no external fallout' claim is false — configcli test calls WeftWorktreePath". Re-verified `configcli/configcli_integration_test.go:111`. The claim is corrected to "production call sites are all inside fabricengine, test callers are not", the file moved from card 30's `Edits:` (where it had no covered call — its only weft call is the slug form card 30's accessors deliberately omit) into card 31's, and the rewrite named: `fabricengine.WeftWorktreePath(f.Layout, slug)`, with the note that the test already imports `fabriccli` so the `fabricengine` import adds no cycle risk. Card 30's "three out-of-package test files" sentence rewritten to name the two real adopters (`audit_test.go:46,96,205`, `preflight_integration_test.go:318`) and to state explicitly which card owns each remaining out-of-package weft/junction/board call (31, 32, 34).
- "[BLOCKING] Card 32 deletes HostLyxLink with two caller files uncovered". Re-verified `add_rollback_adopt_test.go:142,211` and `loomengine/preflight_integration_test.go:414`. Both files added to card 32's `Edits:`; all `HostLyxLink(slug)` callers now enumerated in the card with the rewrite named (`fabricengine.HostLyxLink(l, slug)` / `fabricengine.HostLyxLink(f.Layout, slug)`). The four relocated constructors plus `HostJunction` are now exported — the six `HostLyxLinkHere` caller files and `config_driven_junctions_integration_test.go:52`'s `HostJunctions` call are all external-package, so the old unexported prescription could not compile at all. The `:419` `HostPatternLink` sibling row is explicitly left to card 35.
- "[BLOCKING] Card 34's BoardDir move misses five caller files in batch 6's verify scope". Re-verified all five: `buildercli/weft_integration_test.go:43,58`, `webstercli/weft_integration_test.go:35,55`, `perchcli/run_integration_test.go:30,44`, `loomengine/preflight_integration_test.go:59`, `fabricengine/config_driven_junctions_integration_test.go:107`. All five added to card 34's `Edits:` with the `fabricengine.BoardDir` qualifier retarget named and the no-cycle note (none of these packages is imported by `fabricengine`).
- "[BLOCKING] Card 18's completion grep is unsatisfiable and misses topology.go". Re-verified: `\bLayout\b` matches the retained `PairedFixture.Layout` and deps-struct fields; `fabricengine/topology.go:4,13` names `hubgeometry` in comments and was in no card. `topology.go` added to card 18's `Edits:` and its sweep list (23 → 24 files, with the concrete rewording named); the verification grep narrowed to `'internal/hubgeometry\|hubgeometry\.'` with the `Layout` half dropped and the reason stated; the expected residue enumerated as exactly six files whose corrections later cards own by design (`boardengine/config.go`, `boardengine/template_test.go`, `fabricengine/doc.go` → card 34; `websterengine/report.go` → card 22; `pattern/doc.go` → card 25; `logger/logger.go` → card 26); card 41 gains the zero-residue version of the same grep as the plan's closing check. The residue set was computed programmatically (narrowed grep over the real tree minus the union of batches 1-4's file lists), which is how `template_test.go:25` and two card-12 gaps below were found.
- "[NIT] 'WeftRaddleDir has zero callers anywhere' is false". Re-verified `weft_test.go:114-116` and the `wantWeftRaddleDir` column (field decl `:28`, value rows `:43,58,73` — the review cited the neighbouring `wantWeftRepoRoot` rows; corrected in the applied text). Card 31 now says zero **production** callers, names the sub-test and column, and states they are deleted in the lift rather than rewritten.
- "[NIT] Card 11 instructs godoc edits in files it does not list". Re-verified: `logger/logger.go` and `pattern/doc.go` are absent from card 11's `Edits:`. Both dropped from card 11's correction sentence with an explicit ownership pointer (cards 26 and 25 relocate the constructors those comments describe); card 26 already named `logger.go:48,409`, and card 25 — which had `pattern/doc.go` in `Edits:` with no instruction for it — now names the `:13` and `:49` corrections.

## Found while fixing, not in the review

- **Cards 31-33's unexported-rewrite prescriptions could not compile.** The root-cause defect described in the preamble: ~20 `fabricengine` test files are `package fabricengine_test`, and the callers cards 31-32 themselves enumerate (e.g. card 32's "become `hostLyxLinkHere(hostLayout)`" for six external-package call sites) are unreachable from an external package. Fixed by exporting exactly the constructors with external-package callers — `WeftWorktreePath`, `WeftLyxDirFor`, `WeftHostSlug` (card 31); `HostJunction`, `HostLyxLink`, `HostLyxLinkHere`, `HostJunctions`, `HostJunctionsHere` (card 32); `PortalsDir`, `PortalLink`, `LauncherDir` (card 33) — each with its forcing caller cited in the card. Batch 6's scope paragraph and Batch Tests, card 13, card 30 and card 43 all carried a false "25 in-package / 19 need unexported access" count supporting the old prescription; all corrected to the real ~30-in-package / ~20-external split.
- **Card 33 deleted the portal/launcher surface without listing `lyxcwd_test.go`.** The module's own tests hold the `PortalsDir`/`PortalTarget`/`LaunchersDir`/`LauncherDir` inline assertions at `:134-155` and the `PortalLink`/`LauncherDir`/`MenuLauncherPath`/`LauncherSpawnRel`/`MenuLauncherRel` sub-tests at `:223-499`, so package `lyxcwd` stops compiling the moment card 33 lands — the same defect class cards 31/32 already handled for their surfaces. `lyxcwd_test.go` added to card 33's `Edits:`, the sub-tests lifted into a new `internal/fabricengine/portallauncher_test.go` (`package fabricengine`, untagged), added to `Creates:` and the overview.
- **Card 3's `Prime` deletion missed `TestPreflight_EmptyPrime`'s injection mechanism.** `preflight_integration_test.go:223` sets `l.Prime = ""` on a copied Layout to simulate the no-prime case; the field's deletion kills that mechanism and the file was listed with no instruction for it. Card 3 now instructs deleting the sub-test (`:217-230`) with the field and pins the behavioural contract the rewrite must preserve: a `fabricengine.PrimeName` failure maps to the same `CheckGeometry` failure `Report`, never a hard error.
- **Card 12 named files it did not list, including one with nothing to correct.** `scoutengine/doc.go` and `scoutengine/daemonstate.go` were named in card 12's requirements but absent from its `Edits:` (the `daemonstate.go:7` comment would otherwise have survived every batch); both added. `perchengine/doc.go` and `burlerengine/doc.go` were named for godoc corrections but contain no `hubgeometry`/`Layout` reference at all — the sentence now says so instead of sending the implementer hunting. `scoutengine/doc.go:25`'s import-ceiling enumeration ("stdlib, internal/hubgeometry") is corrected in the same card, since card 12 is where the package's import set reaches its final batch-3 shape.
- **`modelspec/modelspec.go:34`'s import-ceiling godoc goes stale in card 2 and no card corrected it.** After card 2 `modelspec` imports `configengine`, not `hubgeometry`; the enumeration correction is folded into card 2, the card that changes the import set.
- **`fabricengine/doc.go:110` and `lyxcwd.go:76` kept pre-rename resolver naming past card 7.** Card 7 already rewrites the `:110` marker sentence; it now also renames `hubgeometry.Resolve`/the anchor-file path/`RelPath` to their `lyxcwd` forms (the `BoardDir` ownership wording still deferred to card 34), and `internal/lyxcwd/lyxcwd.go` is added to card 7's `Edits:` for the `resolveCore` doc comment (pre-rename `hubgeometry.go:76`) that names `.fabric-anchor`.
- **Stale lowercase cross-references.** Batch 7's scope and card 37 said `hostJunctions`; batch 6's external-interface line said "now-private junction primitives". All updated to the exported names.

## Pushed Back

None. All seven findings re-verified against the source and held; no fix hit a harm category. The export decision in the found-while-fixing root cause is sanctioned by the batch scope's own "private unless an in-scope caller needs otherwise" rule, so it contradicts no documented design decision — the illusion boundary remains a review obligation on non-fabric production callers.

## Validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` → `DAG OK: 8 batches`. `_plan_validate.run` (max_cards_per_batch=20, max_batch_context_tokens=700000, parent_branch=main, wiki_root=git_root=repo) → `findings: 0` (one intermediate run flagged a context-completeness hit on card 7's mention of the pre-rename anchor path in prose; reworded, re-run clean). All Files Touched regenerated programmatically from the batch files (`_parse_edits_only` ∪ `Creates:` union ∪ move targets): **285 paths**, and the overview's listed set matches the union exactly (`missing: []`, `extra: []`) — 283 from round 4 plus the two files this round added (`internal/fabricengine/topology.go`, `internal/fabricengine/portallauncher_test.go`).
