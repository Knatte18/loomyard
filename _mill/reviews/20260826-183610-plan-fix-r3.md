# Plan fix — round 3

## Fixed

- `20260826-183436-plan-review-r3.md` — "[BLOCKING:scope] run.go's own file-header comment and `Long` help text left stale": verified `internal/loomcli/run.go` lines 2-3 say `run` "seeds the status file when absent, commits that seed into the fabric", and `runCmd`'s `Long` step 1 (lines 52-53) repeats it as "seed the status file when it is absent, and commit that seed into the fabric". Card 4 now rewrites both alongside the step-3 comment block, and cites the CLI/Cobra Invariant as why the user-facing `Long` text is part of this card rather than a follow-up.
- `20260826-183436-plan-review-r3.md` — "[BLOCKING:scope] Card 7's doc-comment list misidentifies which accessor still calls the file durable": verified `LoomRunLock`'s comment carries no durability claim (it names `LoomStatusFile` only in an anchoring analogy that stays true after the move), while `LoomDriverLog`'s and `LoomBootstrapLock`'s each carry the identical "mirrored subpath of the durable status file" sentence. Card 7's list now names `LoomDriverLog` in `LoomRunLock`'s place, quotes the shared sentence, and states explicitly that `LoomRunLock`'s comment needs no edit.
- `20260826-183436-plan-review-r3.md` — "[NIT:consistency] Card 6's \"three other callers\" claim for `weftCommitCount` is wrong": verified `weftCommitCount` has four call sites, two of which are inside the test card 6 itself rewrites, leaving one other caller with two call sites. Corrected the count.
- `20260826-183436-plan-review-r3.md` — "[NIT:scope] Newly-migrated `LoomStatusFile` not added to the subpath-anchored two-roots regression map": verified `dotLyxConstructors` at `cmd/lyx/constructoranchoring_test.go:169` lists every other `.lyx` accessor and is documented as the two-roots regression guard. Card 1 now adds a `LoomStatusFile` entry to that map, notes it is defence in depth rather than a live gap, and — per the finding's own optional half — also adds the already-missing `LoomRunLock` to the header's "in full" enumeration, since that sentence is being edited anyway.

## Pushed Back

_None. All four findings were factually accurate against source, and no fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task's scope._
