# Plan fix report — round 1 (APPROVE with NITs)

Source review: `_mill/reviews/20260620-183017-plan-review-r1.md` (verdict APPROVE, 3 NITs).

## Fixed

- `20260620-183017-plan-review-r1.md` — [NIT] "lyxtest.go risks tripping the paths enforcement guard": added an explicit enforcement-guard constraint to batch 1 cards 2 and 3 — `lyxtest.go` must never contain `os.Getwd` or `--show-toplevel`; `paths.Resolve(hub)` is the only geometry primitive.
- `20260620-183017-plan-review-r1.md` — [NIT] "host-hub template pushes `main`, diverging from addRemote semantics": changed batch 1 card 2 so the host-hub (a) and weft-prime (b) bares are left **empty** (matching existing `addRemote`); only the weft-only template (c) runs `git push -u origin main` (upstream tracking is required only there for `Pull`/`hasUnpushed`). Updated the overview "Decision: template-once" and card 3 to match.
- `20260620-183017-plan-review-r1.md` — [NIT] "'All Files Touched' omits the three deletions": added a separate `## Deleted` subsection to the overview listing `internal/paths/helpers_test.go`, `internal/worktree/helpers_test.go`, `internal/worktree/testhelpers_test.go` (kept out of the Creates/Edits union so the `all-files-touched-mismatch` validator stays green).

## Pushed Back

(none — all three NITs were accurate and fixed per the operator's "do everything recommended" instruction.)
