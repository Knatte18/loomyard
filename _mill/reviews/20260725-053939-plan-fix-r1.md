# Plan fix — round 1

Source review: `_mill/reviews/20260725-053752-plan-review-r1.md` (APPROVE, 2 NITs).

## Fixed

- r1 "[NIT] gitrepo.go file-header primitive list omits StageAllAndCommit" —
  Card 1 (batch 01) now requires adding `StageAllAndCommit` to `gitrepo.go`'s
  top-of-file comment enumeration in the same edit.
- r1 "[NIT] Stale gitrepo doc references to board's pushUnpushed/hasUnpushed" —
  Card 2 (batch 01) now also edits `internal/gitrepo/push.go` and requires
  rewording the dangling cross-references to board's soon-deleted
  `pushUnpushed`/`hasUnpushed` in both `push.go` (the `rebaseRetryTriggers` and
  `PushCoalesced` comments) and `doc.go`'s Push-surface section (comments only,
  no code/value change). `internal/gitrepo/push.go` was added to the overview's
  All Files Touched list accordingly. Full plan validator re-run clean.

## Pushed Back

_None — both NITs were factually accurate doc-staleness findings; fixed under
the receiving-review default._
