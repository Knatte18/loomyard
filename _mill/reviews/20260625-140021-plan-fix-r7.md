# Plan fixer report — round 7

Source review: `_mill/reviews/20260625-135844-plan-review-r7.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 2 NITs).

## Fixed

- `20260625-135844-plan-review-r7.md` — [BLOCKING] "Card 12 misses dormant/adopt updates in weftwiring_test.go" — added `internal/warp/weftwiring_test.go` to card 12's `Edits` with a requirement to retarget the junction-after-`Add` tests (`TestWeftSpawnCreatesJunction`, `TestWeftSpawnSeedsExclude`, `TestSeederParity`) to the `WireJunctions` primitive (Add is now dormant), change `TestWeftPrechecks`/`RejectExistingWeftBranch` from abort→adopt, and drop the host-junction-removal expectation from `TestWeftRollbackOnPostHostCreateFailure`.
- `20260625-135844-plan-review-r7.md` — [BLOCKING] "rollbackAdd no longer references removeHostJunction — test churn" — card 12 now states `removeHostJunction` is **retained** (still used by `Remove`/teardown in `remove.go:89`, so no unused-symbol compile error) and only dropped from the create-rollback path; the rollback test expectation is updated accordingly.
- `20260625-135844-plan-review-r7.md` — [NIT] "Card 9 ConfigTemplate qualifier across packages" — card 9 now states the intra-card ordering: create `internal/warp/template.go` (exported `ConfigTemplate`) before editing `internal/configreg/configreg.go`.
- `20260625-135844-plan-review-r7.md` — [NIT] "TestRunDispatchesToWorktree name left stale" — card 10 now renames `TestRunDispatchesToWorktree` → `TestRunDispatchesToWarp` alongside retargeting its `{"worktree","list"}` → `{"warp","list"}` invocation.

## Pushed Back

(none — all four findings were factually accurate with no harm in fixing; applied under the default fix-everything rule.)
