# Discussion fix — round 3

Source review: `20260826-174107-discussion-review-r3.md` (APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- `20260826-174107-discussion-review-r3.md` — "[NIT:scope] loomengine's own path tests not dispositioned" (demoted from BLOCKING): verified `internal/loomengine/loomstatus_test.go` pins `LoomStatusFile` under `lyxdirs.LyxDirName` at lines 26 and 65, and `internal/loomengine/config_test.go` carries `TestLoomStatusRel` (196) and `TestLoomStatusFile_EqualsAnchorPathJoinedWithLoomStatusRel` (206). Added a Tier-1 subsection giving each an explicit disposition (moved assertion vs deleted-with-the-function) and noting these fail at runtime rather than compile time; corrected the "two guard tests" claim in Technical context to name all three guard files.
- `20260826-174107-discussion-review-r3.md` — "[NIT:consistency] Migration rationale contradicts the conflict mechanics" (demoted from BLOCKING): the rationale's "would keep conflicting exactly as it does today" premise was false — once nothing writes the path, no branch rewrites it and every future task stays byte-identical to its merge base there. Restated on true grounds (dead tracked junk on the parent) and kept the in-flight-run gate, which is sound because a branch that diverged *before* the upgrade carries a real divergence.
- `20260826-174107-discussion-review-r3.md` — "[NIT:consistency] Wording pass misses the `loomDirName` doc comment": verified `internal/loomengine/config.go:29–31` says `loomDirName` joins onto `LyxDirName` or `DotLyxDirName`, reachable by neither stated grep. Added it to the second enumeration pass's known-hit list, plus a sentence confirming that `_lyx/loom/` ceasing to exist satisfies the Durable-vs-Ephemeral mirrored-subpath rule vacuously rather than violating it.

## Pushed Back

None. All three findings verified accurate against the cited code, and none of the fixes breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task's scope.
