# Plan fix — round 6

Source review: `_mill/reviews/20260906-113103-plan-review-r6.md`

## Fixed

- r6 "[BLOCKING:scope] format-5 bump's fixture blast radius is unenumerated" — confirmed and larger than the finding listed: `grep -rn "format: 4"` finds twelve files, including three inside `internal/planparser` the finding did not name. Card 8 now sweeps every one of them in the same card as the spec rewrite, with an instruction to re-run the grep rather than trust the list, and batch 2's verify widened to the five packages holding them plus a chained `-tags integration` half for `internal/websterengine/runlevel_test.go`. The sweep belongs to batch 2 because card 7's bump is what breaks them and `go build ./...` compiles tests without running them.
- r6 "[BLOCKING:scope] parity fixtures' `language: none` cannot be added where card 25 says" — confirmed: `planFixture` lives in `internal/loomcli/validate_test.go`, not `parity_test.go`. Card 24 now owns that edit (the file is already in its `Edits:`), card 25 owns `planFixtureInvalidFormat` in `parity_test.go`, `parity_test.go` added to card 24's `Edits:` and `validate_test.go` to card 25's `Context:`, and the `NoPlanDirectory` case is explicitly noted as writing no overview.
- r6 "[BLOCKING:scope] loomrecipe's Plan-Validate fixture is neither edited nor verified" — confirmed: `buildSequenceFixture` drives the real `Plan-Validate` row over a bare `t.TempDir()` anchor. `internal/loomrecipe/fixture_test.go` added to card 23's `Edits:` with the `language: none` requirement, `./internal/loomrecipe/` added to batch 5's verify, and the consequence stated — without the key this card's own change turns that row's `Done` into an infrastructure error and breaks every sequencing test depending on it.
- r6 "[BLOCKING:scope] loom.md's Plan-Validate detail and check count go stale" — confirmed at `manifest/designs/loom.md` lines 146 and 163 and `manifest/designs/plan-card-format.md` line 104. Both added to card 25's `Edits:`: the Plan-Validate paragraph restated over `planglyph`, and both pinned seventeen-ID counts replaced with a pointer to the spec's own validation-checks section rather than a number that goes stale on the next check added.
- r6 "[NIT:consistency] card 6 freezes the two union helpers it must change" — confirmed: neither union builder takes a ref; both walk the plan and filter on `isPathRef`. Card 6 no longer claims they keep their bodies — both gain the same `isGlyphRef` branch and key their union on the identical mapped value `satisfied` looks up, with a test pairing a glyph `Create` target against a glyph `Uses` reference.
- r6 "[NIT:consistency] classifier rule 4 silently root-joins bare filenames" — confirmed: `refKindPath` routes through `normalizeRefIfPath`. Card 4 now states the intended behaviour explicitly — a bare filename under a declared `root:` means the file in that root, consistent with every other relative path — and flags it as a behaviour change; card 5's test list covers both the non-`.` and empty-`root:` cases.

## Pushed Back

_None. All six findings verified accurate against the cited source and none met a harm-check escape._

## Note on disposition

This is the final round under `roles.plan-review.holistic.rounds: 6`. The operator gave a live instruction to approve and hand off at the round cap rather than blocking, so this round's fixes are followed by an implicit approve-at-cap instead of step 6's `_status.set_blocked` halt. The six findings above were fixed, not waived; what the waiver skips is a seventh round re-reviewing them.
