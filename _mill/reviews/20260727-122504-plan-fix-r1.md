# Plan fix — round 1

Source review: `_mill/reviews/20260727-122427-plan-review-r1.md` (verdict APPROVE, 0 blocking).

## Fixed

- r1 "[MEDIUM] Batch 1 Card 3 doc-consistency grep for `rotate`/`next model` is under-scoped" — verified accurate: `grep -rln 'rotate\|next model' crucible/` returns ten files, eight of them outside this batch's `Edits:` list (`board-`, `builder-`, `fabric-`, `gitrepo-`, `loom-planner-`, `reed-`, `webster-review-prompt.md`, and `review-prompt-template.md`). Rewrote Batch Tests item 4 to scope the grep to the two edited files only, name the eight out-of-scope files explicitly as not-to-be-touched, and add `orchestrator-prompt.md`'s renamed "Model + effort selection" section (~56) to the legitimate-hit inventory, since card 2 edit 5 deliberately preserves the model-rotation rationale there.
- r1 "[NIT] Overview Decision title undercounts substituted tokens" — verified accurate: the decision body lists four varying points while its heading said three. Retitled to "the five bodies stay byte-identical except four points". No other occurrence needed changing — Card 1's "exactly three places" refers to the three `<effort>` substitutions and separately names the H1 heading, and Batch Tests item 1 already says "the three substituted tokens and the H1 heading".

## Pushed Back

_None._
