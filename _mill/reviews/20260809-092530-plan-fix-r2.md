# Plan fix — round 2

Source review: `_mill/reviews/20260809-092147-plan-review-r2.md`

## Fixed

- r2 `[BLOCKING:scope]` "Card 8 Context omits warpbinding.go despite naming its constant" — added `internal/fabricengine/warpbinding.go` to card 8's `Context:`; confirmed the constant is declared there and nowhere in `internal/fabricengine/clone.go`.
- r2 `[BLOCKING:scope]` "Card 12 needs export_test.go but never lists it" — added `internal/fabricengine/export_test.go` to card 12's `Edits:` and to the overview's All Files Touched; replaced the conditional wording with an unconditional instruction to re-export `warpProbeDirPrefix` as `WarpProbeDirPrefixForTest`, matching that file's existing shim style.
- r2 `[NIT:consistency]` "Card 21 misses an adjacent stale `.fabric-anchor` reference" — verified the stale name at the fabric suite's unwire-scenario Watch line; card 21 now corrects it and adds the binding to the same repo-wide-records list.
- r2 `[NIT:consistency]` "Card 21's Watch claim for S6 doesn't match the file" — verified the core suite's Watch line names no URLs; card 21 now says only the Goal line needs flipping.
- r2 `[NIT:design]` "Batch 4's depends-on: [3] looks unnecessarily conservative" — took the rationale branch the finding offers: added a "the DAG is deliberately linear" Shared Decision naming batch 4 as the clearest case, and stating the reason (one worktree, one branch, no benefit from interleaved commits at this size; each batch verifies against a tree containing every earlier batch, which is what makes the narrow verify scopes safe).

## Pushed Back

_(none)_
