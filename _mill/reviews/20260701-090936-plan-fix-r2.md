# Plan fix — round 2

Source review: `_mill/reviews/20260701-090831-plan-review-r2.md` (verdict
REQUEST_CHANGES, blocking_count 2, nit_count 1).

## Fixed

- **[BLOCKING] Card 3 missing Context for the identifiers its Requirements name**
  (`01-weftengine-commit-message.md`, Card 3) — verified Card 3's Requirements
  reference `DefaultCommitMessage` (in `weft.go`) and Card 1's new `Commit` signature
  (in `sync.go`), neither of which was listed. Added `internal/weftengine/weft.go` and
  `internal/weftengine/sync.go` to Card 3's `Context:`.
- **[BLOCKING] Card 10 missing Context for lyxtest and fslink identifiers its
  Requirements depend on** (`04-initcli-undo.md`, Card 10) — verified Requirements name
  `lyxtest.CopyPaired`/`CopyPairedLocal`'s `WeftBare` distinction
  (`internal/lyxtest/lyxtest.go:511-517`) and `fslink.Remove` for corruption-simulation
  setup, neither of which was listed. Added `internal/lyxtest/lyxtest.go` and
  `internal/fslink/fslink.go` to Card 10's `Context:`.
- **[NIT] Card 10 references a non-existent `initcli.RunCLI`** (`04-initcli-undo.md`,
  Card 10, `TestRunInit_Undo_HappyPath`) — verified `internal/initcli` only exports
  `RunInit`, never `RunCLI`. Reworded the test description to call
  `initcli.RunInit(&buf, []string{"--undo"})` for the undo path (same seam as the
  forward path, dispatching on the flag), dropping the incorrect `RunCLI` reference.

## Pushed Back

None — all three findings were factually accurate and low-harm to fix.
