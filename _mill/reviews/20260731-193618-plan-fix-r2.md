# Plan fix report — round 2

Source review: `_mill/reviews/20260731-193153-plan-review-r2.md` (verdict REQUEST_CHANGES, blocking_count 5, nit_count 2)

## Fixed

- "sinkOnce/headerOnce test state never reset between cases" — extended batch 4's Card 12 (`SetDurableSinkDir`) to be the full sink-state reset point: every call now resets `sinkOnce`, `sinkWriter`, `sinkOK`, `header`, `headerOnce`, the byte counter, and the truncation-marker flag, not just the override directory. Added explicit "calls `SetDurableSinkDir` at its own start, never sharing one call across cases" notes to batch 4's Cards 15/16, batch 5's Cards 21/22, and batch 6's Card 26 (Card 25 never touches the durable sink, so it needed no change).
- "Composite Handle() per-inner-handler gating unspecified" — batch 5's Card 17 now states explicitly that `Handle()` must independently re-check each inner handler's own `Enabled(record.Level)` before delegating to it, never forwarding unconditionally just because the composite's own OR-gated `Enabled` returned true.
- "Shared sinkMu risks self-deadlock across Handle()/writeDurable" — batch 5's Card 20 now specifies the stderr-handler-state read as its own narrow critical section (lock, copy the handler reference, unlock) that must complete before `Handle()` calls either inner handler's `Handle`, so `writeDurable`'s independent lock/unlock (Card 11) can never nest inside an already-held `sinkMu`.
- "Card 38 omits internal/shuttleengine/run.go from Edits" — moved `internal/shuttleengine/run.go` from Context to Edits in batch 11's Card 38 (and to the overview's All Files Touched), and replaced the hedged "audit... if it qualifies" wording with the confirmed concrete site: `saveRunState`'s failure branch at `run.go:152-165` has no `internal/logger` call (its nested `RemoveStrand`-failure diagnostic uses stdlib `log.Printf`, which doesn't satisfy the obligation).
- "CONSTRAINTS.md addition misdescribes perchengine as a spawn site" — batch 14's Card 46 no longer adds `internal/perchengine/engine.go` to the Live-Substrate Spawn Observability call-sites list; added an explicit "do not add" note explaining perch's Card 39 call is a generic error-passthrough Warn, not a spawn/teardown event, since perch never itself starts an OS process.
- "Batch 2 frontmatter card-count mismatch" — corrected `cards: 4` to `cards: 3` in batch 2's frontmatter (it has three cards: 2, 3, 4).
- "SetOutput doc comment's stale claim left uncorrected" — batch 5's Card 19 now explicitly instructs correcting the pre-existing false "production code never calls it" claim (`configureFromEnv` already calls `SetOutput(f)` at `logger.go:93` today) while rewriting the same comment for the stderr-only-rebind note.

## Pushed Back

(none)
</content>
