# Plan fix — round 2

Source review: `_mill/reviews/20260824-184342-plan-review-r2.md`

## Fixed

- `20260824-184342-plan-review-r2.md` — "[NIT:consistency] Stale declared-order claim in planparser/validate.go is left uncorrected": took the reviewer's first offered remedy rather than accepting the omission. Added card 16 to batch 3, a comment-only correction of `internal/planparser/validate.go`'s banner sentence, with an explicit instruction that no function body, check, signature, or `Check:` ID changes. Amended the overview's `no-planparser-and-no-batcher-change` Shared Decision to record the carve-out and its **Documentation Lifecycle** justification, added the file to `## All Files Touched`, extended batch 3's `verify:` with `./internal/planparser/...`, and updated batch 3's scope paragraph and `## Batch Tests` accordingly.
- `20260824-184342-plan-review-r2.md` — "[NIT:consistency] accumulatedCardSHAs' doc comment goes stale after card 4's reorder": added a requirement to batch 2 card 4 correcting that comment's "in plan order" phrase to execution order, noting it is the more correct order for the SHA-bisect it feeds, and pinning the rest of the comment and the function body as untouched.
- `20260824-184342-plan-review-r2.md` — "[NIT:consistency] Redundant ordering clauses survive card 8's template reword": rewrote batch 2 card 8's instruction so the whole three-clause tail is replaced by one clean statement of the ordering rule — retiring "batch N assumes every batch before it is already committed" alongside "there is no DAG here to reorder around" — and extended card 9's template property assertion to check both retired clauses are gone, not just one.

## Pushed Back

_None._
