MILL_REVIEW_BEGIN
# Review: Migrate planparser.Card to Edits/Uses fields — holistic

```yaml
duration_s: 395.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 4.5 (claude-sonnet-5 per system context)
reviewed_file: plan/
date: 2026-08-24
```

## Findings

### [NIT:consistency] Golden fixture's "six paths" undercounts by one
**Demoted-from:** BLOCKING
**Location:** Batch 2, Card 8 (golden fixture) and Card 11 (validator tests).
**Issue:** Both cards say the zero-findings test must materialize "exactly the six paths the fixture's checked entries name." Tracing Card 8's own card-by-card spec, the checked (must-exist) path-shaped entries are: Card2 target `list.go` + Card2 `Uses` `//internal/output/envelope.go`; Card3 (Custom) `Uses` `list.go` (same file as Card2's, dedups); Card4 target `//internal/boardengine/legacyrows.go`; Card5 `Pairs` Old side `//internal/boardengine/rows.go`; Card6 target `//cmd/lyx/helppins.go`; Card7's two targets `doc.go` and `//docs/boardcli-json.md`. That is **seven** distinct physical paths (list.go, envelope.go, legacyrows.go, rows.go, helppins.go, doc.go, boardcli-json.md), not six — `checkPathMissing`'s own spec in Card 5 confirms Uses is checked on every card type including Custom, and Edit/Delete/Move/Prosa targets are all checked.
**Fix:** Correct "six" to "seven" in both Card 8 and Card 11, or drop one of the seven checked entries from the fixture design (and re-verify the deliberately-absent-pair count of two stays accurate).

### [NIT:consistency] Stale "Moves-bearing card" wording left in a test comment Card 12 edits
**Location:** Batch 3, Card 12 (`internal/websterengine/template_test.go`).
**Issue:** `TestRenderForkPrompt_OmitsRenameMechanic`'s doc comment (line 754) reads "regardless of whether the batch has a Moves-bearing card" — `Moves` is a removed field. Card 12's requirements rewrite the test body (`card.Moves = ...` → format-4 `Rename` card) but say nothing about this comment, and "leave every other test in the file untouched" doesn't cover a comment on the very test being edited.
**Fix:** Add one line to Card 12 instructing the doc comment be reworded to "rename-bearing card" (or equivalent) while the test body is touched.

## Verdict

APPROVE
Golden-fixture path count is internally inconsistent (six claimed vs. seven derivable); one small stale-comment nit alongside it.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
