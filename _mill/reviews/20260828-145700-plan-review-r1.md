MILL_REVIEW_BEGIN
# Review: reed: pane reap isn't applied consistently across up/add's mutating paths — holistic

```yaml
duration_s: 512.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: claude-opus-5 (per runtime environment; self-assessment, not independently verifiable)
reviewed_file: plan/
date: 2026-08-28
```

## Findings

### [BLOCKING:design] Card 12 judges files batch 4 owns, without depending on it
**Location:** batch 3 / card 12 (and batch 3 `depends-on: [2]`)
**Issue:** Card 12's sweep spans `internal/reedcli/*.go`, but batch 4 — which rewrites every adoption comment in `smoke_lifecycle_test.go`, `smoke_panecwd_test.go`, `smoke_teardown_test.go` (~15 live `adopt` hits verified) — is a sibling of batch 3, not a dependency, so in numeric order card 12 must report all of them as "missed rewrites".
**Fix:** Make batch 3 depend on batch 4 (or move card 12 into batch 4), or scope card 12's sweep to the files batch 3's own dependencies have landed.

### [BLOCKING:design] Card 9 forbids fixing a comment that does assert the deleted seam
**Location:** batch 3 / card 9, closing sentence
**Issue:** `state.go:41-48` (`ReedState.HeaderPaneID`) says the header "is excluded from every strand accounting, adoption, reconcile, and layout path" — that is pane adoption, the twin of `doc.go:39` which card 8 does rewrite; only `state.go:57` ("is adopted rather than treated as a mismatch") belongs to the generation stamp. Card 9's blanket "do not touch the `ReedState` field comments mentioning 'adoption' and 'adopted'" rests on a false premise, and card 12's survivor rule ("surrounding prose in … `state.go`") would wave the hit through.
**Fix:** Split the instruction: rewrite the `HeaderPaneID` adoption clause, leave only the `PaneGeneration` comment alone.

### [BLOCKING:scope] Card 3's Context carries no file showing `e.tmux.execHook`
**Location:** batch 1 / card 3
**Issue:** Requirements direct the implementer to write an `e.tmux.execHook` fake, but the field is declared in `internal/reedengine/overlay.go` and demonstrated in `lifecycle_test.go`/`spawn_test.go` — none listed; `lock_test.go`'s `newTestEngine` (the one test file in Context) never touches it, and `reconcile_test.go` has no hook example either. Card 7 lists `lifecycle_test.go` for exactly this reason.
**Fix:** Add `internal/reedengine/lifecycle_test.go` (or `overlay.go`) to card 3's `Context:`.

### [BLOCKING:scope] Card 13 under-enumerates the stale text in the foreign-pane test
**Location:** batch 4 / card 13
**Issue:** The card names "the doc comment above the function and the two inline comments", but `TestSmokeUpWithOnlyForeignPanesKeepsSessionUsable` also carries two "not-yet-adopted initial pane" comments (~lines 239, 249) whose pane counts become wrong post-fix (2→1 and 3→2, since the reap now fires on the FIRST `up`), plus the verbatim `"session has no panes to adopt or split"` quote at ~line 217 that card 4 explicitly promised "later batches" would correct — no card names it.
**Fix:** Enumerate all four stale sites in card 13, including the error-string quote card 4 defers to it.

### [NIT:decision] `lifecycle_test.go`'s initial-pane comment has no disposition
**Location:** plan-wide (discussion's affected-test enumeration, `lifecycle_test.go` ~line 388)
**Issue:** The discussion assigned it "read and confirm, likely out"; the plan gives it no disposition, and card 12's two greps cannot reach it (the comment contains neither sweep term). The comment happens to stay true, so this is a bookkeeping gap, not a defect.
**Fix:** Record the confirmed "out" disposition in card 12's Requirements so the enumeration is closed.

### [NIT:consistency] Card 8 misattributes doc.go's "each adopt the other's panes" passage
**Location:** batch 3 / card 8, final sentence
**Issue:** That passage (~line 223) is the session-name-rewrite bullet, not `adoptPaneGenerationLocked` prose; `doc.go` contains no generation-probe paragraph at all, so the "do not touch" pointer names a file section that does not exist.
**Fix:** Identify it as the session-name-rewrite bullet.

### [NIT:scope] Batch 4's `-run` filter omits a neighbouring test whose behaviour changes
**Location:** batch 4 / `verify:`
**Issue:** `TestSmokeUpSurvivesAScrubbedStateFileWhileTheSessionIsUp` lives in the same file cards 13-16 rewrite and its observable pane set goes from three panes to one under the new reap; smoke is outside `pipeline.done_gate`, so nothing else runs it.
**Fix:** Add it to the batch's `-run` alternation.

## Verdict

REQUEST_CHANGES
Sound plan; two ordering/premise defects and two context gaps must land first.
MILL_REVIEW_END
