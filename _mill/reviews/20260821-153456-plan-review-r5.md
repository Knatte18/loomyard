MILL_REVIEW_BEGIN
# Review: loom: convert to a Shed recipe — holistic

```yaml
duration_s: 500.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: claude-opus-5 (Anthropic Claude Opus 5)
reviewed_file: plan/
date: 2026-08-21
```

## Findings

### [BLOCKING:scope] Card 6 writes a file it declares no Edits for
**Location:** batch 2 / card 6
**Issue:** Card 6's Requirements say "Relocate the `fakeAlwaysDoneProducer` declaration out of this file into `internal/loomrecipe/fixture_test.go`", and card 5 forbids declaring it there — but card 6's `Edits:` is `none`, `Creates:` is `none`, and its only `Moves:` pair is `loomshed_test.go -> shape_test.go`; `internal/loomrecipe/fixture_test.go` appears only in `Context:`.
**Fix:** Add `internal/loomrecipe/fixture_test.go` to card 6's `Edits:` list (it is already in `## All Files Touched` as card 5's move target, so the overview needs no change).

### [NIT:decision] Roadmap's emptied "Shed recipe" Planned section has no stated disposition
**Demoted-from:** BLOCKING
**Location:** batch 6 / card 24
**Issue:** `manifest/roadmap.md`'s `### Shed recipe: declarative producer lists` Planned section contains exactly one item (lines 16–20), the conversion item card 24 moves to Done; the card says only to "close out the group's remaining-work framing in the section intro", leaving a heading plus intro with zero items, and the new parent-fabric item's placement is specified only as "sequence it after this conversion item" — which no longer sits in Planned.
**Fix:** State explicitly whether the emptied section heading+intro is deleted, collapsed into a pointer at Done, or repurposed, and name the section the new parent-fabric-resolution item is added under.

### [NIT:consistency] Card 15 says "ten fields" and lists nine
**Location:** batch 4 / card 15
**Issue:** "`c.env` gets exactly the ten fields loom's thirteen rows read" is followed by nine named fields (`Cwd`, `AnchorPath`, `WorktreeRoot`, `StatusPath`, `StatusLockPath`, `DecisionRecordPath`, `SupportLogPath`, `WebsterDeps`, `WebsterRun`), with `Landing` — the tenth — deliberately left unfilled in the next paragraph.
**Fix:** Reword to "nine of the ten fields loom's thirteen rows read; `Landing` is the tenth and stays unfilled per `landing-parity`", matching card 5's own ten-item list.

### [NIT:consistency] Card 25's commit message contradicts its own requirement
**Location:** batch 6 / card 25
**Issue:** The card requires "Do **not** add `contracts/recipes` to that tree, and do not open a `contracts/` region in it", but its `Commit:` reads `docs(overview): add loomrecipe and contracts/recipes to the module map`.
**Fix:** Reword the commit subject so it does not claim a module-map addition the card forbids (e.g. name loomrecipe's tree entry and contracts/recipes' prose mention).

## Verdict

REQUEST_CHANGES
Two bookkeeping/decision gaps; the conversion mechanism itself verifies clean against source.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
