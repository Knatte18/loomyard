# Plan fix — round 1

Source review: `_mill/reviews/20260820-103934-plan-review-r1.md` (holistic, `sonnetxhigh`, REQUEST_CHANGES, 2 BLOCKING + 3 NIT).

## Fixed

- r1 — "[BLOCKING:scope] Card 11's Context omits the ~20 test-file sources the port actually needs":
  added all 20 `internal/scoutengine/*_test.go` source paths to card 11's `Context:`, so the card's source list is self-contained and matches its own `Creates:` count of 34, following the pattern card 24 already uses for `cli.go` plus `cli_test.go`.
- r1 — "[BLOCKING:scope] Card 17's lyx-vocabulary sweep Edits list omits two production files it needs to cover":
  added `quarry/ensureserver.go` and `quarry/toolchain.go` to card 17's `Edits:`, and named the specific residue in its `Requirements:` — `ensureserver.go`'s six doc-comment mentions at source lines 40, 160, 257, 260, 357, 496, and `toolchain.go`'s surviving `internal/lyxcwd`/Cwd Resolution Invariant mention on the line card 16 otherwise touches.
  Verified both against the tree: `grep -ni 'lyx' internal/scoutengine/ensureserver.go` returns exactly those six lines, and `internal/scoutengine/toolchain.go:10` carries both the `internal/lyxcwd` mention and the `_mill/discussion.md` citation.
  Without this the card's own "confirm every remaining hit is zero" gate could not pass.
- r1 — "[NIT:consistency] Card 46 says it may fix files and record in a commit, but declares Edits: none and Commit: none" (demoted from BLOCKING):
  rescoped card 46 to a strict report-and-classify gate — it now fixes nothing, and a hit needing a real edit is reported as a gap in cards 39 through 45 so the fix lands in the card that owns the file and is reviewed with that card's diff.
  Gave it a real `Commit:` by moving the `docs/research/quarry-port-log.md` deletion into it: the port log exists only to give each quarry-side batch a task-worktree commit and to carry batch 5's go/no-go verdict, so closing it is the natural last act of the batch, and its commit message is now where the surviving-hit count and the justified-mention list are recorded durably.
- r1 — "[NIT:consistency] Batch 04's frontmatter says cards: 9, but it contains 10 cards":
  changed `cards: 9` to `cards: 10`. Confirmed the declared count now matches the actual `### Card` count in all six batch files.
- r1 — "[NIT:consistency] 'dependency budget is exactly four external modules' names five modules in its own body":
  retitled the shared decision to "dependency budget is three direct modules plus cobra's own indirects" and rewrote its body to separate the three chosen direct requires from the two indirects cobra brings with it, so the stop condition reads "a fourth direct dependency" consistently with card 1's own wording.

## Pushed Back

_None. All five findings verified accurate against the tree, and no fix broke existing plan structure, conflicted with a documented decision, or destabilised another batch._

## Verification

- `_plan_dag.validate` — OK.
- `_plan_validate.run` with `skip_checks={"out-of-worktree-target"}` — 0 findings.
