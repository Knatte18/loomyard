# Discussion fix — round 3

Source review: `_mill/reviews/20260817-143814-discussion-review-r3.md` (APPROVE, 0 BLOCKING, 2 NIT, 0 demoted).

## Fixed

- r3 `[NIT:decision]` "`planparser.Validate`'s `worktreeRoot` param undispositioned" — added an explicit out-of-scope decision with its reason.
  Verification went further than the finding: `Validate`'s two production callers pass *different* roots (`internal/webstercli/validate.go:73` → `c.layout.AnchorPath()`, `internal/websterengine/runlevel.go:330` → `deps.WorktreeRoot`), so renaming the parameter would assert an unadjudicated answer to a plan-format semantics question and change behaviour — which this task does not do.
  Recorded as a follow-up to file separately.
  The discussion's own earlier claim that `validate.go:73` was precedent "including the anchor-always argument" was overstated by exactly this disagreement, and is corrected in the same edit to precedent for the told-string shape only.
- r3 `[NIT:scope]` "Three items left as 'verify at implementation' are answerable now" — all three resolved from source and recorded as decisions:
  `cli_test.go:134,152` are not flipped (they back `TestFabricSync_*`, call `fabricSync(layout, …)` directly, never consume `planDir`);
  `cmd/lyx/notransients_test.go` keeps its `loomengine` import (`DiscussionDir`, `LoomStatusFile`, `LoomStatusLock` rows all untouched) and only gains `planparser`, making `webstercli` the sole package that actually sheds the `loomengine` dependency;
  `cmd/lyx/constructoranchoring_test.go`'s "every constructor batch 5 relocated out of `internal/lyxcwd`" framing sentence does need adjusting, since two of its rows will name `planparser` functions batch 5 never relocated.

## Pushed Back

None.
Both findings verified accurate against source before fixing.
