# Discussion fix — round 6

Source review: `_mill/reviews/20260831-152728-discussion-review-r6.md` (APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r6 `[NIT:design]` "Fallback probe premise unstated for >1MB files" — added a "Known limit" bullet to the directory-probe Decision recording the contents API's ~1MB inline ceiling, its parity with today's documented `--jq .content | base64 -d` path, and a live-capture step to observe it and either record or narrow the probe.
- r6 `[NIT:consistency]` "Arg-count rule collides with the `--` terminator" — the `github-read-selftest.sh` argument-count bullet now states the count is checked after the terminator is consumed, with the three-token/two-positional example spelled out.
- r6 `[NIT:scope]` "Guard message \"exact wording\" is asserted but never specified" — the guard-abort Decision now specifies content-level substring assertions (following `github-tree-selftest.sh:435`) instead of literal text, and both guard test scenarios were reworded to match.

## Pushed Back

(none — every finding was fixed under the default rule)
