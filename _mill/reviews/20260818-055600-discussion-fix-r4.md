# Discussion fix — round 4

Source review: `_mill/reviews/20260818-055319-discussion-review-r4.md` (APPROVE, 0 BLOCKING, 3 NIT).
Auto mode: every NIT fixed under the `mill-receiving-review` fix-everything default.

## Fixed

- r4 · "Webster transposition-coverage claim is false for 237" — verified: `patternActiveLayout` is referenced at exactly one site, `template_test.go:803`, inside `RenderRecoveryPrompt`'s "PATTERN active" sub-test. `RenderMasterPrompt` has no non-empty-directive assertion anywhere; its only PATTERN-active coverage is `TestRenderMasterPrompt_MissingPatternStencilErrors` (850) on the separate `patternActiveMissingPatternStencilsLayout` fixture, which detects a transposition by the *absence* of an expected error. Replaced the false one-fixture-covers-both claim with a per-site list naming the actual detector and firing mechanism for all four call sites, and stated that the two webster fixtures are not interchangeable.
- r4 · "Burler assertion target is a random `round-*` directory" — verified `engine.go:116` uses `os.MkdirTemp(burlerDir, "round-")`, and `prompt.go:61-68` puts `pattern_directive` into the instruction-1 values map only, via `stencil.FillOptional`. The Testing section now names `instruction-1-explore.md` as the sole marker carrier and instructs the test to discover the round directory (single-entry read of `<AnchorPath>/.lyx/burler`, or the `instruction_1_path` recorded in the orchestrator prompt) rather than hardcoding a path.
- r4 · "Stale test name/comment in `pattern_test.go` not enumerated" — verified `TestDirective_RelPathNestedSubdirectory` (305) and its header comment (299–304) use "a Layout whose RelPath", vocabulary that is already half-stale (the field is `AnchorRel`) and fully stale after this change. Added to Scope with a rename-and-reword disposition, and the Testing section's nested-anchor bullet now names this existing test instead of asking for "at least one case", with an explicit do-not-weaken note.

## Pushed Back

None.
All three findings verified against current source before fixing.
