# Fixer report: plan-fix round 2

Source review: `20260908-133952-plan-review-r2.md`

## Fixed

- `20260908-133952-plan-review-r2.md` — "[BLOCKING:scope] Card 5 cannot source webster's missing-plan message".
  Verified: card 5's `Context:` listed neither `internal/webstercli/wiring.go` (the only place the refusal string exists) nor `internal/logger/sink.go`, while the card requires both the message and `logger.SetDurableSinkDir`/`logger.Info` assertions.
  Added both to `Context:`, plus `internal/burlercli/wiring.go` for the burler-side descriptor values the fixture mirrors, and told the card to copy the refusal verbatim including its two interpolated paths in their existing order.
- `20260908-133952-plan-review-r2.md` — "[BLOCKING:scope] Stencils seed-failure error path has no test".
  Verified against `_mill/discussion.md`'s cliwire test-candidate list, which names it, and against card 4 step 5, whose `stencilstore.Reconcile` failure is the prologue's only error return that card 5 left unexercised.
  Card 5's stencils bullet now adds the case, names how to drive it (a regular file where the derived stencils path needs a directory), and requires the error to name both `m.Name` and the directory.
- `20260908-133952-plan-review-r2.md` — "[NIT:consistency] Card 3 doc contradicts card 6's wireHub" (demoted from BLOCKING).
  Verified: card 3 prescribed a doc comment claiming `wireHub` calls `DefaultPlanDir`, which card 6 never does.
  Resolved in card 6's favour rather than by adding the hub call: `wireHub` keeps comparing against `geom.PlanDir`, the value `hubgeom.WebsterGeometry` actually built, since the two strings are identical today and comparing against the geometry's own field stays correct if `internal/hubgeom` ever changes how it computes `PlanDir`.
  Card 3 now forbids the `wireHub` clause in the doc text and records why, explicitly noting the departure from `_mill/discussion.md`'s exported-surface decision;
  card 6 records the same reasoning at the call site.
- `20260908-133952-plan-review-r2.md` — "[NIT:consistency] Card 4 states error handling for step 1 only".
  Verified: steps 2 and 3 were written as bare calls despite both being fallible.
  Both now state the return-the-zero-`Standalone`-and-the-error rule, followed by a blanket sentence covering every fallible step below.
- `20260908-133952-plan-review-r2.md` — "[NIT:scope] Card 7 omits burler's runner construction lines".
  Verified: the card names the `runner` argument to `burlerengine.New` without ever naming what builds it.
  Card 7 now states that `reedengine.New(reedCfg, reedGeom)` and the `shuttleengine.NewDetachedRunner(...)` call both survive unchanged, and why `NewDetachedRunner` rather than `NewRunner`.

## Pushed Back

_None._
