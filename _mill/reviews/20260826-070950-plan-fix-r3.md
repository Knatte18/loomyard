# Fixer report: plan-fix round 3

Source review: `_mill/reviews/20260826-070712-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 0 NIT).

## Fixed

- `20260826-070712-plan-review-r3.md` — "[BLOCKING:scope] Card 10's test-breakage enumeration misses a verdict-identical test".
  Verified accurate against `internal/shedadapters/bouncer_replay_test.go`: `TestBouncer_Judged_IgnoresFocusFile` lays out round 1 with a report, an APPROVED verdict, and a ledger, then asserts `Call` returns `shedengine.Done` with the shuttle never invoked — the same disk state card 10's clear trigger fires on, so both assertions break.
  Card 10's closing sentence claiming the remaining tests reach `settle` through the judge path or a BLOCKING verdict was false for this one.
  Card 10 now enumerates all three APPROVED-fixture tests in that file explicitly, and directs this one to be repaired by switching its fixture to BLOCKING and its assertions to the BLOCKING-replay outcome, which keeps it isolating its actual subject — that `judged(N)` does not treat an absent `round-2-focus.md` as debris — rather than colliding with the clear trigger.
  Deleting it is barred in the card, because `judged`'s focus-file exclusion is deliberate and card 9 rests on this test.
  Checked `TestBouncer_FocusSynthesis_OverUnparseableFile`'s two subtests while re-deriving the list: both use a BLOCKING verdict or the seed path, so they are genuinely unaffected and are now named as such rather than left to the discarded blanket claim.
  Added a clause to card 9 recording that the test it cites as its behavior-preservation proof is itself repaired one card later, so the cross-reference does not read as stale once card 10 lands.

## Pushed Back

_None._
The finding verified accurate, and the fix breaks no existing functionality, conflicts with no documented design decision, and destabilizes nothing outside this task.
