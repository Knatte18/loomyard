# Plan fix — round 2

Source review: `_mill/reviews/20260920-095455-plan-review-r2.md`

## Fixed

- `[NIT:consistency]` "Card 15's 'existing strand read' is not in shipped source" — verified accurate: `ensureStatusStrand` in `internal/loomcli/sharedbootstrap.go` returns an error alone and never hands its strand slice to the caller, and the only `reed.Status()` call in `internal/loomcli/start.go` sits at line 259, inside step 7 and after the branch. So there is no pre-branch strand read to reuse. Card 15 now states the two inputs' differing provenance explicitly: the run-lock probe is reused unchanged, the strand read is new and goes through card 12's `driverPaneProbe.Strands()` seam, called once and fed to both the predicate and the corpse check.
- `[NIT:scope]` "Card 9 cites Spec.validate's behavior without listing its file" — verified accurate. Added `internal/shuttleengine/spec.go` to card 9's `Context:` rather than rewording the citation: the implementer composing a per-attempt path benefits from reading the must-not-exist contract that makes the per-attempt suffix necessary, so listing the file is the more useful of the reviewer's two offered fixes.

## Pushed Back

_None._
