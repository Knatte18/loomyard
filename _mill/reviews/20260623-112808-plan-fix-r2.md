# Plan fix report — round 2

Source review: `20260623-112720-plan-review-r2.md` (verdict REQUEST_CHANGES, 1 BLOCKING, 2 NIT).

## Fixed

- `20260623-112720-plan-review-r2.md` — "[BLOCKING] Card 9 misses BenchmarkGetDuringUpsert
  facade write": verified (`concurrency_test.go:150` `b.Setenv` + `New(cfg)` line 155 +
  `UpsertTask` line 170). Extended **card 9**'s concurrency_test.go bullet to also convert
  `BenchmarkGetDuringUpsert` to `cfg.SkipGit = true`.
- `20260623-112720-plan-review-r2.md` — "[NIT] Card 9 rationale 'do not build a Board' is
  wrong for Upsert/Get/List": verified (`bench_test.go:128/149/170` call `board.RunCLI`).
  Reworded **card 9**'s bench_test.go bullet — `BenchmarkUpsert/Get/List` go through `RunCLI`,
  which still folds `BOARD_SKIP_GIT` via `applySkipEnv`, so their `b.Setenv` stays live and
  correct.
- `20260623-112720-plan-review-r2.md` — "[NIT] Card 2 leaves dangling SyncGit refs in
  board-performance.md": verified (refs at lines 18-19, 86-91, 109 beyond the "## Push access"
  section). Extended **card 2**'s board-performance.md bullet to remove every `BenchmarkSyncGit`/
  `SyncGitNoPush`/`TestIntegrationCommitPush` reference (the `-bench SyncGit` run block, the
  Background-sync results table, inline mentions, and the Push access section), with a final
  `grep SyncGit` zero-hit check.

## Pushed Back

(none)
