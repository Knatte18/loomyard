MILL_REVIEW_BEGIN
# Review: Collapse _pattern into _lyx, and un-reserve _raddle as a hub-level name — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-08
```

## Findings

### [NIT] Batch 7's closing residue-grep claim is inaccurate, though the implementation is correct
**Location:** `_mill/plan/07-docs-and-design-sweep.md:289` vs. `docs/overview.md:166,195`, `manifest/designs/raddle.md:15`, `manifest/designs/finalize.md:29`, `manifest/designs/fabric-unified-view.md:18-19,130-131,195-196,205`
**Issue:** The plan states the post-batch-7 sweep must report only two survivor files (`raddle_guard_test.go`, `structuraldirs_test.go`), but cards 32/35/36 themselves require adding corrective negation prose ("no `_raddle` junction of its own now or ever", "`_raddle` is not a reserved hub name", superseded-notes quoting `_pattern`/`_raddle`) to `docs/overview.md` and the design docs — so a repo-wide grep necessarily reports more than two files. The implementation correctly followed each card's own instructions; the plan's own closing self-check is what's wrong.
**Fix:** None needed in code — this is a plan-wording defect (the closing grep description undercounts its own cards' required output), not an implementation defect; verified every reported extra match is a deliberate, accurate corrective statement, not a stale claim.

## Verdict

APPROVE
All seven batches faithfully realize the plan; constraints, cross-batch contracts, and doc corrections all check out against source.
MILL_REVIEW_END
