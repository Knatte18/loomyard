# Plan fix — round 1

Source review: `_mill/reviews/20260703-081926-plan-review-r1.md` (APPROVE, 1 NIT).

## Fixed

- "Card 6 omits test-side updates forced by Card 5's signature/const changes" — Card 6's `suite_test.go` requirements now name the mechanical retargets: `renderScheme(info)` → `renderScheme(info, sandboxSuiteMD)` in `TestRenderScheme_ContainsHeaderAndBody`, `defaultInstruction` → `mainSuite.instruction`, `suiteFileName` → `mainSuite.fileName`, and `runSuite` call sites gain the `mainSuite` spec argument; the "assertions unchanged" sentence is scoped to everything beyond those retargets.

## Pushed Back

(none)
