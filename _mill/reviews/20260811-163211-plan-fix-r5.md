# Plan fix — round 5

Source review: `_mill/reviews/20260811-162458-plan-review-r5.md` (holistic, APPROVE, 0 BLOCKING + 6 NIT).

The round produced an APPROVE, but one finding carries `**Demoted-from:** BLOCKING`, so the convergence gate holds the loop open rather than approving the plan at this round.
All six NITs are fixed below.

## Fixed

- `20260811-162458-plan-review-r5.md` — "[NIT:consistency] runReconcile's CLI record is built out of execution order".
  Verified: `configsync.ReconcileFabricAt` runs before `top.Reconcile(l)` in that handler, so seeding from `r.Mutated()` first would place the config rewrite after the engine's mutations in an array whose order is the only ordering the vocabulary carries.
  Card 25 now spells the sequence out: construct the recorder as soon as `l` resolves, append the config-rewrite entry at its own call site, then `Extend` the engine record, then record the `Bolt.Commit` backfill.
- `20260811-162458-plan-review-r5.md` — "[NIT:scope] Card 14 omits `unseedLyxJunction` from the threading list".
  Verified: it is the only caller of `unseedJunctionRecords`.
  Named in card 14's threading list, with the consequence stated — passing `nil` through the unnamed intermediate compiles and would silently drop every `link_removed` the unwire verb produces.
- `20260811-162458-plan-review-r5.md` — "[NIT:consistency] The `"."`-target coverage rationale is false against `pathAtOrBelowRoot`".
  Verified: that helper splits `"."` into one literal segment and compares it against the diff key's first segment, returning false for every hub-relative path.
  Card 29 now requires normalising a `"."` target to the hub root *before* the segment match, and says why that is a boundary normalisation rather than the second matcher the same card forbids.
- `20260811-162458-plan-review-r5.md` — "[NIT:consistency] Card 21 misplaces the menu launcher, then hedges it".
  Verified: `menuLauncherPath` is `<hub>/_launchers/<anchorRel>/menu.<ext>`, a sibling of `LauncherDir`, never inside it.
  The wrong parenthetical and the conditional are gone;
  card 21 now states two entries unconditionally — one `dir_created` for the launcher directory covering the two scripts inside it, and one `file_written` for the menu launcher, which no recorded root covers.
- `20260811-162458-plan-review-r5.md` — "[NIT:consistency] Card 11 miscounts the `CoalescePushBothAt` call sites".
  Verified: three calls;
  the remaining token hits are `t.Fatalf` message text.
  Dropped the number rather than correcting it, and pointed the implementer at the chained `go vet -tags integration` as the authority — the same posture cards 13, 18 and 30 already take for their own lists.
- `20260811-162458-plan-review-r5.md` — "[NIT:scope] Card 7 edits `mutation_test.go` but files it under Context".
  Verified.
  Moved into `Edits:`, matching card 3's handling of `internal/output/output_test.go`, and `internal/fabricengine/testmain_test.go` put in `Context:` so the card is not left with an empty one.

## Pushed Back

_None._
Every finding was factually accurate and confirmed against the source before fixing.
