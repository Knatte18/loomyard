MILL_REVIEW_BEGIN
# Review: fabric: merge-conflict primitive — holistic

```yaml
duration_s: 418.0
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude (Anthropic) — harness reports model id "claude-opus-5"; my own best-effort read is an Opus-class Claude model, exact version not self-verifiable
reviewed_file: plan/
date: 2026-08-19
```

## Findings

### [NIT:consistency] Cleanup carve-out is an unrecorded third supersession
**Demoted-from:** BLOCKING
**Location:** batch 5 / card 13 (vs `00-overview.md` Shared Decision 1)
**Issue:** `discussion.md` names `Cleanup` explicitly in the refuse-while-record-exists list (`combined-lock-around-mutating-steps-only`, and again in Testing: "`Remove`/`Cleanup` on the pair refuse with the same typed error"); card 13 instead ships no guard and card 14 asserts `Cleanup` *succeeds* — but Shared Decision 1 states "Two such supersessions exist today" and an implementer applying its tie-breaker would add the guard.
**Fix:** promote card 13's Cleanup reasoning into a named Shared Decision that cites the `combined-lock-around-mutating-steps-only` clause it supersedes, and correct the "two supersessions" count to three.

### [NIT:decision] Audit findings have no disposition anywhere in the plan
**Demoted-from:** BLOCKING
**Location:** batch 6 / card 18; `00-overview.md`
**Issue:** the roadmap entry and `discussion.md`'s Scope both make the audit an in-scope deliverable, and `_mill/discussion-meta.md`'s `## Audit findings` records three spun-out items (missing monorepo verb surface; two-sided reset-to-SHA verb; merge-in-progress in `lyx fabric status`) — but no card says whether they land in `manifest/roadmap.md`'s `## Someday`, elsewhere, or nowhere. `_mill/` is task-local and vanishes on merge.
**Fix:** give card 18 an explicit disposition for each of the three findings (e.g. one `## Someday` entry each, or a stated decision to drop them).

### [BLOCKING:scope] Conflict-envelope contract test unreachable under batch 6 verify
**Location:** batch 6 / card 17
**Issue:** the card requires extending `internal/fabriccli/envelopecontract_integration_test.go`'s existing property walk, whose three test functions are `TestRunCLI_ReconcileReportsAFailedPairAsAFailure`, `TestRunCLI_PruneEmitsAnEmptyArrayNotNull`, `TestRunCLI_ReconcileDoesNotFailOnAPairThatVanishedMidWalk` — none matches batch 6's `-tags integration -run Merge ./internal/fabriccli/`, so the added assertions never execute in-batch.
**Fix:** require the extension to be a new `Merge`-named test function in that file (or widen batch 6's integration `-run` pattern).

### [BLOCKING:scope] `logger.Warn` named with no logger file in Context
**Location:** batch 3 / card 7; batch 4 / card 11
**Issue:** both cards require `logger.Warn` calls (fetch-failure tolerance, git-cause suppression), but neither `Context:` nor `Edits:` names `internal/logger` or any fabricengine file that calls it — `pull.go`, `dirtiness.go`, `weftwiring.go`, `branchname.go`, `fabric.go` all lack `logger` usage. Card 8 is fine only incidentally (it lists `checkout.go`).
**Fix:** add `internal/fabricengine/checkout.go` (or `internal/logger`) to cards 7 and 11's `Context:`.

### [NIT:consistency] Wrong file count prescribed for the boundary-guard comment
**Demoted-from:** BLOCKING
**Location:** batch 1 / card 1
**Issue:** the card says update `gitrepoBoundaryMinScannedFiles`' doc comment "to include `merge.go` (now 9 non-test files)". `internal/gitrepo` already has 9 non-test files — the comment's enumeration is stale, omitting `blobread.go` — so adding `merge.go` yields 10, and following the card writes a fresh falsehood.
**Fix:** state the correction as "add `blobread.go` and `merge.go`; the count becomes 10".

### [BLOCKING:scope] finalize.md's discrepancy-document claim left standing
**Location:** batch 6 / card 18
**Issue:** the card rewords lines 3, 21, 51 and the "Only Raddle's output forwards" section, but leaves `finalize.md` lines 16–19 asserting Fabric hands back one of *two* shapes, the second being a precomputed "discrepancy document". This plan ships only the ordinary-git-conflict shape (unmappable conflicts return `*ErrUnmergeableState`), so the doc contradicts itself the moment line 21 says the primitive exists.
**Fix:** add lines 16–19 to card 18's `finalize.md` requirement, reducing them to the shipped shape plus the `ErrUnmergeableState` refusal.

### [NIT:decision] Roadmap move left conditional though the file was in Context
**Location:** batch 6 / card 18
**Issue:** "move or remove it per the file's own convention … if no completed section exists, delete the entry" defers a resolvable decision to implementation time; `manifest/roadmap.md` has a `## Done` section at line 132.
**Fix:** pin it: move the entry to `## Done` in that section's existing style.

### [NIT:consistency] MergeStart's conflict probe silently diverges from the pinned surface
**Location:** batch 1 / card 1
**Issue:** `discussion.md`'s `public-surface-shapes` godoc pins the post-error probe as "unmerged index entries (`git ls-files -u`)"; the card substitutes `ConflictedFiles()` (`git diff --name-only --diff-filter=U`) without a Shared Decision naming the superseded clause, so the plan's own tie-breaker points back at `ls-files -u`.
**Fix:** record the substitution as a Shared Decision (reuse of one probe), or restore the pinned spelling.

### [NIT:scope] Vocabulary walk unrun until batch 6
**Location:** batches 1–5 `verify:`
**Issue:** batch 1 creates `internal/gitrepo/merge.go` and amends `doc.go` — a package outside the Fabric Vocabulary owner set — yet no batch before 6 runs `./internal/lyxcwd/`, where `TestEnforcement_FabricVocabulary` lives; a stray `weft`/`warp` token in that godoc surfaces five batches late.
**Fix:** append `./internal/lyxcwd/` to batches 1–5's untagged `verify:` command.

## Verdict

REQUEST_CHANGES
Six blocking gaps: an unrecorded decision supersession, dropped audit findings, and one unreachable test.
_Note: 3 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 3._
MILL_REVIEW_END
