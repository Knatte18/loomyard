# Plan fixer report — round 6 (final; operator-approved)

Source review: `_mill/reviews/20260802-131701-plan-review-r6.md` (verdict REQUEST_CHANGES, 3 BLOCKING + 1 NIT). All four findings verified accurate against source → all FIXED, none pushed back. This is the 6th (final configured) review round; the operator elected to approve the plan on the strength of these fixes rather than take the standard max-rounds `blocked` halt (findings were converging and non-progress never fired).

## Fixed

- **[BLOCKING] Card 26/27 miscategorize checkout_index_refresh_test.go as in-package** (`20260802-131701-plan-review-r6.md`) — Confirmed `internal/fabricengine/checkout_index_refresh_test.go` is `package fabricengine_test` (external), so its `f.CommitWeft(...)` (`:65`) cannot be casing-fixed once `CommitWeft` is unexported. Rewrote card 27 to split its file list by verified `package` line: it now lists `checkout_index_refresh_test.go` in the EXTERNAL bucket alongside `weftgit_exclude_test.go`, migrating both onto `Fabric.Commit`.
- **[BLOCKING] Card 26's CommitWeft unexport sweep is undeclared; pull_integration_test.go missing from All Files Touched** (`20260802-131701-plan-review-r6.md`) — Made card 27 the declared owner of the entire test reconciliation with an explicit nine-file `Edits:` list (7 in-package casing + 2 external migration), trimmed card 26's requirement to production callers only (`unwire.go` + doc mentions), and added `pull_integration_test.go` to the overview's All Files Touched. Card 27 immediately follows card 26 so the unexport's compile window spans a single commit.
- **[BLOCKING] Card 16 instructs editing drift.go without declaring it** (`20260802-131701-plan-review-r6.md`) — Confirmed `drift.go:13` names both `HostClean` and `PairInSync` and is owned by card 17 (which has it in Edits). Dropped card 16's `drift.go` parenthetical entirely (card 16 now updates only `hostclean.go`'s own header) so exactly one card owns the `drift.go` edit.
- **[NIT] Card 18 leaves parentOf orphaned in syncweft_integration_test.go** (`20260802-131701-plan-review-r6.md`) — Confirmed `parentOf` (`:60`) is called only from the deleted `TestSyncWeft_RecordsPostPushSHA` (`:209`). Added a requirement to card 18 to delete the orphaned `parentOf` helper after removing that test.

## Pushed Back

(none)

## Operator note

Max review rounds (6) reached. BLOCKING trajectory across rounds: 4 → 2 → 3 → 2 → 1 → 3, with content growing steadily more cosmetic (r1: real test-coverage gaps and dead-method test dependencies; r6: package-categorization and file-declaration hygiene). The non-progress guard never fired — every round surfaced new, distinct findings, all fixed. Validator gate is clean (0 findings) and the DAG is valid. On that basis the operator approved the plan for implementation instead of taking the standard max-rounds `blocked` halt.
