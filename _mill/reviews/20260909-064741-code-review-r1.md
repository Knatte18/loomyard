MILL_REVIEW_BEGIN
# Review: Centralize glyph ref-shape enumeration — holistic

```yaml
duration_s: 155.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-09
```

## Findings

### [NIT:consistency] Card.ID test can't call unexported cardID as card 4 literally specifies
**Location:** `internal/planparser/validate_test.go:1889-1900`
**Issue:** Card 2/exported-surface's requirement says the new test should assert `c.ID() == cardID(c)`, but `validate_test.go` is `package planparser_test` (external), so `cardID` is unreachable; the implementer instead asserts against the literal `"N-<slug>"` string with a doc comment explaining why.
**Fix:** No action needed — the workaround is correct and documented; consider updating the batch file's wording in a future planning pass so it doesn't imply direct access to an unexported symbol from an external test package.

## Verdict

APPROVE
All seven batches are fully realized, cross-batch contracts hold, constraints (registry, docs, enforcement scans) are satisfied, and tests are byte-consistent with the plan.
MILL_REVIEW_END
