# Plan fix — round 5

Source review: `_mill/reviews/20260701-092725-plan-review-r5.md` (verdict APPROVE,
blocking_count 0, nit_count 1).

## Fixed

- **[NIT] Card 8 step 4 omits error handling for `os.Stat`/`os.RemoveAll` on
  weft-side content** (`04-initcli-undo.md`, Card 8, step 4) — verified `runInit`'s own
  `os.Stat(lyxDir)` handling and every `os.RemoveAll` call site in `warpengine`
  (`remove.go`, `prune.go`, `launchers.go`) all surface stat/removal errors rather than
  ignoring them. Added the identical `err != nil && !os.IsNotExist(err)` stat-error
  branch for both `os.Stat` calls in step 4, and added an explicit error check on
  `os.RemoveAll(weftLyxDir)`'s return value.

## Pushed Back

None — the finding was factually accurate and low-harm to fix.
