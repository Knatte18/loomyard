# Plan fix — round 4

Source review: `_mill/reviews/20260811-160842-plan-review-r4.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 3 NIT).

## Fixed

- `20260811-160842-plan-review-r4.md` — "[BLOCKING:design] repo_advanced recorded on a weft pull that advanced nothing".
  Verified: `PullWeft` is a bare `f.weft.Pull()` that returns nil when already up to date.
  Card 20 now gates **both** `repo_advanced` entries — weft pull and warp advance — on a `f.weft.CurrentSHA()` / `f.warp.CurrentSHA()` sample taken before and after, recording only on a change and carrying the new SHA as `Detail`.
  `internal/fabricengine/coalesce.go`'s `headOrEmpty` is named as the in-repo precedent, including its `gitrepo.ErrNoCommits` tolerance;
  an observation error records nothing and does not propagate.
  The warp half was not in the finding but had the identical defect — a reset to the SHA warp already carries advances nothing.
- `20260811-160842-plan-review-r4.md` — "[BLOCKING:design] The weft handlers' hub root is unresolvable in `push --bypass`".
  Verified: the bypass branch takes the injected paths and returns before `resolveWarpLocation()`, and `spawnPush` always injects an empty warp path.
  Card 26 now splits the derivation: `l.HubPath` in the four ordinary branches (the canonical accessor, not a hand-rolled parent-of-warp computation), and an **empty** hub root in the bypass branch, which appends no path-targeted entries of its own — it only `Extend`s an already-converted record, and `Extend` performs no conversion.
- `20260811-160842-plan-review-r4.md` — "[BLOCKING:scope] The derived inventory's token list cannot see a content relocation".
  Verified: `adoptDotLyxContent`'s `os.Rename` loop moves every warp-side `.lyx` entry to the weft target.
  `os.Rename(` added to the derivation token list in both the Shared Decision and card 21, and card 17 now gives the relocation an explicit disposition: one `KindFileWritten` at the destination directory with `Detail: "adopted"`, after the rename loop and before the link is created, covering the moved tree under the coverage rule.
  Stated why no sixteenth `Kind` is added for it.
- `20260811-160842-plan-review-r4.md` — "[NIT:consistency] Card 33's guard exempts a "body check" it never defines".
  Verified — three statements of the assertion disagreed.
  Resolved by deciding: the guard asserts the **declaration parameter only**, never executor bodies. `repointLink` therefore needs no exemption and must not be given one, and the blind-spot wording now says "by declaration inspection only".
  The reason is stated rather than assumed: a raw-substring body scan cannot distinguish a real recording call from one in a comment.
- `20260811-160842-plan-review-r4.md` — "[NIT:scope] Threading lists name callers that are not the callers".
  Verified all three intermediates: `restorePortalAndLaunchers` (`reconcile.go:372`), `removeWarpJunction` (`weftwiring.go:144`), `applyStaleRemoval` (`reconcile.go:706`).
  All three now named, with `internal/fabricengine/reconcile.go` added to card 13's `Edits:`, and the caller lists explicitly declared illustrative — the compiler plus each card's grep step is the authority.
- `20260811-160842-plan-review-r4.md` — "[NIT:consistency] runReconcile's Bolt.Commit records without the committed flag".
  Verified: `Bolt.Commit` returns `("", false, nil)` on a no-op.
  The `runReconcile` backfill site now carries the same `committed == true` condition as the `CloneAndWire` site, and its `ReconcileFabricAt` entry is likewise gated on `Result.Applied`.

## Pushed Back

_None._
Every finding was factually accurate and confirmed against the source before fixing.
