# Plan fix — round 3

Source review: `_mill/reviews/20260728-102114-plan-review-r3.md` (verdict APPROVE, 0 BLOCKING + 2 NIT, reviewer `sonnetmax`).

## Fixed

- r3 "[NIT] composePrompt described as filesystem-free but calls os.Stat" — verified accurate: `composePrompt` calls `formatFileSet`, which runs `os.Stat` on each Target/Fasit path at `internal/burlerengine/prompt.go:53` to annotate directory entries. Card 25's claim that it is "a pure string function with no filesystem access" was therefore false, which mattered because that claim was the stated rationale for passing a `patternDirective string` rather than a `*hubgeometry.Layout`. Reworded to "does no filesystem access of its own beyond the directory check `formatFileSet` already performs", and restated the rationale on the accurate ground — the function must not acquire *geometry* awareness, which is the real property being preserved. The required change is unaffected: the directive is still computed one level up at `Run` in `engine.go`, which already holds `e.layout`. Checked the sibling claim in card 28 for the same error and it stands: `composePlanPrompt` builds a values map and calls `stencil.Fill` with no stat, and `plan.go`'s own doc comment confirms `PlanSpec` "does not stat the decision record, does not stat or create `_lyx/plan/`".
- r3 "[NIT] Master-template directive placement clause is imprecise against the general rule" — verified accurate: `internal/websterengine/master-template.md` has a plain paragraph at line 21 ("You are the long-lived Master session for one webster plan run…") sitting between the second orientation blockquote and the first `##` heading, `## Orientation` at line 23. Card 27's "after both leading orientation blockquotes" therefore described a position one paragraph earlier than the batch-wide rule's "immediately before the template's first `##` heading … one mechanical rule with no per-template exceptions". Card 27 now names `## Orientation` as the anchor explicitly, states that the marker goes after that trailing paragraph too, and records why the paragraph is easy to miss — the anchor is the `##` boundary itself, never the last blockquote. Wording precision only; no test distinguishes the two candidate positions, since the only positional assertion checks the marker lands ahead of the first work instruction.

## Pushed Back

None. Both findings were verified against source before acting — the `os.Stat` call in `formatFileSet` and the plain paragraph at `master-template.md:21` were each read directly. Neither fix changes any card's required code change, breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope; both correct prose in a card's `Requirements:` so that the stated rationale matches the source it cites.
