# Plan fix — round 3

## Fixed

- Ref: `_mill/reviews/20260701-081929-plan-review-r3.md` — "Batch-2 Batch Tests names wrong clone test file" [NIT]: corrected the `## Batch Tests` prose in `02-warpengine-stderr-fix.md` to name `clone_test.go` instead of `clone_integration_test.go`, matching Card 11's actual (already-corrected in round 1) target.
- Ref: `_mill/reviews/20260701-081929-plan-review-r3.md` — "Set scaffold-cleanup only covers the unknown-key branch" [NIT]: broadened batch 01, card 3's `Set` requirements so `os.Remove(path)`-on-scaffolded fires on every error branch (read, `SetValues`, `WriteFile`), not only the unknown-key case, fully matching `Edit`'s pre-call-state abort contract.

## Pushed Back

(none)
