# Fixer report: plan-fix round 5

Source review: `20260908-150909-plan-review-r5.md`

Operator instruction for this round: approve and hand off after round 5 regardless of verdict.
Every finding below was still run through the receiving-review decision tree and fixed;
none of these fixes is re-reviewed by a reviewer round, and that is recorded here deliberately.

## Fixed

- `20260908-150909-plan-review-r5.md` — "[BLOCKING:design] New profile test can boot live substrate or assert nothing".
  Verified against `internal/burlercli/run.go`: `RunE` returns early on `clihelp.ShouldAbort`, and a *successful* decode falls through to `c.reedUp()` → `reedEngine.Up()`, a live tmux/reed boot.
  The card's premise that the invocation "is expected to fail later, at wiring or engine construction" was wrong — wiring runs in `PersistentPreRunE`, ahead of `RunE` — so as specified the case either booted substrate from an untagged test or passed vacuously.
  Card 7 now requires a profile file that fails `decodeProfile`'s strict decode, a **positive** assertion that the output contains `profile YAML`, the existing negative `read --profile` assertion alongside it, and a doc comment stating `c.reedUp` must never be reached.
- `20260908-150909-plan-review-r5.md` — "[BLOCKING:scope] Card 7's profile test omits the mandated state-home redirect".
  Verified: a successful pre-run reaches the real `standalonestate.Derive` and the real `stencilstore.Reconcile`, so without both env redirects the untagged case writes into the developer's own state home — against the overview's "every moved test stays tier 1" decision, and the reason `internal/burlercli/cli_integration_test.go` sets both and carries `//go:build integration`.
  Card 7 now requires both `XDG_STATE_HOME` and `LOCALAPPDATA` redirected to fresh `t.TempDir()` values, no `t.Parallel()`, and a `t.Cleanup` sink restore.
- `20260908-150909-plan-review-r5.md` — "[NIT:consistency] `Module.Name` doc comment contradicts the no-caller-names rule".
  Verified: the `Name` bullet told the implementer to write `("webster", "burler")` into a production doc comment, which card 1(c) and card 3's own closing rule forbid.
  The bullet now states the carve-out does not exist: `Name`'s doc comment names no CLI and lists no example value, exactly as the four fields below it already do.
- `20260908-150909-plan-review-r5.md` — "[NIT:consistency] `cmd/lyx/prerunlogging_test.go` left un-retargeted and overstated".
  Verified on both halves.
  The file's header and failure message name webstercli's and burlercli's `wireStandalone` as performing the redirect, which this batch relocates;
  card 7 now retargets both onto `cliwire.ResolveStandalone`, and the file moves into card 7's `Edits:` and the overview's All Files Touched.
  The three verify rationales overstated what it guards — it is a source-level guard that root's pre-run emits no Info-or-above record before `seedStencils`, not an assertion of the redirect — and all three now say that.
- `20260908-150909-plan-review-r5.md` — "[NIT:consistency] Card 5 prescribes a helper it then says nothing calls".
  Verified: `hash8For` was left in the helper list after round 4 added `hash8AndStateDir` and said every `ResolveStandalone` case calls the latter.
  Dropped `hash8For` from card 5, with a note that Go reports no error for a dead test helper.

## Pushed Back

_None._
