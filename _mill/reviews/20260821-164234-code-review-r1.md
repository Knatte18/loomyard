MILL_REVIEW_BEGIN
# Review: loom: convert to a Shed recipe — holistic

```yaml
duration_s: 211.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-21
```

## Findings

None. I read the overview, all six batch files, and every listed source/plan/manifest/doc file (recipe file and embed package, `internal/loomrecipe`'s five production/test files, `internal/loomshed`'s post-deletion production and test files, `internal/loomcli`'s wiring/cli/drive/pause/status/run and their tests, `internal/shedrecipe`'s registry/entries/env/recipe.go and both enforcement tests, `internal/shedbuild`'s doc.go and fixture_test.go, `CONSTRAINTS.md`, `manifest/roadmap.md`, `manifest/designs/shed-recipe.md`, `manifest/designs/loom.md`, `manifest/parallel-work.md`, `docs/overview.md`, and the two "intentionally deleted" testdata/test files) and found no deviation from the approved plan.

Specific things I checked and confirmed match:

- `contracts/recipes/loom-recipe.yaml`/`recipes.go` carry the thirteen rows verbatim, no `Plan-Sweep` row, correct production header, no registry type in `recipes.go`.
- `internal/loomrecipe.New` performs the StatusPath/StatusLockPath coherence check ahead of `Parse`/`Build`, uses `shedbuild.Parse` (never `Load`), never calls `Check`, and wraps errors with the exact `"loomrecipe: "` prefix described in card 3.
- Row-1 substitution (`shed.Producers[0].Producer = fakeAlwaysDoneProducer{}`) is applied per-`New`-call, not per-test, at every `Run`-driving site across `shape_test.go`, `sequence_test.go`, and `resume_test.go` — including the two builds inside `TestResume_DoesNotRestartAtRowOne`/`TestResume_PauseStopsAtBoundaryAndClearsFlag`, and the single shared `countingProducer` instance reused (not re-created) across both builds in `TestResume_CrashRecoveryRecallsUnconditionally`.
- `internal/loomshed`'s seam-enforcement allowlist shrank by exactly `internal/landingshed` (7 entries left), matching the package's actual production imports (`shedengine`, `shedadapters`, `websterengine`, `loomengine`, `planparser`, `batcher`, `state`) confirmed by reading every remaining production file.
- `internal/loomshed/cancellation_test.go` correctly reuses `fakeWebsterRun` from the still-present `webster_test.go` in the same package rather than redeclaring it, and its reduced fixture drops exactly the fields the plan names.
- `internal/loomcli`'s `wire`/`cli.go`/`drive.go`/`pause.go`/`status.go`/`run.go` are fully repointed onto `c.env`/`c.shedPaths`; `WebsterRun` is filled explicitly; `Landing` is deliberately left unfilled with the required comment; `TestVerbRefusals` and `wiring_test.go` are repointed exactly as specified, including the new `TestWire_WebsterRunIsFilled` and the restated `TestWire_CwdIsToldToTheEnv`.
- `loomshed.New`/`loomshed.Deps` are fully deleted with no residual references outside historical/plan-doc prose; the doc-comment sweep (shedcheck/doc.go, shedrecipe/recipe.go and entries_simple.go, preflightshed/preflight_test.go, shedbuild/fixture_test.go, loomcli/smoke_test.go) is complete and each repointed reference resolves to a real file/symbol.
- `CONSTRAINTS.md`'s three edits are all present and internally consistent (Shed Recipe Registry Invariant's two-home pointer, Told-Geometry Invariant's twelve-enumeration count, and the new Recipe-Format Sole-Parser Invariant marked review-obligation).
- `manifest/roadmap.md` moves the item to Done, corrects all four falsified Done entries identified in the plan, deletes the now-empty Planned heading, and adds the `landing: parent-fabric resolution chain` item ahead of `loom: real LLM producers` with the required framing (not blocking the five LLM-producer tasks).
- `docs/overview.md` adds `internal/loomrecipe` to the module tree and loom's module line without opening a `contracts/` region in the tree, per the plan's explicit carve-out.
- Two legitimately-unchanged historical references (`preflightshed/preflight_integration_test.go`'s "former copy" comments and `loom-status-spec.md`'s live `loomshed/seed.go` reference) are correctly left alone.
- `internal/shedbuild/equivalence_test.go` and `internal/shedbuild/testdata/loom-recipe.yaml` are gone; the remaining seven `shedbuild` test files and its production files carry no behavioural change, consistent with the `no-production-change-to-the-consumed-packages` Shared Decision.

## Verdict

APPROVE
Full six-batch implementation matches the plan's cards, shared decisions, and doc/constraint edits with no found deviation.
MILL_REVIEW_END
