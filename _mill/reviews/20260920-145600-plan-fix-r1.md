# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260920-144720-plan-review-r1.md`

## Fixed

- `20260920-144720-plan-review-r1.md` — "Card 29 deletes cancellation_test.go on a false \"no other subject\" premise".
  Verified accurate: `TestCancellation_RealProducersReturnErrorNotStuck` drives five producers, and `NewBatchifier`, `NewWebsterProducer`, and `NewLoomPreflight` all survive this task.
  Card 29 now reduces the file to those three rather than deleting it, and corrects its own doc comment.
  The deletion also silently orphaned three package-level fixture helpers the batch-3 gates test depends on, so card 29 now carries `validDecisionRecord`, `writeDiscussionFixture`, and `seedPlanValidateFixture` into a new `internal/loomshed/fixture_test.go` before deleting their hosts, and batch 3's card 20 names them explicitly as the helpers to reuse.
- `20260920-144720-plan-review-r1.md` — "Card 29's moved-helper requirement omits its own source files from Context".
  Verified accurate: both files appeared only under `Deletes:`, which does not tell the implementer to read them, and the card requires carrying their doc comments over verbatim.
  Both are now in card 29's `Context:` alongside their `Deletes:` entries, with the duplication's reason stated in the card, and the two producer test files plus `webster_test.go` are listed too, since the helper relocation and the fixture reduction both read them.

## Pushed Back

_None._
