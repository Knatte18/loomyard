# Plan fix — round 3

Source review: `_mill/reviews/20260831-123641-plan-review-r3.md`

## Fixed

- `[BLOCKING:design]` "Card 14's readback compares a socket-baked literal across separate test runs" — verified against `internal/reedengine/server.go`'s `ServerName`, which derives the socket key from a SHA-256 of the hub's absolute path, and `hubforge.NewHub`, which builds a fresh temp hub per test: the measuring run's socket and the treatment run's socket differ, so byte-equality against the frozen record was unimplementable for candidate 1. Card 14 in `03-repaint-entry.md` now: (a) redefines `assertRepaintEntryPresent` to take a `mustContain []string` token list and return the matched entry; (b) specifies branch-A tokens the test computes from its **own live** `h.reedSocket` / `h.reedSession` rather than from the frozen record, which is a strictly stronger check because it proves the entry belongs to this session; (c) keeps exact literal equality for branch B, whose body embeds nothing; (d) states explicitly that neither `repaintHookCommand` nor `resizeRepaintHookCommand` may be exported to make a cross-package recomputation possible, because the byte-exact pin already lives in the right package as card 13's `TestRepaintHookCommand_ReproducesTheMeasuredBody`. Added `internal/reedengine/server.go` and `internal/reedengine/overlay.go` to card 14's `Context:` so the socket and target derivations are readable.
- `[NIT:scope]` "No direct unit test of the `resizeRepaintHookCommand()` Engine wrapper (Branch A)" — card 13 now requires two content-level assertions the plan previously left to position and call count alone: a shared bullet requiring `TestInstallResizePinsLocked_IssuesTheSignalEntryLast` to assert the installed repaint argv's last element equals `e.resizeRepaintHookCommand()` (mirroring `WatchdogOnAlsoInstallsTheSignalEntry`'s existing signal-entry content check), and a branch-A `TestResizeRepaintHookCommand` pinning the wrapper's own composition against `repaintHookCommand(shell.ForGOOS(), e.TmuxPath(), e.Socket(), exactSessionTarget(e.SessionName()))` off Windows and `""` on it, following `TestResizeSignalHookCommand`'s GOOS-skipped shape. The same test also pins that the wrapper's answer does not vary with `e.cfg.Watchdog`, expressing the `repaint-is-independent-of-watchdog` decision as a test.

## Pushed Back

_(none — both findings were verified accurate and neither tripped the harm check.)_
