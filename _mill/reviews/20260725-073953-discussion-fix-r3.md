# Discussion-fix report — round 3

Source review: `_mill/reviews/20260725-073849-discussion-review-r3.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- [NOTE] "Guard test self-exclusion unspecified" — added a per-file self-exclusion to the
  invariant guard-test bullet (Testing): the scan excludes its own guard-test source file,
  matching the tierpurity / hermeticenv / sandbox-coverage guard precedent.
- [NOTE] "devbin vs deploy repoRoot derivation depth" — stated that the single repo-root
  derivation lives in `tools/internal/devbin` and that `tools/deploy` sources its repo root
  from `devbin.RepoRoot()` (for both the `-dest` build root and the `-dev` target) rather than
  keeping a second `runtime.Caller` derivation; recorded the differing Caller depth. Updated
  both the shared-devbin-helper decision and the `tools/deploy/main.go` technical-context bullet.

## Pushed Back

_None — both NOTEs were factually accurate with no harm from fixing; applied per the
mill-receiving-review fix-everything default._
