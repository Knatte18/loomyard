# Discussion fix — round 6

Source review: `_mill/reviews/20260831-152420-discussion-review-r6.md` (APPROVE, 0 blocking, 2 NIT).

## Fixed

- r6 `[NIT:design]` "Dedup matching is not defined as case-folding" — the "Duplicate repo refs are deduped silently" decision now states the comparison key (lowercased ref), which spelling is forwarded into the `repo:` qualifier (the caller's, from the surviving first occurrence), and what the emitted first field carries (the API's `.repository.full_name`, so output is canonically cased either way). The rationale records why exact-string comparison would reintroduce the exact double-spend the decision exists to prevent.
- r6 `[NIT:scope]` "No scenario pins all-preflights-before-any-search" — a scenario is added where preflight of repo **2 of 3** fails and the call log is asserted to contain zero search calls, which is what separates all-preflights-first from per-repo interleaving; the existing preflight-failure scenarios fail on repo 1 and cannot. A companion case-insensitive-dedup scenario is added alongside it.

## Pushed Back

(none — both findings verified accurate against the discussion and the source, and neither fix broke existing functionality, conflicted with a documented design decision, or destabilised work outside this task's scope)
