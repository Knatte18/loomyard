# Plan fix — round 1

Source review: `20260828-084356-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 2 NIT).

## Fixed

- `20260828-084356-plan-review-r1.md` — "[BLOCKING:design] Stale resize-pin hook survives applyLayoutLocked's guard-skip paths" — fixed as a plan-omission, using the reviewer's FIRST suggested remedy (verify the sole-pane behaviour), not its second.
  Re-verified live on tmux 3.6 in this worktree: `resize-pane -t %0 -y 1` against a one-pane 24-row window exits 0 and leaves the pane at 24 rows — a silent no-op.
  Added the Shared Decision `guard-skip-leaves-a-stale-array-deliberately` to `00-overview.md`, stating both guard cases' dispositions and the explicit rejection of clearing ahead of the guards.
  Extended batch 2 card 5's doc-comment requirement to make `applyLayoutLocked` state the same, and to forbid implementing the rejected alternative.
  Extended batch 2 card 7's test requirement so the guard-skip assertion explicitly covers "no clear either, so a previously installed array survives".
  Extended batch 3 card 10 so `doc.go` records the install-nothing paths and why both are safe.
- `20260828-084356-plan-review-r1.md` — "[BLOCKING:design] Card 9's \"restore window-size to its prior value\" names no mechanism" — accurate; the prose named no mechanism and no capture-then-restore idiom exists in the package.
  Replaced with the reviewer's second named option: `set-option -uw -t <window target> window-size`, plus the matching `set-hook -u` clear.
  Verified live on tmux 3.6: after `set-option -w window-size manual`, the `-uw` unset exits 0 and `#{window-size}` reads back `latest`.
  Card 9 now forbids adding a capture-then-restore idiom.
- `20260828-084356-plan-review-r1.md` — "[NIT:scope] A few Requirements reference identifiers from files outside that card's Context/Edits" — accurate.
  Added `internal/reedengine/parse.go` to cards 3, 5, 6, 7, 8, 9; `internal/reedengine/lock.go` to cards 4, 5, 6, 7; `internal/reedengine/render/types.go` to cards 4 and 6; `internal/reedengine/state.go` to cards 5, 7 and 10.
- `20260828-084356-plan-review-r1.md` — "[NIT:scope] Card 7 says \"extend the existing hermetic fixtures\" for apply_test.go, but no call-recording fixture exists there yet" — accurate; `apply_test.go` has no recorder today.
  Reworded card 7 to say a NEW recorder is added there, modelled on `attach_test.go`'s `attachRecorder`/`newAttachHook` pair, and named the shape it must have.

## Pushed Back

- `20260828-084356-plan-review-r1.md` — "[BLOCKING:design] Stale resize-pin hook survives applyLayoutLocked's guard-skip paths", second suggested remedy only ("have the guard-skip paths also issue the unconditional clear") — conflicts with a documented design decision.
  `_mill/discussion.md`'s Decision `hook-failure-is-non-fatal-everywhere` explicitly evaluates and rejects it: "Rejected: moving the `set-hook -u` clear ahead of the guards so every call site clears.
  It would strip the pins from exactly that untracked-but-running session, and a clear with no rebuild behind it is strictly worse than a slightly stale array."
  The finding's underlying observation is accepted in full and fixed via the remedy the same decision sanctions — the plan now carries the disposition and the live verification the reviewer asked for.
