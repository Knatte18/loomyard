MILL_REVIEW_BEGIN
# Review: Shed: outer phase-FSM skeleton — holistic

```yaml
duration_s: 423.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-15
```

## Findings

### [NIT:consistency] Card 30 misdescribes the location of the sentence to reword
**Demoted-from:** BLOCKING
**Location:** batch 06-docs-reconciliation.md, Card 30, "The execution-stack section" bullet
**Issue:** The card says "the sentence immediately after that block currently claims the cross-OS spawn primitive is the one remaining internal, non-CLI layer of the stack" — but that sentence ("The cross-OS spawn primitive `proc` is the one remaining internal (non-CLI) layer — the base of the stack…", `docs/overview.md:303-304`) actually sits at the *end of the Modules section*, before the `## Execution stack` heading and well before the layered-stack code block, not after it. The text immediately after the code block is "The whole stack runs **headless** (auto mode)…", which makes no such claim.
**Fix:** Correct the location in the requirement (e.g. "the sentence at the end of the Modules section, immediately before the Execution stack heading") so the implementer edits the actual contradicting sentence instead of scanning past the code block and finding nothing to change there.

## Verdict

APPROVE
Otherwise thorough and decision-faithful; one factual location error in Card 30 risks the required doc correction being skipped.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
