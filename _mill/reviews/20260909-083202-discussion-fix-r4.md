# Discussion fix — round 4

Source review: `_mill/reviews/20260909-083052-discussion-review-r4.md` (APPROVE, 0 blocking / 2 nit, neither demoted).

## Fixed

- r4 "[NIT:decision] Follow-up record has no durable destination" — confirmed the premise against the repo: `git ls-tree origin/main _mill/` is empty, so `_mill/discussion.md` is task-branch-only and does not survive `mill-merge`. Added a bullet to §scope-planglyph-only naming the durable destination as the implementation commit's own message body, with the reasoning (`CLAUDE.md` puts hardening-level notes in git history and module docs; a commit-message paragraph is durable on `main`, adds no diff, and forces no roadmap move the `no-doc-changes` decision rules out) and an explicit instruction that the plan must specify that paragraph. Recorded why a wiki task was not chosen: a one-line hardening fix earns no `manifest/roadmap.md` item, and a wiki task without one has nothing to point at.
- r4 "[NIT:consistency] Table column labels are checkIDs, not emitted Check values" — added a "Reading the table" paragraph under the drift-guard table stating that the columns name `doneCheckEntry.checkID` switch arms rather than `Finding.Check` strings, that `rename-not-done-old` and `rename-not-done-new` both emit `Check: "rename-not-done"` (`donecheck.go:205`, `donecheck.go:214`), and that the two are separable only by detail string or by the driving entry — so a plan writer cannot mistake the columns for `Check`-string assertions.

## Pushed Back

(none — both findings were factually correct against the source and both were fixed.)
