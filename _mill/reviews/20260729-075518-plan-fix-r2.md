# Plan fix report — round 2

Source review: `_mill/reviews/20260729-075040-plan-review-r2.md` (verdict: REQUEST_CHANGES, 3 BLOCKING, 3 NIT).

## Fixed

- `_mill/reviews/20260729-075040-plan-review-r2.md` — "[BLOCKING] Card 11's config-key rename breaks template_test.go in the same package": confirmed `internal/boardengine/template_test.go`'s `TestConfigTemplate_HasRequiredKeys`/`TestConfigTemplate_ResolvesToDefaults` assert the old `home`/`sidebar`/`proposal_prefix` keys and defaults, and would fail batch 3's own plain `go test` gate. Fixed by adding the file to Card 17's Edits with exact old/new key and default-value assertions.
- `_mill/reviews/20260729-075040-plan-review-r2.md` — "[BLOCKING] Config-key rename leaves stale board.yaml fixtures outside boardengine/boardcli": confirmed all three named locations (`cmd/lyx/main_integration_test.go`, `internal/ideengine/menu_test.go`, `internal/boardengine/boardtest/bench_test.go`'s `seedWiki` raw string). Ran a full-repo grep during verification and found three MORE genuine hits the review didn't enumerate: `cmd/lyx/exitcode_test.go`'s `setupBoardConfig` (exercises a real board write command, confirmed by its own doc comment), `internal/configcli/configcli.go`'s help-text example, and `internal/configcli/configcli_test.go`'s partial-seed fixture. Also checked `internal/configengine/config_test.go`'s three `home:`-containing hits and confirmed they are self-contained synthetic fixtures with zero reference to `boardengine`/`ConfigTemplate()` — a false positive, deliberately left untouched. Fixed by widening Card 17 to a repo-wide sweep covering all six genuine files (three named by the review, three found independently), and updating batch 3's Batch Scope paragraph to no longer claim the batch is "entirely internal to internal/boardengine" (true for production code, not for test-fixture fallout) plus noting `pipeline.done_gate` as the backstop for the two touched packages (`internal/ideengine`, `internal/configcli`) that have no dedicated per-batch `verify:` anywhere in this plan.
- `_mill/reviews/20260729-075040-plan-review-r2.md` — "[BLOCKING] Card 3's Requirements cite two style-reference files missing from Context": confirmed Card 3 names `weftgit_pathspec_integration_test.go`/`weftgit_unborn_warp_test.go` as style references in its Requirements prose but omits both from Context. Fixed by adding both to Card 3's Context.
- `_mill/reviews/20260729-075040-plan-review-r2.md` — "[NIT] store.go's lock-file doc comment goes stale after Card 13's rename": confirmed `swapLockSuffix`'s doc comment names `tasks.json.lock` by value. Fixed by adding `internal/boardengine/store.go` to Card 13's Edits with the exact rewording instruction.
- `_mill/reviews/20260729-075040-plan-review-r2.md` — "[NIT] Card 25's non-test-file count for internal/boardengine omits template.go": confirmed the package has 9 non-test files, not 8 (missing `template.go`, the `//go:embed` accessor). Fixed by correcting the count and file list.
- `_mill/reviews/20260729-075040-plan-review-r2.md` — "[NIT] Card 32's fold-in text conflates 'host repo' with the wiki-hosting repo": confirmed against `board-weft-storage.md`'s actual text (lines 7-8) — it's the wiki-*hosting* repo (board's own repo in the old model), never the host/warp repo, that needs to be public. Fixed by rewording Card 32's summary to "whichever repo hosts the wiki," with an explicit note that the source doc never claims the host/warp repo needs to be public.

## Pushed Back

(none — all six findings were factually accurate; the review's second BLOCKING finding was expanded beyond its own three named files after independent verification surfaced three additional genuine hits)
