# Discussion fix — round 5

Source review: `20260831-103129-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r5 · "Call-site map overstates when an attach installs" — corrected `repaint-is-independent-of-watchdog` and the matching Constraints bullet: the array is (re)established only by an attach whose chain *succeeds* or by a non-`SkipFocus` apply, and under `watchdog: off` a no-TTY or chain-suppressed attach runs the clear and returns without rebuilding, re-emptying the array.
  This text is designated for verbatim copy into `doc.go`, so the inaccuracy would have propagated.
- r5 · "Windows assertion placed on a pure function" — split the assertion in Testing: `resizePinHookArgvs` takes command strings and holds no `runtime.GOOS` branch, so it asserts only "an empty body emits no entry";
  the `""`-on-Windows assertion moves to the engine wrapper's test in `windowsize_test.go`, following the GOOS-skipped test that already covers `resizeSignalHookCommand`.
- r5 · "Measurement gate ordering vs. candidate 1's prerequisites" — added an explicit mechanism to the measurement gate: a candidate is measured by writing its body into the array directly from the smoke scenario via the same `set-hook` rewrite the control already uses, so no production code (and no `internal/shell` change) is written before the gate authorises it.
  The accepted body string is then pinned by a pure unit test asserting the builder produces what the scenario measured.

## Pushed Back

(none — all three findings verified accurate against source and fixed under the default rule)
