# Discussion-fix report — round 1

Source review: `_mill/reviews/20260630-192906-discussion-review-r1.md` (APPROVE, 2 NOTE findings)

## Fixed

- "hubgeometry line 93 err-path leak left unaddressed" — clarified the "Raw git stderr
  leak fixed at the source" decision: `hubgeometry.go:96` now changes to bare
  `ErrNotAGitRepo` (no appended text), and `hubgeometry.go:93` (the `err != nil`
  Go-level exec-spawn-failure branch) is explicitly called out as out of scope and
  unchanged, since its `%v` is Go's own subprocess error, not git's raw stderr.
- "Existing status/prune/reconcile tests mask the ToSlash change" — added an explicit
  note to the Testing section that the new no-backslash assertions must check the raw
  JSON field string directly, not through a `filepath.Clean`/`filepath.FromSlash`
  wrapper, since `Clean` re-normalizes forward slashes to backslash on Windows and would
  silently defeat the assertion.

## Pushed Back

(none)
