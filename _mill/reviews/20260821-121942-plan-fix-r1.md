# Plan fix — round 1

Source review: `20260821-121844-plan-review-r1.md`

## Fixed

- `20260821-121844-plan-review-r1.md` — "[BLOCKING:design] Doc claims four disk-touching constructors, only three".
  Verified against source: `bouncerEntry` performs both effects (`os.MkdirAll` on its resolved run dir, plus `shedadapters.NewBouncer`'s eager rubric probe), `burlerRoundEntry` creates a run dir only, `singleLLMEntry` probes a stencil only, and `publishEntry`/`finalizeEntry` touch no disk at construction.
  Three constructors, four effects — the discussion's own "four of the twelve registry entries" wording was the miscount this plan inherited.
  Reworded batch 1 card 1's package-doc requirement and batch 2 card 6's `Build` godoc requirement to say three constructors producing four effects, naming the bouncer constructor as the one doing both.
  Batch 2 card 10's own wording was already accurate and was left alone.

- `20260821-121844-plan-review-r1.md` — "[BLOCKING:scope] Card 7 Context omits shedcheck.Check's defining file".
  Verified: `Check`'s signature and parameter order live in `internal/shedcheck/check.go`, which appeared in no card's `Context:` anywhere in the plan, while card 7's requirement mandates the exact call `shedcheck.Check(producers, r.Entry, r.Terminals)`.
  Added `internal/shedcheck/check.go` to card 7's `Context:`.

- `20260821-121844-plan-review-r1.md` — "[NIT:consistency] Duplicate-key rejection misattributed to KnownFields(true)".
  Verified against `gopkg.in/yaml.v3@v3.0.1`'s `decode.go`: `newDecoder` sets `uniqueKeys: true` unconditionally, and the three duplicate-key rejection sites gate on that field, not on the `KnownFields` flag.
  The behaviour this plan relies on is real and was confirmed empirically before the plan was written;
  only the stated cause was wrong.
  Reworded batch 1 card 2's rationale to attribute the rejection to the library's own default duplicate-key detection, with an added sentence stating explicitly that it is independent of `KnownFields(true)`.
  Card 3's test requirement never attributed a cause, so it needed no edit.

## Pushed Back

_(none — every finding was factually accurate and none of the three fixes breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this plan.)_
