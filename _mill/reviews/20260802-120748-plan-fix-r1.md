# Plan fixer report — round 1

Source review: `_mill/reviews/20260802-120324-plan-review-r1.md` (verdict REQUEST_CHANGES, 4 BLOCKING + 2 NIT). All six findings verified accurate → all FIXED, none pushed back.

## Fixed

- **[BLOCKING] doc.go's SnapshotWarpSHA references go stale, no card fixes them** (`20260802-120324-plan-review-r1.md`) — Confirmed `internal/fabricengine/doc.go` names `Fabric.SnapshotWarpSHA` at lines 84/86/90/92/102/104 ("the sole exported entry point"). Added `internal/fabricengine/doc.go` to batch 5 card 19's Edits with a requirement to update all six mentions to the unexported `snapshotWarpSHA` name and drop the "exported entry point" framing.
- **[BLOCKING] SyncWeft/RevertWithWeft doc-comment cascade misses 3+ files** (`20260802-120324-plan-review-r1.md`) — Confirmed live `RevertWithWeft` comment mentions at `checkout_index_refresh_test.go:9`, `commit_partial_integration_test.go:26`, `revert_test.go:2`, and `StatusWeft` mentions at `diff.go:4`/`:57`. Added the three test files to batch 5 card 20's Edits (comment rewords), reconciled batch 5 card 18's "revert_test.go stays UNCHANGED" to "test bodies stay; header comment reworded in card 20", and added `diff.go` to batch 6 card 24's Edits to strip its `StatusWeft` mentions (cross-referenced with the StatusWeft deletion).
- **[BLOCKING] CONSTRAINTS.md's own Fabric Git Invariant text goes stale mid-task** (`20260802-120324-plan-review-r1.md`) — Confirmed `CONSTRAINTS.md:92` ("Orchestration, not agent") names `fabricengine's SyncWeft/CommitWeft` and the board carve-out names `CommitWeftAt`/`PushWeftAt` as current. Added `CONSTRAINTS.md` to batch 6 card 26's Edits with a requirement to remove the deleted `SyncWeft`, fix all `CommitWeft`/`CommitWeftAt`/`PushWeftAt` casing, reflect that board now routes through `Bolt`, and grep the section for any remaining stale exported name.
- **[BLOCKING] No card adds regression coverage for the new both-sides Clean check** (`20260802-120324-plan-review-r1.md`) — Moved `internal/loomengine/preflight_integration_test.go` from Context to Edits in batch 4 card 16 with an explicit requirement to add a dirty-weft-only case and a both-dirty case (and assert the missing-weft path still surfaces `CheckWeftPairing`, not a `Clean` error). Updated the batch's "Batch Tests" prose to drop the "if needed" hedge and cite card 16. This directly serves the "verify over plan review" principle — the genuinely-new weft-dirty failure mode now ships with positive coverage.
- **[NIT] weftgit.go file header still lists StatusWeft after its removal** (`20260802-120324-plan-review-r1.md`) — Added a requirement to batch 6 card 24 to strip `StatusWeft` from `weftgit.go:1`'s verb enumeration when the method is deleted.
- **[NIT] Card 8's unused-import list omits path/filepath** (`20260802-120324-plan-review-r1.md`) — Confirmed `filepath.ToSlash` is used only inside the deleted `weftPathspecBase`. Updated batch 2 card 8's enumerated import-removal list to include `path/filepath`.

## Pushed Back

(none)
