# Fixer report: plan-fix r2

Source review: `20260919-100734-plan-review-r2.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- r2 holistic — `[BLOCKING:design] Card 15's three new fixture helpers are unnamed` — verified accurate: card 15 described all three only by role, while every other card in the plan names its new symbols and cards 16 through 19 must call these three. Card 15 now names and shapes each — `newReapFixture(t *testing.T, pairNames ...string) reapFixture` with its index-aligned `reapFixture` struct given inline, `compressedReapTiming() watchdogTiming`, and `orphanWorktree(t *testing.T, worktreeRoot string)` — requires `t.Helper()` on all three, and states that `newReapFixture` builds its engines through the package's existing `watchdogIntegrationEngine` rather than a second construction helper. The three names are also stated once in `## Batch Scope` so the consuming cards share them. `internal/hubforge/hub.go` added to card 15's `Context:` for the now-named `hubforge.NewHub` call.
- r2 holistic — `[NIT:consistency] ReapSession's addition makes ListSessions' own doc comment false` — verified accurate against `internal/reedengine/overlay.go` line 106, which reads "It is the one engine-less, exported function in this package". Card 2 now requires that sentence rephrased in the same edit that adds `ReapSession`, keeping the still-true reason (the daemon enumerates a hub socket before any `Engine` exists to bind to) and dropping the only-one claim, with the rest of the comment left alone.

## Pushed Back

_None. Both findings verified accurate against the repository, and neither fix triggered a harm-check escape._
