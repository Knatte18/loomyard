# Fixer report: plan-fix round 3

Source review: `_mill/reviews/20260825-070825-plan-review-r3.md`

## Fixed

- `20260825-070825-plan-review-r3.md` — "[NIT:consistency] Stale `fetchOldRedditHTML` doc comment not scheduled for update" (demoted from BLOCKING) — verified against `plugins/prowler/fetch.go:136-138`; both the "avoids gating" premise and the "Reports false" return description become false in card 8. Card 8 now requires rewriting that doc comment.
- `20260825-070825-plan-review-r3.md` — "[BLOCKING:design] \"credentials absent\" adapter test's tier-2 stub outcome unspecified" — verified; the sub-test only reaches tier 3 (where the credential names appear) if tier 2 also fails, and its raw `fetcher` literal needs `doNoRedirect` wired per card 7's no-nil-fallback rule. Card 9 now states both explicitly and says why.
- `20260825-070825-plan-review-r3.md` — "[NIT:consistency] New files lack the module's per-file header-comment convention" — verified; every existing `.go` file in the module opens with a descriptive header comment. Cards 2 and 4 now require one on each new file, each citing an existing file as the style reference. Card 10 already required a header comment modelled on `browser_integration_test.go`.
- `20260825-070825-plan-review-r3.md` — "[NIT:scope] Batch 4's offline-suite/selftest re-run isn't in either card's Requirements" — verified; the instruction lived only in `## Batch Tests`, which nothing enforces. Folded into card 11's Requirements as its first step, and `plugins/prowler/scripts/selftest.sh` added to that card's `Context:`.

## Pushed Back

_(none)_
