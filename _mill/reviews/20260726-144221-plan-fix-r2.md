# Plan fix report — round 2

```yaml
slug: fabric-cutover
round: 2
review_file: 20260726-143536-plan-review-r2.md
reviewer_model: sonnetmax
date: 2026-07-26
```

## Fixed

- **[BLOCKING] Card 27 Tier-2 grep gate unreachable — unswept `_test.go` comments** — VERIFY:
  confirmed. 14 surviving `_test.go` files name deleted modules in comments while card 23's
  sweep excluded `_test.go` and card 27's gate greps `--include='*.go'` tree-wide. Verified all
  14 are **comment-only** (zero code imports of a deleted engine — no build impact). HARM
  CHECK: none. Fixed by distributing the sweeps to owning cards (no new card, no gate
  renumber): 8 fabricengine/fabriccli test files -> card 23 (Edits + grep scope now includes
  `_test.go`); `hubgeometry/siblinglayout_test.go` -> card 25; `reedengine/config_test.go` ->
  card 26; `initengine/undo_test.go` -> card 2; `loomengine/testmain_test.go` -> card 5;
  `perchcli/run_integration_test.go` -> card 8; `lyxtest/leaf_enforcement_test.go` doc
  comments -> card 17 (extended). Card 27's note now enumerates that test coverage so the
  tree-wide `--include='*.go'` gate is genuinely reachable. All 13 added to All Files Touched.
- **[BLOCKING] Card 12 misses second test using the removed `warp` module** — VERIFY:
  confirmed. `configcli_integration_test.go` has `TestE2ESyncIntegration` (dispatches `"warp"`
  at :86, reads `ConfigFile(".","warp")` :95, asserts `module=="warp"` :149) AND
  `TestDispatchSet_PreservedKeyDetectedByReconcile` (seeds+dispatches `"warp"` :170/:177,
  asserts `mod["module"]=="warp"` :213). After card 9 removes `warp` from the registry, any
  lingering `"warp"` dispatch returns not-found -> exit 1 -> `t.Fatalf`. Fixed card 12 to
  enumerate BOTH tests and require every `"warp"` string in the file move to `"fabric"`.
- **[NIT] Card 11 import guidance imprecise** — VERIFY: confirmed (`configreg_test.go` imports
  only `weftengine`, line 8; needs `fabricengine` added for the re-pointed
  `ConfigTemplate()` assertion). Fixed card 11 wording to "drop the `weftengine` import and add
  `fabricengine`."

## Pushed Back

_None. All three findings were factually accurate and none conflicted with a documented
design decision or destabilised other work._
