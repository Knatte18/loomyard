MILL_REVIEW_BEGIN
# Review: Shed recipe: loader/builder — holistic

```yaml
duration_s: 446.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-21
```

## Findings

### [NIT:consistency] Duplicated implementation-detail claim across two doc comments
**Location:** batch 1 card 1 (`doc.go`) / batch 2 card 6 (`build.go`) **Issue:** both cards independently spell out the identical fact — which of the three registry constructors (bouncer, burler-round, single-LLM) creates a run dir vs. probes a stencil, "four distinct effects" — in `shedbuild`'s package doc and again in `Build`'s own doc comment; a future change to `bouncerEntry`/`burlerRoundEntry`/`singleLLMEntry`'s construction-time side effects has two independent doc sites to update, not one. **Fix:** none required now (both cards are self-contained and verified accurate against `entries_bouncer.go`/`entries_burler.go`/`entries_singlellm.go`); worth having `build.go`'s comment point at the package doc rather than restate it, next time either is touched.

### [NIT:scope] Roadmap Done-entry insertion point unspecified
**Location:** batch 4 card 18 **Issue:** the card says to move the item into the `## Done` group but never says where; the two related, already-shipped Shed-recipe pieces ("Shed recipe: engine registry", "Shed-setup validity checker") sit at the very top of Done, and an unspecified insertion point risks the new entry landing far from them even though numbering itself is cosmetic per the file's own Maintenance section. **Fix:** none blocking — note for the implementer to place it adjacent to the other two Shed-recipe Done entries for readability.

## Verdict

APPROVE
Plan is internally consistent, fully grounded against source, DAG/numbering/file-touched sets are clean, and no constraint violations found.
MILL_REVIEW_END
