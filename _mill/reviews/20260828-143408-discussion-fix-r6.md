# Discussion fix — round 6

Source review: `_mill/reviews/20260828-143241-discussion-review-r6.md` (REQUEST_CHANGES, 1 blocking, 1 NIT).

## Fixed

- r6 · "[BLOCKING:design] \"Permanent\" premise fails under rename-back" — accurate, and the premise was mine. Replaced the park-on-`ctx.Done()` decision with a DORMANT mode: 60s cadence on the proven-gone sentinel, one `Warn` on entry, automatic return to the PREVIOUS mode (poll or signal) when the worktree root comes back. Section renamed to `### the-watch-loop-goes-dormant-rather-than-parking`; parking recorded as rejected with the rename-back reason; recovery and log-once behaviour added to Testing; Scope updated.
- r6 · "[NIT:consistency] Q&A still claims standalone target \"always present\"" — struck the superseded claim from the `AnchorPath` vs `WorktreeRoot` Q&A entry and pointed it at the round-5 entry that supersedes it.
- Follow-through beyond the two findings: five other passages still described the watchdog as parking permanently (stat-error semantics, the shape-check decision, the empty-`WorktreeRoot` test case). All reworded to dormancy, so the file carries one model rather than two.

## Pushed Back

(none)
