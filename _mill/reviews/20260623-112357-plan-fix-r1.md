# Plan fix report — round 1

Source review: `20260623-112131-plan-review-r1.md` (verdict REQUEST_CHANGES, 2 BLOCKING, 3 NIT).

## Fixed

- `20260623-112131-plan-review-r1.md` — "[BLOCKING] board_test.go facade tests rely on
  BOARD_SKIP_GIT env": verified (`board_test.go:19/53/80` use `New(cfg)` + `t.Setenv` on
  write paths; Tier-1, in batch-2 verify scope). Broadened **card 9** to migrate
  `board_test.go` (`TestUpsertTask`/`TestRerender`/`TestHealthCheckPasses`) to
  `cfg.SkipGit = true`; added the file to card 9 Edits and to All Files Touched; softened the
  overview "Single production env read" Decision to state facade tests set cfg explicitly.
- `20260623-112131-plan-review-r1.md` — "[BLOCKING] Facade benchmarks in bench_test.go also
  lose env suppression": verified (`bench_test.go:182 BenchmarkUpsertFacade` uses `New(cfg)`).
  Broadened **card 9** to convert `BenchmarkUpsertFacade` to `cfg.SkipGit = true`; documented
  that store-level benchmarks' `b.Setenv` is vestigial and `seedWiki` is unaffected. Added
  `bench_test.go` to Edits + All Files Touched.
- `20260623-112131-plan-review-r1.md` — "[NIT] Stale doc references to deleted
  tests/benchmarks": broadened **batch-1 card 2** to also update `boardtest/doc.go` (package
  comment), `docs/benchmarks/board-performance.md` ("## Push access" section), and
  `cmd/testtiming/main.go:93` ("real git + network" string). Added all three to Edits + All
  Files Touched; added `go build ./...` to batch-1 verify so the testtiming edit is compiled.
- `20260623-112131-plan-review-r1.md` — "[NIT] Card 12 mischaracterizes one swapped call
  site": reworded **card 12** to note `weft_test.go:254` (`TestWeftRollbackOnPostHostCreateFailure`)
  uses `rollbackAdd` directly, not `Add`, and is safe because it never pushes the weft branch.
- `20260623-112131-plan-review-r1.md` — "[NIT] Card 8 should explicitly drop the helper's
  t.Setenv": reworded **card 8** to name `newSyncRepo`'s line-29 `t.Setenv("BOARD_SKIP_GIT","")`
  as an explicit deletion.

## Pushed Back

(none)
