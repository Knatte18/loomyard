MILL_REVIEW_BEGIN
# Review: fabric: collapse external API surface onto Commit — stop leaking warp/weft — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-02
```

## Findings

### [NIT] Stale capitalized `CommitWeftAt` mention in doc.go
**Location:** `internal/fabricengine/doc.go:52-53`
**Issue:** Package doc still says "the package-level `CommitWeftAt` function ... see `CommitWeftAt`'s own doc comment" — that symbol was renamed to `commitWeftAt` in batch 1 (card 4); this passage wasn't in that card's edit list and was missed by every later doc-cascade card too.
**Fix:** Update the two mentions to `commitWeftAt`.

### [NIT] doc.go's cross-repo-ops enumeration omits Diff/Status
**Location:** `internal/fabricengine/doc.go:6-7`
**Issue:** "adds a small set of genuinely cross-repo operations (`Commit`, `Pull`) on top of what gitrepo deliberately doesn't know about" — `Diff`/`Status` are equally cross-repo ops (and are described as such later in this same file, lines 78-80) but aren't listed here. `internal/fabricengine/fabric.go:7`'s equivalent sentence was correctly updated to include them; this doc.go instance was missed.
**Fix:** Add `Diff`, `Status` to the enumeration.

### [NIT] Stale capitalized `Fabric.SnapshotWarpSHA` mentions in index.go
**Location:** `internal/fabricengine/index.go:191,202` (and similar)
**Issue:** Comments still read "Fabric.SnapshotWarpSHA" after card 19 unexported it to `snapshotWarpSHA`; `index.go` was in card 19's Context list but not its Edits list, so these were never updated even though they now name a symbol that doesn't exist under that casing.
**Fix:** Reword to `snapshotWarpSHA`.

### [NIT] fabriccli/fabric.go verb enumeration omits `diff`
**Location:** `internal/fabriccli/fabric.go:5`
**Issue:** "The weft-git content-sync verbs (status, commit, push, pull, sync) are wired in by weft_verbs.go" omits the new `diff` verb added by card 22, even though two lines below (line 10) the package doc correctly says "16-verb" tree.
**Fix:** Add `diff` to the parenthetical list.

### [NIT] Stale `RevertWithWeft` mention in design doc
**Location:** `manifest/designs/fabric-unified-view.md:117`
**Issue:** The slice-6 "DONE" bullet says "the same nearest-surviving-anchor logic `RevertWithWeft` already used" — `RevertWithWeft` was deleted in batch 5 (card 18); cards 20/29's doc-cascade cleanups didn't cover this specific passage (only lines ~113 and ~130 were in card 29's scope).
**Fix:** Reword to reference the surviving `resolveRevertTarget`/`classifyCorrespondence` path instead.

## Verdict

APPROVE
Renames, deletions, migrations, and cross-batch contracts are all correctly and thoroughly implemented; only cosmetic doc-comment drift remains.
MILL_REVIEW_END
