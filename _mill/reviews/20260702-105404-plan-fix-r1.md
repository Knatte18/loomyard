# Plan fix — round 1

Source review: `_mill/reviews/20260702-105255-plan-review-r1.md` (verdict: APPROVE, 2 NIT findings).

## Fixed

- **"Card 9 references test fns from an unlisted file"** (Batch 3 / Card 9) —
  added `internal/configcli/configcli_test.go` to Card 9's `Context:` so the
  two test-function names it cites (`TestConfigLong_MentionsEditorFallbackAndSet`,
  `TestConfigLong_ContainsModuleNames`) resolve to a listed, readable file.
- **"No end-to-end test for the 'reconcile detects drift' half"** (whole
  plan) — extended Card 10 (`internal/configcli/configcli_test.go`) with
  `TestDispatchSet_PreservedKeyDetectedByReconcile`, an end-to-end test that
  chains a preserving `--set` call into `lyx config reconcile` and asserts
  the preserved orphan key is reported in `removed`, mirroring
  `reconcile_test.go`'s `TestReconcile_DryRun` git-repo setup pattern. Added
  `internal/configcli/reconcile_test.go` to Card 10's `Context:` as the
  pattern reference.

## Pushed Back

None — both findings verified accurate on inspection, both fixes additive
with no conflicting design constraint or existing-functionality risk.
