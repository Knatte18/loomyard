MILL_REVIEW_BEGIN
# Review: Prefer raw fetch, scope large tree listings — holistic

```yaml
duration_s: 221.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-31
```

## Findings

None.

Verified end-to-end: `github-tree.sh`'s flag/`--`-terminator parser, `--children` mode, and incremental entry-count guard match cards 1–3 exactly (hoist order of `output=()`/`emit`, mode-aware abort wording, strictly-greater-than-after-append semantics, base-10 `MAX_ENTRIES` normalisation). The stub `gh`'s absolute-path fixture support, `gen_tree_body`, and the 45 harness scenarios in `github-tree-selftest.sh` trace correctly against the fixtures (`children-src-nonrec.json`'s blob/tree/commit/blob ordering, `trunc1-*` incremental-abort math for test 31, boundary tests). `github-read.sh`'s prerequisite/parse/validate scaffolding, raw-attempt argument vector, and `gh api` fallback (probe → content fetch, three-source status diagnosis) match cards 9–11, and `CAPTURE.md` correctly discloses the two unconditional/three conditional/one best-effort capture dispositions with private-repo redaction while naming the public `Knatte18/quarry` target. `github-read-selftest.sh` and its stub `gh`/`curl` (cards 12–15) reproduce the argument vector, byte-fidelity, cleanup, and status-extraction-ordering assertions correctly, including the curl-free-stub-dir construction distinct from the tree harness's empty-PATH trick. `SKILL.md` and `README.md` (cards 16–17) document the finished CLI surface consistently with the shipped scripts — the read script's signature, the guard's abort shape, the corrected per-script dependency scoping — and the symlink caveat sentence is correctly omitted per `CAPTURE.md`'s "Unresolved" disposition. `All Files Touched` and the `Files included` manifest match the actual creates/edits with no unaccounted files. No constraint violations: this is a Bash/Markdown-only change outside the Go module and the GitHub Auth Invariant's scope, consistent with the plan's Shared Decisions and prior precedent in `github-tree.sh`.

## Verdict

APPROVE
Implementation matches the plan across all three batches with no design, scope, decision, or consistency defects found.
MILL_REVIEW_END
