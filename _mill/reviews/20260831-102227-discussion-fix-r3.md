# Discussion fix — round 3

Source review: `_mill/reviews/20260831-102050-discussion-review-r3.md` (holistic, APPROVE, 0 BLOCKING / 4 NIT).

## Fixed

- r3 holistic — "[NIT:consistency] 40-row clamp threshold contradicts its own arithmetic" (demoted from BLOCKING): recomputed against `render/height.go`'s `stackHeights`. At 40 rows the stack box is 38, five strips plus the active pane leave usable 33 against demand 30 (active pane 3, no clamp); six strips give usable 32 against demand 36. Corrected "five-deep" to six, restated all three thresholds with their arithmetic inline, and defined "N-deep" explicitly as counting collapsed strips with one active pane below and a 1-row header plus divider above.
- r3 holistic — "[NIT:consistency] Integration run: required to land, or not required?" (demoted from BLOCKING): resolved to one disposition — the tagged tier IS a landing gate. Narrowed the Test Tier Purity Invariant line to "the untagged tier stays tmux-free", added a new `integration-run-is-a-landing-gate` decision, and made Testing state the gate is "the tagged suite runs green" (the `t.Skipf` on an absent multiplexer still satisfies it), citing that `pipeline.done_gate` is already `go test ./... && go test -tags integration ./...`.
- r3 holistic — "[NIT:scope] `lock_test.go` missing from the must-not-move enumeration": verified `internal/reedengine/lock_test.go:60,98` sets `CollapsedStripRows: 2`. Added it to the no-edit enumeration, which now covers every `CollapsedStripRows` site in the repo.
- r3 holistic — "[NIT:consistency] Overstated claim about sibling knob comments": verified against both templates — only `mouse` and `watchdog` carry the "keeps whatever value it holds" caveat, `debug_log` carries a missing-key caveat instead, and `width`/`height` carry none. Split the claim into "every knob carries a rationale" (true) and "the adoption caveat comes from `mouse`/`watchdog` specifically" (the precise version), matching Scope.

## Pushed Back

(none — all four findings verified accurate against the source, and no fix broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task's scope.)
