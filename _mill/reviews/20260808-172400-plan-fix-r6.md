# Plan fix — round 6

Source review: `_mill/reviews/20260808-172139-plan-review-r6.md` (holistic, APPROVE, 0 BLOCKING / 2 NIT).

## Fixed

- `20260808-172139-plan-review-r6.md` — "[NIT] pull.go's RelPath-blind-scope paragraph leaves a stale identifier name".
  Verified: that paragraph reads "the pathspec is the bare `patternDirName` at the weft worktree root", naming the very const card 7 deletes, and card 7's instruction to adjust "the path names inside" the paragraph did not reach the identifier.
  Card 7 now calls out the `patternDirName` mention explicitly alongside the `_pattern` path text, with the reason stated: a doc comment naming a deleted const is a dangling reference, not a wording preference.
- `20260808-172139-plan-review-r6.md` — "[NIT] add_test.go comment rewrite (card 21) only half-corrects a pre-existing inaccuracy".
  Verified: `IsReservedHubName` (`internal/fabricengine/junctionnames.go:152-156`) folds `structuralCommittedDirs` in directly, so `_lyx` is reserved regardless of the injected pathspec — the comment's claim was already wrong for `_lyx` before this task, and card 21 flagged only the newly-false `_pattern` half.
  Card 21 now requires all three corrections (the `_pattern` half, the pre-existing `_lyx` half, and dropping `_raddle` from the `HubReservedNames()` list), with an explicit note that leaving the `_lyx` half would preserve a known error in a comment the card is already rewriting.

## Pushed Back

None.
Both NITs were factually accurate and both fixes are confined to `_mill/plan/`.

## Re-validation

`_plan_dag.validate` — OK.
`_plan_validate.run` — 0 findings.

## Outcome

Round 6 verdict was APPROVE with 0 BLOCKING.
Both NITs applied, so `approved: true` is set on `plan/00-overview.md` and the plan proceeds to Handoff.
