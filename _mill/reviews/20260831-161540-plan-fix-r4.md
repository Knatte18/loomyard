# Fixer report: plan-fix round 4

Source review: `_mill/reviews/20260831-160928-plan-review-r4.md`

## Fixed

- `20260831-160928-plan-review-r4.md` — "[NIT:decision] Card 16's symlink caveat has no disposition for \"unresolved\"" (demoted from BLOCKING): card 16's conditional now branches on all three outcomes card 8's capture can record. The unresolved case omits the sentence, same as the non-2xx case, with the reason stated — a caveat asserting behaviour nobody observed would be worse than none — and a pointer to card 8's own record, which carries the open question forward for a later task.
- `20260831-160928-plan-review-r4.md` — "[NIT:consistency] Card 7's final scenario reuses a \"-rec\"-suffixed fixture for a non-recursive endpoint": the combined-flags scenario now maps `children-src-nonrec.json`, which is added to card 7's `Context:`, and the Requirements states why — so the harness's `-rec`/`-nonrec` suffix keeps meaning which endpoint shape a fixture represents. `scoped-src-rec.json` stays in `Context:` for the terminator scenario, which addresses a recursive endpoint.

## Pushed Back

_(none — both findings verified accurate with no harm from fixing.)_
