MILL_REVIEW_BEGIN
# Review: self-report Tier 1: Go-detected structural anomalies — holistic

```yaml
duration_s: 173.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-12
```

## Findings

### [BLOCKING:scope] SubpathAnchored guard is missing two assertPath rows card 3 requires
**Location:** `cmd/lyx/constructoranchoring_test.go:148-158`
**Issue:** Card 3 requires "add the matching assertPath lines and map entries for both accessors to all three sites that currently list `loomengine.LoomBootstrapLock`." Those three sites are `TestConstructorAnchoring_Unanchored`'s assertPath block (line 97), `TestConstructorAnchoring_SubpathAnchored`'s assertPath block (line 155), and the `dotLyxConstructors` regression map inside `TestConstructorAnchoring_SubpathAnchored` (line 176). `LoomSelfreportFiled`/`LoomSelfreportFiledLock` were added to the first site (lines 98-99) and the third (lines 177-178), but not to the second: the `.lyx` group's `assertPath` block in `TestConstructorAnchoring_SubpathAnchored` (lines 152-158) lists only `LoomStatusLock`, `LoomRunLock`, `LoomDriverLog`, `LoomBootstrapLock`, then jumps to `websterengine.PromptsDir`. The `dotLyxConstructors` map that follows only checks a path prefix (not exact join arithmetic), so at the subpath-anchored fixture the two new accessors' exact byte-for-byte path construction is unpinned — a filename typo in either accessor (e.g. a wrong literal) would still pass every test in this file at that fixture, since only the unanchored fixture's `assertPath` calls check the literal join.
**Fix:** Add `assertPath(t, "loomengine.LoomSelfreportFiled", ...)` and `assertPath(t, "loomengine.LoomSelfreportFiledLock", ...)` rows to `TestConstructorAnchoring_SubpathAnchored`'s `.lyx` group block, mirroring the two rows already present in `TestConstructorAnchoring_Unanchored`.

## Verdict

REQUEST_CHANGES
One card-3 test-coverage requirement (all three `LoomBootstrapLock`-listing sites) is only two-thirds implemented.
MILL_REVIEW_END
