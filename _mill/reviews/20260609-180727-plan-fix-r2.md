# Plan Fix — round 2

Source review: `20260609-180510-plan-review-r2.md`

## Fixed

- **[BLOCKING] Card 1 lock API description lists wrong signatures** — Corrected signatures in Card 1 to `(*FileLock, error)` for both Acquire functions; described `Release` as a pointer-receiver method `func (l *FileLock) Release() error`, not a free function.
- **[NIT] Cards 2, 9, 10, 11 describe non-existent Release(...) free-function pattern** — Removed `Release(...)→lock.Release(...)` replacement instructions from Cards 2, 9, 10, 11; added note that existing `.Release()` method calls need no transformation.
- **[NIT] Variable `lock` shadows imported `lock` package in board.go and sync.go** — Changed import in Cards 9, 10, 11 to use alias `flock` (`flock "github.com/Knatte18/mhgo/internal/lock"`); updated all `lock.*` call-site replacement instructions to use `flock.*` prefix.
- **[NIT] Card 16 suggests os.Unsetenv violating shared decision** — Removed the `os.Unsetenv` option from `TestLoadConfig_FallbackPathResolution`; kept only the CI-safe env var name approach.

## Pushed Back

_(none)_
