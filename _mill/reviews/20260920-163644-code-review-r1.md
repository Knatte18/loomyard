MILL_REVIEW_BEGIN
# Review: Producer gates: mechanical gates before session release — holistic

```yaml
duration_s: 284.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-20
```

## Findings

### [BLOCKING:scope] Stale-mention sweep is not actually empty
**Location:** `docs/overview.md:325`, `internal/loomshed/gates.go:120`, `internal/discussionparser/validate_test.go:315-320`
**Issue:** Batch 5's card 36 and batch 6's card 44 both define the sweep's own exit criterion as an *empty* repo-wide grep for the five removed-row strings, with any survivor being "a file the sweep missed rather than an acceptable survivor." A case-insensitive grep for `discussion-validate|plan-validate|plan-revalidate` is not empty: `docs/overview.md:325` still reads `Consumed by \`loomshed.discussionValidate\` and by the \`lyx loom validate-discussion\` verb` — a deleted Go identifier, present-tense, inside a file that IS in card 39's own edit list, so this line was missed rather than out of scope. `internal/loomshed/gates.go:120` reads "the disposition the **standing** plan-validate producer takes" — present tense, describing a producer deleted in batch 5 — and this file is not in card 42's file list at all. `internal/discussionparser/validate_test.go:315-320`'s doc comment still describes current behaviour as "loomshed's Discussion-Validate row maps that to Stuck and bounces to Discussion-Write, respawning on the same document until the bounce budget escalates" — that row and that bounce-based recovery no longer exist; the real current behaviour is an in-session re-prompt loop against the row's own gate. All three read as live, present-tense descriptions rather than history, which is exactly the failure mode the plan's own rationale calls "worse than no pointer."
**Fix:** Re-point `docs/overview.md:325` at `loomshed.NewDiscussionGate`/the Discussion-Write and Discussion-Burler gates; rewrite `gates.go:120`'s "the standing plan-validate producer takes" to past tense naming the removed row; rewrite the `validate_test.go` comment to describe the gate's re-prompt loop rather than a bounce to Discussion-Write. Likely root cause: the sweep method used case-sensitive matching on capitalized identifiers (`DiscussionValidate`/`PlanValidate`) and missed lowercase-initial (`discussionValidate`) and hyphenated-lowercase (`plan-validate`) prose spellings — worth widening the grep case-insensitively before closing the task.

### [NIT:consistency] `gate_attempts: 0` alongside no `gate:` key is silently accepted
**Location:** `internal/shedrecipe/entries_gate.go:51-56`
**Issue:** `resolveGateSpec` treats "`gate_attempts` present with no `gate`" as an author error, but detects presence via `attempts != 0` rather than via `configInt`'s own presence signal. An explicit `gate_attempts: 0` with no `gate:` key therefore passes silently instead of tripping the "requires config key gate" error the doc comment and card 22 promise.
**Fix:** Thread through whether the key was present (e.g. a `configIntPresent` variant or checking `cfg["gate_attempts"]` directly) rather than inferring presence from a non-zero value.

## Verdict

REQUEST_CHANGES
Implementation is faithful to the plan across all six batches; fix the sweep's three missed stale references before landing.
MILL_REVIEW_END
