MILL_REVIEW_BEGIN
# Review: prowler: collapse github-repo-explorer's truncation-fallback tree-walk into one script call — holistic

```yaml
duration_s: 279.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-29
```

## Findings

### [NIT:consistency] 404/409 fixed-wording text differs from card 5's literal quote
**Location:** `plugins/prowler/scripts/github-tree.sh:144` vs `_mill/plan/02-tree-script-and-docs.md:82`
**Issue:** Card 5's fixed wording reads "...not found, not accessible with this token, or has no commits yet...", but the script (and batch 1's `github-tree-selftest.sh:333`, which asserts the substring `may not be accessible`) both say "may not be accessible" — an internal plan/implementation wording drift on the one message class the "error taxonomy is part of the contract" decision is meant to pin exactly.
**Fix:** Reconcile card 5's prose with the actually-implemented/tested wording (or vice versa) so the fixed contract reads identically everywhere; no functional impact since the harness's substring check already matches the shipped wording.

### [NIT:consistency] Queue item is a 4-tuple, not the "triple" card 5 specifies
**Location:** `plugins/prowler/scripts/github-tree.sh:184-186,206,237` vs `_mill/plan/02-tree-script-and-docs.md:98-99`
**Issue:** Card 5 describes each queue item as "a tab-separated triple of a mode, a ref, and a path prefix," but the implementation carries a fourth field, `kind` (`root`/`child`), needed to route `fetch()`'s root/child-specific 404 handling correctly through the `nonrec` fallback re-enqueue. This is functionally necessary and verified by the harness (root vs. child 404 messages are correctly distinct), but it is an undocumented deviation from the card's literal schema description.
**Fix:** Update card 5's queue-item description to a 4-tuple (or note the deviation explicitly) so a future reader isn't misled by the "triple" wording; no code change needed.

### [NIT:consistency] New README prose doesn't fully follow the semantic-line-break convention
**Location:** `plugins/prowler/README.md:33-34`
**Issue:** CLAUDE.md's markdown rule calls for an added break at an internal independent-clause boundary (comma + coordinating conjunction); lines 33 ("...call, and even a repository...") and 34 ("...--jq`, and no system `jq`...") each run such a clause together on one physical line, unlike most of the surrounding file's paragraphs.
**Fix:** Add a line break after the comma-plus-conjunction boundary in each sentence, consistent with the rest of the file.

## Verdict

APPROVE
Both batches match their cards and each other exactly; fixtures, stub, script, and docs are internally consistent and fully tested; only stylistic nits found.
MILL_REVIEW_END
