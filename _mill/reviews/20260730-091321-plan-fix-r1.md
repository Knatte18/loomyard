# Plan-fix round 1 — fabric-commit-api

Source review: `_mill/reviews/20260730-085918-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 5 NIT). Each finding was run through the `mill-receiving-review` decision tree (VERIFY → HARM CHECK → FIX/PUSH BACK); all fixes land only in files under `_mill/plan/`.

## Fixed

- [BLOCKING] "Card 9's self-heal assertion contradicts WeftSHAForWarpSHA" — VERIFIED accurate against `internal/fabricengine/index.go:148-150` (`WeftSHAForWarpSHA` returns `ErrNoCorrespondence` on an index *miss* and only runs `RebuildIndex` on a stale *hit*; a directory-blocked `RecordCorrespondence` writes no entry → miss → no auto-rebuild). Fixed the "self-heals via RebuildIndex" wording in three places: `03-fabric-commit.md` Card 9 scenario (3) now requires an explicit `f.RebuildIndex()` before the lookup and forbids asserting a bare-lookup self-heal; `03-fabric-commit.md` Card 7's `PartialCommitError` semantics clarified (miss vs. stale-hit); `00-overview.md` Shared Decision `partial-failure-report-three-outcomes` reworded to "explicit RebuildIndex, since the auto-rebuild fires only on a stale hit."
- [BLOCKING] "Card 11 Context omits gitrepo's existing fixture helpers" — VERIFIED accurate: `internal/gitrepo/gitrepo_test.go` is `//go:build integration`, package `gitrepo_test`, defining `newRepo`/`writeFile`/`commitAll`; a new `worktree_test.go` in the same package that redeclares them is a compile collision. Fixed in `04-unified-diff-status.md` Card 11: added `internal/gitrepo/gitrepo_test.go` to Context and instructed `worktree_test.go` be package `gitrepo_test` reusing those helpers, no redeclaration.
- [BLOCKING] "classifyPaths comparison under-specifies Windows path-sep handling" — VERIFIED accurate against `internal/fabricengine/fabric.go:105` (`ScopedPathspec` builds prefixes via `filepath.Join`, emitting a backslash on Windows when `relPath != "."`, which the pure classifier is unit-tested with). Fixed in `01-commit-foundations.md` Card 1: require `filepath.ToSlash` normalization of **both** each computed junction prefix and each input path before comparison.
- [NIT] "SkipGit's doc comment goes stale for Fabric.Commit" — VERIFIED accurate against `internal/fabricengine/fabric.go:85` ("Skip all git operations if true", but Fabric.Commit gates only the weft side). Fixed in `05-docs.md` Card 14: the `doc.go` pass now documents that `opts.SkipGit` is weft-scoped for `Fabric.Commit` (warp commit + env-gated async push proceed regardless).
- [NIT] "Card 6 hand-builds a warp fixture CopyHostHub already provides" — VERIFIED accurate against `internal/lyxtest/lyxtest.go` `buildHostHub` (repo + unpushed `init` commit + empty bare origin, no push) and `internal/gitrepo/push.go:61-62,152-156` (`PushCoalesced` performs the first push and establishes tracking even with no upstream). Fixed in `02-async-push-plumbing.md` Card 6: warp side now built via `lyxtest.CopyHostHub` instead of a hand-built `MustRun` sequence.
- [NIT] "Card 5 leaves stale file-header doc comments" — VERIFIED accurate (post-card, `spawnPush` is a one-line delegator and `weft_verbs.go` gains a second hidden flag). Fixed in `02-async-push-plumbing.md` Card 5: added a requirement to refresh both files' header comments (delegation for `spawn.go`; two-flag bypass for `weft_verbs.go`).
- [NIT] "Card 13's exclude-file assertion rests on an unverified go-git behavior" — VERIFIED the assumption HOLDS against vendored go-git v5.19.1: `Worktree.Status()` (`worktree_status.go:165`) calls `gitignore.ReadPatterns`, which reads `.git/info/exclude` first (`plumbing/format/gitignore/dir.go:53-54`, `infoExcludeFile = ".git/info/exclude"`). Fixed in `04-unified-diff-status.md`: Card 11 now cites this confirmed behavior (so no separate exclude filtering is needed) and Card 13 notes its assertion is a regression guard over verified behavior.
- [NIT] "Card 11's raw Lock() is a third, undocumented locking pattern" — VERIFIED accurate against `internal/gitrepo/gogit.go:79-93` (two documented categories: `RLock` plain-ref read, `lookupObjectRetrying` object lookup). Fixed in `04-unified-diff-status.md` Card 11: added `gogit.go` to Edits with a requirement to add a third locking-discipline bullet (`Worktree.Status()` = write-locked, non-retried), and added `internal/gitrepo/gogit.go` to the overview's All Files Touched.

## Pushed Back

_None. Every finding was factually accurate and passed the harm check (all fixes are plan-file clarifications or low-risk doc/fixture edits; no other batch touches the affected files, so none destabilizes other work)._
