# Plan fix — round 1

Source review: `_mill/reviews/20260725-134335-plan-review-r1.md`

## Fixed

- [BLOCKING] "Stale verb Long help text not updated" — card 40 now instructs rewriting the affected verb `Long` strings (`begin-batch`, `validate`) for the flat card-list model, dropping `--restart-chain`/chain/oversized/v2 language, and re-auditing all eight verbs' `Long`; card 39 adds the parent `webster` command `Long`/`Verbs:` re-audit; card 42 asserts no v2 language remains in the `Long` strings.
- [NIT] "validate.go retarget leaves envelope keys unpinned" — card 40 now pins the observable JSON keys: count key `cards` = `len(plan.Cards)` (was `batches`) and finding key `card` = `f.Card` (was `batch`); card 42 asserts them.
- [NIT] "Cited Shared Decision does not exist" — added `### Decision: fork-prompt-plan-level-context` to `00-overview.md` `## Shared Decisions`, matching the citation in cards 27/28 (and 35).
- [NIT] "Stale cross-batch references in prose" — corrected batch numbers: batch 2 scope `9 and 12` → `7 and 9`; batch 3 scope `8 and 9` → `7 and 9`; card 12 `batch 8 adds … batch 12 wires` → `batch 7 (card 26) adds … batch 9 (card 39) wires`.
- [NIT] "Card 32 names gitrepo symbol absent from its Context" — card 32 now captures the per-card SHA via the webster `headSHA` helper (already in-Context via `gitwrap.go`) instead of `gitrepo.CurrentSHA`.

## Pushed Back

_None — all five findings were factually accurate and low-harm; every one was fixed per the receiving-review default rule._
