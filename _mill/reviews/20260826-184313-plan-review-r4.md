MILL_REVIEW_BEGIN
# Review: loom's status file can conflict on the landing merge — holistic

```yaml
duration_s: 441.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-26
```

## Findings

### [NIT:consistency] Stale "dirties the weft" claim survives in a sibling test's doc comment
**Demoted-from:** BLOCKING
**Location:** batch 1 / card 6 (`internal/loomcli/smoke_test.go`)
**Issue:** Card 6 correctly rewrites `TestSmokeBootstrap_CleanlinessOrderingAfterSeedCommit`'s doc comment, dropping its now-false "which would dirty the weft again" claim (line ~657) since the status file no longer lives on the tracked tree. But the identical claim in a different test, `TestSmokeBootstrap_OriginRecordSelfHealsAfterCrashBetweenWriteAndCommit`'s own doc comment (lines 747-752: "...which legitimately dirties the weft again for a reason that has nothing to do with the origin-record healing"), is untouched by any card — the plan demonstrably recognizes this exact stale-claim category and fixes it in one place while leaving the same falsehood standing in the sibling case. That same comment also cross-references the renamed test by its old name ("see TestSmokeBootstrap_CleanlinessOrderingAfterSeedCommit's doc comment"), which will dangle once card 6 renames it to `TestSmokeBootstrap_CleanlinessAfterSeed`.
**Fix:** Add a requirement to card 6 rewriting `TestSmokeBootstrap_OriginRecordSelfHealsAfterCrashBetweenWriteAndCommit`'s comment to drop the weft-dirtying claim (a driver's persists to the now-`.lyx`-rooted status file cannot dirty the weft at all) and to update the cross-reference to the test's new name.

### [NIT:consistency] Card 9 names a nonexistent `phase` field
**Location:** batch 2 / card 9
**Issue:** Card 9 instructs changing the table row's "`phase` field," but the actual Go struct field in `discussiontemplate_test.go`'s `TestLoomTemplateDiscussion_FencesWhatItMayWrite` table is named `phrase` (the struct is `struct{ name, phrase string }`), not `phase`. The intent is unambiguous from context (only one field carries the path literal), so this doesn't block implementation, but it's a misnamed identifier in a Requirements clause.
**Fix:** Correct "phase field" to "phrase field" in card 9's text.

## Verdict

APPROVE
Card 6 misses a parallel stale-comment fix and a dangling test-name cross-reference in smoke_test.go.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
