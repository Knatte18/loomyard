# Plan fix: round 1 — holistic

Source review: `_mill/reviews/20260919-173502-plan-review-r1.md`

## Fixed

- `20260919-173502-plan-review-r1.md` — "Batch 3 & 7 \"Batch Tests\" prose contradicts their own verify".
  Both narratives were written before the `verify-excludes-edited-tagged-test` validator finding forced the `-tags integration ./internal/shedcli/...` clause onto both commands, and neither was updated with it.
  Corrected both to state the integration suite runs in-batch and why: batch 3's card 10 and batch 7's card 30 each edit `internal/shedcli/parity_test.go`, which is `//go:build integration`, so an untagged run would never compile the file whose assertions each batch depends on.
  Batch 3's narrative keeps its separate, still-true claim that `internal/battencli/lifecycle_integration_test.go` is deliberately deferred to batch 8.
- `20260919-173502-plan-review-r1.md` — "Card 8's header-comment edit contradicts its own carve-out".
  Confirmed by grep: `contracts/recipes/lifecycle-recipe.yaml`'s header holds exactly one `Loom-Run` mention, at line 7, and it is the load-bearing-empty-`on_stuck` paragraph the same card carves out for batch 5.
  Dropped the "two mentions" instruction, kept the header's old-filename and `internal/lifecyclerecipe` mentions as the card's real header work, and stated explicitly that the one remaining mention takes the row-name substitution and nothing more.
- `20260919-173502-plan-review-r1.md` — "registry.go's key-count comment goes stale".
  Confirmed: `internal/shedrecipe/registry.go:14` reads "The table is complete at seventeen keys", card 18 adds an eighteenth, and no test pins the literal.
  Added the comment update to card 18's requirements.
- `20260919-173502-plan-review-r1.md` — "Card 34's roadmap edit target text doesn't match current wording".
  Confirmed: `manifest/roadmap.md:20` already reads "the seed's `driver` field selects who steps a run", with no non-existence claim to correct — the instruction described wording that is not there.
  Replaced it with an explicit leave-it-alone note plus a re-read-before-landing check, so the card neither invents an edit nor silently skips the file.

## Pushed Back

_None.
All four findings verified accurate against the cited source, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task._
