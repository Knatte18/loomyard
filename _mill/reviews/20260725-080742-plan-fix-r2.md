# Plan-fix report — round 2

Source review: `_mill/reviews/20260725-080602-plan-review-r2.md` (verdict REQUEST_CHANGES,
2 BLOCKING).

## Fixed

- [BLOCKING] "Guard file trips the Hermetic Git Env guard" — Card 13 now also Edits
  `cmd/lyx/hermeticenv_test.go` to add an `allowedNonHermetic` self-exclusion entry for
  `tools/sandbox/pathresolve_guard_test.go` (its `gitSpawnTokens` also bans `exec.Command`/
  `exec.CommandContext`, so the module-wide walk would otherwise fail). Added
  `cmd/lyx/hermeticenv_test.go` to All Files Touched and updated Batch Tests prose to name both
  module-wide guards.
- [BLOCKING] "resolve.go / devbin.go absent from Context though their symbols are used" — added
  `tools/internal/devbin/devbin.go` to Card 5's Context and `tools/sandbox/resolve.go` to the
  Context of Cards 6, 7, 8, 9, 10, and 11 (every card referencing `resolveLyx`/`prependPath`/
  `sourceDev`/`sourceProd`/`devBinPath`).

## Pushed Back

_None._
