# Discussion fix — round 3

Source review: `_mill/reviews/20260909-082717-discussion-review-r3.md` (APPROVE, 0 blocking / 2 nit, both demoted from BLOCKING by the `blocking_classes: [design]` ceiling).

## Fixed

- r3 "[NIT:consistency] TDD claim contradicts the test's failing mechanism" — §Testing's opening paragraph claimed a TDD red state that cannot exist. Replaced with an explicit statement that there is no TDD candidate: the completeness test is green before and after the `Known()` swap and is a drift guard designed to fail on a future quarry release, not on today's code. The subsection heading "TDD candidate — vocabulary completeness" was renamed to "Vocabulary-completeness drift guard" so the two no longer contradict each other.
- r3 "[NIT:consistency] False claim that `Rejected()` removes the last `.Status` selector" — both statements were factually wrong: `unreadableStatusDetail`'s second branch still returns `fmt.Sprintf("... unrecognized resolve status %q", r.Status)` (`resolve.go:142`), which §Technical context itself confirms by saying both format strings are unchanged. Corrected §comment-prose-updates' Note and §Technical context's tripwire bullet to say the `Status` selector survives in that branch, and dropped the "unused allowlist entry is harmless" rationale as a false premise — the entry stays live.

## Pushed Back

(none — both findings were factually correct against the source and both were fixed.)
