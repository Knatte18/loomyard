# Plan fix — round 2

Source review: `_mill/reviews/20260808-164037-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 0 NIT).

## Fixed

- `20260808-164037-plan-review-r2.md` — "[BLOCKING] pull.go's PatternResidue/PatternResidueEntry doc comments never corrected".
  Verified: `grep -n '_pattern' internal/fabricengine/pull.go` reports lines 67, 73, and 314 in addition to the sites card 7 already named, and no other card in batches 1-7 reaches `pull.go`.
  Card 7 now covers all three explicitly, with the line-314 edit marked **wording only** so `parsePatternResidueRecords`' body, signature, and parsing logic stay untouched and its record-boundary tests remain a regression anchor.
  Added a closing check to card 7: `grep -n '_pattern' internal/fabricengine/pull.go` must return nothing.
- `20260808-164037-plan-review-r2.md` — "[BLOCKING] Batch 4's closing grep-sweep claim is factually wrong and omits the real survivor".
  Verified: `grep -rn 'pattern\.DirName' internal/pattern/` returns zero hits — inside its own package the constant is referenced unqualified as `DirName` (`pattern.go:18,22`, `patternpath_test.go:1`), so the qualified grep can never match there.
  The real survivor at that point in the DAG is `internal/fabricengine/junction_pattern_integration_test.go`, held for batch 5 card 26 by batch 3 card 15's explicit design.
  Corrected batch 4's closing check to name that single expected file, added an explicit "do not fix it early" instruction, and replaced the wrong claim with a note that the qualified grep never reports `internal/pattern/` itself.
  Also strengthened batch 6 card 28, which owns the declaration: it now states the sites are found by the bare identifier rather than the qualified one, and names `patternpath_test.go`'s line-1 header comment as an explicit target rather than leaving it to the blanket "no `_pattern` anywhere in `internal/pattern/`" sentence.

## Pushed Back

None.
Both findings were factually accurate and neither fix breaks existing functionality, conflicts with a documented decision, or destabilises work outside this task.

## Re-validation

`_plan_dag.validate` — OK.
`_plan_validate.run` — 0 findings.
