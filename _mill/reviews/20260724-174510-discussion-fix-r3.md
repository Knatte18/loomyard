# Discussion-fix report — round 3

Source review: `_mill/reviews/20260724-174427-discussion-review-r3.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- [NOTE] "Snapshot FF-only adopt assumes non-divergent history" — added the explicit safe-model
  precondition to the *Snapshot remote sync* decision: snapshot keys track a single,
  monotonically-forward line (never rebased/force-pushed/divergent); divergence/history-rewrite is
  out of the safe model and is caught upstream by `SHAExists` (orphaned SHA → staleness → rebuild),
  not by divergence-aware adopt logic.
- [NOTE] "Board's *.lock glob covers both locks, not just push-lock" — reworded the *Lock ownership*
  decision: board's single `*.lock` glob stays (it covers the `tasks.json.lock` domain mutex);
  nothing is "dropped" from it. How the board rewrite handles `.gitrepo-push.lock` is the later
  board-rewrite task's concern, explicitly out of scope here.

## Pushed Back

_None — both NOTEs were factually accurate and applied under the fix-everything default._
