# Plan fix — round 1

Source review: `_mill/reviews/20260817-151321-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260817-151321-plan-review-r1.md` — "[NIT:consistency] Card 9's \"four siblings\" count is wrong".
  Verified against `cmd/lyx/tierpurity_test.go`: six `allowedSpawners` entries cite `go env GOMOD`, not four.
  Batch 3 card 9 now instructs the invariant text to allowlist the future guard "alongside the entries already there for that same reason" and to state no count at all, so the text cannot drift again as the map grows.
- `20260817-151321-plan-review-r1.md` — "[NIT:scope] Fallback-path non-absence failure wrap is untested".
  Verified: card 2 introduces the `%s config template: %w` wrap keyed on `module`, and card 3's scenario list had no case reaching it.
  Batch 1 card 3 gains a scenario — no `_lyx/` on disk plus a synthetic template carrying a required `${env:NAME}` marker with the variable unset — asserting the error is non-nil, contains `config template:` and the module name, and does not contain `config file`.
  That second half also pins the `fallback-error-messages-name-the-template` decision, which was previously stated in the plan but asserted nowhere.
  Batch 1's `## Batch Tests` section is updated to name the added coverage.

## Pushed Back

_None._
Both findings verified accurate against source, and neither fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task.
