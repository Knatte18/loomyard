MILL_REVIEW_BEGIN
# Review: invariants and docs for the told-geometry rule — holistic

```yaml
duration_s: 438.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (Anthropic), invoked as "Sonnet 5" per harness label
reviewed_file: plan/
date: 2026-08-19
```

## Findings

### [BLOCKING:scope] Batch 4 omits the pre-existing repo-tree gap for `internal/standalonegeom`
**Location:** batch 4 (overview-docs), scope / card 13
**Issue:** `docs/overview.md`'s ASCII repo-tree diagram (under "Weft overlay model" → "### Status", lines ~210-246) already lists `internal/hubgeom` and `internal/preflight` (added by an earlier wave) but never lists `internal/standalonegeom` — verified by direct grep, zero hits for "standalonegeom" anywhere in the file. Batch 4's scope explicitly caps itself at "three targeted edits" (Cwd Resolution Invariant section, `## Modules`, Execution stack) and never revisits this tree, so the plan ships this task's own primary subject (told/hub-mode geometry adapters) with a visibly half-updated illustrative diagram: two of the three sibling packages present, one silently missing.
**Fix:** add a fourth card (or fold into card 13) adding one `internal/standalonegeom/` line to that tree, mirroring the existing `internal/hubgeom`/`internal/preflight` line style.

## Verdict

REQUEST_CHANGES
One real completeness gap in batch 4's `docs/overview.md` file audit; everything else verified accurate against source.
MILL_REVIEW_END
