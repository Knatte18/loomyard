# Plan-fix report — round 1

Source review: `_mill/reviews/20260628-151510-plan-review-r1.md` (verdict REQUEST_CHANGES, 3 BLOCKING + 1 NIT).

## Fixed

- r1 "[BLOCKING] Card 1 rename breaks test files it does not edit" — added
  `internal/board/store_test.go` and `internal/board/cli_test.go` to Card 1 Edits and
  required converting every `s.SetPhase`→`s.SetStatus`, `set-phase`→`set-status`, and
  `phase`→`status` reference in those tests in the same commit. Assigned the `id_or_slug`
  lookup-reference conversion explicitly to Card 2 (which owns the lookup-key change and
  already edits `cli_test.go`).
- r1 "[BLOCKING] Folding `group` into store allowlist invalidates task.go unit tests" —
  Card 4 now requires deleting the `NewTask`/`ApplyPatch` direct `group`-error subtests in
  `task_test.go`, relocating that coverage to `store_test.go`, and updating
  `TestUpsertTaskGroupKeyError`'s exact-message assertion to the new allowlist message.
- r1 "[BLOCKING] Manifest breaks existing single-pass orphan-removal test" — Card 9 now
  requires restructuring the hand-placed `proposal-ghost.md` `TestRenderToDisk` case to a
  pre-seeded-manifest or two-render form, since the manifest only removes files it
  previously recorded.
- r1 "[NIT] Store.MergeTasks signature change not reflected in Card 5 Edits" — added
  `internal/board/store.go` and `internal/board/store_test.go` to Card 5 Edits and named
  the `TestMergeTasks` call site (`s.MergeTasks(..., &[2]any{"c", phase})`) that must be
  converted to the new resolved-selector shape.

## Pushed Back

- None.
