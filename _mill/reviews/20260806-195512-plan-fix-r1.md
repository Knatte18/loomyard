# Plan fix — round 1

Source review: `_mill/reviews/20260806-194139-plan-review-r1.md` (holistic, REQUEST_CHANGES, 5 BLOCKING + 1 NIT).

## Fixed

- r1 holistic — "[BLOCKING] Batch 3 frontmatter cards count is wrong": `03-webster-builder-loom-scratch-seam.md` frontmatter `cards: 9` → `cards: 8`.
  Verified the batch holds exactly cards 15–22.
- r1 holistic — "[BLOCKING] Card 21 missing Context for perchengine.ScratchDir": added `internal/perchengine/identity.go` to card 21's `Context:`.
- r1 holistic — "[BLOCKING] Card 31 missing Context for scoutengine and logger accessors": added `internal/scoutengine/daemonstate.go` and `internal/logger/sink.go` to card 31's `Context:`.
- r1 holistic — "[BLOCKING] Card 20's stated internal/lock rationale is factually wrong": confirmed the finding against the code — `internal/lock/lock.go` contains no `MkdirAll`, and `internal/state/state.go`'s `ReadJSONStrict` doc says outright "Unlike ReadJSON, does not create missing parent directories".
  Card 20 now requires an explicit `os.MkdirAll(filepath.Dir(LoomStatusLock(l)), 0o755)` before the read, cites `ReadJSONStrict`'s doc comment and `internal/reedengine/lock.go`'s existing MkdirAll-before-lock workaround as the in-repo analog, explains why the old coincidence (lock beside `status.json` under `_lyx`) breaks once the lock moves, and names the regression it prevents (Preflight escalating a missing-`.lyx` worktree to a hard infra error instead of honouring its report-not-error contract).
  `internal/reedengine/lock.go` added to the card's `Context:`.
- r1 holistic — "[BLOCKING] Batch 7 never edits CONSTRAINTS.md despite the Shared Decision": took the fix rather than the escape — added card 44 to batch 7 (`docs: record the structural-directories and never-committed routing invariant`), editing `CONSTRAINTS.md` in the same batch that introduces the structural-directory sets, the third `classifyPaths` bucket, `Commit`'s hard error and the `--exclude-standard` fix.
  Batch 7's frontmatter `cards:` 8 → 9;
  batch 8's cards renumbered 44–52 → 45–53 (with its internal `card N` cross-references updated) so global card numbering stays gap-free and later-batch numbers stay above earlier ones.
  Batch 8's docs card (now card 52) no longer restates the structural-directories clause — it extends it only where this batch's wiring changes what it says — and keeps the junction-exclude, unwire-preservation and hub-`.lyx` clauses.
  The `docs-land-with-their-change` Shared Decision's claim of five CONSTRAINTS-editing batches (1, 5, 6, 7, 8) is now true as written.
- r1 holistic — "[NIT] Card 29's 'three buildOptions call sites' undercounts": confirmed 6 invocation sites in `internal/scoutcli/cli.go` (two per verb — single-arg and batch-mode — at lines 189/204, 335/350, 437/460; line 491 is the definition).
  Card 29 now says "every `buildOptions` call site — six in total, two per verb (the single-arg path and the batch-mode path) across the `references`, `definition` and `symbol` verbs", and additionally requires each verb to derive `anchorRoot` alongside its existing `worktreeRoot` line.

## Pushed Back

_None._
Every finding verified accurate against the code, and no fix triggered a harm category (nothing breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task).

## Post-fix validation

`_plan_dag.validate` passes;
`_plan_validate.run` reports 0 errors (one transient `context-completeness` hit on the new card 44 was fixed by adding `internal/fabriccli/cli_test.go` to its `Context:` before this report was written).
