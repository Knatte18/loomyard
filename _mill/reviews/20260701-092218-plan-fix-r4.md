# Plan fix — round 4

Source review: `_mill/reviews/20260701-092211-plan-review-r4.md` (verdict
REQUEST_CHANGES, blocking_count 1, nit_count 1).

## Fixed

- **[BLOCKING] `TestUnwireJunctions_RealDirectoryGuard` likely never reaches the
  real-directory branch** (`03-warpengine-unwire-junctions.md`, Card 7) — verified
  Card 6's mandated check order (target-resolution via `EvalSymlinks` before the
  `IsLink` check, itself a round-1 fix) means this test would hit the
  missing-target branch instead of the intended real-directory branch, since it never
  created the weft-side target. Updated the test spec to first create
  `l.WeftLyxDirFor(slug)` directly (no full `Add`/`WireJunctions` needed) so
  `EvalSymlinks` succeeds, then create the real host-side directory — now genuinely
  exercising the `fslink.IsLink == false` branch.
- **[NIT] `init`'s `Short` stays silent on `--undo` while behavior becomes
  bidirectional** (`04-initcli-undo.md`, Card 8) — updated `Short` to `"scaffold
  _lyx/config/ in the current directory (or reverse it with --undo)"` for `--help`
  subcommand-listing discoverability, keeping `Long` as the detailed documentation.

## Pushed Back

None — both findings were factually accurate and low-harm to fix.
