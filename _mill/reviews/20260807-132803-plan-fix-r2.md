# Plan fix — round 2

Source review: `_mill/reviews/20260807-132555-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NITs).

## Fixed

- r2 holistic — "[BLOCKING] Card 9's scratchDir-resolution placement contradicts its own use sites": confirmed against `internal/treadleengine/run.go` — the "Seam defaulting happens here, once, at Run's entry" block sits after the deferred terminal `clearPauseFlag` closure, `os.MkdirAll(runDir, ...)`, `lock.TryAcquireWriteLock` and the entry-time `clearPauseFlag`, all four of which card 9 retargets onto `scratchDir`.
  Card 9 now requires `scratchDir` to be resolved as `Run`'s **first statement**, explicitly ahead of the deferred closure and every other retargeted site, and explicitly says the `pause`/`runCommand` defaulting block stays where it is rather than being hoisted.
  Added the note that when `e.scratchDir` is empty the two `MkdirAll` calls name the same directory and the second is a harmless no-op, and that `runDir`'s own MkdirAll stays because `state.json` and round artifacts still live there.
  The batch-local decision in `## Batch Scope` carried the same wrong placement and was rewritten to match.
- r2 holistic — "[NIT] Card 28's 'nine sites' count is off by one": confirmed 10 production join sites (`lifecycle.go:545,676,778,961`, `lock.go:66`, `spawn.go:143,183`, `strand.go:309,348,471`).
  Corrected "nine" → "ten" in both places card 28 states the count;
  the per-file enumeration was already right and is unchanged.
- r2 holistic — "[NIT] Shared decision overclaims post-batch-1 literal exclusivity": took both halves of the offered fix rather than only softening the wording.
  Card 2 now also retargets `FindBaseDir`'s two `_lyx` format-string operands (`"not initialized: _lyx/ directory not found"`, `"stat _lyx: %w"`) onto `lyxdirs.LyxDirName`, since a format-string operand is not a path-construction context and so is invisible to `TestEnforcement_GeometryLiterals`.
  Added the constraint that the `"not initialized"` prefix must stay byte-identical, because three packages substring-match on it in their own `LoadConfig` (`internal/fabricengine/config.go`, `internal/loomengine/config.go`, `internal/perchengine/config.go`) — all three added to the card's `Context:` so the implementer can verify rather than trust.
  The `lyxdirs-is-the-only-declarer` Shared Decision now scopes its claim to declaration plus path-construction and message-formatting contexts, and states outright that doc-comment prose is out of scope and that this matches exactly what the machine guard polices.

## Pushed Back

_None._
All three findings verified accurate against the code, and no fix triggered a harm category.

## Post-fix validation

`_plan_dag.validate` passes;
`_plan_validate.run` reports 0 errors.
