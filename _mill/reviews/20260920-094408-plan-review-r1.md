MILL_REVIEW_BEGIN
# Review: Seeded driver choice: ly-drive strand as the child's driver — holistic

```yaml
duration_s: 404.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (Claude Sonnet, per the system prompt's own model-ID disclosure)
reviewed_file: plan/
date: 2026-09-20
```

## Findings

### [BLOCKING:scope] Card 14's signature change breaks `internal/loomcli/arm.go`'s own call site
**Location:** batch 4 / card 14 (sharedbootstrap.go) **Issue:** `loomPreStep` in `internal/loomcli/arm.go:363` calls `_, stage, err := c.seedAndCommitBootstrap(slug, c.parentFlag)`; card 14 grows this function's return arity by one ("return the effective driver … alongside the values it already returns"), which is a compile error at that second call site (verified: `internal/loomcli/arm.go` line 363, current 3-value destructure), and no card in the plan lists `internal/loomcli/arm.go` in its `Context:`/`Edits:` anywhere (grepped the whole plan directory — the only `arm.go` hits are `internal/battencli/arm.go`, a different file). **Fix:** Add `internal/loomcli/arm.go` to card 14's (or a new card's) `Edits:`/`Context:` and update the `loomPreStep` call to discard the new fourth return value.

### [NIT:consistency] Card 14 grows a return value that card 15 never consumes
**Demoted-from:** BLOCKING
**Location:** batch 4 / card 14 vs card 15 **Issue:** card 14's own rationale for the new return is explicit — "so card 15's branch consumes it directly rather than performing a second read of the file this function just wrote" — but card 15's requirements instead say to "Read the seed with the shedrun read function over the shedrun seed-path constructor … immediately after the existing bootstrap-lock and status-strand work," i.e. exactly the second `shedrun.ReadSeed` call card 14 says the new return value exists to avoid. One of the two cards is wrong about the design. **Fix:** Either have card 15 consume `seedAndCommitBootstrap`'s returned driver value and drop its own `ReadSeed` call, or drop card 14's added return value and its stated rationale.

### [NIT:scope] Card 10's "30000 bytes" fact lives in a file outside its Context
**Location:** batch 4 / card 10 (driverprompt.go) **Issue:** the byte cap is declared as `maxLaunchPromptBytes` in `internal/shuttleengine/claudeengine/command.go`, not in the `claudeengine.go` file card 10 lists in `Context:` — verified via grep (`command.go:24: const maxLaunchPromptBytes = 30000`). **Fix:** add `internal/shuttleengine/claudeengine/command.go` to card 10's `Context:`, or cite the constant name explicitly rather than the bare number.

## Verdict

REQUEST_CHANGES
Card 14's signature change omits `internal/loomcli/arm.go`, breaking batch 4's own build.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
