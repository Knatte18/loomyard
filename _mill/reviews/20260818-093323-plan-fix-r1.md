# Plan fix — round 1

Source review: `_mill/reviews/20260818-093240-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 0 NITs).

## Fixed

- `20260818-093240-plan-review-r1.md` — "[BLOCKING:scope] Batch 8 deletes websterCLI struct fields two untouched test fixtures still reference".
  Verified accurate against the live source: `internal/webstercli/cli_test.go`'s `newTestCLI` and `internal/webstercli/verbs_test.go`'s `websterCLI{...}` literal both set `layout`, `planDir`, `websterDir`, `websterScratchDir`, `reportsDir` and `promptsDir` as composite-literal keys, and neither file appeared in any batch-8 card's `Edits:` — card 37 touches `cli_test.go` only for the `fabricSync` signature, and `verbs_test.go` not at all.
  Added both files to card 36's `Edits:` and extended its `Requirements:` to convert the two fixtures in the same card, deleting the six stale keys and letting each fixture's already-present `geom` supply those paths, while keeping every existing expectation and the subpath-anchored pre-run case intact.
  Both paths were already in the overview's All Files Touched (batches 5 and 7 edit them), so that list is unchanged.

## Pushed Back

_(none — the single finding was accurate and the fix carries no harm.)_
