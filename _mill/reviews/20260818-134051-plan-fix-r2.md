# Fixer report: plan-fix round 2 — the standalone CLI path

```yaml
round: 2
review: 20260818-134051-plan-review-r2.md
fixed: 3
pushed_back: 0
```

## Fixed

- `20260818-134051-plan-review-r2.md` — "[BLOCKING:design] Batch 5 mirrors batch-4 output without a declared dependency": added `4` to batch 5's `depends-on` in both the overview Batch Index and `05-perchcli-standalone-entry.md`'s own frontmatter.
  Verified accurate — cards 20, 21, 24 and 25 each list a file batch 4 creates (`internal/burlercli/wiring.go`, `cli.go`, `wiring_test.go`, `cli_integration_test.go`), while batch 5 declared only `[1, 2]`.
  Took the reviewer's first offered fix rather than its second: card 20's nested-`burlerengine.New` requirement is stated as byte-identical to what `burlercli` builds for the same target, which is a real semantic coupling rather than stylistic mirroring, so de-referencing burler from perch's cards would leave the same dependency in a subtler, undeclared form.
  Recorded the reasoning in batch 5's Batch Scope and corrected batch 4's scope note, which claimed the two batches "run in parallel" — now false.
  Batches 1, 2 and 3 still parallelise; only 4 → 5 serialises.

- `20260818-134051-plan-review-r2.md` — "[BLOCKING:scope] `shuttleengine.LoadConfig`'s declaring file missing from Context": added `internal/shuttleengine/config.go` to batch 4 card 13's and batch 5 card 20's `Context:` lists.
  Verified accurate — both cards call `shuttleengine.LoadConfig` twice each while listing only `internal/shuttleengine/run.go`, which declares `NewRunner`.
  `reedengine.LoadConfig`'s and `burlerengine.LoadConfig`'s declaring files were already listed in both, so this was the odd one out.

- `20260818-134051-plan-review-r2.md` — "[NIT:consistency] Batch 3 doesn't apply its own env-redirect decision to pre-existing tests" (demoted from BLOCKING by the stage ceiling): extended batch 3 card 12's Requirements to add `t.Setenv("LOCALAPPDATA", t.TempDir())` alongside the existing `XDG_STATE_HOME` redirect in `TestRunCLIIn_StandalonePreRun_ReachesRunsOwnValidationGate` and `TestRunCLIIn_StandalonePreRun_TargetDirectoryUnchanged`.
  Verified accurate — both pre-existing tests reach `wireStandalone` and redirect `XDG_STATE_HOME` only, so on Windows `standalonestate.Derive` still reads the operator's real `LOCALAPPDATA`.
  The overview's Shared Decision says "new or pre-existing", and batch 4 card 16 and batch 5 card 23 already do exactly this for their own packages, so `webstercli` — the package that shipped standalone first — was the sole gap.
  Fixed under the default rule rather than treated as optional, per `mill-receiving-review`.

## Pushed Back

_None._
