# Fixer report: plan-fix round 1

## Fixed

- `20260820-092443-plan-review-r1.md` — "[NIT:consistency] overview.md placement instruction rests on a false adjacency premise": verified accurate against `docs/overview.md` (the `internal/landingshed` line and the `internal/preflight` line are four entries apart, with `mergeresolve`, `hubgeom` and `standalonegeom` between them, so no single position is adjacent to both).
  Card 30's placement instruction now names one unambiguous position — immediately above the `internal/preflight` line, the layer it wraps — and states outright why the other neighbour is not co-located, so the next reader does not re-derive the same ambiguity.

## Pushed Back

_None._
