# Discussion fix — round 5

Source review: `_mill/reviews/20260906-122837-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, none demoted).
Auto mode: every finding is FIX by rule;
PUSH BACK is unavailable with no operator present.
This is the first round whose findings carry no `Demoted-from: BLOCKING` marker, so the convergence gate is satisfied and the review loop closes here.

## Fixed

- "[NIT:consistency] \"never invented\" rule cannot cover creel struct".
  Verified accurate: `doc-carries-real-go-listings` asserted that every listed signature is "transcribed from the shipped code or derived from it, never invented", while listing the creel message struct — a module that does not exist and has no source to transcribe.
  The rule is now scoped: the two Reed interface slices and the façade re-export shape carry the transcription requirement and name their verification sources (`internal/reedengine`, quarry's `quarry/` package);
  the creel struct is labelled an **illustrative sketch** subject to `creel-is-sketched-not-specified` and explicitly not a measured or verifiable contract.
  The Q&A-log echo was reworded to carry the same split.

- "[NIT:decision] Rename recommendation (a) left at less depth than the Fabric split".
  Verified accurate: the round-2 fix fixed the Fabric split's depth precisely so the follow-up item would not have to guess, while (a) still said only "a rename plus a parameter".
  (a) now states the intended shape to the same standard — `StripEnvKeys(environ []string, exact []string, prefixes []string) (clean []string, stripped []string)`, with the `CLAUDECODE` / `CLAUDE_CODE_` literals relocated out of the function body into `internal/reedengine/lifecycle.go`'s single call site — and says explicitly that the exact identifier name, and only that, is deferred to the follow-up item.

## Pushed Back

None.
