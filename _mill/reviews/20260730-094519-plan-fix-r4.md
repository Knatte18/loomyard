# Plan-fix report — round 4

Source review: `_mill/reviews/20260730-093949-plan-review-r4.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 1 NIT). All findings verified accurate (the marketplace-source claim verified against real manifests on disk); all fixed; none pushed back.

## Fixed

- **[BLOCKING] "Versioned marketplace `source` contradicts every real example checked"** — VERIFIED against the actual manifests: `~/.claude/plugins/marketplaces/claude-plugins-official/.claude-plugin/marketplace.json` has 276 plugins, **0** with a version segment in a local `source`; weblens' cached entry is `source: './plugins/weblens'` with `version: '1.0.0'` (decoupled). Flipped the entire plan to the flat layout: relocated the plugin from `plugins/prowler/1.0.0/` to `plugins/prowler/` (global path rewrite across all three plan files), `marketplace.json` `source: ./plugins/prowler`, batch verifies, `.gitignore`/`.gitattributes` entries, and the SKILL hop all updated. Added an overview Shared Decision recording that this **supersedes the discussion's versioned default** — justified by the discussion's own binding rule ("do not ship the versioned form unverified" + flat as sanctioned fallback) plus the pipeline's inability to run the interactive `/plugin install` gate. No versioning affordance lost (the `version` field carries `1.0.0`); the Go module path already had no version segment.
- **[BLOCKING] "Build-lock staleness reclaim has no ownership token"** — Card 8's lock redesigned: each invocation computes `MYTOKEN="$$-$RANDOM"` written to `$LOCK/owner` at acquire; the exit-trap removes the lock ONLY when `$LOCK/owner` still equals `MYTOKEN` (so a wrongly-reclaimed lock is never deleted out from under its new holder); the build writes to `$BIN.tmp.$MYTOKEN` then `mv -f` atomically renames to `$BIN` (no reader ever sees a partial binary; concurrent builders are last-writer-wins with a complete binary). Raised the staleness/acquire deadline from ~120s to ~300s to sit comfortably above a cold first build (incl. ~4× Windows AV overhead), reducing false-orphan reclaims. Card 9's staleness test updated to the ~300s threshold and notes the live-owner race is guarded structurally (token + atomic rename), with test 3's concurrent-build assertion covering the rename.
- **[NIT] "'pinned exactly to weblens' proven pattern' overclaims for the SKILL.md invocation"** — Card 12 now states explicitly that only the `${CLAUDE_SKILL_DIR}/../../scripts/run.sh` path-resolution hop is identical to weblens' verified `SKILL.md`, while the `path=$(...)` capture is prowler's own addition (weblens redirects run.sh's stdout to one fixed shared file), required by prowler's unique-output-file design. (The overclaiming prose in `discussion.md`'s skill-contract decision itself is outside the plan-review edit scope, which is limited to `plan/`.)

## Pushed Back

None.
