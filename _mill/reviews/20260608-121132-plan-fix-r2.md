# Plan-fix report — round 2

Source review: `_mill/reviews/20260608-121029-plan-review-r2.md` (holistic, APPROVE with NITs)

## Fixed

- [NIT] "Card 5: yaml deep-merge implementation left ambiguous" — card 5 (batch
  2) now commits to the map-then-overlay approach: each layer is unmarshalled
  into `map[string]string` and every present key overlays the merged result;
  the `*string` alternative is removed. Accepted keys named explicitly
  (`path`/`home`/`sidebar`/`proposal_prefix`).

## Pushed Back

- [NIT] "Card 2: context only lists go.mod despite renaming across 11 files;
  add `internal/board/board.go` and `internal/board/git.go` to Context" —
  rejected. Both `internal/board/board.go` and `internal/board/git.go` are
  already in card 2's `Edits:` list, so they are implicitly read; the plan
  template and principles require that Edits files NOT be repeated in `Context:`
  ("Edits: files are implicitly read — do not repeat them in Context:"). Adding
  them to Context would reintroduce the exact defect fixed in round 1 (finding 3,
  `go.sum` listed in both Context and Edits). The reviewer's own note
  acknowledges the current form is "correct per the spec." No change.
