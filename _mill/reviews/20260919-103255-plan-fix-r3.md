# Plan fix — round 3

Source review: `20260919-102428-plan-review-r3.md` (holistic, sonnetxhigh) — verdict APPROVE, two NITs.

## Fixed

- `20260919-102428-plan-review-r3.md` — "[NIT, demoted from BLOCKING] Batch 1 reverses a named architecture decision without updating its two source-of-truth comments": accurate, and both comments verified in place. `internal/reedengine/doc.go` says `internal/lyxcwd` and `internal/fabricengine` are "absent from this package's DIRECT production imports", and `internal/reedcli/watchdog.go`'s header names the `told-geometry-keeps-the-daemon-out-of-reedengine` decision and claims `reedengine` "learns nothing about the daemon's existence" — card 1's new `fabricengine` import falsifies the first, and card 2's delegation falsifies the second. Card 1 now edits `doc.go` in its own commit, narrowing the sentence to `internal/lyxcwd` alone and recording that `internal/fabricengine` became a direct production import. Card 2 now edits `internal/reedcli/watchdog.go` in its own commit, rewriting all three falsified clauses and naming the superseding decision so a reader finds the reasoning rather than only the reversal. Both follow the plan's own `docs-land-with-the-behaviour-card-that-makes-them-true` Shared Decision, which these two files had escaped.
  Card 1 also now states explicitly that the Told-Geometry Invariant is **not** relaxed and that `CONSTRAINTS.md` is not edited: that invariant bars a direct `internal/lyxcwd` import and requires an engine be handed the absolute paths it operates on, and the single `fabricengine.HubScratchDir` call takes the hub path as a parameter and derives nothing. Saying so in the card keeps a later reader from reading the new import as a quiet relaxation. `CONSTRAINTS.md` was added to card 1's `Context:` so the implementer can check that wording rather than take the plan's word for it.
- `20260919-102428-plan-review-r3.md` — "[NIT] Watchdog spawn call site is inserted with no step marker of its own": accurate — `start.go`'s `RunE` numbers its blocks and card 12 inserted a call between step 4's block and the `// Step 5:` marker without saying what happens to the numbering. Card 12 now states it: leave every `// Step N:` comment as it is, give the call no step number, and fold its comment into the tail of the existing step-4 block. The reasoning is recorded too — the call belongs to the substrate step it follows, `--no-attach` performs it, and it reports nothing on the envelope, so it is not one of the steps the verb's `Long` text enumerates to an operator.

## Pushed Back

_None. Both findings were accurate and both fixes were applied, the ceiling-demoted one included._
