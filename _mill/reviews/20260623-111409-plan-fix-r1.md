# Plan fix report — round 1

Source review: `_mill/reviews/20260623-111222-plan-review-r1.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260623-111222-plan-review-r1.md` — "[BLOCKING] e2e config write-through test cannot reach sync": rewrote Batch 4 Card 14(e) to first seed the host `_lyx` junction via `worktree.New(Config{}).Add(f.Layout, slug, AddOptions{SkipPush:true})`, resolve the new host layout, `t.Chdir` into it, and drive the injected sync via `weft.RunCLI(io.Discard, ["commit"])` (commit, not sync — `sync` spawns a detached push that cannot run in-process). Added `internal/worktree/add.go` to Card 14 Context.
- `20260623-111222-plan-review-r1.md` — "[NIT] Optional seeder rename would silently promote add.go to an Edit": dropped the optional-rename clause in Batch 2 Card 6 (keep `seedLyxJunction`/`seedGitExclude` names; `add.go` stays read-only Context) and removed the now-obsolete rename reference in Card 7.

## Pushed Back

- (none)
