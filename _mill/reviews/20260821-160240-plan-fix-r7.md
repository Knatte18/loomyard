# Plan fix — round 7

Source review: `_mill/reviews/20260821-155759-plan-review-r7.md` (holistic, sonnetxhigh, **APPROVE**, 0 BLOCKING + 5 NIT).

Every finding was verified against source before acting.
All five are accurate;
none met a harm-check exception.
All five fixed, none pushed back — NITs are fixed under the default rule, not skipped because the verdict is APPROVE.

## Fixed

- r7 `[NIT:consistency]` "Self-qualified `loomrecipe.New` inside `package loomrecipe`" — accurate and a genuine compile error, not a wording preference: cards 6, 7, 8, 9, and 12 each set their file to `package loomrecipe` and then spelled the call `loomrecipe.New(env, paths)`, which does not compile from inside the declaring package.
  Every such spelling is now the unqualified `New(…)`, with the rule stated once in card 6 and once in card 12;
  `loomshed.Name*` stays qualified, since `internal/loomshed` genuinely is a different package.
  Verified zero remaining occurrences of the self-qualified form across all plan files.
- r7 `[NIT:consistency]` "Two hand-maintained thirteen-row tables land in one package" — accurate;
  card 7's shape assertion is a strict superset of card 6's `wantProducerTable` (same five routing/identity fields plus the concrete producer type), and neither card stated a disposition, so a future row change would have to be applied in two places forever.
  Resolved by deduplication rather than by documenting the overlap: card 6's `wantProducerRow` gains a sixth field carrying each row's expected `reflect.Type`, and that table is declared the package's single authoritative row expectation;
  card 7's shape assertion consumes it instead of declaring its own.
  No test is deleted — `TestNew_ProducerTable` keeps its five-field assertion and ignores the type column, and card 7's assertion is that column's only reader.
  Card 6 precedes card 7 in the same batch, so the ordering works, and batch 2's own card-coupling note already covers the intra-batch dependency.
  Added `internal/loomrecipe/shape_test.go` to card 7's `Context:`.
- r7 `[NIT:consistency]` "Card 26 says \"these four\" for six enumerated repairs" — accurate leftover from rounds 2 and 3, which grew the enumeration from four to six without updating the sweep sentence.
  Corrected to "these six", matching the batch scope's own count.
- r7 `[NIT:consistency]` "`NamePublish` count in card 8 is off" — accurate;
  `internal/loomshed/sequence_test.go` carries five references outside `wantSequenceOrder` (lines 58, 59, 71, 89, 90), not the four my round 6 fix claimed.
  Corrected to five.
  The operative instruction — qualify every bare `Name*` reference in the file — was already unrestricted, so the count is illustrative rather than load-bearing;
  it is corrected rather than dropped so a reader can check their own work against it.
- r7 `[NIT:consistency]` "Batch 4 does not flag cards 15-17 as one compile unit" — accurate, and the coupling is wider than the finding's title suggests: card 15 assigns `c.env`/`c.shedPaths` before card 16 declares them, and card 16 removes the `deps` field while cards 17, 18, **and 19** still read it.
  Added the note to batch 4's Batch Scope covering cards 15 through 19, stating that each card still gets its own commit but that no mid-batch commit builds and `verify:` runs at the batch boundary — the same shape batch 2 already carries for cards 5 and 6.

## Pushed Back

_None._
All five findings verified accurate against source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope.

## Convergence

Round 7 verdict is APPROVE with zero BLOCKING findings, `round >= min_rounds` (7 >= 1), and no finding in this round carries a `demoted: true` marker — so the convergence gate is satisfied genuinely, not by the round cap.
The plan DAG re-validates clean and `_plan_validate.run` reports zero findings after these fixes.
