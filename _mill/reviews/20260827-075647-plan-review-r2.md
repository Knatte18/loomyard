MILL_REVIEW_BEGIN
# Review: Add a local-only file category to weft — holistic

```yaml
duration_s: 390.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 4.5 (self-assessed; exact point version not independently verifiable)
reviewed_file: plan/
date: 2026-08-27
```

## Verification performed

Read the overview and all seven batch files, then cross-checked plan claims against the actual source
across `internal/fabricengine` (merge.go, mergeguards.go, mergestate.go, destroy.go, cleanup.go, pull.go,
commitweftpaths.go, weftgit.go, spawn.go, doc.go), `internal/gitrepo` (push.go, merge.go, pull.go),
`internal/shedengine` (shed.go, run.go, status.go), `internal/loomrecipe/loomrecipe.go`,
`internal/loomcli` (wiring.go, landingdeps.go), `internal/lock/lock.go`, `internal/state/state.go`,
`internal/shedadapters/bouncer.go`, `manifest/designs/loom.md`.

Spot-checked several exact line-number citations against current source
(`destroy.go:520`, `doc.go:40`, `doc.go:858-880`, `doc.go:1018-1046`, `loom.md:273-277`, `loom.md:279-283`)
— all matched precisely. Counted the "seventeen" `&PartialPullError{WeftPulled: true, ...}` sites in
`pull.go` — exactly 17. Verified `gitrepo.PushRebaseFree`/`ErrPushRejected`,
`gitrepo.MergeHeadPresent`/`ConflictedFiles`, `loomengine.LoomStatusRel`,
`fabricengine.CommitAnchoredPaths`, `PushResult`'s `MutationRecord` embed, `lock.AcquireReadLock`, and
`shedengine.State` (string-backed) all exist with the shapes the plan assumes. Verified test-fixture
helpers named in card 7/13/17/20/24 (`newMergeTargetFixture`, `seedSourceAndTarget`,
`newMergePairFixture`, `commitOnCurrentBranch`, `advanceRemoteBranch`, `openFreshFabric`, `gitRevParse`,
`CurrentSHAForTest`) are all defined in the context files those cards list.

Checked the Batch Index DAG (7 batches, all `file:` present, dependencies acyclic, no forward refs,
`doc.go`-editing batches 1→2→3→5 correctly serialized via transitive `depends-on`), global step
numbering (cards 1–32, unique/sequential/no gaps), and the overview's `## All Files Touched` list against
the union of every batch's `Edits`/`Creates` — exact match, nothing missing or extra. Every `Moves:` in
every card is `none`, so no Rename mechanic section is required anywhere, and none is present — correct.

No constraint violations found: Fabric Vocabulary Invariant (vocabulary-neutral `PushAnchored`/
`MergeStateActive`/`commitStatusDeps` correctly avoid `Weft`/`Warp` naming outside the owner set), Shed
Producer-Seam Invariant (batch 6 adds no import to `shedengine`), Mutation Record Invariant
(`MergeStateActive` correctly returns no result type as a read-only probe), CLI/Cobra Invariant (card 19's
flag-help edits keep the flag set and `RunE` shape untouched), Lyxdirs Single-Declarer Invariant
(pathspecs built from `LoomStatusRel()`/`PlanDirRel()` accessors, never inline `_lyx` literals).

No findings.

## Verdict

APPROVE
Plan is thoroughly grounded in current source, internally consistent, and free of constraint violations across all seven batches.
MILL_REVIEW_END
