# Discussion fix — round 1

Source review: `_mill/reviews/20260828-141230-discussion-review-r1.md`

## Fixed

- `[NIT:consistency]` "New reap logging is \"discovered\" but never becomes a Decision, a Scope item, or a test" — took the review's option (a): added Decision `the-reap-logs-what-it-destroys` (a `logger.Info` line in `reconcileLocked`'s kill-pane loop, `Info` per the Spawn Observability lifecycle-vs-probe split, one line per call rather than per pane, untracked reap identifiable), listed the log line in Scope → In beside the gate change on `reconcile.go`, added a Testing subsection covering it via `logger.SetOutput` over the existing fake-tmux harness, and rewrote the dangling Constraints footnote to point at the new Decision instead of trailing off.

## Pushed Back

_None._
