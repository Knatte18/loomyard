# Fixer report: plan-fix round 3 — the standalone CLI path

```yaml
round: 3
review: 20260818-135055-plan-review-r3.md
fixed: 2
pushed_back: 0
```

## Fixed

- `20260818-135055-plan-review-r3.md` — "[BLOCKING:design] Cards 18/24 prescribe an unwritable reed-geometry assertion": dropped the reed-geometry field-value assertions from batch 4 card 18 and batch 5 card 24, and replaced them with an explicit prohibition plus a statement of where those values actually are pinned.
  Verified independently rather than taken on trust: `burlerengine.Engine` exposes only `Run` and holds `shuttle`, `geom`, `cfg` and `stencilsDir` unexported; `shuttleengine.Runner` holds `reed`, `engine`, `anchorPath`, `worktreeRoot` and `cfg` unexported and its only exported methods are `Start`, `Run`, `Interrupt`, `Send` and `Inject`; `reedengine.Engine` has no geometry accessor.
  Both engine packages are distinct from the in-package `wiring_test.go`, so `SocketKey`/`SessionName`/`LogsDir`/`RepoName`/`HubPath` are genuinely unreachable, and none of the three reporting fields encodes them.
  The reviewer offered two fixes and asked which was taken: **dropped rather than adding an accessor**, because adding production surface to `burlerengine` or `shuttleengine` purely to make a CLI test writable is a worse trade than relocating the assertion, and it would also breach this task's own scope-Out on the engine packages.
  The five values remain pinned one layer down by `TestReedGeometry` in `internal/standalonegeom/standalonegeom_test.go`, which asserts all eight fields against told parameters.
  Recorded the residual honestly in both cards rather than claiming full coverage: what no test pins is the *linkage* — that `wireStandalone` calls `standalonegeom.ReedGeometry` with this invocation's own `target`/`stateDir`/`hash8` — which no seam exposes and is therefore a stated review obligation.

- `20260818-135055-plan-review-r3.md` — "[NIT:consistency] Card 17 and card 29 contradict each other about the hermetic-git guard": reworded batch 6 card 29's bullet.
  Verified accurate — card 17 correctly describes `cmd/lyx/hermeticenv_test.go`'s guard as a raw-substring token scan (`gitexec.Run`, `exec.Command`, `gitkit.Copy*`, `hubforge.NewHub`) that cannot see a spawn reached indirectly through `RunCLIIn`, and card 29 then asked the implementer to "confirm the guard sees it" for `internal/burlercli`, which the guard never classifies as git-spawning at all.
  Card 29 now states the suite passes trivially for that package and that this is expected rather than a success signal, and directs the implementer to confirm the `TestMain` by reading `internal/burlercli/testmain_test.go` instead of inferring its presence from a green run.
  Added that file to card 29's `Context:`.

## Pushed Back

_None._

## Validator follow-up

Both edits introduced new `Requirements:` references to `internal/standalonegeom/standalonegeom_test.go`, which the self-run validator gate caught as two `context-completeness` findings.
Applied the fix table's mechanical remedy — added the file to cards 18 and 24's `Context:` lists — and re-ran; the gate and the DAG check are both clean.
