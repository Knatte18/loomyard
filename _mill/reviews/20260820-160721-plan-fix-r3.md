# Plan fix report — round 3

Source review: `_mill/reviews/20260820-160618-plan-review-r3.md`

## Fixed

- r3 `[BLOCKING:design]` "`ensureFocus`'s own failure mode is unspecified" — batch 3 card 6 now pins `ensureFocus` as returning nothing, with its own archive/write failure logged via `logger.Warn` and swallowed there, never propagated and never allowed to change `Call`'s outcome, pointer, or error; the text states why the swallow is the safe direction at `settle`'s `BLOCKING` branch and that the same rule applies at the seed site.
- r3 `[NIT:consistency]` (demoted from BLOCKING) "doc.go's \"one constructor returning an error\" claim is false" — verified against `internal/shedadapters/perch.go:61`, where `NewPerchProducer` already returns an error and eagerly validates. Card 8 now calls the Bouncer the second validating, error-returning constructor after `NewPerchProducer`, and contrasts it with `NewSingleLLMProducer` and `NewWebsterProducer`, which return a bare pointer; card 8's `Context:` gained the three sibling adapter files.
- r3 `[NIT:consistency]` "Card 7's real-template-bytes phrasing invites a fragile file read" — verified against `internal/treadleengine/judge_test.go:32-35`, which seeds its fixture from the `contracts/stencils` package's exported vars. Card 7 now names `stencils.BouncerTemplateSeed` and `stencils.BouncerTemplateJudge` and requires the package import rather than an on-disk path read; its `Context:` gained `contracts/stencils/stencils.go` and `internal/treadleengine/judge_test.go`.
- r3 `[NIT:scope]` "Judge-side harvest-with-Run-error has no explicit test case" — batch 4 card 10's harvest scenario now covers both a non-completion outcome and a `Run` error after real output was written, matching card 7's seed-side symmetry.

## Pushed Back

_None._
