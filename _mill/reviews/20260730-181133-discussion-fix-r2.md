# Discussion fix — round 2

Source review: `_mill/reviews/20260730-181032-discussion-review-r2.md` (APPROVE, 3 NOTEs)

## Fixed

- NOTE "New push lock exclude is redundant under .weft/" — corrected the lock-artifacts decision and the `weftgit.go` technical-context bullet: the `weftPushLockFile` constant is still needed for lock-path construction, but no new git-exclude entry is required, since `seedWeftArtifactExcludes` already seeds the whole-directory `.weft/` exclude (`weftgit.go:131`). Removed the misleading "must add an exclude" instruction.
- NOTE "Rebase-free primitive's return on rejection unpinned" — pinned the contract in rebase-free-async-push: the gitrepo rebase-free push primitive returns a distinguishable rejection signal (typed/sentinel error or `(rejected bool, err)`), which the fabric push step maps to `progressed=false, err=nil` + a log line so the loop exits cleanly; a genuine failure still propagates as a real error.
- NOTE "No-op Fabric.Commit lock behavior unspecified" — clarified combined-commit-lock and Scope (In): the lock is taken whenever the call will commit something (`len(warpFiles) > 0 || weftSide`), and a degenerate no-op call takes no lock and runs no `ensureWeftLockDir`, matching today's behavior.

## Pushed Back

(none)
