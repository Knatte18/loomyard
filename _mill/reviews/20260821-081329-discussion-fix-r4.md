# Discussion fix — round 4

Source review: `_mill/reviews/20260821-081259-discussion-review-r4.md` (holistic, APPROVE, 0 BLOCKING / 2 NIT).

## Fixed

- r4 NIT `"Three rows bounce backwards" names five rows` — Technical context now reads "Five rows bounce backwards, to three distinct targets", and states that the remaining eight rows carry `OnStuck: ""`.
- r4 NIT `Perch items still specify `Segment`, which recipe rows drop` — the "`Segment` is untouched by this task" decision now records that the three perch items' `Segment: "..."` instructions are expected to be moot by the time they are acted on (they are sequenced after the recipe conversion, whose row shape carries no `Segment`), and that `shedcheck` must not accommodate them.

## Pushed Back

None. Both findings verified accurate against `internal/loomshed/loomshed.go:137-151` and `manifest/roadmap.md`, and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task.
