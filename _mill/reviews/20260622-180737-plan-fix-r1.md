# Plan fix — round 1

Source review: `_mill/reviews/20260622-180709-plan-review-r1.md` (verdict APPROVE, 0 blocking, 2 NITs).

## Fixed

- r1 [NIT] "Card 3 references Store.Save calls that do not exist in tests" — reworded Card 3 (batch 02) to state that `store_test.go` uses `NewStore("")` exclusively with no disk-backed Load test and no `Store.Save` call, so the corrupt-`tasks.json` test is the first disk-backed Load test (a new addition, not an edit). Verified accurate against source.
- r1 [NIT] "muxpoc lock-file path silently migrates with no test asserting the new name" — added a requirement to Card 6 (batch 03) to assert `.lyx/muxpoc-state.json.lock` exists after `SaveState`, pinning the lock-filename migration from `.lyx/muxpoc-state.lock`. (The review's parenthetical suggested state_test.go, which already has `TestLockFileLocation`; the real gap was muxpoc, so the assertion is added there.)

## Pushed Back

_None._
