MILL_REVIEW_BEGIN
# Review: fabric: close the weft-visibility leak (slice 8) — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-07
```

## Findings

None.

Verified end-to-end against all 8 batches:

- Batch 01: `Open`, `Ready`, `CommitResult.Committed()`, `RefScanner` all added as pure additions in their own files (`open.go`, `ready.go`, `refscanner.go`, `commit.go`), matching the pinned contracts (host-checked-first `*ErrMissingPath`, three-outcome `Ready`, regex compiled once in `NewRefScanner`).
- Batch 02: `HealthReason`/`HealthCause` typed return landed atomically in `drift.go` + `loomengine/preflight.go`/`report.go`/`status.go`; `CheckFabricReady`/`CheckFabricSync` renames and the `Clean` code-side/state-side reword in `hostclean.go` are all present and consistent with the five migrated `fabricengine_test` files.
- Batch 03: every consumer (`buildercli`, `webstercli`, `perchcli`, `configcli`, `builderengine`, `websterengine`, `fabriccli`) migrated onto `Open`/`Committed()`; `weft.go`→`sync.go` renames preserved via `git mv` (rename history intact); `"fabricCommitted"` JSON key and "fabric sync failed" diagnostics land everywhere card 7-9 requires; `FabricResetter`/`FabricBisector` renames complete with doc comments reworded; `RefScanner` adopted in `websterengine/audit.go` with `ClassFabricReference` and no residual `weftReferencePattern`/`weftname` import.
- Batch 04: `New`→`newPaired`, `Fabric.warp`/`weft` private, `export_test.go` shim correctly re-exports for the three raw-path fixture test files; `fabric_test.go` restated through `Open`; `PartialCommitError` fields private with `CommitResult`'s own fields correctly left exported.
- Batch 05: all four websterengine templates, both builderengine templates, and burlerengine's template/doc.go/prompt.go/profile.go are vocabulary-clean (zero weft/warp/host-phrase hits), including the positive `_lyx` rule and "## A fabric-sync error…" heading, with `template_test.go` pins updated to match the new wording.
- Batch 06: full-repo grep confirms every remaining non-owner, non-test `.go`/`.md` file is vocabulary-clean; `boardguard_test.go`'s invariant name aligned to "Fabric Git Invariant"; `gitrepo/doc.go`, `buildercli/sync.go`, `webstercli/sync.go`, `websterengine/audit.go` (card 24's stragglers) are clean.
- Batch 07: `TestEnforcement_FabricVocabulary` in `enforcement_test.go` implements all three rules (bare token, host-phrase, weftname-import) plus the `.md` walk, correctly scoped owner sets and the `configsync` literal/comment-vs-identifier carve-out, matching the plan's predicate sub-test list.
- Batch 08: `fabricengine/doc.go` records the durable one-repo contract; `CONSTRAINTS.md` has the new Fabric Vocabulary Invariant section and a corrected Fabric Git Invariant "Enforced by" bullet naming `fabricengine.RefScanner`; `docs/overview.md`, `README.md:62`, `docs/reference/status-schema.md`, `docs/reference/plan-format.md`, `docs/reference/builder-contract.md`, `docs/skills.md` are all reworded/clean; the design doc's slice-8 section is compacted and its open-question entry removed; `docs/benchmarks/test-suite-timing.md` correctly carries no diff per its documented deviation.

No cross-batch contract mismatches, no duplicated helpers, no out-of-plan files, no constraint violations found.

## Verdict

APPROVE
Implementation fully matches the approved plan across all 8 batches with no residual vocabulary leaks or contract mismatches.
MILL_REVIEW_END
