# Plan-fix report — round 3

Source review: `_mill/reviews/20260801-191544-plan-review-r3.md` (holistic, **APPROVE**, 0 BLOCKING + 3 NITs). NITs applied per the default fix-everything rule; plan approved.

## Fixed

- `20260801-191544-plan-review-r3.md` — "[NIT] Design-doc 'Scope boundary' not reconciled with the four new forwarding methods": added a Card 1 requirement (and moved `manifest/designs/fabric-unified-view.md` from Context to Edits, added it to the overview's All Files Touched) directing the implementer to add a one/two-line addendum to fabric-unified-view.md's `## Scope boundary` section reconciling it with the four new warp-only methods, forward-pointing to CONSTRAINTS.md's Fabric Git Invariant as the authoritative directive. Encoded as a plan requirement rather than a direct source edit, per the Plan-Review guardrail (fixes touch only plan files).
- `20260801-191544-plan-review-r3.md` — "[NIT] Card 9 misidentifies `TestSpawnBatch_InFlightGuardMatrix` as a `RestartChain: true` test": verified against spawn_test.go (InFlightGuardMatrix subtests set only `BatchNumber`); replaced it in Card 9's enumeration with the two genuine tests `TestSpawnBatch_RestartChainOnChainlessBatchErrors` and `TestSpawnBatch_RestartChainClearsStaleReportBeforeRefusal`.
- `20260801-191544-plan-review-r3.md` — "[NIT] Batch 3's 'Batch Tests' section undercounts the real-reset tests": corrected "three" to "four", naming the four tests that reach the reset and noting `RestartChainOnChainlessBatchErrors` errors out before the reset call.

## Pushed Back

(none)
