MILL_REVIEW_BEGIN
# Review: Adopt quarry's glyph alphabet as the plan alphabet — holistic

```yaml
duration_s: 262.0
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Anthropic Claude, Opus-class; harness reports model ID claude-opus-5
reviewed_file: plan/
date: 2026-09-06
```

## Findings

### [NIT:decision] Rename to-side handle is never computed
**Demoted-from:** BLOCKING
**Location:** batch 3 card 11, batch 4 card 19, batch 7 cards 34/36
**Issue:** Card 11 requires a `plan:` handle on every `Rename` pair's `New` and promises "the pipeline computes and overwrites [it] in batch 7", but no card ever does: card 19's `CanonicalizeHandles` collects only `planparser.CardDeclaration` values, which card 9 populates from `**Create:**` sub-bullets alone; card 34's `BindHandles` operates only on "each completed card's `Create` declarations"; and card 36's gate one explicitly produces "no finding and no repair" for a rename matching a `Rename` card's own pair — so the planner's untrusted draft to-side spelling survives to the end, contradicting the discussion's `rename-pair-grammar` decision ("resolve `old`, take its declaration verbatim, swap the identifier, call `Name`"; "The planner's draft spelling on the to-side is never trusted").
**Fix:** Give one card explicit `Requirements:` for deriving the Rename to-side declaration from the resolved `Old` glyph and binding it — either extending card 19's `Name` batch with Rename-derived declarations, or extending card 34/36's gate-one branch to emit the handle→new-ID substitution.

### [BLOCKING:scope] CanonicalizeHandles is defined with no caller
**Location:** batch 4 card 19; batch 4 card 16; batch 5 cards 23/24
**Issue:** `planglyph.CanonicalizeHandles(plan, planDir)` is created by card 19 (`Edits: none`) and named nowhere else in the plan — card 16's `resolvePass(plan, worktreeRoot)` carries no `planDir` and card 20 is the only card told to wire into it, while cards 23/24 wire only `planglyph.Validate`/`ValidateFormat`. The discussion's `handle-spelling-and-canonicalization` decision places the batched `quarry.Name` call at the validation boundary, so the function ships dead and the decision is unimplemented.
**Fix:** Name the calling boundary in a card's `Requirements:` — which function invokes `CanonicalizeHandles`, with which `planDir`, and how its findings merge into the gate's findings set.

### [NIT:scope] The record-batch Delta call site is named only in Batch Scope
**Location:** batch 7 cards 34/35/36
**Issue:** Cards 34, 35 and 36 all consume a `quarry.GitDeltaAnswer` parameter and all edit `internal/websterengine/recordbatch.go`, but no card's `Requirements:` says to add the single `planglyph.Delta(deps.Geom.WorktreeRoot, bs.StartSHA, report.HeadSHA)` call, where in the sequence it runs, or which card owns it; only batch 7's `## Batch Scope` prose names the shape.
**Fix:** State the call site, its arguments and its ordering explicitly in card 34's `Requirements:`, as card 33 already does for `DoneChecks`.

### [NIT:consistency] Card 29's RunCLIIn recount yields no textual change
**Location:** batch 6 card 29
**Issue:** `CONSTRAINTS.md` today reads "eleven of twelve also carry `RunCLIIn`"; `cmd/lyx/main.go` registers eleven module `Command()` calls and `cmd/lyx/seamsignature_test.go` pins eleven `RunCLI` and ten `RunCLIIn` entries, so card 29's own derivation (post-task twelve and eleven) reproduces the existing sentence byte-for-byte — yet the card frames it as a correction that must be made, inviting an implementer to change something that is already right post-task.
**Fix:** State in the card that the sentence's post-task value coincides with the current text and that the numbers must be verified rather than edited.

### [NIT:consistency] New language check displaces plan-unapproved from position two
**Location:** batch 2 card 3
**Issue:** Card 3 wires `checkLanguageRecognized` "immediately after `checkFormatRecognized`", which in `internal/planparser/validate.go`'s `validate` puts it ahead of the `requireApproved` `checkApproved` call, so `Validate`'s doc comment claim "with plan-unapproved at position two" becomes false; the card instructs updating only the check counts.
**Fix:** Add the position sentence to card 3's list of doc-comment text to update, or place the new check after the `requireApproved` block.

## Verdict

REQUEST_CHANGES
Two handle-lifecycle mechanisms are specified but never wired to any caller.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
