# Discussion fix report — round 3

Source review: `_mill/reviews/20260829-104056-discussion-review-r3.md` (APPROVE, 4 NITs, 0 BLOCKING).

## Fixed

- `20260829-104056-discussion-review-r3.md` — "404-vs-422 discrimination mechanism unstated": added a new Decision, "HTTP status comes from the JSON error body on stdout — no stderr parsing, no `gh auth status` preflight". Verified live that `gh api` writes the raw JSON error body to **stdout** (with a machine-readable `"status":"404"` field) and leaves `--jq` unapplied on the error path, so the code is read from the body the script already captures — no stderr parsing, no coupling to gh's message format or locale.
- `20260829-104056-discussion-review-r3.md` — "`[path]` normalization/encoding undefined": added a Normalization bullet to the `[path]` decision — surrounding slashes stripped, empty-after-strip means whole repo, no other rewriting, and a path containing a character needing URL-encoding is rejected up front with a stderr message naming it. Added a harness scenario asserting `/src`, `src/`, and `src` are byte-identical, and one asserting the encoding rejection happens before any `gh` call.
- `20260829-104056-discussion-review-r3.md` — "\"one call\" claim omits the `gh auth status` preflight": resolved by removing the preflight rather than by qualifying the claim. An unauthenticated run is now detected as HTTP 401 via the same status-code mechanism, so the claim becomes literally true — one `gh api` call and one network round trip, with no preflight behind it. The `HEAD` decision, the harness's fast-path assertion (now "exactly one `gh` invocation of any kind"), the stub-`gh` decision, and the error scenarios were all updated to match.
- `20260829-104056-discussion-review-r3.md` — "Empty/commitless repo maps onto the repo-404 message": added two bullets to the stdout-discipline decision. The unscoped 404 message now names all three causes (nonexistent / not accessible with this token / no commits yet) rather than paying an extra call to tell them apart, with 409 treated as an alias since `git/trees/HEAD`'s behaviour on a commitless repo was not verified (probing it would require creating a repository, and that is recorded as unverified rather than assumed). Separately stated that a legitimately blob-free listing exits 0 with empty stdout, so empty stdout alone never means failure — with a submodule-only fixture asserting it.

## Pushed Back

(none)
