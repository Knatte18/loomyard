# Plan fix — round 3

Source review: `_mill/reviews/20260807-133724-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 0 NITs).

## Fixed

- r3 holistic — "[BLOCKING] Batch 4 card 29 omits scoutengine test files that break on the new ensureServer/ensureSupervised parameter": confirmed both halves of the finding.
  `internal/scoutengine/ensureserver_test.go:380` and `supervised_test.go:96,153,318` call these functions positionally and are untagged, so they break batch 4's own untagged `verify:` run;
  and inside `ensureServer`/`ensureSupervised`, `worktreeRoot`'s only uses are `DaemonStateFile(worktreeRoot, lang)`/`DaemonLock(worktreeRoot, lang)` at `ensureserver.go:299-300` plus one comment, so the card's "keep it flowing for its existing uses" rationale was wrong — none remain once path construction moves to the anchor.

  Card 29 now:
  - lists all five callers under `Edits:` — `ensureserver_test.go` and `supervised_test.go` (untagged), plus `ensureserver_integration_test.go`, `supervised_integration_test.go` and `supervised_scout_test.go`;
  - **replaces** the `worktreeRoot` parameter of both functions with `anchorRoot` instead of adding a second one, so no dead parameter is left behind, and retargets the `(worktreeRoot, lang)`-key comment;
  - resolves the out-of-hub fallback once, in `acquireConnection`, substituting `opts.WorktreeRoot` when `opts.AnchorRoot` is empty — which is the live use `Options.WorktreeRoot` retains inside `scoutengine`, now documented at both fields;
  - requires at least one untagged case to use an anchor root that differs from the worktree root, since every existing call passes the same value twice and the re-keying would otherwise be untestable by construction.

  **Tag correction on top of the finding:** the three non-untagged callers carry `//go:build scout`, not `//go:build integration`, so the finding's expectation that they would surface at the module-wide `go vet -tags integration ./...` gate is not right — no existing gate compiles them at all.
  Batch 4's `verify:` therefore gains `go vet -tags scout ./internal/scoutengine/...` (mirrored in the overview's Batch Index), deliberately a `vet` and not a `test` run because those files spawn a real `gopls` daemon, which a per-batch gate must not require;
  the Batch Tests section states that scoping.

  **Two framing corrections recorded in the batch-local decision**, both verified against the code, so a later reviewer does not re-litigate them: the LSP root is `rootURIFor(opts.TargetDir)`, not `WorktreeRoot`, so no language-server session moves with this change;
  and the supervised daemon's identity *is* its state-file path, so relocating that path necessarily re-keys the daemon.
  The re-keying is the intended consequence of having one `.lyx` root per worktree — the separate `AnchorRoot` field makes the change explicit and named rather than a side effect of overloading an existing field, which is what the discussion's decision actually protects.
  The Batch Tests section's stale "singleton key derived from `WorktreeRoot` is unchanged" assertion was removed for the same reason: there is no such invariant to assert.

## Pushed Back

_None._
The finding was accurate on both counts;
the two corrections above (build-tag name, and the absence of a singleton-key-unchanged invariant) extend the fix rather than reject any part of it, and neither reduces the scope of what the finding asked for.

## Post-fix validation

`_plan_dag.validate` passes;
`_plan_validate.run` reports 0 errors.
The overview's All Files Touched list was regenerated from the cards and now carries the five scoutengine test files.
