# Plan fix report — round 5

Source review: `_mill/reviews/20260728-102101-plan-review-r5.md` (verdict APPROVE, 0 blocking, 1 NIT).

## Fixed

- `20260728-102101-plan-review-r5.md` — "[NIT] Fingerprint helper routed to reads that touch no object": verified and accurate, and it corrects an over-application introduced by round 4's own fix. `CurrentSHA` (card 10), `SnapshotSHA`'s ref read (card 15) and `SetSnapshotSHA`'s adopted-ref read (card 17) are `Head()`/`Reference()` calls only — ref-storage reads that never touch the pack-object store the fingerprint gate exists to protect. `.scratch/gogit-worktree-probe-report.md` states it directly: refs are never cached, and `Head()`, `Reference()` and the snapshot ref read go to disk on every call. Routing them through a helper that reindexes on `object not found` was instructing the implementer to wrap a lookup that does not exist. Reworded all three to the same framing cards 12 and 14 already use — no helper, refs are never stale, read lock still held — and split card 17 explicitly, since its two reads now differ: the `^{commit}` canonicalization **does** resolve a commit and keeps the helper, while the adopted-ref read does not. Also tightened the overview's Shared Decision with an explicit scope clause naming which five sites the helper covers and which five ref reads it deliberately does not, so the distinction is stated once at the top rather than inferred per card.

## Pushed Back

None. The finding was factually accurate and the fix breaks nothing, conflicts with no documented design decision, and destabilises no work outside this task's scope.
