# Plan fixer report — round 3

Source review: `_mill/reviews/20260625-133449-plan-review-r3.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 2 NITs).

## Fixed

- `20260625-133449-plan-review-r3.md` — [BLOCKING] "main_test.go TestRunDispatchesToGitClone left stale" — added `cmd/lyx/main_test.go` to card 6's `Edits` with a requirement to retarget `TestRunDispatchesToGitClone` (currently `run([]string{"git-clone"}, …)`) to `run([]string{"warp", "clone"}, …)` and rename it (e.g. `TestRunDispatchesToWarpClone`), so batch-2 verify `go test ./cmd/lyx/` exercises the new dispatch instead of hitting `default` (exit 1, no output) and failing.
- `20260625-133449-plan-review-r3.md` — [NIT] "Unified RunCLI must resolve per-case, not at top" — added an explicit resolution-hazard note to card 4 (the `RunCLI` skeleton stays resolution-free at the top; `clone` runs outside a git repo) and card 9 (when folding in `worktree.RunCLI`, move `paths.Resolve`/`LoadConfig` *into* the add/list/remove cases — and later checkout/status/reconcile/prune/cleanup — never hoist to the top).
- `20260625-133449-plan-review-r3.md` — [NIT] "init activation slug derivation vs HostJunctions(slug) geometry" — card 14 now specifies init passes `filepath.Base(l.WorktreeRoot)` as the slug to `WireJunctions` (so `HostJunctions(slug)` resolves to the current worktree's `HostLyxLinkHere`); card 11 now notes the primitive is slug-keyed and the caller supplies the slug (spawn slug for `add`, `base(WorktreeRoot)` for `init`).

## Pushed Back

(none — all three findings were factually accurate with no harm in fixing; applied under the default fix-everything rule.)
