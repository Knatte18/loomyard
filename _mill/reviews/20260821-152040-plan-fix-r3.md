# Plan fix — round 3

Source review: `_mill/reviews/20260821-151421-plan-review-r3.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 2 BLOCKING + 3 NIT, one of the NITs demoted from BLOCKING by the stage's blocking-class ceiling).

Every finding was verified against source before acting.
All five are accurate;
none met a harm-check exception.
All five fixed, none pushed back.
The demoted finding is treated as a NIT for loop-control purposes and fixed under the same default rule as every other finding — the demotion governs who adjudicates it, never whether it gets fixed.

## Fixed

- r3 `[NIT:consistency, demoted-from BLOCKING]` "smoke_test.go called integration-tagged twice" — accurate, and it is my own round 2 error surviving in two places I did not correct.
  Verified `internal/loomcli/smoke_test.go:1` is `//go:build smoke`.
  Batch 4's Batch Tests and card 26's smoke paragraph both said `integration` and both credited `pipeline.done_gate`'s `go test -tags integration ./...` with covering the file;
  `-tags integration` does not enable `smoke`, so neither statement was true.
  Both now name the `smoke` tag and point at batch 6's `&& go vet -tags smoke ./internal/loomcli/` tail, which is the only thing in this plan that compiles the file.
- r3 `[BLOCKING:scope]` "Roadmap sweep tokens miss a falsified Done entry" — verified `manifest/roadmap.md:311`'s **loom: phase-machine scaffolding** entry asserts "`internal/loomshed` carries loom's full 13-row producer list", which batch 5 falsifies and which matches none of card 24's six sweep tokens.
  Named the entry in card 24's starting set with its correction stated — after batch 5 the package carries the thirteen row-name constants and six producer constructors while the list is the recipe's — and widened the sweep with `13-row` and `producer list`.
  The rest of that entry (which rows are real, which are stubbed, the `Plan-Sweep` note) is explicitly left untouched, since none of it changes.
- r3 `[BLOCKING:design]` "Named roadmap item for Landing does not exist" — accurate, and it exposes a pre-existing false claim in production code rather than only a plan defect.
  Verified `internal/landingshed/deps.go:73-75` states the resolution chain "belongs to the layer that legitimately resolves geometry, and the next roadmap item builds it", and that no item in `manifest/roadmap.md`'s Planned, Someday, or Done sections covers it — so card 15's "name the roadmap item" requirement was unsatisfiable and `deps.go`'s own comment is currently false.
  Took the reviewer's second option: card 24 now adds a Planned roadmap item for the parent-fabric resolution chain, described the way `deps.go` already describes it (list the worktrees, match the entry whose branch equals the parent branch, resolve that path, open it), stating the consequence that makes it worth an item — until it lands, `Publish`/`Finalize` construction fails for want of `Env.Landing`, so loom cannot complete a run — and sequenced after this conversion.
  Card 15's comment requirement now points at both `deps.go`'s account and that item, and the `landing-parity` Shared Decision's rationale is restated to say the item is being added here rather than assuming it exists.
  Adding a Planned item is sanctioned by the project's own roadmap rule, and this records an already-documented gap rather than proposing new scope;
  nothing about the chain is implemented here.
- r3 `[NIT:consistency]` "`./contracts/...` is claimed to have no test files" — verified `contracts/stencils/registry_test.go` exists and is untagged, so batch 1's `verify:` pattern runs it.
  Restated: the pattern compiles the new embed directive and also runs `contracts/stencils`' registry test, which this task does not touch and which must stay green;
  `contracts/recipes` itself contributes no test.
- r3 `[NIT:consistency]` "`fakeAlwaysDoneProducer` has two owners" — accurate;
  followed literally, cards 5 and 6 would each write the declaration into `internal/loomrecipe/fixture_test.go` and the package would not compile on a duplicate.
  Card 6 now owns the single declaration (its move lifts it out of `shape_test.go` and drops it in the fixture file);
  card 5 states explicitly that it only consumes it and must not declare a second copy.

## Pushed Back

_None._
All five findings verified accurate against source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope.
