# Plan Fix — round 4

Source review: `_mill/reviews/20260801-143421-plan-review-r4.md` (verdict: APPROVE, 3 NITs)

## Fixed

- **[NIT] Redundant isTierTagged re-check in Card 4's sleep-guard wiring** — verified accurate: the walk callback already returns early when `isTierTagged(data)` is true, before reaching the (restructured) banned-token block, so re-checking it in the new Sleep-check condition is always-true dead weight. Dropped the redundant `!isTierTagged(data)` conjunct from Card 4's Requirements (`01-tierpurity-guard-generalization.md`) and added a sentence explaining why it's unnecessary, so a future reader isn't left wondering.
- **[NIT] Scout-tagged tests are never actually executed by any verify: in this plan** — reviewer marked this accepted-as-is (well-justified, already documented as a Decision) but suggested a one-line follow-up note. Added a sentence to batch 2's `## Batch Tests` (`02-scoutengine-scout-tag.md`) explicitly framing the manual `-tags scout` run as a one-time, post-merge PR-description/TODO item.
- **[NIT] Card 15's "fourth ## Commands example line" undercounts existing lines** — verified accurate against `docs/benchmarks/running-tests.md`'s actual `## Commands` block (4 existing example lines: Tier 1, Tier 2, per-test JSON timing, one-package verbose). Fixed Card 15 (`03-substrate-rule-docs-and-sweep.md`) to say "a fifth" and name the four existing lines.

## Pushed Back

None.
