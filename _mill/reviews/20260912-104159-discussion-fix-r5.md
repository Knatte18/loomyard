# Discussion fix — round 5

Source review: `_mill/reviews/20260912-104057-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 5 NIT, 3 of them demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- "Scope says one MkdirAll, Decision says three": rewrote the Scope In-list bullet to name the exported ensure-helper and all three call sites (`run.go`, `drive.go` startup, `Reflect` post-archive), so the work inventory matches the Decision.
- "Unknown `Role`: silent empty vs hard error": resolved in favour of `("", nil)` with no read, matching `internal/pattern/pattern.go:94–98`, and corrected the contradicting Testing bullet. Rationale recorded: an error would fail a real run over optional bookkeeping, and the missing-marker `Warn` plus the zero-notes path already make the miss visible.
- "All four webster composers need a new told parameter, not one": verified against source — all four are package-level free functions taking explicit args from `deps.Geom.*`, none receives `RunDeps`, and `anchorRoot` is `pattern`'s probe root rather than a note path. Scope and the threading Decision now state that all four gain a told note-path parameter and that the four call sites (`beginbatch.go:311`, `recoverbatch.go:154`, `runlevel.go:548`, `runlevel.go:564`) compose the id into `friction.NotePath(deps.FrictionDir, id)`. Also recorded why the field goes on `RunDeps` rather than the `Geometry` struct (Told-Geometry Invariant reserves `Geometry` construction to `hubgeom`/`standalonegeom`).
- "Q&A log carries superseded answers unmarked": struck through both stale answers and pointed each at the entry that supersedes it, matching the "Correction this supersedes" convention the migration decision already uses.
- "`frictionengine` construction semantics unstated": stated that there is no `New(Deps)` — `Reflect` validates its own `Deps` first and a malformed `Deps` is the only non-nil-error case (nil `Shuttle`, empty/non-absolute friction dir while enabled, empty stencils dir); every runtime reflection failure returns nil error plus a `"failed"` `Report`. Both halves are now asserted in Testing.

## Pushed Back

_None._
