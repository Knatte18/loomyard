# Plan fix report — round 1

```yaml
slug: fabric-cutover
round: 1
review_file: 20260726-110123-plan-review-r1.md
date: 2026-07-26
```

## Fixed

- **[BLOCKING] Comment sweep under-scoped vs. card 27 zero-diff gate** — VERIFY: confirmed
  (grep found 23 fabricengine non-test files + `fabriccli/{clone.go,spawn.go,weft_verbs.go}`
  bearing bare `warpengine`/`weftengine` provenance comments, plus `initengine/init.go` and
  `undo.go` comments; card 27's `grep -rnw 'warpengine|weftengine'` would have flagged them
  all). HARM CHECK: none — expanding the sweep is the correct completion. Fixed by (a) adding
  a new Shared Decision "sweep deleted-module comments in every file you already edit"; (b)
  folding the in-file comment sweep into every batch-A/B/C editing card (cards 1, 2, 4, 6, 7,
  8, 9, 13); (c) expanding card 22 to sweep all of `fabriccli/fabric.go`'s deleted-module
  comments and card 23 to sweep all 23 fabricengine + 3 fabriccli files (Edits enumerated);
  (d) rewriting card 27's Tier-2/2b so it is a genuine zero-match confirmation (the sweeping
  now happens in the owning cards; card 27 stays `Commit: none`); (e) adding all newly-swept
  files to the overview's All Files Touched. Batch D3 context estimate re-checked at ~90k
  tokens (< 120k cap).
- **[NIT] Context omits fabricengine files defining named helpers** — VERIFY: confirmed.
  Fixed by adding `topology.go` + `add.go` to the Context of the two Topology-constructing
  cards (card 5, card 12), and citing the Shared Decisions "signature gotchas" mapping as the
  authoritative signature source in the identity-swap cards (cards 1, 2, 4, 6, 7, 8).
- **[NIT] Wrong module count in batch B narrative** — VERIFY: confirmed (`configreg.Modules()`
  has 12 rows: board, builder, burler, fabric, loom, models, reed, perch, shuttle, warp,
  webster, weft; dropping warp+weft yields 10). Fixed batch B scope "thirteen to eleven" ->
  "twelve to ten" and card 9 now states the 12->10 row count explicitly; also added the
  `configreg.go:3` package-comment sweep (it names "board, warp, weft") to card 9.

## Pushed Back

_None. All three findings were factually accurate and none conflicted with a documented
design decision or destabilised other work._
