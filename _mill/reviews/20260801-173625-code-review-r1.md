MILL_REVIEW_BEGIN
# Review: fabric: warp-rebase / remote-reconcile recovery — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-01
```

## Findings

No findings. Verified end-to-end across all four batches:

- **Batch 1 (gitrepo-primitives):** `Fetch`, `IsAncestor`, and the `hasUnpushed`→`HasUnpushed` promotion are implemented exactly to spec (`internal/gitrepo/pull.go`, `ancestry.go`, `push.go`). `IsAncestor`'s tri-state exit mapping, `validSHA`/leading-dash argument guards, and no-stderr-leak error style all match `Pull`/`ResetHard`'s established conventions. The boundary guard (`cmd/lyx/gitrepoboundary_test.go`) and `CONSTRAINTS.md`'s gitrepo Client Boundary Invariant were both updated in the same batch, matching the Shared Decision. `gogit.go`'s stale `hasUnpushed` go-git-lookup-list mention was corrected as a content fix (removed, not case-renamed), per the plan-fix round's instruction. Repo-wide grep confirms zero stale lowercase `hasUnpushed` references remain in any non-review/non-plan file.
- **Batch 2 (fabric-pull):** `reachableAnchor` (`anchor.go`) is pure, git-free, walks newest-to-oldest correctly, and is thoroughly unit-tested (`anchor_test.go`: newest/single-back/multi-back/none/empty/error-propagation). `PullResult`/`PartialPullError`/sentinel errors (`pull.go`) mirror `PartialCommitError`'s shape with roles correctly swapped for weft-first ordering. `Fabric.Pull`'s flow matches the plan's 9-step spec exactly: weft-first with immediate zero-result return on weft failure, pre-fetch `HasUnpushed` capture, fetch, up-to-date short-circuit, `IsAncestor`-based classification (never `SHAExists`), clean-ff path, double-conflict abort, empty-index advance-only, anchor-walk reconcile with weft lock scoped only around the reanchor commit, and `PatternResidueEntry` enumeration with the record-separator-leads-not-trails fix correctly applied. The integration matrix (`pull_integration_test.go`) covers every named case from the plan, including the unreachable-unpruned-object regression guard against `SHAExists`-style detection.
- **Batch 3 (cli-pull):** `pullCmd`'s `RunE` now drives `fab.Pull`, surfaces `PullResult` via `pullResultMap` with the exact field-name mapping the plan specifies, and `Short`/`Long` were rewritten to describe the both-sides/reconcile behavior accurately — verified against the current code, not stale. `cli_test.go` asserts both the `Short` non-emptiness/reconcile wording and `Long`'s both-sides/rewrite-detection prose.
- **Batch 4 (docs-sandbox):** `fabricengine/doc.go` and `fabric.go`'s package-doc cross-repo-methods lists both correctly enumerate `Pull` (and `Commit`, previously omitted). `fabric-unified-view.md` corrects both stale `SHAExists`-detection claims (the "Detection is already honest and shipped" paragraph and the slice-6 build-order entry), marks the fabric-layer half of slice 6 DONE, and leaves the orchestration-layer half explicitly open pending `loom`/`Shed` — no overclaiming of raddle regen or the LLM resolver. `docs/overview.md`'s fabric module bullet and `manifest/roadmap.md`'s Done-fabric and native-clients entries are both updated correctly. The new F6 sandbox scenario carries `**Covers:** fabric` and follows the existing bold-label format.

No BLOCKING or NIT issues found. Cross-batch contracts hold (batch 2's `Fabric.Pull`/`PullResult` signature is consumed unchanged by batch 3; batch 1's `Fetch`/`IsAncestor`/`HasUnpushed` signatures match what batch 2's `pull.go` calls). No out-of-plan files, no duplicated helpers, no Hub Geometry Invariant violations (`hubgeometry.PatternDirName` used correctly, never a `"_pattern"` literal in fabric code). Reachability-never-object-existence is upheld throughout — no `SHAExists` call appears anywhere in the rewrite-detection/anchor-walk path.

## Verdict

APPROVE
All four batches match the approved plan precisely; no constraint violations, contract mismatches, or coverage gaps found.
MILL_REVIEW_END
