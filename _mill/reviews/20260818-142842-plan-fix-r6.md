# Fixer report: plan-fix round 6 — the standalone CLI path

```yaml
round: 6
review: 20260818-142842-plan-review-r6.md
fixed: 3
pushed_back: 0
loop_end: operator-directed
```

## Fixed

- `20260818-142842-plan-review-r6.md` — "[BLOCKING:design] Card 3's plain-repo fixture guidance traps the implementer into a gitkit Leaf Invariant violation": replaced card 3's vague "via the existing `gitkit` fixture helpers the file already imports" with the exact mechanism, `gitkit.MustRun(t, dir, "git", "init", "-b", "main")`, and added an explicit prohibition on `gitkit.CopyRepo`.
  Verified independently: `internal/gitkit/callerset_enforcement_test.go` declares `allowedCopyRepoCallerDir = "internal/lyxcwd"` and `TestCopyRepoCallerSet_LyxcwdOnly` walks every package's AST for a `gitkit.CopyRepo` selector call; `CONSTRAINTS.md`'s gitkit Leaf Invariant states the same pin; and `internal/preflight/preflight_integration_test.go` today uses only `gitkit.MustRun`, which is unpinned.
  The trap was real and specific: `CopyRepo` is the semantically obvious pick for "a plain git repo with a bare origin, never a hub", which is exactly what three of card 3's seven rows need.
  The card now also records why this matters more than an ordinary Context gap — the violation is invisible to batch 1's own `verify:`, since `go test ./internal/preflight/...` compiles and passes with it present and only `go test ./internal/gitkit/...` fails, which no batch in this plan runs.
  Added `internal/gitkit/callerset_enforcement_test.go` and `CONSTRAINTS.md` to card 3's `Context:`.

- `20260818-142842-plan-review-r6.md` — "[NIT:scope] `claudeengine.New()` referenced in Requirements without a backing Context entry": added `internal/shuttleengine/claudeengine/claudeengine.go` to batch 4 card 13's and batch 5 card 20's `Context:` lists.
  Verified accurate — both cards' Requirements name `claudeengine.New()` and neither listed the package.
  Low practical risk, since `internal/webstercli/wiring.go` is in both cards' Context and shows the import and call, but the Context allowlist rule is mechanical.

- `20260818-142842-plan-review-r6.md` — "[NIT:scope] `internal/perchcli/run_integration_test.go` is untouched but unmentioned as batch 5's most relevant regression coverage for card 22": added a paragraph to batch 5's `## Batch Tests` naming the file and its four tests.
  Verified accurate, and the test names corrected against the file rather than taken from the review: they are `TestRunCLI_Run_FabricSyncRunsOnEngineError`, `TestRunCLI_Run_FabricCommitExcludesLockFiles`, `TestRunCLI_Run_FabricCommitExcludesLockFiles_NestedRelPath` and `TestRunCLI_Run_BusyBlockSkipsFabricSync`.
  These exercise precisely the block-exit fabric sync card 22 reroutes from `fabricengine.Open(c.layout)` onto `c.openFabric()`, including the `opts.SkipGit` short-circuit and the `ErrBlockBusy` skip that must compose unchanged with the new nil-opener guard.
  They run in hub mode, where this task claims byte-identity, so a failure there is the clearest signal that the reroute changed hub behaviour rather than relocating it.

## Pushed Back

_None._

## Loop termination

The loop ends here at round 6, by operator decision, rather than by the convergence gate or the round cap.
`max_review_rounds` is 7, so round 7 was available and was not run.

Recorded honestly because the gate's own predicate was not satisfied: round 6's verdict is `REQUEST_CHANGES` with `blocking_count: 1`, which is the 4d branch — its normal continuation is another round to re-review the fixes above, and none of round 6's three findings has been re-reviewed by any reviewer.
The operator weighed that against the remaining rounds' expected yield and directed handoff.

Context for that judgement, for whoever reads this later: rounds 4 and 5 both returned `APPROVE` with zero BLOCKING and a single ceiling-demoted `consistency` finding each, and this stage's `blocking_classes` (`["design", "scope"]`) guarantees every `consistency` finding is demoted while the convergence gate refuses to converge whenever a demoted finding is present — so the loop could not have converged on a consistency-only round no matter how many remained.
Round 6 nevertheless surfaced a genuine BLOCKING design trap, which is the counter-evidence: the rounds were not yet returning pure noise.
