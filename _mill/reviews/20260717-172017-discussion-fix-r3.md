# Discussion fixer report — round 3

Source review: `_mill/reviews/20260717-171936-discussion-review-r3.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- `20260717-171936-discussion-review-r3.md` → **[NOTE] Hung-server teardown may itself block**: added to `lsp-client-surface`'s deadline contract that timeout teardown **hard-kills** the subprocess (`cmd.Process.Kill()`) rather than running the graceful `shutdown`/`exit` handshake, which could re-block on the same unresponsive server; the graceful `close` path is reserved for the normal end of a run.
- `20260717-171936-discussion-review-r3.md` → **[NOTE] TypeScript/Rust built-ins ship unmeasured**: added an explicit bullet under `language-server-registry` stating the TS/Rust entries ship as unverified marker→command configuration, out of the measurement matrix, so a plan writer schedules no benchmark validation for them.
- `20260717-171936-discussion-review-r3.md` → **[NOTE] Overlay schema for markers + AND/OR match-mode undefined**: added an entry-schema bullet pinning the overlay fields (`markers`, `match` ∈ {`all`,`any`} as a validated closed vocab, `command`, `install_hint`) and confirming they ride the same `yaml.Decoder.KnownFields(true)` loud-error path as modelspec.

## Pushed Back

(none — all three NOTEs were accurate and harmless to fix.)
