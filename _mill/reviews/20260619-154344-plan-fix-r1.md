# Plan fix — round 1

Source review: `_mill/reviews/20260619-154219-plan-review-r1.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 2 NIT).

## Fixed

- review-r1 [BLOCKING] "Decision violation: WEFT_SKIP_PUSH must skip the detached spawn" — Card 8 `spawnPush` now returns nil immediately when `WEFT_SKIP_GIT` or `WEFT_SKIP_PUSH` is `"1"` (guard in both `spawn_windows.go`/`spawn_other.go`), so no child is forked in skip mode; Card 10 `sync` note updated to reflect the no-op.
- review-r1 [BLOCKING] "Status junction check: os.Readlink on Windows junctions" — Card 9 rewritten to use the `os.ModeSymlink` mode bit + `filepath.EvalSymlinks` on both ends (never `os.Readlink`, which mis-resolves NTFS junctions to `\??\C:\…`), matching the existing `links.go` approach; `status_test` now requires a Windows junction case via `cmd /c mklink /J` (privilege-free), `os.Symlink` elsewhere.
- review-r1 [NIT] "'seven methods' vs eight throughout Batch 1" — Batch 1 scope + Card 1 prose changed "seven"→"eight"; `WeftLyxDirFor(slug)` added to the "consumed downstream" interface list.
- review-r1 [NIT] "Card 16 Context missing worktree.go/config.go for w.cfg.BranchPrefix" — added `internal/worktree/worktree.go` and `internal/worktree/config.go` to Card 16 Context (and to Card 15, which also references `w.cfg.BranchPrefix`).

## Pushed Back

(none — all findings were factually accurate, harmless to fix, and consistent with the discussion's decisions. Additionally, Card 14's `seedLyxJunction` "correct junction" check was proactively aligned to the same `filepath.EvalSymlinks` approach as the BLOCKING-2 fix, since it shared the identical Windows-junction pitfall.)
