# Plan fixer report — round 5

Source review: `_mill/reviews/20260625-134641-plan-review-r5.md` (verdict REQUEST_CHANGES, 1 BLOCKING + 2 NITs).

## Fixed

- `20260625-134641-plan-review-r5.md` — [BLOCKING] "weft/status_test.go orphans the fslink import" — card 20 now requires removing the now-unused `internal/fslink` import from `weft/status_test.go` after deleting the two junction tests (it was used only by `TestStatus_JunctionOk_Windows`; an unused import is a Go compile error that would fail batch-6 verify).
- `20260625-134641-plan-review-r5.md` — [NIT] "overview.md keeps dead links to deleted modules/warp.md" — card 32 now requires removing/retargeting the two `modules/warp.md` links in `docs/overview.md` (the module-list `See [modules/warp.md]` and the doc-lifecycle reference), since card 31 deletes that file in the same batch.
- `20260625-134641-plan-review-r5.md` — [NIT] "Archival docs left with stale package/command refs" — card 33 now explicitly excludes `docs/roadmap.md` and `docs/benchmarks/test-suite-timing.md` from the sweep as intentionally frozen point-in-time/historical records (rewriting their package names would falsify the record).

## Pushed Back

(none — all three findings were factually accurate with no harm in fixing; applied under the default fix-everything rule.)
