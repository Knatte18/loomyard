MILL_REVIEW_BEGIN
# Review: fabric: fold snapshot-tracking into the Warp-SHA trailer — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (claude-sonnet-5, per system identification)
reviewed_file: plan/
date: 2026-07-31
```

## Findings

### [BLOCKING] Cards 11 and 20 cite gitrepo functions absent from their Context
**Location:** batch 3 card 11 (`Add the SnapshotWarpSHA reader`); batch 4 card 20 (`Pin the correspondence overwrite`)
**Issue:** Card 11's Requirements names `f.Warp.SHAExists(sha)` and `f.Warp.ChangedFilesSince(sha)` and asserts a specific fact about `ChangedFilesSince`'s own godoc ("already asks callers to check SHAExists first") to justify the new godoc's three-step idiom, but Card 11's Context (`index.go`, `trailer.go`, `fabric.go`) omits `internal/gitrepo/gitrepo.go`, where both methods and that exact godoc live. Card 20 similarly cites `f.Warp.SHAExists` in its dangling-SHA case with the same file missing from Context (`index.go`, `corrindex.go`, `revert.go`, `commit.go`, `syncweft_integration_test.go`).
**Fix:** Add `internal/gitrepo/gitrepo.go` to both cards' Context lists — sibling cards in the same batches (16, 19, 21 in batch 4, and card 22 in batch 5) already do this correctly whenever they cite a gitrepo method by name, confirming the omission here is an oversight rather than a deliberate exclusion.

## Verdict

REQUEST_CHANGES
Otherwise exceptionally well-grounded plan; fix the two Context-completeness gaps (cards 11, 20) before proceeding.
MILL_REVIEW_END
