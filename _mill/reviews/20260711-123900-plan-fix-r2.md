# Plan fixer report — round 2

## Fixed

- `20260711-123732-plan-review-r2.md` — "[BLOCKING] Validate never receives worktreeRoot for check 5": card 6's signature is now `Validate(plan *Plan, worktreeRoot string, caps ValidateCaps)`; card 25 passes the deps' worktree root; card 27 passes `layout.Cwd`.
- `20260711-123732-plan-review-r2.md` — "[MEDIUM] poll gather computes the git diff on every running tick": card 18 pins `Changed`/`Dirty` filled ONLY when `Report != nil` (gitquery runs exclusively in the report-present branch; every-tick diff named a defect); card 28's wiring restated as lazy.
- `20260711-123732-plan-review-r2.md` — "[NIT] Distill duplicates report.OutOfScope as a separate param": card 15's `Distill` drops the `outOfScope` parameter and reads `report.OutOfScope`.
- `20260711-123732-plan-review-r2.md` — "[NIT] Card 30 lists dynamic guards as Edits": Edits trimmed to `cmd/lyx/main.go` + `helptree_test.go`; registration/longlist guards noted as dynamic must-pass; overview's All Files Touched updated.

## Pushed Back

(none)
