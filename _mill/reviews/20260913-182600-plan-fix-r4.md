# Plan fix — round 4

Source review: `_mill/reviews/20260913-182405-plan-review-r4.md` (holistic, APPROVE, 0 BLOCKING + 1 NIT).

## Fixed

- r4 holistic — "[NIT:scope] Card 20's \"three sites\" count is off by one".
  Verified: only two shed-recipe `Env` literals carry a stencils directory in that package — `internal/loomrecipe/shape_test.go:88` and `internal/loomrecipe/fixture_test.go:606`.
  The apparent third at `internal/loomrecipe/fixture_test.go:161` is a landing-dependencies literal, a different struct whose identically-named field has no specs sibling.
  The miscount came from a plain grep for the field name during round 1's fix and was mine, not the reviewer's.
  Corrected card 20 to say two sites, one per file, and added an explicit warning that the landing-dependencies literal in the same files is a different struct and must not be edited — which is the actual hazard, since an implementer chasing the phantom third site would try to add the field to a struct that does not have one.

## Pushed Back

_None._
The finding verified as factually accurate against the cited lines, and the fix breaks no existing functionality, conflicts with no documented decision in `_mill/discussion.md`, and destabilises nothing outside this task.
