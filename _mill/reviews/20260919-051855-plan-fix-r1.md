# Plan fix — round 1

Source review: `20260919-051620-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `[NIT:design]` "Card 15's CLI error string names an unsourced \"remote name\"" — batch 4 card 15's Requirements now state that the remote name is the literal `"origin"`, hardcoded at the `fabriccli` site, that `originRemoteName` stays unexported because exporting it purely to spell one error string would widen the engine API for no caller, and that the hardcoded literal matches the discussion's `hardcoded-origin` decision, so the two spellings cannot drift over a value neither side can change.

## Pushed Back

_(none)_
