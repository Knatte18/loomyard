# Plan fixer report — round 6

Source review: `_mill/reviews/20260726-162132-plan-review-r6.md` (APPROVE, 4 NITs)

## Fixed

- r6 review, "[NIT] Seed-file path ambiguous across same-round retries" —
  card 10 now pins the seed as round-scoped: resolved once at attempt 1's
  token before any attempt runs, threaded into every attempt's
  `AttemptInput.SeedPath`, never recomputed per attempt.
- r6 review, "[NIT] queuedShuttle handoff-scripting extension
  underspecified" — card 9 now names the concrete mechanism: a
  `handoffContent string` field on the scripted-call entry, written to
  `Spec.OutputFiles[1]` when non-empty.
- r6 review, "[NIT] Effort-only param-rejection check triplicated with no
  shared helper" — cards 12/13 now share one exported helper,
  `perchengine.ResolveModelSpec(spec, reg) (model, effort string, error)`,
  called by `LoadConfigWithRegistry` and by `decodeProfile` for both
  profile fields — identical errors structurally, not by copy-paste.
- r6 review, "[NIT] Batch DAG rationale doesn't hold for the batch3→batch4
  edge" — overview reworded: batch 4's real file-overlap predecessor is
  batch 2 (`internal/perchengine/doc.go`); the `depends-on: [3]` edge is a
  deliberate single-chain simplicity choice satisfying that dependency
  transitively.

## Pushed Back

- (none)
