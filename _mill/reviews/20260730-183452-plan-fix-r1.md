# Plan fix — round 1

Source review: `_mill/reviews/20260730-183348-plan-review-r1.md` (APPROVE, 1 NIT)

## Fixed

- NIT "CoalescePushBothAt homes lock at weftPath unconditionally" — verified accurate: `CoalescePushBothAt` unconditionally builds the absorbing lock via `ensureWeftLockDirAt(weftPath)`, so a warp-only bypass push (empty `weftPath`, which the CLI still accepts) would `mkdir .weft` + `git rev-parse` relative to cwd. Fixed in Batch 2 card 4 by adding an explicit empty-`weftPath` guard that returns a descriptive error (chosen over a `warpPath` fallback, which would place a lock at the pristine host root and violate Shared Decisions `lock-artifact-under-weft` / `no-host-root-gitrepo-push-lock`). Batch 3 card 7 gains a matching behavior note: the warp-only bypass push now surfaces that error via `output.Err` instead of the old silent single warp push, which is unreachable in production since `SpawnDetachedPush` always injects both paths.

## Pushed Back

(none)
