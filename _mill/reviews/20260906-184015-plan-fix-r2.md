# Plan fix report — round 2

Source review: `_mill/reviews/20260906-183331-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING, 3 NIT — two of them demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r2 holistic, "SetDurableSinkDir's \"exact ... body\" decision contradicts Card 4" — verified: the overview's `hub-mode-is-byte-identical` decision claimed the function keeps "its exact name, signature, and body", while batch 2 card 4 extracts that body into `resetDurableSinkLocked` and makes the function a lock-plus-delegate wrapper.
  Fixed by rewording the decision to bind the observable call-site contract — exact name, exact signature, exact reset semantics — and naming card 4's refactor as the deliberate exception to literal body text.
- r2 holistic, "New wireHub-sink-sentinel test's cleanup falls outside Cards 5/6's stated scope" — verified: both cards scoped the cleanup obligation to tests that reach `wireStandalone`, while the new sentinel test reaches `wireHub` and calls `logger.SetDurableSinkDir` directly, which the overview's own `sink-override-is-process-global` decision already covers.
  Fixed by restating the obligation in both cards as "every test that causes the override to be set, whether by reaching `wireStandalone` or by calling the setter directly", and naming the sentinel test as explicitly in that set.
- r2 holistic, "Card 5 and Card 6 specify cleanup coverage at different granularity" — verified: card 6 enumerated its four shipped burlercli tests while card 5 left the webstercli equivalent implicit.
  Fixed by enumerating the shipped webstercli tests that reach `wireStandalone` in card 5, matching card 6's granularity.

## Pushed Back

_(none — all three findings were factually accurate, and none of the fixes broke existing functionality, conflicted with a documented decision, or destabilised work outside this task.)_
