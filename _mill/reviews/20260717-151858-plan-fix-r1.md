# Plan-fix report — round 1

Source review: `_mill/reviews/20260717-151507-plan-review-r1.md` (verdict REQUEST_CHANGES, 2 BLOCKING).

## Fixed

- **[BLOCKING] "Card 1 `go mod tidy` strips x/tools before any import"** — moved the
  `golang.org/x/tools` dependency addition from Batch 1 Card 1 to Card 2 (the first card that
  imports it). Card 1 now imports stdlib only, has `Edits: none`, and drops the
  `go get`/`go mod tidy` line; Card 2 gained `go.mod`/`go.sum` in `Edits:` and a leading
  `go get golang.org/x/tools@latest` step, so the require is never pruned before an importer
  exists. Card 1 commit message updated to drop the "add x/tools dep" clause.
- **[BLOCKING] "Cards 7 and 8 declare a Commit but stage nothing"** — collapsed Batch 3 cards
  7, 8, 9 into a single Card 7 ("Run all arms, cross-check precision, and write the findings
  doc") whose committed product is the tracked `docs/research/codeintel-spike.md`; all
  intermediate measurement output remains gitignored `.scratch/codeintel/`. Batch 3 `cards:`
  is now 2 (Card 6 commits `.lsp.json`, Card 7 commits the doc) — every card now stages a real
  change. Batch Scope and Batch Tests updated; Batch 4's card renumbered 10 → 8 to keep global
  card numbering contiguous.

## Pushed Back

_None — both findings were factually accurate and their fixes cause no harm (verified against
`.gitignore`'s `**/.scratch/` rule and Go's module-pruning behaviour)._
