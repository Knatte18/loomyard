# Discussion fix — round 5

Source review: `_mill/reviews/20260811-033636-discussion-review-r5.md` (verdict APPROVE, 0 blocking, 3 NIT).
All three fixed; none pushed back.

## Fixed

- r5 · "stderr-sniffing exit path contradicts \"uniform binding\"" — verified at `internal/fabricengine/index.go:217`. A tree-wide grep found a **second** member the review did not name, `internal/gitrepo/push.go:64` (`no rebase in progress`). Added **stderr-content-recovery** as a fourth class in the taxonomy with both members enumerated, a regeneration query, and a disposition (checked form, sniff moved onto `gitErr.Stderr`); softened the uniformity claim to name the exception.
- r5 · "Merge rule does not cover a `%d` from a *different* call" — verified at `internal/fabricengine/reconcile.go:546`/`:550`, which render `exitCode` from the `rev-parse` call at `:528` while merging the error of the `branch --show-current` call at `:538`. Added an exception clause to the `(git exit %d)` drop rule: keep the earlier call raw, or capture its `*GitError`.
- r5 · "`wrapProbeError` call-path count is wrong and mixes line kinds" — verified: seven calls across two functions (`:69`, `:72`, `:79`, `:93`, `:96`, `:134`, `:137`), not four, and `probeTreeHasPath` was unmentioned. Re-keyed to call sites, labelled a snapshot, and given a regeneration query.

## Pushed Back

None.
