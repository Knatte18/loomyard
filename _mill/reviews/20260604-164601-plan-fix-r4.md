# Plan Fix — round 4

Source review: `_mill/reviews/20260604-164411-plan-review-r4.md`

## Fixed

- **[BLOCKING] runGit drops exit code** — changed `runGit` signature to `(stdout, stderr string, exitCode int, err error)`; err set only for execution failures; callers inspect exitCode directly. Updated `pull` and `commitPush` to use exitCode.
- **[NIT] task.go in git_test.go Context unused** — removed from Card 12 Context.
- **[NIT] set_phase []interface{} conversion unspecified** — added conversion note to Card 16: validate length == 2, copy to `[2]interface{}`.
- **[NIT] slug_for_msg fallback unspecified** — added per-operation fallbacks: SetPhase/RemoveTask/SetDeps use the slug arg stringified; UpsertTasksBatch uses "batch".

## Pushed Back

_(none)_
