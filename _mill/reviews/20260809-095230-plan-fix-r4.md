# Plan fix — round 4

Source review: `_mill/reviews/20260809-094321-plan-review-r4.md`

## Fixed

- `[BLOCKING:scope]` "rawgitmutation_test.go card leaves three var-doc comments naming builderengine" — verified against source: `rawGitMutationScanPackages`, `rawGitMutationAllowlist` and `rawGitMutationMinScannedFiles` all carry doc comments naming the deleted package or a two-package walk, and card 1's requirement named only the file-header and function doc comments.
  The requirement now covers **every** comment in the file that names the deleted package or describes the guard as two-package, listing all five sites.
  It also settles the constant itself rather than leaving a judgment call: the vacuous-scan floor is 4 and `internal/websterengine` alone contributes 24 non-test `.go` files, so the one-package walk still clears it and only the doc comment changes.
- `[BLOCKING:scope]` "sync_integration_test.go card misses a doc comment naming BUILDER" — verified: `TestFabricSync_CommitsAtEveryRelPathDepth`'s doc comment reads "a webster commit must hold back BUILDER's pause flag", a fifth comment block the enumeration missed, and card 6 forbids re-editing this file.
  The requirement is now exhaustive for the file — every comment and message string naming builder in any form, including the four `t.Fatalf` strings on the seeding calls, which the review did not name but which fail the same pattern — and states that the sweep's bare-word pattern is case-insensitive so the uppercase spelling is not a special case.

## Pushed Back

_None._
