# Plan fix — round 2

Source review: `_mill/reviews/20260808-062910-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NIT).

## Fixed

- r2 holistic, "[BLOCKING] Card 3's temporary banned-import proofs will not compile as described" — card 3 now requires every temporary import to be a blank import (`_ "…"`), with the reason stated inline.
  Accurate and load-bearing: a plain import of a package the file never references is an "imported and not used" compile error, so `go test` would report a build failure instead of the seam test's per-predicate message, and the `internal/lyxcwd` non-regression proof could never observe green.
  The proposed fix is also sound — both tests read `astFile.Imports`, which records the path regardless of the `_` alias — so the blank import still trips the guard.
  Applied to all four banned-import proofs, the `lyxcwd` non-regression proof, and both guard proofs on `internal/scoutengine/lspclient.go`.
- r2 holistic, "[NIT] 'matching every existing guard in the repo' overstates consistency" — narrowed the Shared Decision's opening sentence to the repo's import guards and named the exception explicitly.
  Verified accurate: `internal/lyxcwd/raddle_guard_test.go` lines 68-91 contain a `t.Run("predicate", …)` table with `want: true`/`want: false` cases, and that file is cited in card 2 as the naming precedent.
  The Rationale's four named modules were already correct and are unchanged.
- r2 holistic, "[NIT] Card 5's leaf-occurrence reference list is inaccurate/incomplete" — corrected the list to the file's real occurrences.
  Verified against `docs/overview.md` by grep: the word appears for `internal/reedengine/render` (line 203), `internal/modelspec` (216), `internal/tokenvocab` (217, 315), `internal/pattern` (218, 283), and `internal/shell` (219).
  There is no `lyxtest`+"leaf" occurrence in the file;
  `lyxtest` was dropped from the list.

## Pushed Back

_None._
