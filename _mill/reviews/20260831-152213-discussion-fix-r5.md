# Discussion fix — round 5

Source review: `_mill/reviews/20260831-152127-discussion-review-r5.md` (APPROVE, 0 blocking, 3 NIT).

## Fixed

- r5 `[NIT:consistency]` "Search stub shape omits `--jq`" — `--jq <expr>` added to the search invocation shape in the stub decision and to the harness assertion in Testing, alongside `-X GET` and the `Accept:` header.
- r5 `[NIT:design]` "Preflight `--jq` rationale rests on an unverified premise" — the causal claim that dropping `--jq` would lose the HTTP status is now explicitly marked **not verified**, and the decision to keep `--jq` is restated on two grounds that are verified (it mirrors `fetch()`'s success path, and it keeps every call to one stub-checkable shape).
- r5 `[NIT:decision]` "Empty `text_matches` behaviour stated only in Testing" — the absent/empty-fragment case is folded into the "Single output format" decision: the record is still emitted with an empty third field, so the record shape is invariant.

## Pushed Back

(none — every finding verified accurate against the source, and no fix broke existing functionality, conflicted with a documented design decision, or destabilised work outside this task's scope)
