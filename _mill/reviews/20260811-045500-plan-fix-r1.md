# Plan fix — round 1

Source review: `_mill/reviews/20260811-045019-plan-review-r1.md`.

## Fixed

- r1 — "[BLOCKING:scope] Card 4 Context omits `recordbatch.go`, which its own Requirements name" — verified accurate: card 4's `Requirements:` names `recordbatch.go`'s `RecordResult.Digest` handling while `Context:` listed only `doc.go`, and `Context:` is an allowlist.
  Added `internal/websterengine/recordbatch.go` to card 4's `Context:`.
- r1 — "[NIT:consistency] Card 11's \"`:68`'s deferred-slot line\" reference is stale" — verified against the live file: `manifest/roadmap.md:68` is the unrelated `gitexec` decision item, and "deferred phase slot between Webster and Finalize" is at `:102`.
  Rewritten to quote the line's text and cite `:102`, matching the plan's own quote-text-not-stale-numbers convention (Shared Decision `line-numbers-in-this-plan-are-pre-edit-anchors`).
- r1 — "[NIT:consistency] Card 2's acceptance grep is case-sensitive and misses the capitalized variant it exists to catch" — verified: `discussion-format.md:12` reads `**Discussion-review gate**` with a capital D.
  Added `-i` to the grep in card 2's acceptance criterion, in card 7's mirrored criterion, and in batch 1's `## Batch Tests` criterion 1, each with a one-line note on why the flag is load-bearing.
- r1 — "[NIT:scope] Card 2's \"the Plan producer\" site inventory omits `:12` without a hand-off note" — verified: `:12` carries an eighth occurrence, and the `:3`/`:14` skips already carry hand-off notes while `:12` did not.
  Card 2 now states that `:12`'s "the Plan producer" and "the **Discussion-review gate**" are both subsumed by card 3's rewrite of the `:11–12` bullet.

## Pushed Back

_None._
All four findings verified accurate against the live tree, and no fix broke existing functionality, contradicted a Decision in `_mill/discussion.md`, or destabilized work outside this task.
