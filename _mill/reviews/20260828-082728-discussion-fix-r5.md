# Discussion fix report — round 5

Source review: `_mill/reviews/20260828-082613-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING after the stage ceiling, 2 NIT of which 1 demoted from BLOCKING).

Round 5 is the configured round cap. The convergence gate's demoted-finding predicate is not satisfied, so this round is taken as an implicit approval per mill-start's Convergence gate; the shortfall is noted in the commit message.

## Fixed

- `20260828-082613-discussion-review-r5.md` — "[NIT:scope] Multiplexer contract surface gains two verbs, undisposed": verified — `grep` finds no `set-hook` or `resize-pane` anywhere in `internal/`, neither appears in `requiredSubcommands` (`probe.go`) or `doc.go`'s "Subcommand set" paragraph, and `doc.go`'s closing "no new psmux risk" sentence is falsified by this task. Added Decision `set-hook-and-resize-pane-are-optional-wire-surface`: the probe does not grow (gating it would take every reed verb down on a psmux lacking `set-hook`, over an option designed to degrade silently), the doc splits required from optional verbs, the closing sentence is rewritten rather than extended, and `TestMultiplexerContract` gains a case. Renamed the doc decision to `doc-work-is-additions-at-four-named-sites` with the fourth site named, updated the Scope bullets, and added a Testing subsection specifying the contract case (independent `set-hook` array entries via `show-hooks -w`; `window-resized` firing after the layout resize).
- `20260828-082613-discussion-review-r5.md` — "[NIT:design] Engine-side seam for obtaining pins is unspecified": verified — `planLayout` owns `toRenderStrands`, the present-pane filter, and the `HeaderPaneID` blanking the zero-pin disposition depends on. Added an engine-seam clause to `pins-come-from-render-policy-not-raw-config` fixing that the mapping happens exactly once, at `planLayout`'s site, and leaving only the signature shape to the plan.

## Pushed Back

_None._
