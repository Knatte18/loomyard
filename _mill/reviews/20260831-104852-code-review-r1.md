MILL_REVIEW_BEGIN
# Review: Reconsider the collapsed strand strip default size — holistic

```yaml
duration_s: 75.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-31
```

## Findings

None. The single batch/card is realized completely and consistently:

- `template_posix.yaml:5` and `template_windows.yaml:5` carry the byte-identical `collapsed_strip_rows: 6` line with the exact readability-rationale and reconcile-adoption-caveat comment text the card specifies, matching the phrasing pattern of the sibling `mouse`/`watchdog` keys. No other key in either template changed.
- `config_test.go:61-63` and `:138-140` both assert `CollapsedStripRows != 6` with `want 6` in the `t.Errorf` format strings, at the exact line numbers the card named. No sibling assertion (`Tmux`, `Shell`, `Width`, `Height`, `MinFullRows` still `3`, `StrandName`, `DebugLog`, `Mouse`, `Header.HeightRows`) was touched.
- `doc.go:371-372` rewords the silent-layout-rescale anecdote to `turned a 3-row collapsed strip (3 was the then-default; it is 6 today) into 1 row`, verbatim per the card, while the `"220x50"`/`"100x30"` live-measurement figures and the `CollapsedStripRows` mention at line 374 stay untouched; re-wrap is contained to that one bullet.
- `attachgeometry_integration_test.go` is unedited and asserts against `e.cfg.CollapsedStripRows`/`e.cfg.Header.HeightRows` (not a literal), so it follows the template automatically, consistent with the card's rationale for listing it as Context only.
- `config.go`'s `Config` struct and `LoadConfig` are unchanged and consistent with both templates' shape; no cross-batch contract issues since this is a single batch.
- No file under `internal/reedengine/render/` or `internal/configsync/` was touched, and no test file other than `config_test.go` changed — matches the `no-render-or-configsync-change` Shared Decision. `render`/`sandbox`/`manifest` mentions of `collapsed_strip_rows` are magnitude-agnostic key-name references, not literal-`3` assertions, so none needed updating.
- The `## Files included` manifest (8 files) matches exactly the plan's `Context:`/`Edits:` union plus the two plan files — no out-of-plan file present.
- No constraint violation: this package is outside every listed structural invariant's scope (no cwd/geometry/CLI/git surface touched).

## Verdict

APPROVE
Single-card implementation matches the plan's value, assertions, comments, and doc.go reword exactly, with no scope creep.
MILL_REVIEW_END
