# Discussion fix — round 5 (round cap)

Source review: `_mill/reviews/20260825-095559-discussion-review-r5.md` (verdict REQUEST_CHANGES, 1 BLOCKING, 3 NITs).

Round 5 is the configured `roles.discussion-review.holistic.rounds` cap.
Per an explicit operator directive during this run ("ved cap: gjør handoff uansett"), this round's findings were fixed and the task proceeds to Handoff rather than taking the `--auto` non-progress extension round or setting `phase: blocked`.

## Fixed

- `20260825-095559-discussion-review-r5.md` — "[BLOCKING] Limiter unstubbed in non-limiter unit tests": rewrote the Constraints bullet so the no-real-time-wait rule binds on **every** untagged test that reaches the RSS tier, not just the limiter's own tests, spelling out why (process-wide singleton + `stubResponses` sending no `x-ratelimit-reset` ⇒ 60 s real sleep on the second such test); specified a shared `stubRedditRSSLimiter(t)` helper (no-op `redditRSSWait`, controllable `timeNow`, `t.Cleanup` reset of token and `nextAllowed`) and made installing it the Testing section's first instruction.
- `20260825-095559-discussion-review-r5.md` — "[NIT] docs decision says five sites, lists six": corrected the count to six and added `reddit.go`'s three comment sites to the Scope "In" doc-updates bullet, so a plan writer reading Scope alone cannot drop them.
- `20260825-095559-discussion-review-r5.md` — "[NIT] `x-ratelimit-reset` parse strictness unpinned": pinned `strconv.ParseFloat` with round-up-to-whole-seconds, citing the captured float-formatted `x-ratelimit-remaining: 0.0` as the evidence Reddit float-formats this header family, and added a `"53.0"` row to the limiter test table.
- `20260825-095559-discussion-review-r5.md` — "[NIT] README lede also mis-describes Reddit fetching": named `plugins/prowler/README.md:3` explicitly as part of doc site 1, alongside the credentials section and the site-adapters paragraph.

## Pushed Back

_None._
