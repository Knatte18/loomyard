# Plan fix — round 1

Source review: `_mill/reviews/20260616-115338-plan-review-r1.md` (verdict APPROVE, 3 × `[NIT]`).

## Fixed

- `[NIT] Card 6/8: CLI usage strings not named explicitly` — card 6 (ide) Requirements
  now names `usage: mhgo ide ...` → `lyx ide ...` in `cli.go`; card 8 (worktree) now
  names the `launchers.go` generated commands `mhgo ide spawn`/`mhgo ide menu` → `lyx
  ide ...` and `config.go` `run "mhgo init"` → `run "lyx init"`, noting the
  `launchers_test.go`/`config_test.go` assertions move with them.
- `[NIT] Card 6: TestMenuRequiresMhgoDir function name not named` — card 6 Requirements
  now explicitly renames `func TestMenuRequiresMhgoDir` → `TestMenuRequiresLyxDir` in
  `menu_test.go`; card 9 (docs) now names the matching `TestMenuRequiresLyxDir` update
  in `docs/benchmarks/test-suite-timing.md`.
- `[NIT] Card 5: board init.go JSON key "mhgo_dir" not named` — card 5 Requirements now
  explicitly renames the JSON status output key `status["mhgo_dir"]` → `status["lyx_dir"]`.

## Pushed Back

(none)
