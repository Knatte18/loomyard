# Plan Fix — round 3

Source review: `20260609-181506-plan-review-r3.md`

## Fixed

- **[BLOCKING] TestMalformedYAMLError will fail after migration** — Updated Card 16 requirement for `TestMalformedYAMLError`: changed the assertion from `stringContains(errMsg, "parsing YAML") || stringContains(errMsg, "error")` to `stringContains(errMsg, "yaml:")`, matching the raw yaml.v3 error format returned by `internal/config.Load`.
- **[NIT] Batch-4 scope claims behaviour is unchanged; .mhgo/ layer is dropped** — Amended the batch 4 scope sentence to note that signatures are unchanged but the `.mhgo/<module>.yaml` override layer is intentionally removed (callers using only `_mhgo/` are unaffected; others must migrate).

## Pushed Back

_(none)_
