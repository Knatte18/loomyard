# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260824-141640-plan-review-r2.md` (holistic, round 2, REQUEST_CHANGES, 1 BLOCKING / 1 NIT).

## Fixed

- r2 holistic, "[BLOCKING:design] Card 17 names a test that does not exist" — verified: `grep "func TestNew_" internal/loomrecipe/*.go` lists ten tests and none is `TestNew_ValidateSucceedsOnTheRealList`. The test carrying the bounce-budget-exhaustion premise is `TestNew_PassesShedValidation` at `internal/loomrecipe/shape_test.go:229`, in the same file card 17 already edits. Card 17 now names that identifier.
- r2 holistic, "[NIT:design] Card 13's \"typed-nil vs untyped-nil\" test split is not a real distinction for these two fields" — verified: `Env.DiscussionSpec` is `shedadapters.SpecSource` and `Env.CommitDiscussion` is `func() error`, both concrete named func types, so a `nil` assignment already boxes as a typed-nil at `requireSeam`'s `any` parameter and the plain-nil branch is unreachable for either. `entries_simple_test.go`'s `zeroEnvField` covers the structurally identical `WebsterRun` func field with a bare zero-value assignment and no second variant. Card 13 now asks for one nil subtest per field, names `zeroEnvField` as the idiom to follow, and states explicitly that `entries_simple_test.go` is read as precedent and not edited — keeping the card's file list exact.

## Pushed Back

_None. Both findings verified accurate against the code and were fixed._
