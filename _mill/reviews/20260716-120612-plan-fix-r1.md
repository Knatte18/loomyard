# Plan-fix report — round 1

Source review: `_mill/reviews/20260716-120333-plan-review-r1.md` (verdict REQUEST_CHANGES,
1 BLOCKING + 1 MEDIUM + 2 LOW + 1 NIT). All findings verified against code and fixed under the
`mill-receiving-review` default rule.

## Fixed

- 20260716-120333-plan-review-r1.md — "[BLOCKING] Keepalive regression test is unreachable by
  verify": verified `internal/muxengine/contract_integration_test.go` begins `//go:build integration`.
  Changed batch 4's `verify:` (overview Batch Index + batch frontmatter) to
  `go test -tags integration ./internal/muxengine/... ./internal/muxcli/...`, and corrected the
  "untagged real-tmux" claims in the `go-verify-scoping-and-tiers` Shared Decision, card 20, and
  batch-4 Batch Tests to state the file is integration-tagged and run via `-tags integration`.
- 20260716-120333-plan-review-r1.md — "[MEDIUM] Folding HeaderPaneID into boundPaneIDs flips reaping
  semantics": verified `boundPaneIDs` gates both `anyBoundPresent` and the reap-exemption
  (reconcile.go:99-108) and the documented "no bound content, foreign panes untouched" invariant
  (reconcile.go:43-45). Rewrote card 18 to compute a SEPARATE `exemptPaneIDs` = `boundPaneIDs` +
  `st.HeaderPaneID` used only in the reap-skip guard, leaving `anyBoundPresent` computed from real
  strand bindings only.
- 20260716-120333-plan-review-r1.md — "[LOW] Card 13 omits lock.go from Context": verified
  `type Engine`/`New` live in `internal/muxengine/lock.go`. Added `internal/muxengine/lock.go` to
  card 13's `Context:`.
- 20260716-120333-plan-review-r1.md — "[LOW] Resume boot path skips eager header validation":
  verified header creation sits in `ensureServerAndSessionLocked` (shared by `Up`/`Resume`). Rewrote
  card 17 to call `e.ValidateHeader()` on the SHARED boot path (so `Resume` validates too), not only
  at the start of `Up`.
- 20260716-120333-plan-review-r1.md — "[NIT] Card 17 references clearAllPaneBindings without its file
  in Context": verified `clearAllPaneBindings` is in `reconcile.go`. Added `internal/muxengine/reconcile.go`
  to card 17's `Context:` and clarified that the `HeaderPaneID` clear lands in lifecycle.go's `if
  booted` block, not inside `clearAllPaneBindings`.

## Pushed Back

(none — all five findings were factually accurate and carried no fix-harm; fixed under the default rule.)
