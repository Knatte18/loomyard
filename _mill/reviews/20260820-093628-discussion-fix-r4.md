# Discussion fix — round 4

Source review: `_mill/reviews/20260820-093540-discussion-review-r4.md` (fablehigh, APPROVE, 0 BLOCKING / 4 NIT).

Auto-mode: every NIT fixed under the `mill-receiving-review` default. No pushback path available.

## Fixed

- r4 "LICENSE choice never made" — added the `license-apache-2-carried-over` decision. Verified Loomyard's root `LICENSE` is Apache-2.0; quarry inherits it, the initial-import commit names the source commit. Flagged in the decision itself as an auto-picked default the owner should confirm before the import lands, since it is the one choice that is expensive to reverse afterwards.
- r4 "Repo scaffolding absent from Scope In" — added a Scope In bullet covering `go.mod`, `.gitignore`, `LICENSE`, and a `README.md` carrying the three statements other decisions mandate (platform set, windows native-strategy caveat, toolchain-cache re-download). Cross-referenced from "Repo state".
- r4 "Stale Scope bullet on lyxdirs inlining" — reworded. Confirmed `daemonstate.go:40,48` are `DotLyxDirName`'s only production uses and that `state-path-ownership-moves-to-the-caller` deletes both segments, so nothing is inlined; the bullet now says the constant is dropped, not inlined.
- r4 "\"59 fmt.Errorf literals\" mischaracterizes the mechanism" — corrected to "59 `\"scoutengine: \"` string literals". Verified `errors.go`'s twelve are seven `errors.New` sentinels and five `fmt.Sprintf` calls in `Error()` methods, zero `fmt.Errorf`; added an explicit warning that a rename batch scoped by a `fmt.Errorf` grep would miss all twelve.

## Pushed Back

None.
