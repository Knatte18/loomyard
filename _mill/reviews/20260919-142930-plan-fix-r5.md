# Plan fix — round 5

Source review: `_mill/reviews/20260919-142147-plan-review-r5.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 5 NIT, one NIT demoted from BLOCKING by the stage ceiling).

Every finding was verified against the real source before acting;
all eight were factually accurate and none triggered a HARM CHECK exception.

Two of them — the undeclared `entry.Verbs` field and the two mis-cased helpers — are round-4 fixes of mine that silently failed to apply, leaving cards asserting against a field and identifiers no card declared.
Each edit in this round was verified to have landed rather than assumed, and the two casing sites the previous round missed were found by re-grepping the batch afterwards.

## Fixed

- r5 holistic, "shedcli `entry` has no `Verbs` field" — confirmed card 29 still declared a two-field `entry` while cards 30 and 31 both read a `Verbs` set. Card 29 now declares all three fields and fills the two recipes' verb sets (`loom` all four, `lifecycle` three), and carries the rationale that had been stranded in card 29's neighbouring paragraph: the four generic verbs are registered once per recipe, so without the set `lyx shed step --recipe lifecycle` reaches the generic `step` body with `StepBusyKind` unset, emitting `kind: ""` outside the closed five and skipping lifecycle's `PreRun`.
- r5 holistic, "Two shedverbs helpers are named twice, with different casing" (NIT, demoted from BLOCKING) — confirmed card 12 declared `StepEnvelope` then called `stepEnvelope`, and card 14 declared `renderStatusLine`/`unavailableLine`/`printStatusLinesOnChange` unexported while the batch's own scope decision and card 23 retarget `internal/loomcli/status_test.go` onto exported names. All four declarations and every call site now use the exported casing. The lowercase names that remain in the plan are deliberate and correct: each one refers to `internal/loomcli`'s own existing unexported identifier being deleted or compared against.
- r5 holistic, "Batch 6's verify rationale rests on a false claim about `constraintchokepoint_test.go`" — verified that file enforces the Glyph Conversion Chokepoint Invariant by scanning production `.go` source and names `CONSTRAINTS.md` only in its doc comment. Dropped the claim from card 39 and the batch's `## Batch Tests`, replacing it with the accurate statement: no test parses `CONSTRAINTS.md`'s structure, so card 35's new section has no mechanical structure guard — its enforcement is the three tests card 35 names plus review discipline. `./cmd/lyx/...` stays in `verify:` for `retiredverbs_test.go` and the help-tree guards, which is the real reason.
- r5 holistic, "Batch 4 rebuilds both live subtrees but its verify never runs `cmd/lyx`" — added `./cmd/lyx/...` to batch 4's `verify:` in both the batch file and the Batch Index. Batch 4 is the batch that replaces both `Command()` bodies' `AddCommand` arguments, and card 24 names `drift_test.go`'s blank-`Short` check as the one machine-checked part of the help-text copy, so running it two batches later was a real gap.
- r5 holistic, "Card 10 says 'five' hooks and then lists six" (NIT) — corrected to six, matching the list and the overview's `poststep-is-a-sixth-hook` Shared Decision.
- r5 holistic, "Cards 22/23 leave loomcli non-compiling until card 24" (NIT) — confirmed `Command()`'s `AddCommand` call still names the deleted constructors until card 24 rewires it. Card 22 now states plainly that card 24 owns the whole `Command()` rewiring including the `--parent` binding, that `internal/loomcli` does not compile between these commits, that both cards record it in their commit messages, and that the batch's `verify:` runs only after card 24 lands — the three cards are one compilation unit even though each carries its own commit.
- r5 holistic, "Card 22's orphaned-call-site list is closed and short" (NIT) — confirmed `step_test.go` also references `stepKindUnseeded`, `stepKindOwnership`, `stepKindBootstrap` and `stepKindProducer`. Added the same "floor, not the ceiling" repo-wide-grep clause cards 4 and 7 already carry, with those four named.
- r5 holistic, "Batch 2's rename greps collide with `loomengine.LoomRunLock`" (NIT) — confirmed the prescribed `.LoomRun.` pattern matches loom's own run-lock accessor at `internal/loomcli/wiring.go` (twice), `internal/loomcli/smoke_test.go` and two `cmd/lyx` guards. `loomengine.LoomRunLock` is now named on the batch's "unchanged, deliberately" list and called out as a hit to discard at the grep instruction itself.

## Pushed Back

None.
