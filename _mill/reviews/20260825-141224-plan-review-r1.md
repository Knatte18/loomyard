MILL_REVIEW_BEGIN
# Review: loom: Discussion-Burler Fabric Git Invariant fix — holistic

```yaml
duration_s: 368.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-25
```

## Findings

### [NIT:consistency] Guard's fix-scope/commit_seam failures not pinned to non-fatal reporting
**Location:** Batch 1, Card 1, Step 1 (the two-halves-of-the-rule paragraph)
**Issue:** "The row's `profile["fix-scope"]` must be the string `overlay` ... Report ... as distinct failures" and the `commit_seam` sentence right after it say "is a failure" without naming `t.Errorf`, unlike the sibling bullets in the same paragraph which explicitly say `t.Errorf`; Step 2 requires the shipped-recipe negative case to report *both* failures in one run, which only holds if both use non-fatal reporting.
**Fix:** Name `t.Errorf` explicitly on both of those two sentences, matching the explicit-`t.Errorf` phrasing already used elsewhere in the same paragraph. Self-healing in practice — Step 2's "run the guard and record it fails exactly that way" check would catch a `t.Fatalf` mistake before the recipe flip lands — but stating it removes the ambiguity up front.

### [NIT:consistency] "CommitPlan ... immediately below" is spatially inaccurate in wiring.go
**Location:** Batch 1, Card 2, `internal/loomcli/wiring.go` requirement
**Issue:** The card describes the `CommitPlan` closure's doc comment as "immediately below" `CommitDiscussion`'s; in the actual file the `PlanSpec` closure (with its own doc comment) sits between them, so `CommitPlan` is two fields down, not immediately below.
**Fix:** Drop "immediately below" or reword to "further down in the same struct literal" — no functional risk, `CommitPlan` is unambiguous to locate by name, but the spatial claim as written doesn't match the file.

## Verdict

APPROVE
Plan is thorough, internally consistent, and every checkable claim against source verified accurate; only two cosmetic wording nits found.
MILL_REVIEW_END
