MILL_REVIEW_BEGIN
# Review: fabric: close the weft-visibility leak (slice 8) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude Opus 4.x-class model (Anthropic); exact build not self-verifiable
reviewed_file: plan/
date: 2026-08-06
```

## Findings

### [BLOCKING] Card 14 omits in-package tests broken by the unexport
**Location:** batch 04 / card 14
**Issue:** The card's grep enumerated production files only; six `package fabricengine` test files use `New(`/`f.Warp`/`f.Weft` and are absent from `Edits:` — `index_integration_test.go:92`, `commit_integration_test.go:753,771,810,822`, `commit_partial_integration_test.go:84,103`, `commit_gating_integration_test.go:35,57`, `pull_integration_test.go:125,128`, `snapshot_integration_test.go:557`.
**Fix:** Add those six files to card 14's `Edits:` (and to `## All Files Touched`) and state they retarget onto `newPaired`/`f.warp`/`f.weft` directly, no shim needed (they are in-package).

### [BLOCKING] Card 16's "zero readers" claim is false in-package
**Location:** batch 04 / card 16
**Issue:** `PartialCommitError`'s fields are read at `commit_partial_integration_test.go:54,57,58,150` and `commit_integration_test.go:870` (both `package fabricengine`), so `WarpSHA`/`WeftSHA`/`WeftCommitted` → private breaks compile; the card lists only `commit.go` and asserts "zero readers outside internal/fabricengine (production and test)".
**Fix:** Add both test files to card 16's `Edits:` and restate the claim as "zero readers *outside the package*; two in-package test files rename with the fields".

### [BLOCKING] Two production `host repo` sites belong to no batch
**Location:** batches 03/05/06 (unassigned)
**Issue:** `internal/builderengine/runlevel.go:399` ("…host repo while holding an absolute report path…") and `internal/burlerengine/profile.go:29` (`FixScopeSource FixScope = "source"  // host repo, with git commits`) carry the policed `host repo` phrase, appear in no card's `Edits:` and in no `## All Files Touched` entry, so batch 07's rule (2) fails on first activation.
**Fix:** Assign `builderengine/runlevel.go` to card 11 (or 20) and `burlerengine/profile.go` to card 19, and add both to `## All Files Touched`.

### [BLOCKING] Test sweep omits the two heaviest non-owner test files
**Location:** batch 06 / card 23 (with cards 5 and 12)
**Issue:** `internal/loomengine/preflight_integration_test.go` (~28 weft hits incl. the test name `TestPreflight_WeftWorktreeRemoved` and comments at :302,322,323,351,440) and `internal/websterengine/audit_test.go` (~32 hits incl. `TestWeftReferencePattern`) are absent from card 23's list; card 5(g) enumerates only `preflight.go`/`report.go`/`status.go` and card 12's reword clause only `audit.go`/`runlevel.go`/`recordbatch.go`.
**Fix:** Either add both files to card 23's list or extend cards 5(g)/12 to name them explicitly, so card 24's grep gate has an owning card.

### [BLOCKING] Card 3 `Context:` misses the file declaring `ScopedPathspec`
**Location:** batch 01 / card 3
**Issue:** Requirements name `ScopedPathspec(AnchorRel, ["_lyx"])`, declared at `internal/fabricengine/fabric.go:125`, but `fabric.go` is in neither `Context:` nor `Edits:` (Context lists `classify.go`, `config.go`, `lyxtest.go`).
**Fix:** Add `internal/fabricengine/fabric.go` to card 3's `Context:`.

### [NIT] Card 5 leaves drift.go's now-false comments
**Location:** batch 02 / card 5(a)
**Issue:** `drift.go:61-66` states "the reason string must keep the substring `junction`" and `Healthy`'s doc comment (`:18-25`) documents `Returns (true, "", nil)`; both are false after typing, but the card only removes preflight.go's twin comment and declares drift.go's comments owner-vocabulary (a vocabulary rule, not a staleness exemption).
**Fix:** Add to card 5(a): update `Healthy`'s doc comment for `HealthReason` and delete the substring-dependency note at `drift.go:61-66`.

### [NIT] `docs/overview.md:133` "weft-synced" left unreworded
**Location:** batch 08 / cards 28-29
**Issue:** Card 28 edits `docs/overview.md` but only `:63-79`; `:133` ("loom resume works across machines *because* its status is weft-synced") is consumer-behaviour prose of the same class as `README.md:62`, which card 29 does reword, and it is outside the machine check.
**Fix:** Name `docs/overview.md:133` in card 28's or card 29's requirements, or record explicitly that overview.md's Weft-overlay section is mechanism prose that keeps the vocabulary.

## Verdict

REQUEST_CHANGES
Five concrete completeness gaps; DAG, sequencing, and decision fidelity are otherwise sound.
MILL_REVIEW_END
