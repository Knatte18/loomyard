MILL_REVIEW_BEGIN
# Review: Replace reed's header pane with a status-line and Selvage — holistic

```yaml
duration_s: 223.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

## Findings

### [NIT:consistency] Stale "header" reference in resizePinHookArgvs doc comment
**Location:** `internal/reedengine/windowsize.go:299`
**Issue:** "The header is always pin index 0 so it fires before any strip pin can go wrong" was not retargeted to "Selvage" when the band was renamed; `render.FixedHeightPins`' own doc comment (rules.go) correctly says "Selvage band pin" for the identical claim, so the two now disagree in wording.
**Fix:** Reword to "The Selvage pin is always pin index 0 …", matching `rules.go`'s updated phrasing.

### [NIT:consistency] Stale file list in reedcli/cli.go's package doc comment
**Location:** `internal/reedcli/cli.go:5`
**Issue:** The leading comment still lists `header.go` among the per-verb files a receiver closes over; `header.go` was renamed to `statusline.go` (card 29) and `watchdog.go` was added (card 33), neither reflected here.
**Fix:** Update the file list to name `statusline.go, watchdog.go` in place of `header.go`.

## Verdict

APPROVE
Implementation matches the plan precisely across all seven batches; only two comment-only naming residues found.
MILL_REVIEW_END
