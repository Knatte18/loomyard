# Plan fix report — round 4

Source review: `_mill/reviews/20260731-195423-plan-review-r4.md` (verdict REQUEST_CHANGES, blocking_count 3, nit_count 2)

## Fixed

- "Context-completeness gaps: functions cited from unlisted files" — added `internal/logger/logger.go` to batch 4's Card 10 Context (cites `configureFromEnv`'s `LYX_LOG_FILE` open flags), `internal/reedengine/env.go` to batch 8's Card 33 Context (calls `CleanClaudeEnv`), and `internal/treadleengine/state.go` to batch 10's Card 37 Context (cites `moveStaleIfExists`).
- "Card 37 mischaracterizes which error is swallowed at run.go:224-227" — verified against source: `saveErr` (line 225) IS returned directly, not swallowed; the actually-discarded value is the original gate-command `err` that line 227 would have returned had `saveState` succeeded. Reworded the bullet to log both `err` (the masked original failure) and `saveErr` (the persistence failure masking it), naming which one is actually lost.
- "Retention liveness test suggests a Test-Tier-Purity-violating alternative" — dropped the "spawn and wait a short-lived helper process" alternative from batch 3's Card 8 (it would put a banned `exec.Command` substring in the untagged `retention_test.go`); mandated the large-implausible-PID approach only, with an explicit note on why the spawn alternative is disallowed.
- "spawn.go line citation off by 3" — corrected `spawn.go:62` to `spawn.go:65` in batch 11's Card 38 (verified: line 62 is `cmd := exec.Command(...)`, line 65 is the actual `return cmd.Start()`).
- "main() line range excludes closing brace" — corrected batch 7 Card 28's `main()` citation from "lines 39-47" to "lines 39-48" (line 48 is the closing brace).

## Pushed Back

(none)
</content>
