MILL_REVIEW_BEGIN
# Review: Add a local-only file category to weft — holistic

```yaml
duration_s: 454.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 5 (model id claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-27
```

## Findings

### [BLOCKING:scope] Cards 1/2 introduce mergeOutcomeAlreadyUpToDate without its source file in Context
**Location:** Batch 1 (merge-drops-weft), Card 1 and Card 2
**Issue:** Both cards' Requirements direct `st.WeftOutcome = mergeOutcomeAlreadyUpToDate`, a constant declared in `internal/fabricengine/mergestate.go`, but neither card's `Context:` (`gitrepo/merge.go`, `mergeguards.go`, `fabric.go`) or `Edits:` (`merge.go`) lists that file, and the identifier does not otherwise appear in `merge.go` today.
**Fix:** Add `internal/fabricengine/mergestate.go` to both cards' `Context:` list.

### [BLOCKING:scope] Card 1 and Card 7 reference helpers defined in files absent from their Context
**Location:** Batch 1, Card 1 (requirement e) and Card 7
**Issue:** Card 1 directs passing `nil` into `unifyConflictPaths`, defined in `internal/fabricengine/mergepaths.go` — not listed in Card 1's Context/Edits. Card 7 directs reusing `fabricengine.CurrentSHAForTest`, an exported var defined in `internal/fabricengine/gitsha_integration_test.go` — also absent from Card 7's Context list (`merge.go`, `merge_target_integration_test.go`, `mergein_integration_test.go`, `mergein_recovery_integration_test.go`, `export_test.go`, `testmain_test.go`, `hub.go`, `gitkit.go`).
**Fix:** Add `mergepaths.go` to Card 1's Context and `gitsha_integration_test.go` to Card 7's Context.

Both findings above are low practical risk — in every instance the exact identifier is spelled out verbatim in the Requirements prose, and (for `unifyConflictPaths`/`gitrepo.New`-style patterns elsewhere in the plan) the call site or an equivalent usage is already visible in a file the card does list — but they meet this stage's explicit Context-completeness rule and are reported per that rule rather than judgment-call severity.

No other findings. Verification against source (mergeguards.go, merge.go, mergestate.go, mergelifecycle.go, destroy.go, cleanup.go, pull.go, gitrepo/push.go, gitrepo/pull.go, spawn.go, weftgit.go, shedengine/shed.go, shedengine/run.go, loomrecipe.go, loomcli/wiring.go, loomcli/landingdeps.go, shedadapters/bouncer.go, fabriccli/weft_verbs.go, loomengine/config.go, manifest/designs/loom.md, doc.go) confirmed the plan's claims, function/constant names, signatures, and even most cited line numbers (e.g. `pull.go`'s seventeen `PartialPullError{WeftPulled: true,...}` sites, `wiring.go:48`/`:248` fill sites, `landingdeps.go:66`, `doc.go:40`, `loom.md:273-277`/`279-283`) are accurate. The Batch Index DAG is acyclic, every named `file:` exists, `depends-on` edges are justified (behavioural for 2→1, 3→2; file-sharing-only for 5→3; both for 7→5,6). Global card numbering (1–32) is sequential with no gaps and matches each batch's declared `cards:` count. `## All Files Touched` is the exact union of every card's `Edits:`/`Creates:` set. Every `Moves:` is `none`, so no rename-mechanic section is required or missing. Every batch adding/editing an `integration`-tagged file chains a `-tags integration` invocation in `verify:`; batches 6 and 7 correctly omit it since neither adds one. Shared Decisions are each faithfully implemented in their owning cards (weft-is-never-merged, weft-guards-drop-with-it, pull-does-not-stall-on-weft, abort-does-not-reset-weft, raddle-gate-removed, commit-hook-lives-in-persist, commit-and-push-every-transition, commit-hard-errors-push-warns, skip-while-mid-merge, mergestate-weft-fields-stay, landing-checkpoint-stays, constraints-gains-one-sentence).

## Verdict

REQUEST_CHANGES
Exceptionally well-grounded plan; fix three narrow Context-completeness gaps in batch 1 (cards 1, 2, 7).
MILL_REVIEW_END
