# Plan fix — round 1

Source review: `_mill/reviews/20260813-145228-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260813-145228-plan-review-r1.md` — "[NIT:consistency] Card 7 miscounts the rebase call sites as six": verified against `internal/scoutcli/cli.go` — the `parseQuery`/`inFileQuery` call sites outside the two definitions are exactly `:161`, `:163`, `:291`, `:293`, `:580`, five in total. Batch 02 card 7 now reads "all five call sites"; the enumerated line numbers were already complete and are unchanged.
- `20260813-145228-plan-review-r1.md` — "[NIT:consistency] Card 17 miscounts sibling `go env GOMOD` allowlist entries as four": verified against `cmd/lyx/tierpurity_test.go`'s `allowedSpawners` — three entries carry the "resolves its scan root via `go env GOMOD`" reason (`gitrepoboundary_test.go`, `boardguard_test.go`, `destructiveguard_test.go`). Batch 03 card 17 now reads "three sibling guards"; the instruction to add the new entry is unchanged.

## Pushed Back

_None. Both findings verified accurate against source and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task._
