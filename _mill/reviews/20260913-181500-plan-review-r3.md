MILL_REVIEW_BEGIN
# Review: Deploy cited spec/design docs to target repos like stencils — holistic

```yaml
duration_s: 592.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-4.5 (Sonnet 5)
reviewed_file: plan/
date: 2026-09-13
```

## Findings

### [BLOCKING:scope] Batch 4 breaks an existing integration test its own verify never runs
**Location:** batch 4 (specs-seeding-wiring), card 14
**Issue:** Card 14 appends `specs.Registry()` rows into `list`'s same `stencils` slice with a new `kind` field. `internal/stencilcli/cli_integration_test.go`'s existing `TestStencilCLI_ListAndValidate` (untouched by this plan, not in "All Files Touched") asserts `len(list) != len(registryNames)` where `registryNames := stencils.Registry().Names()` — this equality will fail the moment card 14 lands, since `list` now also carries the two specs entries. Batch 4's own `-tags integration` verify is scoped to `./cmd/lyx/... ./internal/cliwire/...` only, deliberately excluding `./internal/stencilcli/...` (per the batch's own "Batch Tests" note), so this regression is invisible to every batch verify in the plan and would only surface at the final repository-wide done-gate sweep, after the whole plan has already landed.
**Fix:** Add a card (or extend card 14) that updates `TestStencilCLI_ListAndValidate`'s length assertion to account for both registries (e.g. `len(stencils.Registry().Names())+len(specs.Registry().Names())`), and widen batch 4's tagged verify to include `./internal/stencilcli/...` so the fix is actually exercised within the batch.

## Verdict

REQUEST_CHANGES
Batch 4 ships a verified regression in an existing, untouched integration test that no batch verify catches before the final gate.
MILL_REVIEW_END
