# Plan-fix report — round 3

Source review: `_mill/reviews/20260628-153111-plan-review-r3.md` (verdict APPROVE, 3 NITs).

## Fixed

- r3 "[NIT] Card 2 Context omits store.go" — added `internal/board/store.go` to Card 2
  Context (the type-switch resolvers it relies on are read-only there).
- r3 "[NIT] No explicit atomic-rollback test for merge set_status missing target" — added a
  Card 5 test case asserting a `merge` whose `set_status` targets a non-existent slug/id
  errors and leaves the on-disk store unchanged (`writeOp` discards the in-memory mutation
  on error).
- r3 "[NIT] Card 2 set-status status extraction underspecified" — Card 2 now states the
  `set-status` handler reads `status` from the decoded payload map and passes it to
  `Board.SetStatus`, with the required-vs-`null` presence semantics deferred to Card 3.

## Pushed Back

- None.
