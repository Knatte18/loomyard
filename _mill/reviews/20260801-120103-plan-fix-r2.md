# Plan Fix — round 2

Source review: `_mill/reviews/20260801-120020-plan-review-r2.md`

## Fixed

- **[BLOCKING] Card 4's Sleep-guard wiring instructions leave the check unreachable** — verified accurate: the existing banned-token check block in `TestTierPurity_UntaggedTestsSpawnNothing`'s walk callback returns `nil` on all three of its branches, so any code placed after it as originally worded would never execute. Fixed Card 4 (`01-tierpurity-guard-generalization.md`) to explicitly instruct restructuring the three early `return nil`s into fall-through conditionals (skip the `failures` append rather than returning), with a single trailing `return nil` after both the banned-token check and the new Sleep check have each had a chance to append to `failures`.
- **[NIT] Batch 2's dependency rationale cites the wrong card number** — verified accurate: Card 6 only retags `ensureserver_integration_test.go`; the card touching `allowedSpawners` is Card 11. Fixed the Batch Scope prose in `02-scoutengine-scout-tag.md` to cite Card 11.
- **[NIT] Card 11's updated allowlist reason understates post-split usage** — verified accurate against the source file: all three subtests remaining in `supervised_test.go` after Card 10's split (`TestEnsureSupervised_RetryExhaustionReturnsErrServerSpawnTimeout`, `TestEnsureSupervised_UncontendedLockWithUndialableHealthyStateReturnsErrServerSpawnTimeout`, `TestEnsureSupervised_WedgedEscalationReuseReleasesLock`) call `spawnAndHoldSubprocess`, not only the retry-exhaustion one. Broadened Card 11's new `allowedSpawners` reason string in `02-scoutengine-scout-tag.md` to name all three subtests.

## Pushed Back

None.
