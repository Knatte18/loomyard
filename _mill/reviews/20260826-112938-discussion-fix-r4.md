# Discussion fix — round 4

Source review: `_mill/reviews/20260826-112813-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, 0 demoted).

## Fixed

- r4 `[NIT:scope]` "Two doc surfaces named implicitly, not explicitly" — `docs-in-the-same-commit` is now an explicit five-item list, adding `internal/fabriccli/clone.go`'s file-header comment (which enumerates the same wiring sequence and would go stale) and `internal/hubforge/seed.go`'s `SeedConfig` doc comment (which must justify the new `--allow-empty`).
- r4 `[NIT:design]` "Error-path Decision carries no named verification" — `error-path-returns-zero-result` gains a **Verification** line stating explicitly that no new test is added, that the shared named-result + `defer` idiom already covers it, and that a plan writer must not invent a failure-injection seam.
- r4 `[NIT:consistency]` "Adopt-path claim stated absolutely" — `commit-only-applied-modules` reworded: `Applied: false` holds per module only when the committed config already matches its template; a drifted config reports `Applied: true` via `configsync.ReconcileAll`'s `hasChanges` branch and is committed, which is equally correct.

## Pushed Back

(none)
