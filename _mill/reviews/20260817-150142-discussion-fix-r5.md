# Discussion fix — round 5

Source review: `_mill/reviews/20260817-150047-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, zero demoted findings — convergence gate satisfied).

## Fixed

- r5 NIT `[consistency]` "Doc-surface table counted as five and seven" — the file said "five distinct claims" in the lead-in, "That is seven rows" in the closing sentence, and "five stale claims" in the Q&A, against a seven-row table. Dropped the numeral from all three sites and made them refer to the table instead, so no count can drift again as rows are added.
- r5 NIT `[consistency]` "Same invariant contradiction left in an edited file" — `internal/configengine/config_test.go:383-385`'s comment claims "configengine is the single declarer of the `_lyx` token" while its own assertion three lines down reads `lyxdirs.LyxDirName`, contradicting the Lyxdirs Single-Declarer Invariant. Verified in source. Given an explicit in-pass disposition (reword the comment to name `internal/lyxdirs`; test body and name unchanged) rather than being left to the blanket out-of-file clause, and the `config_test.go` entry in the test-file list now names the comment edit. Also narrowed the out-of-scope clause to "outside `configengine.md` and the six Go files this task edits", which is what it actually meant.

## Pushed Back

None. Both findings verified accurate against source and neither met a Decision Tree harm category.
