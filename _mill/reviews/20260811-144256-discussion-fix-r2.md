# Discussion fixer report — round 2

Source review: `_mill/reviews/20260811-144201-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).
Target: `_mill/discussion.md`.

## Fixed

- `20260811-144201-discussion-review-r2.md` — "[NIT:decision] roadmap.md:51 atomicity claim has no disposition".
  Verified: `manifest/roadmap.md:51` states "a producer is always atomic (one mechanical action or one LLM session, never an internal multi-step process of its own)" and defines Input as "artifact(s) consumed", six lines above the carve-out at `:57–61` that contradicts both.
  Scoped `:51` in as an edit site with an explicit disposition — qualify the atomicity clause to bind simple producers and admit the thin-Input/thin-Output cases, pointing forward to `:57–61` rather than restating it.
  This is the same wording the task already scopes at `shed.md:8` and `loom.md:44`.

- `20260811-144201-discussion-review-r2.md` — "[NIT:consistency] Q&A still says pointers hang off the Type column".
  Verified: the second Q&A entry predated round 1's `Kind`-column decision and contradicted both `shed-md-is-authoritative-loom-md-points` and the round-1 auto-pick entry.
  Reworded to name the `Kind` column and the single above-table anchor.

- `20260811-144201-discussion-review-r2.md` — "[NIT:scope] Acceptance greps scoped to manifest/ and docs/ only".
  Verified: `README.md:97` inlines its own producer chain and sits outside the grep roots; `CONSTRAINTS.md:263` independently records that `README.md`'s outgoing links are checked by nobody.
  Added `README.md` to the grep roots and recorded that it was checked clean at branch point `c3af3c9c` (no `Raddle` phase, no `producer-slot`, no retired phrasing), so the completeness claim is verifiable and a later regression is caught.

## Pushed Back

None.
