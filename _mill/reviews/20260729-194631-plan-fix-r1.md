# Plan fix report — round 1

Source review: `_mill/reviews/20260729-194550-plan-review-r1.md` (verdict APPROVE, 1 NIT).

## Fixed

- `20260729-194550-plan-review-r1.md` — **[NIT] Two doc comments left stale by the split**: verified accurate — Card 3 named only prompt.go's file-level banner, leaving `composePrompt`'s own function doc comment (prompt.go:23-31, "filling `reviewPromptTemplate` ... via `stencil.FillOptional`" / "the template's nine required top-level marker values") stale once Card 2 deletes that var; Card 5 never named `template_test.go`'s top-of-file package comment ("the embedded prompt template ... all nine required markers"), stale once four assets each fill a marker subset. Extended Card 3's Requirements to also rewrite `composePrompt`'s function doc comment (four-asset render + orchestrator-plus-three-pairs return), and Card 5's Requirements to also rewrite `template_test.go`'s top-of-file comment (per-asset pins, per-asset fill tests, the guard test). Plan-file edits only; no source touched.

## Pushed Back

_None._
