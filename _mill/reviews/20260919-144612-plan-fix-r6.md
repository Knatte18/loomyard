# Plan fix — round 6

Source review: `_mill/reviews/20260919-143804-plan-review-r6.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 4 NIT, two of the NITs demoted from BLOCKING by the stage ceiling).

Every finding was verified against the real source before acting;
all six were factually accurate and none triggered a HARM CHECK exception.

This is the configured round cap (`roles.plan-review.holistic.rounds: 6`).
Every finding raised in this round is fixed below;
the plan is approved under the operator's standing waiver of the max-rounds halt, recorded in the approval commit.

## Fixed

- r6 holistic, "No help-tree test ever sees the shed subtree" — verified `cmd/lyx/helptree_test.go`'s `TestHelpTree_RootNamesAllModules` compares against a hardcoded `requiredModules` slice that does not contain `shed`, and `TestHelpTree_VerbModuleSubcommands` against a hardcoded `wantSubs` table, so both would pass vacuously and `_mill/discussion.md`'s Scope requirement of "a seam-enforcement scan **and a help-tree test** enforcing it" would never land. Card 32 now carries `cmd/lyx/helptree_test.go` in its `Edits:` with instructions to append `"shed"` to `requiredModules` and add a `shed` case whose `wantSubs` lists the four verbs, and the file is in All Files Touched. Card 32's and the batch's claims about the guards were corrected: only `drift_test.go`, `longlist_test.go` and `registration_test.go` derive from the live tree, `helptree_test.go` is table-driven, and `jsonhelp_test.go` never sees `shed` at all.
- r6 holistic, "Card 21 names shedverbs.KindBusy with step.go absent from Context" — added `internal/shedverbs/step.go` to card 21's `Context:`, matching card 22, which already lists it.
- r6 holistic, "Card 31 contradicts itself on cli_test.go's cwd" (NIT, demoted from BLOCKING) — verified `RunCLI` delegates to `RunCLIIn("", …)` and thus reads the process cwd, so the required non-git directory is reachable only through the cwd-carrying seam. Card 31 now has `cli_test.go` call `RunCLIIn(t.TempDir(), …)` and states the real tier-1 justification: every one of those cases short-circuits before `Arm` and therefore before `lyxcwd.Resolve` — the bare listing at the group guard, the unknown recipe at `lookup`, the unsupported verb at the `Verbs` check — so none spawns git.
- r6 holistic, "ly-drive against a non-steppable recipe has no disposition" (NIT, demoted from BLOCKING) — real contract gap: `lifecycle`'s verb set excludes `step`, and the `shed` pre-run's refusal is a bare `output.Err` carrying no `kind`, which the skill's five-kind contract has no arm for. Card 36 now requires recording that `loom` is the only steppable recipe shipped today and that the recipe argument exists for the next consumer, plus one new `## Error envelopes` arm: an error envelope with no `kind` field is handed straight back with no retry. The five-kind vocabulary is explicitly not widened — it is closed by a test and by the new invariant, and this refusal is raised above the verb body that owns those kinds.
- r6 holistic, "Deletes: none on cards that determinately empty files" (NIT) — confirmed `internal/loomcli/status.go` declares exactly the four identifiers listed for deletion, `pause.go` exactly one, and `internal/lifecyclecli/run.go` and `status.go` exactly one each. Cards 23 and 26 now carry those four paths in `Deletes:` unconditionally, with the conditional "if it ends up empty" prose replaced by a statement of why the outcome is certain, and the paths removed from those cards' `Edits:` and from All Files Touched so no file is both edited and deleted. Card 26 additionally names `internal/lifecyclecli/arm.go`'s `PostRun` field doc as the new home for the mutations-array rationale, which would otherwise have been lost with the file it ordered deleted.
- r6 holistic, "Three inventory counts misstate the source" (NIT) — corrected all three: `wire_test.go` has two `Env.LoomRun` reads among four seam assertions, not three (card 7 and batch 2's test prose); `wiring_test.go` names `loomrecipe.ShedPaths` only in doc comments and `wire_test.go` not at all (card 4 and batch 1's test prose); and the sandbox guard is `cmd/lyx/sandbox_coverage_test.go`, not a file under `tools/sandbox` (batch 6's test prose). None changes the work, since both rename cards mandate a repo-wide grep as the floor — the corrections keep that floor from being read as a verified ceiling.

## Pushed Back

None.
