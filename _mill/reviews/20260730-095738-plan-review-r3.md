MILL_REVIEW_BEGIN
# Review: fabric: Fabric.Commit classify+dispatch + unified diff/status — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Sonnet 5 (model ID per system prompt: claude-sonnet-5)
reviewed_file: plan/
date: 2026-07-30
```

## Findings

### [BLOCKING] Card 4 Context missing gitrepo.go for gitrepo.New reference
**Location:** 02-async-push-plumbing.md, Card 4
**Issue:** Requirements direct `PushWarpAt` to call `gitrepo.New(warpPath).PushCoalesced()`; `New` is defined in `internal/gitrepo/gitrepo.go`, which is not in Card 4's `Context:` (only `internal/gitrepo/push.go` is listed).
**Fix:** Add `internal/gitrepo/gitrepo.go` to Card 4's `Context:` list. (Low practical risk: `weftgit.go`'s `PushWeftAt`, already in Context, shows the identical `gitrepo.New(x).PushCoalesced()` idiom verbatim, but the letter of the Context-completeness rule is still violated.)

### [NIT] Card 13's .gitrepo-push.lock exclusion assertion is vacuous
**Location:** 04-unified-diff-status.md, Card 13
**Issue:** The described `Fabric.Status` test runs `CommitWeft`/`SyncWeft` (which calls `Repo.Push`, not `PushCoalesced`) before asserting `.gitrepo-push.lock` does not appear in the result; that file is only ever created by `PushCoalesced`, so it never exists in this test's flow and the "excluded" assertion passes trivially without exercising the exclude-file mechanism for that artifact (unlike `.weft/`, which `ensureWeftLockDir` genuinely creates).
**Fix:** Either drop the `.gitrepo-push.lock` half of the assertion or have the test create/touch that file directly (or route a push through `PushCoalesced`) so its absence from `WorktreeChangedFiles` genuinely regression-guards the exclude entry.

## Verdict

REQUEST_CHANGES
One narrow Context-completeness gap (Card 4) and one vacuous test assertion (Card 13); otherwise sound, well-grounded, and internally consistent.
MILL_REVIEW_END
