# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260824-140211-plan-review-r1.md` (holistic, round 1, REQUEST_CHANGES, 3 BLOCKING / 2 NIT).

## Fixed

- r1 holistic, "[BLOCKING:design] Card 14 inserts DiscussionWrite at the wrong sorted position" — verified: `DiscussionValidate` and `DiscussionWrite` share the `Discussion` prefix and diverge at `V` (0x56) versus `W` (0x57), so `DiscussionValidate` sorts first. Card 14 now names the slot as immediately after `"DiscussionValidate"` and before `"Finalize"`, with the byte-wise reason stated inline.
- r1 holistic, "[BLOCKING:scope] buildSequenceFixture's new return arity breaks four untouched call sites" — verified: `grep -n "buildSequenceFixture(t)"` finds seven call sites (one in `sequence_test.go`, six in `resume_test.go`), all destructuring three values. Fixed by removing the arity change entirely rather than migrating the call sites: card 16 now pins `buildSequenceFixture`'s signature as unchanged and folds the commit counter onto `fakeDiscussionShuttle`, which tests reach via `env.Shuttle.(*fakeDiscussionShuttle)`. Cards 19 and 20 updated to use that type assertion and to state their destructuring stays as-is.
- r1 holistic, "[BLOCKING:scope] New wiring_test.go case evaluates DiscussionSpec() without seeding the stencils dir" — verified: `hubgeom.WebsterGeometry` sets `StencilsDir: fabricengine.StencilsDir(l.HubPath)`, `hubLocation` seeds only `_lyx/config/{loom,landing}.yaml` under the anchor, and `stencilstore.Read` has no embedded fallback. Card 22 now specifies a `seedDiscussionStencil` helper writing `stencils.LoomTemplateDiscussion` under `fabricengine.StencilsDir(hubPath)/loom/`, called from `hubLocation`, and its `Context:` gained `internal/stencilstore/reconcile.go`, `internal/fabricengine/junctionnames.go`, `internal/hubgeom/webstergeom.go`, `contracts/stencils/stencils.go`, `internal/loomengine/prompt.go`, and `internal/loomengine/prompt_test.go`.
- r1 holistic, "[NIT:consistency] scribe INDEX.md's \"not yet built\" line goes stale" — verified: `plugins/scribe/skills/INDEX.md:19` names this task by name and calls the wiring "not yet built". Added card 27 to batch 4 editing that line, and added the file to the overview's All Files Touched.
- r1 holistic, "[NIT:consistency] Card 24 miscounts plan-card-format.md's blockquote sentences" — verified: the blockquote's first sentence covers both supersessions together, leaving two other sentences, not three. Card 24 reworded to two, and reworded to name them without backticked paths so `context-completeness` stays satisfied without widening the card's `Context:`.

## Pushed Back

_None. All five findings verified accurate against the code and were fixed._
