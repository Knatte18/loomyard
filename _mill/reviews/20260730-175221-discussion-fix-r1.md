# Discussion fix — round 1

Source review: `_mill/reviews/20260730-175103-discussion-review-r1.md` (verdict APPROVE, 3 NOTEs, 0 blocking).

## Fixed

- From `20260730-175103-discussion-review-r1.md` — "[NOTE] Stale JSON-referencing comments on retained code": added a Scope (In) instruction and a Technical-context "Stale-comment cleanup (same commit)" bullet enumerating the retained comments (`fetch.go` header + `fetchPage` docstring, `fetchOldRedditHTML` doc, `redditHostPattern`/`redditHostReplace` comments, `errorResult`) to rewrite to the old.reddit-only strategy.
- From `20260730-175103-discussion-review-r1.md` — "[NOTE] maxTopComments reuse-vs-own-bound is ambiguous": resolved to a single choice — HN **reuses the existing `maxTopComments` const** (kept, not deleted); updated both the hackernews-adapter decision and the `reddit.go` technical-context bullet to match.
- From `20260730-175103-discussion-review-r1.md` — "[NOTE] Cross-skill 'load' convention not verifiable in this worktree": specified the load-by-name mechanism (Skill tool, plugin-namespaced `prowler:distill-subagent`), noted a codeless guidance skill is loaded by invoking its `SKILL.md`, and clarified the mill-plugin convention is cited as precedent (it lives in the separately-installed `mill` plugin, not this worktree).

## Pushed Back

- None.
