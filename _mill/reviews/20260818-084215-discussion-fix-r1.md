# Discussion fix — round 1

Source review: `_mill/reviews/20260818-083728-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 4 NIT — 2 demoted from BLOCKING by the stage's blocking-class ceiling).
All four fixed; none pushed back.
Every finding was independently verified against current source before being applied.

## Fixed

- `20260818-083728-discussion-review-r1.md` — "[NIT:consistency] burlerengine smoke files are `smoke`-tagged, not `integration`" (demoted from BLOCKING).
  Verified: `smoke_round_test.go:1` and `smoke_cluster_test.go:1` both carry `//go:build smoke`, and they are the only tagged files in `internal/burlerengine`.
  The discussion had called them "integration-tier" and put `-tags integration ./internal/burlerengine/...` in Verify, which compiles neither.
  Corrected the tier wording in both Technical context and Testing, dropped `./internal/burlerengine/...` from the integration line, and added a mandatory `go vet -tags smoke ./internal/burlerengine/...` compile step (running the suite is not required — it spawns real agents — but compiling it is).
- `20260818-083728-discussion-review-r1.md` — "[NIT:consistency] `AnchorRoot` diverges from the cited `reedengine` precedent unexplained" (demoted from BLOCKING).
  Verified: `reedengine/geometry.go:22` spells the field `AnchorPath` and `:25` `WorktreeRoot`.
  Adopted `AnchorPath` for both new structs (25 occurrences renamed) rather than merely documenting the divergence, and added a dedicated Decision recording why: it matches the precedent struct this discussion calls "the pattern to copy exactly", it matches the `lyxcwd.Location.AnchorPath()` accessor both tellers fill it from, and `AnchorRoot` would name a non-root `root` in violation of the Cwd Resolution Invariant's own `root`/`cwd` rule, which this discussion quotes.
  The Decision also records that T8's pinned-values table is unaffected — its `anchorRoot` label names the concept, and `Geometry.AnchorPath` is the field that row resolves to, exactly as reed's already-landed `AnchorPath` is for reed's rows.
- `20260818-083728-discussion-review-r1.md` — "[NIT:scope] `perchengine/run_test.go` call-site count understated ~3x".
  Verified by count: 33 `newTestLayout` references and 39 `New(fb, …)` construction sites in that file, against the discussion's "~10".
  Replaced the estimate with the counted figures and flagged the file as the task's largest mechanical edit.
- `20260818-083728-discussion-review-r1.md` — "[NIT:scope] Stale `*lyxcwd.Location` prose outside `doc.go` not listed".
  Verified: `perchengine/engine.go:11` and `:81`, and `hubgeom/hubgeom.go:1-2`, all become false.
  Widened the Scope→In docs bullet from "`doc.go` updates" to cover file-header and function comments in the edited production files, and enumerated the known sites — adding `burlerengine/engine.go:27-28`/`:36-40`, `burlerengine/prompt.go:10`, and `perchengine/doc.go:245-246` found while checking.

## Pushed Back

None.
