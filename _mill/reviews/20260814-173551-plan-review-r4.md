MILL_REVIEW_BEGIN
# Review: Move <hub>/.lyx into <hub>/_board — holistic

```yaml
duration_s: 394.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 4.5 (Anthropic), self-assessed
reviewed_file: plan/
date: 2026-08-14
```

## Findings

### [BLOCKING:scope] Card 9's Context omits junctionnames.go, needed for fabricengine.HubScratchDir
**Location:** batch 1 / card 9 (`internal/fabricengine/hubscratch_integration_test.go`).
**Issue:** `TestCloneHub_BoardStageAllCommitNeverStagesHubScratch`'s Requirements text asserts against `fabricengine.HubScratchDir(res.HubPath)`, but `HubScratchDir` is declared in `internal/fabricengine/junctionnames.go`, which is not listed in card 9's `Context:` (only `clone.go`, `weftgit.go`, `gitexclude.go`, `junction_pattern_integration_test.go`, `clone_adopt_test.go`, `testmain_test.go`, `bolt.go`, `lyxdirs/dirs.go`). Card 8, which uses the same constructor, correctly lists `junctionnames.go`; card 9 does not.
**Fix:** Add `internal/fabricengine/junctionnames.go` to card 9's `Context:` list.

### [BLOCKING:scope] Card 19's import-pruning instruction omits `os`, leaving an unused import
**Location:** batch 2 / card 19 (`internal/fabricengine/hubreservedroutes_integration_test.go`, moved from `boardjunction_integration_test.go`).
**Issue:** The card instructs deleting five of the six tests in the moved file, keeping only `TestBoardJunction_ExcludedFromPathspecRoutes` (renamed), and says to "prune the import block to what the single surviving case uses, dropping `fslink`, `gitexec` and `lyxcwd` if nothing else references them." Verified on disk: the surviving test uses only `filepath`, `slices`, `testing`, and `fabricengine` — it makes no `os.*` call. The five deleted tests are the file's only `os` users (`os.RemoveAll`, `os.Lstat`, `os.Remove`). The card's enumerated drop-list names `fslink`, `gitexec`, `lyxcwd` but omits `os`, so following it literally leaves an unused `"os"` import, which fails to compile and breaks batch 2's `-tags integration ./internal/fabricengine/...` verify step.
**Fix:** Add `os` to the list of imports to drop (verify each of `os`, `fslink`, `gitexec`, `lyxcwd` against the surviving test's actual usage rather than naming a fixed subset).

## Verdict

REQUEST_CHANGES
Two concrete, verified defects (a missing Context entry and an unused-import compile break); rest of the plan checks out against source.
MILL_REVIEW_END
