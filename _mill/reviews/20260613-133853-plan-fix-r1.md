# Plan Fix — Round 1

Source review: `_mill/reviews/20260613-133746-plan-review-r1.md` (verdict APPROVE, 3 NITs)

## Fixed

- **[NIT] removeLinks Windows junction mode bit** — Card 4 (batch 02): added the caveat that some Go versions report junctions as `ModeSymlink|ModeIrregular` and that the `&os.ModeSymlink` bitmask test (never equality) is required.
- **[NIT] addRemote omits initial-branch push present in discussion** — Card 6 (batch 03): added a note that the base-branch push is deliberately dropped because `Add`'s own `git push -u origin <branch>` populates the empty bare repo.
- **[NIT] Card 13 loads config before validating subcommand presence** — Card 13 (batch 04): clarified that the config-before-subcommand-check ordering is intentional, matches `internal/board/cli.go`, and is relied on by Card 16's routing test.

## Pushed Back

(none)
