# Plan fix — round 3

Source review: `_mill/reviews/20260808-091431-plan-review-r3.md` (verdict APPROVE, 1 NIT)

## Fixed

- `20260808-091431-plan-review-r3.md` — "[NIT] audit_test.go idiom citation omits AnchorRel".
  Verified accurate against source: `fakeLayout` at `internal/websterengine/audit_test.go:24-26` returns `&lyxcwd.Location{HubPath: ..., WorktreeName: ...}` with no `AnchorRel`, leaving it at the zero value, while card 7's prescribed fixture sets `AnchorRel: "."`.
  Took the reviewer's second option rather than the first: kept the cross-reference and named the divergence, instead of dropping the citation.
  Dropping it would lose a useful in-repo shape reference for an idiom with only a handful of precedents;
  naming the divergence removes the ambiguity the NIT identified while keeping that value.
  The reworded text also records why the explicit `"."` is the right choice here — it matches what `resolveLocation` synthesizes in card 5, so fixtures and the production out-of-hub path describe the same value — and confirms no expected value depends on it, since `AnchorRel` does not affect `WorktreePath()`.

## Pushed Back

_None._
