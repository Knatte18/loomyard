# Discussion fix report — round 7 (extension round)

Source review: `_mill/reviews/20260829-105514-discussion-review-r7.md` (APPROVE, 2 NITs, 0 BLOCKING, none demoted).

## Fixed

- `20260829-105514-discussion-review-r7.md` — "Cross-reference points the wrong direction": both directional references to the all-or-nothing output decision now say "above" rather than "below" — the decision precedes the traversal-order decision in the file. Fixed in the no-sort rationale and in the sort Q&A entry.
- `20260829-105514-discussion-review-r7.md` — "No disposition for transient/5xx failures across a 26-call walk": added a "No retries, deliberately" bullet to the stdout-discipline decision, stating that every non-2xx aborts with no backoff loop, no per-call retry, and no partial-progress resume, so a transient 5xx or secondary-rate-limit 403 mid-walk fails the whole `torvalds/linux` invocation and the caller re-invokes. The rationale is recorded — retry timing is precisely what the offline harness cannot assert deterministically, and the caller already checks the exit code — along with an explicit instruction that a plan writer must not add a backoff loop. Mirrored as a Q&A entry.

## Pushed Back

(none)
