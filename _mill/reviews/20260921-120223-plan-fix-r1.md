# Fixer report: plan-fix round 1 — lyx-bin-pane-path

Source review: `20260921-120022-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- `20260921-120022-plan-review-r1.md` — "[NIT:decision] Two discussion decisions cited by name never entered the plan's decision ledger" (demoted from BLOCKING).
  Verified accurate: card 7 cited `prelude-is-session-scoped-in-both-dialects` and batch 3's scope cited `docs-describe-the-landed-mechanism`, and neither name existed anywhere in the plan.
  Added both to `00-overview.md`'s `## Shared Decisions`, matching the four decisions already promoted there from the discussion.
  The session-scope decision's rationale now also carries the reason `ExportEnv` is a new method rather than a reuse of `WithEnv`, which is the point card 1 needed it for.
- `20260921-120022-plan-review-r1.md` — "[NIT:consistency] Card 3 cites resolve.go's exact declaration without it in Context".
  Verified accurate: card 3's Requirements models `executablePath` on `tools/sandbox/resolve.go`'s `var devBinPath = devbin.BinPath` seam, confirmed present at that file's line 30, while the file appeared only in card 8's Context.
  Added `tools/sandbox/resolve.go` to card 3's `Context:` rather than applying the escape marker — the precedent is worth reading in full at the point the seam is written, not just cited.
- `20260921-120022-plan-review-r1.md` — "[NIT:consistency] Shell Mechanics Seam's method enumeration goes stale".
  Verified accurate, and worse than the finding states: the parenthetical already omits `WithEnv` and `Touch`, so it is stale before this task touches it.
  Moved `CONSTRAINTS.md` from card 1's `Context:` into its `Edits:` and added a requirement to rewrite the parenthetical as illustrative rather than exhaustive, so it does not re-stale on the next method added.
  Placed the edit in card 1 rather than card 3 so the enumeration moves in the same commit as the interface it describes.
  Card 1's Batch Scope, its Batch Tests section, and a note under the card record that `CONSTRAINTS.md` is touched by two cards in disjoint sections (`## Shell Mechanics Seam` here, `## Pane Binary Resolution` in card 3), which cannot race because batch 2 depends on batch 1.
  Card 1's `Context:` gained `internal/shell/shell_test.go` and `internal/shuttleengine/claudeengine/command.go` — the existing test shape the card must not break, and the `forkSubagentEnvKey` precedent for riding env through the pane command.

## Pushed Back

_None. All three findings verified accurate, and no fix broke existing functionality, contradicted a documented decision, or destabilized work outside this task's scope._
