# Plan-fix report — round 1

Source review: `_mill/reviews/20260725-075904-plan-review-r1.md` (verdict REQUEST_CHANGES,
1 BLOCKING + 1 NIT).

## Fixed

- [BLOCKING] "Guard test trips cmd/lyx tier-purity guard" — Card 13 now Edits
  `cmd/lyx/tierpurity_test.go` to add an `allowedSpawners` self-exclusion entry for
  `tools/sandbox/pathresolve_guard_test.go` (the guard file carries `exec.Command`/
  `exec.CommandContext` scan literals the module-wide Test Tier Purity guard would otherwise
  reject). Batch 3 `verify:` widened to `go test ./tools/sandbox/ ./cmd/lyx/` (both the batch
  file and the overview Batch Index) so the batch actually exercises the cmd/lyx guard; added
  `cmd/lyx/tierpurity_test.go` to the overview's All Files Touched; updated Batch Tests prose.
- [NIT] "devbin API referenced without Context entry" — added
  `tools/internal/devbin/devbin.go` to the `Context:` of Batch 1 Cards 3 and 4. Confirmed
  plan-created files are valid Context entries: the round-1 validator did not flag Card 13's
  existing `tools/sandbox/resolve.go` Context entry (also a plan Creates target), so the
  validator is plan-aware.

## Pushed Back

_None._
