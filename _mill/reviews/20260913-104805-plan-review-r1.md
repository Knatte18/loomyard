MILL_REVIEW_BEGIN
# Review: modelspec: bare-effort shorthand and version-to-v rename — holistic

```yaml
duration_s: 369.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (Sonnet, Anthropic, tool-use/agentic review mode)
reviewed_file: plan/
date: 2026-09-13
```

## Findings

### [NIT:consistency] Grammar-line wording match (card 1 ↔ card 5) is unpinned
**Location:** batch bracket-grammar, card 1 and card 5
**Issue:** Both cards require the package-doc grammar line (`internal/modelspec/modelspec.go`) and the contract-doc grammar line (`contracts/specs/llm-model-spec.md:8`) to be "worded identically," but neither card gives the literal string — only a prose description ("comma-separated list of items, an item is either `key=value` or a bare effort token"). The plan's own precedent (Shared Decision "error strings are pinned by the plan") pins literal text precisely to avoid this class of cross-card drift, but doesn't extend that treatment here.
**Fix:** Low practical risk since one implementer session executes all six cards in order (verified: no test parses doc prose, so drift can't break `verify:`), but consider pinning the exact fenced-line text once and having both cards copy it verbatim, consistent with the error-string precedent.

### [NIT:consistency] Batch Scope undercounts transiently-broken tests between card 2 and card 3
**Location:** batch bracket-grammar, `## Batch Scope`
**Issue:** The stated rationale for non-green intermediate commits names only the deleted `no '=' separator` case. Verified against `internal/modelspec/parse_test.go:31` (`"alias multiple params"`, input `sonnet[effort=high,version=4.5]`) and `:46` (`"dotted version value"`, input `sonnet[version=4.5]`): both also transiently fail after card 2 lands (their literal `version=` bracket key becomes an unknown-key rejection) until card 3's requirement to rewrite those two inputs to `v=` lands. Card 3's requirements do correctly fix both, so no functional gap — only the narrative undercounts by two.
**Fix:** Update the Batch Scope sentence to name all three transiently-broken cases, or generalize the wording (e.g. "every case using the old `version=` bracket spelling").

## Verdict

APPROVE
Plan is complete, well-grounded against source, and internally consistent; only two low-severity documentation-accuracy nits found.
MILL_REVIEW_END
