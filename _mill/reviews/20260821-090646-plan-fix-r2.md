# Plan fix — round 2

Source review: `_mill/reviews/20260821-090609-plan-review-r2.md`.

## Fixed

- r2 `[NIT:scope]` "Card 13's shape-discovery pointer omits the file that actually defines it" — added `internal/stencilstore/stencilstore.go` to batch 4 card 13's and batch 5 card 17's `Context:`, and reworded both pointers to name `stencilstore.Path`/`stencilstore.RelPath` as where the on-disk stencil layout is actually declared, rather than sending the implementer to `reconcile.go` alone.

## Pushed Back

_None._
