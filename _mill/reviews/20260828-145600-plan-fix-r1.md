# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260828-145217-plan-review-r1.md`

## Fixed

- `20260828-145217-plan-review-r1.md` — "[BLOCKING:scope] Card 2's fixture sweep omits a known integration-tagged file from Context".
  Added `internal/reedengine/watchdog_integration_test.go`, `internal/reedengine/attachgeometry_integration_test.go`, `internal/reedengine/header_test.go`, and `internal/reedengine/server_test.go` to card 2's `Context:`, and replaced the open-ended sweep instruction with a verified five-file inventory of every `Geometry{` literal in the package, stating for each whether it reaches a lock helper.
  Both named integration files are recorded as containing no `Geometry` literal at all — they build through `newIntegrationEngine`, which already creates its worktree directory — so the card now says outright that neither needs a change, rather than leaving the implementer to discover it from a file it was not allowed to read.
- `20260828-145217-plan-review-r1.md` — "[NIT:consistency] Batch 1's sandbox-doc edit documents Batch 2's not-yet-implemented dormancy behaviour".
  Moved the "header pane stopped logging reconcile failures rather than spinning" line out of batch 1 card 3 and into batch 2 card 4, which now carries `tools/sandbox/SANDBOX-REED-SUITE.md` in its own `Edits:`.
  Card 3 is now explicitly forbidden from adding that wording, and card 4 is explicitly forbidden from restating card 3's no-stray-directory assertions, so the two edits to the same file cannot collide or drift.
  Batch 2's `## Batch Tests` was corrected to attribute that sandbox line to card 4 rather than to card 3.
- `20260828-145217-plan-review-r1.md` — "[NIT:consistency] \"existing 'verified live' list\" in doc.go is not an actual named list".
  Both card 3 and card 4 now name the list by its actual introducing line, "Load-bearing behavioral assumptions, each with the rationale that makes it", verified present at `internal/reedengine/doc.go:153`.

## Pushed Back

_None. All three findings verified accurate against the source, and none of the fixes breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task._
