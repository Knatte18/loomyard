# Discussion fix — round 1

Source review: `_mill/reviews/20260706-163213-discussion-review-r1.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- `[NOTE] Interactive runs now terminate Wait at first question` — added an
  explicit "Intended behavior change (not a regression)" bullet to Decision
  `ask-signal-outcome` stating every interactive run now returns `OutcomeAsking`
  at the first `AskUserQuestion` within a single `Wait`, that the old
  ask-answered-live-and-continue flow never worked (so nothing is removed), and
  that answer→continue is out-of-scope orchestration-layer work.
- `[NOTE] Test fake must synthesize both Event kinds` — added
  `internal/shuttleengine/fakes_test.go` to the Technical-context file-change
  list (move `fakeEngine.ParseEvents` to `[]Event`, synthesize both `Kind`s) and
  a matching `fakes_test.go` bullet under Testing; noted the rename ripples into
  `wait_test.go`/`events_test.go`.
- `[NOTE] Marker-hook mechanism not live-verified` — added a "Live smoke-test
  checkpoint" bullet to Testing requiring a manual smoke test of the non-denying
  marker hook against a real interactive `claude` run (marker line lands while
  the dialog is open; tool is not denied), matching the discussion's own
  live-verification bar.

## Pushed Back

_None. All three NOTEs were accurate and harmless to apply._
