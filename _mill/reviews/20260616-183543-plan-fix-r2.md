# Plan fix — round 2

Source review: `20260616-183451-plan-review-r2.md` (reviewer: opushigh via agent-mode dispatch; verdict APPROVE, 2 NITs, 0 blocking).

## Fixed

- `[NIT]` "state_test models an internal-package test" (Batch 4 / Card 9): removed
  `internal/muxpoc/state_test.go` from Card 9's `Context:`. That file is an internal
  (`package muxpoc`) test exercising unexported helpers, while Card 9 prescribes an external
  `package state_test` — a misleading template. Card 9's Requirements already fully specify the
  external package and the exported `WriteJSON`/`ReadJSON` surface, so the reference was unnecessary.

## Pushed Back

- `[NIT]` "Enforcement test not in any batch verify" (Batch 1/4 verify blocks): the proposed fix —
  broadening a batch `verify:` to also run `internal/paths/enforcement_test.go` — conflicts with the
  overview's documented "Go test conventions" decision (per-batch `verify:` runs only the touched
  package suite) and the mill-plan SKILL's explicit warning against over-scoped verify. The underlying
  concern is already moot: `internal/fsx` and `internal/state` introduce no `os.Getwd` /
  `git rev-parse --show-toplevel` tokens (the cards pin their imports to `fmt`/`os`/`path/filepath`/
  `strings`/`encoding/json` plus the internal lock/fsx packages), so the path invariant holds, and the
  reviewer itself marked the finding "Acceptable." The full-tree enforcement test is covered by
  mill-go's final run and CI.
