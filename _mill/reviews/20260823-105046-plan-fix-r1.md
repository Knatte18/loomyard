# Plan fix: round 1

Source review: `20260823-104926-plan-review-r1.md` (holistic, round 1, REQUEST_CHANGES, 3 BLOCKING + 2 NIT).

## Fixed

- [BLOCKING:design] "Card 10's assertion is arithmetically wrong" — batch 2, card 10: removed the extra `filepath.Dir()` wrap on the `LoomScratchDir(loc)` side of the comparison; the card now states the correct three-way equality against `filepath.Dir(LoomRunLock(loc))`/`filepath.Dir(LoomDriverLog(loc))`/`filepath.Dir(LoomBootstrapLock(loc))`.
- [BLOCKING:design] "Batch 3 is missing a dependency on batches 1 and 4" — set batch 3's `depends-on: [1, 4]` in both `03-landingshed-comment-fixes.md` and the overview's Batch Index, and added the rationale to batch 3's own scope note.
- [BLOCKING:scope] "Context completeness gaps in batch 4" — added `internal/loomengine/config.go` to card 14's Context, `internal/landingshed/config.go` to card 16's Context, and `internal/loomcli/landingdeps.go` to card 15's Context.
- [NIT:consistency] "Card 18's Config equality check will not compile" — card 18 now specifies `reflect.DeepEqual(c.landingCfg, want)` instead of `!=`, with a note explaining `landingshed.Config`'s `RequirePRToBase []string` field makes it non-comparable, and an added `"reflect"` import instruction.
- [NIT:consistency] "Card 16's line-range citation is off by one" — corrected `wiring.go:105-113` to `wiring.go:105-112` in card 16's Requirements.

## Pushed Back

(none)
