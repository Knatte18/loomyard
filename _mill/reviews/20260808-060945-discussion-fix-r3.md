# Discussion fix — round 3

Source review: `_mill/reviews/20260808-060905-discussion-review-r3.md` (verdict APPROVE, 0 blocking, 3 NOTEs).

## Fixed

- `20260808-060905-discussion-review-r3.md` — "[NOTE] Seam test's own vacuity case unaddressed": the converted seam test now carries an explicit vacuity assertion (`t.Fatal` when `os.ReadDir` yields zero non-test `.go` files), deliberately **not** inheriting the `shuttleengine` model's silent-pass behaviour, and matching the requirement already placed on `lspclient_guard_test.go`. Recorded in the `os.ReadDir` Decision and in the Testing section.
- `20260808-060905-discussion-review-r3.md` — "[NOTE] Failure-message path form under the ReadDir switch": all four predicates report `entry.Name()`, matching the named `shuttleengine` model. Recorded explicitly as a behaviour change, since the three inherited predicates currently append `WalkDir`'s absolute `path` and only the deleted catch-all used `filepath.Rel` — "kept verbatim" would otherwise have meant absolute paths in every message.
- `20260808-060905-discussion-review-r3.md` — "[NOTE] `doc.go`'s modelspec cross-reference outlives the leaf framing": the `internal/modelspec` comparison at `doc.go:30-34` is now explicitly in scope with the rest of the paragraph. `modelspec` remains an allowlisted leaf, so keeping "the shape this package mirrors most directly" and "cycle-free the same way `internal/modelspec` already is" would have re-imported the framing the edit removes.

## Pushed Back

None.
All three findings were factually accurate, verified against the cited source lines, and none of the fixes breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope.
