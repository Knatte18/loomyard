# Plan fix — round 4

Source review: `_mill/reviews/20260813-151255-plan-review-r4.md` (holistic, APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `20260813-151255-plan-review-r4.md` — "[NIT:consistency] Card 36's \"five remaining files\" undercounts its own Context list".
  Accurate, and the miscount was introduced by this plan's own round-1 fix, which enumerated the prose query's fifteen-file hit set into `Context:` without reconciling the tail count in the prose.
  Recounted: fifteen entries, minus one read-only reference (`internal/gitexec/gitexec.go`), minus four corrected by earlier batches (`internal/gitrepo/doc.go`, `internal/fabricengine/destroy.go`, `CONSTRAINTS.md`, `manifest/roadmap.md`), minus three individually-named unrelated-sense matches (`docs/overview.md`, `docs/shared-libs/lyxcwd.md`, `manifest/designs/fabric-unified-view.md`) leaves seven, not five.
  Card 36 now says seven and names all of them explicitly, with the sense each matches on (tmux, CLI, JSON envelope, and LSP exit codes), so the count is checkable rather than asserted.
  The reviewer independently confirmed all seven are genuine unrelated-sense matches, so only the arithmetic was wrong, not the disposition.

## Pushed Back

_None._
