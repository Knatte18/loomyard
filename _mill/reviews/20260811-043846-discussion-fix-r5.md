# Discussion fix — round 5

Source review: `_mill/reviews/20260811-043754-discussion-review-r5.md` (verdict APPROVE, 0 blocking, 1 NIT).
Verified against source before applying;
not pushed back.

This is the last allowed round (`roles.discussion-review.holistic.rounds: 5`).
The finding carries `demoted: true`, so the convergence predicate is not satisfied;
per the convergence gate's round-cap clause this round is treated as an implicit approval and the terminal actions run anyway, with the shortfall noted in the commit message.

## Fixed

- `20260811-043754-discussion-review-r5.md` — "Doc-site list misses in-code file-header claims": added doc site 10 covering the three falsified in-code comments, verified individually by grep:
  - `internal/batcher/registry.go:3` — "webster resolves the config-chosen active batcher back out by name via the exported Select", false once no webster code calls `Select`.
  - `internal/websterengine/recordbatch.go:34` and `beginbatch.go:52` — both `Deps` struct docs point at `internal/batcher.Select` for how `run` computed its batches; they must point at the injected `RunDeps.Batcher`.
  Also recorded that `internal/websterengine/config.go:30`'s `Select` pointer needs no step of its own, since it is deleted with the `Batcher` field it documents.
- Same finding, second half: the doc-amendments decision now states its **enumeration method** — grep `batcher.Select` and `webster.yaml` across all production Go and markdown, not just `doc.go`/`.md`. The completeness claim was previously asserted; it is now checkable, and the earlier `doc.go`-only sweep is named as the reason three sites were missed.

## Pushed Back

None.
