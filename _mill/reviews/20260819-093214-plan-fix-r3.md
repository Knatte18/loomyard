# Plan fix — round 3

Source review: `_mill/reviews/20260819-093125-plan-review-r3.md` (holistic, sonnet/xhigh, APPROVE, 0 BLOCKING + 1 NIT demoted from BLOCKING).

## Fixed

- r3 "mergestate.go's raw JSON I/O reintroduces a known torn-read race" — verified `corrindex.go` reads and writes the correspondence index in the same weft gitdir exclusively through `internal/state.ReadJSON`/`UpdateJSON` under `path+".lock"`, and that `internal/state` exposes `ReadJSON`/`WriteJSON`/`UpdateJSON` but no delete primitive.
  Card 4 now specifies `loadMergeState`/`saveMergeState` in terms of `state.ReadJSON`/`state.WriteJSON` under `<path>.lock`, states the torn-read hazard the raw form reintroduces (card 13's sibling-verb guards read this record without holding the merge verbs' combined write lock), and keeps `deleteMergeState` on raw `os.Remove` with its `destructiveguard_test.go` allowlist entry, matching `index.go`'s precedent.
  The `cmd/lyx/uncontainedwrite_test.go` allowlist entry is dropped — with no `os.WriteFile` call left in `mergestate.go`, that guard has nothing to allowlist — and the file is removed from the card's `Edits:` and from All Files Touched.
  `internal/fabricengine/corrindex.go` and `internal/state/state.go` added to the card's `Context:`.

## Pushed Back

(none)

## Validation

- `_plan_dag.validate`: 6 batches, DAG OK.
- `_plan_validate.run`: 0 findings.

## Convergence

Round 3's verdict is APPROVE with `blocking_count: 0`, but the single finding carries `demoted: true`, so the convergence gate is not satisfied (`round >= min_rounds` holds; the demoted-predicate does not).
Round 3 is below the round cap, so the NIT fix is committed and the loop continues to round 4 rather than approving here.
