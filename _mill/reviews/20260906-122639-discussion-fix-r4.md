# Discussion fix — round 4

Source review: `_mill/reviews/20260906-122609-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, 1 demoted from BLOCKING).
Auto mode: every finding is FIX by rule;
PUSH BACK is unavailable with no operator present.

## Fixed

- "[NIT:consistency] Q&A log repeats the banned render-imports claim" (demoted from BLOCKING).
  Verified accurate: the round-3 fix corrected the `reed-standalone-layout-mirrors-quarry` decision but left the Q&A-log echo carrying the superseded "`fmt` and `strings` only" wording the same decision now forbids.
  The Q&A entry now reads "773 production lines, zero internal deps, stdlib-only: `fmt`, `sort`, `strings`".

- "[NIT:consistency] Cited `pattern` line numbers are mostly comment prose".
  Verified accurate against `grep -n "pattern\." internal/fabricengine/pull.go`: `:423` and `:441` are doc-comment prose naming `pattern.PathspecFile`/`PathspecDir`, and `:464` is the sole code use;
  the import is `pull.go:23`.
  This is the same comment-prose contamination the discussion warns about in its own Technical context, so citing it wrongly was self-inconsistent.
  The citation now reads "imported at `pull.go:23`, with exactly one code use … at `pull.go:464`", and explicitly marks `:423`/`:441` as comment prose that must not be cited as use sites.

## Pushed Back

None.
