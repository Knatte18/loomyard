MILL_REVIEW_BEGIN
# Review: Surface merge-in-progress in fabric status — holistic

```yaml
duration_s: 293.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-31
```

## Findings

None. Verified against source:

- `fab.Status()`/`fab.MergeInProgress()` call shape, `weftVerbNames["status"]`, and the exact
  `output.Ok` map literal in `internal/fabriccli/weft_verbs.go` match Card 2's Requirements
  byte-for-byte, including the guarded-error-block shape and no-new-import claim.
- `Fabric.MergeInProgress()` (`mergelifecycle.go:418`) delegates to `mergeRecordExists()` only,
  never `mergeSourceInFlight` — confirms the `field-is-this-pair-only` decision and the
  this-pair-vs-hub-wide distinction repeated across the `Long` string, `doc.go` clause, and the F18
  Watch edit are all internally consistent with `mergestate.go`'s actual semantics (`Remove` on a
  merge's own *source* pair refuses via the hub-wide `mergeSourceInFlight`, so `status` on that pair
  correctly reads `false`).
- The quoted `doc.go` sentence (merge-stage rationale, ~line 1114-1118) matches the plan's locator
  text closely enough to identify unambiguously, and the two other `status` claims the plan says
  must stay untouched — the "one side-labelled view" one (line 226) — read as it's described.
- `TestRunCLI_ReadOnlyVerbsOmitMutationsKey` (`cli_test.go:899`) and
  `TestRunCLI_MergeStageRejectsAPathThatIsNotConflicted` (`merge_cli_integration_test.go:432`),
  including `setupConflictingDivergenceCLI`/`branchAtCurrentHEADCLI`, match the exact call sequence
  Card 1 prescribes; neither uses `t.Parallel`, confirming that claim too.
- `hubforge.NewHub(tb, anchor)`/`SeedFabricConfig(tb, h, yaml)`/`PrimeWorktree()`/`PrimeWeft()`
  signatures match Card 1's usage exactly; `testmain_test.go` already supplies the package's
  hermetic-git `TestMain`, matching "do not add a TestMain."
- `docs/overview.md`'s targeted bullet (line 210) is genuinely one physical line carrying four
  sentences, matching the plan's stated reason for not reflowing it.
- `manifest/roadmap.md` has exactly one Planned item and a Done section whose Maintenance note
  (lines 127-129) explicitly sanctions a Done entry pointing at package documentation rather than a
  `designs/` link — matching Card 2's roadmap-entry shape and its "no `manifest/designs/fabric.md`"
  instruction.
- Batch DAG (`00-overview.md`) is a single node, no cycle, references the one file present;
  `All Files Touched` is the exact union of Card 1's `Creates:` and Card 2's `Edits:`;
  no `Moves:` anywhere, so the Rename-mechanic requirement correctly does not apply.
- `verify:` runs both the untagged and `-tags integration` tiers over `./internal/fabriccli/...`,
  covering the new integration-tagged test file — no integration-reachability gap.
- Envelope/Mutation-Record invariants: `status` stays outside `okWithRecord`/`errWithRecord`, gains
  no `mutations`/`partial` key, consistent with `envelope.go`'s file-header comment and
  `CONSTRAINTS.md`'s Mutation Record Invariant.

## Verdict

APPROVE
Plan is fully grounded in the cited source, internally consistent, and every Shared Decision is faithfully implemented.
MILL_REVIEW_END
