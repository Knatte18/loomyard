MILL_REVIEW_BEGIN
# Review: Worktree spawn/teardown as Shed producers — holistic

```yaml
duration_s: 267.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

## Findings

None.

Verified end to end against all six batches:

- `internal/lifecycleshed` (batch 1): `PrimeLock`/`LoomRunDeps`/`TeardownDeps` seam shapes, all three producers' Call bodies (entry/cancel-error ordering, defer-released prime lock on every exit path including Stuck, verbatim error pass-through, the exhaustive `StateDone/Blocked/Paused/Failed/Running` verdict table, the `StateRunning`-final-attempt `break` correctly exits the `switch` and lets the `for` loop's own increment fall through to the exhaustion-Stuck path with no extra sleep), `reportStuck`, and the seam-enforcement allowlist all match the plan verbatim; tests cover every required disposition including the two load-bearing attempt-count assertions.
- `internal/shedrecipe` (batch 2): six new `Env` fields, `requireNonEmpty`, the three registry entries with their exact validation order, the seventeen-key registry, and both fixture files (`shedrecipe/fixture_test.go`, `shedbuild/fixture_test.go`) filled consistently. `TestRegistry_ShipsSeventeenEntries` and `engineMinimalConfig`'s "fourteen take no config" count agree.
- `contracts/recipes` + `internal/lifecyclerecipe` + coverage-guard restructuring (batch 3): the three-row YAML's load-bearing empty `on_stuck`/`on_done` edges, `lifecyclerecipe.New`'s no-coherence-check-by-design, `RecipeEngines()` twins on both recipe packages, loom's guard narrowed to its three local directions with a same-card regression test, and the new cross-consumer guard in `shedrecipe_test` (only place able to import both consumers) — all match the plan's four-coordinated-changes requirement.
- `internal/loomcli` `--no-attach` (batch 4): `mustAttach` inserted exactly where specified (after bootstrap-lock release, before `reed.Status()`), default-false pinned against the built command tree, alias-flag parity test extended.
- `internal/lifecyclecli` (batch 5): path constructors correctly `.lyx`-anchored and mutually distinct; `refuseNonPrime`'s three dispositions; `wire()`'s laziness (every task-worktree-touching seam resolves inside its own closure, proven by `TestWire_LazySeams`); the run verb's four resume dispositions, `StateDone`/lock-contention refusals, and `abandonedSession` conditional key: all cross-checked against the real signatures they call (`fabricengine.WorktreePath`, `lyxcwd.ResolveWorktree`, `reedengine.LoadConfig`/`New`/`Down`, `hubgeom.ReedGeometry`, `fabricengine.LoadConfig`/`NewTopology`/`Add`/`Remove`/`BoardDir`) — all match in name, arity, and return shape. Integration suite covers all six required cases plus the two non-prime refusals.
- Registration and docs (batch 6): `cmd/lyx/main.go` registers `lifecyclecli.Command()`; help-tree, transient-path, and sandbox-coverage guards all updated in step; `CONSTRAINTS.md`'s new Lifecycle Bookend Invariant section and the two amended lines (Told-Geometry bound-package list, CLI/Cobra "twelve of thirteen" + third Deviations entry) match exactly; `docs/overview.md`'s module bullet, execution-stack paragraph, and the "seventeen engine names" correction all present with resolving links; `manifest/roadmap.md`'s Done entry, VS Code disposition note, and both cross-reference corrections (Someday count, Next Up qualifier) are all applied; the design doc is deleted; the new `F22` sandbox scenario carries the `**Covers:** lifecycle` tag the coverage guard matches on.

Two items I checked closely and did not escalate:
- `lifecycle_integration_test.go` uses `h.PairWarpWorktree(...)`, a "warp"-bearing identifier, inside a package outside the Fabric Vocabulary Invariant's owner set. The mechanical AST scan (`internal/lyxcwd/enforcement_test.go`) explicitly skips `_test.go` files, and `internal/landingshed`'s own integration tests (`finalize_integration_test.go`) already use the identical pattern (`h.PairWarpWorktree`, `h.PairWeftSibling`, even local vars named `taskWarp`/`taskWeft`) under the same self-imposed, non-owner-set constraint — established convention, not a new violation.
- The `LoomRun.Spawn` closure in `wire.go` uses `cmd.Run()` (no `CommandContext`), so a cancelled `ctx` would not kill the spawned `loom run` child. This matches Card 23's literal specification (which never asks for `CommandContext`) and no cancellation source is wired anywhere else in this task, so it is not a plan deviation.

No out-of-plan files, no duplicated helpers across batches, no constraint violations found.

## Verdict

APPROVE
Implementation matches the plan precisely across all six batches, with correct cross-batch signatures and no constraint violations.
MILL_REVIEW_END
