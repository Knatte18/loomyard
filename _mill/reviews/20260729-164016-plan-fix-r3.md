# Plan fix report — round 3

Source review: `_mill/reviews/20260729-163641-plan-review-r3.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 2 NIT). Every finding was verified accurate against source and fixed per `mill-receiving-review`'s fix-everything default; all edits confined to `_mill/plan/` files. Zero pushbacks.

## Fixed

- [BLOCKING] "Card 14's new --in-file integration test leaks a real supervised daemon" — verified: `builtins()`'s Go entry has `HasNativeDaemon: true`, so after batch 4's flip both `--in-file` subcases route `ensureServer`→`ensureSupervised` and spawn a lyx-owned daemon that `connKindSupervised` teardown never kills; case 1's `WorktreeRoot: repoRoot(t)` also anchored it in the real repo's `.lyx/codeintel/go/`. Changed case 1 to `WorktreeRoot: t.TempDir()` (kept `TargetDir: repoRoot(t)` for indexing), required both subcases to anchor `WorktreeRoot` at an isolated `t.TempDir()` and reap the spawned daemon in `t.Cleanup` (`readDaemonState` at `Layout{WorktreeRoot}.CodeintelDaemonStateFile("go")` → `os.FindProcess(pid).Kill()`, mirroring card 13 / `supervised_test.go`), and deleted the now-false "spawns only gopls, never a lyx-owned daemon" claim. Added `internal/codeintelengine/supervised_test.go` to card 14's `Context:` so the implementer has the canonical reap/`Layout` pattern in scope.
- [BLOCKING] "Pre-existing TestReferences_Integration now leaks an unreaped, relative-path daemon post-flip" — verified: `TestReferences_Integration`'s "live gopls references" subtest calls `References` with `Registry: builtins()` and no `WorktreeRoot`, so post-flip (card 12) it anchors a supervised daemon at a relative `.lyx/codeintel/go/` under cwd and never reaps it. Since batch 5 card 14 already edits `refs_integration_test.go`, added a requirement there to also set that subtest's `WorktreeRoot: t.TempDir()` (keep `TargetDir: root`) and add the same `t.Cleanup` reap — closing the leak the flip introduces in the pre-existing test.
- [NIT] "Card 11's deadline-bounding instruction for reconnectUnderLock is underspecified" — the helper signature takes only `ctx` (no deadline) yet was told to "bound its retry by the deadline" while mirroring step 6's deadline-blind loop. Took the reviewer's option (a): removed the "bound the retry itself" language; clarified that `reconnectUnderLock`'s dial retry is already inherently bounded (finite 10×50ms, ~500ms worst case) and the deadline is enforced solely by the explicit after-return `time.Now().After(deadline)` check before `KillPID`/respawn — which is exactly what keeps the 300ms uncontended-lock test green.
- [NIT] "Card 7 overstates how similar refs'/definition's existing Long text is" — verified: `refsCommand`'s `Long` has "The result set is complete and semantically resolved ..." but `definitionCommand`'s has "The result is semantically resolved ..., not text-matched" (no "complete"/"result set" phrasing). Card 7 now notes the two commands' existing trust-note wording differs and instructs extending each command's own sentence rather than assuming a shared verbatim sentence.

## Pushed Back

- None.
