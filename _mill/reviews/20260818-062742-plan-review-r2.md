MILL_REVIEW_BEGIN
# Review: lift the orchestrator preflight out of loomengine, plus the shared standalone-CLI foundations — holistic

```yaml
duration_s: 521.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5 (Sonnet 5)
reviewed_file: plan/
date: 2026-08-18
```

## Findings

### [BLOCKING:scope] Card 16 Context omits lyxcwd.WithCwd's declaring file
**Location:** Batch 4, Card 16 (`04-cli-gate-and-ldflags.md`) **Issue:** Requirements instruct "threading the cwd in with `lyxcwd.WithCwd(ctx, dir)`", but `WithCwd` is declared in `internal/lyxcwd/cwdcontext.go` — not `internal/lyxcwd/lyxcwd.go`, the only lyxcwd file Card 16 lists in Context. `cwdcontext.go` also documents that `WithCwd` panics on a non-absolute `dir`, a fact the implementer has no listed source for. **Fix:** Add `internal/lyxcwd/cwdcontext.go` to Card 16's Context.

### [BLOCKING:scope] Card 1 Context omits the stencilstore source backing its doc.go claim
**Location:** Batch 1, Card 1 (`01-buildinfo-and-mode-mapping.md`) **Issue:** Card 1 requires `internal/buildinfo/doc.go` to state that `stencilstore.Mode`'s package "imports `internal/logger` and `internal/stencil`" — true today (`reconcile.go` imports `internal/logger`, `stencilstore.go` imports `internal/stencil`) but unverifiable from Card 1's Context, which lists only `internal/lyxdirs/doc.go`, `internal/hubgeom/doc.go`, and `cmd/lyx/stencilseed.go`. **Fix:** Add `internal/stencilstore/stencilstore.go` and `internal/stencilstore/reconcile.go` to Card 1's Context.

### [NIT:scope] Existing stencilseed integration test not surfaced in cards 15-16's Context
**Location:** Batch 4, Cards 15-16 **Issue:** `cmd/lyx/stencilenvelope_integration_test.go` already drives `seedStencils`/`seedStencilsAt` end-to-end (the `testing.Testing()` no-op guard, and the envelope-carries-no-`mutations`-key regression), but neither card lists it in Context even though card 15 restructures `seedStencils` and card 16 adds a sibling integration test file. Verified no actual regression risk (both existing tests stay gated behind `testing.Testing()`, unaffected by the gate change), so this is advisory only. **Fix:** Add it to Card 15 or 16's Context so the implementer knows it exists before adding `stencilseed_integration_test.go`.

## Verdict

REQUEST_CHANGES
Two Context-completeness gaps (Cards 1 and 16) need the missing source files added before implementation.
MILL_REVIEW_END
