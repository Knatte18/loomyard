# Discussion fix report — round 4

Source review: `_mill/reviews/20260825-115635-discussion-review-r4.md` (APPROVE, 0 blocking, 4 NIT).

## Fixed

- r4 `[NIT:scope]` "\"git grep -n sixteen\" not-touch list is incomplete" — dropped the exhaustiveness claim, made the criterion authoritative, and enumerated the three omitted must-not-touch sites (`internal/planparser/validate_test.go`, `manifest/designs/plan-card-format.md:91`, `manifest/designs/loom.md:155`) plus the note that `loom.md` appears in both lists and cannot be blind-replaced.
- r4 `[NIT:scope]` "loom.md:78 task list not in the doc-update inventory" — added line 78 to the `manifest/designs/loom.md` edit list, with the `(shipped)` marker convention it must follow.
- r4 `[NIT:consistency]` "\"nothing but Webster commits warp content\" is stronger than the mechanism" — softened the rationale to "normally carries no commits", named `Discussion-Burler`'s shipped `fix-scope: source` as the in-system route to a pre-Webster warp commit (`internal/burlerengine/prompt.go:144-149`), and folded that sibling into the existing merge-base open risk.
- r4 `[NIT:consistency]` "Derivation recipe duplicated across recipe and stencil with no pin" — named the rubric stencil canonical, reduced `profile.target.instructions` to a pointer at its named heading, cited the Producer Pointer-Rule Invariant as the reason, and removed the duplicate-derivation phrasing from the `bouncer-artifact-paths-names-the-plan` decision.

## Pushed Back

(none — `--orch` mode has no PUSH BACK path; all four findings were verified accurate against source before fixing.)
