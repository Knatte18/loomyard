# Plan fixer report — round 4

Source review: `_mill/reviews/20260726-153928-plan-review-r4.md`

## Fixed

- r4 review, "[BLOCKING] Card 2 maps RoundSummary/Profile fields without
  their file in Context" — verified accurate (both files are read-only
  dependencies of the mapping and validate call, and neither is in card 2's
  Edits, so implicit-read does not apply). Added
  `internal/perchengine/profile.go` and `internal/perchengine/result.go` to
  card 2's Context.
- r4 review, "[BLOCKING] Card 14's doc.go coverage list outruns its
  Context" — verified accurate. Added `internal/treadleengine/judge.go`,
  `gate.go`, `roundfiles.go`, `state.go`, and `engine.go` to card 14's
  Context.
- r4 review, "[NIT] Treadle's seam allowlist admits an import no moved file
  uses" — verified: no moved production file imports `internal/hubgeometry`
  (only doc-comment mentions in state.go). Dropped hubgeometry from card
  4's allowlist and from the overview's no-burler-import decision, with an
  explicit geometry-blind rationale.

## Pushed Back

- r4 review, "[NIT] Overview's 'All Files Touched' omits the deleted design
  doc" — factually wrong per the plan-format contract, and the proposed fix
  is unsatisfiable: the overview template defines All Files Touched as the
  union of `Creates:`/`Edits:`/`Moves:` TARGET paths with Deletes tokens
  excluded, and `_plan_validate._check_all_files_touched_mismatch` enforces
  this symmetrically — its `cards_set` explicitly excludes `Deletes:`
  tokens ("per issue #494"), and any overview entry not in that derived
  union produces an `all-files-touched-mismatch` error. Adding
  `manifest/designs/treadle.md` (a Deletes-only path) would fail the
  pre-review validator gate. The deletion remains visible in card 14's
  `Deletes:` field, which is its format-correct home.
