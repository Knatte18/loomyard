# Plan fixer report — round 2

Source review: `_mill/reviews/20260625-132324-plan-review-r2.md` (verdict REQUEST_CHANGES, 3 BLOCKING + 2 NITs).

## Fixed

- `20260625-132324-plan-review-r2.md` — [BLOCKING] "configcli integration test imports deleted internal/worktree" — added `internal/configcli/configcli_integration_test.go` to card 10's `Edits` with a requirement to swap the `internal/worktree` import + `worktree.New`/`Config`/`AddOptions` symbols for the `internal/warp` equivalents and the `"worktree"` module-name strings for `"warp"`; also added it to card 12's `Edits` so that, once `Add` becomes dormant, the test wires the `_lyx` junction explicitly (`WireJunctions`) before exercising config edits.
- `20260625-132324-plan-review-r2.md` — [BLOCKING] "configreg_test asserts the old worktree module name" — added `internal/configreg/configreg_test.go` to card 9's `Edits` with a requirement to update `want := []string{"board","worktree","weft"}` → `"warp"`.
- `20260625-132324-plan-review-r2.md` — [BLOCKING] "configcli tests reference the renamed config module" — added `internal/configcli/configcli_test.go` to card 10's `Edits` with a requirement to rename every `"worktree"` config-module literal and the `"worktree (configured)"` menu assertion to `"warp"`.
- `20260625-132324-plan-review-r2.md` — [NIT] "init dormant-pairing detection mechanism unspecified" — card 14 now names the predicate: `os.Stat(l.WeftWorktree())` not existing (equivalently a `weftRepoExists`-style sibling check).
- **Beyond the review (same failure class, swept proactively to converge):** a full `grep` for `internal/worktree` importers and `"worktree"` config-module literals surfaced three more consumers that batch 3 would break — added all to card 10's `Edits` with requirements: `internal/initcli/initcli_test.go` (import + module list + `LoadConfig` → warp), `internal/lyxtest/leaf_enforcement_test.go` (banned-import **string literal** worktree→warp; it is a string in the ban list, not a real import — the leaf invariant now bans importing `internal/warp`), and `cmd/lyx/main_test.go` (`run([]string{"worktree","list"})` → `{"warp","list"}`). Batch 3 `verify:` extended to run `./internal/configcli/ ./internal/initcli/ ./internal/lyxtest/` (with `-tags integration` for the configcli integration test) so these breaks are caught at the batch that introduces them, not deferred to the batch-9 module-wide gate.

## Pushed Back

- `20260625-132324-plan-review-r2.md` — [NIT] "weft/cli.go caller of Status not in Card 20 Edits" — **factually inaccurate.** `internal/weft/cli.go` is already listed under card 20's `Edits:` (it was moved there in the round-1 fix, commit `878404e`); it is no longer in `Context:`. No change needed.
