# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260828-062744-plan-review-r1.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- `20260828-062744-plan-review-r1.md` [BLOCKING:scope] "Card 1's Context omits three files its own Requirements assert a fact about" — verified accurate: card 1's Requirements bullet 2 asserts that `internal/githubclient/doc.go`, `internal/reedengine/doc.go`, and `internal/reedengine/attach.go` hit the `exec.Command` substring with zero real calls, while none of the three was in that card's `Context:` allowlist. Added all three to card 1's `Context:` in `01-audit-doc-and-constraints.md`, so the implementer can re-verify the claim rather than copying it on faith — which is what the plan's own `selector-reruns-are-the-authority` decision requires.
- `20260828-062744-plan-review-r1.md` [NIT:consistency] "Card 7 says \"four\" outcomes but enumerates only three, omitting the most-hit path" — verified accurate against `internal/treadleengine/gate.go`: `execGateCommand` has four non-error returns after `cmd.CombinedOutput()`, and card 7's list named only three, silently omitting the ordinary non-zero-exit path (`errors.As(err, &exitErr)` → `return output, false, nil`). Rewrote card 7's second Requirements bullet in `03-spawn-site-log-lines.md` to enumerate all four explicitly in source order, and stated that the fourth is the "gate command ran and failed" case `converged` checks every round.

## Pushed Back

_None._
Both findings were factually accurate against the cited source, and neither fix breaks existing functionality, conflicts with a documented decision in `_mill/discussion.md`, or destabilizes work outside this task.
