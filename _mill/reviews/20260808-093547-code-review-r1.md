MILL_REVIEW_BEGIN
# Review: Scout owns its own lyxcwd-based geometry accessors (drop Options.AnchorRoot threading) — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-08
```

## Findings

None. Verified against every card in `01-location-threading.md` and all nine Shared Decisions:

- Card 1: `DaemonStateFile`/`DaemonLock` re-signatured onto `*lyxcwd.Location`, `l.WorktreePath()` substituted correctly, TODO(dotlyx) markers present, doc comments reworded without naming a string param.
- Card 2: `Options.WorktreeRoot` → `Options.Layout *lyxcwd.Location` in the same struct position, `TargetDir` kept separate, only `Layout` carries a doc comment, no nil check added, `acquireConnection` passes `opts.Layout` correctly.
- Card 3: `ensureServer`/`ensureSupervised` signatures threaded exactly as specified; `statePath`/`lockPath` now call `DaemonStateFile(layout, lang)`/`DaemonLock(layout, lang)`; header sentence and inline `socketPath` comment reworded to `(layout, lang)`; stale citation to `scout-redesign.md` left untouched.
- Card 4: all three `doc.go` sites reworded to `layout`, consistent with `ensureserver.go`'s own copy of the socket-path sentence; `os.UserCacheDir()`/ephemerality paragraphs untouched.
- Card 5: `resolveLocation`/`lookupContext` added exactly as specified (including the `filepath.Abs` error-path fallback from `targetDir`, not `abs`); `lookupContext`'s doc comment states the `dir`-not-raw-flag rationale; all four `RunE` blocks (including `assert-no-callers`) now share one `lookupContext` call and one `buildOptions` value; no stray `resolveWorktreeRoot`/`WorktreeRoot` references remain in `scoutcli`.
- Card 6: `resolveLocation`/`lookupContext` tests assert `WorktreePath()` and `AnchorRel == "."`; `buildOptions` test compares `Layout.WorktreePath()` (not pointer identity); registry spot-checked via `"go"` entry, not `reflect.DeepEqual` over the whole map (note: the new `lookupContext` test does use `reflect.DeepEqual` but scoped to the single `"go"` entry, matching the card's intent).
- Cards 7–9: every untagged, integration, and `//go:build scout` test file adapted with the hand-built `Location` fixture shape (`AnchorRel: "."` explicit, matching `websterengine/audit_test.go`'s `fakeLayout` idiom), no expected values changed, `lyxcwd` import added throughout, no stray `WorktreeRoot`/`worktreeRoot` prose left in comments (only local variable names, which is expected since `targetDir` stays a string).
- Card 8: `constructoranchoring_test.go` reuses the existing `l` from `anchoringFixture`, no second helper, all `dotLyxBase` expectations unchanged in both tests.
- Cross-cutting: no out-of-plan files present beyond the manifest; no global utility duplication; Scout Engine-Seam Invariant intact (no cobra/output/io.Writer in scoutengine); `lspclient.go` untouched; CONSTRAINTS.md/roadmap/overview untouched as required.

## Verdict

APPROVE
Implementation matches every card and shared decision; no deviations, no missed call sites, no constraint violations found.
MILL_REVIEW_END
