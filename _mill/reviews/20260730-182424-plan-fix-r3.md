# Plan fix — round 3

Source review: `_mill/reviews/20260730-182254-plan-review-r3.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260730-182254-plan-review-r3.md` — "[NIT] Build-unit note understates the true non-compiling window": reworded Batch 1's Build-unit note to distinguish `go build` (compiles after Card 4) from `go test`/the batch `verify` (compiles only after Card 6, since `reddit_test.go` references deleted symbols until Card 5 and `fetch_test.go`'s Reddit routing test until Card 6); kept the "verify runs once at batch end, not per card" guidance.
- `20260730-182254-plan-review-r3.md` — "[NIT] marketplace.json's per-plugin version not bumped alongside plugin.json": added `.claude-plugin/marketplace.json` to Card 14's Edits and Requirements (bump the prowler entry's `plugins[0].version` 1.0.0 → 1.1.0, leaving the marketplace's own top-level version unchanged); also added the file to the overview's All Files Touched and updated Batch 2's scope prose.

## Pushed Back

- None.
