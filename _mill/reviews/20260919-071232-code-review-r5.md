MILL_REVIEW_BEGIN
# Review: Rename loom CLI run/drive/step for verb/engine symmetry, plus rename ly-supervise — holistic

```yaml
duration_s: 347.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

## Findings

None. I read all five batch files plus the overview and cross-checked every card's `Requirements:` against the delivered source across all 66 manifest files, including full-text reads of `internal/loomcli/*.go` and `_test.go`, `cmd/lyx/main.go` + tests, `internal/fabricengine/launchers.go`/`launcher_content.go`/`launcher_content_test.go`, the comment-sweep targets in `loomengine`, `loomshed`, `frictionengine`, `webstercli`, `websterengine`, `landingshed`, `shuttleengine`, `friction`, and the test-comment files, the skill rename (`ly-drive/SKILL.md`, `INDEX.md`, `plugin.json`), and every docs/manifest file in batch 5 including the new "The shape it would take" section in `shed-generic-watchdog.md`, the roadmap Planned→Done move, the spec/sandbox-suite edits, and the `loom-cli-rename.md` deletion with both inbound links fixed first.

Every landmark the plan calls out — the two-file move chain (`run.go`→`start.go`, `drive.go`→`run.go`), the `runCmd`/`startCmd`/`StartAliasCommand` renames, the refusal-string rewrites, the `findDriverPIDs` adjacent-pair rewrite, the smoke-test argv retargeting and test-name renames, the launcher content/filename-unchanged pairing, the contrast-prose sentences rewritten (never token-substituted) in `loom.md`, `shed.md`, `self-report-tier1.md`, `loom-status-spec.md`, `reed-born-as-strand.md`, `attach.go`, `seed.go`, `friction.go` — matches its required post-rename form, with both sides of every contrast still distinct. The noun-sense carve-outs (`webstercli/wiring.go`'s "standalone webster run is not a loom run", `landingshed/deps.go:88`, `friction.go`'s "run" as execution) are correctly left untouched. Filename citations were retargeted correctly and specifically (`start.go` vs `run.go` chosen per which post-move file the cited logic actually lives in, e.g. `sharedbootstrap.go`'s four citations, `wiring.go`'s two, `landingdeps.go`'s, `logger-coverage.md`'s two, `reed-fabric-standalone-api.md`'s de-line-ranged retarget). No out-of-plan files, no duplicated helpers, no stray `ly-supervise`/`driveCmd`/`RunAliasCommand`/`"loom", "drive"` residue anywhere outside `_mill/` (excluded) and the two sanctioned historical "X renamed to Y" records in `roadmap.md` and `shed-generic-watchdog.md`. Constraint compliance (CLI/Cobra interactive-handoff list, Test Tier Purity in the new `writeLaunchers`/`retiredverbs_test.go` tests, Markdown Link Integrity around the deletion) all check out.

## Verdict

APPROVE
Every batch's cards are fully and correctly realized with no scope, consistency, or constraint findings.
MILL_REVIEW_END
