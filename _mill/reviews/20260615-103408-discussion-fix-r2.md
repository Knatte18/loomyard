# Discussion-fix report — round 2

Source review: `_mill/reviews/20260615-103334-discussion-review-r2.md` (verdict APPROVE, 2 NOTEs)

## Fixed

- [r2 NOTE] "PortalsDir/LaunchersDir doc-table semantics drift" — added an explicit
  mirrored-leaf vs un-mirrored-root role-split note to the Scope `PortalsDir()`/
  `LaunchersDir()` bullet and to the `docs-in-sync` decision (paths.md line 76 table
  must be rewritten from `LauncherDir = Join(LaunchersDir(), slug)`).
- [r2 NOTE] "MenuLauncherRel target uses spawning worktree's RelPath" — added a
  "Cross-worktree subpath uniformity" paragraph to the `relative-climb-via-paths`
  decision making explicit that all worktrees share subpath structure, so the
  resolving worktree's RelPath validly indexes the main worktree's hub subpath.

## Pushed Back

(none — both NOTEs were factually accurate and harmless to apply)
