# Plan fixer report — round 3

Source review: `_mill/reviews/20260726-152407-plan-review-r3.md`

## Fixed

- r3 review, "[BLOCKING] Card 14 misattributes the anchored treadle.md link
  to the wrong file" — verified against the repo grep: the
  `#process--do-not-fold-this-into-hardeners-task` anchor is in
  `manifest/designs/shed.md` (line 46); hardener.md's four links are plain.
  Card 14 now attributes the anchor rewording to shed.md and marks
  hardener.md's links as simple retargets.
- r3 review, "[NIT] Card 3's 'surgical edits only' scope leaves two moved
  files' header comments stale" — card 3 now explicitly licenses
  header-comment corrections where the move falsifies them, naming
  `smoke_judge_test.go`'s stays-in-perchengine rationale and
  `roundfiles_test.go`'s `buildRoundProfile` claim.
- r3 review, "[NIT] Card 13's Context omits modelspec.go despite
  referencing its types" — `internal/modelspec/modelspec.go` added to
  card 13's Context.

## Pushed Back

- (none)
