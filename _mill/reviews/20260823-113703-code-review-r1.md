MILL_REVIEW_BEGIN
# Review: landing: parent-fabric resolution chain — holistic

```yaml
duration_s: 150.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-23
```

## Findings

No findings. Checked, batch by batch, against the plan and CONSTRAINTS.md:

- **Batch 1** (`fabricengine-parent-resolution`): `WorktreeEntry.Prunable` parsed via `strings.HasPrefix(line, "prunable")` exactly as specified (`worktreelist.go:22,71-75`); `matchParentBranch` skips `Prunable` before checking branch, normalizes via `filepath.FromSlash`, special-cases nothing (`worktreelist.go:96-106`); `OpenParent` implements the four-step chain with the exact wrap-message shapes for each failure branch, uses `ResolveWorktree` not `Resolve` (`worktreelist.go:117-140`); `export_test.go`'s `MatchParentBranchForTest` re-export matches the file's existing idiom; `Fabric.OriginURL`/`Fabric.PushBranch` placed after `WeftLyxDir`, delegate exactly as specified (`fabric.go:154-169`); `doc.go`'s new sentence is inserted at the exact specified location between the two named sentences. Test coverage: `matchparent_test.go` covers all seven required scenarios; `openparent_integration_test.go` covers all five required test functions with the specified assertions (including the origin-URL fold-in and the worktree-lock trick); `worktreelist_test.go`'s `TestList_ParsesPrunable` covers both the set-and-unset cases via `List`.
- **Batch 2** (`loomengine-scratch-dir`): `LoomScratchDir` placed after `LoomBootstrapLock`, joins `AnchorPath()`/`DotLyxDirName`/`loomDirName` exactly (`config.go:124-131`); `LoomStatusLock`'s doc comment no longer carries the stale "no Dir(l) accessor" clause. `TestLoomScratchDir_MirrorsRunLockDriverLogAndBootstrapLockParent` compares unwrapped against `filepath.Dir` of the other three, built over a synthetic `Location`, no temp-dir/git spawn.
- **Batch 3** (`landingshed-comment-fixes`): all three `deps.go` comment corrections landed as specified — the `OpenFabric`/`OpenParentFabric` doc now names `internal/loomcli/drive.go` as the filler, the `PushBranch` doc now attributes verb-naming to `internal/fabricengine`'s `Fabric.PushBranch` rather than "the caller." No production logic touched.
- **Batch 4** (`loomcli-landing-wiring`): `resolveLandingParent` wraps `resolveParentBranch(recorded, found, "")` and adds the self-parent refusal exactly as specified (`seedinput.go:70-82`); `TestResolveLandingParent` covers all three required cases. `landingDeps` matches the card's literal function body verbatim, including field-by-field mapping and the two lazy closures over `l`/`parentBranch` (`landingdeps.go:25-55`). `landingdeps_test.go`'s reflection-based drift guard is Tier 1 (no fixture) and uses a non-zero `Config{Squash: true}`. `cli.go`'s three new `loomCLI` fields and doc comments match the card's text. `wiring.go` loads `landingshed.LoadConfig` in the specified position and stores `registry`/`runner`/`landingCfg`; the comment-block replacement is byte-exact against the plan's before/after text. `wiring_test.go` seeds `landing.yaml` via `hubLocation`, and `TestWire_LandingSeamFieldsPopulated` uses `reflect.DeepEqual` (required, since `Config.RequirePRToBase []string` makes it non-comparable). `drive.go` inserts the exact block specified, immediately before `loomrecipe.New`, with exactly one `fabricengine.Open` call in the function.
- **Batch 5** (`docs-roadmap-and-design`): the Planned subsection is fully removed from `roadmap.md`; the Done entry is inserted immediately after `shedadapters: Burler-round producer` and before `## Maintenance` (not misplaced mid-section), correctly states `fabricengine.List` pre-existed, and links both `internal/fabricengine/doc.go` and `designs/loom.md` — both link targets verified to exist. The `loom: convert to a Shed recipe` entry's stale forward-reference is reworded to past tense, still naming the closing entry. `designs/loom.md`'s stale "once ... lands" sentence is reworded to present/past tense citing the Done entry by name, linking `internal/landingshed/doc.go`, which exists.

Cross-batch contracts check out: `internal/loomcli` (not a Fabric Vocabulary Invariant owner) reaches `fabricengine.OpenParent`/`Fabric.OriginURL`/`Fabric.PushBranch` only through the neutral surface batch 1 built, never a bare `warp`/`weft` token or `PushWarpRebaseFreeAt` directly — the `fabric-vocabulary-owner-confinement` decision holds throughout `seedinput.go`, `landingdeps.go`, `drive.go`, `wiring.go`. `landingDeps`'s field values (`websterengine.Geometry`, `loomengine.LoomScratchDir`, `modelspec.Registry`, `*shuttleengine.Runner` against `mergeresolve.Shuttle`) all match the producer signatures shown in their respective packages. No out-of-plan files present, no duplicated helpers, no constraint violations found.

## Verdict

APPROVE
Every batch's cards are fully and correctly realized; cross-batch contracts, shared decisions, and constraints all hold.
MILL_REVIEW_END
