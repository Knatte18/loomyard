MILL_REVIEW_BEGIN
# Review: fabric: close the corrindex two-phase read-modify-write race (slice 15) — holistic

```yaml
duration_s: 239.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet (harness-reported model ID "claude-sonnet-5")
reviewed_file: plan/
date: 2026-08-12
```

## Findings

None.

Cross-checked all three batches against `_mill/discussion.md`'s Decisions, Technical context, and Testing sections, and against the actual current source (`state.go`, `lock.go`, `fsx.go`, `corrindex.go`, `index.go`, `doc.go`) and the four `manifest/designs/*.md` reference targets.

- Batch Index DAG is a clean chain (1 → 2 → 3), all `file:` entries exist, no cycle.
- `## All Files Touched` is exactly the union of `Edits:`/`Creates:` across all seven cards (11 files), correctly excluding the Move-source/Deletes-only `fabric-crucible-followups.md`.
- Every `### Decision:` in `## Shared Decisions` is traceable to a specific card requirement (lock-free-core extraction, `UpdateJSON` signature/dispositions, single-direction-only guarantee restated in `state/doc.go`, `corrindex.go`'s doc comment, `fabricengine/doc.go`'s fold, and `roadmap.md`'s Done entry; no new `CONSTRAINTS.md` entry; semantic line breaks; prose-not-anchor repointing).
- No `Moves:` anywhere, so the absent `## Rename mechanic` section is correctly not required.
- Card 6's nine-vs-six reference split and each repointed/rewritten sentence's exact wording were verified byte-for-byte against the live text of `fabric-unified-view.md:228`, `fabric-windows-verification.md:34,73`, `gitexec-error-shape.md:510`, and `lyxtest-real-hubs.md:7,20` — all match current line content, no staleness.
- Card 7's roadmap inventory (Planned item deletion at :12-33, Done-entry pointer sentences at :208/:215, slice-12's stale ":201" cross-reference) matches the file's current line numbers exactly.
- Context/Edits completeness holds for every card — no `Requirements:` identifier traced to a file outside its card's `Context:`/`Edits:`.
- Test plan for `corrindex_test.go` correctly targets `record()` vs. an external write (not `record()` vs. `record()`), matching the call-graph argument that two `record()` calls cannot overlap in production.

## Verdict

APPROVE
Plan is internally consistent, fully traces to the discussion's decisions, and all cross-references verified against live source.
MILL_REVIEW_END
