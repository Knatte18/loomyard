MILL_REVIEW_BEGIN
# Review: pattern told-geometry — holistic

```yaml
duration_s: 111.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-18
```

## Findings

None. All five cards of batch 01 were verified against the plan and hold up end-to-end.

- `internal/pattern/pattern.go`: `Directive`/`isActive` take `anchorPath string`; the empty-string guard sits exactly where the old `l == nil` guard did, before `isActive`; `FileHere` is deleted; `lyxcwd` import is gone. `File`, `PathspecFile`, `PathspecDir`, the role/stencil constants, and `Directive`'s stencil-read body are byte-identical to the plan's "do not change" list.
- `grep -rn lyxcwd internal/pattern/` returns no hits — production and test files alike (verified directly).
- `internal/pattern/pattern_test.go` and `patternpath_test.go`: `layoutAt`/`newTestLocation` removed, every enumerated test converted to pass a bare `root`/`anchorPath` string, `TestDirective_EmptyAnchorPath` added with the statFile-not-called assertion, `TestDirective_NestedAnchorSubdirectory` reworded off `lyxcwd` vocabulary including the three inline `RelPath` spellings the card called out individually.
- `internal/pattern/leaf_enforcement_test.go`: `allowedImports` map is tightened to the three-package allowlist, no `lyxcwd` entry remains.
- All four `pattern.Directive` call sites (`burlerengine/engine.go:103`, `websterengine/render.go:174,237`, `loomengine/plan.go:71`) pass `*.AnchorPath()` inline, no re-created nil guard, no signature change to `New`/`RenderRecoveryPrompt`/`RenderMasterPrompt`/`PlanSpec`, no statement reordering.
- `cmd/lyx/constructoranchoring_test.go`: both `pattern.FileHere` rows rewritten to `pattern.File(l.AnchorPath())`; the tautology comments name the two `planparser` rows plus the `pattern.File` row explicitly (no "contiguous count" claim) and both list `TestPlanSpec_PatternDirectiveAnchoredUnderAnchorPath` in the pointer list, added in card 3's own commit per the plan.
- `internal/loomengine/plan_test.go`: `TestPlanSpec_PatternDirectiveAnchoredUnderAnchorPath` sits immediately after `TestPlanSpec_AnchoredUnderAnchorPathNotWorktreePath`, uses a real `t.TempDir()` hub with `AnchorRel: "backend"`, positive case plants `PATTERN.md` under `AnchorPath()` and asserts `"## Constraints"` is present (confirmed that heading is the real stencil's first line), negative case plants it under `WorktreePath()` and asserts absence.
- `internal/burlerengine/engine_test.go`: `TestEngine_Run_PatternDirectiveReachesInstruction1` reuses `newEngineTestProfile`/`newEngineForTest`/`newTestStencilsDir`, discovers the round dir via `os.ReadDir` rather than a hardcoded `round-` prefix, and asserts the literal `_lyx/PATTERN.md` pointer on `instruction-1-explore.md`'s disk content for both the active and inactive sub-tests. The file-header comment now names the new detector among its enumerated subjects.
- `internal/websterengine/template_test.go`: `patternActiveLayout`'s comment now points at `writePatternFile`/told-anchor-path semantics rather than the stale `PatternFileHere`/`layoutAt` names; fixture bodies and every test assertion left byte-identical.
- `CONSTRAINTS.md`'s Pattern Leaf Invariant opening sentence drops `internal/lyxcwd` from the allowlist; the three justification paragraphs and the `Enforced by` bullet are unchanged.
- `internal/pattern/doc.go`'s three call-outs (active-check resolution, stencil-read-path empty-case wording, and the relative-pointer contrast) were rewritten to told-anchor-path vocabulary with no bare `_lyx` token introduced; the review-obligation sentence about `File`/pathspec constants is preserved verbatim.
- No out-of-plan files: every file under `## All Files Touched` in the overview matches this batch's edits, and no surprise file surfaced during review.

## Verdict

APPROVE
Every card's requirements are met exactly, all four call sites and both transposition detectors check out, no constraint violations found.
MILL_REVIEW_END
