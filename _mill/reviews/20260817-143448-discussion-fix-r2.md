# Discussion fix — round 2

Source review: `_mill/reviews/20260817-143332-discussion-review-r2.md` (APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- r2 `[NIT:consistency]` "Q&A still says `cli.go:194` stays uncovered" — the superseded Q&A answer now records the r1 re-decision explicitly, marked as superseded so the reversal is auditable rather than silently overwritten.
- r2 `[NIT:scope]` "Stale-comment inventory misses `websterengine/runlevel.go:100`" — replaced the read-assembled list with a stated grep method over both `lyxcwd-resolved` spellings, and took its full 5-line result into a disposition table: `cli.go:57-58` and `runlevel.go:100` in scope, `beginbatch.go:64,66` dispositioned out (webster dirs only, still true until T7).
- r2 `[NIT:consistency]` "`cli_test.go` anchor flip is tautological too" — recorded that `newTestCLI` both computes `planDir` and seeds into it, so the flip proves nested-anchor path handling and not anchoring; the anchor-always decision now names only the `loomengine.PlanSpec` case and the new `PersistentPreRunE` case as anchoring proof.

## Pushed Back

None.
All three findings verified accurate against source before fixing (`internal/websterengine/runlevel.go:100`, `internal/webstercli/run.go:66`, `internal/webstercli/cli_test.go:169-177,201,252`).
