# Discussion fix — round 5

Source review: `_mill/reviews/20260828-060702-discussion-review-r5.md` (verdict APPROVE, 3 NITs, none demoted).
Every finding was verified against source before being applied; none were pushed back.
This round satisfies the convergence gate: `round >= min_rounds` and no finding was demoted.

## Fixed

- `20260828-060702-discussion-review-r5.md` — "[NIT:consistency] Link-integrity claim overstates the invariant's reach": confirmed `CONSTRAINTS.md`'s Markdown Link Integrity entry scans `manifest/` and `docs/` only, with `contracts/specs/` a target rather than a scan source. Rewrote the Constraints bullet to say the invariant covers `docs/overview.md` alone among this task's files, and that correct links in the two spec files are review discipline with no `go test` enforcement behind them.
- `20260828-060702-discussion-review-r5.md` — "[NIT:consistency] Retained webster-spec sentence names the wrong consumer": confirmed `contracts/specs/webster-spec.md:43` reads "because Finalize dumps `summary.md` verbatim into the PR body", and that `publish.go:169-182` is what builds the PR, passing `summary.Title` and `summary.Body` as separate `github.NewPullRequest` fields rather than dumping the file. Added a bullet to the `spec-file-split` Decision requiring the sentence be corrected to name `Publish` and drop "verbatim" as it is retained.
- `20260828-060702-discussion-review-r5.md` — "[NIT:scope] Kept-specs enumeration at overview.md:98 is already stale": confirmed `contracts/specs/loom-plan-spec.md` exists and is linked from `docs/overview.md:304` but is absent from the `docs/overview.md:98` enumeration. Chose to repair rather than preserve the drift, and said so explicitly in the Scope bullet with the reasoning.

## Pushed Back

_None._
