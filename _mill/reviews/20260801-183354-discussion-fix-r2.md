# Discussion-fix report — round 2

Source review: `_mill/reviews/20260801-183325-discussion-review-r2.md` (verdict: APPROVE, 2 NOTE findings)

## Fixed

- **[NOTE] Guard can't catch re-added mutation in allowlisted files** — added a "Residual coverage limit" paragraph to the `regression-guard` decision in `_mill/discussion.md`, stating the allowlist exempts `gitwrap.go`/`gitquery.go` wholesale (not per-line), acknowledging a future mutating call re-added into either file would pass silently, and recording why that residual risk is accepted (both files are small, single-purpose, and read-only by design once `ResetHard` is deleted from `gitquery.go`) — mirroring `tierpurity_test.go`'s own `allowedSpawners` tradeoff.
- **[NOTE] Guard rationale overstates the post-migration token absence** — reworded the `regression-guard` decision's rationale from "neither package ever constructs... or calls... directly for anything" to "outside the two allowlisted files, neither package constructs/calls..." and added a parenthetical naming the exact surviving call sites (`gitwrap.go:27,40`, `gitquery.go:23,39,62`) so a plan-writer isn't misled about which files still legitimately carry the banned tokens.

## Pushed Back

(none)
