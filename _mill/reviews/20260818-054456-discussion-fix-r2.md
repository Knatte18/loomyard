# Discussion fix — round 2

Source review: `_mill/reviews/20260818-054348-discussion-review-r2.md` (APPROVE, 0 BLOCKING, 5 NIT).
Auto mode: every NIT fixed under the `mill-receiving-review` fix-everything default.

## Fixed

- r2 · "Q&A entry contradicts the relocation decision" — marked the third Q&A answer superseded in part, stating plainly that the proof is a new sibling test, not an extension of the existing case, so a plan writer reading the log top-down cannot implement the retracted option.
- r2 · "Stale-reference inventory misses three in-tree sites" — Scope now names `pattern.go:84-85` (`Directive`'s "for a nil `l`"), `pattern.go:120` (`isActive`'s "an absent `FileHere(l)`"), `doc.go:55` (the "built from a `Location` field" phrasing), `doc.go:69` (the nil-layout lazy-read sentence), and `patternpath_test.go:1` (the "`File/FileHere` constructors" header).
- r2 · "`newTestStencilsDir` is not in `plan_test.go`" — both mentions corrected to "same package, different file: `internal/loomengine/prompt_test.go:21`". Added an instruction to confirm the helper seeds the three `pattern-directive-*` stencils, since the new case depends on it doing so.
- r2 · "Relocated tautology comment names only the planparser rows" — the decision now says the existing comment block is **amended** (count widened, pointer list extended with the new `plan_test.go` case), never copied verbatim onto a third row.
- r2 · "Type safety lost between two adjacent string params" — added to Constraints → Discovered during exploration: the transposition failure mode is described (compiles, fails silently as inactive PATTERN), the decision to accept it rather than introduce a named string type is stated with its reason, and the two behavioural detectors relied on are named.

## Pushed Back

None.
Every finding verified accurate against current source before fixing: `internal/loomengine/prompt_test.go:21` confirmed by grep as `newTestStencilsDir`'s sole declaration; the `pattern.go` and `doc.go` line references confirmed by reading those files; the `cmd/lyx/constructoranchoring_test.go` comment block confirmed to open "The two planparser rows below…" and to point at `plan_test.go` and `webstercli/verbs_test.go`.
