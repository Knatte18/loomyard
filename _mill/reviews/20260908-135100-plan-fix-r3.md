# Fixer report: plan-fix round 3

Source review: `20260908-134807-plan-review-r3.md`

## Fixed

- `20260908-134807-plan-review-r3.md` — "[BLOCKING:design] Real descriptors ship untested; fixture mitigation is false".
  Verified: after batch 2's prune, no test in either CLI package asserts any descriptor field's text, and the retained `TestWire_TargetDirRefusedInHubMode` only checks the error mentions `--target-dir` — so the Shared Decision's "a reworded message fails the suite" was untrue of the six new fields.
  Cards 6 and 7 each now add a `TestWireModule_DescriptorIsVerbatim` pinning that package's own real `wireModule`: every string field against its literal, the hub refusal and (webster) the missing-plan refusal verbatim, and one real `ResolveStandalone` nested-geometry refusal verbatim.
  Card 5's mitigation sentence is corrected to name those two tests and to stop claiming the composition tests or the `cli_integration_test.go` files catch the divergence.
- `20260908-134807-plan-review-r3.md` — "[NIT:consistency] `module.go` doc comments reinstate the per-CLI text".
  Verified against this batch's own "no production file under `internal/cliwire` names `webstercli` or `burlercli`" rule, which card 3 restated and then contradicted.
  The four varying fields' doc comments now state only the role and sentence position each field occupies;
  the concrete values live solely in cards 6 and 7's `wireModule` declarations.
  Card 3 also now marks its fenced hub-refusal example as a plan-side illustration of the produced string, not text to embed in a doc comment.
- `20260908-134807-plan-review-r3.md` — "[NIT:scope] cliwire's named test helpers cannot yield `stateDir`".
  Verified: every listed case that drives `ResolveStandalone` needs `stateDir`, which neither `hash8For` nor `setStandaloneStateRoot` returns.
  Card 5's helper list gains `hash8AndStateDir(t, target) (stateDir, hash8 string)`, with a note that it — not `hash8For` — is what those cases call.
- `20260908-134807-plan-review-r3.md` — "[NIT:consistency] Enforcement tests cite an invariant two commits before it exists".
  Verified.
  Resolved by the reviewer's second option rather than by reordering: card 10's `Context:` lists both enforcement-test files and its text describes what they assert, so running it first would have the docs card describe files that do not exist yet.
  Batch 3's scope now states the forward reference is deliberate and points at the overview's landing-commit decision, since the task squash-merges and the intermediate ordering is never a state anyone reads.
- `20260908-134807-plan-review-r3.md` — "[NIT:consistency] Stale doc comments left in the pruned test files".
  Verified: `seedStandalonePlanDir`'s doc names `standalonePlanDirHasContent` and `seedGitRepositoryRoot`'s names `repositoryRootOf`, both deleted by the same cards.
  Cards 6 and 7 now list every doc-comment retarget their own deletions invalidate, each conditioned on the helper surviving the deletion pass.
- `20260908-134807-plan-review-r3.md` — "[NIT:decision] Whose doc comment survives is unstated where the two differ materially".
  Verified: batch 1's scope said webster's body moves and "every doc comment moves with it", which silently drops burler-only rationale.
  Batch 1's `## Batch Scope` now states the outcome per function, and calls for a **merge** on `resolveStandaloneTarget` (burler's Windows-absoluteness note, its relative-profile-path half of R4-26, its fix-phase-writes half of R6-7) and on `ResolveStandalone` (burler's two-asymmetry paragraph in full, including the empty fourth `Reconcile` argument note).

## Pushed Back

_None._
