# Plan fix report — round 1

Source review: `_mill/reviews/20260728-092358-plan-review-r1.md` (verdict REQUEST_CHANGES, 2 blocking, 0 NIT).

## Fixed

- `20260728-092358-plan-review-r1.md` — "[BLOCKING] Card 19 Context omits gitrepo.go despite naming StageAndCommit/StageAllAndCommit": verified and accurate. Card 19 mandates mixed-backend parity cases asserting that `StageAndCommit` and `StageAllAndCommit` return the SHA the CLI commit actually created, but both methods are defined in `internal/gitrepo/gitrepo.go`, which appeared in neither the card's `Context:` nor its `Edits:`. Because both methods stay CLI-bound throughout, no earlier batch covers their contract either, so the implementer would have had to assert on a return semantics it could not read. Added `internal/gitrepo/gitrepo.go` to Card 19's `Context:`.
- `20260728-092358-plan-review-r1.md` — "[BLOCKING] Card 23 Context omits websterengine/integration.go, the file its own text names": verified and accurate. The card's `Requirements:` enumerate the consumers as `websterengine/{gitwrap,integration,runlevel}.go` while the `Context:` allowlist listed only `gitwrap.go` and `runlevel.go`, making the named file uninspectable. Added `internal/websterengine/integration.go` to Card 23's `Context:`. Also strengthened the card's `Requirements:` to say why that file deserves the closest reading: it is the integration bisect, the one consumer driving `CurrentBranch` → `CheckoutDetached` → `RestoreBranch` as a single flow — the flow that motivated keeping the checkout pair on the CLI, and whose first call is the one method batch 3 writes from scratch rather than lifts.

## Pushed Back

None. Both findings were factually accurate, and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope. Both were `Context:`-completeness gaps in the same class — a card naming a file in prose without granting the implementer read access to it.
