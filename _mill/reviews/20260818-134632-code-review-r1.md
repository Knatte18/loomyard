MILL_REVIEW_BEGIN
# Review: scoutengine told-geometry (optional uniformity pass) — holistic

```yaml
duration_s: 103.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-18
```

## Findings

No findings.

Verified end-to-end:

- `DaemonStateFile`/`DaemonLock` (`internal/scoutengine/daemonstate.go`) are told a bare `anchorRoot string`, no `lyxcwd` import, doc comments rewritten per card 1's exact wording, matching `websterengine.Geometry`'s "caller's obligation" phrasing convention.
- `Options.AnchorRoot` (`internal/scoutengine/refs.go`) replaces `Layout *lyxcwd.Location`; `acquireConnection`/`ensureServer`/`ensureSupervised` (`ensureserver.go`) thread `anchorRoot string` through in the same positions, no `lyxcwd` import remains, no validation added (per the accepted-misuse-mode decision).
- `doc.go` rewrites "layout" wording in the EnsureServer-seam and daemon-state sections only, correctly leaves the toolchain-cache paragraph's `internal/lyxcwd` mention untouched.
- All seven test files in `internal/scoutengine` (untagged and `//go:build scout`-tagged) are converted with value-preserving swaps (`l.AnchorPath()` → `worktreeRoot` string), no assertion/expected-value changes, no `lyxcwd` imports remaining, and `refs_integration_test.go`'s `lyxcwdFile` path-string literal and `lyxcwd.Resolve`-naming prose are correctly left intact per card 6/10.
- `internal/scoutcli/cli.go`: `resolveLocation` deleted; `lookupContext(cwd, dir) (Registry, string, error)` derives the anchor root correctly on both the hub branch (`loc.AnchorPath()`) and the out-of-hub branch (`filepath.Abs(dir)`, falling back to `filepath.Clean(dir)`), matching the pre-refactor synthesis exactly. `buildOptions` threads `AnchorRoot`. `lyxcwd` import retained (still used for `Resolve`/`CwdFrom`).
- `internal/scoutcli/cli_test.go` reshaped per card 8: `TestResolveLocation_*` deleted, `TestLookupContext_OutsideHubReturnsAbsoluteAnchorRootAndBuiltinRegistry` covers both the explicit-dir and cwd-defaulted cases, `TestBuildOptions_ThreadsEveryFieldFromItsArguments` uses a plain string literal.
- `cmd/lyx/constructoranchoring_test.go` and `notransients_test.go` (card 9): scout rows call `scoutengine.DaemonStateFile(l.AnchorPath(), "go")` / `DaemonLock(l.AnchorPath(), "go")`, matching the `websterengine.PromptsDir(l.AnchorPath())` sibling-row shape; expected values unchanged; `lyxcwd` import retained in both files as required.
- `internal/scoutcli/testmain_test.go` (card 11) is a faithful copy of `internal/perchcli/testmain_test.go` with only the package clause and header prose changed.
- `internal/scoutcli/cli_integration_test.go` (card 12) carries `//go:build integration`, exercises both the unanchored hub (mismatched `dir` argument, anchor-root-only assertion) and the subpath-anchored hub (`h.Location.AnchorPath()` as `cwd`, never `h.PrimeWorktree()`; seeded `servers.yaml` entry satisfies `validateEntry`'s four required keys and is read back from the registry) — matches card 12's requirements and `hubforge.NewHub`/`hubforge.SeedConfig`/`scoutengine.LoadRegistry`/`validateEntry` signatures exactly.
- Cross-batch contract: batch 2's `lookupContext(cwd, dir string) (scoutengine.Registry, string, error)` signature matches what batch 1 produced.
- Constraint checks: no `internal/lyxcwd` added to `scoutengine`'s seam-enforcement banned list; no new CLI flags/Short/Long changes; `CONSTRAINTS.md` untouched; the Hermetic Git Test Environment Invariant is satisfied without editing `cmd/lyx/hermeticenv_test.go`'s allowlist, since `scoutcli`'s own untagged `testmain_test.go` supplies the `HermeticGitEnv` presence token package-wide; the Test Tier Purity Invariant holds (`cli_integration_test.go`'s `hubforge.NewHub` sits behind `//go:build integration`, `testmain_test.go` carries no banned token).
- No out-of-plan files, no duplicated helpers, no cross-batch signature mismatches found.

## Verdict

APPROVE
Full told-geometry conversion matches the plan's Shared Decisions and every card's requirements with no deviations found.
MILL_REVIEW_END
