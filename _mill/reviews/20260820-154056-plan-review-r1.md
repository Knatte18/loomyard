MILL_REVIEW_BEGIN
# Review: shedadapters: Burler-round producer — holistic

```yaml
duration_s: 465.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [BLOCKING:scope] Card 8 Context omits verdict.go despite naming its constants
**Location:** batch 3 / card 8 (`03-burler-producer.md`). **Issue:** Card 8's Requirements state `Call` returns `shedengine.Stuck` "for both `burlerengine.VerdictApproved` and `burlerengine.VerdictBlocking`", but its Context list (`ctx.go`, `archive.go`, `focus.go`, `singlellm.go`, `perch.go`, `shedengine/producer.go`, `burlerengine/engine.go`, `burlerengine/profile.go`, `shuttleengine/engine.go`) never names `internal/burlerengine/verdict.go`, which is where both constants are declared. Card 10, reusing the same two identifiers for its outcome-test cases, correctly lists `internal/burlerengine/verdict.go` in its own Context — confirming the omission on card 8 is a real gap, not an intentional one. **Fix:** Add `internal/burlerengine/verdict.go` to card 8's Context.

### [NIT:consistency] Card 1's "four-line" block description undercounts the code it replaces
**Location:** batch 1 / card 1 (`01-cluster-exclude.md`). **Issue:** Requirements describes the existing `if p.ClusterFan != ""` block in `(*Profile).validate` as "four-line", but the actual block in `profile.go` (comment line, `if`, the resolve call, the nested error check, the assignment, and the closing brace) spans eight lines. **Fix:** Drop the line-count adjective or correct it — the surrounding identifiers (`ResolveFan`, `p.clusterLenses = lenses`) already pin the edit location unambiguously, so this is cosmetic only.

## Verdict

REQUEST_CHANGES
Card 8's Context omits internal/burlerengine/verdict.go despite naming its Verdict constants; otherwise the plan is unusually thorough and internally consistent.
MILL_REVIEW_END
