# Fixer report: plan-fix r3

Source review: `20260919-101651-plan-review-r3.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 2 NIT).

## Fixed

- r3 holistic — `[BLOCKING:scope] Card 9 omits server.go despite calling ServerName` — verified accurate: `func ServerName(hubPath string) string` is declared at `internal/reedengine/server.go:55`, and card 9's `Requirements:` calls it while its `Context:` listed only `overlay.go` and `lock.go`. Added `internal/reedengine/server.go` to card 9's `Context:`, taking the add-to-Context remedy rather than the citation-marker escape — the implementer is writing a real call to this function, not citing it in prose, so the allowlist should let them read its signature.
- r3 holistic — `[NIT:consistency] Cards 5 and 7 list an unused Context file` — accurate for card 7, inaccurate for card 5 (see Pushed Back). Card 7's `planReapCycle` is pure Go with no spawn-site dependency, so `internal/reedcli/spawnwatchdog.go` was indeed dead weight there. Replaced it with `_mill/discussion.md`, which is a genuine read for this card: the three counter rules the function encodes are argued in that file's `three-consecutive-cycles-before-a-reap`, `counter-resets-after-a-reap` and `the-hub-itself-is-probed-before-the-reap-pass` decisions and exist nowhere in the code yet. Card 7's `Requirements:` now says why the entry is there, so the next round can see the justification without re-deriving it.
- r3 holistic — `[NIT:scope] Card 8 drops the discussion's "other stat error" predicate case` — verified accurate against the discussion's Testing section, which calls for a stat error that is neither missing nor a non-directory. Card 8 now specifies that case: a target inside a `t.TempDir()` subdirectory chmod-ed to `0o000` so its stat is denied, asserting both `worktreeRootGone` and `hubIsLiveDir` return false, with `t.Cleanup` restoring the mode. Guarded with `t.Skip` on Windows (directory mode bits do not deny traversal there) and when running as uid 0 (mode bits unenforced), because in both cases the stat succeeds and the assertion would pass for the wrong reason — a false pass being worse than the missing case the finding reported.

## Pushed Back

- r3 holistic — `[NIT:consistency] Cards 5 and 7 list an unused Context file`, **card 5 half only** — factually wrong on card 5. The finding's premise is that "neither card's `Requirements:` references anything from it". Card 5's `Requirements:` does: it instructs the implementer to write a doc comment naming the consequence a hard `--shell` pre-flight would have, and that consequence is stated as a claim about `ensureWatchdogSpawned` — "is best-effort and its child's stderr is discarded" — which is declared in `internal/reedcli/spawnwatchdog.go` and nowhere else. Under this plan's own allowlist rule, dropping the entry would leave the implementer asserting that behaviour in a committed doc comment with no permission to read the code it describes. Kept, with the reason now stated inline in card 5's `Requirements:`. The card-7 half of the same finding was accepted and fixed above.

  Empirically checked rather than argued: removing the entry from card 5 and re-running `_plan_validate.run` returns zero errors, so this is not a validator dependency either way — it is the allowlist-correctness judgment above, which the validator does not model.
