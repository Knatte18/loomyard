# Fixer report: plan-fix r1

Source review: `20260919-095916-plan-review-r1.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 2 BLOCKING + 2 NIT).

## Fixed

- r1 holistic — `[BLOCKING:scope] Card 13 undercounts runWatchdogLoop's call sites in the tagged file` — verified accurate against `grep -n "runWatchdogLoop(ctx"`, which returns four hits (lines 168, 366, 465, 527). Card 13 now states four, names each enclosing test function, notes they share the `loopDone <- ...` goroutine wrapper, and states that missing one fails the batch's own `go vet -tags integration` half. Card 14's gate text now reads five total call sites and calls an off-by-one grep count the failure it exists to catch.
- r1 holistic — `[BLOCKING:design] Cards 17–19 never require the -run-matching test-name prefix Card 16 uses` — verified accurate; only card 16 stated the naming requirement. Cards 17, 18 and 19 each now require the `TestWatchdogReap` prefix on their top-level test functions, with the consequence spelled out (a name outside the filter compiles and then silently never runs). The rule is also stated once batch-wide in `## Batch Scope` so it survives a card being read alone.
- r1 holistic — `[NIT:consistency] Batch 5's own text contradicts its stated independence from batch 4` — verified accurate; `depends-on: [3]` with Batch Tests asserting batch 4 had already run. Batch Tests now justifies itself from batch 3 alone and states explicitly that nothing here assumes batch 4 has run.
- r1 holistic — `[NIT:consistency] Card 21's stated reason for dropping the design-doc link is false` — verified accurate; nine `## Done` entries carry a `See [designs/...]` line, including the shipped header/Selvage/watchdog entry linking `reed-header-selvage.md`, the same doc card 20 extends. Card 21 now keeps the link on the moved entry and cites that sibling as the precedent.

## Pushed Back

_None. All four findings verified accurate against the repository, and no fix triggered a harm-check escape._
