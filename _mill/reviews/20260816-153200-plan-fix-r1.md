# Plan fix — round 1

Source review: `_mill/reviews/20260816-152707-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260816-152707-plan-review-r1.md` — "[NIT:consistency] shed.md kept alive past its Documentation Lifecycle expiry" (demoted from BLOCKING).
  Adopted the finding's option (b).
  Batch 4 card 11's correction (1) now requires the shed.md banner to state and *justify* the retention against `docs/overview.md`'s two-class taxonomy — naming the four still-unbuilt design docs and the one durable reference doc written against it, and the anchor-bearing links Markdown Link Integrity enforces — instead of silently rewording the expiring rationale.
  Card 11 gained the six cited docs in its `Context:`, card 13's roadmap edit now points at that banner rather than restating a second rationale that could drift, and the overview's "the doc set lands in this task" Shared Decision records the retention decision and why it is stated rather than assumed.
  Option (a) (deleting shed.md) was not taken: it contradicts `_mill/discussion.md`'s "Doc set" Decision, which pins five in-place corrections to that file, and it would break the anchor-bearing links from `loom.md`, `hardener.md`, `finalize.md`, `raddle.md`, and `docs/reference/status-schema.md` — all modules that are not built.
- `20260816-152707-plan-review-r1.md` — "[NIT:consistency] Batch 4's enforcement-test coverage claim overstates the fabric-vocabulary walk's reach".
  Verified against `CONSTRAINTS.md`'s Fabric Vocabulary Invariant: the markdown half of that walk covers `internal/**/*.md` and `stencils/**/*.md` only, never `manifest/` or `docs/`.
  Batch 4's `## Batch Tests` section now states each of the three walks' reach over this batch's files separately — the markdown-link walk is the only one reading all three edited `.md` files, while the fabric-vocabulary and geometry-literal walks reach nothing here but the new `doc.go`.

## Pushed Back

_None._
