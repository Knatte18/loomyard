# Discussion fix report — round 5

Source review: `_mill/reviews/20260824-134029-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- r5 "Archive-then-respawn dirt window unstated" — added a bullet to `commit-produced-artifacts` accepting the window explicitly (two tracked-file deletions plus two untracked siblings between a bounce round's archive and its next `Done`), noting it closes on the next `Done` and persists only in an already-operator-attention state.
- r5 "Step 2/Step 3 interview framing left implicit" — extended the Scope stencil bullet to state that Step 1, Step 2's pre-ask rule, Step 3's mode-neutral interview framing, and Step 5's section shapes stay verbatim, with all mode-specific wording confined to the `{{.mode_rules}}` marker.

## Pushed Back

None.
