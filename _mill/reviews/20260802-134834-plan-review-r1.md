MILL_REVIEW_BEGIN
# Review: webster: stop re-rendering already-inherited context into fork prompts — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-4.5 (self-reported; harness-pinned reviewer_model is sonnetxhigh)
reviewed_file: plan/
date: 2026-08-02
```

## Findings

### [BLOCKING] recoverbatch.go's own doc comments still claim "the SAME fork prompt"
**Location:** Batch 2 / Card 3
**Issue:** Card 3's Requirements only fix the `RenderForkPrompt` call site (near line 216) and doc.go's "cold recovery" section, but `recoverbatch.go`'s own top-of-file package comment (lines 1–13) and `recoverSpawn`'s function doc (~lines 172–173) both assert the recovery strand "render[s] the SAME fork prompt a fork would have gotten via `RenderForkPrompt`" — this becomes false once the call is `RenderRecoveryPrompt` into a deliberately distinct, fuller template. `doc.go`'s separate "Plan consumption" sentence (line 17, naming `RenderForkPrompt` as a reader of plan-level sections) also goes stale, since `plan` becomes an unused parameter of the thinned `RenderForkPrompt`.
**Fix:** Add a Requirements bullet for Card 3 that updates `recoverbatch.go`'s package-level comment and `recoverSpawn`'s doc comment (name `RenderRecoveryPrompt`, drop "SAME fork prompt"), and updates `doc.go` line 17's claim, so no file this task touches contradicts the fork-context-hygiene decision it exists to implement.

### [NIT] fork-prefix.md's cited source range includes the marker it must exclude
**Location:** Batch 2 / Card 2
**Issue:** `fork-prefix.md` is specified as "derived from today's `fork-template.md` lines 20–27", but line 24 of that exact range is `{{.pattern_directive}}` — the same bullet says fork-prefix.md "MUST NOT contain any of the markers ... `{{.pattern_directive}}`". The citation and the prohibition contradict each other.
**Fix:** Narrow the cited range to exclude line 24 (or note "except the `{{.pattern_directive}}` line, dropped").

### [NIT] implementer-body.md's cited source range omits required sections
**Location:** Batch 2 / Card 2
**Issue:** `implementer-body.md` is said to derive "from today's `fork-template.md` body (lines 53–82)", but the same bullet also requires the FRESH-READ rule and the `## Prior-batch context` section, which live at lines 43–51 — outside the cited range.
**Fix:** Widen the citation to "lines 43–82" so the source range matches the content the bullet actually requires.

## Verdict

REQUEST_CHANGES
Card 3's doc-comment sweep is incomplete (recoverbatch.go/doc.go left stale); two line-range citations in Card 2 are self-inconsistent.
MILL_REVIEW_END
