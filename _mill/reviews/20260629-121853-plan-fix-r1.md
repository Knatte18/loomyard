# Plan-fix report — round 1

Source review: `_mill/reviews/20260629-121630-plan-review-r1.md`

## Fixed

- `20260629-121630-plan-review-r1.md` [BLOCKING] "board cards 1 & 3 mishandle external `board_test` files" — verified accurate: `board_test.go`/`store_test.go`/`task_test.go`/`layer_test.go`/`render_test.go`/`config_test.go` (card 1) and `cli_test.go`/`help_test.go` (card 3) are external `package board_test`; only `template_test.go`, `skipenv_internal_test.go`, `cli.go` are internal `package board`. Rewrote card 1 and card 3 Requirements to preserve the external/internal distinction (external test files → `boardengine_test`/`boardcli_test`; internal files → `boardengine`/`boardcli`). Also hardened warp card 10 (the external `warp_test.go` → `package warpcli_test`; the internal-origin reset-swap test → `package warpcli`) to prevent the same class of error.
- `20260629-121630-plan-review-r1.md` [NIT] "card 20 reconcile_test package vs. qualifier mismatch" — verified accurate: card 20 declared `package configcli` but called `configcli.RunCLI` (illegal self-qualify). Rewrote card 20 to make the test an internal `package configcli` file (matching `configcli_test.go` and `update_test.go`) calling the seam unqualified as `RunCLI(...)`.

## Pushed Back

_(none — both findings fixed)_
