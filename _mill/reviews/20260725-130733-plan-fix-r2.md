# Plan Fix — round 2

Source review: `_mill/reviews/20260725-130733-plan-review-r2.md` (verdict APPROVE, 1 NIT).

## Fixed

- `20260725-130733-plan-review-r2.md` — "[NIT] go-git ignores GIT_CONFIG_GLOBAL for commit identity":
  `lyxtest.HermeticGitEnv()` supplies its neutral `user.name`/`user.email` only via
  `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_NOSYSTEM`, which go-git v5 does not honor (it
  resolves global config from `$HOME`/XDG), so the card's "read them via go-git
  config" option would either find no identity or leak the operator's real
  `~/.gitconfig` (non-hermetic). Reworded `03-write-surface.md` Card 8's identity
  note to drop the config-read option and require the poc to pass an explicit
  `object.Signature` (fixed test identity mirroring the hermetic neutral config:
  name `Test` / email `test@test.com`) on `Worktree.Commit`.

## Pushed Back

_(none)_
