# Discussion fix — round 2

Source review: `_mill/reviews/20260725-052552-discussion-review-r2.md` (APPROVE, 2 NOTEs).

## Fixed

- r2 "[NOTE] doc.go Push-surface still asserts \"no add -A\"" — discussion's
  wildcard-method decision now requires reconciling **both** `doc.go` assertion
  sites (Scope-boundaries *and* Push-surface) with `StageAllAndCommit`; the
  Documentation Lifecycle bullet in Constraints and the Scope-in doc bullet were
  updated to match.
- r2 "[NOTE] sync.go file-level doc describes removed hasUnpushed path" — added
  `sync.go`'s own file/package comment to the Documentation-Lifecycle doc-update
  list (Scope-in + Constraints), so it is rewritten from "pushes all unpushed
  commits, looping until nothing is left" to the unconditional-push-per-iteration
  behavior the migration introduces.

## Pushed Back

_None — both NOTEs were factually accurate doc-consistency findings; fixed under
the receiving-review default._
