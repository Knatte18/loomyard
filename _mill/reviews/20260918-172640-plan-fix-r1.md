# Plan fix — round 1

Source review: `20260918-172203-plan-review-r1.md`

## Fixed

- `[BLOCKING:scope]` "Card 6's WriteConfig signature change leaves a third call site unaddressed" — verified against `internal/vscode/config_test.go`, which calls `WriteConfig` positionally at three sites (lines 21, 128, 169);
  card 6 named only the first two by test name.
  Card 6's Requirements now name `TestWriteVSCodeConfigRegistersInGitignore`, instruct its call site be moved to the new signature with its own gitignore assertions unchanged, and state that all three must move together or the package stops compiling and batch 2's `verify:` fails.
- `[NIT:scope]` "Card 2's engine-level no-mutation test only spells out the alive-no-op branch" — verified: the card said "no-op branches" plural while giving concrete assertions only for `ifAbsentNoOpAlive`.
  Card 2's Requirements now specify one case per branch: the alive-candidate no-op keeps the four existing assertions, and the hidden-only no-op repeats them plus an unchanged-strand-count assertion and a non-empty `GUID`/`Name` on the returned hidden strand.

## Pushed Back

_(none — both findings were accurate and neither fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task.)_
