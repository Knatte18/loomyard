MILL_REVIEW_BEGIN
# Review: Surface merge-in-progress in fabric status — holistic

```yaml
duration_s: 107.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-31
```

## Findings

No findings. Verified against every card's Requirements:

- `internal/fabriccli/status_mergeinprogress_integration_test.go`: `//go:build integration` line 1, correct package, both required test functions present with exact hub-setup sequences (`hubforge.NewHub`/`SeedFabricConfig` for the clean case; `setupConflictingDivergenceCLI`/`branchAtCurrentHEADCLI`/`merge-in` exit 1 for the parked case), comma-ok presence checks used correctly (not bare type assertions), no `t.Parallel`, no new `TestMain`, no error-path scenario added — matches `error-path-intentionally-untested`.
- `internal/fabriccli/weft_verbs.go`: `statusCmd.RunE` calls `fab.Status()` first, then `fab.MergeInProgress()`, byte-for-byte matching error-handling shape, unconditional `merge_in_progress` key added to the existing `output.Ok` map alongside `changes`, no `okWithRecord` routing (confirmed via `envelope.go`'s header naming `status` a read-only verb), no new imports. `Use`/`Args`/`Short` untouched; `Long` gains the required this-pair-qualified second paragraph.
- `internal/fabricengine/doc.go`: the targeted sentence (`cannot re-run merge-in to reprint the list...`) is intact verbatim; the required new clause about `merge_in_progress` is appended immediately after it without weakening the surrounding `merge-stage` argument; the two untouched claims (junction-drift, "one side-labelled view") remain unedited.
- `docs/overview.md`: only the `### Status` section bullet (line 210) is extended in place with the this-pair-qualified clause, appended to the existing sentence, single physical line preserved (not reflowed); the separate `## Modules` fabric bullet (line 290) is correctly left untouched, matching the plan's narrower scope.
- `tools/sandbox/SANDBOX-FABRIC-SUITE.md`: F3's `Watch` gains the `merge_in_progress: false` note (Goal line untouched); F18's `Watch` gains the this-pair-qualified sentence about `merge_in_progress` reporting, correctly positioned two lines below the refusal list it references, one-sentence-per-line convention followed.
- `manifest/roadmap.md`: Planned section emptied but heading/lead-in preserved; Done entry inserted first, matching the two-line shape of the existing `fabric: two-sided reset-to-SHA verb` entry, pointing at the `internal/fabricengine` package doc.
- No out-of-plan files present; the 17-file manifest matches exactly what each card's Context/Edits/Creates lists require.
- Cross-batch/cross-card contract holds: card 2's engine call (`fab.MergeInProgress() (bool, error)`) matches `mergelifecycle.go`'s actual signature and semantics (this-pair-only, no foreign-state consultation), and `envelope-unchanged`/`engine-is-untouched` decisions are honored — no `fabricengine` file changed except `doc.go` prose.
- `TestRunCLI_ReadOnlyVerbsOmitMutationsKey` (cli_test.go:894-929) is unmodified and still exercises `status` with no `mutations`/`partial` keys expected.

## Verdict

APPROVE
Implementation matches the plan exactly across both cards; no constraint violations, no out-of-plan files, no cross-batch contract issues.
MILL_REVIEW_END
