# Discussion fix — round 5

Source review: `_mill/reviews/20260821-142704-discussion-review-r5.md` (APPROVE, 0 blocking, 4 NIT).

Operator directive for this round: proceed to Handoff regardless of R5's outcome.
All four NITs were verified accurate and fixed before Handoff;
the convergence gate was not applied, by that directive.

## Fixed

- r5 — "Cancellation test calls neither New nor Run": corrected the counts to seven `Run`-driving tests and exactly ten `New` sites, and gave `TestCancellation_RealProducersReturnErrorNotStuck` an explicit disposition — it **stays** in `internal/loomshed` (its subject is loomshed's own five constructors, the same criterion keeping `batchifier_test.go`/`planvalidate_test.go` in place), with `internal/loomshed` keeping a reduced path-bag fixture for it since `Deps` is gone.
- r5 — "countingProducer must be one instance across both New calls": stated that a stateful fake is substituted as the **same instance** at every `New` in a test, naming `resume_test.go:114`/`:131`/`:142`.
- r5 — "Comment-sweep token set misses two live stale sites": named `internal/loomshed/doc.go:1-2` and `internal/preflightshed/preflight_test.go:33`, and widened the sweep tokens to include bare `internal/loomshed/` path mentions and the moved test-file names.
- r5 — "\"Three constructors reach disk\" is true but not of loom's rows": rewrote the Technical-context paragraph — the three disk-touching constructors (`Bouncer`, `BurlerRound`, `SingleLLM`) are exactly the three engines loom does not use, so loom's own `Build` touches disk only via `landingshed`'s pair opener, which is not a behaviour change and not a tier-1 obstacle.

## Pushed Back

(none — all four findings verified accurate against the cited code before fixing)

## Note carried forward from round 4

Round 4's fixture-helper finding listed `writeBatcherConfig` among `buildSequenceFixture`'s dependencies.
It is not one — the fixture deliberately writes no `batcher.yaml` so `batcher.Active` resolves the embedded template.
The discussion records the accurate four-helper list rather than the reviewer's five.
