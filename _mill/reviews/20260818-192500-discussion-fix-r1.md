# Discussion fix — round 1

Source review: `_mill/reviews/20260818-191411-discussion-review-r1.md` (APPROVE, 0 blocking, 5 NIT — 2 demoted from BLOCKING).

## Fixed

- `20260818-191411-discussion-review-r1.md` — "[NIT:consistency] Vocabulary walk does not reach the edited .md files": verified `enforcement_test.go:940` walks `.md` under `{internal, contracts/stencils}` only. Technical context and Testing now state the guard covers the `doc.go` edits (via the `{internal, cmd}` `.go` walk at line 907) and that Fabric vocabulary in `CONSTRAINTS.md`/`docs/overview.md`/`manifest/roadmap.md` is a review obligation.
- `20260818-191411-discussion-review-r1.md` — "[NIT:scope] \"Named exactly\" package sets have no membership predicate": added the membership predicate (takes absolute paths from its caller **and** imports no `internal/lyxcwd` in production; machine-enforced when its own package's import-set test asserts that non-import). Both sets are now explicitly non-exhaustive, and `internal/batcher`, `internal/stencilstore`, `internal/shedadapters` are named as bound-but-unconverted. Verified all three satisfy the predicate.
- `20260818-191411-discussion-review-r1.md` — "[NIT:design] \"Two sole constructors\" is broader than the pair supports": point 3 is now scoped to engine `Geometry` structs, with plain told values named as the other permitted shape (`treadleengine`'s `runDir`/`Profile.GateDir`, `shedengine`'s `StatusPath`/`LockPath`/`StatusLockPath`), and the `standalonegeom.StencilsDir` asymmetry recorded.
- `20260818-191411-discussion-review-r1.md` — "[NIT:consistency] \"Only real validation\" contradicts the sub-points beside it": sub-point 1 now reads "the only validation `Resolve` makes of the **repository** itself", with an added sentence placing `ErrStaleAnchorMarker` and `ErrCwdOutsideAnchor` as anchor/position checks rather than repository checks.
- `20260818-191411-discussion-review-r1.md` — "[NIT:scope] docslink allowlist's stale-entry failure mode unmentioned": verified `docsLinkAllowlist` at `docslink_test.go:394-397` and its self-expiring semantics. Technical context now records both entries, the instruction to leave the `#package-naming` link in `docs/overview.md` untouched, and the reverse hazard for `#hub-geometry-invariants`.

## Pushed Back

None. All five findings verified accurate against the cited source lines;
none of the fixes breaks existing content, conflicts with a documented decision, or destabilizes work outside this task.
