# Fixer report: plan-fix round 5

Source review: `_mill/reviews/20260912-120659-plan-review-r5.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 1 NIT demoted-from-BLOCKING).

## Fixed

- r5 BLOCKING:scope — "`configtemplate.go` never listed in any card's Context/Edits".
  Accurate.
  `ConfigTemplate()` is declared in `internal/loomengine/configtemplate.go`, a separate file from `config.go` holding the `//go:embed` of `template.yaml` and its accessor;
  only `config.go` and the calling test files were listed anywhere in the plan.
  Card 4 references `ConfigTemplate()` three times (the `writeLoomConfigWithKey` switch and the well-formed-default case) and card 9 reasons about `fastDeadlineLoomConfig` inheriting the template through it, so both cards were asking the implementer to reason about a file `Context:` never let it read.
  Added `internal/loomengine/configtemplate.go` to both cards' `Context:`.

- r5 NIT:consistency (demoted from BLOCKING) — "Moving Tier 1 to Done leaves stale sibling cross-references in `roadmap.md`".
  Accurate, and fixed under the default rule — the demotion governs whether the finding can hold the loop open, never whether it gets fixed.
  Confirmed against the current Planned section: the `lyx loom step` entry says "Independent of the two self-report items below", which is false once only Tier 2 remains below it, and the Tier 2 entry's "Independent of Tier 1 and of the `lyx loom step` item above" would point at an item no longer in that section.
  Card 9 now requires updating both siblings in the same edit, and names the repo's own precedent as the phrasing model: the Someday `webster: worktree-per-card parallel execution` entry already refers to "The now-Done `Adopt quarry's glyph alphabet as the plan alphabet` item" in exactly this shape.

## Pushed Back

_(none — both findings verified accurate and were fixed)_
