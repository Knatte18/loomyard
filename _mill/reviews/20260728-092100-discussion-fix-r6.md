# Discussion fix — round 6

Source review: `_mill/reviews/20260728-091742-discussion-review-r6.md` (verdict APPROVE, reviewer `sonnetmax`).

## Fixed

- r6 "[NOTE] checkout omitted from Short/Long re-read list" — added `lyx fabric checkout` to the CLI/Cobra Invariant's list of commands whose `Short`/`Long` must be re-read, noting it is the third `WireJunctions` caller (`checkout.go:152`) and that its existing help already speaks of "junctions" in the plural, so this is a re-read-and-confirm rather than a rewording.
- r6 "[NOTE] removeHostJunction's generalised loop error policy is unstated" — pinned the loop as **best-effort, continuing past a per-junction error**, explicitly contrasted with `unseedLyxJunction`'s abort-on-first-error rule so the two are not written by analogy. Rationale recorded: `remove.go:91` discards the return value (`_ = removeHostJunction(...)`) exactly as the adjacent `removePortal`/`removeLaunchers` calls do, so aborting early would silently leave later junctions in place. Added a matching test case.

## Pushed Back

None. Both findings were verified accurate against the cited code and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope.
