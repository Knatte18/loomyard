# Plan fix — round 1

Source review: `_mill/reviews/20260715-073436-plan-review-r1.md` (verdict APPROVE, 4 NITs).

## Fixed

- [r1] "[NIT] Card 3 Context omits mouse.go it calls mouseOption from" — Added `internal/muxengine/mouse.go` to Card 3's `Context:`. Confirmed validator-safe: `_plan_validate`'s `non-existent-path` check suppresses refs that are `Creates:` targets elsewhere in the plan (mouse.go is created in Card 1); full validator re-run returns zero errors.
- [r1] "[NIT] Card 2 references mouseOption without listing mouse.go" — Added `internal/muxengine/mouse.go` to Card 2's `Context:` for the same reason; validator-safe.
- [r1] "[NIT] Card 5 cites a non-existent contract-test option-read precedent" — Accurate; `contract_integration_test.go` sets `remain-on-exit` but never reads an option back. Reworded Card 5 to model only the general `PsmuxCmd`/output read style (list-sessions/display-message) and to state explicitly there is no `show-options` assertion in the contract test to mirror.
- [r1] "[NIT] Integration test (Card 5) runs in no automated verify gate" — Reviewer marked "None required"; the deliberate gating is documented in the Batch Tests section and the `integration-test-gating` Shared Decision, both of which already record the `go test -tags integration ./internal/muxengine/` command, so the integration lane is already discoverable. No plan edit needed (pointing the frontmatter `verify:` at a tmux-requiring integration test would wrongly force it into the fast batch gate).

## Pushed Back

_None._
