# Plan fixer report — round 1

Source review: `_mill/reviews/20260708-061614-plan-review-r1.md` (APPROVE, 3 NITs).

## Fixed

- r1 NIT "Cards 4 & 5 omit intra-package prior-card files from Context" — added
  `internal/burlerengine/profile.go` + `template.go` to card 4's Context and
  `profile.go` + `verdict.go` + `prompt.go` to card 5's Context (all are Creates: targets
  of earlier cards, tolerated by the validator's creates-union rule).
- r1 NIT "Burler JSON envelope key casing diverges from shuttlecli" — card 7 now
  prescribes camelCase envelope keys (`reviewPath`, `fixerReportPath`, `sessionId`,
  `strandGuid`, `lastAssistantMessage`), matching shuttlecli's Ok-envelope convention.
- r1 NIT "clihelp.Abort pattern is imprecise for RunE errors" — card 7 now states RunE
  errors go through `output.Err` + `clihelp.SetExit(ctx, ...)` (verified to exist at
  `internal/clihelp/exec.go:68`); `clihelp.Abort` stays reserved for the
  PersistentPreRunE failures.

## Pushed Back

(none)
