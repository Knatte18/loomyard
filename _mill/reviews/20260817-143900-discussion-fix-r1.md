# Discussion fix — round 1

Source review: `_mill/reviews/20260817-143428-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 3 NIT — 2 demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r1 — "[NIT:consistency] `AnchorRoot`/`anchorRoot` collides with the root-vs-cwd rule" (demoted from BLOCKING).
  Verified against `CONSTRAINTS.md:12-14`, `manifest/designs/fabric-unified-view.md:61-65`, and the same decomposition doc's T4 spelling at line 330-331 — all confirm the finding.
  Added a new Decision, "Naming — `AnchorPath`, not `AnchorRoot`; `WorktreeRoot` keeps `Root`", pinning `AnchorPath`/`anchorPath` for the anchor-derived value and keeping `WorktreeRoot` (which genuinely is the worktree root).
  Renamed all six occurrences across Scope, the `Geometry` table, the shuttle-signature decision and the Q&A log.
  Recorded that this overrides the T3 brief's own `anchorRoot` spelling at `producers-standalone.md:291-292`, with the rationale that `CONSTRAINTS.md` outranks a task brief and T4 already uses `anchorPath`.
  Widened the Constraints section's Cwd Resolution Invariant entry to cite the naming half, not just the `os.Getwd` half.

- r1 — "[NIT:scope] Blast-radius enumeration covered `reedengine.New`, not `NewRunner`" (demoted from BLOCKING).
  Verified: `internal/shuttlecli/cli_test.go:237`, `internal/websterengine/recoverbatch_test.go:189` and `internal/webstercli/verbs_test.go:227` all call `shuttleengine.NewRunner` and were absent from the Scope list; four further `NewRunner` lines sat adjacent to already-listed `reedengine.New` lines and were likewise unnamed.
  Re-ran the enumeration symbol-by-symbol over all four changed exported symbols and added a **call-site table** to Technical context covering `NewRunner`, `FindRun`, `New` and `HubLogsDir`, split production vs test.
  Rewrote the Scope bullet to list all eleven affected test files explicitly.
  Restated the `websterengine` boundary as **production**-files-only ("two production files"), and named `recoverbatch_test.go` as a signature fixup rather than a conversion.
  Confirmed `runlevel_test.go:215` is a comment, not a call site, and excluded it.
  The Verify command needed no change — every affected package was already listed.

- r1 — "[NIT:consistency] Design-doc row 64 goes stale under the no-edit rule".
  Verified: `manifest/designs/producers-standalone.md:64` attributes `HubLogsDir` to `internal/reedengine/lifecycle.go`, false once the function moves to `fabricengine`.
  Took the finding's first offered fix: added row 64 to the permitted design-doc edit set, alongside the two T6/T7 pointers.
  The permitted set is now stated as exactly three edits, in both the Scope bullet and the design-doc Decision.

## Pushed Back

(none)
