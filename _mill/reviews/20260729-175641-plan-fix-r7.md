# Plan fix report — round 7

Source review: `_mill/reviews/20260729-175313-plan-review-r7.md` (reviewer: Fable 5, xhigh — verdict REQUEST_CHANGES, 1 BLOCKING + 4 NIT). All five verified accurate against the actual source; all fixed per `mill-receiving-review`'s fix-everything default; edits confined to `_mill/plan/`. Zero pushbacks.

## Fixed

- [BLOCKING] "Stale ensureSupervised godoc sentences unnamed" — verified against `internal/codeintelengine/ensureserver.go` lines 208-210, 218-222: all three cited sentences exist verbatim ("no V1 registry entry requests it yet", "The daemon this function spawns is never killed by its caller" + "gopls's own -listen.timeout, left unconfigured here, defaulting per gopls itself"). Named the `-listen.timeout` sentence in card 10, the "never killed by its caller" sentence in card 11, and the "no V1 registry entry requests it yet" sentence in card 12 (which already named only the "Known limitation" paragraph).
- [NIT] "KillPID error handling unspecified in escalation" — verified: card 2's own `killpid_test.go` spec asserts `KillPID` returns a non-nil error for a dead/nonexistent PID, so card 11's unqualified `proc.KillPID(state.PID)` call would propagate that error if the daemon died between the failed re-dial and the kill. Updated card 11 to make the call best-effort — ignore/log a dead-PID error and continue to the respawn.
- [NIT] "No documentSymbolProvider capability gate on InFile branch" — verified: `lspclient.go`'s `capabilities` struct has only `WorkspaceSymbolProvider`, and the workspace/symbol branch gates on `supportsWorkspaceSymbol()` before calling, but card 4's InFile branch called `documentSymbol` unconditionally. Added a `DocumentSymbolProvider` capability field + `supportsDocumentSymbol()` method to card 3, gated card 4's InFile branch on it (returns `ErrResolverUnsupported`, mirroring the workspace/symbol precedent in `symbol_test.go`'s `ErrResolverUnsupportedSentinel` test), and added matching capability-parse coverage to card 5 and an `ErrResolverUnsupportedSentinel` resolve-branch case to card 6.
- [NIT] "Fallback-test stub mechanism is Linux-only; lock dir not created" — verified against `toolchain.go`'s `resolveGoToolchain` (platform-specific `binName`, `gopls.exe` on Windows) and `lock.go`'s `AcquireWriteLock` (no directory creation of its own; the existing `supervised_test.go` precedent relies on `writeDaemonState`'s incidental `MkdirAll` to create the lock's parent dir first, which card 12's fallback test does not do). Updated card 12's native-fallback test to write the stub under the platform-correct `binName` and to `os.MkdirAll` the lock's parent directory before pre-holding it.
- [NIT] "Context omissions in cards 13/14" — verified: both cards' Requirements use `builtins()` (`registry.go`) and card 14 uses `ErrAmbiguousSymbolSentinel` (`errors.go`), neither file listed in either card's Context. Added `internal/codeintelengine/registry.go` to both cards' Context and `internal/codeintelengine/errors.go` to card 14's Context.

## Pushed Back

- None.
