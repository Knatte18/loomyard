# Fixer report: plan-fix round 5 — the standalone CLI path

```yaml
round: 5
review: 20260818-141248-plan-review-r5.md
fixed: 1
pushed_back: 0
converged: false
```

## Fixed

- `20260818-141248-plan-review-r5.md` — "[NIT:consistency] \"byte-identical, four deviations\" decision omits webstercli's envelope" (demoted from BLOCKING by the stage ceiling): scoped deviation (3) in the overview's hub-byte-identity Shared Decision to `lyx burler run` and `lyx perch run` only, and named both exclusions explicitly.
  Verified accurate — deviation (3) claimed the three fields appear in "both modes' run-verb success envelopes" with no CLI scoping, under a decision that applies to all batches, while batch 3 declares itself a narrow mode-trigger repoint that changes no verb and no envelope.
  The claimed-exhaustive list was therefore false for `lyx webster run`, defeating its own stated purpose.

  The reviewer offered two fixes.
  Took the scoping fix rather than adding a card that gives `webstercli` the three fields, because the second option contradicts a documented design decision: the discussion states that T7 shipped webster's standalone entry without these fields, that this is "a gap in T7, not a precedent to copy", and that it is "out of scope to fix here".
  Under the receiving-review decision tree that is a HARM CHECK (b) — conflicts with a documented design decision — for the *second* remedy only, so the finding itself is fixed rather than pushed back; only the reviewer's suggested second remedy is declined, with the reason cited.

  Also recorded the `lyx perch pause` exclusion in the same paragraph.
  Card 22 already carried that reasoning locally, but the Shared Decision's list claims exhaustiveness, so an exclusion documented only in a batch file left the same hole one verb over.

## Pushed Back

_None._
The finding was fixed; one of the reviewer's two suggested remedies was declined with the design-doc citation above, which is not the same as pushing back on the finding.

## Convergence

Round 5's verdict is `APPROVE` with `blocking_count: 0`, but the round's single finding carries `demoted: true`, so `converged = false` and the terminal actions stay withheld.
`round` is 5 and `max_review_rounds` is 7, so the loop continues to round 6.

**Structural note for the remaining rounds.**
This stage's `blocking_classes` is `["design", "scope"]`, so a `consistency`-class finding is always ceiling-demoted, and the convergence gate refuses to converge while any demoted finding exists in the round.
Rounds 4 and 5 were both `APPROVE` + one demoted `consistency` NIT.
Convergence therefore requires a round that returns either zero findings or only `design`/`scope` findings that are themselves resolved — not merely another `APPROVE`.
If round 7 still carries a demoted finding, the gate's implicit-approve-at-cap path fires and the shortfall is recorded in that round's commit message.
