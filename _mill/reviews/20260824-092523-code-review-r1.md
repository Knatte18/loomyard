MILL_REVIEW_BEGIN
# Review: loom: redesign the Discussion format — holistic

```yaml
duration_s: 260.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-24
```

## Findings

No findings.

Verified against the plan: all five `## All Files Touched` targets present and correctly scoped
(`manifest/designs/loom-format-discussion.md` created; `manifest/designs/loom.md`,
`manifest/designs/plan-card-format.md`, `manifest/designs/review-finding-classification.md`,
`manifest/roadmap.md` edited), no out-of-plan files in `manifest/designs/`.

Confirmed the pinned heading `### Discussion-Review rubric — what to also flag (relocation and
exclusion)` is reproduced character-for-character in `loom.md:112` and correctly derives the
anchor `discussion-review-rubric--what-to-also-flag-relocation-and-exclusion` per
`docsLinkSlug`'s em-dash-deletion/double-hyphen rule (`internal/lyxcwd/docslink_test.go`); every
inbound link using that anchor (`loom-format-discussion.md:29`, `roadmap.md:41`,
`review-finding-classification.md:57`) resolves against it.

Verified both reworded supersession sentences read grammatically as two-item lists
(`plan-card-format.md:3`'s "Card fields ... and `loom-template-plan.md` — neither is rewritten
yet", `roadmap.md:14`'s "Card fields and `loom-template-plan.md`"), neither still claims outright
supersession of the discussion stencil, and `loom-format-discussion.md`'s own scoped Step 2/Step 3
supersession claim is present and accurate against the actual stencil
(`contracts/stencils/loom/loom-template-discussion.md`'s Step 2/Step 3 headings and `Architecture`
category checked directly).

Verified the roadmap move: **loom: redesign the Discussion format** appears once as a `## Done`
entry (`roadmap.md:169-170`, literal `1.` marker, first entry), is gone from `## Planned`, and both
positional cross-references in the Wave 2 `Discussion-Write producer` item are reworded to bold
item names (`roadmap.md:23`) per the file's own Maintenance rule. Wave 1/Wave 2 headings and the
remaining Wave 1 item are structurally untouched.

Verified `loom.md`'s producer-table row (line 35) carries the required single-line trailing
pointer, and the new "what to also flag" subsection (117-124) reproduces Fix 2's three parts in
the required order with the same pointer-rule framing as the sibling "what not to flag" subsection
above it; that subsection is untouched.

Verified `review-finding-classification.md` item 5's added sentence sits on its own indented
continuation line (line 57), item 1's pre-existing anchor link is unchanged, and the `Status:
Someday` header is unchanged.

Cross-checked `loom-format-discussion.md` against its card 1 requirements section-by-section (H1,
status blockquote, "what is not changing," Fix 1, Fix 2, Lifecycle, "Open, not decided here," in
that order); all links resolve, the stencil is referenced only as backtick prose (never linked),
and no self-referential "this task" phrasing appears, matching the house style of
`plan-card-format.md`.

One stylistic observation, not raised as a finding: the new pointer sentence appended at
`roadmap.md:41` shares a physical line with the retained first sentence rather than getting its
own line, which reads as a deviation from the "one sentence per line" Shared Decision — but every
other pre-existing roadmap item (lines 40, 42, 43) already packs multiple sentences onto one
physical continuation line, so matching that file's own established convention here is the correct
call per this review's own convention-over-strictness guidance, and `loom.md`'s and
`review-finding-classification.md`'s edits in the same batch do use one-sentence-per-line where
that is this file's own local convention. No fix needed.

## Verdict

APPROVE
Plan alignment, cross-batch link contracts, and shared decisions all verified directly against source; no issues found.
MILL_REVIEW_END
