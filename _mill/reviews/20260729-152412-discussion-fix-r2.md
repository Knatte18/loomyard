# Discussion fix report — round 2

Source review: `_mill/reviews/20260729-152307-discussion-review-r2.md` (verdict APPROVE, 2 NOTEs). Both NOTEs applied per `mill-receiving-review`'s fix-everything default (both factually accurate against the source; neither fix causes harm — pure design-doc clarifications).

## Fixed

- [NOTE] "--in-file × batch-mode interaction unspecified" (r2 review) — added a **Batch-mode composition** clause to `item4-flag-shape`: `--in-file` composes with batch mode (not rejected pre-flight); each positional name is resolved independently via `documentSymbol` within the same `<path>` under the existing batch per-entry status/exit-code contract. Pinned the natural reading so plan time is unambiguous, and noted the `buildOptions` refactor is the single site that carries the in-file query form into each batch entry.
- [NOTE] "Query third-form field unnamed" (r2 review) — named the third `Query` form in both `item4-documentsymbol-mechanism` (a pointer field `InFile *InFileQuery{File, Name}`, selected by `resolvePosition` before its `Pos`/`Symbol` branches) and the `refs.go` Technical-context bullet, making the CLI→engine handoff and the new `resolvePosition` branch discriminant explicit.

## Pushed Back

- None.
