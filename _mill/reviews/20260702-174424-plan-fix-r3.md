# Plan fix — round 3

Source review: `_mill/reviews/20260702-174308-plan-review-r3.md` (verdict **APPROVE**, 0
BLOCKING + 3 NIT). Fixer: mill-plan. All three NITs passed the `mill-receiving-review`
decision tree (factually accurate, fix causes no harm) → FIX (NITs are not optional). No
push-backs. DAG + `_plan_validate` re-run clean (0 errors). Overview `approved` flipped to
`true`; this is the terminal plan-fix round.

## Fixed

- NIT "Card 9 Context omits checksum.go" (batch 3) — added
  `internal/muxengine/render/checksum.go` to card 9's Context so the implementer can read
  `layoutChecksum` for the `rules_test.go` assertion `checksum prefix == layoutChecksum(body)`.
- NIT "Card 21 leaves baseDir for LoadConfig unspecified" (batch 6) — named the value in the
  PreRunE requirement: `baseDir = layout.Cwd` (the `_lyx/config/` root is anchored at
  `layout.Cwd`; `configengine.FindBaseDir` walks up from there), mirroring the explicit
  base-dir naming in the weftcli pattern — no cold-start exploration needed.
- NIT "Card 29 comment misstates registration order" (batch 7) — corrected the `wantSubs`
  annotation: helptree is a set/superset `strings.Contains` check, so the listed order need
  not match the `AddCommand` order in `internal/muxcli/cli.go` (the previous "matching the
  Command() registration order" note was inaccurate but harmless).

## Pushed Back

(none)
