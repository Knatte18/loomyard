# Fixer report: plan-fix round 2 — self-report-tier2

Source review: `20260912-112552-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 2 NIT).

## Fixed

- `20260912-112552-plan-review-r2.md` — "[BLOCKING:design] Composer-level friction.Directive errors hard-fail the run".
  Verified accurate: cards 14, 17, and 21 each instructed wrapping a `friction.Directive` error in the composer's existing house style, mirroring `pattern.Directive`'s propagating handling at `internal/loomengine/plan.go:71-74` and `internal/burlerengine/engine.go:104-107`.
  That error returns out of a Spec/Result constructor, which `shedengine.Run`'s `callErr != nil` branch treats as a full task failure — so with Tier 2 defaulting to on, one transient stencil read would kill a task over optional bookkeeping, contradicting the plan's own "every Tier 2 failure is a `Warn`" decision.
  Fixed at both levels.
  A new Shared Decision, "a composer swallows a `friction.Directive` error, never propagates it", states the rule once and records why `friction.Directive` and the `pattern.Directive` call on the adjacent line are deliberately **not** symmetrical: PATTERN's directive carries binding constraints an agent must not silently lose, while a friction note is optional by construction.
  Cards 14, 17, and 21 each now require the composer to `logger.Warn` the error naming the role and stencil and continue with an empty directive, and each explicitly forbids the wrapping it previously prescribed while stating that the neighbouring `pattern.Directive` handling is left untouched.
  `internal/logger/logger.go` was added to all three cards' `Context:` as the new call site requires it, and card 21 additionally records that `internal/websterengine/render.go` does not import `internal/logger` today (the package uses it in `strand.go`, `integration.go`, and `runlevel.go`) so the import must be added.

- `20260912-112552-plan-review-r2.md` — "[NIT:scope] `shuttleengine.OutcomeAsking` runtime-failure branch is untested".
  Verified accurate: card 11 names four runtime-failure outcomes, card 12's test bullet listed three.
  Fixed by adding `shuttleengine.OutcomeAsking` to card 12's runtime-failure bullet, so all four documented branches are asserted.

- `20260912-112552-plan-review-r2.md` — "[NIT:scope] `internal/logger/logger.go` not in card 1's Context despite a direct `logger.Warn` call".
  Verified accurate: card 1's requirements name `logger.Warn` twice while its `Context:` omitted the declaring file, inconsistent with cards 11, 23, and 25.
  Fixed by adding `internal/logger/logger.go` to card 1's `Context:`.

## Pushed Back

_None._
All three findings were factually accurate, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task.
