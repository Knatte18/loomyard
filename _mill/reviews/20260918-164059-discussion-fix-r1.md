# Discussion fix — round 1

Source review: `_mill/reviews/20260918-164049-discussion-review-r1.md` (orchestrator, demoted BLOCKING:consistency → NIT by the `blocking_classes: [design]` ceiling).

## Fixed

- `20260918-164049-discussion-review-r1.md` — "config-reconcile gotcha contradicts actual reconcile behavior".
  Verified against the code: `internal/configsync/configsync.go:122` calls `yamlengine.Reconcile`, which diffs leaf key-paths both ways and returns `added` **and** `removed` (`internal/yamlengine/reconcile.go:21`), and `TestReconcileAll_DropsStaleReedClaudeKey` pins the identical stale-reed-key case.
  The original claim (additive-only, "do not write removal logic") was factually wrong.
  Rewrote the gotcha in the `config-block-replaces-header-with-status_line-and-selvage` decision to state that reconcile removes the stale `header.*` leaves, that it fires only on `lyx config reconcile --apply` (`internal/configcli/configcli.go:274`) and `fabric clone` (`internal/fabriccli/clone.go:100`), that no reed verb reconciles, and that the plan must therefore handle both the reconciled and un-reconciled config states.
  Added the reconcile-removal test case to Testing as its own `internal/configsync` entry, and logged the correction in the Q&A log.

## Pushed Back

None.
