# Plan fix report — round 3

Source review: `_mill/reviews/20260731-194409-plan-review-r3.md` (verdict REQUEST_CHANGES, blocking_count 1, nit_count 3)

## Fixed

- "Card 44 cites refs.go internals not in its Context/Edits" — added `internal/scoutengine/refs.go` to batch 13's Card 44 `Context:`, since its Requirements cites `teardownConnection`'s `connKindSupervised` branch to justify why no separate teardown call site exists.
- "Adoption-scope criterion applied inconsistently in batch 10" — batch 10's Card 37 now adds `run.go:183` (the near-identical first `moveStaleArtifacts` call, previously omitted) alongside `run.go:426-429`, and explicitly distinguishes both from the excluded `run.go:447-448`: `moveStaleArtifacts`'s own wrap (via `moveStaleIfExists`, `state.go:204,213`) carries `name`/`path` context but no `round`/`attempt` field, unlike 447-448's `"round %d attempt run: %w"` wrap — so a reader of the bare error knows which file failed but not which round/attempt, which is why these two still qualify.
- "Inconsistent rigor on an identical bare-cmd.Start() pattern" — batch 11's Card 38 now states plainly that `internal/fabricengine/spawn.go:62`'s bare `return cmd.Start()` qualifies (zero identifying context — less wrapped than batch 9's `internal/reedengine/lifecycle.go:357-359`, which Card 34 already treats as an automatic qualifier despite carrying more context) and requires the `logger.Warn`, replacing the earlier hedged "confirm... add a call only if it qualifies" wording.
- "identity.go's TerminalOutcome has no branch to attach a Warn to" — batch 12's Card 39 now notes explicitly that `internal/perchengine/identity.go:100-103`'s single unconditional return has no existing `if err != nil` branch, and that one must be introduced before the log call, not inserted into existing branching.

## Pushed Back

(none)
</content>
