# Discussion fix — round 4

Source review: `_mill/reviews/20260817-145341-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 4 NIT — one demoted from BLOCKING).

## Fixed

- r4 NIT `[consistency]` (demoted from BLOCKING) "Shared-body shape contradicts two later decisions" — the real defect was mine: `shared-body-refactor` claimed `MissingKeys`, `envsource.Build` and `yamlengine.Resolve` are "shared verbatim" while `no-missingkeys-on-the-fallback-path` and `fallback-error-messages-name-the-template` require diverging at exactly those steps. Replaced the claim with a three-point concrete shape (two flag-consultation points, one shared fallback tail that skips `MissingKeys` and wraps as `%s config template: %w`, an unchanged on-disk path) and narrowed the anti-drift rationale to what genuinely stays shared: the on-disk path and the env-marker resolution semantics. Left an explicit supersession note so the old reading cannot be implemented.
- r4 NIT `[scope]` "Doc-surface table claims completeness it does not have" — added rows for `docs/shared-libs/configengine.md:87` (stale `board`/`worktree`/`weft` wrapper naming, same class the task fixes in `config.go`'s header) and `:136` ("five-step flow" against the six-step Resolution model above it). Verified both against the file. Table is now seven rows and states that the Q&A log defers to it.
- r4 NIT `[scope]` "Markdown Link Integrity absent from Constraints" — added the invariant to Constraints, noting that `configengine.md` contains zero inline links today so any link the doc pass adds is newly exposed, and that a `CONSTRAINTS.md` link must resolve in both its file and `#anchor` halves. Confirmed the zero-link count by grep.
- r4 NIT `[consistency]` "Membership-rule attribution overstates the design doc" — reworded both the decision's rationale and its Q&A entry: the design gave *both* rationales (`producers-standalone.md:271` governs-what, `:272` standalone-entry), they agree on Webster and disagree on `loomengine`, and only the standalone-entry one survives. Verified the two lines' wording directly.

## Pushed Back

None. All four findings verified accurate against source — the first against this discussion's own text — and none met a Decision Tree harm category.
