# Plan fix report — round 2

Source review: `_mill/reviews/20260623-111715-plan-review-r2.md` (verdict REQUEST_CHANGES, 3 BLOCKING + 3 NIT; one BLOCKING and one NIT were reviewer-withdrawn/informational).

## Fixed

- `20260623-111715-plan-review-r2.md` — "[BLOCKING] e2e sync test cannot use the injected commit cleanly": Card 14(e) now explicitly `t.Setenv("WEFT_SKIP_GIT", "")` + `t.Setenv("WEFT_SKIP_PUSH", "")` (so `envSyncOptions()` does not no-op the commit) and states the test must not `t.Parallel` (incompatible with `t.Chdir`).
- `20260623-111715-plan-review-r2.md` — "[BLOCKING] editOne sync failure message swallows the actual error": changed `syncFunc` to `func(w io.Writer) int`; `editOne` now captures sync output in a `bytes.Buffer`, discards it on success (stream stays clean) and surfaces it in the failure message on non-zero (diagnosable). Threaded the writer through `RunCLI`'s real sync and Card 14's fake sync.
- `20260623-111715-plan-review-r2.md` — "[NIT] Abort message inaccurate after fresh scaffold": Card 8 `Edit` now tracks whether it scaffolded the file this call and, on abort, `os.Remove`s a just-scaffolded file so the filesystem returns to its pre-call state; the abort message reads "unchanged" truthfully. Card 9 test asserts the file is absent after abort-on-missing and left in place after abort-on-pre-existing.
- `20260623-111715-plan-review-r2.md` — "[NIT] weft.ConfigTemplate parse round-trip claim is loose": Card 3 dropped the `config.Load(...)` round-trip phrasing; the testable assertion (uncomment + `yaml.Unmarshal` → `_lyx`) lives in Card 4(b).

## Pushed Back

- `20260623-111715-plan-review-r2.md` — "[BLOCKING] Batch DAG verify names a non-existent path": the reviewer self-withdrew this finding ("Fix: None needed — withdraw"). It is also factually inaccurate — `internal/worktree` contains untagged test files (e.g. `config_test.go`, `list_test.go`), so `go test ./internal/worktree/` compiles and runs untagged tests, and the new untagged `template_test.go` runs there. No change needed.
- `20260623-111715-plan-review-r2.md` — "[NIT] Batch 1 verify runs vacuously for worktree integration files": the reviewer marked this "Fix: None required; informational." It is not a defect — Batch 1's relocation guard lives in the untagged `template_test.go`/`init_test.go`, which the verify command runs. No change needed.
