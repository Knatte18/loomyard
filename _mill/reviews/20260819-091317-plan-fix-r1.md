# Plan fix — round 1

Source review: `_mill/reviews/20260819-090700-plan-review-r1.md` (holistic, opus/high, REQUEST_CHANGES, 6 BLOCKING + 2 NIT).

## Fixed

- r1 "Weft ownership kind refuses the prime pair" — verified against `destroy.go:255-264` (`ownedRegisteredLinkedWorktree` excludes the main worktree) and `clone.go:274` (`cloneRepo(opts.WeftURL, weftPath)` makes the weft primary a main worktree).
  Added Shared Decision "the weft abort reset needs its own any-worktree ownership kind";
  card 6 now builds `ownedWeftCheckout` as a new closed-set member (constructor, `resolvePathOwnership` arm, side-accurate refusal message) over a renamed side-free `isAnyWorktreeOf` predicate, and its test requires the prime pair explicitly, not an `AddPair`-only fixture.
- r1 "Cleanup protection mechanism does not exist as described" — verified against `cleanup.go:150-153` (live pairs `continue` before entry construction) and `CleanupBranchEntry`'s four fields (no message-carrying field; `Error` is deletion-failure-only).
  Card 13 no longer edits `cleanup.go` and states why no guard is needed;
  card 14 now pins the existing skip behaviour (dry-run and apply, with and without force) instead of a protection entry that could not exist. `cleanup.go` dropped from All Files Touched.
- r1 "Foreign-state disposition contradicts the discussion, undeclared" — confirmed `discussion.md`'s `lifecycle-quartet-on-both-verbs` and its Testing section disagree.
  Added Shared Decision "foreign-state disposition": the four *mutating* verbs refuse with `*ErrForeignMergeState`, `MergeInProgress` reports false without erroring, `*ErrNoMergeInProgress` is reserved for neither-record-nor-foreign-state.
  Card 8's `MergeContinue`/`MergeAbort`/`MergeInProgress` and card 10's foreign-state test updated to match.
- r1 "No disposition for a genuine MergeStart error mid-attempt" — accurate; card 1's classification admits a non-conflict error and neither card covered it.
  Added Shared Decision "a genuine MergeStart error mid-attempt self-aborts, symmetrically" (reset to captured start SHAs, delete the record, return the wrapped error; a failing reset retains the record and returns the reset error), and pinned it in card 8 step 8 and card 11 step 8.
- r1 "Context lists omit files whose identifiers the Requirements name" — added `internal/gitkit/gitkit.go` (card 4), `internal/gitrepo/merge.go` + `internal/gitkit/gitkit.go` + `internal/fabricengine/clone.go` (card 6), `internal/gitrepo/pull.go` + `internal/gitrepo/ancestry.go` (card 7), `internal/lyxcwd/lyxcwd.go` + `internal/fabricengine/pull.go` + `internal/gitrepo/ancestry.go` (card 8).
- r1 "addMergeVerbs signature elided; fab captures nil" — verified `fab` is an `addWeftVerbs` local assigned inside `PersistentPreRunE` (`weft_verbs.go:41,52`).
  Card 16 now pins `func addMergeVerbs(cmd *cobra.Command, fabric func() *fabricengine.Fabric)`, states the by-value capture failure mode explicitly, and switches every dispatch to `fabric()`;
  the registration call site and the file-header comment update are named.
- r1 "[NIT] Shared Decision 1 overrides Shared Decision 6" — scoped Decision 1's tie-breaker to "except where a later Shared Decision explicitly names the clause it supersedes", and listed both supersessions (remote-only weft counterpart, foreign-state disposition).
- r1 "[NIT] User-facing error text names Go identifiers, not CLI verbs" — the strings are discussion-pinned and asserted byte-exactly by batch 5, so they are not reworded;
  card 3 now documents the mismatch and where it is closed, and card 16 requires both verbs' `Long` help to spell out the identifier-to-verb mapping.

## Pushed Back

(none)

## Validation

- `_plan_dag.validate`: 6 batches, DAG OK.
- `_plan_validate.run`: 0 findings (after adding `internal/fabricengine/destroy_test.go` to All Files Touched).
