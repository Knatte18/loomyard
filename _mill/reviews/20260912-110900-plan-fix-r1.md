# Plan fix — round 1

Source review: `20260912-110736-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 0 NITs).

## Fixed

- `[BLOCKING:scope]` "Batch 2 card 5 cites `testEnv(t)` in the wrong file, and its real home is missing from Context" — verified accurate: `grep -rn "func testEnv" internal/loomrecipe/` returns only `shape_test.go:66`, and `fixture_test.go` declares `testLandingDeps`, not `testEnv`.
  Added `internal/loomrecipe/shape_test.go` to card 5's `Context:`, corrected the Requirements citation to name `shape_test.go` as `testEnv`'s home, and recorded why `fixture_test.go` stays listed alongside it — `testEnv` fills its Landing seam via `testLandingDeps`, which is declared there.
  Also extended the do-not-redeclare list from `loomRowEngines`/`testEnv` to include `testLandingDeps`, since the same collision hazard applies to it.

## Pushed Back

_None._
