# Plan fix report — round 4

Source review: `_mill/reviews/20260729-082234-plan-review-r4.md` (verdict: REQUEST_CHANGES, 1 BLOCKING, 3 NIT).

## Fixed

- `_mill/reviews/20260729-082234-plan-review-r4.md` — "[BLOCKING] Batch 2 Cards 5/6 are in forward-reference order": confirmed Card 5 (`CloneHub` rewrite) called `ensureBoardWorktree`, explicitly "from Card 6's new `boardweft.go`," while Card 6 (which creates it) was sequenced after. Fixed by swapping the two cards' content — `boardweft.go`'s creation is now Card 5, `CloneHub`'s rewrite (which consumes it) is now Card 6 — and updating the one cross-reference from "Card 6's new boardweft.go" to "Card 5's new boardweft.go, already created."
- `_mill/reviews/20260729-082234-plan-review-r4.md` — "[NIT] Batch 3: render.go signature change and board.go's call-site fix are split across three cards": confirmed Card 12 changes `RenderToDisk`/`Render`'s signatures but board.go's call site isn't fixed until Card 14, with unrelated Card 13 landing in between — a genuine multi-commit compile gap within one Go package (`internal/boardengine`), the same class of issue as the BLOCKING finding above, not merely cosmetic. Fixed by having Card 12 itself apply an interim single-store fix to board.go's `RenderToDisk` call site (`nil` for the not-yet-existing `notes` argument), keeping the package compiling continuously; Card 14 still replaces that line with its final dual-store form when it lands, and now explicitly says so. While fixing this, also noticed and fixed a related gap on the same card: Card 14's Context omitted `render.go` even though its Requirements describe calling `RenderToDisk` by its new signature — added.
- `_mill/reviews/20260729-082234-plan-review-r4.md` — "[NIT] Batch 6 Scope contradicts Card 32 on fabric-unified-view.md": confirmed the Scope text's blanket "NOT edited by this batch" claim contradicted Card 32's own line-94 sweep of that same passage. Fixed by narrowing the Scope sentence to say only the passage's sequencing/dependency CLAIM stays unedited (it already reads correctly), not the dead design-doc-name reference on the same line that Card 32's sweep corrects.
- `_mill/reviews/20260729-082234-plan-review-r4.md` — "[NIT] Card 33 misses a second stale wiki reference in SANDBOX-FABRIC-SUITE.md": confirmed Pre-conditions item 4's "...alongside initializing the weft wiki" clause is a second dangling reference beyond the ones Card 33 already fixed. Fixed by adding this line to Card 33's Requirements.

## Pushed Back

(none — all four findings were factually accurate and fixed as recommended)
