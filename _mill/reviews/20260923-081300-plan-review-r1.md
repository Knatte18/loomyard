MILL_REVIEW_BEGIN
# Review: llm-driven child can park forever on Claude Code's own workspace-trust dialog — holistic

```yaml
duration_s: 404.2
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-09-23
```

## Findings

### [NIT:scope] Card 2's `*Run` build recipe omits the `attached` field
**Location:** batch 1, card 2 **Issue:** the recipe list ("runner, spec ..., runDir, state ..., and clock") for building each test `*Run` literal never names the `attached` field, but the "an attached run whose `state.Started` is true" test case (verified against `internal/shuttleengine/run.go`'s `Run.attached` field and `AwaitStarted`'s `run.attached && run.state.Started` short-circuit, per the card's own shape) requires setting it. **Fix:** add `attached` to the recipe list, or note it explicitly beside that one bullet.

## Verdict

APPROVE
Both batches are internally consistent, source-verified against every listed file, and the tripwire/doc-comment edits check out mechanically.
MILL_REVIEW_END
