MILL_REVIEW_BEGIN
# Review: loom: session bootstrap — holistic

```yaml
duration_s: 247.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-20
```

## Findings

None. Cross-checked every card across all seven batches against the delivered source:

- Batch 1 (`fabricengine/origin.go`, `commitweftpaths.go` + tests): `Origin`, the three path
  accessors, `ReadOrigin`/`WriteOrigin`, `CommitWeftPaths`, and `weftCommitPathspec` all match
  cards 1-3's exact requirements (body order, lock idiom, recorder discipline, doc comments).
  Card 4's unit tests cover all required cases (JSON round-trip, both-anchor path assertions,
  pathspec table, SkipGit/empty-relPaths early returns with empty-recorder assertions).
- Batch 2 (`add.go`, `launchers.go`, `launcher_content.go` + tests): the origin-record
  write-and-commit step lands at the documented position in `Add` (after junction wiring, before
  the warp push), `rollbackAdd`'s doc comment states the no-new-removal-step rationale, the run
  launcher is added identically to `ide`/`fabric-checkout` via `writeLauncherScriptIfChanged`, and
  `removeLaunchers`'s hardcoded slice gains the `run`+ext entry. `origin_integration_test.go`
  covers all of card 8's sub-cases (a)-(h) including the mutation-record entry counts and the
  SkipGit no-commit guard; the two containment integration tests gained the `run.sh`/`run`+ext
  canary entries. The prior round's non-blocking item (stale `launcher_content.go` file-header
  omitting the run launcher) is resolved — the header now lists `ide, fabric-checkout, ide-menu,
  run`. `manifest/designs/loom.md`'s run-launcher paragraph was rewritten per card 10, with no
  absolute-path-embedding claim left standing.
- Batch 3 (`loomengine/config.go`, `loomshed/seed.go` + guards): the two new accessors
  (`LoomDriverLog`, `LoomBootstrapLock`) are ephemeral-tree-anchored via the lifted `loomDirName`
  constant, `LoomStatusFile`/`LoomStatusLock`/`LoomRunLock` stay byte-identical per the refactor
  requirement, `ErrSeedExists` wraps the existing refusal text via `%w`, and both `cmd/lyx` guard
  tables (`notransients_test.go`, `constructoranchoring_test.go`) gained rows for both new
  accessors.
- Batch 4 (`loomcli` skeleton, `wire`, `drive`/`status`/`pause`): the receiver, the
  `runnerMasterStarter` duplication, `resolvePersistentPreRun`'s group short-circuit, and `wire`'s
  full dependency assembly all match cards 15-16 field-for-field (including the
  never-the-standalone-stand-in `RefMatcher` and the lazy `OpenBisector` closure). `drive`/
  `status`/`pause` all check `ShouldAbort` first and match the documented pre-flight/refusal
  shapes; `status --watch`'s loop degrades to an unavailable marker without emitting a second
  envelope. `wiring_test.go`/`cli_test.go`/`status_test.go` cover every assertion card 20
  specifies.
- Batch 5 (`seedinput.go`, `bootstrap.go`, `run.go`): `resolveParentBranch`'s five-row table (plus
  the present-but-empty variant) is implemented exactly, `awaitRunLock`'s poll order (lock, then
  liveness, then wait) matches the load-bearing ordering the plan states, and `run.go`'s seven
  steps match card 23 including the self-healing unconditional inclusion of the origin-record path
  in every commit (a real correctness improvement beyond the plan's literal wording, documented
  in-line and covered by the batch-7 smoke test `TestSmokeBootstrap_
  OriginRecordSelfHealsAfterCrashBetweenWriteAndCommit`). `RunAliasCommand` builds a fresh receiver
  and attaches `resolvePersistentPreRun` correctly. The design-doc step diagram and the status spec
  (card 25) were both updated to bind the seed role to the bootstrap verb and correct the
  attribution of the phase-machine rewrite loop to `drive`.
- Batch 6 (registration/guards): `cmd/lyx/main.go` registers `loomcli.Command()` and
  `loomcli.RunAliasCommand()` and extends `root.Long`; `seamsignature_test.go` pins both slices at
  thirteen/twelve with `loomcli` correctly excluded from the RunCLIIn-exception rationale;
  `helptree_test.go` and `sandbox_coverage_test.go` were extended per cards 28-29 (the `run` alias
  is allowlisted, `loom` gets a real S8 scenario in `SANDBOX-CORE-SUITE.md`); `CONSTRAINTS.md` and
  `docs/overview.md` both carry the thirteen/twelve counts, the alias-shape bullet, and the two new
  interactive-handoff exception holders.
- Batch 7 (smoke suite, roadmap): `run.go`'s `go func(){ driveCmd.Wait() }()` reap fix (surfaced by
  the smoke suite's own live-hub runs, per the plan's card 32 narrative) is present. The smoke
  suite covers cases (a)-(i) plus an extra regression case (g2) for the crash-between-write-and-
  commit self-heal gap, all using the real built binary rather than in-process `RunCLI`, matching
  the documented rationale (`os.Executable()` resolution). `manifest/roadmap.md` moved the item to
  Done with the required content and no stale worktree-local-launcher wording.

No out-of-plan files, no duplicated global utilities, no constraint violations, and no
cross-batch contract mismatches were found. The Mutation Record Invariant, Cwd Resolution
Invariant, Told-Geometry Invariant, and CLI/Cobra Invariant are all honored by the delivered code
exactly as the plan's Shared Decisions describe.

## Verdict

APPROVE
Full plan-to-source alignment across all seven batches; no blocking or nit findings.
MILL_REVIEW_END
