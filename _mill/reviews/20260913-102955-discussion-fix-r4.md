# Discussion fix — round 4

Source review: `_mill/reviews/20260913-102859-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 3 NIT — one demoted from BLOCKING by the stage's `blocking_classes` ceiling).

## Fixed

- `20260913-102859-discussion-review-r4.md` — "[NIT:consistency] Spec-doc line 112 rename contradicts normalization" (demoted from BLOCKING): the Scope bullet for `contracts/specs/llm-model-spec.md` replaces the blanket "lines 64, 66, 112 become `v=`" with a per-line disposition — `:64` and `:66` rename to `v=` (bracket spellings), `:65` stays (names the resolved param the provider engine reads), `:112` keeps the word `version` and only drops the `=` (Provider seam side, which never sees `v`). Line 65 was previously uninventoried and is now covered.
- `20260913-102859-discussion-review-r4.md` — "[NIT:consistency] \"`Parse` is untouched\" vs parse.go's grammar doc comments": the `parse.go` Scope bullet now says `Parse`'s *body* is untouched and names the three doc comments that describe the bracket as key=value-only and must be updated — the file header (`:1-4`), `Parse`'s comment (`:36-38`), and `parseBracket`'s comment (`:122-123`).
- `20260913-102859-discussion-review-r4.md` — "[NIT:design] No stated relationship between `bracketKeys` and `knownParams`": the vocabulary Decision now states the invariant (`bracketKeys`' value set is a subset of `knownParams`' key set, reverse containment deliberately not required), puts a sync assertion in scope with the failure mode it prevents, adds a `knownParams` doc-comment pointer, and adds a Rejected entry for leaving the relation implicit. Testing gains a matching "Vocabulary-sync test (new)" entry.

## Pushed Back

None.
