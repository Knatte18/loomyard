# Plan-fix report — round 2

Source review: `20260801-170609-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING + 4 NIT).

## Fixed

- [NIT] "Card 8: PartialPullError return value not pinned per stage" — verified accurate (only step 2's weft-pull failure named `PullResult{}`; post-weft-pull returns were unspecified). Pinned that every post-weft-pull `*PartialPullError` returns the accumulated `result`, not a zero value, mirroring `Fabric.Commit`'s report convention — so `WarpAdvanced`/`NewWarpHEAD`/`AnchorWarpSHA` stay readable on the `reanchor` stage (02-fabric-pull.md, Card 8).
- [NIT] "Card 8: contradictory error-handling pseudocode for the index load" — verified accurate (`path, _ :=` / `ix, _ :=` discarded errors the prose said to surface). Rewrote the snippet to capture and check `err` for both `corrIndexPath` and `loadCorrIndex`, each mapping to `*PartialPullError{Stage: "load-index"}` (02-fabric-pull.md, Card 8).
- [NIT] "Card 7: `WarpFetched` field left undocumented" — verified accurate. Added the parenthetical "(warp fetch ran and succeeded)" so all ten `PullResult` fields are documented (02-fabric-pull.md, Card 7).
- [NIT] "Card 12: fabric.go's own cross-repo-methods sentence stays stale" — verified accurate (`internal/fabricengine/fabric.go`'s package doc names "(`SyncWeft`, `RevertWithWeft`)", omitting pre-existing `Commit` and now `Pull`). Added `internal/fabricengine/fabric.go` to Card 12's Edits with a one-line requirement to bring its cross-repo-methods list in line with `doc.go` (add `Commit` and `Pull`); updated the overview's All Files Touched union accordingly (04-docs-sandbox.md, Card 12; 00-overview.md).

## Pushed Back

_None — every finding was accurate, fixable within the plan files, and harm-free._
