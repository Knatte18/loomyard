# Plan fix report — round 1

Source review: `_mill/reviews/20260729-160007-plan-review-r1.md` (verdict REQUEST_CHANGES, 2 BLOCKING + 4 NIT). Every finding was verified accurate against source and fixed per `mill-receiving-review`'s fix-everything default; all edits are confined to `_mill/plan/` files (the Plan Review guardrail — no source file touched). Zero pushbacks.

## Fixed

- [BLOCKING] "Context omits test-helper files named in Requirements" — added `internal/codeintelengine/supervised_test.go` to batch 1 card 2's `Context:` (it names `spawnAndHoldSubprocess` for the live-child fixture, which `isalive_test.go`'s spawn-and-die pattern does not cover), and added both `internal/codeintelengine/supervised_test.go` and `internal/codeintelengine/daemonstate_test.go` to batch 4 card 11's `Context:` (Requirements names `spawnAndHoldSubprocess`/`spawnAndWaitForDeadPID`). The implementer now has read license for every helper the cards reference.
- [BLOCKING] "Card 11 risks an un-allowlisted spawn in an untagged file" — reworked card 11 so the `reconnectUnderLock` decision helper is **pure** (takes injectable `dial`/`finalize` funcs, performs no `KillPID`/spawn/state I/O — those stay in `ensureSupervised`), making all three race tests (a)/(b)/(c) process-free fake-only cases that live safely untagged in `ensureserver_test.go`. Added an explicit prohibition: no real-process-spawning fixture may go in `ensureserver_test.go` (not on `cmd/lyx/tierpurity_test.go`'s `allowedSpawners`, so it would pass the batch gate but fail `TestTierPurity` on the repo-wide `done_gate`); any real-spawn coverage belongs in the already-allowlisted `supervised_test.go` or the `//go:build integration` suite.
- [NIT] "Card 10 over-claims which existing test needs the rename" — corrected card 10 to state only `TestNativeArgv_IncludesExtendedIdleTimeout` references the constant by identifier and needs the rename; `TestNativeArgv_PreservesBinPathAndExtraArgs` asserts argv ordering only and needs no edit (but must keep passing).
- [NIT] "Stale `nativeDaemonIdleTimeout` prose mention in doc.go not explicitly assigned" — added `internal/codeintelengine/doc.go` to card 10's `Edits:` and made updating the "The EnsureServer seam" prose reference to `nativeDaemonIdleTimeout` an explicit part of the rename commit, so no stale name is left behind.
- [NIT] "Card 11's core helper is unnamed" — named it `reconnectUnderLock` and sketched its signature `func reconnectUnderLock(ctx, network, address string, dial func(...) (*lspClient, error), finalize func(...) error) (client *lspClient, healthy bool, err error)`, alongside the algorithm description (returns `healthy == true` to reuse without killing, `false` to trigger `KillPID`+respawn in the caller).
- [NIT] "Native fallback re-resolves the toolchain" — documented in card 12 that the second `resolveGoToolchain` in the `ensureNative` fallback is an accepted redundancy (cached fast-path `os.Stat`), explicitly rejecting the alternative of threading `binPath` into `ensureNative` (which would change its signature and its `TestEnsureNative_Integration` call site for no real benefit).

## Pushed Back

- None.
