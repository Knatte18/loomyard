# Plan fix — round 3

Source review: `_mill/reviews/20260809-093234-plan-review-r3.md`

## Fixed

- r3 `[BLOCKING:design]` "Old-order guard shadows the pre-existing stale-`.fabric-anchor` migration error" — took the first branch the finding offers: card 3's probe now runs the same `ls-tree` discriminator a second time for `staleFabricAnchorName` when the current anchor name comes back empty, so a legacy hub is admitted as a weft and `clone.go`'s existing stale-marker hard error stays reachable.
  The corollary is resolved by the same change: card 7's ForceBootstrap heuristic now names both marker spellings and states explicitly that the stale-marker test must NOT get `ForceBootstrap: true`, because its assertion is on a message the bootstrap guard never produces.
- r3 `[BLOCKING:scope]` "Card 13's Requirements name `BoardDir` from a file absent from its Context/Edits" — verified `BoardDir` is declared at `internal/fabricengine/junctionnames.go:100` and only mentioned in a doc comment in `reconcile.go`; added that file to card 13's `Context:`.
- r3 `[NIT:consistency]` "board branch tracks its remote from the clone overstates the orphan-bootstrap case" — narrowed card 14's comment instruction: the upstream-from-clone claim is the adopt path only, and the orphan-bootstrapped board genuinely has no upstream and attempts the push every reconcile.
  The instruction now forbids claiming it never happens.
- r3 `[NIT:consistency]` "Card 18 perpetuates a pre-existing roadmap/design-doc contradiction on slice 8" — verified the design doc's slice 8 section is headed shipped and states the CLI-wording question was resolved.
  Card 18 now resolves the contradiction in the roadmap's favour of the design doc rather than preserving it, and says why the design doc survives the task.

## Pushed Back

_(none)_
