# Discussion fix report — round 5

Source review: `_mill/reviews/20260818-055900-discussion-review-r5.md` (verdict APPROVE, 0 blocking, 4 NIT).

Round 5 is the configured round cap (`roles.discussion-review.holistic.rounds: 5`).
The convergence gate's demoted-finding predicate was still unsatisfied at this round, so the round cap closes the loop as an implicit approval — recorded in this round's commit message per the gate's round-cap clause.

## Fixed

- r5 — "Scope bullet still names the rejected seed gate": the highest-value finding of the round. Scope still read "gates `seedStencils` on the tier-1-AND-tier-2 wiring predicate", the exact wording the round-2 gap rejected, and Scope is what a plan writer reads first — acting on it would have reinstated the `_board` seed regression. Reworded to `preflight.HubPresent` with the `Wired`-is-not-the-seed-gate note inline and a pointer to the owning Decision.
- r5 — "`export_test.go` both forbidden and prescribed": two stale references survived round 4's fix. Restated both as reference-only: the seam paragraph now says `loomengine`'s shim is an in-repo precedent not replicated here (with the reason — its integration tests must be external to avoid the `hubforge` cycle), and the Technical-context helper row says the same. Verified by grep that all nine remaining mentions are consistent with "this task adds no `export_test.go`".
- r5 — "preflight listed as shared infrastructure": accepted the substance rather than arguing the `.md` contract. `docs/shared-libs/README.md:7-9` requires a shared lib to carry *no* domain logic, and `internal/preflight` carries orchestrator precondition policy. Narrowed the docs plan: only `buildinfo` and `standalonestate` get shared-libs bullets and a mention in `docs/overview.md:315`; `preflight` gets its `docs/overview.md` tree row and `doc.go` alone. Stated why, so the exclusion is not read as an oversight.
- r5 — "Stale line cite for `looksLikeHub`": corrected `clone.go:641` to `clone.go:645` (641 begins the comment block).

## Pushed Back

None. All four findings verified accurate; none met a Decision Tree escape.
