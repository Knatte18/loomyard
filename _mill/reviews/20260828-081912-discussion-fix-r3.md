# Discussion fix report — round 3

Source review: `_mill/reviews/20260828-081734-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING after the stage ceiling, 3 NIT of which 2 demoted from BLOCKING).

## Fixed

- `20260828-081734-discussion-review-r3.md` — "[NIT:decision] Roadmap watchdog-daemon item has no disposition": verified against `manifest/roadmap.md`, whose **reed: watchdog daemon** item both owns "reconciles session geometry after a live terminal resize" and records a prior discussion's rejection of a one-off `set-hook`+`run-shell` reaction. Added Decision `roadmap-watchdog-item-is-narrowed` (narrow the resize clause, keep the item, with the surviving daemon rationale spelled out), added the roadmap edit to Scope, and rewrote the Constraints bullet that claimed the roadmap must not move so it names the one edit and why it is the planned-item-changes-scope case rather than the bugfix case CLAUDE.md forbids.
- `20260828-081734-discussion-review-r3.md` — "[NIT:consistency] \"Rebuilds the array\" is falsified by the install guards": verified — both `applyLayoutLocked` guards precede the install statement, and `state.go` documents a reachable state where `anyPlacedStrand` is false indefinitely. Weakened the self-healing claim to the paths that reach the install, and stated the disposition for each guard-skip state: `len(live) < 2` is harmless because `resize-pane -y` against a window's only pane is a verified silent no-op (probed live: exit 0, height unchanged), and `!anyPlacedStrand` keeps the stale array deliberately because it still pins live panes in a session reed no longer tracks. Recorded the rejected alternative of clearing ahead of the guards.
- `20260828-081734-discussion-review-r3.md` — "[NIT:scope] Doc-correction target under-specified": verified — `doc.go`'s chained-attach bullet's verbatim-landing claim is true, so there was no falsehood to correct. Replaced the Decision `threshold-attribution-is-corrected-in-writing` with `doc-work-is-additions-at-three-named-sites`, naming the two `doc.go` bullets and the integration-test file comment to extend, what each addition says, and why rewriting the existing bullets was rejected. Updated the matching Scope bullet.

## Pushed Back

_None._
