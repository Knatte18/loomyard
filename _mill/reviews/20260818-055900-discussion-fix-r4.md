# Discussion fix report — round 4

Source review: `_mill/reviews/20260818-055341-discussion-review-r4.md` (verdict APPROVE, 0 blocking, 4 NIT).

## Fixed

- r4 — "standalonestate: in-package tests vs export_test.go": a real self-contradiction. Resolved toward **in-package tests, no shim**, and stated it in all four places that referenced it (the Decision, the test-package Decision's parenthetical, the injectable-seam rationale, and § Testing). Added the reason in-package is available here but was not for `preflight`: `standalonestate` is stdlib-only and needs no `hubforge` fixture, so there is no import cycle to route around. This task now adds no `export_test.go` anywhere.
- r4 — "\"any other Resolve error\" is narrower than stated": verified against `internal/lyxcwd/lyxcwd.go:150-163` — `gitWorktreeRoot` returns the bare sentinel for a `*gitexec.GitError` and `fmt.Errorf("%w: %v", ErrNotAGitRepo, err)` otherwise, so an exec-level failure also satisfies `errors.Is(err, ErrNotAGitRepo)` and the comment at `loomengine/preflight.go:48-50` is already false. Added a Gotchas entry instructing the lift to reword rather than carry it across, and naming the anchor-gate errors as the residual branch's only reachable causes.
- r4 — "`stencilstore.ModeFor` ships with no named test": added an `internal/stencilstore` test row pinning both mappings, added the test to the Scope bullet, and noted in § Testing why the `ModeProduction`-is-`iota`-zero property has no other pin once `stencilseed.go` stops doing the comparison itself.
- r4 — "ldflags repoint has no drift detection": chose the guard rather than the disclaimer. Added a `tools/deploy/main_test.go` drift guard asserting the `-X` string matches `internal/buildinfo`'s symbol path, a Scope bullet for it, a Gotchas sentence explaining that `tools/` lies outside every enforcement walk, and a note that `go test ./...` is what runs the two packages the task's named verify command omits.

## Pushed Back

None. All four findings verified accurate against current source before fixing; none met a Decision Tree escape.
