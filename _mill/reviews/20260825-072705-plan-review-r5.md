MILL_REVIEW_BEGIN
# Review: Fix prowler: Reddit adapter blocked — holistic

```yaml
duration_s: 444.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 5 (model id claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-25
```

## Findings

### [NIT:consistency] maxTopComments doc comment left stale by card 5/9
**Location:** batch 2 card 5 / batch 3 card 9
**Issue:** `reddit.go`'s `maxTopComments` doc comment reads "Used by the Hacker News adapter"; card 5 wires `formatRedditThread` (in `redditoauth.go`) to consume the same constant for both top-level and reply caps, and card 9 explicitly instructs "leave ... `maxTopComments` unchanged," so the stale single-adapter claim ships uncorrected even though every other doc comment this plan's changes falsify (fetcher struct, `fetchOldRedditHTML`, `redditAdapter` type/method, the package comment) is explicitly caught and rewritten.
**Fix:** Add one line to card 5's requirements updating that comment to note both adapters now read it.

### [NIT:design] Token-cache concurrency guard is advisory-only, never in a machine-run verify
**Location:** batch 2 Batch Tests / batch 4 card 11
**Issue:** the plan calls `TestRedditTokenCaching`'s concurrent sub-test "the one that matters most" for the shared, mutex-guarded token cache `runAll`'s goroutines hit concurrently, but the only race-detection instruction anywhere in the plan is "run it under `-race` at least once by hand during implementation" (repeated similarly for batch 3). No batch `verify:` command and no item in card 11's final re-run list ever invokes `-race`, so the one check most likely to catch a real concurrency bug is easy to skip and is never re-verified by the loop that gates completion.
**Fix:** Add a `-race` invocation of the batch 2/3 test filters to card 11's re-run list (or fold `-race` into one batch's `verify:`), rather than leaving it as an implementer aside.

## Verdict

REQUEST_CHANGES
Two NIT findings: a doc comment the plan's own changes falsify, and a concurrency check that is advisory rather than gated.
MILL_REVIEW_END
