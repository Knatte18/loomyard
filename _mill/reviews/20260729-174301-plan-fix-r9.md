# Plan-fix report — round 9 (second/final short-review round)

Source review: `_mill/reviews/20260729-174301-plan-review-r9.md` (reviewer opus · xhigh, one transient-API retry; verdict REQUEST_CHANGES, 2 BLOCKING + 1 NIT). The two BLOCKINGs are regressions from my own R8 fix (I over-corrected by adding hard config-loads to the teardown paths). The NIT is a false positive.

## Fixed

- [BLOCKING] "Remove config-load breaks nested-junction test" — verified: my R8 change made `Remove` hard-load the removed slug's config and return on error, but `TestRemove_TearsDownNestedJunction` sets up a nested (`RelPath == "sub"`) pair via `Add`+`WireJunctions` with no `lyx init`, so no config exists at `<slug>-weft/sub/_lyx/config/` → `Remove` errored before teardown. Fix (card 7): `Remove`'s name-load is now BEST-EFFORT (on load failure set `names = nil` and continue, matching `Remove`'s existing `_ = removeHostJunction` discard posture — a teardown must not hard-fail on a broken/absent config, and no-fallback targets the *wiring* path, not teardown). Fix (card 6): the nested test seeds `fabric.yaml` at the nested weft base before `Remove` so the full nested-teardown happy path is verified (best-effort alone would leak the nested junction the test asserts is gone). Added `template.go` to card 6 Context for `ConfigTemplate()`.
- [BLOCKING] "Undo name-load breaks unpaired-host test" — verified: my R8 change added an unconditional `WiredNames` load before `undo.go:81`'s `UnwireJunctions`, but `TestUndo_NoWeftPairing` runs `Undo` on a bare `git init` (no weft) and asserts `err == nil` per Undo's "no weft-pairing pre-gate; no-op when target absent" contract → the load failed and broke it. Fix (card 11): gate the name-load on `os.Stat(l.WeftWorktree())` — when the weft is absent, `names` stays `nil` and `UnwireJunctions(l, slug, nil)` no-ops (nothing is wired without a weft); when present, its config is present too and the load proceeds.

## Pushed Back

- [NIT] "junction_repoint_test.go absent from All Files Touched" — PUSH BACK: factually incorrect. `internal/fabricengine/junction_repoint_test.go` IS present in the overview's `## All Files Touched` (line 98, added during the hybrid re-plan). No change needed.
