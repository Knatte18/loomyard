MILL_REVIEW_BEGIN
# Review: reed: born-as-strand for loom start's operator attach — holistic

```yaml
duration_s: 449.0
verdict: APPROVE
reviewer_model: sonnetxhigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-09-19
```

## Findings

### [NIT:consistency] Batch 1 reverses a named architecture decision without updating its two source-of-truth comments
**Demoted-from:** BLOCKING
**Location:** Batch 1, Card 1 (and Card 2). **Issue:** Card 1 gives `internal/reedengine/spawnwatchdog.go` a direct `fabricengine` import (`fabricengine.HubScratchDir`). This contradicts two comments the plan never touches: `internal/reedengine/doc.go` states plainly "`internal/lyxcwd` and `internal/fabricengine` are consequently absent from this package's DIRECT production imports," and `internal/reedcli/watchdog.go`'s own header names the very decision being reversed — "`internal/reedcli` owns the daemon outright, per the discussion's `told-geometry-keeps-the-daemon-out-of-reedengine` decision: ... `internal/reedengine` gains exactly one new engine-less function (`ListSessions`) and learns nothing about the daemon's existence." Card 14 even lists `internal/reedcli/watchdog.go` as Context, so the plan writer read this text and still left it standing. Once Card 1 lands both statements are false, and the plan's own "docs land with the behaviour card that makes them true" Shared Decision (Batch 2) is exactly the policy this omission breaks — it just never named these two files. **Fix:** Add a requirement to Card 1 or Card 2 to update (or delete) the "absent from this package's DIRECT production imports" sentence in `internal/reedengine/doc.go`, and to correct/remove the "told-geometry-keeps-the-daemon-out-of-reedengine" rationale in `internal/reedcli/watchdog.go`'s header comment so it no longer claims `reedengine` "learns nothing about the daemon's existence."

### [NIT:consistency] Watchdog spawn call site is inserted with no step marker of its own
**Location:** Batch 2, Card 12. **Issue:** `start.go`'s `RunE` numbers its blocks with `// Step N:` comments (steps 4 through 7 visible around the insertion point). Card 12 inserts `c.spawnWatchdog(...)` between the existing Step 4 block and the `// Step 5:` comment, with three placement facts required as prose but no instruction on whether the surrounding step numbering should be preserved, renumbered, or left as-is. **Fix:** Have Card 12 state explicitly that the call is unnumbered prose immediately following Step 4 (or fold it into Step 4's own comment block) so a later reader isn't left to guess why "Step 5" no longer immediately follows "Step 4."

## Verdict

APPROVE
Card 1 imports fabricengine into reedengine without reconciling two explicit doc comments that say it never would.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
