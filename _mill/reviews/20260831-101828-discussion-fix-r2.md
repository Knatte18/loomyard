# Discussion fix — round 2

Source review: `_mill/reviews/20260831-101619-discussion-review-r2.md` (holistic, APPROVE, 0 BLOCKING / 3 NIT).

## Fixed

- r2 holistic — "[NIT:scope] Integration test consuming the default is unenumerated" (demoted from BLOCKING): verified against `internal/reedengine/attachgeometry_integration_test.go:248-249,367-368` and the fixture chain `newIntegrationEngine` → `seedReedConfig` → `ConfigTemplate()` (`contract_integration_test.go:33-49`). Added an explicit value-agnostic/no-edit disposition under Technical context with the 100x30 non-clamping arithmetic, and corrected Testing's "unit tier only / no live tmux" and "only place behaviour is asserted" claims, adding the integration-tier run requirement and `go test -tags integration ./...` to the whole-repo gate.
- r2 holistic — "[NIT:design] Six-row cost analysis omits the clamping case": verified against `render/height.go`'s `stackHeights` — at `6`, four strips on a 30-row client drive `fullRemaining` to `0` where `3` leaves 12. Added the depth×window thresholds (four-deep at 30 rows, five-deep at 40, seven-deep on the 50-row boot box) to `strip-default-six` as accepted degradation, cross-referencing `clamp-path-unchanged`.
- r2 holistic — "[NIT:decision] \"Documented\" no-migration has no named artefact": verified that both templates' `mouse` and `watchdog` comments already carry the "already-materialized reed.yaml keeps whatever value it holds" caveat. Named the reconcile adoption caveat as a required part of the new `collapsed_strip_rows` comment in both Scope and the `no-value-migration` decision.

## Pushed Back

(none — all three findings verified accurate against the source, and no fix broke existing functionality, conflicted with a documented design decision, or destabilized work outside this task's scope.)
