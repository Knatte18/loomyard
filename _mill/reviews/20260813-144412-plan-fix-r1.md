# Plan fix — round 1

Source review: `_mill/reviews/20260813-144053-plan-review-r1.md` (holistic, APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- `20260813-144053-plan-review-r1.md` — "[NIT:consistency] Card 36's prose sweep contradicts the bounded-read Shared Decision".
  Card 36's `Context:` now enumerates the prose regeneration query's actual hit set (fifteen files, as measured when the plan was written), so the card's read set is bounded exactly like every other card's.
  The query is retained but re-purposed from "read every hit" to "confirm the hit set": a file the query returns that is not listed is reported as post-planning prose drift rather than read.
  The card also now states, per listed file, why only `docs/shared-libs/README.md` needs an edit — which is what makes the single `Edits:` entry correct rather than an omission.
  The reviewer's alternative remedy (drop the query, rely on the hand-list) was not taken: `_mill/discussion.md`'s `prose-corrections` decision requires a query rather than a hand-list, on the stated ground that a prose miss survives silently and that four of its own items were found only by grepping.
  The remedy applied satisfies both that decision and the bounded-`Context:` Shared Decision.

## Pushed Back

_None._
