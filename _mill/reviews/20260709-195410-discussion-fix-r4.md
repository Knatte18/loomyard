# Discussion-fix report — round 4

Source review: `20260709-195341-discussion-review-r4.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- `20260709-195341-discussion-review-r4.md` — "[NOTE] shell seam boundary vs.
  flags in the posix example": clarified in Decisions › shell-abstraction that
  `internal/shell` builds only the `bin + prompt-read` mechanics and
  `claudeengine` appends all `--` flags (`--session-id`/`--settings`/`--resume`),
  keeping Claude content out of the invariant-bound leaf per the Shuttle
  Provider-Seam Invariant. Removed the illustrative flags from the pinned posix
  form.
- `20260709-195341-discussion-review-r4.md` — "[NOTE] posix `$(cat)` vs
  `Get-Content -Raw` newline semantics": recorded that `"$(cat <path>)"` strips
  trailing newlines while `Get-Content -Raw` is byte-exact; noted the difference
  is benign for prompt text and that the posix form reproduces the
  *single-argument* semantics, not byte-equality — so the "reproduces" claim is
  no longer overstated.

## Pushed Back

(none — both NOTEs were accurate and applied)
