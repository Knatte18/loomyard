# Discussion fix — round 3

Source review: `_mill/reviews/20260828-142342-discussion-review-r3.md`

## Fixed

- `[BLOCKING:design]` "Kills now precede persistence on a failing launch" — correct, and the round-2 "safe in every caller" claim was too narrow: it addressed only the launching strand, not the fact that `AddStrand`/`UpdateStrand` reach `SaveState` only after `launchStrandLocked` returns nil.
  Disposition taken: **accept as self-healing, with the reasoning stated**, and explicitly forbid the obvious fix.
  Added an "Accepted consequence" block to the chokepoint Decision covering why the window is benign (`reed.json` is left exactly as it was; `Status`/`toRenderStrands` derive liveness from the live pane set, not the persisted binding, so nothing is misled in the interim; the next verb's reconcile clears the stale bindings; pane-id reuse across a rebirth is already handled by `adoptPaneGenerationLocked`/`clearAllPaneBindings`) and why persisting first is worse (a `SaveState` inside the helper would write the half-added strand record on the `add` path, so a failed `add` would leave a phantom strand for `Resume` to launch — trading a benign stale binding for real corruption).
  Also told mill-plan not to close the window by adding a `SaveState`, and named the correct shape if it ever must be closed (reap before the strand record is appended).
- `[NIT:scope]` "Affected-test enumeration stops at reedengine" — correct.
  Added an "Affected-test enumeration" subsection stating the sweep method (`grep -rn "adopt\|initial pane"` across both packages' test files, then reading each hit to separate real dependencies from unrelated `adoptPaneGenerationLocked` matches), instructing mill-plan to re-run it rather than trust the list, and giving a per-file disposition.
  Both tests the review named are now in scope with explicit instructions: `smoke_panecwd_test.go` (its control-vs-split contrast collapses into two identical cases once both strands are splits — rewritten to keep both cases on their now-different `planPaneTarget` branches and restate the `-c` subject) and `TestSmokeHeaderPaneSurvivesUpAddRemoveAndReconcile` (comment and expectation corrected; what it pins is unchanged).
  The sweep also surfaced a third the review did not name — `smoke_teardown_test.go:216` — now listed as comment-only, plus dispositions for `lifecycle_test.go:388` (read-and-confirm), `smoke_resume_test.go:85` and `contract_integration_test.go` (out).
  `spawn_test.go` was added to Scope → In as well, since its whole table is organised around the adopt-vs-split decision.
- `[NIT:consistency]` "\"single line to change\" vs. return-shape change" — correct and self-contradictory as written.
  Replaced the "single line to change" phrasing with an explicit statement that the gate flip is not the whole edit, naming the return-shape change and cross-referencing the-reap-logs-what-it-destroys.

## Pushed Back

_None._
