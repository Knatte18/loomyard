MILL_REVIEW_BEGIN
# Review: loom: convert to a Shed recipe — holistic

```yaml
duration_s: 456.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Opus 5 (claude-opus-5), Anthropic
reviewed_file: plan/
date: 2026-08-21
```

## Findings

### [NIT:consistency] smoke_test.go called integration-tagged twice
**Demoted-from:** BLOCKING
**Location:** batch 4 `## Batch Tests`; batch 6 card 26 (the `internal/loomcli/smoke_test.go` paragraph)
**Issue:** Both say the file "carries an `integration` build tag" and that `go test -tags integration ./...` covers it; the file's first line is `//go:build smoke`, and batch 6's own `## Batch Tests` correctly states the opposite ("neither `go test ./internal/loomcli/` nor the module-wide `go vet ./...` compiles it ... `-tags integration` does not enable `smoke`").
**Fix:** Correct both statements to `smoke` and point the compile gate at batch 6's `&& go vet -tags smoke ./internal/loomcli/` tail, which is the only thing in this plan that reaches the file.

### [BLOCKING:scope] Roadmap sweep tokens miss a falsified Done entry
**Location:** batch 6 card 24 (the `manifest/roadmap.md` sweep)
**Issue:** The card's enumeration method is a six-token grep (`loomshed.New`, `loomshed.Deps`, `coverage_guard_test`, `equivalence_test`, `loomshed.go:137-151`, `Go literal`); `manifest/roadmap.md`'s Done entry **loom: phase-machine scaffolding** asserts "`internal/loomshed` carries loom's full 13-row producer list", which batch 5 falsifies and which matches none of the six tokens and is not in the card's named starting set.
**Fix:** Name that entry in the starting set, or widen the sweep with a token that reaches it (e.g. `13-row` / `producer list`).

### [BLOCKING:design] Named roadmap item for Landing does not exist
**Location:** `## Shared Decisions` → `landing-parity`; batch 4 card 15
**Issue:** The decision says "the parent-fabric resolution chain is a separate roadmap item" and card 15 requires the `Env.Landing` comment to name that item, but no item in `manifest/roadmap.md` (Planned, Someday, or Done) covers wiring loom's `landingshed.Deps` — the requirement is unsatisfiable as written and the implementer will either invent a name or stall.
**Fix:** Either drop the naming clause and have the comment state plainly that no roadmap item exists yet, or add the item in card 24 and name it.

### [NIT:consistency] `./contracts/...` is claimed to have no test files
**Location:** batch 1 `## Batch Tests`
**Issue:** "`./contracts/...` has no test files and reports `[no test files]` at exit 0" is false — `contracts/stencils/registry_test.go` is untagged and runs under that pattern.
**Fix:** Restate: the pattern also runs `contracts/stencils`' registry test, which this task does not touch and which must stay green.

### [NIT:consistency] `fakeAlwaysDoneProducer` has two owners
**Location:** batch 2 card 5 vs card 6
**Issue:** Card 5 says "Add `fakeAlwaysDoneProducer` here too" to `internal/loomrecipe/fixture_test.go`, while card 6 says "Relocate `fakeAlwaysDoneProducer` out of this file into `internal/loomrecipe/fixture_test.go` (card 5)" — followed literally, both cards write the declaration and `package loomrecipe` gets a duplicate.
**Fix:** State in card 5 that the declaration arrives with card 6's move and card 5 adds nothing, or in card 6 that the relocation is a deletion from `shape_test.go` only.

## Verdict

REQUEST_CHANGES
Three false premises about tags, sweep coverage, and a nonexistent roadmap item.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 2._
MILL_REVIEW_END
