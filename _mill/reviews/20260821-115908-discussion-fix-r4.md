# Discussion NIT fixes — round 4

Source review: `_mill/reviews/20260821-115749-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 5 NIT, none demoted).
Auto mode: every NIT is fixed under `mill-receiving-review`'s default rule;
pushback is unavailable, since no operator is present to escalate to.
Convergence gate satisfied this round — `round >= min_rounds` and no finding carries `demoted: true`.

## Fixed

- `[NIT:design]` "Stencil fixture needs a parent mkdir, unstated" — verified `stencilstore.RelPath` returns `<family>/<name>.md` for any hyphenated name (`internal/stencilstore/stencilstore.go`), so a bare `os.WriteFile` at `stencilstore.Path` into a fresh `t.TempDir()` fails with ENOENT. Testing § `Build` now states the `os.MkdirAll(filepath.Dir(path), 0o755)` step and names `internal/shedrecipe/entries_singlellm_test.go`'s `writeStencilFile` as the pattern.
- `[NIT:design]` "Piece-4 framing ignores `shedrecipe` → `loomshed`" — confirmed `internal/shedrecipe/entries_simple.go` imports `internal/loomshed`, so piece 4 as the roadmap words it would close a production import cycle. Added a carried-forward Technical-context subsection recording the chain, why this task is unaffected (`shedbuild` reaches `loomshed` only from `_test.go` files), and the two resolutions available to piece 4 — consumer above `loomshed` (`internal/loomcli`), or `loomshed` sheds the constructors `shedrecipe` reaches for. The choice is explicitly left to piece 4.
- `[NIT:decision]` "No disposition on a sole-parser invariant" — Constraints now records an explicit decision not to add one in this commit, with the reason (after this task there is no production on-disk recipe format at all, so a second parser cannot plausibly appear), the trigger for adding it (piece 4, which ships the first real recipe and consumer), and the analogue's own enforcement posture (`Planparser Sole-Parser Invariant` is a review obligation, not a test).
- `[NIT:decision]` "\"`Config` never contains absolute paths\" unaddressed" — the validation split now records that the rule is enforced per-entry by `shedrecipe.resolveUnderRoot` (rejects empty, absolute, and `..`-escaping values), that `shedbuild` deliberately adds nothing because only the entry knows which `Config` keys are paths, and that `BurlerRound`'s `profile.target.paths`/`profile.fasit.paths` remain the one documented bypass.
- `[NIT:decision]` "Duplicate YAML mapping keys unaddressed" — verified empirically that `yaml.v3` + `KnownFields(true)` already rejects them (`line 3: mapping key "entry" already defined at line 2`). Recorded as decoder-enforced pass-through with no `shedbuild` check, explicitly contrasted with the duplicate-row-`name` check that `shedbuild` does perform itself, and a matching `Parse` table case added.

## Pushed Back

None.
All five findings verified accurate against the cited code — the duplicate-key and empty-input behaviours by running `yaml.v3` directly rather than by reading its docs — and none met the decision tree's harm criteria.
