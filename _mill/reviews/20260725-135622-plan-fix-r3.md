# Plan fix — round 3

Source review: `_mill/reviews/20260725-135441-plan-review-r3.md`

## Fixed

- [BLOCKING] "Overview build gate cannot pass at batch-7/8 boundaries" — the engine decouple is a multi-batch API migration: batch 7 changes `websterengine`'s public surface while `internal/webstercli`/`cmd/lyx` are not rewired until batch 9, so a per-boundary whole-module `go build ./...` is intentionally red across batches 7–8. Set the overview module-wide `verify:` to `null`; rewrote the `verify-command-native-go` Shared Decision to state `go build ./...` is a **plan-final gate** (batch 10's `verify:`), not a per-boundary gate, and that batches 7–8 are gated only by their package-scoped `go test -tags integration ./internal/websterengine/...` (which does not compile the downstream packages). Corrected the batch-7 Batch Tests "caught by the overview gate" wording and the batch-9 reference to point at batch 10's final `go build ./...`.
- [NIT] "Card 40 calls BeginBatch without its file in Context" — added `internal/websterengine/beginbatch.go` to card 40's `Context:` so the new (batch-7) `BeginBatch` arity is readable from the card's declared context.

## Pushed Back

_None — both findings were factually accurate and low-harm; the BLOCKING one exposed a genuine gate contradiction inherent to a multi-batch API migration, now resolved by scoping the whole-module build to plan-final._
