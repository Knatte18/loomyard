# Discussion fix — round 5

Source review: `_mill/reviews/20260820-085835-discussion-review-r5.md` (APPROVE, 0 BLOCKING, 2 NIT).

Round 5 is the configured round cap (`roles.discussion-review.holistic.rounds: 5`).
The convergence gate's demoted-finding predicate was not satisfied — one of this round's two findings was demoted from BLOCKING by the stage's blocking-class ceiling — so this round is accepted as an implicit approval under the round cap rather than a clean convergence, and the commit message records that.

## Fixed

- `20260820-085835-discussion-review-r5.md` — "loomengine package doc undisposed": added `internal/loomengine/status.go:5-12` to the files-that-change list, quoting the three sentences that name the deleted `Preflight` or claim tier-1/2 checks, with the rewrite target (`CheckSeed` as loom's own seed-coherence check). Also added the sweep correction the finding diagnosed: the closed grep was run on the qualified spelling `loomengine.Preflight`, which is blind to in-package references, so the sweep must additionally cover unqualified `\bPreflight\b` within `internal/loomengine`.
- `20260820-085835-discussion-review-r5.md` — "'coherent seed' fixture for the row-2 test": stated in TDD candidate 5 that the fixture must be hand-written in the post-row-1 shape (`current_producer: "Loom-Preflight"` plus a `Preflight`/`done` history entry), never built by `loomshed.Seed`, which writes `current_producer: "Preflight"` and would make the "coherent seed → `Done`" case assert the opposite of its name.

## Pushed Back

None.
Both findings verified accurate against the cited code.
The second is a latent test-authoring trap rather than a documentation gap — a `Seed`-built fixture fails in the direction that reads as a passing test — so it would have been fixed under the default rule regardless of severity.
