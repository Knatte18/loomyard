# Review: Extract shared infrastructure (config, git, lock) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-06-09
```

## Findings

### [BLOCKING] TestMalformedYAMLError fails after migration
**Location:** Batch 4 / Card 16, `internal/board/config_test.go`
**Issue:** The test asserts `stringContains(errMsg, "parsing YAML") || stringContains(errMsg, "error")`. Old `board.LoadConfig` wrapped yaml errors as `"error parsing YAML in layer file %s: %w"`, guaranteeing the match. After migration, `config.Load → loadYAMLLayer` returns the raw `yaml.v3` error (a syntax error for `[ unclosed`) which formats as `"yaml: line N: did not find expected ']'"` — containing neither substring. The plan says to keep this test unchanged, so batch 4's `go test ./...` fails.
**Fix:** In Card 16's config_test.go requirements, update `TestMalformedYAMLError` to assert only `err != nil` (dropping the substring check), or have `config.Load` / `LoadConfig` wrap yaml errors with "parsing YAML" text so the existing assertion still holds.

### [NIT] Lock API signatures in Card 1 don't match source
**Location:** Batch 1 / Card 1
**Issue:** Requirements list `AcquireWriteLock(path string) (FileLock, error)`, `AcquireReadLock(path string) (FileLock, error)`, and `Release(lock FileLock) error` (free function), but the source returns `*FileLock` (pointer) and `Release` is a method `func (l *FileLock) Release() error`. "Verbatim lift" saves correctness, but the mismatched annotations plus Card 2's "board.Release → lock.Release" example (no such free-function call exists in `lock_test.go`) will confuse the implementer.
**Fix:** Correct the listed signatures in Card 1 to `(*FileLock, error)` returns and remove the `Release(lock FileLock) error` description; update Card 2's example to remove the nonexistent `board.Release → lock.Release` substitution.

### [NIT] "Replace Release(...)" instructions are misleading in cards 9–11
**Location:** Batch 4 / Cards 9, 10, 11
**Issue:** Each card says "replace `Release(...)` calls with `lock.Release(...)`", but `Release` is a method on `*FileLock` — no free-function `Release(...)` calls exist in `board.go`, `store.go`, or `sync.go`. The instruction would match nothing and could cause an implementer to introduce incorrect `lock.Release(l)` calls.
**Fix:** Remove the "replace Release(...)" sentence from all three cards; note that existing `<var>.Release()` method calls need no change.

### [NIT] Card 3 specifies bytes.Buffer; source uses strings.Builder
**Location:** Batch 2 / Card 3
**Issue:** Requirements say "assign `bytes.Buffer` values to `cmd.Stdout` and `cmd.Stderr`" and list `bytes` in imports, but `board/git.go` uses `strings.Builder` for both buffers.
**Fix:** Change to `strings.Builder` (import `strings` instead of `bytes`) to stay consistent with source.

## Verdict

REQUEST_CHANGES
One blocking issue: `TestMalformedYAMLError` will fail after migration due to changed error message format.