# Discussion-fix report — round 2

Source review: `_mill/reviews/20260729-145104-discussion-review-r2.md` (verdict APPROVE, 3 NOTEs).

## Fixed

- [NOTE] "Wiring-guard filter set: only accessor option is single-source" — updated Decisions → wiring-guard "Note for mill-plan" to require the single exported `hubgeometry` hub-reserved-names accessor for both `IsReservedHubName` and the fabric filter; dropped the fabricengine-const alternative that would duplicate the list.
- [NOTE] "Enforcement claim overstates the guard's reach" — corrected Technical context → Enforcement interaction to state the three caught contexts (`filepath.Join` arg, binary-`+` operand, `const`-value `BasicLit`) and that a `[]string{...}` slice/composite literal is **not** machine-caught, so the "don't hardcode config-migrated names in fabric" rule rests on review discipline, not a machine backstop.
- [NOTE] "Config-load failure surfaced as junction-health reason may misdirect" — updated Decisions → no-fallback to require the `(bool, string)` health-function reason to distinctly name the config-load failure (`"cannot load fabric.yaml: <err>"`), never a generic junction-drift message.

## Pushed Back

(none — all three NOTEs were factually accurate and improved the design; applied under the fix-everything default.)
