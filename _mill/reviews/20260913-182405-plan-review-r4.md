MILL_REVIEW_BEGIN
# Review: Deploy cited spec/design docs to target repos like stencils — holistic

```yaml
duration_s: 441.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-09-13
```

## Findings

### [NIT:scope] Card 20's "three sites" count is off by one
**Location:** batch 05-specs-dir-marker-plumbing, card 20 **Issue:** The card claims "the three sites that construct an `Env` with a stencils directory" across `internal/loomrecipe/fixture_test.go` and `internal/loomrecipe/shape_test.go`, but source shows only two `shedrecipe.Env{}` literals with a `StencilsDir` field (fixture_test.go:606, shape_test.go:88); the apparent third occurrence at fixture_test.go:166 is `testLandingDeps`'s unrelated `landingshed.Deps{StencilsDir: dir}` literal, a different struct. **Fix:** Say "two sites" (or drop the count and just say "every site"); an implementer chasing a phantom third site risks trying to add `SpecsDir` to `landingshed.Deps`, which has no such field.

## Verdict

APPROVE
Extensively source-verified against every referenced file; one harmless miscount found, no constraint violations or gaps.
MILL_REVIEW_END
