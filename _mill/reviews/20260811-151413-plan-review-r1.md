MILL_REVIEW_BEGIN
# Review: shed: land the producer-typology decision (atomicity carve-out) and sweep remaining doc contradictions — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Claude Sonnet 4.5 (Anthropic), presenting as "Sonnet 5" per system context
reviewed_file: plan/
date: 2026-08-11
```

## Findings

### [NIT:consistency] loom.md's `shed.md` anchor count exceeds the plan's own "exactly twice" cardinality
**Demoted-from:** BLOCKING
**Location:** batch 2, card 9 (vs. `00-overview.md`'s `shed-md-is-authoritative-loom-md-points` Shared Decision and batch 2's own scope statement).
**Issue:** The Shared Decision states verbatim "The link appears in `loom.md` exactly twice," batch 2's own scope note repeats "the `shed.md` anchor appears exactly twice in this file," and card 7's requirements say "Two pointers into `manifest/designs/shed.md`, and only two." Card 9 then adds at least one more inline link to the same `shed.md#producer-contract-vs-producer-definition` anchor at line 70/71 (its own text: "the link this card writes points at `shed.md`'s contract section … it is a different sentence making a different claim, not a repeated per-row link"), pushing the true count to three (or four, if (a)'s thin-Input/Output pointer and (b)'s pointer-rule pointer are two separate links rather than one merged sentence — the card text is ambiguous on this point too).
**Fix:** Either update the Shared Decision and batch 2's scope note to state the correct count (and clarify whether card 9 adds one link or two), or rewrite card 9 to cite the carve-out in prose without a new markdown link, preserving the "exactly twice" invariant the rest of the batch is built on.

### [BLOCKING:design] roadmap.md card 12's forward-pointer target doesn't contain what it's admitting
**Location:** batch 3, card 12.
**Issue:** The card instructs qualifying `roadmap.md:51`'s atomicity clause AND admitting the thin-Input/thin-Output cases in the Input/Output definitions, then says "In both cases point forward to lines 57-61 rather than restating them." Lines 57-61 (verified against the live file) discuss only the simple/bespoke atomicity typology — they never mention thin-Input or thin-Output. The actual authoritative location for those two carve-outs is `shed.md`'s producer-contract section (per the `shed-md-is-authoritative-loom-md-points` decision), not `roadmap.md:57-61`. Implemented literally, this would land an internal roadmap cross-reference that points readers at text that doesn't discuss what's being admitted — exactly the kind of doc contradiction this task exists to remove. (This ambiguity is inherited verbatim from `_mill/discussion.md:291-292`, so the plan didn't introduce it, but it also didn't catch/resolve it.)
**Fix:** Point the thin-Input/thin-Output admission at `manifest/designs/shed.md`'s contract section (prose reference, since `roadmap.md` isn't in the "anchors that must stay valid" set for cross-doc links per se — or clarify in the card that only the atomicity clause points at 57-61 while the thin-case admission points at `shed.md`).

### [NIT:consistency] card 14's placement instruction is self-contradictory as literally worded
**Location:** batch 3, card 14.
**Issue:** "placed beside the atomicity resolution at lines 57-61 (immediately after it, before the existing pointer to the surfaced-open-questions record)" — but line 61 itself *is* "the existing pointer to the surfaced-open-questions record" (`See designs/shed-followups.md for the full surfaced-open-questions record`), which is inside the cited 57-61 range. "After lines 57-61" and "before the pointer" can't both hold under a literal reading.
**Fix:** Reword to unambiguously say the new record goes between line 60 and line 61 (i.e., "after the atomicity resolution at lines 57-60, before the shed-followups.md pointer at line 61").

## Verdict

REQUEST_CHANGES
Fix the anchor-count contradiction in batch 2 card 9 and the mis-targeted roadmap pointer in batch 3 card 12.
MILL_REVIEW_END
