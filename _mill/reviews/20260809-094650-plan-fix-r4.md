# Plan fix — round 4

Source review: `_mill/reviews/20260809-094249-plan-review-r4.md`

## Fixed

- r4 `[BLOCKING:design]` "Roadmap Planned-item relocation left unspecified" — verified the fabric entry sits under the roadmap's Planned heading and that the file's Maintenance section mandates a Planned-to-Done move on ship;
  card 18 now instructs the relocation explicitly, keeping the design-doc pointer and following the Done section's own numbering convention.
- r4 `[NIT:consistency]` "`TestCloneHub_StrictAbortRemovesHubOnFailure` stops proving what its name/comment claim" — verified the test reaches teardown today via a nonexistent weft, which the new ordering intercepts at the probe.
  Card 7 now carries an explicit exception to its own no-assertion-changes rule: retarget the test to a valid weft and a nonexistent warp so the failure lands after the hub exists and `teardownHub` genuinely runs, and update its doc comment.
  Noted that the nonexistent-weft case is not lost — batch 4 covers it as a probe-taxonomy hard error.
- r4 `[NIT:consistency]` "Uniform `ForceBootstrap: true` comment text misfits one call site" — verified `TestCloneHub_CreatesHubDotLyx` builds its weft with `initTinyRepo`, a non-bare working repo;
  card 7's comment instruction is now fixture-specific rather than one pasted sentence, and names that site.
- r4 `[NIT:scope]` "New fabric-suite scenario lacks a matching session-log trailer line" — verified the closing session-log block carries one line per scenario through F6;
  card 21 now requires appending the new scenario at the end of the sequence (not inserting, which would renumber the block) and appending its matching trailer line.
- r4 `[NIT:scope]` "`Bolt` named in card 13's Requirements without its declaring file in Context" — added `internal/fabricengine/bolt.go` to card 13's `Context:`.

## Pushed Back

_(none)_
