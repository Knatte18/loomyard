# Discussion fix report — round 1

Source review: `_mill/reviews/20260809-082047-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 4 NIT).
All four NITs verified accurate against the tree; none tripped a harm-check category, so all four were fixed under the `mill-receiving-review` fix-everything default.

## Fixed

- `20260809-082047-discussion-review-r1.md` — "[NIT:consistency] plan-format-v3.md:5 link dangles, excluded anyway".
  Verified: `docs/reference/plan-format-v3.md:5` reads `[plan-format.md v2](plan-format.md)`, and this task deletes that file.
  Generalized the `loom-md-links-fixed-prose-deferred` decision into an explicit rule — wherever this task deletes a file it repairs every inbound link, even in another task's territory, touching nothing else on those lines — and applied it to `plan-format-v3.md:5` in Scope → Out.
- `20260809-082047-discussion-review-r1.md` — "[NIT:scope] Acceptance grep does not cover the module-word sweep".
  Verified: `tools/sandbox/SANDBOX-WEBSTER-SUITE.md:5,7,28,49,123,193,195` and the `suite.go`/`main.go` doc comments are real, un-inventoried sites invisible to both original grep patterns.
  Added a third module-word grep pattern (`lyx builder`, `builder.yaml`, `builder-suite`, `builder suite`, `SANDBOX-BUILDER-SUITE`, `_lyx/builder`, `.lyx/builder`) with an explicit pattern-level exclusion of ordinary-English false positives (`strings.Builder`, "fixture builders", "fluent builder method"), and added the missing sites to the Markdown-sites inventory.
- `20260809-082047-discussion-review-r1.md` — "[NIT:scope] S9 line range truncates the scenario body".
  Verified: S9 runs `:224` through its `**Verdict:**` line at `:284`, with the closing `---` at `:287`; the spec's `:224–232` covers only heading and preamble.
  Restated the range as the whole block and noted the bracketing `---` rules must stay balanced.
- `20260809-082047-discussion-review-r1.md` — "[NIT:design] summary.md contract omits the integration-failure append".
  Verified: `internal/websterengine/summary.go:101–106`'s `AppendIntegrationFailure` writes a `## Integration suite failed` section into an already-written `summary.md`.
  Declared it part of the consumer-facing contract — Finalize dumps `summary.md` verbatim into the PR body, so the section reaches the consumer — while keeping the bisect mechanism itself webster-internal.

## Pushed Back

None.
