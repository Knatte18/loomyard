# Fixer report — plan-fix round 1

Source review: `_mill/reviews/20260824-183200-plan-review-r1.md`

## Fixed

- `20260824-183200-plan-review-r1.md` — "[BLOCKING:scope] `fakeDiscussionShuttle`→`fakeLoomShuttle` rename is incomplete".
  Verified accurate: a case-sensitive grep confirms `internal/loomrecipe/resume_test.go` names the type at lines 228 and 307, `internal/loomrecipe/shape_test.go` at lines 56, 88, and 258, and `internal/loomrecipe/sequence_test.go` at lines 103 and 105 — none of which card 8 previously reached.
  Card 8 now owns the whole rename: `internal/loomrecipe/resume_test.go`, `internal/loomrecipe/sequence_test.go`, and `internal/loomrecipe/shape_test.go` were added to its `Edits:`, and its `Requirements:` now enumerates every reference site per file, including the `Run` receiver and the three `fmt.Errorf` strings the review did not name.
  Cards 9 and 10 keep their own narrower edits to those files and no longer restate the rename.
  Beyond the finding as written, `shape_test.go`'s `testEnv` also needed `PlanSpec`/`CommitPlan` filled — it would fail `New` construction for `TestNew_ProducerTable` and both coverage-guard tests the moment card 9 flips the recipe row — so card 8 now requires that too;
  the review found the rename half of this defect and the seam half is the same defect's other half.
  Batch 2's Batch Scope records why the rename is owned by one card rather than split, and `resume_test.go` was added to the overview's All Files Touched.

- `20260824-183200-plan-review-r1.md` — "[BLOCKING:scope] New `TestWire_PlanSpecEvaluatesToExpectedShape` needs the plan stencil seeded".
  Verified accurate: `hubLocation` in `internal/loomcli/wiring_test.go` calls `seedLoomConfig`, `seedLandingConfig`, and `seedDiscussionStencil` only, and `seedDiscussionStencil` writes `loom-template-discussion.md` alone, so `stencilstore.Read(stencilsDir, "loom-template-plan")` inside `composePlanPrompt` would hard-error.
  Card 11 now requires a `seedPlanStencil` helper writing `stencils.LoomTemplatePlan`, a call to it from `hubLocation`, and the matching `hubLocation` doc-comment update, stated as a prerequisite step before the two new tests rather than as an afterthought.
  Also recorded, from reading `internal/pattern/pattern.go`, that no pattern stencil needs seeding: `pattern.Directive` returns an empty string and a nil error when PATTERN is inactive, which it is in a bare `t.TempDir()`.
  `internal/pattern/pattern.go` and `contracts/stencils/stencils.go` were added to card 11's `Context:` so the implementer can confirm both facts without exploring.

- `20260824-183200-plan-review-r1.md` — "[NIT:consistency] \"row 7\" mislabels Plan-Write's actual list position".
  Verified accurate: `wantSequenceOrder` in `internal/loomrecipe/sequence_test.go` places `Plan-Write` sixth, because the built list has no `Plan-Sweep` row at all, and that file's existing comments already number `Publish` as row 12 and `Finalize` as row 13 against the same built list.
  Cards 8 and 10 now say row 6 and each carries a one-sentence note explaining why the design table's 7 does not apply to a test doc comment.
  Card 14's own "recipe row 7" phrasing was a separate instance of the same confusion in a different file, so it was fixed in the same pass: the module-table sentence now names the `Plan-Write` row instead of numbering it, since 7 and 6 are both defensible readings there and neither is unambiguous.

## Pushed Back

_None. All three findings were verified accurate and none triggered a harm-check exception._
