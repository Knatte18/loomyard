# Plan fix — round 2

Source review: `_mill/reviews/20260818-091116-plan-review-r2.md`

## Fixed

- `20260818-091116-plan-review-r2.md` — "[BLOCKING:scope] hubgeom.BurlerGeometry/PerchGeometry named outside Context".
  Verified accurate: cards 1, 2, and 6 each prescribe a doc-comment sentence naming a `hubgeom` teller while listing no `hubgeom` file to read.
  Added `internal/hubgeom/hubgeom.go` to the `Context:` of all three cards, so the implementer can copy `ReedGeometry`'s doc-comment shape rather than reconstruct it.
- `20260818-091116-plan-review-r2.md` — "[BLOCKING:scope] pattern.File named outside Context in card 13".
  Verified accurate: card 13's Requirements cites `pattern.File(baseDir string)` as the shape to mirror, but its `Context:` listed only `internal/lyxdirs/dirs.go` and `internal/planparser/parse.go`.
  Added `internal/pattern/pattern.go`.
- `20260818-091116-plan-review-r2.md` — "[NIT:consistency] notransients_test.go row count mismatch".
  Verified accurate: batch 3's `## Batch Scope` still said "five" after round 1 corrected card 21 to six lines / seven call expressions.
  The scope prose now says six, matching the card.

## Pushed Back

_(none — every finding verified accurate and none of the fixes caused harm under the receiving-review decision tree.)_
