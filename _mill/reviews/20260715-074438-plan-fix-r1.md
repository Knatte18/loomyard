# Plan-fix report — round 1

Source review: `_mill/reviews/20260715-073924-plan-review-r1.md` (REQUEST_CHANGES, 5 BLOCKING, 0 NIT). All 5 verified accurate against the tree; all fixed, none pushed back.

## Fixed

- `[BLOCKING] Contradicts passing smoke test that pins corpse behavior` (r1) —
  Verified: `internal/muxcli/smoke_lifecycle_test.go::TestSmokeRemoveLastStrandThenAddRunsTheNewCommand`
  (`//go:build smoke`, runs against psmux) asserts remove-of-sole-strand → 0 then
  a live second strand, which requires the session to survive on psmux. Brought
  the smoke test into scope: Card 3 now edits its doc comment to note the premise
  is psmux-specific and tmux behaves oppositely; overview Shared Decision now
  reconciles all three encodings (strand.go / doc.go / smoke test).
- `[BLOCKING] "psmux last-pane behavior unverified" is factually wrong` (r1) —
  Verified: the smoke test *verifies* psmux corpses the last pane. Rewrote the
  overview Shared Decision to state the assumption is BINARY-DEPENDENT (tmux
  destroys — repro; psmux corpses — smoke-test-verified) and removed the
  "unverified" label; Card 3 requirements now demand a per-binary doc/comment
  correction and forbid calling psmux behavior unverified.
- `[BLOCKING] verify scope cannot catch the muxcli contradiction` (r1) —
  Verified: original `verify` ran only `./internal/muxengine/`; the smoke test
  lives in `internal/muxcli` behind `//go:build smoke`. Changed batch + overview
  verify to `go test -tags "integration smoke" ./internal/muxengine/ ./internal/muxcli/`
  so Card 3's smoke-test edit is compiled; documented the scope choice in Batch
  Tests.
- `[BLOCKING] Card 5 regression may never exercise the swallow path` (r1) —
  Verified: `cfg.Psmux` is PATH-resolved `tmux` on POSIX (`template_posix.go`),
  so on this box Card 5 runs on tmux and *does* hit the swallow; on psmux it
  would pass via the normal path. Added a coverage note to Card 5 requirements
  and Batch Tests: the swallow branch is exercised only on last-pane-destroy
  backends (tmux); the Card 4 pure-helper test covers the decision logic on all
  platforms.
- `[BLOCKING] Card 3 Context omits overlay.go` (r1) — Verified: Card 3
  Requirements reference `hasSession`/`listPanes` defined in `overlay.go`, which
  was neither Context nor Edits. Added `internal/muxengine/overlay.go` to Card 3
  Context.

## Pushed Back

_None._
