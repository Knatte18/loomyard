# Plan fix — round 4

Source review: `_mill/reviews/20260821-152450-plan-review-r4.md` (holistic, sonnetxhigh, REQUEST_CHANGES, 1 BLOCKING + 2 NIT).

Every finding was verified against source before acting.
All three are accurate;
none met a harm-check exception.
All three fixed, none pushed back.

## Fixed

- r4 `[BLOCKING:scope]` "Sweep misses in-package bare `New`/`Deps` doc references" — verified all three sites.
  `internal/loomshed/loompreflight.go:29-32` says row 2 "is still built internally by New (see loomshed.go), never injected -- unlike Deps.Preflight";
  `internal/loomshed/seed.go:29-30` says `Seed` "takes bare told paths rather than a Deps";
  `internal/loomshed/loomshed.go:22` ends the `Name*` const-block doc with "because loom's own producer table is what New assembles from them".
  All three spell the deleted symbols unqualified, which no cross-package sweep token can match, and the first two files were in no card's `Edits:`.
  Added both files to card 20's `Edits:` and to `## All Files Touched`, named the exact sentence to restate in each with its surviving point preserved, extended card 20's const-block requirement to cover the trailing clause rather than only "keep and extend", and added the bare `Deps`/`New(` spellings to card 26's sweep tokens scoped to `internal/loomshed`.
  Card 26's sweep now serves as a backstop there rather than the primary fix — a hit surviving into batch 6 means card 20 missed one.
- r4 `[NIT:design]` "Coherence-check ordering and the divergent-value shape are unpinned" — accurate, and it would have produced a test failing against a correct implementation.
  Card 3 now pins both coherence checks as `New`'s first act, ahead of `shedbuild.Parse` and therefore ahead of `Build`, with the reason stated: `loomPreflightEntry` calls `requireAbsRoot("LoomPreflight", "StatusPath", …)` during `Build`, so a divergence involving an empty or relative value would otherwise surface as that entry's absoluteness error and hide which copy was wrong.
  Card 7 now requires the overwritten value to stay absolute and merely different — a second path under the same `t.TempDir()` — never empty and never relative.
- r4 `[NIT:consistency]` "Card 5 claims to consume a symbol its own file cannot reach" — accurate;
  the converted `buildSequenceFixture` returns `(anchorPath, shedrecipe.Env, ShedPaths)`, none of which carries a producer field, so the moved `fixture_test.go` never references the fake.
  The real consumers are the substituting tests in cards 6, 8, and 9.
  Card 5 now says it neither declares nor consumes it, and names those three cards as the consumers card 6's relocated declaration serves.

## Pushed Back

_None._
All three findings verified accurate against source, and no fix breaks existing functionality, conflicts with a documented decision, or destabilizes work outside this task's scope.
