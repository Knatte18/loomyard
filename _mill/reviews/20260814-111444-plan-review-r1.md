MILL_REVIEW_BEGIN
# Review: Relocate producer prompt files into a stencils/ directory — holistic

```yaml
duration_s: 582.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetxhigh
reviewer_self_id: Sonnet-4.5-class model (harness-reported ID "claude-sonnet-5"); exact build uncertain
reviewed_file: plan/
date: 2026-08-14
```

## Findings

### [BLOCKING:scope] Pre-existing unit tests calling changed signatures are never listed in any card
**Location:** batch 4 (cards 18, 19), batch 5 (cards 22, 24)
**Issue:** Cards 18/22/24 change the arity of `burlerengine.New`, `composePrompt`, `treadleengine.runTriage`, `treadleengine.runTargeting`, and `perchengine.Engine.Run`, but none of these cards' `Edits:`/`Context:` name the existing hand-written, untagged (Tier‑1) test files that call these exact functions positionally with the OLD arity: `internal/burlerengine/prompt_test.go` (composePrompt, ~16 call sites, e.g. line 79), `internal/burlerengine/engine_test.go` (`New(shuttle, layout, cfg)`, 7 sites, e.g. line 172), `internal/treadleengine/judge_test.go` (`runTriage(sh, "perch", 2, ..., "haiku", "low")`, ~5 sites, e.g. line 331), `internal/treadleengine/engine_test.go` (`runTargeting(sh, "perch", 3, ..., "opus", "high")`, line 1347), and `internal/perchengine/run_test.go` (`e.Run(p, runDir, runDir)`, ~30 sites, e.g. line 276). All five are untagged packages whose compilation is required by their own batch's stated `verify:` (e.g. batch 4's `go test ./internal/burlerengine/...`, batch 5's `go test ./internal/perchengine/...`), so each batch's own verify would fail to even compile as scoped. `internal/burlerengine/smoke_round_test.go`/`smoke_cluster_test.go` (`//go:build smoke`) also call the old 3-arg `New`, lower urgency since gated out of both batch `verify:` and `pipeline.done_gate`.
**Fix:** Add each file above to the relevant card's `Edits:` (18 → `prompt_test.go`, `engine_test.go`, and the two smoke files; 22 → `judge_test.go`; 24 → `engine_test.go`, `run_test.go`) with a Requirements line instructing the implementer to thread the new parameter through every existing call site.

### [BLOCKING:scope] Stale-reference sweep (card 40) is scoped to `.md` only, missing two `.go` comment hits
**Location:** batch 9, card 40
**Issue:** `internal/websterengine/integration.go:8` and `internal/websterengine/integration_test.go:9` both carry a doc comment naming `master-template.md` ("per master-template.md's own integration-fork bracket"). Card 40's sweep is explicitly scoped to `docs/`, `manifest/`, `CLAUDE.md`, `README.md`, and `internal/**/*.md` — neither file is `.md`, and neither is in any other card's `Edits:`, so these two comments go stale and are never caught by the "re-run the sweep" instruction as written.
**Fix:** Extend card 40's grep scope to include `internal/**/*.go` comments (or add these two files by name, mirroring the "Known hits" list format already used).

### [NIT:consistency] Card 18's import claim is factually wrong for both named files
**Location:** batch 4, card 18
**Issue:** "Add the `internal/fabricengine` import to whichever of those two files does not already carry it" implies exactly one of `internal/burlercli/cli.go` / `internal/perchcli/cli.go` already imports `fabricengine`. Neither currently does.
**Fix:** Reword to "add the import to both files."

### [NIT:consistency] Several batch-3 line-number citations have drifted from current source
**Location:** batch 3, cards 12–13
**Issue:** Card 12 cites `board.go:112` for `boardCriticalSection`'s `writeLockFile` use (actual: line 109). Card 13 cites `commit.go:182` for the `defer func(){ _ = l.Release() }()` idiom (actual: line 186) and `weftgit.go:256` for the same idiom (actual: line 260). The named functions/idioms are still correctly identified in every case, so this doesn't block implementation, but the cited line numbers should not be trusted literally.
**Fix:** Re-derive line numbers at implementation time rather than trusting the plan's citations verbatim; no plan edit strictly required.

## Verdict

REQUEST_CHANGES
Batches 4/5 break compilation of five existing test files no card lists; must be fixed before implementation starts.
MILL_REVIEW_END
