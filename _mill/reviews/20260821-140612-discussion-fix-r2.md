# Discussion fix — round 2

Source review: `_mill/reviews/20260821-140440-discussion-review-r2.md` (APPROVE, 0 blocking, 6 NIT).

## Fixed

- r2 — "`loomshed_test.go` has no \"non-New half\"": added a per-test disposition for all eight `TestNew_*` tests under `test-ownership`, including `TestNew_NilPreflightReturnsError` deleted outright and `TestNew_ToldShedFields`/`TestNew_MissingLandingClosureReturnsError` repointed at the Shed-paths carrier and `shedbuild`'s row-indexed error text.
- r2 — "\"no change to the four packages\" blocks required comment repair": added an explicit doc-comment carve-out to the Out clause, naming `shedcheck/doc.go:8`, `shedrecipe/recipe.go:69`, and `entries_simple.go:33-34`/`:53-55`, plus a grep sweep obligation.
- r2 — "Docs decision says five files, lists six": heading, rationale count, and the Q&A entry now all say six and name `manifest/parallel-work.md`.
- r2 — "Seam-allowlist shrink over-stated": corrected to `landingshed` alone becoming droppable, with `shedadapters`/`websterengine` staying for `webster.go:9-13`.
- r2 — "\"Genuine weakening\" mischaracterises today's guard": reworded as a newly added orphan-check half carrying a named three-engine allowance, keeping the write-it-down requirement.
- r2 — "\"Refusal test passes unchanged\" is false": named `TestVerbRefusals` (`cli_test.go:106-148`, `loomshed.Deps` fixture at line 128) and stated its assertions stay while its fixture is repointed.

## Pushed Back

(none — all six findings verified accurate against the cited code before fixing)
