MILL_REVIEW_BEGIN
# Review: Loom persists done only after post-run friction reflection — holistic

```yaml
duration_s: 38.5
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-26
```

## Findings

None.

Verified against source:
- `internal/loomshed/frictionreflect.go`: nil-closure refusal, `entryErr` first, single closure call, always `Done`, no post-call `cancelErr` — matches card 1.
- `contracts/recipes/loom-recipe.yaml`: `Friction-Reflect` is the sole terminal, `Finalize.on_done` points at it, and the empty-`on_done` comment moved onto the new row.
- `internal/loomcli/run.go`, `arm.go` and `cli.go`: `armedVerb` is set first in `armAt`, and `reflectFrictionRow` gates on `frictionDir` and `armedVerb == "run"`, never the seed driver, so the Driver Choice Single-Site Invariant holds.
- `reflectFriction(wait)` blocks with `AcquireWriteLock` on the row path and keeps `TryAcquireWriteLock` on the blocked path.
- `loomPostRun` reports `rowFrictionStatus` on `RunDone` and reflects only on `RunBlocked`.
- `internal/shedrecipe/recipe.go` and `entries_simple.go`: the `Env.ReflectFriction` closure keeps friction, lock and loomengine imports out of the Told-Geometry-bound packages.
- No edits to `shedengine`, `shedverbs`, `battenshed` or `battencli`, per the row-shaped-fix Decision.
- Stale "fourteen"/"sixteen"/"last row" text is gone from source, yaml and docs; the remaining grep hits are plan and review artefacts plus the deliberate illustrative example in `plugins/scribe/skills/prose/SKILL.md`.
- The ly-drive "fifteen rows, thirty-six steps, cap 40" edit is present.
- The lock-timing comments in `bootstrap.go`, `start.go` and `loomengine/config.go` are narrowed to the blocked path.

## Verdict

APPROVE
Both batches are realised as planned, the cross-batch `Env.ReflectFriction` contract composes, and no constraint violation was found.
MILL_REVIEW_END
