# Plan Fix — round 3

Source review: `_mill/reviews/20260801-120923-plan-review-r3.md`

## Fixed

- **[BLOCKING] `go/token` import collides with local `token` var** — verified accurate: the existing walk callback declares `token, bad := firstBannedToken(data)`, and Card 4 also imports `go/token` and calls `token.NewFileSet()` in the same scope, which would shadow the package and fail to compile. Fixed Card 4 (`01-tierpurity-guard-generalization.md`) to explicitly rename the local variable to `bannedTok` (and its one other use in the `failures` append's `fmt.Sprintf` call), leaving `token` free to refer to the `go/token` package.
- **[NIT] `allowedLongSleepers` rationale misattributes safety to `refuseCLIReexec`** — verified accurate: `internal/reedengine/testmain_test.go` never imports `lyxtest` at all, and `internal/reedcli/testmain_test.go`'s sleep-loop branch is an infinite `for` loop that never reaches its own later `lyxtest.HermeticGitEnv()` call. Reworded Card 3's map-value description to cite each file's own `TestMain` intercept (`os.Args[1] == "reed"`) as the actual safety mechanism, and to explicitly note `HermeticGitEnv()` plays no part in the sleep-loop path's safety for `reedcli`.
- **[NIT] CONSTRAINTS.md bullet under-describes the guard's unresolvable-flag scope** — verified accurate against Card 3's own conservative-flag design. Added a clause to Card 5's "Real-time-wait guard (additive)" bullet text noting an unresolvable/non-constant argument shape is also conservatively flagged.
- **[NIT] `sleepDurationAtLeastOneSecond`'s ParseFloat failure path unspecified** — verified accurate: Card 3's case (b) description named only the "does not resolve to a known time.* scale" branch as `unresolvable`. Added an explicit clause: a `strconv.ParseFloat` error also sets `unresolvable = true`.

## Pushed Back

None.
