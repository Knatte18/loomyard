# Plan fix — round 2

Source review: `_mill/reviews/20260906-105651-plan-review-r2.md`

## Fixed

- r2 "[BLOCKING:design] Informational findings still bounce Plan-Validate/Plan-Revalidate" — severity now governs the verdict at every gate. Card 23 replaces `loomshed/planvalidate.go`'s `if len(findings) > 0` predicate with a blocking-only one, still logging on any non-empty set so an informational finding stays visible on the pass path; card 24 applies the same rule to `validate-plan` and `webster validate`, carrying informational findings under their own success-envelope key; the blocking-policy Shared Decision now states the rule once for all three boundaries and records why it is load-bearing (`create-new-unit` fires on every new package, including this task's own two, and is not something Plan-Write can fix). Card 25 gains a parity fixture for the informational-only cell in both modes, which is what proves the two sides read severity identically.
- r2 "[NIT:consistency] Batch 7 scope narrative claims DoneChecks shares the DeltaGit delta" — the Batch Scope sentence now says the single delta serves handle binding, the scope guard and drift detection, while the done-checks run their own batched `Resolve` within the same `record-batch` invocation. The card signatures were already correct; only the batch-level rationale was imprecise.

## Pushed Back

_None. Both findings verified accurate against the cited source and neither met a harm-check escape._
